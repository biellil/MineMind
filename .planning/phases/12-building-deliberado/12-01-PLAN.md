---
phase: 12-building-deliberado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/skills/blueprints.ts
  - src/skills/blueprints.test.ts
  - src/skills/builder.ts
  - src/skills/builder.test.ts
  - src/config.ts
autonomous: true
requirements: [BUILD-02, BUILD-03]
must_haves:
  truths:
    - "Um blueprint determinístico de abrigo fecha TODOS os 6 vizinhos da célula do bot (sem buraco)"
    - "O builder re-roda um blueprint pulando posições já preenchidas (idempotência) e retorna success só com cobertura total"
    - "Uma lista de blocos ruim (sem face alcançável) degrada para partial/no_effect — nunca lança nem soterra o bot"
    - "Cada bloco é um ponto de cedência: AbortSignal abortado entre blocos para o loop antes do próximo place"
    - "O retry idempotente (placeRetries) re-tenta um bloco até N vezes, sempre checando isFilled antes (nunca recoloca)"
  artifacts:
    - path: "src/skills/blueprints.ts"
      provides: "Geradores puros {tipo,dims,origin} -> {pos,bloco}[] para shelter/wall/tower"
      contains: "export function genShelter"
    - path: "src/skills/builder.ts"
      provides: "runBlueprint: loop ordenado sobre placeBlockSafe com retry + grounding por cobertura"
      contains: "export async function runBlueprint"
    - path: "src/skills/builder.test.ts"
      provides: "Cobertura inline mock-bot: idempotência, partial, abort entre blocos, retry"
      contains: "import { test, expect } from 'bun:test'"
    - path: "src/config.ts"
      provides: "placeRetries default 2 + BUILD_* thresholds (dims default, delay entre blocos)"
      contains: "buildBlockDelayMeanMs"
  key_links:
    - from: "src/skills/builder.ts"
      to: "src/skills/placeBlock.ts"
      via: "import getRefAndFace + placeBlockSafe + isFilled"
      pattern: "from './placeBlock'"
    - from: "src/skills/builder.ts"
      to: "src/skills/executor.ts"
      via: "gaussianDelay entre blocos (pacing anti-cheat D-16)"
      pattern: "gaussianDelay"
    - from: "src/skills/blueprints.ts"
      to: "src/skills/shelter.ts"
      via: "reusa PLACEABLE / mecânica de vedação (D-09)"
      pattern: "genShelter"
---

<objective>
Implementar o NÚCLEO determinístico e puro da Fase 12 (sem tocar no loop cognitivo): os geradores de blueprint (`blueprints.ts`) e o builder genérico idempotente (`builder.ts`) que executa qualquer blueprint num skill-run sobre o primitivo robusto `placeBlockSafe` da Fase 9.

Esta fase é ORQUESTRAÇÃO sobre primitivos já provados (RESEARCH). As três únicas peças genuinamente novas estão TODAS neste plano: (1) o corpo do retry idempotente que liga o `config.placeRetries` reservado (D-02), (2) o passo de ordenação determinística que preserva reach e não auto-soterra (D-05), (3) os geradores determinísticos `{tipo,dims,origin} → {pos,bloco}[]` (D-07/D-09).

Purpose: SC1 (abrigo que "fecha de verdade", validado por blockAt) e SC2 (estruturas simples) ficam DETERMINÍSTICOS e independentes do LLM acertar coordenadas. A integração no loop (Plans 02/03) é trivial depois que este núcleo está testado.
Output: `src/skills/blueprints.ts`, `src/skills/builder.ts`, colocated tests, e novos campos de config. NÃO registra a skill no índice nem rota no loop (Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-building-deliberado/12-CONTEXT.md
@.planning/phases/12-building-deliberado/12-RESEARCH.md

<interfaces>
<!-- Contratos extraídos do código-fonte. O executor usa estes DIRETAMENTE — sem exploração. -->

From src/skills/placeBlock.ts (REUSE — não reimplementar):
```typescript
// PURO: escolhe vizinho sólido + faceVector; retorna null se nenhuma face alcançável (a rede de segurança D-04).
export function getRefAndFace(bot: Bot, target: { x:number; y:number; z:number }): { ref: Block; face: Vec3 } | null

// Equipa o item, coloca, e DERIVA outcome de bot.blockAt(targetPos) — engole QUALQUER throw (timeout = falso-negativo).
export async function placeBlockSafe(bot: Bot, ref: Block, faceVector: Vec3, blockItem: Item, targetPos: {x:number;y:number;z:number}): Promise<SkillResult>

// isFilled é PRIVADO em placeBlock.ts (linha 51). O builder PRECISA dele → este plano EXPORTA isFilled de placeBlock.ts.
// function isFilled(bot, pos): boolean  →  return b != null && b.name !== 'air' && b.name !== 'cave_air'
```

From src/grounding/types.ts:
```typescript
export type SkillOutcome = 'success' | 'partial' | 'no_effect' | 'error'
export interface SkillResult {
  readonly outcome: SkillOutcome
  readonly observed: number      // cobertura real (blocos sólidos confirmados)
  readonly expected: number      // total de blocos do blueprint
  readonly delta: Readonly<Record<string, number>>  // vazio aqui (irrelevante)
  readonly reason?: string
}
```

From src/skills/executor.ts (REUSE):
```typescript
export function gaussianDelay(meanMs: number, stdDevMs: number): number   // Box-Muller, sempre >= 0
export async function executeWithSafety<T>(action: () => Promise<T>, opts: { timeoutMs?: number; signal?: AbortSignal }): Promise<T>
```

From src/skills/shelter.ts (REUSE a lista de materiais — NÃO modificar shelter.ts neste plano):
```typescript
const PLACEABLE = /_(planks|log)$|^(cobblestone|stone|dirt|netherrack|deepslate)$/   // privado — duplicar a regex em builder/blueprints OU exportá-la
const UNSAFE_BELOW = new Set(['air','cave_air','void_air','lava','water'])           // guarda anti-lava (privado)
```

From src/config.ts (campos a ADICIONAR/MODIFICAR):
```typescript
placeRetries: parseInt(process.env.PLACE_RETRIES || '0', 10)  // ← FLIP default '0' → '2' (D-02 liga o corpo)
// validação já existe em config.ts:346: if (config.placeRetries < 0) throw ...
```

Vec3-like sem import em runtime (Pitfall 7 — reusar o padrão makeVec):
```typescript
function makeVec(x: number, y: number, z: number): Vec3 { return { x, y, z } as unknown as Vec3 }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Geradores de blueprint puros (blueprints.ts) — shelter full-seal + wall + tower</name>
  <files>src/skills/blueprints.ts, src/skills/blueprints.test.ts</files>
  <read_first>
    - src/skills/blueprints.ts (NÃO existe ainda — criar; ler para confirmar ausência)
    - src/skills/shelter.ts (mecânica cavar-e-tampar / pilar 1×1, PLACEABLE, UNSAFE_BELOW — D-09 ESTENDE isto; shelter.ts permanece INTACTO)
    - src/grounding/types.ts (SkillResult — os geradores NÃO retornam SkillResult, retornam {pos,bloco}[]; ler só para entender o ecossistema)
    - .planning/phases/12-building-deliberado/12-RESEARCH.md §"Pattern 4" + §"Pitfall 1" (geometria do teto que auto-soterra)
  </read_first>
  <behavior>
    - genShelter({dims:{w:3,h:3,d:3}, origin:{x:0,y:64,z:0}}) → retorna lista que, ao ser preenchida, fecha os 6 vizinhos da célula central (chão + 4 paredes + teto), interior oco. Inclui o bloco do TETO (origin.y + altura do bot) e os blocos a altura de cabeça.
    - genShelter NÃO inclui na lista a célula ocupada pelo corpo do bot (pés + cabeça) como alvo de colocação — só a CASCA. (O builder coloca a casca; o bot fica dentro.)
    - genWall({dims:{w:5,h:3,d:1}, origin}) → plano w×h ao longo de um eixo (5 blocos de largura, 3 de altura) = 15 posições.
    - genTower({dims:{w:1,h:4,d:1}, origin}) → coluna vertical de h blocos a partir de origin para cima = 4 posições.
    - Cada item da lista tem forma { pos:{x,y,z}, bloco:string }. `bloco` default = 'cobblestone' (ou spec.bloco se fornecido).
    - Determinismo: mesmo input → mesma lista (mesma ordem, mesmas coords). Sem Math.random, sem Date.now.
    - dims inválidas (w/h/d <= 0) → retorna [] (lista vazia, nunca lança).
  </behavior>
  <action>
    Criar `src/skills/blueprints.ts`. Definir tipos e geradores PUROS (sem bot, sem I/O):

    ```typescript
    export type BuildKind = 'shelter' | 'wall' | 'tower'
    export interface BuildDims { w: number; h: number; d: number }
    export interface BuildSpec {
      tipo: BuildKind
      dims: BuildDims
      origin: { x: number; y: number; z: number }
      bloco?: string   // default 'cobblestone'
    }
    export interface BlueprintBlock { pos: { x: number; y: number; z: number }; bloco: string }
    ```

    Implementar três geradores `(spec: BuildSpec) => BlueprintBlock[]`:

    1. **genShelter(spec)** (D-09 — vedação total estendendo a mecânica da Fase 8): tratar `origin` como o canto MÍNIMO (x,y,z) da caixa w×h×d. Gerar a CASCA oca: para cada célula (dx in 0..w-1, dy in 0..h-1, dz in 0..d-1), incluir a posição APENAS se ela está na borda (dx===0 || dx===w-1 || dy===0 || dy===h-1 || dz===0 || dz===d-1). Isso produz chão + 4 paredes + teto, interior oco. O `bloco` = `spec.bloco ?? 'cobblestone'`. Defaults de dims se não vier completas: usar `config.buildShelterDims` (ver Task 3). IMPORTANTE (Pitfall 1): NÃO excluir o bloco do teto — ele É parte da casca; o builder (Task 2) resolve o reach do teto via getRefAndFace e ordenação. A célula INTERNA (oca) é onde o bot fica — nunca aparece na lista pois não é borda.

    2. **genWall(spec)**: plano vertical w×h. Para o eixo: se w >= d, varia x em 0..w-1 e y em 0..h-1 com z fixo em origin.z; senão varia z em 0..d-1 e y em 0..h-1 com x fixo. Cada (i, j) → pos. `bloco` default 'cobblestone'.

    3. **genTower(spec)**: coluna n×n (n = max(1,w)) por h de altura. Para dy in 0..h-1, para dx in 0..n-1, dz in 0..n-1 → pos em (origin.x+dx, origin.y+dy, origin.z+dz). Default n=1 (coluna 1×1).

    Guarda: cada gerador, se `dims.w<=0 || dims.h<=0 || dims.d<=0`, retorna `[]`.

    Exportar uma fachada `generateBlueprint(spec: BuildSpec): BlueprintBlock[]` que faz switch em `spec.tipo` e delega ao gerador. Default (tipo desconhecido) → `[]`.

    NÃO importar `config` no topo se isso criar ciclo — receber dims já resolvidas via spec (a resolução de defaults a partir de config acontece na skill `build`, não nos geradores puros). Os geradores são puros; defaults de dims entram pela spec montada pelo chamador.
  </action>
  <verify>
    <automated>bun test src/skills/blueprints.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function genShelter" src/skills/blueprints.ts` → presente
    - `grep -q "export function genWall" src/skills/blueprints.ts` → presente
    - `grep -q "export function genTower" src/skills/blueprints.ts` → presente
    - `grep -q "export function generateBlueprint" src/skills/blueprints.ts` → presente
    - `grep -q "export interface BuildSpec" src/skills/blueprints.ts` → presente
    - blueprints.test.ts contém um teste que afirma: genShelter com w=h=d=3 produz uma lista cujo conjunto de posições inclui o teto (y === origin.y + 2) E exclui a célula interna central (origin.x+1, origin.y+1, origin.z+1)
    - blueprints.test.ts contém teste de determinismo (duas chamadas com mesmo spec → arrays iguais por JSON.stringify)
    - blueprints.test.ts contém teste de dims inválidas (w=0) → []
    - `bun test src/skills/blueprints.test.ts` passa (todos verdes)
    - shelter.ts NÃO foi modificado: `git diff --quiet src/skills/shelter.ts` (exit 0)
  </acceptance_criteria>
  <done>Geradores puros criados e testados; genShelter fecha os 6 lados (casca oca); determinismo e guarda de dims inválidas verdes; shelter.ts intacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Builder genérico idempotente (builder.ts) — loop ordenado + retry + grounding por cobertura</name>
  <files>src/skills/builder.ts, src/skills/builder.test.ts, src/skills/placeBlock.ts</files>
  <read_first>
    - src/skills/placeBlock.ts (getRefAndFace/placeBlockSafe/isFilled — o builder é um LOOP sobre eles; isFilled é privado e PRECISA ser exportado aqui)
    - src/skills/station.ts (padrão __stationDeps de injeção de dependência — o builder expõe __builderDeps idêntico; ADJACENT_OFFSETS; pathfinder.goto com GoalNear + abort listener)
    - src/skills/executor.ts (gaussianDelay; executeWithSafety com {timeoutMs, signal})
    - src/skills/shelter.ts (PLACEABLE regex p/ seleção de material; padrão makeVec)
    - src/skills/blueprints.ts (BlueprintBlock, BuildSpec, generateBlueprint — criados na Task 1)
    - src/skills/shelter.test.ts §L1-70 (convenção de mock-bot inline + vec() com offset — replicar)
    - .planning/phases/12-building-deliberado/12-RESEARCH.md §"Pattern 1/2/3" + §"Pitfall 1/2"
  </read_first>
  <behavior>
    - runBlueprint(bot, [], signal) → outcome 'no_effect', observed 0, expected 0 (lista vazia).
    - runBlueprint sobre 3 posições, todas colocáveis (placeBlockSafe stub retorna success e marca isFilled) → outcome 'success', observed 3, expected 3.
    - runBlueprint sobre 3 posições onde 1 não tem face alcançável (getRefAndFace stub → null) → pula essa, outcome 'partial', observed 2, expected 3 — NÃO lança.
    - Idempotência: posição já preenchida (isFilled true antes) é pulada sem chamar placeBlockSafe; conta como coberta.
    - Abort entre blocos: signal.aborted=true após o 1º bloco → o loop PARA antes do 2º place (placeBlockSafe NÃO é chamado para os restantes); outcome reflete só o que foi colocado.
    - Retry idempotente (D-02): placeOneWithRetry checa isFilled ANTES de cada tentativa; se já preenchido, retorna true sem recolocar; re-resolve getRefAndFace a cada tentativa; até config.placeRetries+1 tentativas.
    - Grounding por cobertura REAL (D-03/D-10): outcome derivado de contar quantas posições do blueprint estão isFilled DEPOIS do loop — NUNCA da resolução das Promises.
  </behavior>
  <action>
    Primeiro, em `src/skills/placeBlock.ts`: trocar `function isFilled` por `export function isFilled` (a única mudança neste arquivo — o builder e o blueprint precisam da verdade-do-mundo única). Atualizar o import interno se necessário (continua usado localmente).

    Criar `src/skills/builder.ts`:

    1. **Seam de injeção** (espelhar `__stationDeps` de station.ts:23):
    ```typescript
    import { placeBlockSafe as realPlaceBlockSafe, getRefAndFace as realGetRefAndFace, isFilled as realIsFilled } from './placeBlock'
    export const __builderDeps = {
      placeBlockSafe: realPlaceBlockSafe,
      getRefAndFace: realGetRefAndFace,
      isFilled: realIsFilled,
    }
    ```

    2. **selectMaterial(bot, bloco)** — resolve o `Item` concreto do inventário. Prioridade: (a) item cujo nome === `bloco`; (b) fallback heurística "preferir descartáveis": primeiro item que casa `/^(cobblestone|dirt|cobbled_deepslate|netherrack)$/`, depois qualquer item que case a regex PLACEABLE (`/_(planks|log)$|^(cobblestone|stone|dirt|netherrack|deepslate)$/`, duplicada de shelter.ts). Retorna null se nenhum. (D-? Claude's discretion: preferir cobblestone/dirt sobre úteis.)

    3. **orderForReach(blueprint, bot)** (D-05, RESEARCH Pattern 2): ordenar baixo→cima (y crescente), fora→dentro, e a célula do bot (pés `by` + cabeça `by+1` na coluna `bx,bz`) por ÚLTIMO. Algoritmo exato conforme RESEARCH §Pattern 2 (sort estável com isBotCell → last, depois y, depois distância horizontal do centro decrescente). Computar `bx,by,bz` via Math.floor da posição do bot.

    4. **placeOneWithRetry(bot, pos, item)** (D-02, RESEARCH Pattern 3):
    ```typescript
    for (let attempt = 0; attempt <= config.placeRetries; attempt++) {
      if (__builderDeps.isFilled(bot, pos)) return true        // race do blockUpdate → já lá
      const rf = __builderDeps.getRefAndFace(bot, pos)
      if (!rf) return false                                     // sem face → pula (D-04, não throw)
      try { await bot.lookAt(makeVec(pos.x + 0.5, pos.y + 0.5, pos.z + 0.5)) } catch { /* lookAt best-effort */ }
      const r = await __builderDeps.placeBlockSafe(bot, rf.ref, rf.face, item, pos)
      if (r.outcome === 'success') return true
    }
    return __builderDeps.isFilled(bot, pos)                      // verdade-do-mundo final
    ```
    Usar `makeVec(x,y,z) => ({x,y,z} as unknown as Vec3)` local (Pitfall 7 — NÃO importar Vec3 em runtime).

    5. **runBlueprint(bot, blueprint, signal?)** (D-01, RESEARCH Pattern 1):
    ```typescript
    export async function runBlueprint(bot, blueprint, signal?): Promise<SkillResult> {
      const total = blueprint.length
      if (total === 0) return { outcome: 'no_effect', observed: 0, expected: 0, delta: {} }
      const ordered = orderForReach(blueprint, bot)
      for (const { pos, bloco } of ordered) {
        if (signal?.aborted) break                               // D-15: cede a lifeCritical ENTRE blocos
        if (__builderDeps.isFilled(bot, pos)) continue           // D-03 idempotência
        const item = selectMaterial(bot, bloco)
        if (!item) continue                                       // sem material → pula (degrada, não lança)
        await placeOneWithRetry(bot, pos, item)                   // D-02
        await new Promise<void>(r => setTimeout(r, gaussianDelay(config.buildBlockDelayMeanMs, config.buildBlockDelayStdMs)))  // D-01/D-16 pacing
      }
      // D-03/D-10: outcome por COBERTURA real (blockAt), nunca por Promise
      const covered = blueprint.filter(b => __builderDeps.isFilled(bot, b.pos)).length
      const outcome: SkillOutcome = covered >= total ? 'success' : covered > 0 ? 'partial' : 'no_effect'
      return { outcome, observed: covered, expected: total, delta: {} }
    }
    ```

    6. **Skill `build` + schema + tool** (a skill que o Plan 02 registra):
    ```typescript
    export const BuildSchema = z.object({
      tipo: z.enum(['shelter','wall','tower','station','custom']),
      dims: z.object({ w: z.number(), h: z.number(), d: z.number() }).optional(),
      origin: z.object({ x: z.number(), y: z.number(), z: z.number() }).optional(),
      bloco: z.string().max(64).optional(),
      blocks: z.array(z.object({ pos: z.object({x:z.number(),y:z.number(),z:z.number()}), bloco: z.string() })).optional(), // caminho ad-hoc D-08
    })
    export async function build(bot, rawParams): Promise<SkillResult> { ... }
    export const buildTool = { name: 'build', description: '...', schema: BuildSchema, execute: build } as const
    ```
    A skill `build`:
    - Extrai `signal` antes do Zod (padrão dig.ts/placeBlock.ts).
    - `tipo === 'station'` → chama `ensureStation(bot, ...)` via import e retorna SkillResult derivado (Block != null → success; null → no_effect). NÃO usa runBlueprint para station (reusa o helper provado da Fase 9).
    - `tipo === 'custom'` → usa `params.blocks` cru (D-08 ad-hoc, coords absolutas) direto no runBlueprint.
    - shelter/wall/tower → monta `BuildSpec` com `origin = origin ?? floor(bot.entity.position)` (Open Question 3: origin = posição do bot no momento da execução) e `dims = dims ?? config.build<Tipo>Dims`, chama `generateBlueprint(spec)` → runBlueprint.
    - Embrulhar runBlueprint em `executeWithSafety(() => ..., { timeoutMs: config.buildTimeoutMs, signal })` para herdar timeout/abort (D-16). SEMPRE resolve com SkillResult (D-12 — nunca lança como fluxo; envolver em try/catch que degrada para error).

    Criar `src/skills/builder.test.ts` seguindo a convenção shelter.test.ts (mock-bot inline com `vec()` que tem `.offset`, `blockAt`, `inventory.items()`, `lookAt`, `equip`). Sobrescrever `__builderDeps.placeBlockSafe`/`getRefAndFace`/`isFilled` para testar SEM mock.module. Cobrir os 7 casos de `<behavior>`.
  </action>
  <verify>
    <automated>bun test src/skills/builder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function isFilled" src/skills/placeBlock.ts` → isFilled agora exportado
    - `grep -q "export async function runBlueprint" src/skills/builder.ts` → presente
    - `grep -q "export const __builderDeps" src/skills/builder.ts` → seam de injeção presente
    - `grep -q "export async function build" src/skills/builder.ts` → skill presente
    - `grep -q "export const buildTool" src/skills/builder.ts` → tool descriptor presente
    - `grep -q "export const BuildSchema" src/skills/builder.ts` → schema Zod presente
    - `grep -q "config.placeRetries" src/skills/builder.ts` → o retry consome o config reservado
    - `grep -q "gaussianDelay" src/skills/builder.ts` → pacing entre blocos presente
    - builder.test.ts cobre: lista vazia (no_effect), 3-de-3 (success), face-nula-pula (partial 2/3 sem throw), idempotência (posição preenchida não chama placeBlockSafe), abort-entre-blocos (placeBlockSafe não chamado após abort), retry (re-tenta e converge)
    - `bun test src/skills/builder.test.ts` passa (todos verdes)
    - shelter.ts NÃO modificado: `git diff --quiet src/skills/shelter.ts` (exit 0)
    - nodes.ts NÃO modificado neste plano: `git diff --quiet src/cognition/nodes.ts` (exit 0)
  </acceptance_criteria>
  <done>runBlueprint testado: idempotência, partial sem throw, abort entre blocos, retry idempotente, grounding por cobertura real. Skill `build` resolve shelter/wall/tower/station/custom. isFilled exportado. shelter.ts e nodes.ts intactos.</done>
</task>

<task type="auto">
  <name>Task 3: Config — ligar placeRetries + thresholds de building (dims default, delay, timeout)</name>
  <files>src/config.ts</files>
  <read_first>
    - src/config.ts §L32-36 (placeTimeoutMs/placeRetries/gatherSearchRadius — bloco da Fase 9) + §L344-352 (validações da Fase 9/10)
    - src/skills/builder.ts (quais campos config.build* o builder consome — criados na Task 2)
    - .planning/phases/12-building-deliberado/12-RESEARCH.md §"Runtime State Inventory" (PLACE_RETRIES flip 0→2-3; novos BUILD_* keys)
  </read_first>
  <action>
    Em `src/config.ts`, no objeto `config`:

    1. **Ligar placeRetries (D-02):** trocar o default da linha 34 de `'0'` para `'2'`:
    ```typescript
    placeRetries: parseInt(process.env.PLACE_RETRIES || '2', 10),  // D-02 Fase 12: corpo do retry LIGADO (builder.ts)
    ```
    Atualizar o comentário da linha 34 para refletir que o corpo do retry agora EXISTE (Fase 12). A validação `if (config.placeRetries < 0)` (linha 346) já cobre — não mudar.

    2. **Adicionar campos de building** (novo bloco `// === Fase 12: Building Deliberado ===` após o bloco da Fase 10):
    ```typescript
    // Delay gaussiano (ms) entre colocar cada bloco — pacing anti-cheat D-01/D-16 (rajada é flagável).
    buildBlockDelayMeanMs: parseInt(process.env.BUILD_BLOCK_DELAY_MEAN_MS || '350', 10),
    buildBlockDelayStdMs: parseInt(process.env.BUILD_BLOCK_DELAY_STD_MS || '120', 10),
    // Teto total (ms) de um skill-run de build inteiro (executeWithSafety) — generoso (muitos blocos).
    buildTimeoutMs: parseInt(process.env.BUILD_TIMEOUT_MS || '120000', 10),
    // Dims default por estrutura quando o caminho autônomo não fornece (Open Question 3: dims default + origin=bot).
    buildShelterDims: { w: 3, h: 3, d: 3 },   // abrigo 3x3x3 oco — fecha os 6 lados ao redor do bot
    buildWallDims: { w: 5, h: 3, d: 1 },      // parede 5 de largura x 3 de altura
    buildTowerDims: { w: 1, h: 4, d: 1 },     // torre 1x1 x 4 de altura
    ```

    3. **Adicionar validações** (junto às validações da Fase 9, após linha 348):
    ```typescript
    // Fase 12: validação dos parâmetros de building
    if (config.buildBlockDelayMeanMs < 0) throw new Error(`BUILD_BLOCK_DELAY_MEAN_MS inválido: ${config.buildBlockDelayMeanMs}. Deve ser >= 0.`)
    if (config.buildBlockDelayStdMs < 0) throw new Error(`BUILD_BLOCK_DELAY_STD_MS inválido: ${config.buildBlockDelayStdMs}. Deve ser >= 0.`)
    if (config.buildTimeoutMs < 1) throw new Error(`BUILD_TIMEOUT_MS inválido: ${config.buildTimeoutMs}. Deve ser >= 1.`)
    ```
  </action>
  <verify>
    <automated>bun test src/skills/builder.test.ts && bun -e "import('./src/config.ts').then(m => { if (m.config.placeRetries < 2) throw new Error('placeRetries não ligado'); if (!m.config.buildShelterDims) throw new Error('buildShelterDims ausente'); console.log('config OK') })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "PLACE_RETRIES || '2'" src/config.ts` → default ligado para 2
    - `grep -q "buildBlockDelayMeanMs" src/config.ts` → presente
    - `grep -q "buildBlockDelayStdMs" src/config.ts` → presente
    - `grep -q "buildTimeoutMs" src/config.ts` → presente
    - `grep -q "buildShelterDims" src/config.ts` → presente
    - `grep -q "buildWallDims" src/config.ts` → presente
    - `grep -q "buildTowerDims" src/config.ts` → presente
    - `grep -q "BUILD_BLOCK_DELAY_MEAN_MS inválido" src/config.ts` → validação presente
    - `grep -q "BUILD_TIMEOUT_MS inválido" src/config.ts` → validação presente
    - O comando de verify imprime "config OK" (placeRetries >= 2 e buildShelterDims definido)
    - `bun test src/skills/builder.test.ts` continua verde (builder lê os novos campos sem erro)
  </acceptance_criteria>
  <done>placeRetries ligado (default 2); buildBlockDelayMeanMs/StdMs/buildTimeoutMs e dims default por estrutura adicionados com validações; builder consome sem erro.</done>
</task>

</tasks>

<verification>
- `bun test src/skills/blueprints.test.ts src/skills/builder.test.ts` — todos verdes (núcleo determinístico provado)
- `git diff --quiet src/skills/shelter.ts` — exit 0 (reflexo da Fase 8 INTACTO, D-11)
- `git diff --quiet src/cognition/nodes.ts` — exit 0 (loop não tocado neste plano; D-14)
- `bun build src/skills/builder.ts src/skills/blueprints.ts --target node 2>&1 | grep -i error` — sem erros de tipo (ou usar `bunx tsc --noEmit` se configurado)
</verification>

<success_criteria>
- genShelter produz uma casca oca que fecha os 6 vizinhos da célula do bot (base determinística do SC1 "fecha de verdade")
- runBlueprint: idempotente (pula preenchidos), grounded por cobertura real (success só com cobertura total, senão partial), cede a abort entre blocos (preemptável), retry idempotente ligado, NUNCA lança nem soterra (rede de segurança D-04)
- config.placeRetries ligado + thresholds de building com validação
- shelter.ts (Fase 8) e nodes.ts (Fase 9 G-01) byte-for-byte intactos
</success_criteria>

<output>
Após completar, criar `.planning/phases/12-building-deliberado/12-01-SUMMARY.md` documentando: assinaturas finais de runBlueprint/build/generateBlueprint, o algoritmo de ordenação escolhido (D-05), o valor de placeRetries, e o handoff para Plan 02 (skill `build` pronta para registro) e Plan 03 (build:shelter precisa de origin=posição do bot — confirmado no skill).
</output>
