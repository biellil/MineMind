---
phase: 12-building-deliberado
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - src/cognition/nodes.ts
  - src/chat/conversation.ts
  - src/chat/conversation.test.ts
  - src/cognition/nodes.shelter-bridge.test.ts
autonomous: true
requirements: [BUILD-02, BUILD-03]
must_haves:
  truths:
    - "À noite e sem teto sólido acima, o agente gera autonomamente um goal build:shelter (need de abrigo → goal)"
    - "Um pedido de jogador 'constrói um abrigo/parede/torre' vira um goal build:<sub> em ASSISTANT (não fica só na conversa)"
    - "O abrigo deliberado NÃO preempta o reflexo de emergência da Fase 8 (precedência mantida, D-15)"
    - "A ponte de abrigo é isolada no observe node — não muda o contrato stub de shelter/social no módulo motivation (D-12 via bridge)"
  artifacts:
    - path: "src/cognition/nodes.ts"
      provides: "Ponte observe: noite + exposto → holder.currentGoal = build:shelter"
      contains: "build:shelter"
    - path: "src/chat/conversation.ts"
      provides: "kind 'build' em SUPPORTED_REQUEST_KINDS + detectRequestKind keywords + makePlayerRequestGoal build:<sub>"
      contains: "'build'"
  key_links:
    - from: "src/cognition/nodes.ts (observe)"
      to: "holder.currentGoal (build:shelter)"
      via: "bridge noite+exposto define o goal que o roteador build:* do Plan 02 consome"
      pattern: "build:shelter"
    - from: "src/chat/conversation.ts"
      to: "holder.goals (build:<sub>)"
      via: "detectRequestKind('build') → makePlayerRequestGoal emite goal id build:<sub>"
      pattern: "build:"
---

<objective>
Ativar o building deliberado AUTONOMAMENTE (D-12): (1) uma ponte no observe node que, à noite e com o bot exposto (sem teto sólido), promove um goal `build:shelter` — sem alterar o contrato stub de `shelter`/`social` no módulo motivation (Open Question 1 da RESEARCH → bridge isolado, menor blast radius); (2) estender o canal de pedido de jogador para o kind `build`, de modo que "constrói um abrigo/parede/torre" vire um goal `build:<sub>` (Pitfall 6 — `SUPPORTED_REQUEST_KINDS` é fechado hoje).

Esses goals `build:*` são consumidos pelo roteador determinístico criado no Plan 02. O reflexo de emergência da Fase 8 (`shelter.ts`, sub-segundo, lifeCritical) mantém precedência e NÃO é tocado (D-11/D-15); o abrigo deliberado é PROATIVO e cede a vida-crítica via a preempção generalizada já existente.

Purpose: fecha SC1 (abrigo funcional via estado building real, ativado por need) e SC2 (estrutura autônoma sob pedido do jogador).
Output: ponte de abrigo no observe + kind 'build' no canal conversacional, ambos testados. NÃO toca shelter.ts, needs.ts, goals.ts, types.ts (contrato stub preservado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-building-deliberado/12-CONTEXT.md
@.planning/phases/12-building-deliberado/12-RESEARCH.md
@.planning/phases/12-building-deliberado/12-02-SUMMARY.md

<interfaces>
<!-- Contratos extraídos do código-fonte — usar DIRETAMENTE. -->

From src/cognition/nodes.ts (observe node — onde a ponte entra, espelhando a ponte Fase 10 nas linhas 200-257):
```typescript
// observe roda: evaluateNeeds → generateGoals → selectGoal → escreve holder.currentGoal.
// A ponte Fase 10 (linha 200) é o MOLDE: lê snapshot, condição determinística, sobrescreve holder.currentGoal/goals.
// holder.currentGoal: Goal | null;  holder.goals: Goal[];  holder.disposition: Disposition
// Goal = { id, kind, priority, progress, dependsOn: string[], source: 'need'|'player_request', committedAt }
// snapshot.status.position {x,y,z}; snapshot.status.isDay: boolean (noite = !isDay); snapshot.status.timeOfDay normalizado 0.0–1.0 (NÃO é tick).
```

From src/perception/types.ts (BotStatus — campo de tempo CONFIRMADO):
```typescript
// BotStatus.timeOfDay: number  // 0.0 (meia-noite) a 1.0; < 0.5 = dia — escala NORMALIZADA, NÃO é tick Minecraft
// BotStatus.isDay: boolean     // já pronto — usar `!snapshot.status.isDay` para "noite"
```

From src/chat/conversation.ts (canal de pedido — Pitfall 6):
```typescript
export const SUPPORTED_REQUEST_KINDS = ['gather', 'follow', 'navigate'] as const   // ← adicionar 'build'
function detectRequestKind(message: string): SupportedRequestKind | null { /* regex pt/en por kind — MÓDULO-PRIVADO hoje, precisa export */ }
function makePlayerRequestGoal(kind: SupportedRequestKind, now: number): Goal { /* MÓDULO-PRIVADO hoje */
  return { id: `player_request:${kind}:${now}`, kind, priority: 1, progress: 0, dependsOn: [], source: 'player_request', committedAt: now }
}
// handleConversation: em ASSISTANT, kind!==null && trust>=threshold → holder.playerRequestPending=true + holder.goals.push(makePlayerRequestGoal(kind, now))
```

From src/config.ts (thresholds de noite/exposição — adicionar se necessário, Claude's discretion):
```typescript
// Gatilho fino do need de abrigo (limiar de noite / sem-teto) é Claude's discretion (CONTEXT D-? Discretion).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ponte de abrigo no observe node — noite + exposto → goal build:shelter</name>
  <files>src/cognition/nodes.ts, src/cognition/nodes.shelter-bridge.test.ts</files>
  <read_first>
    - src/cognition/nodes.ts §L155-273 (observe node completo) e §L200-257 (ponte Fase 10 resources→DAG — o MOLDE exato a espelhar)
    - src/perception/types.ts §L61-68 (BotStatus — campo de noite CONFIRMADO: `isDay: boolean` e `timeOfDay` NORMALIZADO 0.0–1.0; usar `!snapshot.status.isDay` para noite, NUNCA threshold de tick contra o snapshot)
    - src/motivation/types.ts (STUB_NEEDS inclui 'shelter' — NÃO modificar; a ponte NÃO mexe no need stub, define o goal direto no holder)
    - .planning/phases/12-building-deliberado/12-RESEARCH.md §"Pitfall 5" (shelter need é STUB — usar a ponte do observe, não promover o need) + §"Open Questions 1" (recomendação: bridge)
    - .planning/phases/12-building-deliberado/12-02-SUMMARY.md (confirma que build:shelter já roteia para a skill build)
  </read_first>
  <action>
    Na observe node de `src/cognition/nodes.ts`, adicionar uma ponte determinística ESPELHANDO a ponte Fase 10 (linhas 200-257). Posicioná-la APÓS a ponte Fase 10 e ANTES do cálculo de `enteredIdle` (linha 262), de modo que `holder.currentGoal` final reflita a decisão de abrigo.

    **Gatilho (Claude's discretion sobre o fino, mas concreto aqui):**
    - "Noite": derivar do booleano JÁ PRONTO do snapshot — `const isNight = !snapshot.status.isDay`. CRÍTICO: `snapshot.status.timeOfDay` é NORMALIZADO 0.0–1.0 (NÃO é o tick Minecraft 0–24000), então NUNCA comparar o campo do snapshot contra 13000–23000 (seria sempre falso → a ponte nunca dispararia e SC1 falharia silenciosamente). O threshold de tick 13000–23000 só é válido para `bot.time.timeOfDay` (raw); a ponte NÃO precisa do bot para o sinal de noite — usa o snapshot.
    - "Exposto / sem teto": checar via `bot.blockAt` se NÃO há bloco sólido numa coluna de ~5 blocos acima da cabeça do bot. Concreto:
      ```typescript
      const bp = bot.entity.position
      let hasRoof = false
      for (let dy = 2; dy <= 6; dy++) {
        const b = bot.blockAt(bp.offset(0, dy, 0) as Parameters<typeof bot.blockAt>[0])
        if (b != null && b.name !== 'air' && b.name !== 'cave_air') { hasRoof = true; break }
      }
      const exposed = !hasRoof
      ```

    **Ação da ponte:**
    ```typescript
    // === Fase 12 D-12: Ponte de abrigo (noite + exposto → goal build:shelter). Bridge ISOLADO no
    // observe (Pitfall 5 / Open Question 1) — NÃO promove o need stub shelter; define o goal direto.
    // Noite vem do booleano pronto do snapshot (timeOfDay é normalizado 0.0–1.0, NÃO é tick):
    const isNight = !snapshot.status.isDay
    // Só ativa quando NÃO há survivalCritical em curso (o reflexo da Fase 8 mantém precedência, D-15)
    // e quando o currentGoal atual não é já um build:* (evita re-commit a cada tick).
    const currentIsBuild = holder.currentGoal?.id.startsWith('build:') ?? false
    if (isNight && exposed && !survivalCritical && !currentIsBuild) {
      const shelterGoal: Goal = {
        id: 'build:shelter',
        kind: 'shelter',
        priority: 0.9,          // alto, mas abaixo de player_request(1) e survivalCritical
        progress: 0,
        dependsOn: [],
        source: 'need',
        committedAt: t,
      }
      // não duplicar se já existe nos candidatos
      if (!holder.goals.some(g => g.id === 'build:shelter')) holder.goals.push(shelterGoal)
      holder.currentGoal = shelterGoal
      log('[build] noite + exposto → goal build:shelter (abrigo deliberado)')
    }
    // === Fim da ponte de abrigo Fase 12 ===
    ```
    Reusar a variável `survivalCritical` já calculada no observe (linha ~179). `t` é o `now()` já capturado. `Goal` já está importado no arquivo. `snapshot` é o WorldSnapshot já disponível no observe (mesma fonte que a ponte Fase 10 lê).

    **Retomada por idempotência (D-03):** não precisa de subsistema de pendência — se o build for interrompido por preempção (vida-crítica), no próximo tick em que ainda for noite + exposto a ponte re-seleciona `build:shelter`, e o builder (Plan 01) pula os blocos já colocados. Documentar isso no comentário.

    **Teste** (`src/cognition/nodes.shelter-bridge.test.ts`): como a ponte está embutida no observe (que precisa de bot+holder+provider), o teste mais barato e robusto é EXTRAIR a condição em um helper puro exportado e testá-lo. NOTE que o helper recebe `isNight` JÁ DERIVADO (`!snapshot.status.isDay`), não o campo bruto — isso mantém o teste alinhado à escala normalizada:
    ```typescript
    export function shouldBuildShelter(isNight: boolean, exposed: boolean, survivalCritical: boolean, currentGoalId: string | null): boolean {
      return isNight && exposed && !survivalCritical && !(currentGoalId?.startsWith('build:') ?? false)
    }
    ```
    Usar `shouldBuildShelter(!snapshot.status.isDay, exposed, survivalCritical, holder.currentGoal?.id ?? null)` dentro da ponte (substituir a condição inline pela chamada) e cobrir no teste: noite+exposto+seguro+sem-build → true; dia → false; com teto (exposed=false) → false; survivalCritical → false; já em build:* → false.
  </action>
  <verify>
    <automated>bun test src/cognition/nodes.shelter-bridge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function shouldBuildShelter" src/cognition/nodes.ts` → helper puro presente
    - `grep -q "!snapshot.status.isDay" src/cognition/nodes.ts` → noite derivada do booleano normalizado (NÃO de threshold de tick contra o snapshot)
    - `grep -q "build:shelter" src/cognition/nodes.ts` → ponte gera o goal
    - `grep -q "abrigo deliberado" src/cognition/nodes.ts` → log da ponte presente
    - NENHUM threshold de tick contra o snapshot: a ponte NÃO contém `snapshot.status.timeOfDay` comparado a 13000/23000 — se houver qualquer 13000–23000, deve estar escopado SOMENTE a `bot.time.timeOfDay` (raw), nunca ao snapshot
    - A ponte usa `survivalCritical` existente (precedência do reflexo Fase 8): `grep -q "survivalCritical" src/cognition/nodes.ts` (já existia; confirmar uso na condição)
    - shelter.ts NÃO modificado: `git diff --quiet src/skills/shelter.ts` (exit 0)
    - needs.ts/goals.ts/types.ts (motivation) NÃO modificados: `git diff --quiet src/motivation/needs.ts src/motivation/goals.ts src/motivation/types.ts` (exit 0) — contrato stub preservado (Pitfall 5)
    - nodes.shelter-bridge.test.ts cobre 5 casos de shouldBuildShelter (noite+exposto→true; dia→false; com-teto→false; survivalCritical→false; já-build→false)
    - `bun test src/cognition/nodes.shelter-bridge.test.ts` passa (verde)
  </acceptance_criteria>
  <done>Ponte de abrigo no observe: noite (`!snapshot.status.isDay`) + exposto + seguro → build:shelter; precedência do reflexo Fase 8 respeitada (não ativa sob survivalCritical); módulo motivation e shelter.ts intactos; helper shouldBuildShelter testado.</done>
</task>

<task type="auto">
  <name>Task 2: Kind 'build' no canal de pedido de jogador (SUPPORTED_REQUEST_KINDS + detectRequestKind + makePlayerRequestGoal)</name>
  <files>src/chat/conversation.ts, src/chat/conversation.test.ts</files>
  <read_first>
    - src/chat/conversation.ts (SUPPORTED_REQUEST_KINDS, detectRequestKind, makePlayerRequestGoal, handleConversation — bloco ASSISTANT na linha 155). NOTA: `detectRequestKind` (L40) e `makePlayerRequestGoal` (L68) são MÓDULO-PRIVADOS hoje — precisam de `export` para os testes importarem.
    - src/chat/conversation.test.ts (convenção de teste do handleConversation/detectRequestKind — replicar para o kind build)
    - .planning/phases/12-building-deliberado/12-RESEARCH.md §"Pitfall 6" (SUPPORTED_REQUEST_KINDS fechado; gate ASSISTANT; manter o gate de disposição salvo se o usuário quiser AUTONOMOUS) + §"Open Questions 2" (recomendação: manter gate ASSISTANT)
    - .planning/phases/12-building-deliberado/12-02-SUMMARY.md (confirma que build:<sub> roteia para a skill)
  </read_first>
  <action>
    Em `src/chat/conversation.ts`:

    1. **Estender SUPPORTED_REQUEST_KINDS** com `'build'`:
    ```typescript
    export const SUPPORTED_REQUEST_KINDS = ['gather', 'follow', 'navigate', 'build'] as const
    ```

    2. **Exportar e estender detectRequestKind.** Hoje é módulo-privado (`function detectRequestKind`); o teste em conversation.test.ts vai importá-la e chamar `detectRequestKind('constrói um abrigo')` diretamente, então ela PRECISA ser exportada. Mudar a assinatura para `export function detectRequestKind`. Em seguida adicionar as keywords de building (pt/en) ANTES do `return null`, mas DEPOIS de gather/follow/navigate (ordem importa — build é mais específico que navigate para "construir"):
    ```typescript
    export function detectRequestKind(message: string): SupportedRequestKind | null {
      const m = message.toLowerCase()
      // ... gather / follow / navigate existentes (NÃO alterar) ...
      if (/\b(constr[uoói]\w*|build|abrigo|parede|muro|torre|estação|estacao|shelter|wall|tower|station)\b/.test(m)) return 'build'
      return null
    }
    ```
    NOTA: testar que "constrói", "construir", "construa" casam (radical `constr`). Manter a ordem: gather/follow/navigate primeiro mantém o comportamento existente; build é adicionado por último.

    3. **makePlayerRequestGoal deve emitir um goal id build:<sub> quando kind==='build'.** Hoje gera `id: player_request:${kind}:${now}` com `kind` literal e é módulo-privado. Para o kind 'build', o roteador do Plan 02 espera um id que COMECE com `build:`. Resolver o sub-tipo a partir da mensagem e gerar o id correto. Exportar também se o teste importar diretamente (Claude's discretion — exportar se houver teste unitário direto).

    Refatorar `makePlayerRequestGoal` para aceitar a mensagem e derivar o sub-tipo de build:
    ```typescript
    function detectBuildSub(message: string): 'shelter' | 'wall' | 'tower' | 'station' {
      const m = message.toLowerCase()
      if (/\b(abrigo|shelter|casa)\b/.test(m)) return 'shelter'
      if (/\b(parede|muro|wall)\b/.test(m)) return 'wall'
      if (/\b(torre|tower)\b/.test(m)) return 'tower'
      if (/\b(estação|estacao|station|bancada|fornalha)\b/.test(m)) return 'station'
      return 'shelter'   // default seguro: abrigo
    }

    function makePlayerRequestGoal(kind: SupportedRequestKind, now: number, message: string): Goal {
      const id = kind === 'build'
        ? `build:${detectBuildSub(message)}`           // Fase 12: roteável pelo buildGoalToSkillParams (Plan 02)
        : `player_request:${kind}:${now}`
      return { id, kind, priority: 1, progress: 0, dependsOn: [], source: 'player_request', committedAt: now }
    }
    ```
    Atualizar a chamada em `handleConversation` (linha ~160) para passar `message`:
    ```typescript
    holder.goals.push(makePlayerRequestGoal(kind, now, message))
    ```

    Manter o gate `holder.disposition === 'ASSISTANT'` (Open Question 2 — recomendação da RESEARCH: manter o canal player_request existente em ASSISTANT). NÃO mudar shouldRespond (a resposta conversacional em ambos os modos do quick 260622-nif fica intacta; só a GERAÇÃO de goal segue gated em ASSISTANT).

    4. **Testes** em `src/chat/conversation.test.ts` (adicionar, não substituir):
    - `detectRequestKind('constrói um abrigo')` → 'build'
    - `detectRequestKind('faz uma parede aqui')` → 'build'
    - `detectRequestKind('build a tower')` → 'build'
    - handleConversation em ASSISTANT com "constrói um abrigo" e trust suficiente → holder.goals contém um goal cujo id === 'build:shelter' e playerRequestPending === true
    - handleConversation em AUTONOMOUS com a mesma mensagem → holder.goals NÃO ganha goal de build (gate ASSISTANT mantido)
    Reusar o mock de holder/bot/provider já presente em conversation.test.ts. O teste importa `detectRequestKind` de conversation.ts — por isso o export do passo 2 é obrigatório.
  </action>
  <verify>
    <automated>bun test src/chat/conversation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'gather', 'follow', 'navigate', 'build'" src/chat/conversation.ts` → kind build adicionado
    - `grep -q "export function detectRequestKind" src/chat/conversation.ts` → detectRequestKind exportada (teste consegue importar)
    - `grep -q "function detectBuildSub" src/chat/conversation.ts` → resolução de sub-tipo presente
    - `grep -q "build:\${detectBuildSub" src/chat/conversation.ts` → id roteável gerado
    - `grep -q "makePlayerRequestGoal(kind, now, message)" src/chat/conversation.ts` → chamada passa message
    - conversation.test.ts: detectRequestKind para 3 frases de build retorna 'build'
    - conversation.test.ts: pedido de build em ASSISTANT gera goal id 'build:shelter'; em AUTONOMOUS não gera goal
    - O gate ASSISTANT preservado: `grep -q "holder.disposition === 'ASSISTANT'" src/chat/conversation.ts` → presente
    - `bun test src/chat/conversation.test.ts` passa (verde — incluindo testes existentes, sem regressão)
  </acceptance_criteria>
  <done>Pedido "constrói abrigo/parede/torre/estação" em ASSISTANT vira goal build:<sub> roteável; detectRequestKind exportada e detectBuildSub mapeia keywords pt/en; gate ASSISTANT e shouldRespond preservados; testes verdes sem regressão.</done>
</task>

</tasks>

<verification>
- `bun test src/cognition/ src/chat/ src/skills/builder.test.ts src/skills/blueprints.test.ts` — todos verdes
- `git diff --quiet src/skills/shelter.ts src/motivation/needs.ts src/motivation/goals.ts src/motivation/types.ts` — exit 0 (D-11/Pitfall 5: reflexo e contrato stub intactos)
- `grep -q "state === 'building' && fresh" src/cognition/nodes.ts` — G-01 ainda intacto (D-14)
- Cadeia completa verificável: ponte observe gera build:shelter → roteador build:* (Plan 02) resolve skill build → runBlueprint (Plan 01) fecha o abrigo (SC1); pedido de jogador gera build:<sub> → mesma cadeia (SC2)
</verification>

<success_criteria>
- À noite (`!snapshot.status.isDay`) + exposto + seguro, o agente gera build:shelter autonomamente (SC1 — ativação por need, via bridge isolado)
- Pedido de jogador "constrói X" em ASSISTANT vira goal build:<sub> roteável (SC2 — estrutura autônoma sob pedido)
- O reflexo de emergência da Fase 8 mantém precedência (ponte não ativa sob survivalCritical; D-11/D-15)
- Módulo motivation (needs/goals/types) e shelter.ts byte-for-byte intactos — Open Question 1 resolvida pelo bridge de menor blast radius
</success_criteria>

<output>
Após completar, criar `.planning/phases/12-building-deliberado/12-03-SUMMARY.md` documentando: o gatilho fino escolhido para "noite + exposto" (noite = `!snapshot.status.isDay`, escala normalizada), a decisão Open Question 1 (bridge) e Open Question 2 (gate ASSISTANT mantido), e o estado final da Fase 12 (cadeia ponte→roteador→builder completa; SC1/SC2 cobertos por unit tests; SC3 = pacing/bounds herdados, validação ao vivo é soak).
</output>
</output>
