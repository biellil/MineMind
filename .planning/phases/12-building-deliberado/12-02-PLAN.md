---
phase: 12-building-deliberado
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/skills/index.ts
  - src/cognition/nodes.ts
  - src/cognition/states.ts
  - src/cognition/nodes.build.test.ts
autonomous: true
requirements: [BUILD-02, BUILD-03]
must_haves:
  truths:
    - "Um goal com id 'build:shelter' roteia DETERMINISTICAMENTE para a skill build (sem LLM, sem tocar o dispatch G-01)"
    - "A skill build é executável pelo loop via skillRegistry['build']"
    - "O estado 'building' não é mais stub (removido de STUB_STATES) sem quebrar o dispatch G-01 craft/smelt/equip/place"
    - "O roteador build:* roda ANTES dos ramos gathering/building no execute, paralelo ao roteador DAG"
  artifacts:
    - path: "src/skills/index.ts"
      provides: "build registrado em skillRegistry + buildTool em toolRegistry"
      contains: "build,"
    - path: "src/cognition/nodes.ts"
      provides: "BUILD_PREFIXES + buildGoalToSkillParams + roteamento build:* no execute"
      contains: "buildGoalToSkillParams"
    - path: "src/cognition/states.ts"
      provides: "'building' removido de STUB_STATES (só 'fighting' resta)"
      contains: "STUB_STATES"
  key_links:
    - from: "src/cognition/nodes.ts"
      to: "src/skills/index.ts"
      via: "skillRegistry['build'] chamado pelo roteador build:*"
      pattern: "skillRegistry\\[skill"
    - from: "src/cognition/nodes.ts"
      to: "src/cognition/nodes.ts (buildGoalToSkillParams)"
      via: "execute roteia currentGoal.id que começa com 'build:'"
      pattern: "build:"
---

<objective>
Integrar a skill `build` (criada no Plan 01) no loop cognitivo por um CAMINHO SEPARADO, sem acoplar nem quebrar o dispatch G-01 da Fase 9 (D-14). Espelha exatamente o roteador determinístico do DAG da Fase 10 (`goalToSkillParams` + `DAG_PREFIXES`), adicionando `build:*` como um prefixo paralelo: um goal `build:shelter`/`build:wall`/`build:tower`/`build:station` resolve para a skill `build` SEM o LLM conhecer a mecânica de construção (D-13).

Purpose: torna o estado `building` real e selecionável por goal determinístico. Habilita SC2 (estrutura autônoma) — a ativação que GERA esses goals vem no Plan 03.
Output: skill `build` registrada, roteador `build:*` no execute node, `'building'` removido de STUB_STATES. NÃO modifica o bloco G-01 (nodes.ts:392-425) nem o roteador DAG.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-building-deliberado/12-CONTEXT.md
@.planning/phases/12-building-deliberado/12-RESEARCH.md
@.planning/phases/12-building-deliberado/12-01-SUMMARY.md

<interfaces>
<!-- Contratos extraídos do código-fonte — usar DIRETAMENTE. -->

From src/skills/builder.ts (Plan 01 — já existe):
```typescript
export async function build(bot: Bot, rawParams: unknown): Promise<SkillResult>
export const buildTool = { name: 'build', description: string, schema: BuildSchema, execute: build } as const
// BuildSchema aceita { tipo: 'shelter'|'wall'|'tower'|'station'|'custom', dims?, origin?, bloco?, blocks?, signal? }
// tipo só (sem dims/origin) → skill usa config.build<Tipo>Dims + origin = floor(bot.entity.position)
```

From src/skills/index.ts (estrutura a estender):
```typescript
export const skillRegistry: Record<string, SkillFunction> = { navigate, dig, follow, attack, eat, flee, shelter, placeBlock, craft, smelt, equip }
export const toolRegistry: SkillTool[] = [navigateTool, digTool, /* ... */, equipTool]
```

From src/cognition/nodes.ts (MOLDE a espelhar — D-13):
```typescript
// Linha 62: const DAG_PREFIXES = ['gather:', 'craft:', 'smelt:', 'ensure:'] as const
// Linha 96: export function goalToSkillParams(goalId: string): { skill: string; paramsJson: string } | null { ... }
// Linha 331 (dentro de execute): if (snap && currentGoal && DAG_PREFIXES.some(p => currentGoal.id.startsWith(p))) { routing = goalToSkillParams(...); skill = routing.skill; target = routing.paramsJson }
// O bloco G-01 (linha 392): if (!skill && snap && state === 'building' && fresh) { ... }  ← NÃO TOCAR (D-14/Pitfall 3)
// A skill resolvida cai no dispatch comum: const params = ...JSON.parse(target)..., result = await skillRegistry[skill!]!(bot, params)
// craft/smelt/equip/placeBlock já passam por `{ ...JSON.parse(target), signal }` (linha 499) — 'build' usa o MESMO caminho.
```

From src/cognition/states.ts:
```typescript
export const STUB_STATES: ReadonlyArray<CognitiveState> = ['fighting', 'building']  // ← remover 'building'
export function isStub(s: CognitiveState): boolean { return STUB_STATES.includes(s) }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar a skill build no skillRegistry e buildTool no toolRegistry</name>
  <files>src/skills/index.ts</files>
  <read_first>
    - src/skills/index.ts (estrutura completa: imports, re-exports, skillRegistry, toolRegistry)
    - src/skills/builder.ts (build, buildTool, BuildSchema — exports do Plan 01)
  </read_first>
  <action>
    Em `src/skills/index.ts`:

    1. Adicionar o import (junto aos imports de skills, após a linha de `equip`):
    ```typescript
    import { build, buildTool } from './builder'
    ```

    2. Adicionar o re-export (após a linha de re-export de equip):
    ```typescript
    export { build, BuildSchema, buildTool, runBlueprint } from './builder'
    ```

    3. Adicionar `build` ao `skillRegistry` (objeto):
    ```typescript
    export const skillRegistry: Record<string, SkillFunction> = {
      navigate, dig, follow, attack, eat, flee, shelter, placeBlock, craft, smelt, equip, build,
    }
    ```

    4. Adicionar `buildTool` ao `toolRegistry` (array), após `equipTool`:
    ```typescript
    export const toolRegistry: SkillTool[] = [navigateTool, digTool, followTool, attackTool, eatTool, fleeTool, shelterTool, placeBlockTool, craftTool, smeltTool, equipTool, buildTool]
    ```
  </action>
  <verify>
    <automated>bun -e "import('./src/skills/index.ts').then(m => { if (typeof m.skillRegistry.build !== 'function') throw new Error('build não registrado'); if (!m.toolRegistry.find(t => t.name === 'build')) throw new Error('buildTool ausente no toolRegistry'); console.log('registry OK') })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { build, buildTool } from './builder'" src/skills/index.ts` → import presente
    - `grep -q "export { build" src/skills/index.ts` → re-export presente
    - O comando de verify imprime "registry OK" (skillRegistry.build é function E toolRegistry contém um tool name==='build')
    - `grep -c "build," src/skills/index.ts` → build aparece no objeto skillRegistry
  </acceptance_criteria>
  <done>skillRegistry['build'] resolve para a skill build; toolRegistry inclui buildTool; imports/re-exports adicionados.</done>
</task>

<task type="auto">
  <name>Task 2: Roteador determinístico build:* no execute node + remover 'building' de STUB_STATES</name>
  <files>src/cognition/nodes.ts, src/cognition/states.ts, src/cognition/nodes.build.test.ts</files>
  <read_first>
    - src/cognition/nodes.ts §L56-116 (LIFE_CRITICAL_TRIGGERS, DAG_PREFIXES, goalToSkillParams — molde D-13) e §L311-425 (execute node, roteador DAG na linha 331, bloco G-01 na linha 392 que NÃO pode ser tocado)
    - src/cognition/states.ts (STUB_STATES — remover 'building')
    - .planning/phases/12-building-deliberado/12-RESEARCH.md §"Pitfall 3" (NÃO acoplar no G-01) + §"Pitfall 4" (grep isStub antes de remover building) + §"Code Examples → Mirroring the deterministic goal router"
  </read_first>
  <action>
    **Parte A — buildGoalToSkillParams + BUILD_PREFIXES em nodes.ts:**

    Após a função `goalToSkillParams` (linha 116), adicionar o molde espelhado (D-13):
    ```typescript
    // Fase 12 D-13: prefixo de sub-goals de building deliberado. Caminho SEPARADO do G-01 (D-14)
    // e do DAG (Fase 10) — espelha goalToSkillParams.
    const BUILD_PREFIXES = ['build:'] as const

    /**
     * Fase 12 D-13: Mapeia ID de goal build:* para (skill, params) SEM LLM.
     * 'build:shelter' → build({tipo:'shelter'}); idem wall/tower/station.
     * dims/origin ficam undefined aqui — a skill build preenche com config defaults + origin=posição do bot
     * no momento da execução (Open Question 3 da RESEARCH).
     */
    export function buildGoalToSkillParams(goalId: string): { skill: string; paramsJson: string } | null {
      if (!goalId.startsWith('build:')) return null
      const sub = goalId.slice('build:'.length)   // 'shelter' | 'wall' | 'tower' | 'station'
      if (!sub) return null
      return { skill: 'build', paramsJson: JSON.stringify({ tipo: sub }) }
    }
    ```

    **Parte B — rotear no execute, ANTES dos ramos gathering/building:**

    No execute node, o roteador DAG está na linha 331 (`if (snap && currentGoal && DAG_PREFIXES.some(...))`). Adicionar um bloco PARALELO logo APÓS o bloco DAG (após a linha 349 `// === Fim do roteador Fase 10 ===`) e ANTES do `if (!skill && snap && state === 'gathering')` (linha 351):
    ```typescript
    // === Fase 12 D-13: Roteador determinístico de goals build:* (caminho separado do G-01) ===
    // Se o currentGoal é um goal de building deliberado (prefixo build:), roteia para a skill build.
    // Paralelo ao roteador DAG; NÃO toca o dispatch G-01 (state==='building' && fresh, linha ~392).
    if (!skill && snap && currentGoal && BUILD_PREFIXES.some(p => currentGoal.id.startsWith(p))) {
      const routing = buildGoalToSkillParams(currentGoal.id)
      if (routing) {
        skill = routing.skill
        target = routing.paramsJson
        log(`[build] roteando ${currentGoal.id} → ${skill}`)
      }
    }
    // === Fim do roteador Fase 12 ===
    ```
    NOTA: usar `!skill &&` no guard para não sobrescrever uma skill já resolvida pelo DAG (defensivo; build:* e gather:* são prefixos disjuntos, mas mantém a ordem clara). O dispatch comum (linha 495-502) já trata a skill `build`: cai no ramo `else` (`{ ...JSON.parse(target), signal }`) porque `build` não é `dig` nem `navigate` — `JSON.parse('{"tipo":"shelter"}')` vira `{tipo:'shelter', signal}`, exatamente o que a skill build espera. NÃO precisa alterar o dispatch de params.

    **Parte C — remover 'building' de STUB_STATES:**

    Antes de remover, confirmar que nada usa `isStub('building')` como gate (Pitfall 4): rodar `grep -rn "isStub" src/`. Se algum call-site curto-circuita o G-01 via isStub('building'), PARAR e documentar (não deveria existir — o G-01 não consulta isStub). Assumindo limpo:

    Em `src/cognition/states.ts`, alterar:
    ```typescript
    export const STUB_STATES: ReadonlyArray<CognitiveState> = ['fighting']
    ```
    (Remover apenas `'building'`; `'fighting'` permanece stub — Fase 13.) Atualizar o comentário da linha 9 para `// Estados stub (D-06): 'fighting' segue stub (Fase 13); 'building' implementado na Fase 12.`

    **Parte D — teste de roteamento** (`src/cognition/nodes.build.test.ts`):
    Teste UNITÁRIO de `buildGoalToSkillParams` (função pura exportada):
    - `buildGoalToSkillParams('build:shelter')` → `{ skill: 'build', paramsJson: '{"tipo":"shelter"}' }`
    - `buildGoalToSkillParams('build:wall')` → tipo 'wall'
    - `buildGoalToSkillParams('build:station')` → tipo 'station'
    - `buildGoalToSkillParams('gather:oak_log')` → null (prefixo errado)
    - `buildGoalToSkillParams('build:')` → null (sub vazio)
    Seguir a convenção `import { test, expect } from 'bun:test'`.
  </action>
  <verify>
    <automated>bun test src/cognition/nodes.build.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function buildGoalToSkillParams" src/cognition/nodes.ts` → presente
    - `grep -q "const BUILD_PREFIXES = \['build:'\]" src/cognition/nodes.ts` → presente
    - `grep -q "\[build\] roteando" src/cognition/nodes.ts` → roteador no execute presente
    - `grep -q "STUB_STATES: ReadonlyArray<CognitiveState> = \['fighting'\]" src/cognition/states.ts` → 'building' removido, 'fighting' mantido
    - O bloco G-01 NÃO foi alterado: `grep -q "state === 'building' && fresh" src/cognition/nodes.ts` → ainda presente (dispatch G-01 intacto)
    - `grep -rn "isStub('building')" src/` → sem resultados (nada curto-circuita via isStub building)
    - nodes.build.test.ts cobre os 5 casos de buildGoalToSkillParams
    - `bun test src/cognition/nodes.build.test.ts` passa (verde)
  </acceptance_criteria>
  <done>buildGoalToSkillParams + BUILD_PREFIXES adicionados; execute roteia build:* num bloco paralelo antes de gathering/building; 'building' fora de STUB_STATES; G-01 e roteador DAG intactos; teste do roteador verde.</done>
</task>

</tasks>

<verification>
- `bun test src/cognition/nodes.build.test.ts src/skills/builder.test.ts src/skills/blueprints.test.ts` — todos verdes
- `grep -q "state === 'building' && fresh" src/cognition/nodes.ts` — bloco G-01 da Fase 9 intacto (D-14)
- `bun test src/cognition/` — suíte de cognição não regride (DAG/G-01 ainda passam)
- `grep -rn "isStub('building')" src/` — vazio (Pitfall 4 confirmado)
</verification>

<success_criteria>
- skillRegistry['build'] executável; toolRegistry expõe buildTool
- buildGoalToSkillParams roteia build:shelter/wall/tower/station para a skill build deterministicamente (D-13)
- O roteador build:* roda no execute em caminho SEPARADO, sem tocar o dispatch G-01 (D-14) nem o roteador DAG
- 'building' não é mais stub; 'fighting' permanece (Fase 13)
</success_criteria>

<output>
Após completar, criar `.planning/phases/12-building-deliberado/12-02-SUMMARY.md` documentando: como o build:* foi fiado em paralelo ao DAG, a confirmação de que G-01 e isStub ficaram intactos, e o handoff para Plan 03 (falta GERAR os goals build:* — bridge de need + player-request).
</output>
