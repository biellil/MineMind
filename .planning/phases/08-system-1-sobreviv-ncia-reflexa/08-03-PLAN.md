---
phase: 08-system-1-sobreviv-ncia-reflexa
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/skills/flee.ts
  - src/skills/flee.test.ts
  - src/skills/shelter.ts
  - src/skills/shelter.test.ts
  - src/skills/index.ts
autonomous: true
requirements: [SURV-02, SURV-03, SURV-05]
must_haves:
  truths:
    - "A skill flee usa GoalInvert(GoalFollow(mob)) com setGoal(goal,true) e cai para sprint cego quando o A* falha (D-06)"
    - "flee herda os bounds do 999.1 (searchRadius/thinkTimeout) e retorna SkillResult grounded por distância ao mob antes/depois"
    - "A skill shelter faz cavar-e-tampar OU pilar 1×1 condicionalmente (D-08) via placeBlock mínimo, checando blockAt abaixo antes de cavar"
    - "flee e shelter estão registradas no skillRegistry"
  artifacts:
    - path: "src/skills/flee.ts"
      provides: "Skill reflexa flee (D-06) grounded por delta de distância"
      exports: ["flee", "FleeSchema", "fleeTool"]
      min_lines: 60
    - path: "src/skills/shelter.ts"
      provides: "Abrigo de emergência cavar-vs-pilar (D-08) com placeBlock mínimo"
      exports: ["shelter", "ShelterSchema", "shelterTool"]
      min_lines: 50
  key_links:
    - from: "src/skills/flee.ts"
      to: "mineflayer-pathfinder goals"
      via: "GoalInvert(GoalFollow)"
      pattern: "GoalInvert"
    - from: "src/skills/flee.ts"
      to: "src/config.ts"
      via: "bounds 999.1 (pathfinderSearchRadius/pathfinderThinkTimeoutMs)"
      pattern: "pathfinderSearchRadius"
---

<objective>
Entregar as duas skills reflexas de evasão (Wave 1, ramo B): `flee` (NOVA, D-06 — fugir de mob via GoalInvert(GoalFollow) + fallback sprint cego) e `shelter` (NOVA, D-08 — abrigo de emergência cavar-vs-pilar com placeBlock mínimo). Roda em paralelo com o Plan 02 (eat/attack) — arquivos disjuntos; ambos editam `src/skills/index.ts` (registro), então o merge resolve registro aditivo.

Purpose: SURV-02 (fugir de hostil) e SURV-03 (abrigo de emergência) precisam destas primitivas de ação antes que o System 1 (Plan 04) as dispare por preempção. Toda nova chamada de pathfinder herda os bounds do 999.1 (SURV-05 critério #5).
Output: `src/skills/flee.ts` + testes, `src/skills/shelter.ts` + testes, registro no `skillRegistry`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-system-1-sobreviv-ncia-reflexa/08-CONTEXT.md
@.planning/phases/08-system-1-sobreviv-ncia-reflexa/08-RESEARCH.md

<interfaces>
<!-- Contratos que o executor usa diretamente. -->

SkillResult (src/grounding/types.ts):
```typescript
export interface SkillResult {
  readonly outcome: 'success' | 'partial' | 'no_effect' | 'error'
  readonly observed: number      // flee: 1 se aumentou distância ao mob; shelter: 1 se ficou coberto
  readonly expected: number
  readonly delta: Readonly<Record<string, number>>
  readonly reason?: string
}
```

Bounds do 999.1 a aplicar em TODA nova navegação (config.ts, ler estes campos):
```typescript
config.pathfinderSearchRadius       // 48 — passar ao Movements/pathfinder
config.pathfinderThinkTimeoutMs     // 2000
config.navigateTimeoutMs            // 30000 — timeout do executeWithSafety
```

executeWithSafety (src/skills/executor.ts) — envelope com timeout/watchdog/abort (4° racer):
```typescript
executeWithSafety<T>(action: () => Promise<T>, opts: {
  timeoutMs?, progressChecker?, progressIntervalMs?, noProgressToleranceMs?, signal?: AbortSignal
}): Promise<T>
```

Padrão de honrar abort dentro da skill (de navigate.ts:68-73) — flee usa setGoal(null) por D-07:
```typescript
if (signal) {
  signal.addEventListener('abort', () => {
    try { bot.pathfinder.setGoal(null) } catch {}   // D-07: forçado, NÃO stop()
  }, { once: true })
}
```

Primitivas Mineflayer/pathfinder confirmadas (RESEARCH):
```typescript
import { goals } from 'mineflayer-pathfinder'
new goals.GoalInvert(new goals.GoalFollow(mobEntity, radius))   // GoalRunAway NÃO existe
bot.pathfinder.setGoal(goal: Goal | null, dynamic?: boolean): void   // (null) = parada forçada
bot.pathfinder.goto(goal): Promise<void>
bot.nearestEntity(filter?): Entity | null
bot.blockAt(point: Vec3): Block | null
bot.placeBlock(refBlock: Block, faceVector: Vec3): Promise<void>
bot.setControlState(control, state): void
bot.lookAt(point: Vec3): Promise<void>
bot.entity.position: Vec3
bot.inventory.items(): Item[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar skill flee (D-06) com GoalInvert + fallback sprint + bounds 999.1 + testes</name>
  <read_first>
    - src/skills/navigate.ts (padrão completo de skill com pathfinder: signal, executeWithSafety, captureGroundState, SkillResult)
    - src/skills/executor.ts (executeWithSafety + ExecuteOptions.signal como 4° racer)
    - src/config.ts (bounds 999.1: pathfinderSearchRadius, pathfinderThinkTimeoutMs, navigateTimeoutMs)
    - src/grounding/types.ts (SkillResult)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-RESEARCH.md (Code Example "Fugir (D-06)"; D-07 setGoal(null); anti-pattern stop() na preempção)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-CONTEXT.md (D-06 GoalInvert+sprint fallback; D-07; SURV-05 herança de bounds)
  </read_first>
  <behavior>
    - Mob alvo presente: cria GoalInvert(GoalFollow(mob, fleeRadius)) e chama setGoal(goal, true). Após executar, mede distância ao mob: aumentou → outcome:'success', observed:1.
    - A* falha (goto rejeita com noPath/timeout): cai para sprint cego (vetor oposto + setControlState forward+sprint) por um curto período; se a distância ainda aumentou → success/partial; senão no_effect. O fallback NÃO lança.
    - Sem mob alvo (nearestEntity null) → outcome:'no_effect', observed:0, reason 'sem mob para fugir'.
    - Abort via signal → bot.pathfinder.setGoal(null) (D-07, forçado) + limpa controlStates do sprint; outcome derivado do delta de distância real.
    - Distância ao mob NÃO aumentou após tudo → no_effect (grounded por delta real, não pela Promise).
  </behavior>
  <action>
    Criar `src/skills/flee.ts`:

    1. `FleeSchema = z.object({ entityName: z.string().max(64).optional().describe('Nome do mob a fugir; se ausente, foge do hostil mais próximo'), radius: z.number().min(4).max(32).default(16).describe('Raio de fuga') })`. `export type FleeParams = z.infer<typeof FleeSchema>`.
    2. `export async function flee(bot: Bot, rawParams: unknown): Promise<SkillResult>`:
       - Extrair `signal` ANTES do Zod (padrão navigate.ts).
       - `const { entityName, radius } = FleeSchema.parse(rawParams ?? {})`.
       - Localizar mob: `const mob = entityName ? bot.nearestEntity((e) => e.name === entityName) : bot.nearestEntity((e) => (e as any).kind === 'Hostile mobs')`.
       - Se `!mob` → `{ outcome:'no_effect', observed:0, expected:1, delta:{}, reason:'sem mob para fugir' }`.
       - `const distBefore = bot.entity.position.distanceTo(mob.position)`.
       - Aplicar bounds 999.1: configurar o pathfinder com `searchRadius`/`thinkTimeout` dos campos de config ANTES do goto. Seguir EXATAMENTE como navigate/dig aplicam (ler navigate.ts/dig.ts para o ponto de aplicação — se o projeto seta via `bot.pathfinder.thinkTimeout`/Movements, replicar; senão documentar que herda do Movements global). No mínimo: passar `timeoutMs: config.navigateTimeoutMs` ao executeWithSafety.
       - Registrar abort: `if (signal) signal.addEventListener('abort', () => { try { bot.pathfinder.setGoal(null) } catch {}; clearSprint(bot) }, { once:true })` (D-07).
       - `import { goals } from 'mineflayer-pathfinder'`. `const goal = new goals.GoalInvert(new goals.GoalFollow(mob, radius))`.
       - `let threw: unknown = null; try { await executeWithSafety(() => { bot.pathfinder.setGoal(goal, true); return bot.pathfinder.goto(goal) }, { timeoutMs: config.navigateTimeoutMs, signal, progressChecker: () => Math.round(bot.entity.position.distanceTo(mob.position)) }) } catch (err) { threw = err }`.
       - Fallback sprint cego SE `threw` indica noPath/timeout: helper `blindSprintAway(bot, mob)` que faz `await bot.lookAt(...vetor oposto)`, `bot.setControlState('forward', true)`, `bot.setControlState('sprint', true)`, espera ~600ms, depois `clearSprint(bot)` (limpa forward/sprint). NUNCA lança.
       - Grounding por delta de distância: `const distAfter = bot.entity.position.distanceTo(mob.position)`. `const gained = distAfter - distBefore`. `outcome = gained > 1 ? 'success' : (gained > 0 ? 'partial' : 'no_effect')`. `observed = gained > 0 ? 1 : 0`. Anexar reason do throw se houve.
       - Helper local `clearSprint(bot)`: `bot.setControlState('forward', false); bot.setControlState('sprint', false)` em try/catch.
    3. `export const fleeTool = { name:'flee', description:'Foge do mob hostil mais próximo (GoalInvert + fallback sprint)', schema: FleeSchema, execute: flee } as const`.

    Criar `src/skills/flee.test.ts` (`import { test, expect } from 'bun:test'`) com mock bot mínimo: `bot.entity.position` (Vec3-like com distanceTo), `bot.nearestEntity`, `bot.pathfinder.setGoal` (spy), `bot.pathfinder.goto` (async resolve ou reject para testar fallback), `bot.setControlState` (spy), `bot.lookAt` (async). Cobrir TODOS os casos do <behavior>. Mínimo 5 testes (sucesso aumentou distância; sem-mob=no_effect; goto rejeita→fallback sprint chamado; abort→setGoal(null) chamado; distância não aumentou=no_effect).
  </action>
  <verify>
    <automated>bun test src/skills/flee.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GoalInvert" src/skills/flee.ts` retorna 1 match
    - `grep -n "GoalFollow" src/skills/flee.ts` retorna 1 match
    - `grep -n "setGoal(null)" src/skills/flee.ts` retorna >= 1 match (D-07 abort forçado)
    - `grep -n "setControlState.*sprint" src/skills/flee.ts` retorna >= 1 match (fallback sprint cego)
    - `grep -n "config.navigateTimeoutMs\|config.pathfinderSearchRadius\|config.pathfinderThinkTimeoutMs" src/skills/flee.ts` retorna >= 1 match (bounds 999.1)
    - `grep -c "test(" src/skills/flee.test.ts` retorna >= 5
    - `bun test src/skills/flee.test.ts` passa (goto-rejeita aciona fallback sprint; abort chama setGoal(null))
  </acceptance_criteria>
  <done>flee.ts foge via GoalInvert com fallback sprint cego, abort forçado por setGoal(null) (D-07), grounded por delta de distância, herda bounds 999.1; flee.test.ts verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar skill shelter (D-08) cavar-vs-pilar + placeBlock mínimo + registrar flee/shelter + testes</name>
  <read_first>
    - src/skills/dig.ts (padrão de skill que mexe em blocos + bounds 999.1 + SkillResult)
    - src/skills/index.ts (skillRegistry e toolRegistry — adicionar flee e shelter)
    - src/grounding/types.ts (SkillResult)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-RESEARCH.md (D-08 cavar-e-tampar vs pilar; placeBlock mínimo = Fase 9 faz o robusto; checar blockAt abaixo)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-CONTEXT.md (D-08 condicional dual; D-17 abrigo só emergência reativo)
  </read_first>
  <behavior>
    - Decisão cavar-vs-pilar: se há bloco SÓLIDO 2 abaixo do bot (blockAt(pos.offset(0,-2,0)) não é ar/lava/água) → cavar-e-tampar (descer 1 e tampar o topo). Senão (terreno plano/sem base) → pilar 1×1 (colocar bloco sob os pés e subir).
    - Segurança ANTES de cavar: blockAt(2 abaixo) NÃO pode ser lava nem ar (não cair em caverna/lava) — se for, NÃO cava, tenta pilar.
    - Sem blocos colocáveis no inventário (nenhum item sólido) → outcome:'no_effect', reason 'sem blocos para abrigar'.
    - Sucesso: após colocar/cavar, o bot fica "coberto" (há bloco acima OU paredes) → outcome:'success', observed:1.
    - placeBlock MÍNIMO: 1 chamada bot.placeBlock com try/catch (robustez definitiva = Fase 9); falha de placeBlock → partial/no_effect, NÃO lança.
  </behavior>
  <action>
    Criar `src/skills/shelter.ts`:

    1. `ShelterSchema = z.object({})` (sem params além do signal). `export type ShelterParams = z.infer<typeof ShelterSchema>`.
    2. `export async function shelter(bot: Bot, rawParams: unknown): Promise<SkillResult>`:
       - Extrair `signal` ANTES do Zod; `ShelterSchema.parse(rawParams ?? {})`.
       - Selecionar bloco colocável: `const block = bot.inventory.items().find((it) => /_(planks|log)$|^(cobblestone|stone|dirt)$/.test(it.name))` (lista mínima de blocos sólidos comuns). Se `!block` → `{ outcome:'no_effect', observed:0, expected:1, delta:{}, reason:'sem blocos para abrigar' }`.
       - Ler `const pos = bot.entity.position`. `const below2 = bot.blockAt(pos.offset(0, -2, 0))`. `const canDig = below2 != null && below2.name !== 'air' && below2.name !== 'lava' && below2.name !== 'water'`.
       - Branch CAVAR-E-TAMPAR (`canDig`): cavar o bloco 1 abaixo (`await bot.dig(blockBelow1)` em try/catch), descer, e tampar o topo via `await bot.placeBlock(refBlock, faceVector)` em try/catch. (placeBlock MÍNIMO — Fase 9 robustece.)
       - Branch PILAR 1×1 (else): `await bot.equip(block, 'hand')`; olhar para baixo; `bot.setControlState('jump', true)`; no ápice, `await bot.placeBlock(blockBelowRef, new Vec3(0,1,0))` em try/catch; `bot.setControlState('jump', false)`.
       - Grounding: verificar cobertura — `const above = bot.blockAt(pos.offset(0, 2, 0))`; `outcome = (above && above.name !== 'air') ? 'success' : 'partial'`; `observed = outcome === 'success' ? 1 : 0`. Anexar reason se algum try/catch capturou.
       - Importar `Vec3` de `'vec3'` se necessário para os offsets (ver como dig.ts/navigate.ts obtêm Vec3 — provavelmente via `bot.entity.position.offset`).
    3. `export const shelterTool = { name:'shelter', description:'Cria abrigo de emergência (cavar-e-tampar ou pilar 1×1)', schema: ShelterSchema, execute: shelter } as const`.

    Em `src/skills/index.ts`:
    - `import { flee, fleeTool } from './flee'` + `import { shelter, shelterTool } from './shelter'`.
    - Re-exportar: `export { flee, FleeSchema, fleeTool } from './flee'` e `export { shelter, ShelterSchema, shelterTool } from './shelter'`.
    - Adicionar `flee` e `shelter` ao `skillRegistry`.
    - Adicionar `fleeTool` e `shelterTool` ao `toolRegistry`.
    NOTA: o Plan 02 também edita index.ts (adiciona eat). Se houver conflito de merge, manter TODOS os registros aditivos (navigate, dig, follow, attack, eat, flee, shelter).

    Criar `src/skills/shelter.test.ts` com mock bot mínimo: `bot.entity.position.offset`, `bot.blockAt` (retorna mock block com `.name`), `bot.inventory.items()`, `bot.dig`/`bot.placeBlock`/`bot.equip` (async no-op spies), `bot.setControlState` (spy). Cobrir: canDig=true→cava; canDig=false→pilar; sem blocos→no_effect; cobertura após→success. Mínimo 4 testes.
  </action>
  <verify>
    <automated>bun test src/skills/shelter.test.ts src/skills/schemas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "bot.placeBlock" src/skills/shelter.ts` retorna >= 1 match
    - `grep -n "bot.blockAt" src/skills/shelter.ts` retorna >= 1 match (checagem de segurança antes de cavar)
    - `grep -n "lava" src/skills/shelter.ts` retorna >= 1 match (guarda anti-lava/caverna do D-08)
    - `grep -n "flee\|shelter" src/skills/index.ts` retorna >= 6 matches (imports + re-exports + registros)
    - `grep -c "test(" src/skills/shelter.test.ts` retorna >= 4
    - `bun test src/skills/shelter.test.ts` passa (canDig branch e pilar branch ambos cobertos)
    - `bun test src/skills/schemas.test.ts` continua verde
  </acceptance_criteria>
  <done>shelter.ts faz cavar-vs-pilar condicional com placeBlock mínimo e guarda anti-lava; flee/shelter registrados no skillRegistry; testes verdes.</done>
</task>

</tasks>

<verification>
- `bun test src/skills/flee.test.ts src/skills/shelter.test.ts src/skills/schemas.test.ts` verde.
- flee usa setGoal(null) no abort (D-07), não stop() — latência sub-segundo.
- flee/shelter grounded por delta real (distância ao mob / cobertura), não pela Promise.
- Toda navegação reflexa herda os bounds do 999.1 (SURV-05 critério #5).
- skillRegistry contém flee e shelter (mais eat do Plan 02).
</verification>

<success_criteria>
- SURV-02 (fuga): flee aumenta a distância ao mob via GoalInvert ou sprint cego.
- SURV-03: shelter cria abrigo de emergência cavar-vs-pilar, com guarda anti-lava.
- SURV-05: bounds 999.1 aplicados; abort por setGoal(null) forçado.
- Zero dependência nova; placeBlock é mínimo (robusto = Fase 9).
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-system-1-sobreviv-ncia-reflexa/08-03-SUMMARY.md`.
</output>
