---
phase: 08-system-1-sobreviv-ncia-reflexa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/config.test.ts
  - src/cognition/reflex.ts
  - src/cognition/reflex.test.ts
autonomous: true
requirements: [SURV-01, SURV-02, SURV-03, SURV-04, SURV-05]
must_haves:
  truths:
    - "arbitrateReflex(sensors) devolve o reflexo vencedor por gravidade (ambiental > hostil > queda > fome) ou null"
    - "Reflexos vida-crítica trazem lifeCritical=true; fome traz lifeCritical=false"
    - "Os novos limiares de sobrevivência existem em config.ts com defaults derivados da mecânica MC 1.21 e validação de range"
  artifacts:
    - path: "src/cognition/reflex.ts"
      provides: "Função pura arbitrateReflex + tipos ReflexSensors/ReflexDecision"
      exports: ["arbitrateReflex", "ReflexSensors", "ReflexDecision", "REFLEX_GUARDS"]
      min_lines: 60
    - path: "src/cognition/reflex.test.ts"
      provides: "Tabela-verdade SURV-01..05 sem mock de bot"
      contains: "arbitrateReflex"
    - path: "src/config.ts"
      provides: "Limiares de sobrevivência (hungry 16, survivalCritical 0.5, oxygen/fall/lava/healthCritical)"
      contains: "healthCriticalThreshold"
  key_links:
    - from: "src/cognition/reflex.ts"
      to: "src/config.ts"
      via: "import { config }"
      pattern: "from '\\.\\./config'"
---

<objective>
Entregar a FUNDAÇÃO da camada reflexa (Wave 0): a função pura de arbitragem `arbitrateReflex` (D-01/D-03, testável por tabela-verdade sem mock de bot) e os limiares de sobrevivência em `config.ts` (D-11..D-14). Esta é a peça "função pura no driver" da decisão travada D-01 — a DECISÃO de qual reflexo vence, separada do ABORT FÍSICO (que fica no nó execute, Plan 04).

Purpose: O System 1 precisa de uma decisão determinística e isolada do bot/LLM antes que qualquer skill reflexa ou preempção seja conectada. Sem ela, eat/flee/shelter e a generalização da preempção (Plans 02-04) não têm o que consultar.
Output: `src/cognition/reflex.ts` (puro), `src/cognition/reflex.test.ts` (tabela-verdade) e novos limiares validados em `src/config.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-system-1-sobreviv-ncia-reflexa/08-CONTEXT.md
@.planning/phases/08-system-1-sobreviv-ncia-reflexa/08-RESEARCH.md

<interfaces>
<!-- O executor usa estes contratos diretamente — não precisa explorar o codebase. -->

Padrão de função pura testável SEM mock de bot — já estabelecido em src/cognition/arbiter.test.ts:
```typescript
import { test, expect } from 'bun:test'
// arbiter.ts exporta funções puras (arbitrate, highestPriorityGatherTarget); o teste passa
// objetos literais mínimos castados com `as unknown as WorldSnapshot`. Espelhar este estilo.
```

EntityInfo.kind já existe (src/perception/types.ts) — categoria Mineflayer do mob:
```typescript
readonly kind: string  // 'Hostile mobs' | 'Passive mobs' | 'Vehicles' | 'Immobile' | 'Projectiles' | 'UNKNOWN'
```

Limiares existentes em config.ts a ALTERAR (defaults atuais entre parênteses):
```typescript
hungryThreshold: parseInt(process.env.HUNGRY_THRESHOLD || '6', 10),            // linha 146 — flip 6→16
survivalCriticalThreshold: parseFloat(process.env.SURVIVAL_CRITICAL_THRESHOLD || '0.3'),  // linha 89 — flip 0.3→0.5
hostileRadius: parseInt(process.env.HOSTILE_RADIUS || '16', 10),              // linha 142 — MANTER (reusado por ranged ≤16)
hostileDebounceMs: parseInt(process.env.HOSTILE_DEBOUNCE_MS || '800', 10),    // linha 144 — MANTER
```

Esboço de referência do arbitrateReflex (RESEARCH.md Pattern 1, honra D-02/D-03 — adaptar nomes a discrição):
```typescript
export interface ReflexSensors {
  food: number; health: number; oxygen: number
  isNight: boolean
  nearestHostile: { kind: string; name: string; distance: number } | null
  lavaAhead: boolean
  fallAhead: number          // blocos de queda à frente
  cornered: boolean          // A* de fuga deu noPath/timeout (D-16)
}
export interface ReflexDecision { reflex: 'eat'|'flee'|'shelter'|'retreatEnv'|'defend'; lifeCritical: boolean }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar limiares de sobrevivência em config.ts + validação de range + testes</name>
  <read_first>
    - src/config.ts (estado atual — ver linhas 89, 142, 144, 146 e o bloco de validação 207-276)
    - src/config.test.ts (padrão de teste de defaults via import dinâmico)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-RESEARCH.md (D-11..D-14, Runtime State Inventory)
  </read_first>
  <action>
    Em `src/config.ts`, dentro do objeto `config`:

    1. FLIP do default de `hungryThreshold` (linha ~146): trocar `'6'` por `'16'`.
       Comentário: `// D-11: come quando food<=16 (regen para em food<=17; 6 era limiar de sprint, não de saúde)`.
    2. FLIP do default de `survivalCriticalThreshold` (linha ~89): trocar `'0.3'` por `'0.5'`.
       Comentário acrescentado: `// D-12: subido 0.3→0.5 — vida crítica preempta/foge/abriga`.
    3. ADICIONAR no bloco "=== Fase 07.1 ===" (após `idleWakeIntervalMs`, ~linha 148) os novos limiares (todos lidos de env com `||` default, padrão do arquivo):
       ```
       // === Fase 8: System 1 — limiares reflexos (D-11..D-14) ===
       // D-11: histerese de fome — para de comer quando food>=18 (regen natural)
       hungerExitThreshold: parseInt(process.env.HUNGER_EXIT_THRESHOLD || '18', 10),
       // D-12: health<=10 dispara reflexo vida-crítica (foge/abriga); exit health>=14
       healthCriticalThreshold: parseInt(process.env.HEALTH_CRITICAL_THRESHOLD || '10', 10),
       healthExitThreshold: parseInt(process.env.HEALTH_EXIT_THRESHOLD || '14', 10),
       // D-14: afogamento — oxygen<=6 emerge (≈4.5s de margem); exit oxygen>=14
       oxygenEmergeThreshold: parseInt(process.env.OXYGEN_EMERGE_THRESHOLD || '6', 10),
       oxygenExitThreshold: parseInt(process.env.OXYGEN_EXIT_THRESHOLD || '14', 10),
       // D-14: queda perigosa só > 3 blocos (dano = blocos-3)
       fallDangerBlocks: parseInt(process.env.FALL_DANGER_BLOCKS || '3', 10),
       // D-14: lookahead de lava à frente, em blocos
       lavaLookahead: parseInt(process.env.LAVA_LOOKAHEAD || '2', 10),
       // D-13: distâncias graduadas de reação a mob hostil por tipo
       creeperReactDistance: parseInt(process.env.CREEPER_REACT_DISTANCE || '10', 10),
       meleeReactDistance: parseInt(process.env.MELEE_REACT_DISTANCE || '8', 10),
       rangedReactDistance: parseInt(process.env.RANGED_REACT_DISTANCE || '16', 10),
       ```
    4. ADICIONAR no bloco de validação (após linha ~276) os checks de range:
       ```
       // Fase 8: validação dos limiares reflexos
       if (config.hungryThreshold < 0 || config.hungryThreshold > 20) throw new Error(`HUNGRY_THRESHOLD inválido: ${config.hungryThreshold}. Deve estar em [0,20].`)
       if (config.hungerExitThreshold <= config.hungryThreshold || config.hungerExitThreshold > 20) throw new Error(`HUNGER_EXIT_THRESHOLD (${config.hungerExitThreshold}) deve ser > hungryThreshold e <= 20 (histerese).`)
       if (config.healthCriticalThreshold < 0 || config.healthCriticalThreshold > 20) throw new Error(`HEALTH_CRITICAL_THRESHOLD inválido: ${config.healthCriticalThreshold}. Deve estar em [0,20].`)
       if (config.healthExitThreshold <= config.healthCriticalThreshold || config.healthExitThreshold > 20) throw new Error(`HEALTH_EXIT_THRESHOLD (${config.healthExitThreshold}) deve ser > healthCriticalThreshold e <= 20.`)
       if (config.oxygenEmergeThreshold < 0 || config.oxygenEmergeThreshold > 20) throw new Error(`OXYGEN_EMERGE_THRESHOLD inválido: ${config.oxygenEmergeThreshold}. Deve estar em [0,20].`)
       if (config.oxygenExitThreshold <= config.oxygenEmergeThreshold || config.oxygenExitThreshold > 20) throw new Error(`OXYGEN_EXIT_THRESHOLD (${config.oxygenExitThreshold}) deve ser > oxygenEmergeThreshold e <= 20.`)
       if (config.fallDangerBlocks < 0) throw new Error(`FALL_DANGER_BLOCKS inválido: ${config.fallDangerBlocks}. Deve ser >= 0.`)
       if (config.lavaLookahead < 1 || config.lavaLookahead > 8) throw new Error(`LAVA_LOOKAHEAD inválido: ${config.lavaLookahead}. Deve estar em [1,8].`)
       for (const [name, v] of [['CREEPER_REACT_DISTANCE', config.creeperReactDistance], ['MELEE_REACT_DISTANCE', config.meleeReactDistance], ['RANGED_REACT_DISTANCE', config.rangedReactDistance]] as const) {
         if (v < 1 || v > 64) throw new Error(`${name} inválido: ${v}. Deve estar em [1,64].`)
       }
       ```

    Em `src/config.test.ts`, ADICIONAR um novo `describe('config — limiares reflexos Fase 8', ...)` com `it(...)` que importa `./config` dinamicamente e afirma:
    - `expect(config.hungryThreshold).toBe(16)`
    - `expect(config.survivalCriticalThreshold).toBe(0.5)`
    - `expect(config.healthCriticalThreshold).toBe(10)`
    - `expect(config.oxygenEmergeThreshold).toBe(6)`
    - `expect(config.fallDangerBlocks).toBe(3)`
    - `expect(config.lavaLookahead).toBe(2)`
    - `expect(config.creeperReactDistance).toBe(10)`
    - `expect(config.rangedReactDistance).toBe(16)`
  </action>
  <verify>
    <automated>bun test src/config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "hungryThreshold.*'16'" src/config.ts` retorna 1 match (flip aplicado)
    - `grep -n "survivalCriticalThreshold.*'0.5'" src/config.ts` retorna 1 match
    - `grep -n "healthCriticalThreshold\|oxygenEmergeThreshold\|fallDangerBlocks\|lavaLookahead\|creeperReactDistance" src/config.ts` retorna >= 5 matches
    - `grep -n "HEALTH_EXIT_THRESHOLD.*deve ser >" src/config.ts` retorna 1 match (validação de histerese)
    - `bun test src/config.test.ts` passa com os novos asserts (hungryThreshold=16, survivalCriticalThreshold=0.5)
  </acceptance_criteria>
  <done>config.ts tem os 9 novos limiares + flips de 2 existentes, todos com validação de range/histerese, e config.test.ts cobre os defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar reflex.ts (arbitrateReflex pura) + tabela-verdade reflex.test.ts</name>
  <read_first>
    - src/cognition/arbiter.test.ts (padrão de teste de função pura SEM mock de bot — espelhar)
    - src/perception/types.ts (EntityInfo.kind — categorias Mineflayer)
    - src/config.ts (já editado na Task 1 — os limiares que arbitrateReflex consulta)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-RESEARCH.md (Pattern 1: array de guards, winner-take-all; D-01/D-02/D-03/D-13/D-16)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-CONTEXT.md (D-03 ordem de gravidade; D-15/D-16 fugir vs revidar; D-17 abrigo só noite+ameaça)
  </read_first>
  <behavior>
    Tabela-verdade de arbitrateReflex (SURV-01..05), função PURA, sem mock de bot:
    - SURV-04/D-03: lavaAhead=true OU oxygen<=6 → {reflex:'retreatEnv', lifeCritical:true}. É a guarda de MAIOR prioridade — vence até hostil (lavaAhead + hostil próximo ainda devolve retreatEnv).
    - SURV-02/D-13: nearestHostile creeper dist<=10 → reflexo dispara; melee (zombie) dist<=8 → dispara; skeleton (ranged) dist<=16 → dispara; cada um ACIMA da sua distância → NÃO dispara (null se nada mais).
    - SURV-02/D-15/D-16: hostil dentro do alcance E cornered=false → {reflex:'flee', lifeCritical:true}; hostil no alcance E cornered=true → {reflex:'defend', lifeCritical:true}.
    - SURV-03/D-17: shelter NÃO dispara por anoitecer sozinho (isNight=true, sem hostil, food ok → null). Abrigo é tratado como variação do caminho hostil quando cornered + noite (decidir representação; mínimo: cornered+night+hostile pode mapear 'shelter' OU 'defend' — documentar a escolha no comentário; o teste deve fixar o comportamento escolhido).
    - SURV-01/D-02: food<=16 e SEM perigos vida-crítica → {reflex:'eat', lifeCritical:false} (fome NUNCA preempta). Com hostil presente, o hostil (lifeCritical=true) vence a fome.
    - SURV-04 queda/D-14: fallAhead > config.fallDangerBlocks (3) → {reflex:'retreatEnv', lifeCritical:true}; fallAhead <= 3 → não dispara por queda.
    - SURV-05/D-03 ordenação: com lava + hostil + queda + fome simultâneos, vence retreatEnv (ambiental). Sem lava mas com hostil + queda + fome, vence o hostil. Sem ambiental nem hostil, com queda + fome, vence queda (retreatEnv). Só fome → eat.
    - Estado totalmente saudável (food=20, health=20, oxygen=20, sem hostil, sem lava, sem queda) → arbitrateReflex devolve null.
  </behavior>
  <action>
    Criar `src/cognition/reflex.ts` — módulo PURO (importa só `config`, NUNCA `Bot`):

    1. Exportar interfaces `ReflexSensors` e `ReflexDecision` (ver bloco <interfaces> do contexto). `ReflexDecision.reflex` é a união `'eat'|'flee'|'shelter'|'retreatEnv'|'defend'`.
    2. Exportar um helper puro `hostileThreatDistance(kind: string, name: string): number` que devolve a distância-limite de reação por tipo (D-13): se `name` contém 'creeper' → `config.creeperReactDistance` (10); se `name` contém 'skeleton' → `config.rangedReactDistance` (16); senão, qualquer `kind === 'Hostile mobs'` → `config.meleeReactDistance` (8). Retorna 0 se kind não-hostil.
    3. Exportar um helper puro `isHostileThreat(s: ReflexSensors): boolean` = `s.nearestHostile != null && s.nearestHostile.distance <= hostileThreatDistance(s.nearestHostile.kind, s.nearestHostile.name)`.
    4. Exportar `REFLEX_GUARDS: Array<(s: ReflexSensors) => ReflexDecision | null>` ORDENADO por gravidade (D-03):
       ```
       (s) => (s.lavaAhead || s.oxygen <= config.oxygenEmergeThreshold) ? { reflex: 'retreatEnv', lifeCritical: true } : null,
       (s) => isHostileThreat(s) ? (s.cornered ? { reflex: 'defend', lifeCritical: true } : { reflex: 'flee', lifeCritical: true }) : null,
       (s) => (s.fallAhead > config.fallDangerBlocks) ? { reflex: 'retreatEnv', lifeCritical: true } : null,
       (s) => (s.health <= config.healthCriticalThreshold) ? { reflex: 'flee', lifeCritical: true } : null,   // vida crítica sem ameaça localizada → recua/foge
       (s) => (s.food <= config.hungryThreshold) ? { reflex: 'eat', lifeCritical: false } : null,
       ```
       NOTA D-17: shelter não é uma guarda isolada por anoitecer. Se for representar abrigo, fazê-lo SOMENTE quando `isHostileThreat(s) && s.cornered && s.isNight` → mapear para `{reflex:'shelter', lifeCritical:true}` ANTES da guarda de defend (ajustar a guarda hostil para checar `s.isNight && s.cornered → 'shelter'` senão o caminho cornered→defend). Documentar a escolha em comentário e FIXAR no teste.
    5. Exportar `arbitrateReflex(s: ReflexSensors): ReflexDecision | null` = percorre `REFLEX_GUARDS`, devolve o primeiro não-nulo (winner-take-all, D-01), senão null.

    Criar `src/cognition/reflex.test.ts` (`import { test, expect } from 'bun:test'`) cobrindo TODOS os casos do bloco <behavior> acima. Construir `ReflexSensors` literais (objeto puro, sem cast de bot). Cada caso = 1 `test(...)`. Mínimo 12 testes cobrindo SURV-01..05 + estado saudável→null + cada distância graduada de mob.
  </action>
  <verify>
    <automated>bun test src/cognition/reflex.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function arbitrateReflex" src/cognition/reflex.ts` retorna 1 match
    - `grep -n "export const REFLEX_GUARDS\|export interface ReflexSensors\|export interface ReflexDecision" src/cognition/reflex.ts` retorna 3 matches
    - `grep -n "import type { Bot }" src/cognition/reflex.ts` retorna 0 matches (módulo puro, não importa bot)
    - `grep -c "test(" src/cognition/reflex.test.ts` retorna >= 12
    - `bun test src/cognition/reflex.test.ts` passa (todos os casos da tabela-verdade verdes)
    - Caso ordenação D-03 testado: sensors com lavaAhead=true E nearestHostile creeper dist=2 devolve reflex='retreatEnv' (ambiental vence hostil)
  </acceptance_criteria>
  <done>reflex.ts expõe arbitrateReflex puro winner-take-all com ordenação de gravidade D-03 e flag lifeCritical (D-02); reflex.test.ts é tabela-verdade verde sem mock de bot.</done>
</task>

</tasks>

<verification>
- `bun test src/cognition/reflex.test.ts src/config.test.ts` verde.
- arbitrateReflex é função pura (não importa Bot) — confirma a "função pura no driver" do D-01.
- Ordem de gravidade D-03 (ambiental > hostil > queda > fome) provada por teste.
- Flag lifeCritical correta: true para ambiental/hostil/queda/health; false para fome (D-02).
</verification>

<success_criteria>
- arbitrateReflex devolve o reflexo vencedor por gravidade ou null, testável por tabela-verdade sem bot.
- config.ts tem todos os limiares de D-11..D-14 com validação de range e histerese enter/exit.
- Zero dependências novas; nenhuma importação de Bot em reflex.ts.
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-system-1-sobreviv-ncia-reflexa/08-01-SUMMARY.md`.
</output>
