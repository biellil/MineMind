---
phase: 08-system-1-sobreviv-ncia-reflexa
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/cognition/trigger-bus.ts
  - src/cognition/nodes.ts
  - src/cognition/loop.ts
autonomous: false
requirements: [SURV-01, SURV-02, SURV-03, SURV-04, SURV-05]
must_haves:
  truths:
    - "O TriggerBus emite novos gatilhos lifeCritical (healthCritical/drowning/lavaAhead/fallAhead) por edge-detection, sem flood (physicsTick com histerese)"
    - "O nó execute preempta a skill em curso para TODOS os gatilhos lifeCritical (não só hostileNearby), chamando setGoal(null) ANTES de skillAbort.abort()"
    - "O driver consulta arbitrateReflex e dispara a skill reflexa vencedora; reflexos lifeCritical=false (eat/shelter) só rodam no actionFinished"
    - "O reflexo é registrado como MemEvent grounded (debounced/coalesced) pelo mesmo pipeline da Fase 7"
    - "O reflexo NUNCA chama o LLM nem toca inFlight — o [reflect] continua disparando (gate D-20)"
  artifacts:
    - path: "src/cognition/trigger-bus.ts"
      provides: "Novos gatilhos lifeCritical via physicsTick edge-detection"
      contains: "physicsTick"
    - path: "src/cognition/nodes.ts"
      provides: "Preempção generalizada para todos os gatilhos lifeCritical + setGoal(null)"
      contains: "setGoal(null)"
    - path: "src/cognition/loop.ts"
      provides: "Fiação do System 1 (arbitrateReflex + dispatch de skill reflexa) ao lado do TriggerBus"
      contains: "arbitrateReflex"
  key_links:
    - from: "src/cognition/loop.ts"
      to: "src/cognition/reflex.ts"
      via: "import { arbitrateReflex }"
      pattern: "arbitrateReflex"
    - from: "src/cognition/nodes.ts"
      to: "bot.pathfinder.setGoal"
      via: "preempção forçada D-07"
      pattern: "setGoal\\(null\\)"
    - from: "src/cognition/loop.ts"
      to: "src/skills/index.ts"
      via: "skillRegistry[reflex] dispatch"
      pattern: "skillRegistry"
---

<objective>
INTEGRAÇÃO (Wave 3): conectar a POLÍTICA reflexa ao MECANISMO existente. Três edits cirúrgicos: (1) adicionar os gatilhos `lifeCritical` faltantes no `TriggerBus` (healthCritical/drowning/lavaAhead/fallAhead via `physicsTick` edge-detection); (2) generalizar a preempção no nó `execute` de só `hostileNearby` para TODOS os gatilhos `lifeCritical`, com `setGoal(null)` ANTES do abort (D-07); (3) fiar o System 1 no driver (`loop.ts`): construir `ReflexSensors` do bot, consultar `arbitrateReflex`, despachar a skill reflexa vencedora pelo `skillRegistry`, e registrar o resultado como `MemEvent` grounded (debounced).

Purpose: É o plano que torna o System 1 OBSERVÁVEL e ATIVO. Sem ele, as skills (Plans 02/03) e a arbitragem (Plan 01) existem mas nada as dispara. Resolve as Open Questions #1 (fronteira arbitragem vs abort físico) e #2 (grounding vital local) da RESEARCH. Termina com um checkpoint humano AO VIVO (D-20) — o re-teste do `[reflect]`.
Output: trigger-bus.ts (novos gatilhos), nodes.ts (preempção generalizada), loop.ts (System 1 fiado), e o gate D-20 confirmado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-system-1-sobreviv-ncia-reflexa/08-CONTEXT.md
@.planning/phases/08-system-1-sobreviv-ncia-reflexa/08-RESEARCH.md

<interfaces>
<!-- Contratos que o executor usa diretamente. -->

arbitrateReflex (criado no Plan 01, src/cognition/reflex.ts):
```typescript
export interface ReflexSensors {
  food: number; health: number; oxygen: number; isNight: boolean
  nearestHostile: { kind: string; name: string; distance: number } | null
  lavaAhead: boolean; fallAhead: number; cornered: boolean
}
export interface ReflexDecision { reflex: 'eat'|'flee'|'shelter'|'retreatEnv'|'defend'; lifeCritical: boolean }
export function arbitrateReflex(s: ReflexSensors): ReflexDecision | null
```

TriggerBus ATUAL (src/cognition/trigger-bus.ts) — padrão de edge-detector a espelhar:
```typescript
export type TriggerEvent = 'actionFinished'|'nightFell'|'dayBroke'|'hostileNearby'|'stuck'|'hungry'
// setupMineflayerListeners(bot, cfg): () => void  — registra listeners, retorna cleanup
// _wasDay/_lastFood/_hostileDebounce = estado de borda; cleanup remove TODOS os listeners
```

Preempção ATUAL no nó execute (src/cognition/nodes.ts:257-330) — generalizar:
```typescript
const skillAbort = new AbortController()
const onHostileNearby = () => { log(...); skillAbort.abort('hostileNearby') }
triggerBus.once('hostileNearby', onHostileNearby)
// ... try/catch/finally ...
finally {
  triggerBus.off('hostileNearby', onHostileNearby)   // remove ANTES do abort (Pitfall 6)
  skillAbort.abort()
}
```

Driver ATUAL (src/cognition/loop.ts) — pontos de fiação:
```typescript
// linha ~75-81: triggerBus criado + setupMineflayerListeners(bot, triggerCfg)
// linha ~287: const wakeReason = await makeParkPromise(triggerBus, sessionAbort.signal, nextWakeMs)
// O System 1 (eat/shelter lifeCritical=false) deve rodar quando wakeReason indica idle/actionFinished.
// holder.memory + push (src/memory/shortTerm) — pipeline de MemEvent (mesma forma do execute node)
```

MemEvent grounded (mesma forma usada em nodes.ts:290-300) — o reflexo emite assim:
```typescript
holder.memory = push(holder.memory, {
  type: 'action', skill, target, outcome, observed, expected,
  result: outcome === 'success' ? 'success' : 'failure', reason, timestamp,
})
```

bot sensores (RESEARCH): bot.food, bot.health, bot.oxygenLevel, bot.time.timeOfDay (<13000=dia),
bot.entity.position, bot.blockAt(Vec3), bot.nearestEntity, EntityInfo.kind==='Hostile mobs'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar gatilhos lifeCritical no TriggerBus (healthCritical/drowning/lavaAhead/fallAhead via physicsTick edge-detection)</name>
  <files>src/cognition/trigger-bus.ts</files>
  <read_first>
    - src/cognition/trigger-bus.ts (estado de borda _wasDay/_lastFood; padrão de edge-detector com histerese; cleanup)
    - src/config.ts (limiares do Plan 01: healthCriticalThreshold/healthExitThreshold, oxygenEmergeThreshold/oxygenExitThreshold, fallDangerBlocks, lavaLookahead)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-RESEARCH.md (Pitfall 5: sensor ambiental sem guarda; D-09 physicsTick + blockAt à frente/abaixo; D-14 limiares)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-CONTEXT.md (D-09/D-14 sensores ambientais)
  </read_first>
  <action>
    Em `src/cognition/trigger-bus.ts`:

    1. ESTENDER `TriggerEvent`: adicionar `| 'healthCritical' | 'drowning' | 'lavaAhead' | 'fallAhead'`.
    2. ESTENDER `TriggerConfig`: adicionar campos lidos do Plan 01:
       `healthCriticalThreshold: number; healthExitThreshold: number; oxygenEmergeThreshold: number; oxygenExitThreshold: number; fallDangerBlocks: number; lavaLookahead: number`.
    3. ADICIONAR estado de borda privado (espelhar `_wasDay`/`_lastFood`):
       `private _wasHealthCritical = false; private _wasDrowning = false; private _wasLavaAhead = false; private _wasFallAhead = false`.
    4. Em `setupMineflayerListeners`, ADICIONAR um edge-detector `onPhysicsTick` assinado em `bot.on('physicsTick', onPhysicsTick)` (alta frequência — SÓ emite ao CRUZAR a borda, nunca a cada tick — Pitfall 5):
       ```
       const onPhysicsTick = () => {
         // health crítico (enter <= critical / exit >= exitThreshold) — histerese
         const healthCrit = bot.health <= cfg.healthCriticalThreshold
         if (healthCrit && !this._wasHealthCritical) this.emit('healthCritical', { health: bot.health })
         if (bot.health >= cfg.healthExitThreshold) this._wasHealthCritical = false
         else if (healthCrit) this._wasHealthCritical = true

         // afogamento (oxygen <= emerge / exit >= exitThreshold)
         const drown = bot.oxygenLevel <= cfg.oxygenEmergeThreshold
         if (drown && !this._wasDrowning) this.emit('drowning', { oxygen: bot.oxygenLevel })
         if (bot.oxygenLevel >= cfg.oxygenExitThreshold) this._wasDrowning = false
         else if (drown) this._wasDrowning = true

         // lava à frente (lookahead cfg.lavaLookahead blocos na direção da mira) — barato (1-2 blockAt)
         const lava = isLavaAhead(bot, cfg.lavaLookahead)
         if (lava && !this._wasLavaAhead) this.emit('lavaAhead')
         this._wasLavaAhead = lava

         // queda iminente (> cfg.fallDangerBlocks de ar abaixo à frente)
         const fall = fallDepthAhead(bot) > cfg.fallDangerBlocks
         if (fall && !this._wasFallAhead) this.emit('fallAhead')
         this._wasFallAhead = fall
       }
       bot.on('physicsTick', onPhysicsTick)
       ```
       Implementar helpers de módulo `isLavaAhead(bot, lookahead)` e `fallDepthAhead(bot)`:
       - `isLavaAhead`: calcula a posição à frente do bot (usar `bot.entity.position` + vetor de direção do yaw via `Math.sin/cos`, ou `bot.entity.position.offset` na direção; manter simples — 1 a `lookahead` blocos à frente no nível dos pés/cabeça) e checa `bot.blockAt(p)?.name === 'lava'`. Retorna boolean. Guardas null-safe (bot.entity pode ser null em morte/void — retornar false).
       - `fallDepthAhead`: conta quantos blocos de `air` há abaixo da posição à frente do bot (até um cap de ~6). Retorna número. Null-safe.
    5. No retorno de cleanup, ADICIONAR `bot.off('physicsTick', onPhysicsTick)`.

    NOTA null-safety: `bot.entity` pode ser null (morte/void). Todos os helpers devem retornar false/0 quando `!bot.entity`.
  </action>
  <verify>
    <automated>bun test src/cognition/loop.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "'healthCritical'\|'drowning'\|'lavaAhead'\|'fallAhead'" src/cognition/trigger-bus.ts` retorna >= 4 matches (no type TriggerEvent)
    - `grep -n "physicsTick" src/cognition/trigger-bus.ts` retorna >= 2 matches (on + off)
    - `grep -n "_wasHealthCritical\|_wasDrowning\|_wasLavaAhead\|_wasFallAhead" src/cognition/trigger-bus.ts` retorna >= 4 matches (estado de borda)
    - `grep -n "bot.off('physicsTick'" src/cognition/trigger-bus.ts` retorna 1 match (cleanup)
    - `grep -n "if (!bot.entity)" src/cognition/trigger-bus.ts` retorna >= 1 match (null-safety morte/void)
    - `bun test src/cognition/loop.smoke.test.ts` continua verde (TriggerBus não quebrou a fiação)
  </acceptance_criteria>
  <done>TriggerBus emite 4 novos gatilhos lifeCritical por edge-detection em physicsTick com histerese e null-safety; cleanup remove o listener.</done>
</task>

<task type="auto">
  <name>Task 2: Generalizar a preempção no nó execute (todos os gatilhos lifeCritical + setGoal(null) D-07)</name>
  <files>src/cognition/nodes.ts</files>
  <read_first>
    - src/cognition/nodes.ts (linhas 256-330 — AbortController, onHostileNearby, try/catch/finally; Pitfall 6: remover listener antes do abort)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-RESEARCH.md (Pattern 2: generalização da preempção; D-07 setGoal(null) ANTES do abort; Pitfall 6 registrar N / remover N)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-CONTEXT.md (D-02 lifeCritical preempta; D-07 setGoal(null) não stop())
  </read_first>
  <action>
    Em `src/cognition/nodes.ts`, no nó `execute` (~linha 257-330), SUBSTITUIR o listener único `onHostileNearby` por uma generalização para TODOS os gatilhos lifeCritical:

    1. Definir o array de gatilhos lifeCritical (constante de módulo perto do topo de nodes.ts):
       ```typescript
       const LIFE_CRITICAL_TRIGGERS = ['hostileNearby', 'healthCritical', 'drowning', 'lavaAhead', 'fallAhead'] as const
       ```
    2. SUBSTITUIR (linhas ~261-265):
       ```typescript
       const onHostileNearby = () => { log(`preemptando ${skill} — hostileNearby recebido`); skillAbort.abort('hostileNearby') }
       triggerBus.once('hostileNearby', onHostileNearby)
       ```
       por uma fábrica de listeners registrados em loop, cada um chamando `setGoal(null)` ANTES do abort (D-07):
       ```typescript
       const preemptListeners: Array<[string, () => void]> = []
       for (const trig of LIFE_CRITICAL_TRIGGERS) {
         const fn = () => {
           log(`preemptando ${skill} — ${trig} (lifeCritical)`)
           // D-07: parada FORÇADA imediata (não stop() gracioso) ANTES do abort do signal.
           try { bot.pathfinder.setGoal(null) } catch { /* pathfinder pode não estar carregado */ }
           skillAbort.abort(trig)
         }
         triggerBus.once(trig, fn)
         preemptListeners.push([trig, fn])
       }
       ```
    3. No bloco `finally` (~linha 323-326), SUBSTITUIR a remoção única por remoção de TODOS (Pitfall 6 — remover ANTES do abort idempotente):
       ```typescript
       finally {
         for (const [trig, fn] of preemptListeners) triggerBus.off(trig, fn)
         skillAbort.abort()  // cleanup idempotente
       }
       ```
    4. Atualizar o comentário acima do bloco (linha ~260) de "hostileNearby é o gatilho de preempção v1 (D-18 — Phase 8 adicionará mais gatilhos)" para refletir que a Fase 8 generalizou para todos os lifeCritical com setGoal(null) (D-02/D-07/Pattern 2).
  </action>
  <verify>
    <automated>bun test src/cognition/loop.smoke.test.ts src/cognition/loop.death.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "LIFE_CRITICAL_TRIGGERS" src/cognition/nodes.ts` retorna >= 2 matches (definição + uso)
    - `grep -n "'healthCritical', 'drowning', 'lavaAhead', 'fallAhead'" src/cognition/nodes.ts` retorna 1 match
    - `grep -n "setGoal(null)" src/cognition/nodes.ts` retorna 1 match (D-07, ANTES do abort)
    - `grep -n "for (const \[trig, fn\] of preemptListeners)" src/cognition/nodes.ts` retorna 1 match (cleanup de N listeners)
    - `grep -n "triggerBus.once('hostileNearby'" src/cognition/nodes.ts` retorna 0 matches (listener único substituído)
    - `bun test src/cognition/loop.smoke.test.ts src/cognition/loop.death.test.ts` verde
  </acceptance_criteria>
  <done>O nó execute preempta para os 5 gatilhos lifeCritical com setGoal(null) forçado antes do abort; todos os listeners são removidos no finally (sem orphan).</done>
</task>

<task type="auto">
  <name>Task 3: Fiar o System 1 no driver (arbitrateReflex + dispatch de skill reflexa + MemEvent grounded debounced)</name>
  <files>src/cognition/loop.ts</files>
  <read_first>
    - src/cognition/loop.ts (fiação do triggerBus ~75-81; o while do driver; makeParkPromise ~287; onde inFlight/deliberação ficam — NÃO tocar inFlight)
    - src/cognition/reflex.ts (arbitrateReflex + ReflexSensors — criado no Plan 01)
    - src/skills/index.ts (skillRegistry com eat/flee/shelter/attack — Plans 02/03)
    - src/memory/shortTerm.ts (push — para o MemEvent grounded)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-RESEARCH.md (Open Q#1 onde roda arbitrateReflex; Pitfall 4 regressão B1 do [reflect]; D-18 re-percebe do zero; D-19 MemEvent debounced)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-CONTEXT.md (D-02 lifeCritical=false roda no actionFinished; D-18/D-19)
  </read_first>
  <action>
    Em `src/cognition/loop.ts`, fiar o System 1 ao lado do TriggerBus (resolve Open Question #1: arbitrateReflex é função pura reutilizável; o ABORT físico já está no execute (Task 2); aqui o driver despacha os reflexos `lifeCritical=false` quando idle/actionFinished):

    1. Imports: `import { arbitrateReflex, type ReflexSensors } from './reflex'`; `import { skillRegistry } from '../skills/index'`; `import { push } from '../memory/shortTerm'`.
    2. Atualizar `triggerCfg` (~linha 76-80) para passar os novos campos lidos da config (Task 1 estendeu TriggerConfig):
       `healthCriticalThreshold: config.healthCriticalThreshold, healthExitThreshold: config.healthExitThreshold, oxygenEmergeThreshold: config.oxygenEmergeThreshold, oxygenExitThreshold: config.oxygenExitThreshold, fallDangerBlocks: config.fallDangerBlocks, lavaLookahead: config.lavaLookahead`.
    3. Adicionar um helper de módulo `buildReflexSensors(bot): ReflexSensors` que lê o bot diretamente (o System 1 vive FORA do grafo, então PODE ler o bot — D-01 "função pura no driver" refere-se à arbitragem, não à coleta de sensores):
       ```typescript
       function buildReflexSensors(bot: Bot): ReflexSensors {
         const e = bot.entity
         const hostile = e ? bot.nearestEntity((x) => (x as any).kind === 'Hostile mobs') : null
         return {
           food: bot.food ?? 20, health: bot.health ?? 20, oxygen: bot.oxygenLevel ?? 20,
           isNight: bot.time ? bot.time.timeOfDay >= 13000 : false,
           nearestHostile: hostile && e ? { kind: (hostile as any).kind ?? '', name: hostile.name ?? '', distance: e.position.distanceTo(hostile.position) } : null,
           lavaAhead: false,   // o gatilho lavaAhead já preempta no execute (Task 2); aqui só p/ eat/shelter idle
           fallAhead: 0,
           cornered: false,    // predicado simples D-16 — pode ser refinado; default false (foge por padrão)
         }
       }
       ```
    4. Adicionar dispatch de reflexo idle (lifeCritical=false) no ponto do `wakeReason` (após `makeParkPromise`, ~linha 287). SOMENTE quando idle/actionFinished e o bot está vivo:
       ```typescript
       // Fase 8 (D-02/D-18): System 1 idle — reflexos lifeCritical=false (eat/shelter) rodam SÓ quando
       // o bot está ocioso, NUNCA interrompendo a deliberação. lifeCritical=true já preempta no execute (Task 2).
       if (alive && bot.entity) {
         const decision = arbitrateReflex(buildReflexSensors(bot))
         if (decision && !decision.lifeCritical && (decision.reflex === 'eat' || decision.reflex === 'shelter')) {
           await runReflex(bot, holder, decision.reflex)
         }
       }
       ```
    5. Adicionar `runReflex` (módulo) que despacha a skill pelo `skillRegistry`, faz grounding e registra MemEvent DEBOUNCED (D-19 — não inundar a memória com re-triggers). Usar um timestamp de último-reflexo por tipo (variável de sessão `let lastReflexAt: Record<string, number> = {}` + um intervalo mínimo, ex. 3000ms):
       ```typescript
       async function runReflex(bot, holder, reflex): Promise<void> {
         const skill = skillRegistry[reflex]
         if (!skill) return
         try {
           const result = await skill(bot, {})   // sem signal — reflexo idle não é preemptado por si mesmo
           // D-19: MemEvent grounded debounced/coalesced
           const nowTs = Date.now()
           if (nowTs - (lastReflexAt[reflex] ?? 0) > 3000) {
             lastReflexAt[reflex] = nowTs
             holder.memory = push(holder.memory, {
               type: 'action', skill: reflex, target: 'reflex',
               outcome: result.outcome, observed: result.observed, expected: result.expected,
               result: result.outcome === 'success' ? 'success' : 'failure',
               reason: result.reason, timestamp: nowTs,
             })
             console.log(`[reflex] ${reflex} ${result.outcome} (${result.observed}/${result.expected})`)
           }
         } catch (err) {
           console.error(`[reflex] ${reflex} falhou:`, err instanceof Error ? err.message : err)
         }
       }
       ```
       Declarar `let lastReflexAt: Record<string, number> = {}` dentro de `startCognitiveLoop` (escopo de sessão) OU passar como parâmetro — manter por sessão para o debounce funcionar.
    6. CRÍTICO (Pitfall 4 / D-18 / D-20): `runReflex` NUNCA chama `maybeDeliberate`/provider/LLM e NUNCA toca `inFlight`. O reflexo idle roda ANTES do próximo `graph.invoke` do while — ao terminar, o while continua e re-percebe (re-percebe do zero, D-18). NÃO bloquear o caminho do `.then((ran) => …)` da reflexão.

    NOTA sobre estrutura: ajustar a posição EXATA do dispatch para depois do `makeParkPromise` mas antes de `if (wakeReason === 'abort') break` OU logo após — desde que (a) só rode quando vivo, (b) lifeCritical=false, (c) não interfira no inFlight. O executor decide o ponto exato lendo o while atual; o requisito é o COMPORTAMENTO acima.
  </action>
  <verify>
    <automated>bun test src/cognition/loop.smoke.test.ts src/cognition/loop.phase3.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import { arbitrateReflex" src/cognition/loop.ts` retorna 1 match
    - `grep -n "buildReflexSensors" src/cognition/loop.ts` retorna >= 2 matches (def + uso)
    - `grep -n "runReflex" src/cognition/loop.ts` retorna >= 2 matches (def + uso)
    - `grep -n "!decision.lifeCritical" src/cognition/loop.ts` retorna 1 match (idle só p/ lifeCritical=false, D-02)
    - `grep -n "lastReflexAt" src/cognition/loop.ts` retorna >= 2 matches (debounce D-19)
    - `grep -n "maybeDeliberate\|provider" src/cognition/loop.ts` — o corpo de runReflex NÃO contém nenhuma destas chamadas (inspeção: runReflex só usa skillRegistry/push)
    - `grep -n "healthCriticalThreshold\|oxygenEmergeThreshold" src/cognition/loop.ts` retorna >= 1 match (triggerCfg estendido)
    - `bun test src/cognition/loop.smoke.test.ts src/cognition/loop.phase3.smoke.test.ts` verde
  </acceptance_criteria>
  <done>O driver consulta arbitrateReflex, despacha eat/shelter quando idle (lifeCritical=false), registra MemEvent grounded debounced (D-19), e NUNCA toca o LLM/inFlight (preserva o [reflect], D-18).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Gate D-20 — re-teste AO VIVO do [reflect] + sobrevivência reflexa</name>
  <files>(nenhum — checkpoint de verificação humana, sem produção de código)</files>
  <what-built>
    O System 1 completo está fiado: arbitrateReflex no driver, preempção generalizada lifeCritical no execute (setGoal(null)), skills eat/flee/shelter/attack registradas, e MemEvent grounded dos reflexos. A camada reflexa muda QUANDO o lock inFlight do LLM fica livre — o critério #4 da fase (D-20) exige confirmar AO VIVO que a regressão B1 não reapareceu.
  </what-built>
  <action>
    CHECKPOINT DE VERIFICAÇÃO HUMANA — não há código a escrever (D-20 é gate, não tarefa automatizável). O agente, ao chegar nesta task: (1) roda `bun test` (suite completa) e confirma verde; (2) apresenta ao usuário os passos de <how-to-verify> abaixo; (3) PAUSA e aguarda o sinal de resume. Não prosseguir nem marcar a fase concluída sem o "approved" do usuário.
  </action>
  <how-to-verify>
    Pré-requisitos: servidor Minecraft Java 1.21.4 (offline-mode) rodando + LM Studio com modelo de chat carregado (Developer tab → server ON).

    1. ATENÇÃO ao .env (RESEARCH Runtime State Inventory): se houver `HUNGRY_THRESHOLD=6` ou `SURVIVAL_CRITICAL_THRESHOLD=0.3` no `.env`, REMOVER/atualizar — senão o flip de default do Plan 01 não tem efeito. Confirmar que `.env` não fixa os valores antigos.
    2. `bun start` e conectar ao servidor. Observar o log.
    3. [reflect] (gate primário D-20): deixar o bot rodar ~10-15 min com atividade. CONFIRMAR que aparece `[reflect] reflexão executada` no log periodicamente (a regressão B1 = ausência total deste log). Se NÃO aparecer, é regressão — reportar.
    4. SURV-01 (eat): reduzir a fome do bot (esperar/usar comando do servidor para baixar food <=16 com comida no inventário). CONFIRMAR log `[reflex] eat success` e que o bot comeu (food subiu).
    5. SURV-02 (flee/defend): spawnar/atrair um mob hostil para perto (creeper/zombie). CONFIRMAR preempção (`preemptando … (lifeCritical)`) e que o bot foge (ou dá 1 golpe se encurralado).
    6. SURV-04 (ambiental): aproximar o bot de lava ou de uma queda > 3 blocos. CONFIRMAR `lavaAhead`/`fallAhead` preempta e o bot recua (não anda para a lava/abismo).
    7. SURV-05: confirmar que durante uma preempção reflexa o bot NÃO travou esperando o LLM (a parada foi imediata via setGoal(null)).
  </how-to-verify>
  <verify>
    <automated>bun test</automated>
  </verify>
  <acceptance_criteria>
    - `bun test` (suite completa) verde antes de apresentar o checkpoint
    - Usuário confirmou AO VIVO: `[reflect] reflexão executada` aparece no log (regressão B1 não reapareceu)
    - Usuário confirmou pelo menos um reflexo de sobrevivência funcionando (eat/flee/ambiental) com preempção imediata
  </acceptance_criteria>
  <done>Suite `bun test` verde E o usuário respondeu "approved" confirmando AO VIVO: [reflect] dispara, reflexos de sobrevivência funcionam, e a preempção é imediata (sem travar no LLM).</done>
  <resume-signal>Digite "approved" se o [reflect] dispara AO VIVO e os reflexos de sobrevivência funcionam; senão descreva qual reflexo/comportamento falhou (com trecho do log).</resume-signal>
</task>

</tasks>

<verification>
- `bun test` (suite completa) verde — nenhum teste existente quebrado pela integração.
- O nó execute preempta para os 5 gatilhos lifeCritical com setGoal(null) (D-07).
- O driver despacha eat/shelter SÓ quando idle (lifeCritical=false, D-02) e registra MemEvent grounded debounced (D-19).
- runReflex não chama LLM/inFlight (Pitfall 4 / D-18).
- Gate D-20 AO VIVO: [reflect] confirmado disparando após o System 1 (verificação humana).
</verification>

<success_criteria>
- SURV-04/SURV-05: gatilhos ambientais (lava/queda/afogamento/health) preemptam a ação em curso em sub-segundo via setGoal(null), sem tocar o LLM.
- SURV-01/SURV-02/SURV-03: eat/flee/shelter/defend disparados pela arbitragem; idle vs preempção respeitando lifeCritical (D-02).
- D-18/D-19: reflexo re-percebe do zero e é registrado como MemEvent grounded; D-20 gate AO VIVO confirmado.
- Zero dependência nova; o [reflect] não regrediu.
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-system-1-sobreviv-ncia-reflexa/08-04-SUMMARY.md`.
</output>
