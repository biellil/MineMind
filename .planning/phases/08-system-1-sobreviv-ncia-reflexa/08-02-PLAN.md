---
phase: 08-system-1-sobreviv-ncia-reflexa
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/skills/eat.ts
  - src/skills/eat.test.ts
  - src/skills/attack.ts
  - src/skills/attack.test.ts
  - src/skills/index.ts
autonomous: true
requirements: [SURV-01, SURV-02]
must_haves:
  truths:
    - "A skill eat equipa a melhor comida, consome, re-equipa o item anterior e retorna SkillResult grounded por delta de bot.food"
    - "eat honra AbortSignal via bot.deactivateItem() (abort no meio da mastigação)"
    - "A skill attack dá UM golpe real (bot.attack) sem perseguir, retornando SkillResult grounded (D-15 1-shot)"
    - "eat e attack estão registradas no skillRegistry"
  artifacts:
    - path: "src/skills/eat.ts"
      provides: "Skill reflexa eat (D-05) grounded por delta de food"
      exports: ["eat", "EatSchema", "eatTool"]
      min_lines: 50
    - path: "src/skills/attack.ts"
      provides: "Implementação mínima 1-shot de attack (D-15) substituindo o stub"
      contains: "bot.attack"
  key_links:
    - from: "src/skills/index.ts"
      to: "src/skills/eat.ts"
      via: "skillRegistry.eat = eat"
      pattern: "eat,"
    - from: "src/skills/eat.ts"
      to: "bot.consume"
      via: "consumo nativo Mineflayer"
      pattern: "bot\\.consume\\(\\)"
---

<objective>
Entregar duas skills reflexas vitais (Wave 1, ramo A): `eat` (NOVA, D-05) e `attack` (substituir o stub por implementação mínima 1-shot, D-15). Ambas seguem o contrato `(bot, params) ⇒ Promise<SkillResult>` grounded por delta REAL — eat mede `bot.food` antes/depois, attack mede o HP do mob. Zero dependência nova: tudo via API nativa Mineflayer.

Purpose: SURV-01 (comer reflexo) e a metade defensiva de SURV-02 (revidar 1-golpe quando encurralado) precisam destas primitivas de ação antes que o System 1 (Plan 04) as dispare. Este plano roda em paralelo com o Plan 03 (flee/shelter) — arquivos disjuntos.
Output: `src/skills/eat.ts` + testes, `src/skills/attack.ts` reimplementado + testes, registro no `skillRegistry`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-system-1-sobreviv-ncia-reflexa/08-CONTEXT.md
@.planning/phases/08-system-1-sobreviv-ncia-reflexa/08-RESEARCH.md

<interfaces>
<!-- Contratos que o executor usa diretamente. -->

SkillResult (src/grounding/types.ts) — o que toda skill retorna:
```typescript
export type SkillOutcome = 'success' | 'partial' | 'no_effect' | 'error'
export interface SkillResult {
  readonly outcome: SkillOutcome
  readonly observed: number      // delta REAL (food ganho; 1/0 para golpe acertou)
  readonly expected: number
  readonly delta: Readonly<Record<string, number>>   // por nome de item
  readonly reason?: string
}
```

SkillFunction + skillRegistry (src/skills/index.ts):
```typescript
export type SkillFunction = (bot: Bot, params: unknown) => Promise<SkillResult>
export const skillRegistry: Record<string, SkillFunction> = { navigate, dig, follow, attack }
export const toolRegistry: SkillTool[] = [navigateTool, digTool, followTool, attackTool]
```

Padrão de extração do signal ANTES do Zod (de src/skills/navigate.ts:42-44):
```typescript
const signal = (rawParams as Record<string, unknown>)?.signal as AbortSignal | undefined
const { target, range } = NavigateSchema.parse(rawParams)
```

Primitivas Mineflayer confirmadas na RESEARCH (index.d.ts):
```typescript
bot.heldItem: Item | null                       // para re-equipar
bot.inventory.items(): Item[]
bot.registry.foods: Record<number, { foodPoints: number; saturation: number }>  // chave = item.type (confirmar em runtime)
bot.equip(item: Item, dest: 'hand'): Promise<void>
bot.consume(): Promise<void>                     // resolve no fim do ato de comer
bot.deactivateItem(): void                       // cancela a mastigação (abort)
bot.attack(entity: Entity): void                 // 1 golpe; retorna void
bot.food: number                                 // 0–20
bot.nearestEntity(filter?): Entity | null
```

attack.ts ATUAL é stub (substituir o corpo da função, manter AttackSchema/attackTool):
```typescript
export async function attack(_bot, rawParams): Promise<SkillResult> {
  AttackSchema.parse(rawParams)
  return { outcome: 'error', observed: 0, expected: 0, delta: {}, reason: 'skill attack não implementada (stub)' }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar skill eat (D-05) grounded por delta de food + testes</name>
  <read_first>
    - src/skills/navigate.ts (padrão de skill: extração de signal, Zod parse, SkillResult de retorno)
    - src/skills/dig.test.ts (padrão de teste de skill com mock bot mínimo `any`)
    - src/skills/index.ts (registro de skill + tool)
    - src/grounding/types.ts (contrato SkillResult)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-RESEARCH.md (Code Example "Comer (D-05)"; Pitfall 2 grounding vital local; Open Question #3 forma de bot.registry.foods)
  </read_first>
  <behavior>
    - Sucesso: bot.food sobe após consume → outcome:'success', observed = foodAfter - foodBefore.
    - Sem comida no inventário (filtro registry.foods vazio) → outcome:'no_effect', observed:0, reason indicando "sem comida".
    - food já cheio (já em 20) e consume não muda nada → outcome:'no_effect', observed:0.
    - Abort no meio: se signal já abortado OU abortar durante consume → chama bot.deactivateItem(); outcome derivado do delta real (no_effect se não comeu).
    - Re-equipa: após consume, se havia prevHeld != a comida, bot.equip(prevHeld, 'hand') é chamado.
  </behavior>
  <action>
    Criar `src/skills/eat.ts`:

    1. `EatSchema = z.object({})` (eat não precisa de params além do signal de runtime; aceitar objeto vazio). `export type EatParams = z.infer<typeof EatSchema>`.
    2. `export async function eat(bot: Bot, rawParams: unknown): Promise<SkillResult>`:
       - Extrair `signal` ANTES do Zod (padrão navigate.ts): `const signal = (rawParams as Record<string, unknown>)?.signal as AbortSignal | undefined`.
       - `EatSchema.parse(rawParams ?? {})` (tolerar undefined).
       - Capturar `const foodBefore = bot.food` e `const prevHeld = bot.heldItem`.
       - Selecionar comida: `const foods = bot.inventory.items().filter((it) => bot.registry.foods?.[it.type]).sort((a,b) => (bot.registry.foods[b.type].foodPoints) - (bot.registry.foods[a.type].foodPoints))`. `const food = foods[0]`.
       - Se `!food` → retornar `{ outcome:'no_effect', observed:0, expected:0, delta:{}, reason:'sem comida no inventário' }`.
       - Registrar abort: se `signal`, `signal.addEventListener('abort', () => { try { bot.deactivateItem() } catch {} }, { once:true })`. Se `signal?.aborted` já no início, retornar no_effect com reason 'abortado antes de comer'.
       - `try { await bot.equip(food, 'hand'); await bot.consume() } catch (err) { /* guarda o reason; segue para grounding */ }` — NUNCA deixar lançar como fluxo (padrão D-12).
       - Re-equipar: `if (prevHeld && prevHeld.type !== food.type) { try { await bot.equip(prevHeld, 'hand') } catch {} }`.
       - Grounding vital LOCAL (Pitfall 2 — NÃO mexer no GroundState genérico): `const gained = bot.food - foodBefore`. `expected` = `bot.registry.foods[food.type].foodPoints` (esperança de pontos). `outcome = gained > 0 ? 'success' : 'no_effect'`. `observed = Math.max(0, gained)`. Anexar `reason` do throw se houve.
       - Retornar `{ outcome, observed, expected, delta:{}, reason }`.
    3. `export const eatTool = { name:'eat', description:'Come a melhor comida disponível no inventário (reflexo de sobrevivência)', schema: EatSchema, execute: eat } as const`.

    Criar `src/skills/eat.test.ts` (`import { test, expect } from 'bun:test'`) com mock bot mínimo `as any` (estilo dig.test.ts): mockar `bot.food` mutável, `bot.heldItem`, `bot.inventory.items()`, `bot.registry.foods`, `bot.equip` (async no-op), `bot.consume` (async que incrementa `bot.food`), `bot.deactivateItem`. Cobrir TODOS os casos do bloco <behavior>. Mínimo 5 testes.
  </action>
  <verify>
    <automated>bun test src/skills/eat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "bot.consume()" src/skills/eat.ts` retorna 1 match
    - `grep -n "bot.deactivateItem()" src/skills/eat.ts` retorna 1 match
    - `grep -n "bot.registry.foods" src/skills/eat.ts` retorna >= 1 match
    - `grep -n "export async function eat\|export const eatTool\|export const EatSchema" src/skills/eat.ts` retorna 3 matches
    - `grep -c "test(" src/skills/eat.test.ts` retorna >= 5
    - `bun test src/skills/eat.test.ts` passa (sucesso=delta>0, sem-comida=no_effect, abort=deactivateItem chamado)
  </acceptance_criteria>
  <done>eat.ts equipa→consume→re-equipa, grounded por delta de bot.food, honra abort via deactivateItem; eat.test.ts verde com mock mínimo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar attack 1-shot real (D-15) + registrar eat/attack no skillRegistry + testes</name>
  <read_first>
    - src/skills/attack.ts (stub atual — substituir só o corpo da função attack, manter AttackSchema/attackTool)
    - src/skills/index.ts (skillRegistry e toolRegistry — adicionar eat; attack já está registrado)
    - src/grounding/types.ts (SkillResult)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-RESEARCH.md (Pitfall 6: attack stub; D-15 1-shot sem perseguir; bot.attack(entity):void)
    - .planning/phases/08-system-1-sobreviv-ncia-reflexa/08-CONTEXT.md (D-15: 1 golpe, sem manter alvo; combate real = Fase 13)
  </read_first>
  <behavior>
    - Alvo encontrado (entityName casa com um mob próximo): chama bot.attack(entity) UMA vez → outcome:'success', observed:1, expected:1.
    - Nenhum alvo casando (entityName não está entre as entidades) → outcome:'no_effect', observed:0, reason 'alvo não encontrado'.
    - NÃO há loop de perseguição: bot.attack é chamado no máximo 1 vez por invocação (1-shot, D-15).
    - Sem navegação/pathfinder: attack não move o bot (apenas golpeia se o alvo já está ao alcance/visível).
  </behavior>
  <action>
    Em `src/skills/attack.ts`, SUBSTITUIR o corpo da função `attack` (manter `AttackSchema` e `attackTool` como estão; atualizar a description do tool removendo "[STUB]"):

    ```typescript
    export async function attack(bot: Bot, rawParams: unknown): Promise<SkillResult> {
      const { entityName } = AttackSchema.parse(rawParams)
      // D-15: 1-shot defensivo — encontra o alvo nomeado mais próximo e dá UM golpe. Sem perseguir,
      // sem manter alvo, sem pathfinder. Combate real (manter alvo/recuar/kiting) = Fase 13.
      const target = bot.nearestEntity((e) => (e.name === entityName || (e as any).username === entityName))
      if (!target) {
        return { outcome: 'no_effect', observed: 0, expected: 1, delta: {}, reason: `alvo '${entityName}' não encontrado` }
      }
      bot.attack(target)   // index.d.ts:345 — void, golpe único
      return { outcome: 'success', observed: 1, expected: 1, delta: {} }
    }
    ```
    Atualizar `attackTool.description` para `'Dá um golpe defensivo único em um jogador ou mob (1-shot, sem perseguir)'`.
    Remover o comentário de stub (`// D-12: stub …`) e o comentário interno sobre "implementação real na Fase 2+".

    Em `src/skills/index.ts`:
    - Importar eat: `import { eat, eatTool } from './eat'` e re-exportar `export { eat, EatSchema, eatTool } from './eat'`.
    - Adicionar `eat` ao `skillRegistry` (objeto): `{ navigate, dig, follow, attack, eat }`.
    - Adicionar `eatTool` ao `toolRegistry` array.
    (attack já está registrado — não duplicar; apenas se beneficia da nova implementação.)

    Criar `src/skills/attack.test.ts` (`import { test, expect } from 'bun:test'`) com mock bot mínimo: `bot.nearestEntity` que devolve um mock entity ou null; `bot.attack` spy (contar chamadas). Cobrir: alvo encontrado → success + bot.attack chamado EXATAMENTE 1 vez; alvo ausente → no_effect + bot.attack NÃO chamado. Mínimo 3 testes.
  </action>
  <verify>
    <automated>bun test src/skills/attack.test.ts src/skills/schemas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "bot.attack(target)" src/skills/attack.ts` retorna 1 match
    - `grep -n "skill attack não implementada (stub)" src/skills/attack.ts` retorna 0 matches (stub removido)
    - `grep -n "eat" src/skills/index.ts` retorna >= 3 matches (import, re-export, registro)
    - `grep -n "eat," src/skills/index.ts` (dentro de skillRegistry) confirma registro
    - `bun test src/skills/attack.test.ts` passa: success chama bot.attack 1x; no_effect chama 0x
    - `bun test src/skills/schemas.test.ts` continua verde (registro de tools não quebrou)
  </acceptance_criteria>
  <done>attack.ts dá 1 golpe real grounded sem perseguir (stub removido); eat/attack registrados no skillRegistry; testes verdes.</done>
</task>

</tasks>

<verification>
- `bun test src/skills/eat.test.ts src/skills/attack.test.ts src/skills/schemas.test.ts` verde.
- eat grounded por delta de bot.food (não pela resolução de consume) — GRND-01/D-19 preservado.
- attack é 1-shot literal (no máximo 1 bot.attack por invocação) — não invade a Fase 13.
- skillRegistry tem eat; attack não é mais stub.
</verification>

<success_criteria>
- SURV-01: eat equipa/consome a melhor comida e re-equipa, com abort via deactivateItem.
- SURV-02 (defesa): attack dá 1 golpe real quando o System 1 mandar (Plan 04), sem perseguir.
- Zero dependência nova; ambas as skills retornam SkillResult grounded.
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-system-1-sobreviv-ncia-reflexa/08-02-SUMMARY.md`.
</output>
