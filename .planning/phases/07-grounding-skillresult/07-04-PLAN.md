---
phase: 07-grounding-skillresult
plan: 04
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - src/chat/postFilter.ts
  - src/chat/postFilter.test.ts
  - src/chat/conversation.ts
autonomous: true
requirements: [GRND-02]
must_haves:
  truths:
    - "Antes de bot.chat, a fala do LLM é reconciliada contra o último observedDelta: 'peguei 10 tábuas' com observed:3 é reescrito para 'peguei 3 tábuas'"
    - "O post-filter é o gate determinístico final escopado a quantidade/coleta (não um validador semântico geral de NLG)"
    - "Quando não há divergência de quantidade detectável, a fala passa inalterada"
  artifacts:
    - path: "src/chat/postFilter.ts"
      provides: "reconcileQuantities(reply, observedDelta): string puro"
      contains: "export function reconcileQuantities"
    - path: "src/chat/postFilter.test.ts"
      provides: "testes do post-filter (pt-BR, reescrita de quantidade)"
      contains: "peguei"
    - path: "src/chat/conversation.ts"
      provides: "reconcileQuantities aplicado antes do bot.chat"
      contains: "reconcileQuantities"
  key_links:
    - from: "src/chat/conversation.ts"
      to: "src/chat/postFilter.ts"
      via: "reply = reconcileQuantities(reply, holder.lastObservedDelta) antes de bot.chat"
      pattern: "reconcileQuantities\\("
---

<objective>
Adicionar a camada (C) de defesa em profundidade (D-09 C / D-10): um post-filter DETERMINÍSTICO no único ponto de saída de fala (`conversation.ts`, antes do `bot.chat`) que reconcilia afirmações de QUANTIDADE/COLETA da fala do LLM contra o `holder.lastObservedDelta` real, e REESCREVE para o número grounded (D-10: "peguei 10" → "peguei 3"). O bot continua falando — mas com a verdade.

Purpose: GRND-02 critério #3 — medido AO VIVO em centenas de ações. O LLM local fraco drifta mesmo com o prompt autoritativo (Plan 03 camada A) — A sozinho é instrução, não gate (D-11). C é a rede determinística que fecha o gap. Começar MINIMALISTA: escopado ao padrão "peguei/coletei N <item>" que o critério #3 mede, NÃO um validador semântico geral de NLG (deferido). Esta é a última camada (D-11: B+A primeiro, depois C escopado).
Output: `src/chat/postFilter.ts` (função pura testável) + testes + fiação no `conversation.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-grounding-skillresult/07-CONTEXT.md

<interfaces>
<!-- Contrato do holder.lastObservedDelta criado na Plan 03 (Task 2). -->

De src/cognition/state.ts (Plan 03):
```typescript
// holder.lastObservedDelta: o fato autoritativo a reconciliar contra
lastObservedDelta: {
  skill: string; target: string;
  outcome: SkillOutcome; observed: number; expected: number;
  delta: Record<string, number>; at: number
} | null
```

De src/chat/conversation.ts §L122-135 (ponto de saída ATUAL — onde inserir o filtro):
```typescript
let reply: string
try { reply = await provider.chat(messages) } catch (err) { ...; return }
const trimmed = reply.trim()
if (trimmed.length > 0) {
  bot.chat(trimmed.slice(0, MAX_REPLY_LEN))   // ← ÚNICO ponto de saída; aplicar reconcile ANTES
}
```

handleConversation recebe `holder: CognitiveStateHolder` — então `holder.lastObservedDelta` está disponível no escopo.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reconcileQuantities — post-filter puro escopado a quantidade/coleta (D-09 C / D-10)</name>
  <files>src/chat/postFilter.ts, src/chat/postFilter.test.ts</files>
  <read_first>
    - src/cognition/state.ts (shape de lastObservedDelta — campos observed/delta/skill)
    - src/chat/conversation.ts (estilo do módulo; detectRequestKind usa regex pt/en — espelhar o estilo de regex pt-BR)
    - src/grounding/evaluate.test.ts (estilo de teste bun, construção de fakes)
  </read_first>
  <behavior>
    - reconcileQuantities("Peguei 10 tábuas!", delta com observed=3) → "Peguei 3 tábuas!" (reescreve o número para o grounded — D-10).
    - reconcileQuantities("Peguei 3 tábuas", observed=3) → inalterado (números batem).
    - reconcileQuantities("Que dia bonito", qualquer delta) → inalterado (sem padrão de quantidade → não toca).
    - reconcileQuantities(reply, null) → inalterado (sem delta autoritativo, não há o que reconciliar).
    - Cobre variantes pt-BR: "peguei|coletei|minerei|consegui N <item>" (case-insensitive). Escopo MÍNIMO (D-09 C) — só o padrão de quantidade de coleta; NÃO validador semântico geral.
    - Quando outcome é 'no_effect' (observed=0) e a fala afirma "peguei N", reescreve para 0 / forma "não consegui pegar" (a alucinação âncora "peguei 10 tábuas" com inventário inalterado).
  </behavior>
  <action>
    Criar `src/chat/postFilter.ts` com uma função PURA (sem bot, sem LLM, sem eval — só regex + lookup):

    ```typescript
    // src/chat/postFilter.ts
    // Fase 7 D-09 C / D-10 — gate determinístico final: reconcilia QUANTIDADE de coleta da fala
    // do LLM contra o observedDelta real e REESCREVE para o número grounded. Escopo MÍNIMO
    // (padrão "peguei/coletei N <item>") — NÃO um validador semântico geral de NLG (deferido).
    import type { SkillOutcome } from '../grounding/types'

    export interface ObservedDeltaFact {
      skill: string
      observed: number
      outcome: SkillOutcome
      delta: Record<string, number>
    }

    /** Verbos pt-BR de coleta que disparam a reconciliação de quantidade. */
    const COLLECT_VERB = /(peguei|coletei|minerei|consegui|obtive|juntei)/i
    /** Captura: <verbo> <número> <resto> — número é o que reconciliamos. */
    const QUANTITY_RE = /\b(peguei|coletei|minerei|consegui|obtive|juntei)\s+(\d+)\b/gi

    /**
     * Reescreve afirmações de quantidade de coleta na fala para o número observado real (D-10).
     * @param reply  texto do LLM (não confiável).
     * @param fact   último delta observado (holder.lastObservedDelta) ou null.
     * @returns fala com os números de coleta reconciliados ao grounded; inalterada se não houver padrão/fact.
     */
    export function reconcileQuantities(reply: string, fact: ObservedDeltaFact | null): string {
      if (!fact) return reply
      if (!COLLECT_VERB.test(reply)) return reply
      // observed é a fonte de verdade (D-06). Para no_effect, observed é 0.
      const grounded = fact.observed
      return reply.replace(QUANTITY_RE, (_m, verb: string, num: string) => {
        const claimed = parseInt(num, 10)
        if (claimed === grounded) return `${verb} ${num}` // bate → não toca
        return `${verb} ${grounded}` // reescreve para o número grounded (D-10)
      })
    }
    ```

    NÃO ampliar o escopo para outros padrões (preço, distância, tempo) — manter mínimo (D-09 C, deferido o validador semântico geral). Iterar a heurística depois se necessário (Claude's discretion).
  </action>
  <verify>
    <automated>bun test src/chat/postFilter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/chat/postFilter.ts` contém `export function reconcileQuantities`
    - `src/chat/postFilter.test.ts` existe e `grep -in "peguei" src/chat/postFilter.test.ts` retorna >= 1
    - teste cobre o caso âncora: "Peguei 10 tábuas" com observed=3 vira string contendo "3" e NÃO "10"
    - teste cobre: fala sem padrão de quantidade passa inalterada; fact=null passa inalterada; números que batem passam inalterados
    - `bun test src/chat/postFilter.test.ts` sai com código 0
    - `bun run typecheck` sai com código 0
  </acceptance_criteria>
  <done>reconcileQuantities puro reescreve a quantidade de coleta para o grounded (D-10); escopo mínimo pt-BR; testes cobrem reescrita/no-op/null; verde.</done>
</task>

<task type="auto">
  <name>Task 2: aplicar reconcileQuantities antes do bot.chat em conversation.ts (D-09 C)</name>
  <files>src/chat/conversation.ts</files>
  <read_first>
    - src/chat/conversation.ts §L122-135 (o ponto exato antes de bot.chat — único ponto de saída de fala)
    - src/chat/postFilter.ts (reconcileQuantities + ObservedDeltaFact — assinatura)
    - src/cognition/state.ts (holder.lastObservedDelta — shape disponível em handleConversation)
  </read_first>
  <action>
    Em `src/chat/conversation.ts`:
    1. Importar: `import { reconcileQuantities } from './postFilter'`.
    2. No bloco de saída (§L131-135), ANTES de `bot.chat(...)`, aplicar o filtro usando `holder.lastObservedDelta`:
       ```typescript
       const trimmed = reply.trim()
       if (trimmed.length > 0) {
         const fact = holder.lastObservedDelta
           ? { skill: holder.lastObservedDelta.skill, observed: holder.lastObservedDelta.observed, outcome: holder.lastObservedDelta.outcome, delta: holder.lastObservedDelta.delta }
           : null
         const grounded = reconcileQuantities(trimmed, fact)
         bot.chat(grounded.slice(0, MAX_REPLY_LEN))
       }
       ```
    3. Não alterar mais nada do fluxo (a extração de objetivo D-13 em ASSISTANT permanece intocada). Garantir que `holder` já está no escopo (está — é parâmetro de `handleConversation`).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "reconcileQuantities" src/chat/conversation.ts` retorna >= 2 (import + uso)
    - `grep -n "holder.lastObservedDelta" src/chat/conversation.ts` retorna >= 1
    - o `reconcileQuantities(` aparece ANTES do `bot.chat(` no arquivo (verificar ordem das linhas)
    - `bun run typecheck` sai com código 0
    - `bun test` sai com código 0 (suite inteira — nenhuma regressão na conversa)
  </acceptance_criteria>
  <done>conversation.ts aplica reconcileQuantities(holder.lastObservedDelta) no único ponto de saída antes do bot.chat; suite verde.</done>
</task>

</tasks>

<verification>
- `bun test src/chat/postFilter.test.ts` sai 0 (reescrita determinística verde).
- `bun test` sai 0 (suite inteira, conversa sem regressão).
- `grep -n "reconcileQuantities" src/chat/conversation.ts` confirma o gate no ponto de saída (D-09 C).
- `grep -rn "validador semântico\|NLG geral" src/chat/postFilter.ts` retorna 0 (escopo mantido mínimo — deferido não implementado).
</verification>

<success_criteria>
- A fala "peguei 10 tábuas" com inventário inalterado/observed:3 é reescrita para o número grounded antes do bot.chat (GRND-02 critério #3 / D-10).
- Post-filter é determinístico e escopado a quantidade/coleta (D-09 C) — não valida NLG em geral.
- Fala sem divergência passa inalterada; sem lastObservedDelta, no-op.
</success_criteria>

<output>
After completion, create `.planning/phases/07-grounding-skillresult/07-04-SUMMARY.md`
</output>
