---
phase: 07-grounding-skillresult
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/grounding/types.ts
  - src/grounding/capture.ts
  - src/grounding/evaluate.ts
  - src/grounding/evaluate.test.ts
autonomous: true
requirements: [GRND-01]
must_haves:
  truths:
    - "Existe um tipo SkillResult tagueado por outcome ('success'|'partial'|'no_effect'|'error') com observed/expected/delta"
    - "captureGroundState(bot) retorna um snapshot imutável de inventário+posição+bloco-alvo antes/depois de uma ação"
    - "evaluate puro (before, after, params) decide o outcome por comparação numérica observed vs expected, sem mock de bot"
  artifacts:
    - path: "src/grounding/types.ts"
      provides: "SkillResult + GroundState + SkillOutcome types"
      contains: "export interface SkillResult"
    - path: "src/grounding/capture.ts"
      provides: "captureGroundState(bot): GroundState imutável"
      contains: "export function captureGroundState"
    - path: "src/grounding/evaluate.ts"
      provides: "evaluateDig / evaluateNavigate predicados puros"
      contains: "export function evaluateDig"
    - path: "src/grounding/evaluate.test.ts"
      provides: "testes do evaluate puro (sem bot)"
      contains: "no_effect"
  key_links:
    - from: "src/grounding/capture.ts"
      to: "src/perception/snapshot.ts"
      via: "espelha o padrão structuredClone+Object.freeze de buildWorldSnapshot"
      pattern: "Object\\.freeze\\(structuredClone"
---

<objective>
Criar o módulo `grounding/` que é o CONTRATO de toda a Fase 7: o tipo `SkillResult` (tagueado por `outcome`), o tipo `GroundState`, a captura central imutável `captureGroundState(bot)` (D-04/D-05), e os avaliadores PUROS `evaluateDig`/`evaluateNavigate` (D-04) que decidem o `outcome` por delta numérico observado vs esperado.

Purpose: Separar as duas responsabilidades hoje fundidas no `progressChecker` — *captura do mundo* (1 lugar, genérico) vs *julgamento de sucesso* (puro, por skill). Tudo nas Plans 02/03/04 consome estes tipos. Esta é a Wave 0 de interfaces: nada implementa lógica de skill aqui, só contrato + captura + predicados puros testáveis.
Output: `src/grounding/{types,capture,evaluate}.ts` + `evaluate.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-grounding-skillresult/07-CONTEXT.md

<interfaces>
<!-- Tipos existentes que o executor DEVE espelhar/reusar. Extraídos do código. -->

De src/perception/types.ts (padrão de imutabilidade a seguir):
```typescript
export interface Position3D { readonly x: number; readonly y: number; readonly z: number }
export interface InventorySlot {
  readonly slot: number; readonly name: string; readonly type: number
  readonly count: number; readonly metadata: number; readonly nbt: unknown
}
```

De src/perception/snapshot.ts (padrão de captura a espelhar em captureGroundState):
```typescript
// retorna Object.freeze(structuredClone(raw)) — ZERO referência ao bot
const inventory = bot.inventory.items().map((item) => ({ slot: item.slot, name: item.name, type: item.type, count: item.count, metadata: item.metadata, nbt: ... }))
const pos = bot.entity.position // {x,y,z}
```

De src/skills/dig.ts (o progressChecker numérico a generalizar — D-04):
```typescript
// dig usa: soma de inventário como sinal de progresso
() => bot.inventory.items().reduce((sum, item) => sum + item.count, 0)
// para alvo string, expected = count (DigParams.count); para alvo XYZ, expected = bloco vira air
```

De src/skills/navigate.ts (progressChecker de posição):
```typescript
// navigate usa: hash de posição como sinal de progresso; expected = chegar a range do alvo
() => Math.round(pos.x*10) + Math.round(pos.y*10)*1000 + Math.round(pos.z*10)*1000000
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Definir SkillResult, SkillOutcome e GroundState (contrato)</name>
  <files>src/grounding/types.ts</files>
  <read_first>
    - src/grounding/types.ts (se já existir — provavelmente não; confirmar estado)
    - src/perception/types.ts (replicar Position3D e InventorySlot como base imutável)
    - src/cognition/types.ts (ver o padrão de discriminated union por string-literal do MemEvent — espelhar a tag `outcome`)
  </read_first>
  <action>
    Criar `src/grounding/types.ts` com os contratos abaixo VERBATIM (D-01/D-02/D-03/D-04/D-06):

    ```typescript
    // src/grounding/types.ts
    // Fase 7 — contrato de grounding. SkillResult deriva de delta REAL observado, nunca da Promise.
    import type { Position3D } from '../perception/types'

    /** Discriminante string-literal do resultado de uma skill (D-01/D-06). */
    export type SkillOutcome = 'success' | 'partial' | 'no_effect' | 'error'

    /**
     * Snapshot imutável do estado do mundo relevante a uma ação, capturado antes/depois (D-04).
     * Numérico onde possível para o evaluate comparar sem mock de bot.
     */
    export interface GroundState {
      /** Soma total de itens no inventário (generaliza o progressChecker do dig). */
      readonly inventoryCount: number
      /** Contagem por nome de item (ex.: {'oak_planks': 3}) — fonte do delta por-item. */
      readonly itemsByName: Readonly<Record<string, number>>
      /** Posição do bot no momento da captura. */
      readonly position: Position3D
      /** Nome do bloco no alvo XYZ (quando a skill mira posição); null se N/A. */
      readonly targetBlockName: string | null
      /** Timestamp Unix (ms) da captura. */
      readonly capturedAt: number
    }

    /** Delta inventário antes→depois, por nome de item (apenas chaves que mudaram). */
    export type InventoryDelta = Readonly<Record<string, number>>

    /**
     * Resultado verificado de uma skill (D-01). Base FLAT tagueada por `outcome`.
     * `outcome` deriva de `observed` vs `expected` — NUNCA da resolução da Promise (GRND-01).
     * `no_effect` = Promise resolveu mas o mundo não mudou (a alucinação "peguei 10 tábuas").
     * `error` = exceção lançada; distinto de partial/no_effect (falhas observadas).
     */
    export interface SkillResult {
      readonly outcome: SkillOutcome
      /** Quantidade REAL observada (itens coletados, ou 1/0 para chegou/não-chegou). Fonte da verdade. */
      readonly observed: number
      /** Quantidade esperada derivada dos params (count do dig; 1 para navigate). */
      readonly expected: number
      /** Delta de inventário por item (chaves que mudaram). Vazio quando irrelevante. */
      readonly delta: InventoryDelta
      /** Motivo legível (timeout, stuck, não implementado, exceção). Opcional em success. */
      readonly reason?: string
    }
    ```

    NÃO tipar `observed` por skill aqui (D-02): follow/attack ficam no shape base; só dig/navigate ganham predicados (Task 3). NÃO criar union tipada de `observed` para as 4 skills (deferido para Fases 11/13).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/grounding/types.ts` existe e contém `export type SkillOutcome = 'success' | 'partial' | 'no_effect' | 'error'`
    - contém `export interface SkillResult` com os campos `outcome`, `observed`, `expected`, `delta`
    - contém `export interface GroundState` com `inventoryCount`, `itemsByName`, `position`, `targetBlockName`
    - `bun run typecheck` sai com código 0
    - `grep -n "import type { Position3D }" src/grounding/types.ts` retorna 1 linha (reusa o tipo da perception, não redefine)
  </acceptance_criteria>
  <done>Tipos `SkillResult`/`SkillOutcome`/`GroundState`/`InventoryDelta` exportados; typecheck passa; `observed` não tipado por skill (D-02 respeitado).</done>
</task>

<task type="auto">
  <name>Task 2: captureGroundState(bot) — captura central imutável</name>
  <files>src/grounding/capture.ts</files>
  <read_first>
    - src/grounding/types.ts (assinatura GroundState criada na Task 1)
    - src/perception/snapshot.ts (espelhar EXATAMENTE o padrão Object.freeze(structuredClone(raw)) e a leitura de bot.inventory.items()/bot.entity.position/bot.blockAt)
  </read_first>
  <action>
    Criar `src/grounding/capture.ts` com `captureGroundState` (D-04/D-05). Espelhar o padrão imutável de `buildWorldSnapshot` (structuredClone + Object.freeze) — NÃO inflar `executeWithSafety` com isto (D-05).

    ```typescript
    // src/grounding/capture.ts
    // Fase 7 D-04/D-05 — captura central do estado de chão (ground state) antes/depois de uma ação.
    // Espelha o padrão imutável de buildWorldSnapshot (perception/snapshot.ts).
    import type { Bot } from 'mineflayer'
    import type { GroundState } from './types'

    /**
     * Captura um GroundState imutável: inventário (total + por-item), posição e bloco no alvo XYZ.
     * @param bot   instância Mineflayer (após 'spawn').
     * @param targetPos posição XYZ opcional — quando a skill mira um bloco específico (dig em coordenada).
     */
    export function captureGroundState(bot: Bot, targetPos?: { x: number; y: number; z: number }): GroundState {
      const items = bot.inventory.items()
      const inventoryCount = items.reduce((sum, it) => sum + it.count, 0)
      const itemsByName: Record<string, number> = {}
      for (const it of items) itemsByName[it.name] = (itemsByName[it.name] ?? 0) + it.count

      const p = bot.entity.position
      const position = { x: p.x, y: p.y, z: p.z }

      let targetBlockName: string | null = null
      if (targetPos) {
        const b = bot.blockAt({ x: targetPos.x, y: targetPos.y, z: targetPos.z } as Parameters<typeof bot.blockAt>[0])
        targetBlockName = b?.name ?? null
      }

      const raw: GroundState = { inventoryCount, itemsByName, position, targetBlockName, capturedAt: Date.now() }
      return Object.freeze(structuredClone(raw)) as GroundState
    }

    /** Helper puro: delta por-item entre dois GroundState (só chaves que mudaram). */
    export function inventoryDelta(before: GroundState, after: GroundState): Record<string, number> {
      const delta: Record<string, number> = {}
      const names = new Set([...Object.keys(before.itemsByName), ...Object.keys(after.itemsByName)])
      for (const name of names) {
        const d = (after.itemsByName[name] ?? 0) - (before.itemsByName[name] ?? 0)
        if (d !== 0) delta[name] = d
      }
      return delta
    }
    ```
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/grounding/capture.ts` existe e contém `export function captureGroundState`
    - contém `export function inventoryDelta`
    - `grep -n "Object.freeze(structuredClone" src/grounding/capture.ts` retorna 1 linha (padrão imutável espelhado de snapshot.ts)
    - `bun run typecheck` sai com código 0
    - NÃO importa nada de `src/skills/executor.ts` (captura é independente do executor — D-05)
  </acceptance_criteria>
  <done>`captureGroundState(bot, targetPos?)` retorna GroundState congelado; `inventoryDelta(before, after)` puro; typecheck passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: evaluate puro por skill (dig/navigate) + testes sem bot</name>
  <files>src/grounding/evaluate.ts, src/grounding/evaluate.test.ts</files>
  <read_first>
    - src/grounding/types.ts (SkillResult/GroundState/SkillOutcome)
    - src/grounding/capture.ts (inventoryDelta — reusar no evaluate)
    - src/skills/dig.ts §L19-21,L63-72 (DigParams.count = expected; progressChecker de inventário a generalizar)
    - src/skills/navigate.ts §L20,L48-66 (NavigateParams.range; progressChecker de posição)
  </read_first>
  <behavior>
    evaluateDig(before, after, expectedCount):
    - delta de inventário > 0 e == expectedCount → outcome 'success', observed=expectedCount
    - delta de inventário > 0 mas < expectedCount → outcome 'partial', observed=delta real (ex.: coletou 3 de 10 → observed:3)
    - delta de inventário == 0 (nada mudou) → outcome 'no_effect', observed:0 (mata "peguei 10 tábuas")
    evaluateNavigate(before, after, targetPos, range):
    - distância final do alvo <= range → outcome 'success', observed:1, expected:1
    - moveu mas não chegou ao range → outcome 'partial', observed:0 (não chegou) — distinto de no_effect
    - posição idêntica (não saiu do lugar) → outcome 'no_effect', observed:0
    - Ambos os evaluates são PUROS (recebem GroundState + params, sem bot) — testáveis sem mock.
  </behavior>
  <action>
    Criar `src/grounding/evaluate.ts` com dois predicados puros (D-04/D-06 — ternário quantificado, `observed` é a fonte de verdade, label derivado):

    ```typescript
    // src/grounding/evaluate.ts
    // Fase 7 D-04/D-06 — julgamento PURO de sucesso por skill. observed = fonte de verdade; label derivado.
    import type { GroundState, SkillResult } from './types'
    import { inventoryDelta } from './capture'

    /** Soma só os deltas POSITIVOS (itens ganhos) — coleta nunca credita por perda de item. */
    function gainedTotal(delta: Record<string, number>): number {
      return Object.values(delta).reduce((s, d) => (d > 0 ? s + d : s), 0)
    }

    /** evaluateDig: classifica coleta por delta de inventário (D-06 ternário). expected = count do dig. */
    export function evaluateDig(before: GroundState, after: GroundState, expected: number): SkillResult {
      const delta = inventoryDelta(before, after)
      const observed = gainedTotal(delta)
      let outcome: SkillResult['outcome']
      if (observed === 0) outcome = 'no_effect'
      else if (observed >= expected) outcome = 'success'
      else outcome = 'partial'
      return { outcome, observed, expected, delta }
    }

    /** evaluateNavigate: chegou ao range? observed 1/0; expected sempre 1. */
    export function evaluateNavigate(
      before: GroundState,
      after: GroundState,
      targetPos: { x: number; y: number; z: number },
      range: number,
    ): SkillResult {
      const dx = after.position.x - targetPos.x
      const dy = after.position.y - targetPos.y
      const dz = after.position.z - targetPos.z
      const dist = Math.sqrt(dx * dx + dy * dy + dz * dz)
      const moved =
        Math.round(before.position.x * 10) !== Math.round(after.position.x * 10) ||
        Math.round(before.position.y * 10) !== Math.round(after.position.y * 10) ||
        Math.round(before.position.z * 10) !== Math.round(after.position.z * 10)
      let outcome: SkillResult['outcome']
      let observed: number
      if (dist <= range) { outcome = 'success'; observed = 1 }
      else if (moved) { outcome = 'partial'; observed = 0 }
      else { outcome = 'no_effect'; observed = 0 }
      return { outcome, observed, expected: 1, delta: {} }
    }
    ```

    Criar `src/grounding/evaluate.test.ts` (bun test) cobrindo TODOS os ramos do behavior acima: dig success/partial/no_effect (incluindo o caso âncora coletou 3 de 10 → observed:3, outcome:'partial'), navigate success/partial/no_effect. Construir GroundState literais (sem bot). Pelo menos 1 assert que `evaluateDig` com delta zero retorna `outcome:'no_effect'` e `observed:0`.
  </action>
  <verify>
    <automated>bun test src/grounding/evaluate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/grounding/evaluate.ts` contém `export function evaluateDig` e `export function evaluateNavigate`
    - `src/grounding/evaluate.test.ts` existe e `grep -n "no_effect" src/grounding/evaluate.test.ts` retorna >= 1 linha
    - `bun test src/grounding/evaluate.test.ts` sai com código 0 (todos os testes passam)
    - teste cobre o caso "coletou 3 de 10": existe assert com `observed` igual a 3 e `outcome` igual a `'partial'`
    - `bun run typecheck` sai com código 0
  </acceptance_criteria>
  <done>evaluateDig/evaluateNavigate puros classificam success/partial/no_effect por delta numérico; testes cobrem os 3 ramos de cada (incl. caso âncora 3/10); suite verde.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` sai 0 (contratos compilam, reusam Position3D/InventorySlot da perception).
- `bun test src/grounding/evaluate.test.ts` sai 0 (predicados puros verdes).
- `grep -rn "Object.freeze(structuredClone" src/grounding/capture.ts` confirma padrão imutável espelhado de snapshot.ts.
- Nenhum import de `src/skills/` dentro de `src/grounding/` (módulo é a base, não depende das skills).
</verification>

<success_criteria>
- Tipo `SkillResult` tagueado por `outcome` existe e deriva veredito de `observed`/`expected` (GRND-01 base).
- `captureGroundState` central e imutável existe (D-04/D-05).
- `evaluate` puro por skill (dig/navigate) decide outcome sem mock de bot (D-04), testado nos 3 ramos.
- `observed` não tipado por skill especulativamente (D-02 respeitado).
</success_criteria>

<output>
After completion, create `.planning/phases/07-grounding-skillresult/07-01-SUMMARY.md`
</output>
