---
phase: 07-grounding-skillresult
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/cognition/types.ts
  - src/cognition/nodes.ts
  - src/cognition/state.ts
  - src/memory/longTerm.ts
  - src/cognition/personality.ts
  - src/llm/prompts.ts
  - src/cognition/reconnect.test.ts
  - src/cognition/reflection.test.ts
  - src/cognition/personality.test.ts
  - src/memory/longTerm.test.ts
  - src/cognition/reflection.integration.smoke.test.ts
autonomous: true
requirements: [GRND-02, GRND-04]
must_haves:
  truths:
    - "O execute node grava na memória o outcome DERIVADO do SkillResult observado, nunca 'success' por não-throw (mata o bug do nodes.ts:222)"
    - "O MemEvent action carrega outcome + observed + expected; consumidores TS tratam o caso partial explicitamente (D-13)"
    - "Uma ação com observed<expected (partial/no_effect) é gravada como falha-não-sucesso (GRND-04), preservando o número real do progresso"
    - "O serializeContext injeta o último observedDelta como fato autoritativo no prompt (D-09 A)"
  artifacts:
    - path: "src/cognition/types.ts"
      provides: "MemEvent.action estendido com outcome/observed/expected"
      contains: "outcome:"
    - path: "src/cognition/nodes.ts"
      provides: "execute node consome SkillResult e deriva memória do delta"
      contains: "outcome"
    - path: "src/cognition/state.ts"
      provides: "holder.lastObservedDelta para o prompt e o post-filter (Plan 04)"
      contains: "lastObservedDelta"
    - path: "src/llm/prompts.ts"
      provides: "serializeContext injeta observedDelta autoritativo"
      contains: "observedDelta"
  key_links:
    - from: "src/cognition/nodes.ts"
      to: "src/skills/index.ts (skillRegistry → SkillResult)"
      via: "const result: SkillResult = await skillRegistry[skill](...); outcome derivado de result"
      pattern: "result\\.outcome"
    - from: "src/cognition/nodes.ts"
      to: "src/cognition/state.ts (holder.lastObservedDelta)"
      via: "holder.lastObservedDelta = { skill, target, ...result, at: now() }"
      pattern: "lastObservedDelta"
---

<objective>
Fechar o bug de raiz da fase (D-09 B — memory-level, OBRIGATÓRIO): o execute node hoje grava `result:'success'` quando a Promise não lança (nodes.ts:218-229). Passa a consumir o `SkillResult` da Plan 02 e DERIVAR a memória do `outcome`/`observed` real. Estender o `MemEvent` action (D-13) com `outcome`/`observed`/`expected` — o TS estrito força os consumidores downstream (longTerm, personality, reflection) a tratar o caso `partial`. Injetar o último `observedDelta` no `serializeContext` como fato autoritativo (D-09 A). Persistir o delta no holder (`lastObservedDelta`) para o post-filter da Plan 04.

Purpose: GRND-02 (relata só o confirmado — camadas B+A, as estruturais/baratas, ordem sugerida por D-11) + GRND-04 (observed≠expected → falha, sem jogar fora o número parcial). Torna a memória estruturalmente incapaz de lembrar falsidade e protege a memória longa (SQLite+sqlite-vec, Fase 4) de contaminação permanente.
Output: MemEvent estendido + execute node grounded + holder.lastObservedDelta + prompt autoritativo + testes migrados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-grounding-skillresult/07-CONTEXT.md

<interfaces>
<!-- Contratos das Plans 01/02 consumidos aqui. -->

De src/grounding/types.ts:
```typescript
export type SkillOutcome = 'success' | 'partial' | 'no_effect' | 'error'
export interface SkillResult { readonly outcome: SkillOutcome; readonly observed: number; readonly expected: number; readonly delta: Readonly<Record<string, number>>; readonly reason?: string }
```

De src/skills/index.ts (Plan 02):
```typescript
export type SkillFunction = (bot: Bot, params: unknown) => Promise<SkillResult>
export const skillRegistry: Record<string, SkillFunction>
```

Estado ATUAL do bug em src/cognition/nodes.ts §L218-229 (a corrigir):
```typescript
try {
  const params = skill === 'dig' ? { target } : { target: JSON.parse(target) }
  await skillRegistry[skill!]!(bot, params)        // descarta o retorno
  recordSuccess(safety)
  holder.memory = push(holder.memory, { type: 'action', skill, target, result: 'success', timestamp: now() }) // BUG: success = não-lançou
} catch (err) { /* result:'failure' */ }
```

MemEvent action ATUAL em src/cognition/types.ts §L20:
```typescript
| { type: 'action'; skill: string; target: string; result: 'success' | 'failure'; reason?: string; timestamp: number }
```

Consumidores de e.result/e.skill (action variant) que vão precisar migrar:
- src/memory/longTerm.ts §L37-40 (importanceOf: if e.result === 'failure'), §L54-55 (summarizeEvent)
- src/cognition/personality.ts §L39,L42 (e.result === 'success' / 'failure')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender MemEvent.action com outcome/observed/expected (D-13) + migrar consumidores</name>
  <files>src/cognition/types.ts, src/memory/longTerm.ts, src/cognition/personality.ts</files>
  <read_first>
    - src/cognition/types.ts §L18-22 (MemEvent discriminated union; variante action)
    - src/memory/longTerm.ts §L26-60 (importanceOf + summarizeEvent — switch exaustivo por e.type)
    - src/cognition/personality.ts §L37-43 (lê e.result === 'success'/'failure')
    - src/cognition/reflection.ts (confirmar se lê e.result da action; ajustar se sim)
  </read_first>
  <behavior>
    - O tipo MemEvent action passa a exigir `outcome: SkillOutcome` + `observed: number` + `expected: number`; `result` continua presente DERIVADO ('success' quando outcome==='success', senão 'failure') para compat dos consumidores existentes.
    - importanceOf trata `outcome === 'partial'` explicitamente (nota intermediária — partial é não-sucesso mas não falha total).
    - summarizeEvent narra o número observed/expected (ex.: "Ação dig em oak_log: partial (3/10).").
    - typecheck força todos os consumidores a compilar com o novo shape (D-13).
  </behavior>
  <action>
    1. Em `src/cognition/types.ts`: importar `import type { SkillOutcome } from '../grounding/types'` e substituir a variante action por (D-13):
       ```typescript
       | {
           type: 'action'
           skill: string
           target: string
           outcome: SkillOutcome              // fonte de verdade (D-06)
           observed: number                   // quantidade REAL observada
           expected: number                   // quantidade esperada
           result: 'success' | 'failure'      // DERIVADO de outcome (compat consumidores)
           reason?: string
           timestamp: number
         }
       ```
       (Manter `result` derivado evita reescrever todo consumidor; `outcome`/`observed` são a verdade nova.)
    2. Em `src/memory/longTerm.ts`:
       - `importanceOf` case 'action': trocar a lógica para usar `outcome`:
         ```typescript
         case 'action':
           if (e.outcome === 'error' || e.outcome === 'no_effect') return 6   // falha observada
           if (e.outcome === 'partial') return 4                               // progresso parcial (D-13 trata explícito)
           if (e.skill === 'gather' || e.skill === 'dig' || e.skill === 'collect') return 5
           return 2
         ```
       - `summarizeEvent` case 'action': `return \`Ação ${e.skill} em ${e.target}: ${e.outcome} (${e.observed}/${e.expected})${e.reason ? \` (${e.reason})\` : ''}.\``
    3. Em `src/cognition/personality.ts` §L37-43: trocar `e.result === 'success'` → `e.outcome === 'success'` e `e.result === 'failure'` → `(e.outcome === 'error' || e.outcome === 'no_effect' || e.outcome === 'partial')` (partial conta como não-sucesso para o humor — coerente com GRND-04). Manter o resto da lógica.
    4. Em `src/cognition/reflection.ts`: se houver leitura de `e.result` da action, manter funcionando (result derivado já cobre); se preferir, migrar para `e.outcome`. Garantir typecheck.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "outcome: SkillOutcome" src/cognition/types.ts` retorna 1
    - `grep -n "observed: number" src/cognition/types.ts` retorna >= 1 na variante action
    - `grep -n "e.outcome" src/memory/longTerm.ts` retorna >= 2 (importanceOf + summarizeEvent migrados)
    - `grep -n "e.outcome" src/cognition/personality.ts` retorna >= 2
    - `grep -n "partial" src/memory/longTerm.ts` retorna >= 1 (caso partial tratado explicitamente — D-13)
    - `bun run typecheck` sai com código 0
  </acceptance_criteria>
  <done>MemEvent action carrega outcome/observed/expected; consumidores (longTerm/personality/reflection) compilam tratando partial explicitamente; result derivado preservado.</done>
</task>

<task type="auto">
  <name>Task 2: execute node consome SkillResult e deriva a memória do delta (D-09 B) + holder.lastObservedDelta</name>
  <files>src/cognition/nodes.ts, src/cognition/state.ts</files>
  <read_first>
    - src/cognition/nodes.ts §L139-231 (execute node — o try/catch do bug em L218-229)
    - src/cognition/state.ts (CognitiveStateHolder — onde adicionar lastObservedDelta; ver memory/needs/goals)
    - src/skills/index.ts (skillRegistry agora retorna Promise<SkillResult>)
    - src/grounding/types.ts (SkillResult)
    - src/cognition/safety.ts (recordSuccess/recordFailure — quando chamar cada um)
  </read_first>
  <action>
    1. Em `src/cognition/state.ts`: adicionar ao `CognitiveStateHolder` um campo:
       ```typescript
       /** Último delta observado de uma skill (D-09 A/C): fato autoritativo p/ prompt e post-filter. */
       lastObservedDelta: { skill: string; target: string; outcome: SkillOutcome; observed: number; expected: number; delta: Record<string, number>; at: number } | null
       ```
       Importar `SkillOutcome` de `../grounding/types`. Inicializar como `null` onde o holder é construído (procurar o objeto literal inicial do holder e adicionar `lastObservedDelta: null`).
    2. Em `src/cognition/nodes.ts`, substituir o bloco try/catch §L218-229 por consumo do SkillResult (D-09 B — outcome derivado do delta, NÃO do não-throw):
       ```typescript
       try {
         const params = skill === 'dig' ? { target } : { target: JSON.parse(target) }
         const result = await skillRegistry[skill!]!(bot, params)   // SkillResult (Plan 02)
         const success = result.outcome === 'success'
         if (success) recordSuccess(safety)
         else recordFailure(safety, target, now())
         holder.lastObservedDelta = { skill, target, outcome: result.outcome, observed: result.observed, expected: result.expected, delta: { ...result.delta }, at: now() }
         holder.memory = push(holder.memory, {
           type: 'action', skill, target,
           outcome: result.outcome, observed: result.observed, expected: result.expected,
           result: success ? 'success' : 'failure',   // derivado (GRND-04: partial/no_effect/error = failure)
           reason: result.reason,
           timestamp: now(),
         })
         log(`${result.outcome.toUpperCase()} ${skill} ${target} (${result.observed}/${result.expected})`)
       } catch (err) {
         // catch agora SÓ para exceções genuínas inesperadas (D-12) — skills não lançam como fluxo
         const reason = err instanceof Error ? err.name : String(err)
         recordFailure(safety, target, now())
         holder.lastObservedDelta = { skill, target, outcome: 'error', observed: 0, expected: 0, delta: {}, at: now() }
         holder.memory = push(holder.memory, {
           type: 'action', skill, target,
           outcome: 'error', observed: 0, expected: 0, result: 'failure', reason,
           timestamp: now(),
         })
         log(`ERRO inesperado ${skill} ${target}: ${reason}`)
       }
       ```
    3. Atualizar TAMBÉM o push do ramo anti-repeat (§L203-210) para o novo shape: adicionar `outcome: 'no_effect', observed: 0, expected: 0` ao objeto `{ type:'action', ... result:'failure', reason:'anti-repeat' }`.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "result.outcome" src/cognition/nodes.ts` retorna >= 1 (consome SkillResult)
    - `grep -n "lastObservedDelta" src/cognition/nodes.ts` retorna >= 2 (sucesso E catch)
    - `grep -n "lastObservedDelta" src/cognition/state.ts` retorna >= 2 (campo + init null)
    - `grep -cn "result: 'success', timestamp" src/cognition/nodes.ts` retorna 0 (o success-por-não-throw foi removido)
    - `grep -n "outcome: 'no_effect'" src/cognition/nodes.ts` retorna >= 1 (ramo anti-repeat migrado)
    - `bun run typecheck` sai com código 0
  </acceptance_criteria>
  <done>execute node deriva memória do result.outcome (não do não-throw — bug morto); holder.lastObservedDelta setado em sucesso/erro/anti-repeat; GRND-04 honrado (partial/no_effect/error → result:'failure').</done>
</task>

<task type="auto">
  <name>Task 3: serializeContext injeta observedDelta autoritativo (D-09 A)</name>
  <files>src/llm/prompts.ts</files>
  <read_first>
    - src/llm/prompts.ts §L89-149 (serializeContext — assinatura e onde acrescentar linhas; bloco "Eventos recentes")
    - src/cognition/state.ts (shape de lastObservedDelta criado na Task 2)
    - src/cognition/nodes.ts ou o call-site de serializeContext (descobrir quem chama serializeContext e passa os args — grep)
  </read_first>
  <action>
    1. Em `src/llm/prompts.ts`, estender `serializeContext` com um parâmetro OPCIONAL no fim (compat call-sites antigos):
       ```typescript
       export function serializeContext(
         snapshot: WorldSnapshot | null,
         needs: unknown,
         goals: unknown,
         recentEvents: ReadonlyArray<MemEvent>,
         lastObservedDelta?: { skill: string; target: string; outcome: string; observed: number; expected: number } | null,
       ): string {
       ```
       Antes do `return lines.join('\n')`, se `lastObservedDelta` existir, acrescentar um bloco AUTORITATIVO (D-09 A — "isto é o que de fato aconteceu; narre só a partir disto"):
       ```typescript
       if (lastObservedDelta) {
         const d = lastObservedDelta
         lines.push(
           `FATO VERIFICADO (autoritativo — narre SÓ a partir disto): última ação '${d.skill}' em '${d.target}' resultou em ${d.outcome}, observado ${d.observed} de ${d.expected} esperado(s). NÃO afirme quantidades diferentes destas.`,
         )
       }
       ```
    2. Localizar o call-site de `serializeContext` (grep `serializeContext(` em src/) e passar `holder.lastObservedDelta` como 5º argumento. Se o call-site não tiver acesso ao holder, passar via o objeto de contexto já disponível ali (documentar no SUMMARY qual call-site foi tocado).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "lastObservedDelta" src/llm/prompts.ts` retorna >= 2 (param + uso)
    - `grep -n "FATO VERIFICADO" src/llm/prompts.ts` retorna 1
    - `grep -rn "serializeContext(" src/` mostra o call-site passando lastObservedDelta (5º arg) OU documentado no SUMMARY se nenhum call-site de runtime existir ainda
    - `bun run typecheck` sai com código 0
  </acceptance_criteria>
  <done>serializeContext injeta o observedDelta como fato autoritativo (D-09 A); call-site passa holder.lastObservedDelta; assinatura retrocompatível (param opcional).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: migrar os testes que constroem MemEvent action (outcome/observed/expected obrigatórios)</name>
  <files>src/cognition/reconnect.test.ts, src/cognition/reflection.test.ts, src/cognition/personality.test.ts, src/memory/longTerm.test.ts, src/cognition/reflection.integration.smoke.test.ts</files>
  <read_first>
    - src/cognition/reconnect.test.ts §L55-58 (constrói action com result:'success')
    - src/cognition/reflection.test.ts §L94 (action inline)
    - src/cognition/personality.test.ts §L34,L41,L94,L95 (actions com result)
    - src/memory/longTerm.test.ts §L33-66 (importanceOf/summarizeEvent com actions)
    - src/cognition/reflection.integration.smoke.test.ts §L64 (factory de action)
    - src/cognition/types.ts (novo shape da action — para saber quais campos adicionar)
  </read_first>
  <behavior>
    - Todo literal `{ type:'action', ..., result:'success'|'failure' }` nos testes ganha `outcome` + `observed` + `expected` coerentes (ex.: success → outcome:'success', observed:N, expected:N; failure → outcome:'no_effect'|'error', observed:0, expected:N).
    - Os asserts de importanceOf/summarizeEvent que dependiam de `result` passam a refletir a nova lógica baseada em `outcome` (ex.: summarizeEvent agora inclui "(observed/expected)").
    - Todos os arquivos de teste compilam e a suite inteira passa.
  </behavior>
  <action>
    Atualizar CADA literal de MemEvent action nos 5 arquivos de teste para o novo shape (D-13 — migrar os testes que assertam result:'success'):
    1. `src/cognition/reconnect.test.ts` L55-58: adicionar `outcome: 'success', observed: 1, expected: 1` ao objeto que tem `result: 'success'`.
    2. `src/cognition/reflection.test.ts` L94 e similares: `{ type:'action', skill:'gather', target:'oak_log', outcome:'success', observed:1, expected:1, result:'success', timestamp:1100 }`.
    3. `src/cognition/personality.test.ts` L34/L41/L94/L95: o de success → `outcome:'success', observed:1, expected:1`; os de failure → `outcome:'no_effect', observed:0, expected:1`. Ajustar quaisquer asserts de humor que assumiam `result` (a lógica nova lê outcome — o resultado de humor deve permanecer o mesmo: success sobe, não-success desce).
    4. `src/memory/longTerm.test.ts` L33-66: adicionar outcome/observed/expected aos actions; ATUALIZAR os asserts de `importanceOf` para a nova tabela (failure→6 agora é error/no_effect→6; partial→4; gather/dig success→5) e o assert de `summarizeEvent` para incluir o trecho `(observed/expected)`.
    5. `src/cognition/reflection.integration.smoke.test.ts` L64: na factory, retornar `{ type:'action', skill:'gather', target, outcome:'success', observed:1, expected:1, result:'success', timestamp:ts }`.
    Rodar a suite inteira ao final para garantir nenhum regressão.
  </action>
  <verify>
    <automated>bun test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "type: 'action'" src/cognition/*.test.ts src/memory/longTerm.test.ts` — TODA ocorrência tem `outcome:` na mesma definição (verificar manualmente que nenhum literal de action ficou sem outcome)
    - `grep -rcn "result: 'success'" src/cognition/reconnect.test.ts` >= 1 mas agora acompanhado de `outcome: 'success'`
    - `bun test` sai com código 0 (suite INTEIRA verde, não só os arquivos tocados)
    - `bun run typecheck` sai com código 0
  </acceptance_criteria>
  <done>Todos os literais de MemEvent action nos 5 testes migrados para outcome/observed/expected; asserts de importanceOf/summarizeEvent atualizados; suite inteira verde.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` sai 0 (MemEvent estendido propaga por todos os consumidores — D-13).
- `bun test` sai 0 (suite inteira, incluindo os 5 testes migrados).
- `grep -rn "result: 'success', timestamp" src/cognition/nodes.ts` retorna 0 (o bug do success-por-não-throw foi eliminado).
- `grep -n "result.outcome" src/cognition/nodes.ts` confirma que a memória deriva do SkillResult observado (D-09 B).
- `grep -n "FATO VERIFICADO" src/llm/prompts.ts` confirma o fato autoritativo no prompt (D-09 A).
</verification>

<success_criteria>
- A memória grava o outcome derivado do delta observado, nunca 'success' por não-throw (GRND-02 camada B; bug morto).
- MemEvent action carrega outcome/observed/expected; consumidores tratam partial explicitamente (D-13).
- partial/no_effect/error gravados como falha-não-sucesso, preservando observed numérico (GRND-04).
- observedDelta injetado como fato autoritativo no prompt (GRND-02 camada A / D-09 A).
- holder.lastObservedDelta disponível para o post-filter da Plan 04.
</success_criteria>

<output>
After completion, create `.planning/phases/07-grounding-skillresult/07-03-SUMMARY.md`
</output>
