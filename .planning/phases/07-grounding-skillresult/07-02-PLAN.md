---
phase: 07-grounding-skillresult
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/skills/dig.ts
  - src/skills/navigate.ts
  - src/skills/follow.ts
  - src/skills/attack.ts
  - src/skills/index.ts
  - src/skills/grounding.test.ts
autonomous: true
requirements: [GRND-03]
must_haves:
  truths:
    - "navigate/dig retornam SkillResult cujo outcome vem de captureGroundState before/after + evaluate (não da Promise)"
    - "follow/attack param de dar throw e retornam SkillResult{outcome:'error', reason:'não implementado'}"
    - "Uma skill que coletou 3 de 10 antes de um timeout reporta observed:3 (delta capturado no finally), não falha total"
  artifacts:
    - path: "src/skills/dig.ts"
      provides: "dig(bot, params): Promise<SkillResult> grounded"
      contains: "captureGroundState"
    - path: "src/skills/navigate.ts"
      provides: "navigate(bot, params): Promise<SkillResult> grounded"
      contains: "evaluateNavigate"
    - path: "src/skills/follow.ts"
      provides: "follow(bot, params): Promise<SkillResult> com outcome:'error'"
      contains: "outcome: 'error'"
    - path: "src/skills/index.ts"
      provides: "SkillFunction retipada para Promise<SkillResult>"
      contains: "Promise<SkillResult>"
  key_links:
    - from: "src/skills/dig.ts"
      to: "src/grounding/capture.ts"
      via: "captureGroundState before/after da ação + evaluateDig"
      pattern: "captureGroundState"
    - from: "src/skills/navigate.ts"
      to: "src/grounding/evaluate.ts"
      via: "evaluateNavigate(before, after, target, range)"
      pattern: "evaluateNavigate"
---

<objective>
Converter as 4 skills (navigate/dig/follow/attack) para o contrato `SkillResult` da Plan 01 (D-12: toda skill SEMPRE retorna `SkillResult`, nunca lança como fluxo normal). dig/navigate viram skills GROUNDED de verdade: capturam `GroundState` before/after via `captureGroundState` e julgam o `outcome` com `evaluateDig`/`evaluateNavigate`. follow/attack (stubs) param de dar throw e retornam `SkillResult{outcome:'error'}`. O delta é capturado MESMO quando a ação lança mid-progresso (D-08): um timeout que coletou 3/10 reporta `observed:3`.

Purpose: GRND-03 — generalizar o `progressChecker` do dig para um veredito verificado por delta real. Esta é a camada que produz o `SkillResult` que a Plan 03 (execute node/memória) e a Plan 04 (post-filter) consomem.
Output: 4 skills retornando `SkillResult`, `SkillFunction` retipada, suite de grounding de skills.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-grounding-skillresult/07-CONTEXT.md

<interfaces>
<!-- Contratos da Plan 01 que esta Plan consome. NÃO explorar o codebase — usar daqui. -->

De src/grounding/types.ts:
```typescript
export type SkillOutcome = 'success' | 'partial' | 'no_effect' | 'error'
export interface SkillResult { readonly outcome: SkillOutcome; readonly observed: number; readonly expected: number; readonly delta: Readonly<Record<string, number>>; readonly reason?: string }
export interface GroundState { readonly inventoryCount: number; readonly itemsByName: Readonly<Record<string, number>>; readonly position: { x:number;y:number;z:number }; readonly targetBlockName: string | null; readonly capturedAt: number }
```

De src/grounding/capture.ts:
```typescript
export function captureGroundState(bot: Bot, targetPos?: {x:number;y:number;z:number}): GroundState
export function inventoryDelta(before: GroundState, after: GroundState): Record<string, number>
```

De src/grounding/evaluate.ts:
```typescript
export function evaluateDig(before: GroundState, after: GroundState, expected: number): SkillResult
export function evaluateNavigate(before: GroundState, after: GroundState, targetPos: {x:number;y:number;z:number}, range: number): SkillResult
```

De src/skills/executor.ts (já existe — `executeWithSafety<T>` retorna T; SkillTimeoutError/SkillStuckError):
```typescript
export async function executeWithSafety<T>(action: () => Promise<T>, opts?: ExecuteOptions): Promise<T>
export class SkillTimeoutError extends Error {}
export class SkillStuckError extends Error {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dig retorna SkillResult grounded com captura no finally (D-08)</name>
  <files>src/skills/dig.ts</files>
  <read_first>
    - src/skills/dig.ts (estado atual: Promise<void>, dois ramos string/XYZ, executeWithSafety com progressChecker de inventário)
    - src/grounding/capture.ts (captureGroundState/inventoryDelta — assinaturas)
    - src/grounding/evaluate.ts (evaluateDig)
    - src/grounding/types.ts (SkillResult)
  </read_first>
  <action>
    Mudar a assinatura de `dig` de `Promise<void>` para `Promise<SkillResult>` e torná-la grounded (D-08 — capturar delta mesmo se a ação lançar mid-progresso):

    1. Adicionar imports: `import { captureGroundState } from '../grounding/capture'`, `import { evaluateDig } from '../grounding/evaluate'`, `import type { SkillResult } from '../grounding/types'`, `import { SkillTimeoutError, SkillStuckError } from './executor'`.
    2. Logo após `const { target, count } = DigSchema.parse(rawParams)`, capturar o baseline: `const before = captureGroundState(bot, typeof target === 'string' ? undefined : target)`.
    3. Envolver a chamada `executeWithSafety(...)` (ambos os ramos string e XYZ) num `try/catch`. O delta deve ser lido num bloco que roda SEMPRE (D-08):
       ```typescript
       let threw: unknown = null
       try {
         // ... os dois ramos existentes (await executeWithSafety(...)) ...
       } catch (err) {
         threw = err
       }
       const after = captureGroundState(bot, typeof target === 'string' ? undefined : target)
       const result = evaluateDig(before, after, count)
       // D-08: se lançou mas coletou algo (observed>0) → reportar o parcial real, não erro total
       if (threw && result.observed === 0) {
         const reason = threw instanceof Error ? threw.name : String(threw)
         return { outcome: 'error', observed: 0, expected: count, delta: result.delta, reason }
       }
       return threw ? { ...result, reason: threw instanceof Error ? threw.name : String(threw) } : result
       ```
    4. Para os ramos que HOJE dão `throw new Error(...)` por pré-condição (bloco não encontrado, nenhuma instância alcançável, ar em posição) — converter para `return { outcome: 'no_effect', observed: 0, expected: count, delta: {}, reason: '<mensagem atual>' }` em vez de throw (D-12: skill não lança como fluxo). O `progressChecker` interno do `executeWithSafety` permanece inalterado.
    5. Atualizar `digTool.execute` continua apontando para `dig` (mesma referência — só a assinatura mudou).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Promise<SkillResult>" src/skills/dig.ts` retorna >= 1 (assinatura mudou)
    - `grep -n "captureGroundState" src/skills/dig.ts` retorna >= 2 (before E after)
    - `grep -n "evaluateDig" src/skills/dig.ts` retorna 1
    - `grep -cn "throw new Error" src/skills/dig.ts` retorna 0 (nenhum throw de fluxo — D-12; pré-condições viram return no_effect)
    - `bun run typecheck` sai com código 0
  </acceptance_criteria>
  <done>dig retorna SkillResult; captura before/after; delta lido após catch (D-08, observed:3 num timeout de 3/10); pré-condições retornam no_effect em vez de throw.</done>
</task>

<task type="auto">
  <name>Task 2: navigate retorna SkillResult grounded com captura no finally</name>
  <files>src/skills/navigate.ts</files>
  <read_first>
    - src/skills/navigate.ts (estado atual: Promise<void>, ramos string/XYZ, GoalNear, executeWithSafety com progressChecker de posição)
    - src/grounding/capture.ts (captureGroundState)
    - src/grounding/evaluate.ts (evaluateNavigate)
    - src/grounding/types.ts (SkillResult)
  </read_first>
  <action>
    Mudar `navigate` de `Promise<void>` para `Promise<SkillResult>` e torná-la grounded:

    1. Imports: `import { captureGroundState } from '../grounding/capture'`, `import { evaluateNavigate } from '../grounding/evaluate'`, `import type { SkillResult } from '../grounding/types'`.
    2. Após resolver `goal`, determinar a posição-alvo concreta para o evaluate. Para alvo XYZ: `const targetPos = { x: target.x, y: target.y, z: target.z }`. Para alvo string (bloco): `const targetPos = { x: block.position.x, y: block.position.y, z: block.position.z }` (usar o `block` já encontrado pelo `findBlock`).
    3. Capturar baseline antes do `executeWithSafety`: `const before = captureGroundState(bot)`.
    4. Envolver o `await executeWithSafety(() => bot.pathfinder.goto(goal), {...})` em try/catch; ler `after` SEMPRE e avaliar:
       ```typescript
       let threw: unknown = null
       try { await executeWithSafety(() => bot.pathfinder.goto(goal), { /* opts atuais */ }) }
       catch (err) { threw = err }
       const after = captureGroundState(bot)
       const result = evaluateNavigate(before, after, targetPos, range)
       return threw ? { ...result, reason: threw instanceof Error ? threw.name : String(threw) } : result
       ```
       (Para navigate, chegar ao range é o sinal: se chegou apesar do timeout-no-pós-delay, `result.outcome` já será 'success'; se não chegou, 'partial'/'no_effect' — o `reason` do throw é anexado para diagnóstico.)
    5. O `throw new Error('Bloco do tipo ... não encontrado')` (bloco não encontrado no findBlock) vira `return { outcome: 'no_effect', observed: 0, expected: 1, delta: {}, reason: '<mensagem atual>' }` (D-12).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Promise<SkillResult>" src/skills/navigate.ts` retorna >= 1
    - `grep -n "captureGroundState" src/skills/navigate.ts` retorna >= 2 (before E after)
    - `grep -n "evaluateNavigate" src/skills/navigate.ts` retorna 1
    - `grep -cn "throw new Error" src/skills/navigate.ts` retorna 0 (D-12)
    - `bun run typecheck` sai com código 0
  </acceptance_criteria>
  <done>navigate retorna SkillResult derivado de distância final ao alvo (success/partial/no_effect); bloco não encontrado vira no_effect; sem throw de fluxo.</done>
</task>

<task type="auto">
  <name>Task 3: follow/attack stubs retornam SkillResult{outcome:'error'} + retipar registry</name>
  <files>src/skills/follow.ts, src/skills/attack.ts, src/skills/index.ts</files>
  <read_first>
    - src/skills/follow.ts (stub atual: throw new Error)
    - src/skills/attack.ts (stub atual: throw new Error)
    - src/skills/index.ts (SkillFunction = (bot, params) => Promise<void>; skillRegistry)
    - src/grounding/types.ts (SkillResult)
  </read_first>
  <action>
    Converter os stubs (D-12: param de dar throw como fluxo; contrato uniforme):

    1. Em `src/skills/follow.ts`: importar `import type { SkillResult } from '../grounding/types'`. Mudar `follow` de `Promise<void>` para `Promise<SkillResult>`. O corpo: manter `FollowSchema.parse(rawParams)` (validação) e substituir o `throw new Error(...)` por:
       ```typescript
       return { outcome: 'error', observed: 0, expected: 0, delta: {}, reason: 'skill follow não implementada (stub)' }
       ```
       NÃO tipar `observed` por skill (D-02 — follow fica no shape base genérico enquanto stub).
    2. Em `src/skills/attack.ts`: idêntico — `Promise<SkillResult>`, manter `AttackSchema.parse`, retornar `{ outcome: 'error', observed: 0, expected: 0, delta: {}, reason: 'skill attack não implementada (stub)' }`.
    3. Em `src/skills/index.ts`: importar `import type { SkillResult } from '../grounding/types'` e retipar:
       ```typescript
       export type SkillFunction = (bot: Bot, params: unknown) => Promise<SkillResult>
       ```
       Re-exportar `export type { SkillResult } from '../grounding/types'` para conveniência dos consumidores. `skillRegistry` e `toolRegistry` permanecem (as 4 funções agora batem a nova assinatura).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "outcome: 'error'" src/skills/follow.ts` retorna 1
    - `grep -n "outcome: 'error'" src/skills/attack.ts` retorna 1
    - `grep -cn "throw new Error" src/skills/follow.ts` e `src/skills/attack.ts` retornam 0 cada
    - `grep -n "Promise<SkillResult>" src/skills/index.ts` retorna >= 1 (SkillFunction retipada)
    - `bun run typecheck` sai com código 0
  </acceptance_criteria>
  <done>follow/attack retornam SkillResult{outcome:'error'} sem lançar; SkillFunction agora é Promise<SkillResult>; registry compila.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: teste de grounding das skills (delta-driven, sem servidor)</name>
  <files>src/skills/grounding.test.ts</files>
  <read_first>
    - src/skills/dig.ts, src/skills/navigate.ts, src/skills/follow.ts, src/skills/attack.ts (assinaturas SkillResult já convertidas)
    - src/grounding/evaluate.test.ts (padrão de construção de fakes; estilo de teste bun)
    - src/cognition/reconnect.test.ts (ver como os testes existentes mockam o bot — replicar o padrão de fake bot)
  </read_first>
  <behavior>
    - follow(bot, validParams) → Promise resolve com { outcome:'error', reason contém 'não implementada' } (NÃO rejeita)
    - attack(bot, validParams) → Promise resolve com { outcome:'error' } (NÃO rejeita)
    - Para dig/navigate (que exigem bot real para o caminho feliz): testar via fake-bot mínimo que o RETORNO é um SkillResult (tem `outcome` e `observed`) e que params inválidos ainda fazem o Zod `.parse` lançar (validação preservada). O julgamento de delta numérico já está coberto por evaluate.test.ts (Plan 01) — aqui foca-se no CONTRATO de retorno e no não-lançamento dos stubs.
  </behavior>
  <action>
    Criar `src/skills/grounding.test.ts` (bun test). Cobrir:
    1. `follow` e `attack` RESOLVEM (não rejeitam) com `outcome:'error'` e `reason` contendo "não implementada" — usar um objeto fake `{} as any` como bot (stubs não tocam o bot além do parse).
    2. `follow`/`attack` com params inválidos (ex.: `{}`) ainda lançam via Zod `.parse` (validação preservada — usar `expect(...).rejects.toThrow()`).
    3. (Opcional, se viável com fake-bot) um smoke de `dig` confirmando que o retorno tem as chaves `outcome`/`observed`/`expected`/`delta`. Se o fake-bot for muito custoso, documentar no teste com comentário que o delta-driven está coberto em evaluate.test.ts e cobrir só o shape via type-level + stubs.
    NÃO exigir servidor Minecraft nem RUN_LIVE — testes determinísticos offline.
  </action>
  <verify>
    <automated>bun test src/skills/grounding.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/skills/grounding.test.ts` existe
    - contém assert de que `follow` resolve com `outcome` igual a `'error'` (não rejeita)
    - contém assert de que params inválidos rejeitam (Zod parse preservado)
    - `bun test src/skills/grounding.test.ts` sai com código 0
    - `bun run typecheck` sai com código 0
  </acceptance_criteria>
  <done>Suite confirma: stubs resolvem com outcome:'error' (não lançam), validação Zod preservada; shape SkillResult verificado; verde offline.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` sai 0 (4 skills + registry na nova assinatura Promise<SkillResult>).
- `bun test src/skills/grounding.test.ts` sai 0.
- `grep -rn "throw new Error" src/skills/dig.ts src/skills/navigate.ts src/skills/follow.ts src/skills/attack.ts` retorna 0 ocorrências (D-12: nenhum throw como fluxo normal).
- `grep -rln "captureGroundState" src/skills` retorna dig.ts e navigate.ts (grounding aplicado às skills reais).
</verification>

<success_criteria>
- navigate/dig retornam SkillResult com outcome derivado de delta real before/after, não da Promise (GRND-03).
- follow/attack retornam SkillResult{outcome:'error'} sem lançar (D-12, contrato uniforme).
- Delta capturado mesmo quando a ação lança mid-progresso (D-08 — observed:3 num timeout de 3/10).
- SkillFunction retipada para Promise<SkillResult>; registry compila.
</success_criteria>

<output>
After completion, create `.planning/phases/07-grounding-skillresult/07-02-SUMMARY.md`
</output>
