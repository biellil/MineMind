---
phase: 11.1-llm-recebe-posi-es-e-dist-ncias-de-blocos-mobs-e-entidades-percep-o-espacial-no-contexto-user-human
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/llm/prompts.ts
  - src/llm/prompts.test.ts
autonomous: true
requirements: []   # Fase sem REQ-IDs no ROADMAP — must_haves derivados do Goal + D-01..D-04

must_haves:
  truths:
    - "O LLM recebe, por tipo de bloco próximo, a posição absoluta (x,y,z) de até 3 exemplos (mais próximos) + distância em metros + Δy cru com sinal (D-01/D-03)"
    - "O LLM recebe a posição absoluta + distância + Δy de cada entidade/mob listada e de cada jogador, no mesmo formato híbrido (D-04)"
    - "O texto NÃO contém veredito de alcançabilidade ('inalcançável'/'no alto'/'fora de alcance') — só dado cru (D-02)"
    - "TODOS os tipos de bloco aparecem com nome×count (count NUNCA some); o teto global de 18 coordenadas é o ÚNICO gate de quantos próx( são emitidos, distribuídos por prioridade troncos>pedra>minério>lixo (D-03)"
    - "Jogador com position null degrada para 'sem-pos' sem quebrar; serializeContext(null, ...) e qualquer position null NUNCA lançam (D-04a)"
    - "A saída continua sendo a mesma mensagem user/human (contexto) — nenhuma assinatura, call-site ou caminho assistant é alterado"
  artifacts:
    - path: "src/llm/prompts.ts"
      provides: "serializeContext enriquecido: linha de bloco e linhas de entidade/jogador com próx(x,y,z)+Nm+Δy; helper de Δy/coord; helper de priorização + teto global de coords"
      contains: "próx("
    - path: "src/llm/prompts.test.ts"
      provides: "Testes do novo formato: coord no bloco, teto global como único gate (todos os nomes presentes), position null, snapshot null, cálculo correto do Δy, ausência de veredito"
      contains: "Δy"
  key_links:
    - from: "serializeContext (bloco)"
      to: "snapshot.nearbyBlockTypes[].examples[0..2] + snapshot.status.position"
      via: "cálculo de distância/Δy dos exemplos mais próximos, sob teto global priorizado"
      pattern: "examples\\[0\\]"
    - from: "serializeContext (entidade/jogador)"
      to: "EntityInfo.position / PlayerInfo.position + status.position"
      via: "render híbrido com guarda de null"
      pattern: "Δy"
---

<objective>
Enriquecer SOMENTE a serialização do contexto user/human (`serializeContext` em `src/llm/prompts.ts`)
com percepção espacial: posição absoluta (x,y,z) + distância (m) + Δaltura (Δy cru) para blocos
próximos (até 3 exemplos mais próximos por tipo, sob teto global priorizado), entidades/mobs e
jogadores — reaproveitando dados que o `WorldSnapshot` JÁ captura e hoje são descartados
(`nearbyBlockTypes[].examples`, `EntityInfo.position`, `PlayerInfo.position`).

Purpose: hoje o LLM decide às cegas (manda coletar tronco sem saber se está a 2m ou no topo de uma
árvore inalcançável) — causa-raiz do "bot parado". Expor o dado espacial CRU deixa o LLM inferir
perto/longe e acima/abaixo, sem o código dar veredito.

Output: `serializeContext` produz linhas no formato híbrido (ex: `oak_log×4 próx(12,70,-5) 8m Δy+7`)
+ testes cobrindo o novo formato. Continua mensagem user/human — invariante da fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-llm-recebe-posi-es-e-dist-ncias-de-blocos-mobs-e-entidades-percep-o-espacial-no-contexto-user-human/11.1-CONTEXT.md

@src/llm/prompts.ts
@src/perception/types.ts
@src/perception/snapshot.ts
@src/llm/prompts.test.ts

<interfaces>
<!-- Contratos que o executor usa diretamente — NÃO alterar nenhum destes tipos (são fonte-de-verdade, fora de escopo). -->

De src/perception/types.ts:
```typescript
interface Position3D { readonly x: number; readonly y: number; readonly z: number }

interface BlockSummary {
  readonly count: number
  readonly examples: ReadonlyArray<Position3D>  // até 3; examples[0..2] ordenados por distância (findBlocks ordenado); examples[0] = mais próximo
}

interface EntityInfo {
  readonly name: string
  readonly position: Position3D   // SEMPRE presente para entidades
  readonly distance: number       // metros até o bot
  // ...id, type, kind, health, metadata
}

interface PlayerInfo {
  readonly username: string
  readonly position: Position3D | null   // null se entidade fora do chunk → degradar p/ "sem-pos"
  readonly distance: number | null        // null se posição indisponível
  // ...displayName, gamemode, ping
}

interface BotStatus {
  readonly position: Position3D   // base do Δy (já no prompt, status.position)
  // ...health, food, timeOfDay, isDay
}
```

De src/llm/prompts.ts (assinatura ATUAL — NÃO mudar):
```typescript
export function serializeContext(
  snapshot: WorldSnapshot | null,
  needs: unknown,
  goals: unknown,
  recentEvents: ReadonlyArray<MemEvent>,
  lastObservedDelta?: { skill; target; outcome; observed; expected } | null,
  recalled?: ReadonlyArray<{ id: number; summary: string; score: number }>,
  poisLine?: string,
): string
```

Trechos a SUBSTITUIR (formato atual, src/llm/prompts.ts):
- Bloco (linha ~142-146): `${name}×${info.count}` → adicionar até 3 `próx(x,y,z) Nm Δy±k` por tipo, sob teto global; REMOVER o `.slice(0, 8)` de tipos (todos os tipos mantêm name×count).
- Jogadores (linha ~148-153): `${p.username} (${dist}m)` → adicionar coord/Δy ou "sem-pos"
- Entidades (linha ~164-169): `${e.name} (${dist}m)` → adicionar `próx(x,y,z) Nm Δy±k`
- `status.position` (linha ~136-139) é a base do Δy — JÁ disponível como `status.position`.
</interfaces>
</context>

<scope_guardrails>
NÃO FAZER (fora de escopo, invariantes da fase):
- NÃO alterar `src/perception/types.ts`, `src/perception/snapshot.ts` nem o builder do `WorldSnapshot`.
- NÃO tocar o caminho **assistant** nem nenhuma outra função de `prompts.ts` (buildPersonaPrompt, buildDecisionGuide). Só `serializeContext` (e helpers privados novos no mesmo arquivo).
- NÃO mudar a assinatura de `serializeContext` nem nenhum call-site.
- NÃO emitir veredito textual de alcançabilidade ("inalcançável"/"no alto"/"fora de alcance"/"alto demais"). Δy é número com sinal, nunca julgamento (D-02).
- NÃO computar alcançabilidade real (raycast/pathfinding). Só renderizar o dado já capturado.
- NÃO reordenar `examples` — confiar na ordem (examples[0..2] = mais próximos) (D-03a).
- A saída continua user/human; serializeContext NUNCA lança (snapshot null, position null, examples vazio).
</scope_guardrails>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enriquecer a linha de blocos com coord+distância+Δy, teto global priorizado como ÚNICO gate</name>
  <files>src/llm/prompts.ts, src/llm/prompts.test.ts</files>

  <read_first>
  - src/llm/prompts.ts (alvo; ler serializeContext inteiro + buildDecisionGuide p/ a ordem de prioridade de recursos ~linha 75)
  - src/perception/types.ts (BlockSummary.examples, Position3D, BotStatus.position)
  - src/perception/snapshot.ts §76-101 (examples ordenados por distância, cap 3 por tipo — confirmar que examples[0..2] são os mais próximos)
  - .planning/phases/11.1-.../11.1-CONTEXT.md (D-01, D-02, D-03 e Claude's Discretion)
  </read_first>

  <behavior>
  Tests (em src/llm/prompts.test.ts) que devem passar APÓS a implementação:
  - Bloco com examples não-vazio → linha contém `próx(` seguido das coords inteiras do examples[0], a distância em `m` e `Δy` com sinal explícito. Ex: snapshot com oak_log count=4, examples[0]={x:12,y:70,z:-5}, bot em y=63 → saída contém `oak_log×4 próx(12,70,-5)` e `Δy+7`.
  - Δy negativo: bloco abaixo do bot (examples[0].y < status.position.y) → saída contém `Δy-` (sinal de menos). Ex: bot y=70, exemplo y=65 → `Δy-5`.
  - Δy zero: mesma altura → saída contém `Δy+0` (sinal `+` para zero; sem `Δy-0`).
  - Bloco SEM examples (examples vazio) → ainda renderiza `name×count` (count nunca some), apenas sem `próx(...)`.
  - Múltiplos exemplos por tipo: um tipo prioritário (ex: oak_log) com examples.length>=3, sob orçamento folgado → a saída contém os 3 `próx(` desse tipo (coords de examples[0], examples[1] e examples[2]).
  - Teto global como ÚNICO gate de coords: snapshot com 12 tipos de bloco distintos, cada um com examples → (a) TODOS os 12 nomes aparecem na saída como `name×count` (nenhum tipo é cortado — não há slice de tipos); (b) o número total de ocorrências de `próx(` na saída é <= 18.
  - Priorização sob teto: com tipos `oak_log`, `stone` e `dirt` todos com 3 examples e teto baixo o suficiente p/ não caber todos, `oak_log` recebe seus `próx(` antes de `dirt` (recurso prioritário consome o orçamento primeiro; dirt pode ficar só com `name×count`).
  - Sem veredito: a saída NÃO contém as strings `inalcançável`, `no alto`, `fora de alcance` nem `alto demais`.
  </behavior>

  <action>
  Em `src/llm/prompts.ts`, substituir o bloco de render de blocos (atual ~linha 142-146) e adicionar helpers privados (acima de serializeContext). Valores CONCRETOS escolhidos (Claude's Discretion já resolvida — ver nota D-03 no item 4):

  1. Helper `fmtDelta(d: number): string` → `(d >= 0 ? '+' : '') + Math.round(d)`. Garante `Δy+0` para zero e `+`/`-` explícito (Math.round(-0) === 0 → `+0`, ok).

  2. Helper `fmtBlockExample(ex: Position3D, botPos: Position3D): string` que monta:
     `próx(${Math.round(ex.x)},${Math.round(ex.y)},${Math.round(ex.z)}) ${Math.round(Math.hypot(ex.x-botPos.x, ex.y-botPos.y, ex.z-botPos.z))}m Δy${fmtDelta(ex.y-botPos.y)}`
     (distância = euclidiana 3D do bot ao exemplo; coords inteiras; Δy = ex.y - botPos.y).

  3. Prioridade de tipo (alinhada ao buildDecisionGuide, ~linha 75): função `blockPriority(name: string): number`:
     - `/_log$/.test(name)` → 0 (troncos, mais prioritário)
     - `name === 'stone' || name === 'cobblestone'` → 1
     - `/_ore$/.test(name)` → 2
     - senão → 3 (inclui grass_block/dirt/sand/etc — "lixo")
     Ordenar os blockEntries por `blockPriority` ASC, estável (preservar ordem original como desempate — usar índice).

  4. Teto global de coordenadas como ÚNICO gate (D-03 — "2-3 exemplos por tipo COM teto global"):
     - const `BLOCK_COORD_BUDGET = 18`.
     - const `MAX_EXAMPLES_PER_TYPE = 3` (D-03 "2-3 exemplos por tipo" → até 3, limitado pelos examples reais e pelo orçamento).
     - REMOVER o `.slice(0, 8)` de TIPOS: `Object.entries(snapshot.nearbyBlockTypes)` SEM slice de tipos — TODOS os tipos sempre renderizam `name×count` (count nunca some). O orçamento de prompt para blocos passa a ser controlado pelo teto de COORDENADAS, não por corte de tipos.
     - Iterar os blockEntries JÁ ordenados por prioridade, mantendo `coordsUsed = 0`. Para cada tipo, montar uma entrada de texto que começa em `${name}×${info.count}` e, enquanto `coordsUsed < BLOCK_COORD_BUDGET`, anexar até `MAX_EXAMPLES_PER_TYPE` exemplos (`info.examples[0..2]`, na ordem dada — NÃO reordenar), cada um via ` ${fmtBlockExample(ex, status.position)}`, incrementando `coordsUsed` por exemplo emitido. Parar de anexar coords a este tipo se o budget acabar ou os examples acabarem (o que vier primeiro). Tipos seguintes (mais baixa prioridade) ficam só com `name×count` quando o budget estourou.
     - Resultado: o budget de 18 é REALMENTE exercido (ex.: 6 tipos prioritários × 3 exemplos = 18 coords) e distribui as coords por prioridade; lixo fica com count só. Juntar cada entrada com `, ` numa única linha `Blocos próximos: ...`.

  Manter o estilo defensivo `lines.push(...)`. NÃO mudar a assinatura. NÃO tocar entidades/jogadores nesta task (Task 2). NÃO emitir nenhum texto de veredito. NÃO reintroduzir nenhum `.slice(0, N)` de TIPOS de bloco.
  </action>

  <verify>
    <automated>cd "C:\Users\biel1\OneDrive\Documentos\GitHub\MineMind" && bun test src/llm/prompts.test.ts</automated>
  </verify>

  <acceptance_criteria>
  - `src/llm/prompts.ts` contém a string literal `próx(`
  - `src/llm/prompts.ts` contém `Δy` (no helper de formatação)
  - `src/llm/prompts.ts` contém `BLOCK_COORD_BUDGET` com valor `18` e o constante É efetivamente usado para limitar `coordsUsed` (grep mostra uso, não só definição)
  - `src/llm/prompts.ts` contém `_log$` (regra de prioridade de troncos)
  - `src/llm/prompts.ts` NÃO contém `.slice(0, 8)` aplicado a tipos de bloco (nenhum corte de tipos — todos os nomes renderizam)
  - `src/llm/prompts.ts` NÃO contém as strings `inalcançável`, `no alto`, `fora de alcance`, `alto demais`
  - `bun test src/llm/prompts.test.ts` sai com código 0
  - A saída de `serializeContext` para um snapshot com oak_log examples[0]={12,70,-5} e bot y=63 casa `/oak_log×4 próx\(12,70,-5\)[^,]*Δy\+7/`
  - Para um snapshot com 12 tipos de bloco (cada um com examples): a saída contém os 12 nomes `name×count` E no máximo 18 ocorrências de `próx(`
  - A assinatura de `serializeContext` é idêntica à atual (7 parâmetros, mesma ordem) — `grep` confirma nenhum parâmetro novo
  </acceptance_criteria>

  <done>
  A linha "Blocos próximos:" inclui TODOS os tipos como `name×count` (nunca cortados) e anexa até 3
  `próx(x,y,z) Nm Δy±k` por tipo, distribuídos por prioridade troncos>pedra>minério>lixo até esgotar
  o teto global de 18 coordenadas (único gate). Count nunca some; nenhum veredito textual; assinatura
  intacta; testes verdes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enriquecer entidades/mobs e jogadores com coord+distância+Δy (null-safe) e cobrir testes</name>
  <files>src/llm/prompts.ts, src/llm/prompts.test.ts</files>

  <read_first>
  - src/llm/prompts.ts (após a Task 1; ler os blocos de jogadores ~linha 148-153 e entidades ~linha 164-169 + os helpers fmtDelta/fmtBlockExample criados na Task 1)
  - src/perception/types.ts (EntityInfo.position sempre presente; PlayerInfo.position pode ser null; distance pode ser null)
  - src/perception/snapshot.ts §58-74 (como players são populados; position null quando entidade fora do chunk)
  - src/llm/prompts.test.ts (testes existentes a estender; usar baseSnapshot/entity helpers já lá)
  - .planning/phases/11.1-.../11.1-CONTEXT.md (D-04, D-04a, Claude's Discretion sobre null)
  </read_first>

  <behavior>
  Tests (estender src/llm/prompts.test.ts) que devem passar APÓS a implementação:
  - Entidade com position → "Entidades próximas:" contém `Creeper próx(5,64,0) 5m Δy+0` (entity('Creeper',5) com position {x:5,y:64,z:0}, bot y=64). Mantém os testes existentes de ordem/limite-5 passando.
  - Entidade Δy != 0: entity com position.y acima do bot → contém `Δy+`; abaixo → `Δy-`.
  - Jogador com position → "Jogadores próximos:" contém `username próx(x,y,z) Nm Δy±k`.
  - Jogador com position null → "Jogadores próximos:" contém `username` e a string `sem-pos`, e NÃO contém `próx(` para esse jogador; serializeContext NÃO lança.
  - serializeContext(null, ...) continua não lançando e retorna `(sem percepção disponível)` (teste existente continua verde).
  - Sem veredito: saída de entidades/jogadores NÃO contém `inalcançável`/`no alto`/`fora de alcance`/`alto demais`.
  - Todos os 9 testes pré-existentes em prompts.test.ts continuam passando (regressão zero).
  </behavior>

  <action>
  Em `src/llm/prompts.ts`, dentro de `serializeContext`, atualizar os dois trechos (reutilizando os helpers da Task 1):

  1. Entidades (atual ~linha 164-169): trocar o `.map((e) => \`${e.name} (${Math.round(e.distance)}m)\`)` por
     `.map((e) => \`${e.name} ${fmtBlockExample(e.position, status.position)}\`)`.
     (EntityInfo.position é sempre presente — sem guarda de null. fmtBlockExample já produz `próx(x,y,z) Nm Δy±k`; a distância recalculada por fmtBlockExample é consistente com e.distance, mantém o formato único.) Manter `.slice(0, 5)` e a condicional de array não-vazio.

  2. Jogadores (atual ~linha 148-153): trocar o map por uma função que trata null:
     ```
     .map((p) => {
       if (p.position) return `${p.username} ${fmtBlockExample(p.position, status.position)}`
       const d = p.distance != null ? ` ${Math.round(p.distance)}m` : ''
       return `${p.username}${d} sem-pos`
     })
     ```
     String EXATA de ausência de posição: `sem-pos`. Manter `.slice(0, 5)` e a condicional de array não-vazio.

  3. NÃO alterar a linha de status nem a lógica de blocos da Task 1.

  Depois, ESTENDER `src/llm/prompts.test.ts`:
  - O helper `entity(name, distance)` continua válido (já tem position {x:distance,y:64,z:0}); ajustar os asserts dos testes existentes de entidade que checavam `Creeper (2m)` para o novo formato — trocar `toContain('Creeper (2m)')` por `toContain('Creeper próx(')` (e similar para Zombie/mobN), pois o formato `(2m)` deixa de existir. NÃO remover testes, só relaxar os asserts literais que quebram. Adicionar:
    - teste de coord+Δy de entidade,
    - teste de Δy de entidade (positivo e negativo),
    - teste de jogador com position (coord presente),
    - teste de jogador com position null → contém `sem-pos`, não contém `próx(` para ele, não lança,
    - teste de ausência de veredito (saída não contém `inalcançável`/`no alto`),
    - teste do Δy de bloco da Task 1 (coord + `Δy+7`),
    - teste do teto global como único gate (12 tipos → todos os nomes presentes E <=18 `próx(`) e da priorização (oak_log recebe coords antes de dirt),
    - teste de múltiplos exemplos por tipo (tipo prioritário com 3 examples sob orçamento folgado → 3 `próx(` desse tipo).
  </action>

  <verify>
    <automated>cd "C:\Users\biel1\OneDrive\Documentos\GitHub\MineMind" && bun test src/llm/prompts.test.ts && bunx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
  - `src/llm/prompts.ts` contém a string `sem-pos`
  - `src/llm/prompts.ts` usa `fmtBlockExample` no render de entidades E de jogadores (grep encontra >=2 usos além da definição)
  - `src/llm/prompts.test.ts` contém um teste com `sem-pos` (jogador sem posição)
  - `src/llm/prompts.test.ts` contém `Δy` em pelo menos um assert
  - `src/llm/prompts.test.ts` contém um assert de teto global referenciando `próx(` E um assert de que todos os 12 nomes de tipo aparecem
  - `bun test src/llm/prompts.test.ts` sai com código 0 e reporta >= 16 testes passando (9 antigos + novos)
  - `bunx tsc --noEmit` sai com código 0 (sem erro de tipo)
  - `src/llm/prompts.ts` NÃO contém `inalcançável`/`no alto`/`fora de alcance`/`alto demais`
  </acceptance_criteria>

  <done>
  Entidades e jogadores são renderizados no formato híbrido `próx(x,y,z) Nm Δy±k`; jogador com
  position null degrada para `... sem-pos` sem lançar; todos os testes (antigos + novos) verdes; tsc
  limpo; nenhum veredito textual; caminho user/human e assinatura intactos.
  </done>
</task>

</tasks>

<verification>
- `bun test src/llm/prompts.test.ts` → todos verdes (9 pré-existentes ajustados + novos do formato espacial).
- `bunx tsc --noEmit` → sem erros (assinatura de serializeContext inalterada, sem novos tipos).
- Inspeção: `serializeContext` é a ÚNICA função alterada; `types.ts` e `snapshot.ts` intocados (git diff vazio nesses arquivos).
- Saída de exemplo de bloco casa `/×\d+ próx\(\d+,\d+,-?\d+\) \d+m Δy[+-]\d+/`.
- Snapshot com 12 tipos: todos os 12 nomes presentes e <=18 `próx(` (o teto é o único gate).
- Nenhuma string de veredito de alcançabilidade na saída.
</verification>

<success_criteria>
- D-01: linha de bloco = `name×count próx(x,y,z) Nm Δy±k` para o(s) exemplo(s) mais próximo(s) (verificável).
- D-02: zero veredito textual — só dado cru com Δy sinalizado.
- D-03: até 3 exemplos por tipo, sob teto global de 18 coords (ÚNICO gate); priorização troncos>pedra>minério>lixo; count de TODOS os tipos preservado (sem corte de tipos).
- D-04: entidades e jogadores no mesmo formato híbrido; PlayerInfo.position null → `sem-pos`, sem lançar.
- Invariante: mensagem user/human; assinatura e call-sites inalterados; types/snapshot intocados.
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-llm-recebe-posi-es-e-dist-ncias-de-blocos-mobs-e-entidades-percep-o-espacial-no-contexto-user-human/11.1-01-SUMMARY.md`
</output>
