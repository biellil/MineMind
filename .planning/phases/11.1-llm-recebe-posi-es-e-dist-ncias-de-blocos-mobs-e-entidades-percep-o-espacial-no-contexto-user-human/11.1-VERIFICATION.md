---
phase: 11.1-llm-recebe-posi-es-e-dist-ncias-de-blocos-mobs-e-entidades-percep-o-espacial-no-contexto-user-human
verified: 2026-06-22T00:00:00Z
status: passed
score: 6/6 must-haves verified
re_verification: null
human_verification:
  - test: "Rodar o agente ao vivo (LM Studio + servidor Minecraft) e confirmar que o LLM local infere perto/longe e acima/abaixo a partir do Δy cru, parando de mandar coletar alvos inalcançáveis"
    expected: "Bot deixa de tentar gather em alvos com Δy alto/distância grande; usa explore quando o prioritário está inalcançável"
    why_human: "Comportamento de inferência de modelo local em runtime — não verificável estaticamente (causa-raiz do bot parado)"
---

# Phase 11.1: Percepção espacial no contexto user/human — Verification Report

**Phase Goal:** O LLM passa a saber o que tem em volta dele com posição e distância (blocos, mobs e entidades) — em vez de adivinhar. Enriquece o contexto user/human com posições/distâncias. Invariantes: entra como mensagem user/human (nunca assistant); assinatura de serializeContext e call-sites inalterados; types.ts e snapshot.ts intocados; SEM veredito textual de alcançabilidade (só dado cru com Δy sinalizado).

**Verified:** 2026-06-22
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| - | ----- | ------ | -------- |
| 1 | LLM recebe, por tipo de bloco, posição (x,y,z) de até 3 exemplos + distância (m) + Δy cru sinalizado (D-01/D-03) | ✓ VERIFIED | prompts.ts:185-194 monta entry com `fmtBlockExample`; spot-check: `oak_log×4 próx(12,70,-5) 15m Δy+7`; tests linhas 108-175 verdes |
| 2 | LLM recebe posição + distância + Δy de cada entidade/mob e de cada jogador no mesmo formato híbrido (D-04) | ✓ VERIFIED | prompts.ts:198-205 (jogadores), 216-221 (entidades); spot-check: `Creeper próx(5,64,0) 5m Δy+1`, `Steve próx(10,68,0) 11m Δy+5` |
| 3 | Texto NÃO contém veredito de alcançabilidade — só dado cru (D-02) | ✓ VERIFIED | Grep em prompts.ts: única ocorrência de `inalcançável` é buildDecisionGuide:112 (fora de escopo, pré-existente, não está na saída de serializeContext); tests 211-222, 287-299 verdes |
| 4 | TODOS os tipos aparecem com name×count (count nunca some); teto global de 18 coords é o ÚNICO gate, distribuído por prioridade troncos>pedra>minério>lixo (D-03) | ✓ VERIFIED | prompts.ts:178-194 sem slice de tipos; `BLOCK_COORD_BUDGET=18` gate em :188; `blockPriority` :39-44; tests 177-209 verdes (12 tipos todos presentes, ≤18 próx() |
| 5 | Jogador com position null degrada para `sem-pos` sem quebrar; serializeContext(null,...) e position null NUNCA lançam (D-04a) | ✓ VERIFIED | prompts.ts:199-201 (guarda null + sem-pos), :222-224 (snapshot null → fallback); spot-check `Alex sem-pos`; tests 80-84, 261-285 verdes (não lança) |
| 6 | Saída continua mensagem user/human; nenhuma assinatura, call-site ou caminho assistant alterado | ✓ VERIFIED | serializeContext mantém 7 params (prompts.ts:150-164); call-sites deliberation.ts:215 (7 args) e :275 (4 args) ambos em `new HumanMessage(...)`; zero AIMessage/assistant em prompts.ts |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/llm/prompts.ts` | serializeContext enriquecido + helpers fmtDelta/fmtBlockExample/blockPriority + teto global | ✓ VERIFIED | Contém `próx(`, `Δy`, `BLOCK_COORD_BUDGET=18` (usado em :188), `_log$` (:40); NÃO contém slice de tipos; helpers wired e usados (fmtBlockExample em 3 sites: blocos/jogadores/entidades) |
| `src/llm/prompts.test.ts` | Testes do novo formato: coord, teto único gate, position null, snapshot null, Δy, ausência de veredito | ✓ VERIFIED | Contém `Δy`, asserts de teto (≤18 próx() + 12 nomes presentes (:177-190), sem-pos (:261-285), snapshot null (:80-84); 23 testes passam |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| serializeContext (bloco) | nearbyBlockTypes[].examples + status.position | distância/Δy dos exemplos sob teto priorizado | ✓ WIRED | prompts.ts:187 `info.examples[i]` passado a fmtBlockExample com status.position; ordenação por blockPriority :180-182 |
| serializeContext (entidade/jogador) | EntityInfo.position / PlayerInfo.position + status.position | render híbrido com guarda de null | ✓ WIRED | prompts.ts:199 (player.position guard), :218 (entity.position direto); Δy via fmtBlockExample |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| serializeContext output | snapshot.nearbyBlockTypes / entities / players | WorldSnapshot (snapshot.ts §58-101, intocado nesta fase) — captura examples/position reais | ✓ FLOWING | Call-site deliberation.ts:216 passa `snapshot` real do loop cognitivo; dados antes descartados agora renderizados |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Render espacial completo de bloco/entidade/jogador | bun run snapshot sintético → serializeContext | `Blocos próximos: oak_log×4 próx(12,70,-5) 15m Δy+7, dirt×9` / `Steve próx(10,68,0) 11m Δy+5, Alex sem-pos` / `Creeper próx(5,64,0) 5m Δy+1` | ✓ PASS |
| Suíte de testes da fase | bun test src/llm/prompts.test.ts | 23 pass / 0 fail / 57 expect | ✓ PASS |
| Type check | bunx tsc --noEmit | exit 0 | ✓ PASS |
| name×count preservado sem coord (dirt sem examples) | spot-check | `dirt×9` presente, sem próx() | ✓ PASS |

### Requirements Coverage

Fase sem REQ-IDs no ROADMAP (`requirements: []`). Must-haves derivados do Goal + decisões D-01..D-04 do plan frontmatter. Todos verificados acima (truths 1-6). Sem requisitos órfãos.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| src/llm/prompts.ts | 112 | String `inalcançável` em buildDecisionGuide | ℹ️ Info | Pré-existente, fora de escopo (guardrail "NÃO tocar buildDecisionGuide"); é instrução de decisão, NÃO veredito espacial na saída de serializeContext. D-02 satisfeito (saída de serializeContext sem veredito, coberto por testes) |

Nenhum blocker ou warning. Sem TODO/FIXME/placeholder, sem stubs (return null/[]), sem props vazias hardcoded nas linhas alteradas.

### Human Verification Required

1. **Inferência espacial ao vivo** — Rodar o agente com LM Studio + servidor Minecraft e observar se o LLM local usa o Δy/distância crus para inferir perto/longe e acima/abaixo, parando de mandar coletar alvos inalcançáveis (investigação-mãe do "bot parado").
   - Expected: bot deixa de tentar gather em alvos altos/distantes; usa explore quando o prioritário está inalcançável.
   - Não-bloqueante: se o modelo local não inferir bem sozinho, a fase deferida "rótulo/heurística de alcançável derivado do Δy" (CONTEXT.md §Deferred) é o próximo passo. Não afeta a verificação do código desta fase.

### Gaps Summary

Nenhum gap. Todos os 6 must-haves verificados no código real, não apenas nas claims do SUMMARY. Os invariantes da fase foram confirmados independentemente:
- types.ts e snapshot.ts intocados (último commit ce35fa3, fase 07.1).
- Assinatura de serializeContext idêntica (7 params); ambos os call-sites em deliberation.ts usam `new HumanMessage(...)` → invariante user/human respeitado.
- Caminho assistant não tocado (zero AIMessage/role assistant em prompts.ts).
- Sem veredito de alcançabilidade na saída (única ocorrência de `inalcançável` é pré-existente, fora de escopo, em buildDecisionGuide).
- Teto global de 18 coords é o único gate; count de todos os tipos preservado (`.slice(0,8)` de tipos removido).

O único item pendente é verificação humana ao vivo (comportamento de inferência do modelo local), explicitamente não-bloqueante. Suíte verde (23/23), tsc limpo, spot-check confirma o formato híbrido exato `próx(x,y,z) Nm Δy±k`.

---

_Verified: 2026-06-22_
_Verifier: Claude (gsd-verifier)_
