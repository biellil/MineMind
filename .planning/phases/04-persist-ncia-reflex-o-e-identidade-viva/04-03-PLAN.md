---
phase: 04-persist-ncia-reflex-o-e-identidade-viva
plan: 03
type: tdd
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/memory/longTerm.ts
  - src/memory/longTerm.test.ts
autonomous: true
requirements: [MEM-02, MEM-03]

must_haves:
  truths:
    - "Um MemEvent acima do piso de importância é persistido em events + vec_events atomicamente"
    - "summarizeEvent gera texto natural (não JSON cru) para embeddar"
    - "importanceOf mapeia cada tipo de MemEvent a uma nota 1-10 determinística"
    - "retrieve combina recência × importância × relevância (min-max [0,1], pesos iguais) e ordena desc"
    - "retrieve renova last_access dos eventos recuperados"
  artifacts:
    - path: "src/memory/longTerm.ts"
      provides: "store de eventos LP: importanceOf, summarizeEvent, persistEvent, retrieve (scoring Generative Agents)"
      exports: ["importanceOf", "summarizeEvent", "persistEvent", "retrieve", "recencyRaw", "minMaxNormalizer"]
      min_lines: 80
    - path: "src/memory/longTerm.test.ts"
      provides: "testes de importância heurística + scoring + persistência atômica"
  key_links:
    - from: "src/memory/longTerm.ts"
      to: "vec_events"
      via: "INSERT na mesma transação que events + KNN MATCH na retrieve"
      pattern: "MATCH"
    - from: "src/memory/longTerm.ts"
      to: "src/memory/persistence.ts"
      via: "usa o Database de openDb"
      pattern: "Database"
---

<objective>
Construir o coração de MEM-03: `src/memory/longTerm.ts` — o store de eventos de longo prazo com importância heurística determinística (D-06), pipeline de escrita atômica (evento + embedding na mesma transação — D-05/Pattern 3), e recuperação semântica com o scoring de Generative Agents (recência × importância × relevância, min-max [0,1], pesos iguais α=1 — D-07/Pattern 4).

Purpose: É a implementação literal de MEM-03 ("recupera memórias semânticas relevantes por similaridade, combinando recência × relevância × importância") e completa MEM-02 no lado dos eventos.
Output: longTerm.ts (importanceOf, summarizeEvent, persistEvent, retrieve) testado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-CONTEXT.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-01-SUMMARY.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-02-SUMMARY.md

<interfaces>
<!-- MemEvent é a taxonomia que importanceOf/summarizeEvent precisam cobrir EXAUSTIVAMENTE. -->
From src/cognition/types.ts:
```typescript
export type MemEvent =
  | { type: 'state_transition'; from: CognitiveState; to: CognitiveState; timestamp: number }
  | { type: 'action'; skill: string; target: string; result: 'success' | 'failure'; reason?: string; timestamp: number }
  | { type: 'world'; event: 'damage' | 'hunger' | 'player_joined' | 'player_left'; detail: string; timestamp: number }
  | { type: 'chat_command'; command: string; from: string; mode: ControlMode; timestamp: number }
```

<!-- Persistence (Plan 02) fornece o Database aberto e helpers. embed() vem do provider. -->
From src/memory/persistence.ts: `export function openDb(path?): Database; export const EMBEDDING_DIM`
From src/llm/provider.ts: `embed(text: string): Promise<number[]>`

<!-- Config knobs do Plan 02. -->
config.ltImportanceFloor, config.retrievalHalfLifeMs, config.retrievalK
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: importanceOf + summarizeEvent + scoring puro (recencyRaw, minMax)</name>
  <files>src/memory/longTerm.ts, src/memory/longTerm.test.ts</files>
  <read_first>
    - src/cognition/types.ts (a union MemEvent — cobrir TODOS os tipos no switch)
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md (Pattern 4 tabela de importância 1-10; fórmula recência/minMax; "Code Examples" summarizeEvent)
    - src/config.ts (config.ltImportanceFloor, retrievalHalfLifeMs)
  </read_first>
  <behavior>
    - Test: importanceOf de world/damage = 9; world/player_left = 3; chat_command qualquer = 7; action result='failure' = 6; action success mundano (navigate) = 2; state_transition mundano (idle→exploring) = 1; state_transition para socializing = 5.
    - Test: importanceOf é total — nenhum MemEvent retorna undefined (switch exaustivo; default = 1).
    - Test: summarizeEvent retorna texto natural (contém o nome do evento/skill, NÃO contém `{"type"`).
    - Test: recencyRaw(0) === 1; recencyRaw(halfLife) ≈ 0.5; é monotonicamente decrescente.
    - Test: minMaxNormalizer([1,2,3])(1) === 0, (3) === 1, (2) === 0.5; com todos iguais retorna 0 (trata empate, Park).
  </behavior>
  <action>
    Criar `src/memory/longTerm.ts` com as funções PURAS primeiro (sem DB):

    ```typescript
    import type { MemEvent } from '../cognition/types'
    import { config } from '../config'

    const W_RECENCY = 1, W_IMPORTANCE = 1, W_RELEVANCE = 1 // pesos iguais α=1 (D-07)

    /** Nota de importância heurística 1-10 por tipo de MemEvent (D-06). Switch EXAUSTIVO. */
    export function importanceOf(e: MemEvent): number {
      switch (e.type) {
        case 'world':
          if (e.event === 'damage') return 9
          if (e.event === 'player_joined') return 8   // (refino "já conhecido"=4 fica para o caller com contexto de players; aqui o default do tipo é 8)
          if (e.event === 'hunger') return 6
          if (e.event === 'player_left') return 3
          return 4
        case 'chat_command': return 7
        case 'action':
          if (e.result === 'failure') return 6
          if (e.skill === 'gather' || e.skill === 'dig' || e.skill === 'collect') return 5
          return 2
        case 'state_transition':
          if (e.to === 'socializing' || e.to === 'fighting') return 5
          return 1
        default: return 1
      }
    }

    /** Texto NL canônico a embeddar (NÃO o JSON cru — Anti-pattern). Determinístico, sem LLM. */
    export function summarizeEvent(e: MemEvent): string {
      switch (e.type) {
        case 'world': return `Evento de mundo: ${e.event} — ${e.detail}.`
        case 'action': return `Ação ${e.skill} em ${e.target}: ${e.result}${e.reason ? ` (${e.reason})` : ''}.`
        case 'state_transition': return `Mudei de estado: ${e.from} → ${e.to}.`
        case 'chat_command': return `Comando de chat de ${e.from}: "${e.command}" (modo ${e.mode}).`
      }
    }

    export function recencyRaw(ageMs: number): number {
      return Math.pow(0.5, ageMs / config.retrievalHalfLifeMs)
    }
    export function minMaxNormalizer(xs: number[]): (x: number) => number {
      const lo = Math.min(...xs), hi = Math.max(...xs), span = hi - lo
      return (x) => (span === 0 ? 0 : (x - lo) / span)
    }
    ```
    Exportar `W_RECENCY/W_IMPORTANCE/W_RELEVANCE` (ou mantê-los module-private mas usados na Task 2).

    Criar `src/memory/longTerm.test.ts` cobrindo os comportamentos acima (puro, sem DB — rápido).
  </action>
  <verify>
    <automated>bun test src/memory/longTerm.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/memory/longTerm.ts contém `export function importanceOf`
    - src/memory/longTerm.ts contém `export function summarizeEvent`
    - src/memory/longTerm.ts contém `export function recencyRaw`
    - src/memory/longTerm.ts contém `export function minMaxNormalizer`
    - src/memory/longTerm.test.ts contém ao menos um caso para cada um dos 4 tipos de MemEvent
    - `bun test src/memory/longTerm.test.ts` sai 0
  </acceptance_criteria>
  <done>As funções puras de importância, sumarização e normalização existem e estão testadas; switch de MemEvent exaustivo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: persistEvent (escrita atômica) + retrieve (KNN + scoring ponderado)</name>
  <files>src/memory/longTerm.ts, src/memory/longTerm.test.ts</files>
  <read_first>
    - src/memory/longTerm.ts (as funções puras da Task 1)
    - src/memory/persistence.ts (openDb, EMBEDDING_DIM — assinaturas do Plan 02)
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-01-SUMMARY.md (técnica de bind Float32Array)
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md (Pattern 3 transação; Pattern 4 fluxo de retrieve 1-5 + SQL KNN)
  </read_first>
  <behavior>
    - Test: persistEvent de um MemEvent com importância >= floor insere 1 linha em events E 1 em vec_events (mesmo rowid). Retorna o id.
    - Test: persistEvent de um evento abaixo do floor (importance < config.ltImportanceFloor) NÃO insere (retorna null) — controla crescimento (Pitfall 6).
    - Test: retrieve com um embedding de query retorna candidatos ordenados por score desc; o evento cujo embedding é idêntico ao query (relevância máxima) e recente aparece no topo.
    - Test: após retrieve, last_access dos eventos retornados foi atualizado para `now`.
    - Test: retrieve com embedding null/LLM off NÃO lança — cai para ordenação por recência×importância (graceful, Environment Availability).
    - Usar DB em arquivo temporário (openDb) com cleanup; embeddings sintéticos (Float32Array de EMBEDDING_DIM), sem chamar LM Studio.
  </behavior>
  <action>
    Adicionar a `src/memory/longTerm.ts`:

    ```typescript
    import { Database } from 'bun:sqlite'
    import { EMBEDDING_DIM } from './persistence'

    export interface RetrievedEvent { id: number; summary: string; payload: string; importance: number; score: number }

    // Bind: usar a técnica do 04-01-SUMMARY (Float32Array direto OU Buffer.from(f32.buffer)).
    function toVecBlob(v: number[]): Float32Array /* ou Buffer */ { return new Float32Array(v) }

    /** Persiste um evento + embedding atomicamente (D-05/Pattern 3). Retorna o id, ou null se abaixo do floor. */
    export function persistEvent(db: Database, e: MemEvent, embedding: number[] | null, now: number, player?: string | null): number | null {
      const importance = importanceOf(e)
      if (importance < config.ltImportanceFloor) return null
      const summary = summarizeEvent(e)
      const payload = JSON.stringify(e)
      const tx = db.transaction(() => {
        const res = db.prepare(
          `INSERT INTO events (type, ts, importance, summary, payload, player, last_access) VALUES (?,?,?,?,?,?,?)`
        ).run(e.type, e.timestamp, importance, summary, payload, player ?? null, e.timestamp)
        const id = Number(res.lastInsertRowid)
        if (embedding && embedding.length === EMBEDDING_DIM) {
          db.prepare(`INSERT INTO vec_events (rowid, embedding, ts, importance, event_id) VALUES (?,?,?,?,?)`)
            .run(id, toVecBlob(embedding), e.timestamp, importance, id)
        }
        return id
      })
      return tx()
    }

    /** Recupera os top-N eventos por score Generative Agents (D-07/Pattern 4). */
    export function retrieve(db: Database, queryEmbedding: number[] | null, now: number, opts?: { player?: string; limit?: number }): RetrievedEvent[] { ... }
    ```

    `retrieve` DEVE seguir o Pattern 4 (passos 1-5):
    1. Se `queryEmbedding` existe e tem dimensão certa: KNN no vec_events (`WHERE embedding MATCH ? AND k = config.retrievalK`), JOIN events por rowid; relevance = `1 - distance` (clamp [0,1]). Opcionalmente filtrar por `player` via metadata WHERE ou no JOIN.
    2. Se `queryEmbedding` é null (LLM off): selecionar os `config.retrievalK` eventos mais recentes de events (relevance = 0 para todos).
    3. Para cada candidato: `recency = recencyRaw(now - last_access)`, `importance = events.importance`, `relevance` (do passo 1/2).
    4. Min-max normalizar CADA vetor de fatores SOBRE os candidatos (via minMaxNormalizer); `score = W_RECENCY*recN + W_IMPORTANCE*impN + W_RELEVANCE*relN`; ordenar desc; cortar em `opts?.limit ?? 5`.
    5. `UPDATE events SET last_access = ? WHERE id IN (...)` para os retornados (renova recência, fiel a Park).
    Envolver em try/catch — nunca lançar (degrada para [] e loga).

    Estender `src/memory/longTerm.test.ts` com os 5 comportamentos da Task 2 (DB temporário + cleanup).
  </action>
  <verify>
    <automated>bun test src/memory/longTerm.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/memory/longTerm.ts contém `export function persistEvent`
    - src/memory/longTerm.ts contém `export function retrieve`
    - src/memory/longTerm.ts contém `db.transaction` (escrita atômica)
    - src/memory/longTerm.ts contém `MATCH` (KNN no vec_events)
    - src/memory/longTerm.ts contém `last_access = ` (renovação de recência)
    - src/memory/longTerm.ts contém `config.ltImportanceFloor` (gate de escrita)
    - `bun test src/memory/longTerm.test.ts` sai 0
    - `bun run typecheck` sai 0
  </acceptance_criteria>
  <done>persistEvent escreve evento+vetor atomicamente respeitando o piso de importância; retrieve faz KNN + scoring ponderado normalizado + renova last_access, com fallback gracioso sem embedding.</done>
</task>

</tasks>

<verification>
- `bun test src/memory/longTerm.test.ts` passa (importância + sumarização + scoring + persistência atômica + retrieve).
- `bun run typecheck` limpo.
</verification>

<success_criteria>
MEM-03 está implementado e testado: eventos persistem com embedding na mesma transação e a recuperação combina recência × relevância × importância (min-max [0,1], pesos iguais) exatamente como o baseline Generative Agents. MEM-02 está completo no lado dos eventos.
</success_criteria>

<output>
After completion, create `.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-03-SUMMARY.md`.
DOCUMENTAR: assinaturas de persistEvent/retrieve, a forma do RetrievedEvent, e como o filtro por player foi implementado (metadata WHERE vs JOIN).
</output>
