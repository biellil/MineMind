---
phase: 04-persist-ncia-reflex-o-e-identidade-viva
plan: 07
type: execute
wave: 5
depends_on: ["04-02", "04-03", "04-05", "04-06"]
files_modified:
  - src/cognition/deliberation.ts
  - src/cognition/loop.ts
  - src/llm/prompts.ts
  - src/chat/conversation.ts
  - src/cognition/reflection.integration.smoke.test.ts
autonomous: false
requirements: [REFL-01, SOC-01, SOC-02, MEM-02, MEM-03]

must_haves:
  truths:
    - "A reflexão dispara via a deliberação single-flight existente (não há nó novo no StateGraph)"
    - "O estado de personalidade é reinjetado no prompt sobre a baseline imutável em TODOS os caminhos LLM: ação (deliberation.ts), conversa (conversation.ts) e reflexão (runReflection)"
    - "Eventos sociais (jogador fala/ajuda/ataca) alimentam perfis e trust"
    - "O gate de trust controla pedido-vira-objetivo em ASSISTANT"
    - "A mente é flushed ao disco ao fim de cada ciclo de reflexão"
  artifacts:
    - path: "src/cognition/deliberation.ts"
      provides: "trigger 'reflect' + ramo de reflexão (runReflection) na maybeDeliberate"
      contains: "reflect"
    - path: "src/cognition/loop.ts"
      provides: "disparo do gatilho híbrido de reflexão + recuperação gatilhada + flush por reflexão"
      contains: "shouldReflect"
    - path: "src/llm/prompts.ts"
      provides: "buildPersonaPrompt injeta bloco de personalidade; helper de trust por interlocutor"
      contains: "personality"
    - path: "src/chat/conversation.ts"
      provides: "upsertPlayer + applyTrustEvent + gate de trust no pedido-vira-objetivo"
      contains: "upsertPlayer"
  key_links:
    - from: "src/cognition/loop.ts"
      to: "src/cognition/reflection.ts"
      via: "shouldReflect -> maybeDeliberate('reflect')"
      pattern: "shouldReflect"
    - from: "src/llm/prompts.ts"
      to: "PersonalityState"
      via: "buildPersonaPrompt anexa bloco de estado interno"
      pattern: "personality|mood"
    - from: "src/chat/conversation.ts"
      to: "src/social/profiles.ts"
      via: "upsertPlayer/applyTrustEvent por mensagem de jogador"
      pattern: "upsertPlayer"
---

<objective>
Ligar todas as peças construídas (Plans 03/04/05/06) no loop vivo — a "identidade viva" passa a se manifestar em runtime:
1. **Reflexão (REFL-01/D-12):** disparar `shouldReflect` no loop e reusar `maybeDeliberate` com trigger `'reflect'` (ramo que usa `ReflectionOutputSchema` + `consolidate` + `applyGoalUpdates`), sem nó novo no StateGraph; flush do holder ao fim de cada reflexão (D-02).
2. **Personalidade no prompt (SOC-02/D-14):** `buildPersonaPrompt` reinjeta o `PersonalityState` sobre a baseline imutável.
3. **Perfis sociais (SOC-01/D-15/D-16/D-17):** `conversation.ts` faz upsert do jogador + ajusta trust por interação, e o gate de trust controla pedido-vira-objetivo em ASSISTANT.
4. **Recuperação gatilhada (MEM-03/D-08):** ao refletir, recuperar memórias relevantes para colorir o contexto.

Termina com um checkpoint humano ao vivo provando persistência através de RESTART, reflexão, perfil e personalidade.

Purpose: Fecha REFL-01, SOC-01, SOC-02 em runtime e exercita MEM-02/MEM-03 de ponta a ponta.
Output: deliberation/loop/prompts/conversation wired + smoke de integração + verificação humana.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-CONTEXT.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-03-SUMMARY.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-05-SUMMARY.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-06-SUMMARY.md

<interfaces>
<!-- Deliberação atual (a ESTENDER com trigger 'reflect'). -->
From src/cognition/deliberation.ts:
```typescript
export type DeliberationTrigger = 'chat' | 'goal_changed' | 'need_threshold' | 'periodic'
export async function maybeDeliberate(state, holder, provider, snapshot, trigger, now): Promise<void>
// usa: decideAction(provider, messages, fallback); buildPersonaPrompt(holder.disposition); serializeContext(...)
```

<!-- Loop atual (dispara maybeDeliberate por tick via void; pickTrigger escolhe o trigger). -->
From src/cognition/loop.ts: `startCognitiveLoop(bot, holder)`; `void deliberator.maybeDeliberate(...); function pickTrigger(holder)`

<!-- Prompts (a ESTENDER buildPersonaPrompt para injetar personalidade). -->
From src/llm/prompts.ts: `buildPersonaPrompt(disposition): string` (concatena PERSONA_BASE + trecho de disposição)

<!-- Conversation (a ESTENDER com perfis/trust). -->
From src/chat/conversation.ts: `handleConversation(provider, holder, bot, username, message, now)`; `detectRequestKind(message)`; `makePlayerRequestGoal(kind, now)`

<!-- Peças prontas dos planos anteriores. -->
From src/cognition/reflection.ts: `shouldReflect(args); consolidate(db, recent, now, embedding, summary?); applyGoalUpdates(goals, updates, now); ReflectionState`
From src/llm/schemas.ts: `ReflectionOutputSchema; ReflectionOutput`
From src/social/profiles.ts: `upsertPlayer(db, username, now, displayName?); applyTrustEvent(db, username, kind); getProfile(db, username)`
From src/cognition/personality.ts: `PersonalityState; applyEventToPersonality`
From src/memory/longTerm.ts: `retrieve(db, queryEmbedding, now, opts); persistEvent(db, e, embedding, now, player?)`
From src/memory/holder.persistence.ts: `persistHolder(db, holder, now)`
From src/config.ts: config.trustRequestThreshold; holder.db (Plan 06)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Injetar personalidade no prompt + perfis/trust + gate de trust na conversa</name>
  <files>src/llm/prompts.ts, src/chat/conversation.ts</files>
  <read_first>
    - src/llm/prompts.ts (buildPersonaPrompt + PERSONA_BASE imutável)
    - src/chat/conversation.ts (handleConversation, detectRequestKind, makePlayerRequestGoal, o ramo ASSISTANT)
    - src/social/profiles.ts (upsertPlayer/applyTrustEvent/getProfile — Plan 04 SUMMARY)
    - src/cognition/personality.ts (PersonalityState)
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md (Pattern 6 injeção "cor de prompt" + gate de trust)
  </read_first>
  <action>
    1. Em `src/llm/prompts.ts`, estender `buildPersonaPrompt` para aceitar um `PersonalityState` opcional e anexar um bloco derivado APÓS o trecho de disposição (sobre a PERSONA_BASE imutável — não a substitui):
       ```typescript
       import type { PersonalityState } from '../cognition/personality'

       function moodWord(m: number): string { return m > 0.3 ? 'bom' : m < -0.3 ? 'ruim' : 'neutro' }
       function pct(x: number): string { return `${Math.round(x * 100)}%` }

       export function buildPersonaPrompt(disposition: Disposition, personality?: PersonalityState): string {
         const tail = disposition === 'ASSISTANT' ? PERSONA_ASSISTANT : PERSONA_AUTONOMOUS
         const persona = `${PERSONA_BASE}\n\n${tail}`
         if (!personality) return persona
         return `${persona}\n\nEstado interno atual: humor ${moodWord(personality.mood)}, ` +
           `energia social ${pct(personality.socialEnergy)}, confiança ${pct(personality.confidence)}.`
       }
       ```
       Manter a chamada antiga compatível (personality opcional). Atualizar os call-sites em `deliberation.ts` e `conversation.ts` para passar `holder.personality` (na Task 2 da deliberação; aqui na conversa).

    2. Em `src/chat/conversation.ts`:
       - Importar `upsertPlayer`, `applyTrustEvent`, `getProfile` de `../social/profiles` e `config` de `../config`.
       - No início de `handleConversation`, registrar a interação social (D-15/D-16) se `holder.db`:
         ```typescript
         if (holder.db) {
           upsertPlayer(holder.db, username, now)
           applyTrustEvent(holder.db, username, 'interaction') // +0.01 frequência
         }
         ```
       - Passar a personalidade ao prompt: `buildPersonaPrompt(holder.disposition, holder.personality)`.
       - Colorir o prompt por interlocutor (D-17): se `holder.db`, ler `getProfile` e anexar uma SystemMessage curta:
         ```typescript
         const prof = holder.db ? getProfile(holder.db, username) : undefined
         if (prof && prof.trust < 0) messages.push(new SystemMessage(`Sobre ${username}: confiança baixa — seja cauteloso e mantenha distância.`))
         ```
       - GATE DE TRUST no ramo ASSISTANT (D-17): só transformar pedido em objetivo se o trust >= `config.trustRequestThreshold`:
         ```typescript
         if (holder.disposition === 'ASSISTANT') {
           const kind = detectRequestKind(message)
           const trust = prof?.trust ?? 0
           if (kind !== null && trust >= config.trustRequestThreshold) {
             holder.playerRequestPending = true
             holder.goals.push(makePlayerRequestGoal(kind, now))
           }
         }
         ```
         (Pedido de quem tem trust < limiar recebe resposta conversacional, mas NÃO vira objetivo.)
  </action>
  <verify>
    <automated>bun test src/chat/conversation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/llm/prompts.ts contém `personality?: PersonalityState` e `Estado interno atual`
    - src/chat/conversation.ts contém `upsertPlayer(holder.db`
    - src/chat/conversation.ts contém `applyTrustEvent(holder.db`
    - src/chat/conversation.ts contém `config.trustRequestThreshold`
    - src/chat/conversation.ts contém `buildPersonaPrompt(holder.disposition, holder.personality)`
    - `bun test src/chat/conversation.test.ts` sai 0 (ajustar o teste existente se a assinatura mudou; testes de db null devem passar)
    - `bun run typecheck` sai 0
  </acceptance_criteria>
  <done>O prompt reflete a personalidade; jogadores são perfilados e têm trust ajustado por interação; o gate de trust controla pedido-vira-objetivo.</done>
</task>

<task type="auto">
  <name>Task 2: Reflexão na deliberação single-flight + gatilho no loop + flush por reflexão</name>
  <files>src/cognition/deliberation.ts, src/cognition/loop.ts, src/cognition/reflection.integration.smoke.test.ts</files>
  <read_first>
    - src/cognition/deliberation.ts (maybeDeliberate, DeliberationTrigger, shouldTrigger, inFlight; ATENÇÃO à chamada `buildPersonaPrompt(holder.disposition)` na linha ~99 — caminho de ação)
    - src/cognition/loop.ts (driver, pickTrigger, o void maybeDeliberate por tick)
    - src/cognition/reflection.ts (shouldReflect/consolidate/applyGoalUpdates — Plan 05)
    - src/llm/schemas.ts (ReflectionOutputSchema)
    - src/memory/longTerm.ts (retrieve), src/memory/holder.persistence.ts (persistHolder)
    - src/cognition/loop.smoke.test.ts e loop.phase3.smoke.test.ts (padrão de smoke headless existente)
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md (Pattern 5 encaixe single-flight)
  </read_first>
  <action>
    1. Em `src/cognition/deliberation.ts`:
       - Adicionar `'reflect'` a `DeliberationTrigger`: `export type DeliberationTrigger = 'chat' | 'goal_changed' | 'need_threshold' | 'periodic' | 'reflect'`.
       - Em `shouldTrigger`, retornar `true` para `'reflect'` (a decisão de QUANDO refletir é do shouldReflect no loop; o single-flight aqui só evita sobreposição).
       - Em `maybeDeliberate`, após adquirir o lock `inFlight`, RAMIFICAR por trigger:
         ```typescript
         if (trigger === 'reflect') {
           await runReflection(holder, provider, snapshot, now)
         } else {
           // caminho de ação existente (decideAction + arbiter fallback)
         }
         ```
       - **SOC-02/D-14 (OBRIGATÓRIO — injeção de personalidade no caminho de AÇÃO):** no ramo `else` (caminho de ação existente), a chamada atual em deliberation.ts linha ~99 `new SystemMessage(buildPersonaPrompt(holder.disposition))` DEVE passar a injetar a personalidade evolutiva, virando `new SystemMessage(buildPersonaPrompt(holder.disposition, holder.personality))`. Sem isso, o estado de personalidade (D-14) só apareceria na conversa + reflexão e NÃO no caminho principal de deliberação de ação — o que é uma cobertura PARCIAL de SOC-02. O resto do ramo `else` (serializeContext + decideAction + arbiter fallback) permanece inalterado; somente esta chamada de buildPersonaPrompt muda.
       - Implementar `runReflection` (nova função, exportada para teste):
         ```typescript
         import { ReflectionOutputSchema, type ReflectionOutput } from '../llm/schemas'
         import { consolidate, applyGoalUpdates } from './reflection'
         import { retrieve } from '../memory/longTerm'
         import { getEvents } from '../memory/shortTerm'
         import { persistHolder } from '../memory/holder.persistence'

         export async function runReflection(holder, provider, snapshot, now): Promise<void> {
           const recent = getEvents(holder.memory)
           let summary: string | undefined
           let updates: ReflectionOutput['goalUpdates'] = []
           if (await provider.available()) {
             try {
               // contexto recuperado (D-08): memórias relevantes ao resumo recente
               const recalled = holder.db ? retrieve(holder.db, null, now, { limit: 5 }) : []
               const messages = [
                 new SystemMessage(buildPersonaPrompt(holder.disposition, holder.personality)),
                 new HumanMessage(`Reflita sobre os eventos recentes e atualize objetivos.\n` +
                   serializeContext(snapshot, holder.needs, holder.currentGoal, recent) +
                   (recalled.length ? `\nMemórias relevantes:\n${recalled.map(r => r.summary).join('\n')}` : '')),
               ]
               const out = ReflectionOutputSchema.parse(await provider.decide(ReflectionOutputSchema, messages))
               summary = out.summary
               updates = out.goalUpdates
             } catch (e) {
               console.error('[reflect] LLM inválido — consolidação determinística (no-op em goals):', e instanceof Error ? e.message : e)
             }
           }
           // SEMPRE consolida CP→LP (D-13), com ou sem LLM:
           let emb: number[] | null = null
           if (holder.db && summary) { try { emb = await provider.embed(summary) } catch { emb = null } }
           if (holder.db) consolidate(holder.db, recent, now, emb, summary)
           // atualiza objetivos (fallback no-op se vazio):
           holder.goals = applyGoalUpdates(holder.goals, updates, now)
           // flush da mente ao fim do ciclo de reflexão (D-02):
           if (holder.db) persistHolder(holder.db, holder, now)
         }
         ```
       - `runReflection` NUNCA lança (try/catch interno); respeita o lock `inFlight` herdado de maybeDeliberate (single-flight, D-12).

    2. Em `src/cognition/loop.ts`:
       - Manter uma `ReflectionState` na sessão (`{ lastReflectionAt: -Infinity, importanceAccum: 0 }`).
       - Acumular importância: a cada tick, somar `importanceOf` dos eventos novos da memória ao `importanceAccum` (ou aproximar pela diferença de eventos). Manter simples: somar a importância dos eventos do snapshot/transições do tick.
       - Após o disparo de ação existente, avaliar o gatilho de reflexão:
         ```typescript
         import { shouldReflect } from './reflection'
         import { importanceOf } from '../memory/longTerm'
         // ...
         if (lastSnapshot && shouldReflect({
           enteredIdle: /* derivar do resultado do grafo */ false,
           goalDoneOrFailed: holder.currentGoal == null,
           importanceAccum: reflState.importanceAccum,
           lastReflectionAt: reflState.lastReflectionAt,
           now: Date.now(),
         })) {
           reflState.lastReflectionAt = Date.now()
           reflState.importanceAccum = 0
           void deliberator.maybeDeliberate(deliberator.state, holder, provider, lastSnapshot, 'reflect', Date.now())
         }
         ```
       - Garantir que o disparo de reflexão NÃO bloqueia o tick (`void`) e respeita o single-flight (não sobrepõe ação — o lock `inFlight` cobre).

    3. Criar `src/cognition/reflection.integration.smoke.test.ts` (headless, sem servidor MC/LLM real — usar um provider fake que retorna `available()=true` e um `decide` que devolve um ReflectionOutput válido; DB temporário via openDb):
       - Test: chamar `runReflection` com eventos recentes e um provider fake → um evento episódico é persistido em `events` (consolidação) e `persistHolder` gravou kv['holder'].
       - Test: com provider fake `available()=false` → `runReflection` ainda consolida deterministicamente (sem lançar) e goals ficam inalterados.
       - Test: single-flight — duas chamadas concorrentes de `maybeDeliberate('reflect')` não rodam duas reflexões simultâneas (a 2ª retorna cedo por inFlight).
  </action>
  <verify>
    <automated>bun test src/cognition/reflection.integration.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/cognition/deliberation.ts DeliberationTrigger contém `'reflect'`
    - src/cognition/deliberation.ts contém `buildPersonaPrompt(holder.disposition, holder.personality` (SOC-02/D-14 no caminho de ação — a chamada da linha ~99 foi atualizada)
    - src/cognition/deliberation.ts contém `export async function runReflection`
    - src/cognition/deliberation.ts contém `ReflectionOutputSchema`
    - src/cognition/deliberation.ts contém `persistHolder` (flush por reflexão)
    - src/cognition/loop.ts contém `shouldReflect`
    - src/cognition/loop.ts contém `maybeDeliberate(deliberator.state, holder, provider, lastSnapshot, 'reflect'`
    - `bun test src/cognition/reflection.integration.smoke.test.ts` sai 0
    - `bun test src/cognition` (suíte da cognição) sai 0 — nada regrediu
    - `bun run typecheck` sai 0
  </acceptance_criteria>
  <done>A reflexão dispara pelo gatilho híbrido via a deliberação single-flight (sem nó novo), consolida CP→LP, atualiza objetivos com fallback no-op e faz flush da mente; o caminho de ação da deliberação injeta a personalidade no prompt (SOC-02/D-14 completo); smoke headless verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana ao vivo — persistência, reflexão, perfil, personalidade</name>
  <files>(nenhum — checkpoint de verificação, sem código)</files>
  <action>
    Checkpoint NÃO-AUTÔNOMO: toda a automação foi entregue nas Tasks 1-2. Aqui o executor conduz a verificação ao vivo com o usuário (servidor MC + LM Studio), seguindo EXATAMENTE os passos de <how-to-verify> abaixo, aguarda o <resume-signal>, e registra o veredito no SUMMARY. Não há código a escrever nesta task.
  </action>
  <what-built>
    A "identidade viva" completa: memória LP em SQLite que sobrevive a RESTART, estado vivo (needs/goals/personality) retomado com decay-on-boot, reflexão que consolida memória e atualiza objetivos, perfis por jogador com trust, e personalidade reinjetada no prompt.
  </what-built>
  <how-to-verify>
    Pré-requisitos: servidor Minecraft Java local rodando (MC 1.21.4) e LM Studio com um modelo de chat E um modelo de embedding carregados (EMBEDDING_MODEL no .env). Confirmar `.env` tem DB_PATH (ou usar o default ./minemind.sqlite).

    1. Apagar qualquer `minemind.sqlite*` antigo para um cold start limpo.
    2. `bun start` — confirmar nos logs: "Mente hidratada do disco" (cold start, sem snapshot) e o loop iniciando.
    3. No jogo, entre no servidor e converse com o bot em ASSISTANT (`!ajudante`): mande algumas mensagens e um pedido (ex.: "coleta madeira"). Confirme que ele responde e (com trust suficiente) aceita o pedido como objetivo.
    4. Deixe rodar ~2-3 min para acumular eventos e disparar ao menos uma reflexão (procure logs de reflexão / consolidação).
    5. Inspecione o DB (ex.: via `bun repl` ou um script): a tabela `players` tem seu username com `interactions > 0` e algum `trust`; a tabela `events` tem linhas (incluindo o evento consolidado de alta importância); kv['holder'] existe.
    6. Pare o processo com Ctrl+C — confirme o log "persistindo a mente e fechando o DB".
    7. `bun start` de novo (RESTART COMPLETO) — confirme "Mente hidratada do disco" agora COM dados: needs/goals retomados (goals muito velhos descartados por decay-on-boot), e o bot NÃO recomeça do zero.
    8. Converse de novo — confirme que ele "lembra" de você (perfil persistido, interactions incrementando a partir do valor anterior).
  </how-to-verify>
  <verify>Confirmação humana via resume-signal ("approved" ou descrição da falha com logs).</verify>
  <resume-signal>Digite "approved" se persistência através de restart + reflexão + perfil + personalidade funcionam, ou descreva o que falhou (com logs).</resume-signal>
  <done>O usuário confirmou (ou reportou falhas em) persistência através de RESTART + reflexão + perfil de jogador + personalidade ao vivo.</done>
</task>

</tasks>

<verification>
- `bun test` (suíte completa) passa — nenhuma regressão.
- `bun run typecheck` limpo.
- Checkpoint humano: persistência através de RESTART, reflexão, perfil de jogador e personalidade confirmados ao vivo.
</verification>

<success_criteria>
Todos os 5 requisitos da fase manifestam-se em runtime: MEM-02 (sobrevive a restart), MEM-03 (recuperação semântica exercitada na reflexão), REFL-01 (reflexão consolida + atualiza objetivos via single-flight), SOC-01 (perfil por jogador com trust), SOC-02 (personalidade evolutiva reinjetada). A "identidade viva" está fechada.
</success_criteria>

<output>
After completion, create `.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-07-SUMMARY.md` com o resultado do checkpoint humano e quaisquer gaps observados (ex.: detecção de "atacado por jogador" — Open Question 3 — fica como heurística/gap conhecido).
</output>
