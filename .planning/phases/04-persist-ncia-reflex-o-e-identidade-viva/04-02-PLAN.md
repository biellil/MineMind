---
phase: 04-persist-ncia-reflex-o-e-identidade-viva
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/config.ts
  - src/memory/persistence.ts
  - src/memory/persistence.test.ts
  - src/llm/provider.ts
autonomous: true
requirements: [MEM-02, MEM-03]
user_setup:
  - service: lm-studio
    why: "provider.embed() chama /v1/embeddings; precisa de um modelo de embedding carregado no LM Studio em runtime. O código degrada gracioso se off, mas a relevância semântica (MEM-03) só funciona com ele ligado."
    env_vars:
      - name: EMBEDDING_MODEL
        source: "LM Studio -> modelo de embedding carregado (default: text-embedding-nomic-embed-text-v1.5)"
      - name: EMBEDDING_DIM
        source: "Dimensão do modelo de embedding (nomic-embed-text-v1.5 = 768)"

must_haves:
  truths:
    - "Abrir o DB cria o schema do zero quando o arquivo não existe (cold start)"
    - "O DB sobrevive a uma reabertura: dados escritos antes da close() são lidos depois"
    - "Um DB corrompido não aborta o processo (recuperação graceful)"
    - "provider.embed(text) retorna um array de floats da dimensão configurada"
  artifacts:
    - path: "src/memory/persistence.ts"
      provides: "openDb (PRAGMA/schema/migrations), recuperação graceful, helpers de KV"
      exports: ["openDb", "EMBEDDING_DIM"]
    - path: "src/llm/provider.ts"
      provides: "método embed(text) via fetch direto a /v1/embeddings"
      contains: "embed"
    - path: "src/config.ts"
      provides: "novos knobs .env (dbPath, embeddingModel, embeddingDim, limiares)"
      contains: "dbPath"
  key_links:
    - from: "src/memory/persistence.ts"
      to: "sqlite-vec"
      via: "sqliteVec.load(db) + CREATE VIRTUAL TABLE vec0"
      pattern: "sqliteVec\\.load"
    - from: "src/llm/provider.ts"
      to: "/v1/embeddings"
      via: "fetch direto (contorna OpenAIEmbeddings — Pitfall 1)"
      pattern: "/embeddings"
---

<objective>
Construir a camada de persistência base que TODOS os módulos da fase consomem: `src/memory/persistence.ts` (abrir o DB, carregar `sqlite-vec`, aplicar PRAGMAs de durabilidade, criar o schema `events`/`players`/`places`/`kv`/`vec_events` via `PRAGMA user_version`, e recuperação graceful contra corrupção — D-02/D-03). Adicionar os knobs de config (.env) e o método `embed(text)` ao `LlmProvider` via fetch direto a `/v1/embeddings` (contornando o `OpenAIEmbeddings` frágil — Pitfall 1).

Purpose: É a fundação atômica de MEM-02 (um único SQLite transacional) e o substrato de MEM-03 (índice `vec0` + embeddings). Sem isto, nenhum store ou perfil pode persistir.
Output: persistence.ts (schema + recovery + KV), embed() no provider, config knobs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-CONTEXT.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-01-SUMMARY.md

<interfaces>
<!-- Provider atual (provider.ts) — embed() é ADICIONADO à interface LlmProvider. -->
From src/llm/provider.ts:
```typescript
export interface LlmProvider {
  decide<T>(schema: ZodType<T>, messages: BaseMessage[]): Promise<T>
  chat(messages: BaseMessage[]): Promise<string>
  available(): Promise<boolean>
}
// resolveBaseUrl() já existe (default http://localhost:1234/v1); available() já usa fetch ao /models.
```

<!-- Config atual usa o padrão `process.env.X || 'default'` e valida ranges no fim do arquivo. -->
From src/config.ts: `export const config = { ... } as const` + bloco de validações `throw new Error(...)`.

<!-- Bind de Float32Array: herdar a técnica documentada no 04-01-SUMMARY (Float32Array direto OU Buffer.from). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config knobs + provider.embed() via fetch direto</name>
  <files>src/config.ts, src/llm/provider.ts</files>
  <read_first>
    - src/config.ts (padrão `process.env.X || default` e o bloco de validações no fim)
    - src/llm/provider.ts (interface LlmProvider, resolveBaseUrl, o fetch já usado em available())
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md (Pitfall 1 — código de embed via fetch; Pitfall 2 — validação de dimensão)
  </read_first>
  <action>
    1. Em `src/config.ts`, adicionar ao objeto `config` (dentro do `as const`), na seção "Fase 4":
       ```typescript
       // === Fase 4: Persistência, reflexão, identidade ===
       // D-01/D-02: caminho do arquivo SQLite (store único relacional + vetorial)
       dbPath: process.env.DB_PATH || './minemind.sqlite',
       // D-09: modelo de embedding no LM Studio + dimensão (Pitfall 2 — validar no boot)
       embeddingModel: process.env.EMBEDDING_MODEL || 'text-embedding-nomic-embed-text-v1.5',
       embeddingDim: parseInt(process.env.EMBEDDING_DIM || '768', 10),
       // D-06: limiar de importância mínimo para persistir um evento em LP (descarta ticks triviais — Pitfall 6)
       ltImportanceFloor: parseInt(process.env.LT_IMPORTANCE_FLOOR || '3', 10),
       // D-07: meia-vida de recência (ms) e nº de candidatos KNN antes da reordenação ponderada
       retrievalHalfLifeMs: parseInt(process.env.RETRIEVAL_HALF_LIFE_MS || String(6 * 60 * 60 * 1000), 10),
       retrievalK: parseInt(process.env.RETRIEVAL_K || '12', 10),
       // D-10: gatilho de reflexão (soma de importância) + teto temporal anti-starvation (ms)
       reflectionImportanceThreshold: parseInt(process.env.REFLECTION_IMPORTANCE_THRESHOLD || '50', 10),
       reflectionMaxIntervalMs: parseInt(process.env.REFLECTION_MAX_INTERVAL_MS || String(10 * 60 * 1000), 10),
       // D-17: limiar de trust para pedido-vira-objetivo em ASSISTANT
       trustRequestThreshold: parseFloat(process.env.TRUST_REQUEST_THRESHOLD || '0.0'),
       // D-19: idade máxima (ms) de um goal comprometido antes de ser descartado no boot (decay-on-boot)
       goalStaleMs: parseInt(process.env.GOAL_STALE_MS || String(30 * 60 * 1000), 10),
       ```
       NÃO mudar nenhum knob existente. Adicionar APÓS o último campo da Fase 3 (resourceTargets), antes do `} as const`.

    2. No bloco de validações no fim de `src/config.ts`, adicionar:
       ```typescript
       // Fase 4: validação dos parâmetros de persistência/reflexão
       if (config.embeddingDim < 1) throw new Error(`EMBEDDING_DIM inválido: ${config.embeddingDim}. Deve ser >= 1.`)
       if (config.ltImportanceFloor < 1 || config.ltImportanceFloor > 10) throw new Error(`LT_IMPORTANCE_FLOOR inválido: ${config.ltImportanceFloor}. Deve estar em [1,10].`)
       if (config.retrievalK < 1) throw new Error(`RETRIEVAL_K inválido: ${config.retrievalK}. Deve ser >= 1.`)
       if (config.retrievalHalfLifeMs < 1) throw new Error(`RETRIEVAL_HALF_LIFE_MS inválido: ${config.retrievalHalfLifeMs}. Deve ser >= 1.`)
       if (config.trustRequestThreshold < -1 || config.trustRequestThreshold > 1) throw new Error(`TRUST_REQUEST_THRESHOLD inválido: ${config.trustRequestThreshold}. Deve estar em [-1,1].`)
       ```

    3. Em `src/llm/provider.ts`, adicionar `embed` à interface `LlmProvider`:
       ```typescript
       /** Retorna o vetor de embedding de um texto via /v1/embeddings. Lança em erro de rede/HTTP. */
       embed(text: string): Promise<number[]>
       ```
       E implementar no objeto retornado por `createLmStudioProvider` (usar o `baseURL` já resolvido por `resolveBaseUrl()`, mesma base do probe `available()`):
       ```typescript
       async embed(text: string): Promise<number[]> {
         const res = await fetch(`${baseURL.replace(/\/$/, '')}/embeddings`, {
           method: 'POST',
           headers: { 'Content-Type': 'application/json' },
           body: JSON.stringify({
             model: process.env.EMBEDDING_MODEL || 'text-embedding-nomic-embed-text-v1.5',
             input: text.replace(/\n/g, ' '),
           }),
         })
         if (!res.ok) throw new Error(`embeddings ${res.status}`)
         const json = (await res.json()) as { data: { embedding: number[] }[] }
         return json.data[0].embedding
       }
       ```
       NÃO adicionar `OpenAIEmbeddings` nem novo pacote (Pitfall 1 — fetch direto é deliberado).
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/config.ts contém `dbPath:` e `embeddingDim:` e `reflectionImportanceThreshold:`
    - src/config.ts contém `EMBEDDING_DIM inválido`
    - src/llm/provider.ts contém `embed(text: string): Promise<number[]>` na interface
    - src/llm/provider.ts contém `/embeddings`
    - src/llm/provider.ts NÃO contém `OpenAIEmbeddings`
    - `bun run typecheck` sai 0
  </acceptance_criteria>
  <done>Config tem todos os knobs da Fase 4 com validação de range; o provider expõe embed() via fetch direto; tsc limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: persistence.ts — openDb (schema/PRAGMA/migrations) + recuperação graceful + KV helpers</name>
  <files>src/memory/persistence.ts, src/memory/persistence.test.ts</files>
  <read_first>
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-01-SUMMARY.md (técnica de bind Float32Array herdada do Wave 0)
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md (Pattern 1 abertura; Pattern 2 schema SQL EXATO; Pattern 3 recuperação graceful)
    - src/memory/shortTerm.test.ts (padrão de teste bun:test)
    - src/config.ts (config.dbPath, config.embeddingDim)
  </read_first>
  <behavior>
    - Test 1: openDb num path inexistente cria o schema (user_version vira 1; as 5 tabelas existem via sqlite_master/sql).
    - Test 2: escrever um KV ('k','v') com kvSet, fechar, reabrir o MESMO path, kvGet('k') retorna 'v' (sobrevivência a reabertura = base de MEM-02).
    - Test 3: abrir um arquivo corrompido (escrever bytes lixo num arquivo .sqlite) NÃO lança — openDb retorna um handle utilizável (cold-start de um novo DB renomeando o corrompido), provando recuperação graceful (D-03).
    - Test 4: vec_events tem dimensão = config.embeddingDim (criar e inserir um Float32Array do tamanho certo NÃO lança; do tamanho errado lança).
    - Limpar arquivos *.sqlite/-wal/-shm em afterAll.
  </behavior>
  <action>
    Criar `src/memory/persistence.ts` exportando:

    ```typescript
    import { Database } from 'bun:sqlite'
    import * as sqliteVec from 'sqlite-vec'
    import { renameSync, existsSync } from 'node:fs'
    import { config } from '../config'

    export const EMBEDDING_DIM = config.embeddingDim
    const SCHEMA_VERSION = 1

    export function openDb(path: string = config.dbPath): Database { ... }
    export function kvSet(db: Database, key: string, value: string, now: number): void { ... }
    export function kvGet(db: Database, key: string): string | undefined { ... }
    ```

    `openDb` DEVE:
    1. `const db = new Database(path)`; `sqliteVec.load(db)`.
    2. Aplicar PRAGMAs (D-02, Pattern 1): `PRAGMA journal_mode = WAL`, `PRAGMA synchronous = NORMAL`, `PRAGMA foreign_keys = ON`, `PRAGMA busy_timeout = 5000`.
    3. Ler `PRAGMA user_version`; se `0`, rodar TODO o DDL abaixo (Pattern 2) numa transação e setar `PRAGMA user_version = 1`. O DDL é EXATAMENTE (note `float[${EMBEDDING_DIM}]`):
       ```sql
       CREATE TABLE IF NOT EXISTS events (
         id INTEGER PRIMARY KEY, type TEXT NOT NULL, ts INTEGER NOT NULL,
         importance INTEGER NOT NULL, summary TEXT NOT NULL, payload TEXT NOT NULL,
         player TEXT, last_access INTEGER NOT NULL );
       CREATE INDEX IF NOT EXISTS idx_events_ts ON events(ts);
       CREATE INDEX IF NOT EXISTS idx_events_player ON events(player);
       CREATE TABLE IF NOT EXISTS players (
         username TEXT PRIMARY KEY, display_name TEXT, first_seen INTEGER NOT NULL,
         last_seen INTEGER NOT NULL, interactions INTEGER NOT NULL DEFAULT 0,
         trust REAL NOT NULL DEFAULT 0, notes TEXT );
       CREATE TABLE IF NOT EXISTS places (
         key TEXT PRIMARY KEY, label TEXT, x INTEGER NOT NULL, y INTEGER NOT NULL,
         z INTEGER NOT NULL, first_seen INTEGER NOT NULL, last_seen INTEGER NOT NULL,
         visits INTEGER NOT NULL DEFAULT 1, notes TEXT );
       CREATE TABLE IF NOT EXISTS kv ( key TEXT PRIMARY KEY, value TEXT NOT NULL, ts INTEGER NOT NULL );
       CREATE VIRTUAL TABLE IF NOT EXISTS vec_events USING vec0(
         embedding float[${EMBEDDING_DIM}] distance_metric=cosine,
         ts integer, importance integer, +event_id integer );
       ```
       (A virtual table NÃO pode entrar em transação com algumas versões — criar `vec_events` FORA da transação se `db.transaction` falhar nela; documentar a ordem.)
    4. Recuperação graceful (D-03, Pattern 3): envolver os passos 1–3 em try/catch. Se `sqliteVec.load` ou o DDL ou `PRAGMA integrity_check` lançar/falhar, logar a perda, e SE o arquivo existe e está corrompido: `renameSync(path, \`${path}.corrupt-${Date.now()}\`)` e reabrir um DB novo cold-start (recursão única). NUNCA propagar — o Core Value exige que o loop sempre rode. Como último recurso retornar um DB válido vazio.

    `kvSet`: `INSERT OR REPLACE INTO kv (key,value,ts) VALUES (?,?,?)`.
    `kvGet`: `SELECT value FROM kv WHERE key=?` → `row?.value`.

    Bind de Float32Array no teste de dimensão: usar a técnica documentada no 04-01-SUMMARY.

    Criar `src/memory/persistence.test.ts` cobrindo os 4 comportamentos acima (DB em arquivo temporário com cleanup em afterAll).
  </action>
  <verify>
    <automated>bun test src/memory/persistence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/memory/persistence.ts contém `export function openDb`
    - src/memory/persistence.ts contém `sqliteVec.load`
    - src/memory/persistence.ts contém `PRAGMA journal_mode = WAL`
    - src/memory/persistence.ts contém `CREATE VIRTUAL TABLE IF NOT EXISTS vec_events`
    - src/memory/persistence.ts contém `user_version`
    - src/memory/persistence.ts contém `.corrupt-` (renomeação de DB corrompido)
    - src/memory/persistence.ts contém `export function kvSet` e `export function kvGet`
    - `bun test src/memory/persistence.test.ts` sai 0 (os 4 testes passam)
  </acceptance_criteria>
  <done>openDb cria o schema do zero, sobrevive a reabertura, recupera graciosamente de corrupção e valida a dimensão do vec0; KV helpers funcionam; testes verdes.</done>
</task>

</tasks>

<verification>
- `bun test src/memory/persistence.test.ts` passa.
- `bun run typecheck` limpo.
- O DB criado tem `PRAGMA user_version = 1` e as 5 tabelas (events/players/places/kv/vec_events).
</verification>

<success_criteria>
A fundação de MEM-02 existe: um único arquivo SQLite com schema versionado, WAL, recuperação graceful e um índice vetorial dimensionado; o provider sabe embeddar texto. Stores (Plan 03), perfis (Plan 04) e o holder durável (Plan 05) podem agora persistir.
</success_criteria>

<output>
After completion, create `.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-02-SUMMARY.md`.
DOCUMENTAR: assinaturas exatas de openDb/kvSet/kvGet, se vec_events foi criada dentro ou fora de transação, e a técnica de bind de Float32Array herdada.
</output>
