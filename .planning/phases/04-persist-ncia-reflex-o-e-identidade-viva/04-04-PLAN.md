---
phase: 04-persist-ncia-reflex-o-e-identidade-viva
plan: 04
type: tdd
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/social/profiles.ts
  - src/social/profiles.test.ts
  - src/cognition/personality.ts
  - src/cognition/personality.test.ts
autonomous: true
requirements: [SOC-01, SOC-02]

must_haves:
  truths:
    - "Um jogador visto pela primeira vez cria um perfil persistido; revê-lo incrementa interactions"
    - "Eventos verificáveis (deu item / atacou / interação) ajustam trust deterministicamente, clampado em [-1,1]"
    - "O LLM nunca calcula trust — só deltas determinísticos o alteram"
    - "PersonalityState (mood/socialEnergy/confidence) é atualizado por contadores determinísticos sobre uma baseline"
    - "Nenhum parâmetro de modelo é treinado (fronteira estrutural vs ADV-01)"
  artifacts:
    - path: "src/social/profiles.ts"
      provides: "upsert de players, bumpTrust por evento, getProfile (SOC-01/D-15/D-16)"
      exports: ["upsertPlayer", "applyTrustEvent", "getProfile", "TRUST_DELTA"]
    - path: "src/cognition/personality.ts"
      provides: "PersonalityState + defaultPersonality + applyEventToPersonality + decayPersonality (SOC-02/D-14)"
      exports: ["PersonalityState", "defaultPersonality", "applyEventToPersonality", "decayPersonality"]
  key_links:
    - from: "src/social/profiles.ts"
      to: "players (tabela)"
      via: "INSERT ... ON CONFLICT(username) DO UPDATE"
      pattern: "ON CONFLICT"
    - from: "src/cognition/personality.ts"
      to: "MemEvent"
      via: "applyEventToPersonality deriva deltas determinísticos de eventos"
      pattern: "MemEvent"
---

<objective>
Construir a "identidade social" da fase, em dois módulos independentes que só dependem da persistência (Plan 02):
1. `src/social/profiles.ts` — perfis por jogador (SOC-01/D-16): upsert de `players`, `trust` como escalar determinístico atualizado por eventos verificáveis do Mineflayer (D-15), clampado [-1,1]. O LLM nunca calcula `trust`.
2. `src/cognition/personality.ts` — personalidade evolutiva (SOC-02/D-14): `PersonalityState` estruturado (mood/socialEnergy/confidence) atualizado por contadores determinísticos sobre uma baseline imutável + decaimento por tempo (mean-reversion). NENHUM ML — a fronteira vs ADV-01 (v2) é estrutural.

Purpose: SOC-01 (perfil por jogador) e SOC-02 (personalidade que evolui de uma baseline sem ML).
Output: profiles.ts + personality.ts, ambos testados como módulos puros/determinísticos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-CONTEXT.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-02-SUMMARY.md

<interfaces>
<!-- Persistence (Plan 02) fornece o Database com a tabela players já no schema. -->
From src/memory/persistence.ts: `export function openDb(path?): Database`
Tabela players (schema do Plan 02): username PK, display_name, first_seen, last_seen, interactions, trust REAL DEFAULT 0, notes.

<!-- MemEvent: applyEventToPersonality e applyTrustEvent derivam deltas dele. -->
From src/cognition/types.ts:
```typescript
export type MemEvent =
  | { type: 'state_transition'; ... }
  | { type: 'action'; skill: string; target: string; result: 'success' | 'failure'; ... }
  | { type: 'world'; event: 'damage' | 'hunger' | 'player_joined' | 'player_left'; ... }
  | { type: 'chat_command'; command: string; from: string; mode: ControlMode; ... }
```

<!-- Padrão de módulo puro estabelecido (needs.ts/goals.ts): tempo por parâmetro, sem Date.now()/config global onde possível. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: profiles.ts — upsert de players + trust determinístico (SOC-01/D-15/D-16)</name>
  <files>src/social/profiles.ts, src/social/profiles.test.ts</files>
  <read_first>
    - src/memory/persistence.ts (openDb; schema da tabela players — Plan 02 SUMMARY)
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md (Pattern 6 TRUST_DELTA + clamp; "Code Examples" upsert SQL)
    - src/motivation/needs.ts (padrão de módulo puro/clamp para replicar o estilo)
  </read_first>
  <behavior>
    - Test: upsertPlayer de um username novo cria a linha (interactions=1, trust=0, first_seen=last_seen=now).
    - Test: upsertPlayer do MESMO username de novo incrementa interactions para 2 e atualiza last_seen.
    - Test: applyTrustEvent(db, user, 'gaveItem') sobe trust em +0.20; 'attacked' desce -0.40; clamp em [-1,1] (várias chamadas 'attacked' não passam de -1).
    - Test: getProfile retorna { username, interactions, trust, ... } ou undefined se ausente.
    - Test: trust nunca é alterado por nenhuma string livre/LLM — só pelos kinds de TRUST_DELTA (chamar com kind inválido é erro de tipo / não compila).
    - DB temporário via openDb + cleanup.
  </behavior>
  <action>
    Criar `src/social/profiles.ts`:

    ```typescript
    import { Database } from 'bun:sqlite'

    /** Deltas determinísticos de trust por evento verificável do Mineflayer (D-15). LLM nunca calcula. */
    export const TRUST_DELTA = {
      gaveItem: 0.20, helped: 0.10, attacked: -0.40, stole: -0.30, interaction: 0.01,
    } as const
    export type TrustEventKind = keyof typeof TRUST_DELTA
    const TRUST_MIN = -1, TRUST_MAX = 1

    export interface PlayerProfile {
      username: string; displayName: string | null; firstSeen: number; lastSeen: number
      interactions: number; trust: number; notes: string | null
    }

    /** Upsert de um jogador (D-16): cria ou atualiza last_seen + incrementa interactions. */
    export function upsertPlayer(db: Database, username: string, now: number, displayName?: string): void {
      db.prepare(`
        INSERT INTO players (username, display_name, first_seen, last_seen, interactions, trust)
        VALUES (?, ?, ?, ?, 1, 0)
        ON CONFLICT(username) DO UPDATE SET
          last_seen = excluded.last_seen,
          interactions = interactions + 1,
          display_name = COALESCE(excluded.display_name, players.display_name)
      `).run(username, displayName ?? null, now, now)
    }

    /** Aplica um delta de trust determinístico, clampado em [-1,1] (D-15). */
    export function applyTrustEvent(db: Database, username: string, kind: TrustEventKind): void {
      const delta = TRUST_DELTA[kind]
      db.prepare(`UPDATE players SET trust = max(?, min(?, trust + ?)) WHERE username = ?`)
        .run(TRUST_MIN, TRUST_MAX, delta, username)
    }

    export function getProfile(db: Database, username: string): PlayerProfile | undefined { ... }
    ```
    `getProfile` mapeia a linha SQL para PlayerProfile (snake_case → camelCase). Garantir que `applyTrustEvent` só aceita `TrustEventKind` (tipagem fecha a porta ao LLM).

    Criar `src/social/profiles.test.ts` cobrindo os comportamentos acima (DB temporário + cleanup).
  </action>
  <verify>
    <automated>bun test src/social/profiles.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/social/profiles.ts contém `export function upsertPlayer`
    - src/social/profiles.ts contém `ON CONFLICT(username) DO UPDATE`
    - src/social/profiles.ts contém `export const TRUST_DELTA`
    - src/social/profiles.ts contém `max(?, min(?, trust + ?))` (clamp determinístico)
    - src/social/profiles.ts contém `export function getProfile`
    - `bun test src/social/profiles.test.ts` sai 0
  </acceptance_criteria>
  <done>Perfis de jogador persistem por upsert; trust é um escalar determinístico clampado [-1,1] alterado só por TRUST_DELTA; getProfile lê o perfil.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: personality.ts — PersonalityState evolutivo determinístico (SOC-02/D-14)</name>
  <files>src/cognition/personality.ts, src/cognition/personality.test.ts</files>
  <read_first>
    - src/cognition/types.ts (MemEvent — a fonte dos deltas)
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md (Pattern 6 PersonalityState + regras determinísticas de update + mean-reversion no boot)
    - src/llm/prompts.ts (PERSONA_BASE imutável — a baseline que a personalidade NÃO substitui, só colore; o wiring do prompt é o Plan 05)
  </read_first>
  <behavior>
    - Test: defaultPersonality(now) retorna { mood: 0, socialEnergy: 1, confidence: 0.5, updatedAt: now } (baseline neutra).
    - Test: applyEventToPersonality com world/damage baixa mood (-0.15, clamp -1).
    - Test: applyEventToPersonality com action success de goal sobe mood (+0.05) e confidence (+0.05); action failure baixa confidence (-0.08).
    - Test: applyEventToPersonality com state_transition para socializing baixa socialEnergy (-0.1, clamp 0).
    - Test: decayPersonality aplica mean-reversion: mood→0 e confidence→0.5 ao longo do tempo; socialEnergy recarrega para 1 (+0.01/min). Após muito tempo, converge à baseline.
    - Test: todos os campos permanecem nos ranges (mood [-1,1], socialEnergy [0,1], confidence [0,1]).
    - Módulo PURO — tempo por parâmetro, sem Date.now() interno; sem nenhuma chamada de LLM (fronteira estrutural vs ADV-01).
  </behavior>
  <action>
    Criar `src/cognition/personality.ts`:

    ```typescript
    import type { MemEvent } from './types'

    /** Estado de personalidade evolutivo (D-14). Campos derivados — NENHUM parâmetro de modelo. */
    export interface PersonalityState {
      mood: number          // -1..1
      socialEnergy: number  // 0..1
      confidence: number    // 0..1
      updatedAt: number
    }

    const clampSigned = (x: number) => Math.max(-1, Math.min(1, x))
    const clamp01 = (x: number) => Math.max(0, Math.min(1, x))
    const BASELINE_MOOD = 0, BASELINE_CONFIDENCE = 0.5

    export function defaultPersonality(now: number): PersonalityState {
      return { mood: BASELINE_MOOD, socialEnergy: 1, confidence: BASELINE_CONFIDENCE, updatedAt: now }
    }

    /** Atualiza o estado por contadores determinísticos derivados de um MemEvent (D-14). Imutável. */
    export function applyEventToPersonality(p: PersonalityState, e: MemEvent, now: number): PersonalityState {
      let { mood, socialEnergy, confidence } = p
      if (e.type === 'world' && e.event === 'damage') mood = clampSigned(mood - 0.15)
      else if (e.type === 'action' && e.result === 'success') { mood = clampSigned(mood + 0.05); confidence = clamp01(confidence + 0.05) }
      else if (e.type === 'action' && e.result === 'failure') confidence = clamp01(confidence - 0.08)
      else if (e.type === 'state_transition' && e.to === 'socializing') socialEnergy = clamp01(socialEnergy - 0.1)
      return { mood, socialEnergy, confidence, updatedAt: now }
    }

    /** Mean-reversion por tempo decorrido (boot/idle): converge à baseline; socialEnergy recarrega. */
    export function decayPersonality(p: PersonalityState, now: number): PersonalityState { ... }
    ```
    `decayPersonality`: calcular `elapsedMin = (now - p.updatedAt) / 60000`; mover `mood` e `confidence` em direção às baselines por uma taxa pequena por minuto (ex.: 0.02/min, clampada para não ultrapassar a baseline); `socialEnergy = clamp01(socialEnergy + 0.01 * elapsedMin)`. Atualizar updatedAt = now. Tolerar elapsed <= 0 (retorna inalterado).

    Criar `src/cognition/personality.test.ts` cobrindo os comportamentos acima.
  </action>
  <verify>
    <automated>bun test src/cognition/personality.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/cognition/personality.ts contém `export interface PersonalityState`
    - src/cognition/personality.ts contém `export function defaultPersonality`
    - src/cognition/personality.ts contém `export function applyEventToPersonality`
    - src/cognition/personality.ts contém `export function decayPersonality`
    - src/cognition/personality.ts NÃO contém `provider` nem `LlmProvider` nem `import ... openai` (fronteira estrutural — sem LLM)
    - `bun test src/cognition/personality.test.ts` sai 0
    - `bun run typecheck` sai 0
  </acceptance_criteria>
  <done>PersonalityState evolui por contadores determinísticos sobre uma baseline e reverte à média por tempo; nenhum ML/LLM toca o estado; ranges respeitados.</done>
</task>

</tasks>

<verification>
- `bun test src/social/profiles.test.ts src/cognition/personality.test.ts` passa.
- `bun run typecheck` limpo.
- Nenhum dos dois módulos importa o provider/LLM (fronteira estrutural D-14/D-15).
</verification>

<success_criteria>
SOC-01 e SOC-02 implementados e testados: perfis por jogador com trust determinístico persistido, e personalidade evolutiva estruturada que muda só por estado reinjetado (não por treino). A injeção no prompt e o disparo por eventos reais são wiring do Plan 05.
</success_criteria>

<output>
After completion, create `.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-04-SUMMARY.md`.
DOCUMENTAR: assinaturas exatas de upsertPlayer/applyTrustEvent/getProfile e de defaultPersonality/applyEventToPersonality/decayPersonality (o Plan 05 importa estas).
</output>
