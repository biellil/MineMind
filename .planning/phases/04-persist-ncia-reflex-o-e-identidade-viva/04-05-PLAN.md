---
phase: 04-persist-ncia-reflex-o-e-identidade-viva
plan: 05
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - src/cognition/types.ts
  - src/cognition/states.ts
  - src/llm/schemas.ts
  - src/cognition/reflection.ts
  - src/cognition/reflection.test.ts
autonomous: true
requirements: [REFL-01]

must_haves:
  truths:
    - "'reflecting' existe no enum CognitiveState e no PRIORITY_ORDER com prioridade baixa (antes de idle)"
    - "O gatilho híbrido de reflexão dispara por event-driven OU acúmulo de importância OU piso temporal"
    - "Uma reflexão consolida eventos recentes CP→LP (promove um evento episódico) mesmo sem LLM"
    - "Com LLM, a reflexão produz summary + goalUpdates validados por Zod; inválido => no-op gracioso"
  artifacts:
    - path: "src/cognition/reflection.ts"
      provides: "shouldReflect (gatilho híbrido), consolidate (CP→LP), applyGoalUpdates (REFL-01/D-10/D-13)"
      exports: ["shouldReflect", "consolidate", "applyGoalUpdates", "ReflectionState"]
    - path: "src/llm/schemas.ts"
      provides: "ReflectionOutputSchema (summary + goalUpdates)"
      contains: "ReflectionOutputSchema"
    - path: "src/cognition/states.ts"
      provides: "PRIORITY_ORDER com 'reflecting'"
      contains: "reflecting"
  key_links:
    - from: "src/cognition/reflection.ts"
      to: "src/memory/longTerm.ts"
      via: "consolidate chama persistEvent para promover CP→LP"
      pattern: "persistEvent"
    - from: "src/cognition/reflection.ts"
      to: "src/motivation/goals.ts"
      via: "applyGoalUpdates reordena/dropa goals do holder"
      pattern: "goalUpdates"
---

<objective>
Implementar REFL-01: o estado `reflecting` e a lógica de reflexão. (1) Adicionar `reflecting` ao enum `CognitiveState` e ao `PRIORITY_ORDER` com prioridade baixa, sempre preemptível (D-11). (2) Definir `ReflectionOutputSchema` (Zod) para o produto faseado (D-13). (3) Criar `src/cognition/reflection.ts` com o gatilho híbrido (D-10), a consolidação CP→LP (sempre, mesmo sem LLM) e a aplicação de goalUpdates (D-13). A reflexão NÃO é um nó novo no StateGraph — reusa a deliberação single-flight (o wiring do disparo é o Plan 06); este plano entrega as PEÇAS puras + schema + enum.

Purpose: REFL-01 ("no Reflecting o agente revisa acontecimentos, consolida memória e atualiza objetivos").
Output: enum/PRIORITY_ORDER atualizados, ReflectionOutputSchema, reflection.ts testado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-CONTEXT.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-03-SUMMARY.md

<interfaces>
<!-- Enum e prioridade atuais (a ESTENDER). -->
From src/cognition/types.ts:
```typescript
export type CognitiveState = 'idle' | 'exploring' | 'gathering' | 'socializing' | 'fighting' | 'building'
```
From src/cognition/states.ts:
```typescript
export const PRIORITY_ORDER: ReadonlyArray<CognitiveState> = ['socializing', 'gathering', 'exploring', 'idle']
```

<!-- Schema existente + padrão Zod (a ESTENDER, não substituir). -->
From src/llm/schemas.ts: `export const ActionDecisionSchema = z.object({ action: z.enum([...]), target, reason })`

<!-- longTerm (Plan 03): consolidate promove CP→LP. -->
From src/memory/longTerm.ts: `persistEvent(db, e: MemEvent, embedding: number[]|null, now, player?) => number|null; importanceOf(e); retrieve(db, queryEmbedding, now, opts)`

<!-- Goals + memória CP que a reflexão lê. -->
From src/motivation/types.ts: `interface Goal { id; kind; priority; progress; dependsOn; source; committedAt }`
From src/memory/shortTerm.ts: `getEvents(mem): ReadonlyArray<MemEvent>`
From src/cognition/types.ts: `MemEvent` (4 variantes discriminadas por type)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: enum 'reflecting' + PRIORITY_ORDER + ReflectionOutputSchema</name>
  <files>src/cognition/types.ts, src/cognition/states.ts, src/llm/schemas.ts</files>
  <read_first>
    - src/cognition/types.ts (a union CognitiveState atual)
    - src/cognition/states.ts (PRIORITY_ORDER e STUB_STATES atuais)
    - src/llm/schemas.ts (padrão z.object existente)
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md (Pattern 5 ReflectionOutputSchema verbatim; D-11 posição no PRIORITY_ORDER)
  </read_first>
  <action>
    1. Em `src/cognition/types.ts`, adicionar `'reflecting'` à union `CognitiveState` (após 'socializing', antes dos stubs):
       ```typescript
       export type CognitiveState =
         | 'idle'
         | 'exploring'
         | 'gathering'
         | 'socializing'
         | 'reflecting'   // D-11: trabalho de "tempo livre", prioridade baixa, sempre preemptível
         | 'fighting'   // stub (D-06)
         | 'building'   // stub (D-06)
       ```
    2. Em `src/cognition/states.ts`, inserir `'reflecting'` no `PRIORITY_ORDER` com prioridade baixa — penúltimo, ANTES de 'idle' (D-11):
       ```typescript
       export const PRIORITY_ORDER: ReadonlyArray<CognitiveState> = ['socializing', 'gathering', 'exploring', 'reflecting', 'idle']
       ```
       NÃO mover os demais. NÃO adicionar 'reflecting' a STUB_STATES (ele tem lógica real).
    3. Em `src/llm/schemas.ts`, adicionar o schema da saída da reflexão (D-13/Pattern 5) APÓS o ActionDecisionSchema existente (não tocar nele):
       ```typescript
       /** Saída restrita da reflexão (REFL-01/D-13): consolidação + deltas de objetivo. */
       export const ReflectionOutputSchema = z.object({
         summary: z.string().max(500),
         goalUpdates: z.array(z.object({
           id: z.string(),
           action: z.enum(['keep', 'drop', 'reprioritize']),
           priority: z.number().min(0).max(1).optional(),
         })).max(8).default([]),
       })
       export type ReflectionOutput = z.infer<typeof ReflectionOutputSchema>
       ```
    4. Verificar que nenhum switch exaustivo sobre CognitiveState quebrou ao adicionar 'reflecting' (rodar typecheck — em especial `cognitiveStateToAction` em deliberation.ts: se houver `default`, ok; se não, mapear 'reflecting' → 'idle'). Se quebrar, adicionar o caso `case 'reflecting': return 'idle'` onde necessário.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/cognition/types.ts contém `'reflecting'`
    - src/cognition/states.ts PRIORITY_ORDER contém `'reflecting'` posicionado entre `'exploring'` e `'idle'`
    - src/llm/schemas.ts contém `export const ReflectionOutputSchema`
    - src/llm/schemas.ts contém `goalUpdates`
    - `bun run typecheck` sai 0 (nenhum switch exaustivo quebrado)
  </acceptance_criteria>
  <done>'reflecting' está no enum e no PRIORITY_ORDER (prioridade baixa); ReflectionOutputSchema existe; tsc limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reflection.ts — gatilho híbrido + consolidação CP→LP + applyGoalUpdates</name>
  <files>src/cognition/reflection.ts, src/cognition/reflection.test.ts</files>
  <read_first>
    - src/cognition/reflection.ts (não existe ainda — criar)
    - src/memory/longTerm.ts (persistEvent, importanceOf — Plan 03 SUMMARY)
    - src/memory/shortTerm.ts (getEvents)
    - src/motivation/goals.ts e src/motivation/types.ts (Goal, selectGoal)
    - src/llm/schemas.ts (ReflectionOutput — Task 1)
    - src/config.ts (config.reflectionImportanceThreshold, reflectionMaxIntervalMs)
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md (Pattern 5 gatilho híbrido + consolidação + fallback)
  </read_first>
  <behavior>
    - Test: shouldReflect retorna true por event-driven (enteredIdle && goalDoneOrFailed).
    - Test: shouldReflect retorna true por acúmulo (soma de importanceOf(eventos novos) >= config.reflectionImportanceThreshold).
    - Test: shouldReflect retorna true por piso temporal (now - lastReflectionAt >= config.reflectionMaxIntervalMs).
    - Test: shouldReflect retorna false quando nenhuma condição vale.
    - Test: consolidate (sem LLM/summary) promove um evento episódico CP→LP — chama persistEvent com um MemEvent derivado dos N eventos de maior importância do ring buffer (importância alta), retorna o id ou null.
    - Test: consolidate (com summary do LLM) usa o summary como conteúdo do evento promovido.
    - Test: applyGoalUpdates aplica 'drop' (remove o goal por id), 'reprioritize' (muda priority), 'keep' (no-op); ids desconhecidos são ignorados; lista vazia => goals inalterados (fallback no-op seguro).
    - Módulo testável: tempo/eventos/config por parâmetro onde viável; persistEvent mockável via DB temporário.
  </behavior>
  <action>
    Criar `src/cognition/reflection.ts`:

    ```typescript
    import { Database } from 'bun:sqlite'
    import type { MemEvent } from './types'
    import type { Goal } from '../motivation/types'
    import type { ReflectionOutput } from '../llm/schemas'
    import { persistEvent, importanceOf } from '../memory/longTerm'
    import { config } from '../config'

    /** Estado do gatilho de reflexão (vive no holder/closure — testável). */
    export interface ReflectionState { lastReflectionAt: number; importanceAccum: number }

    /** Gatilho híbrido (D-10): event-driven OU acúmulo de importância OU piso temporal. */
    export function shouldReflect(args: {
      enteredIdle: boolean; goalDoneOrFailed: boolean;
      importanceAccum: number; lastReflectionAt: number; now: number;
    }): boolean {
      const eventDriven = args.enteredIdle && args.goalDoneOrFailed
      const accum = args.importanceAccum >= config.reflectionImportanceThreshold
      const floor = args.now - args.lastReflectionAt >= config.reflectionMaxIntervalMs
      return eventDriven || accum || floor
    }

    /**
     * Consolida CP→LP (D-13): promove os eventos recentes de maior importância a UM evento
     * episódico persistido em LP. Sempre roda (mesmo sem LLM). `summary` opcional vem do LLM.
     */
    export function consolidate(
      db: Database, recent: ReadonlyArray<MemEvent>, now: number,
      embedding: number[] | null, summary?: string,
    ): number | null {
      if (recent.length === 0) return null
      const top = [...recent].sort((a, b) => importanceOf(b) - importanceOf(a)).slice(0, 5)
      const detail = summary ?? top.map((e) => `${e.type}`).join('; ')
      const promoted: MemEvent = { type: 'world', event: 'player_joined' /* placeholder? NÃO */, ... }
      // CORRETO: usar um MemEvent que represente consolidação. Como MemEvent não tem um 'reflection'
      // variant, promover como um 'world' com detail = resumo NÃO encaixa semanticamente.
      // DECISÃO: persistir DIRETAMENTE em events com type='reflection' via persistEvent NÃO é possível
      // (persistEvent recebe MemEvent). Então: criar um MemEvent sintético do tipo mais próximo OU
      // expor um helper persistConsolidation em longTerm. Preferir: adicionar em longTerm.ts (Plan 03
      // já fechado) NÃO — em vez disso, persistir o resumo como um evento 'chat_command' sintético é
      // ruim. USAR: gravar o resumo num MemEvent representado como state_transition->reflecting? Também ruim.
      return null // ver action: a escolha concreta está abaixo
    }

    export function applyGoalUpdates(goals: Goal[], updates: ReflectionOutput['goalUpdates'], now: number): Goal[] { ... }
    ```

    RESOLVER a consolidação concretamente (a nota acima é deliberada para o executor decidir limpo): como `persistEvent` recebe um `MemEvent` e `MemEvent` não tem variante "reflexão", a abordagem CORRETA é: persistir o evento consolidado representando-o como um `MemEvent` do tipo `action` sintético — `{ type: 'action', skill: 'reflect', target: 'memory', result: 'success', reason: detail.slice(0,200), timestamp: now }`. Isso passa por `importanceOf` (action success mundano = 2) o que é BAIXO demais para sobreviver ao floor. Portanto, NÃO usar persistEvent para a consolidação: em vez disso, inserir DIRETAMENTE em `events` com `importance` ALTA (ex.: 8) e o `summary = detail`, mais o embedding na mesma transação — replicando o padrão de persistEvent mas com importância forçada. Implementar essa inserção atômica em `consolidate` usando o mesmo SQL de persistEvent (events + vec_events na mesma transação).

    **DUPLICAÇÃO INTENCIONAL (decidido em revisão — opção "inline documentado", não helper):** este INSERT em `events` + `vec_events` replica DELIBERADAMENTE o pipeline de `persistEvent` do Plan 04-03 (`longTerm.ts`), porém com a importância FORÇADA alta (ex.: 8) em vez de derivada de `importanceOf`. NÃO refatorar `persistEvent` nem extrair um helper `persistConsolidation` em `longTerm.ts` (o Plan 04-03 é um plano TDD já fechado; reabri-lo custa mais que o ganho). Em vez disso, ao implementar:
      - Reusar EXATAMENTE a mesma técnica de bind do blob de embedding documentada no 04-01-SUMMARY / usada por persistEvent (Float32Array ou Buffer.from(f32.buffer)). Esta é uma **dependência de técnica compartilhada com 04-03**: se a forma do bind mudar lá, este INSERT precisa acompanhar.
      - Manter o MESMO conjunto de colunas/ordem do INSERT de persistEvent (events: type, ts, importance, summary, payload, player, last_access; vec_events: rowid, embedding, ts, importance, event_id) para não divergir do schema do Plan 04-02.
      - Adicionar um comentário no código de `consolidate`: "// DUPLICAÇÃO INTENCIONAL de persistEvent (longTerm.ts): MemEvent não modela reflexão; importância forçada alta. Manter em sincronia com o bind/schema do Plan 04-03/04-02."
    Documentar essa duplicação intencional e a dependência de técnica de bind compartilhada com 04-03 também no SUMMARY (gap conhecido: MemEvent não modela reflexão — consolidação grava direto com importância alta).

    `applyGoalUpdates`: mapear updates por id; 'drop' filtra fora; 'reprioritize' seta `priority` (se fornecido); 'keep'/desconhecido = inalterado; retornar novo array (imutável).

    Criar `src/cognition/reflection.test.ts` cobrindo os comportamentos (DB temporário para consolidate; cleanup).
  </action>
  <verify>
    <automated>bun test src/cognition/reflection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/cognition/reflection.ts contém `export function shouldReflect`
    - src/cognition/reflection.ts contém `export function consolidate`
    - src/cognition/reflection.ts contém `export function applyGoalUpdates`
    - src/cognition/reflection.ts contém `config.reflectionImportanceThreshold` e `config.reflectionMaxIntervalMs`
    - src/cognition/reflection.ts contém `db.transaction` (consolidação atômica em events + vec_events)
    - `bun test src/cognition/reflection.test.ts` sai 0
    - `bun run typecheck` sai 0
  </acceptance_criteria>
  <done>O gatilho híbrido de reflexão decide por três condições; a consolidação promove um evento episódico de alta importância CP→LP atomicamente (mesmo sem LLM); applyGoalUpdates aplica keep/drop/reprioritize com fallback no-op.</done>
</task>

</tasks>

<verification>
- `bun test src/cognition/reflection.test.ts src/llm/schemas.test.ts` passa.
- `bun run typecheck` limpo (enum estendido sem quebrar switches).
- A suíte existente continua verde: `bun test src/cognition`.
</verification>

<success_criteria>
REFL-01 está implementado em peças puras + schema + enum: o estado `reflecting` existe com prioridade baixa, o gatilho híbrido decide quando refletir, e a reflexão consolida memória CP→LP e atualiza objetivos. O disparo via deliberação single-flight é wiring do Plan 06.
</success_criteria>

<output>
After completion, create `.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-05-SUMMARY.md`.
DOCUMENTAR: assinaturas de shouldReflect/consolidate/applyGoalUpdates, e a decisão concreta de como a consolidação grava em events (gap conhecido: MemEvent não modela reflexão).
</output>
