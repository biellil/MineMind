---
phase: 04-persist-ncia-reflex-o-e-identidade-viva
plan: 06
type: execute
wave: 4
depends_on: ["04-02", "04-04"]
files_modified:
  - src/cognition/state.ts
  - src/memory/holder.persistence.ts
  - src/memory/holder.persistence.test.ts
  - src/bot/index.ts
autonomous: true
requirements: [MEM-02]

must_haves:
  truths:
    - "O CognitiveStateHolder ganha db + personality e é hidratado do disco no boot"
    - "needs/goals/currentGoal/disposition/personality persistem e sobrevivem a um RESTART do processo"
    - "No boot, estado estálido é mitigado: needs re-decaem por timestamp e goals velhos (committedAt) são descartados"
    - "No shutdown gracioso (SIGINT/SIGTERM) o holder é flushed e o DB fechado (WAL checkpoint)"
  artifacts:
    - path: "src/memory/holder.persistence.ts"
      provides: "persistHolder/hydrateHolder com decay-on-boot (D-04/D-19)"
      exports: ["persistHolder", "hydrateHolder"]
    - path: "src/cognition/state.ts"
      provides: "CognitiveStateHolder com campos db + personality"
      contains: "personality"
    - path: "src/bot/index.ts"
      provides: "boot: openDb + hydrate 1x; SIGINT/SIGTERM flush+close"
      contains: "SIGINT"
  key_links:
    - from: "src/bot/index.ts"
      to: "src/memory/holder.persistence.ts"
      via: "hydrateHolder no boot, persistHolder no shutdown"
      pattern: "hydrateHolder"
    - from: "src/memory/holder.persistence.ts"
      to: "kv (tabela)"
      via: "kvSet/kvGet do persistence.ts"
      pattern: "kv"
---

<objective>
Tornar a "mente" durável EM DISCO (D-04, estende o D-20 da Fase 3 de em-processo para disco). (1) `CognitiveStateHolder` ganha `db: Database` e `personality: PersonalityState`. (2) `src/memory/holder.persistence.ts` serializa/hidrata o estado vivo (needs/goals/currentGoal/disposition/personality) via a tabela `kv`, aplicando decay-on-boot (D-19): needs re-decaem por `lastSatisfiedAt`, goals com `committedAt` velho são descartados. (3) `bot/index.ts` abre o DB e hidrata o holder UMA vez no boot, e faz flush+close no SIGINT/SIGTERM.

Purpose: Completa MEM-02 no lado do estado vivo — o agente retoma onde parou após um RESTART COMPLETO do processo, não só após reconexão.
Output: holder com db+personality, holder.persistence.ts testado, boot/shutdown wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-CONTEXT.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-02-SUMMARY.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-04-SUMMARY.md

<interfaces>
<!-- Holder atual (a ESTENDER com db + personality). -->
From src/cognition/state.ts:
```typescript
export interface CognitiveStateHolder {
  control: ControlState; safety: SafetyState; memory: ShortTermMemory
  needs: Need[]; goals: Goal[]; currentGoal: Goal | null; disposition: Disposition
  playerRequestPending: boolean
  llmDecision: { decision: ActionDecision; at: number } | null
}
export function createCognitiveStateHolder(now?: number): CognitiveStateHolder
```

<!-- Persistence (Plan 02): openDb + kv helpers. -->
From src/memory/persistence.ts: `openDb(path?): Database; kvSet(db,key,value,now); kvGet(db,key): string|undefined`

<!-- Personality (Plan 04): tipo + default a guardar no holder. -->
From src/cognition/personality.ts: `interface PersonalityState; defaultPersonality(now): PersonalityState; decayPersonality(p, now)`

<!-- Decay-on-boot reusa o decaimento de needs da Fase 3. -->
From src/motivation/needs.ts: `evaluateNeeds(prev, snap, now, cfg)` — porém no boot NÃO há snapshot; usar a parte temporal (curiosity decay por lastSatisfiedAt). Ver action.
From src/motivation/types.ts: `Need { kind; value; lastSatisfiedAt }; Goal { ...; committedAt }`
From src/config.ts: config.goalStaleMs

<!-- Boot atual. -->
From src/bot/index.ts: `const holder = createCognitiveStateHolder(); function onBotReady(bot){ startCognitiveLoop(bot, holder) }; createBot(onBotReady)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender CognitiveStateHolder com db + personality</name>
  <files>src/cognition/state.ts</files>
  <read_first>
    - src/cognition/state.ts (interface e factory atuais)
    - src/cognition/personality.ts (defaultPersonality — Plan 04 SUMMARY)
    - src/memory/persistence.ts (Database type)
  </read_first>
  <action>
    Em `src/cognition/state.ts`:
    1. Importar:
       ```typescript
       import type { Database } from 'bun:sqlite'
       import type { PersonalityState } from './personality'
       import { defaultPersonality } from './personality'
       ```
    2. Adicionar ao `interface CognitiveStateHolder`:
       ```typescript
       /** Handle do DB durável (D-04). Aberto 1x no boot (bot/index.ts); null em testes que não persistem. */
       db: Database | null
       /** Estado de personalidade evolutivo (SOC-02/D-14), reinjetado no prompt sobre a baseline. */
       personality: PersonalityState
       ```
    3. Em `createCognitiveStateHolder(now = Date.now())`, adicionar aos campos retornados:
       ```typescript
       db: null,
       personality: defaultPersonality(now),
       ```
       NÃO alterar os campos existentes. `db` começa null e é setado no boot após openDb.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/cognition/state.ts contém `db: Database | null`
    - src/cognition/state.ts contém `personality: PersonalityState`
    - src/cognition/state.ts contém `personality: defaultPersonality(now)`
    - `bun run typecheck` sai 0
    - `bun test src/cognition/state.test.ts` sai 0 (o teste existente do holder continua passando)
  </acceptance_criteria>
  <done>O holder tem db (null por default) e personality (baseline por default); factory atualizada; tsc + teste do holder verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: holder.persistence.ts — persistHolder/hydrateHolder com decay-on-boot</name>
  <files>src/memory/holder.persistence.ts, src/memory/holder.persistence.test.ts</files>
  <read_first>
    - src/memory/persistence.ts (kvSet/kvGet)
    - src/cognition/state.ts (CognitiveStateHolder estendido — Task 1)
    - src/motivation/needs.ts (decaimento de curiosity por lastSatisfiedAt)
    - src/cognition/personality.ts (decayPersonality)
    - src/config.ts (config.goalStaleMs)
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md (Pattern 7 serialize/hydrate + decay-on-boot; Pitfall 4)
  </read_first>
  <behavior>
    - Test: persistHolder grava um snapshot JSON em kv['holder']; hydrateHolder de um DB sem snapshot retorna o holder base inalterado (cold start, D-03).
    - Test: round-trip — persistHolder seta needs/goals/currentGoal/disposition/personality; reabrir o DB e hydrateHolder restaura esses valores (sobrevivência a RESTART — MEM-02).
    - Test (decay-on-boot D-19): um goal com committedAt mais velho que config.goalStaleMs é DESCARTADO ao hidratar; um recente é mantido.
    - Test (decay-on-boot D-19): currentGoal que foi descartado vira null; se sobrevive, é mantido.
    - Test (decay-on-boot D-19): personality passa por decayPersonality (mean-reversion) no hydrate — após muito tempo, mood/confidence movem-se à baseline.
    - Test: persistHolder/hydrateHolder com db null NÃO lança (no-op gracioso).
  </behavior>
  <action>
    Criar `src/memory/holder.persistence.ts` (Pattern 7):

    ```typescript
    import { Database } from 'bun:sqlite'
    import type { CognitiveStateHolder } from '../cognition/state'
    import type { Goal, Need } from '../motivation/types'
    import { kvSet, kvGet } from './persistence'
    import { decayPersonality, defaultPersonality } from '../cognition/personality'
    import { config } from '../config'

    const HOLDER_KEY = 'holder'

    /** Serializa o estado vivo do holder na tabela kv (D-04). control/safety são por-sessão (não persistem). */
    export function persistHolder(db: Database | null, holder: CognitiveStateHolder, now: number): void {
      if (!db) return
      const snap = {
        needs: holder.needs, goals: holder.goals, currentGoal: holder.currentGoal,
        disposition: holder.disposition, personality: holder.personality,
      }
      kvSet(db, HOLDER_KEY, JSON.stringify(snap), now)
    }

    /** Hidrata o holder do disco aplicando decay-on-boot (D-19). Muta os campos vivos de `holder` e o retorna. */
    export function hydrateHolder(db: Database | null, holder: CognitiveStateHolder, now: number): CognitiveStateHolder {
      if (!db) return holder
      const raw = kvGet(db, HOLDER_KEY)
      if (!raw) return holder // cold start (D-03)
      const snap = JSON.parse(raw) as {
        needs: Need[]; goals: Goal[]; currentGoal: Goal | null;
        disposition: typeof holder.disposition; personality?: typeof holder.personality
      }
      // DECAY-ON-BOOT (D-19 / Pitfall 4):
      //  - needs: re-decair curiosity por tempo desde lastSatisfiedAt (clamp 0). survival/resources
      //    serão recalculados do snapshot no 1º tick (evaluateNeeds) — aqui só preservamos lastSatisfiedAt.
      const needs = snap.needs.map((n) => {
        if (n.kind === 'curiosity') {
          const value = Math.max(0, n.value - config.curiosityDecayPerMs * (now - n.lastSatisfiedAt))
          return { ...n, value }
        }
        return n
      })
      //  - goals: descartar comprometidos velhos demais (não retomar cego).
      const goals = snap.goals.filter((g) => now - g.committedAt < config.goalStaleMs)
      const currentGoal = snap.currentGoal && goals.some((g) => g.id === snap.currentGoal!.id) ? snap.currentGoal : null
      //  - personality: mean-reversion por tempo decorrido.
      const personality = decayPersonality(snap.personality ?? defaultPersonality(now), now)

      holder.needs = needs
      holder.goals = goals
      holder.currentGoal = currentGoal
      holder.disposition = snap.disposition
      holder.personality = personality
      return holder
    }
    ```
    Criar `src/memory/holder.persistence.test.ts` cobrindo os comportamentos (DB temporário via openDb + cleanup em afterAll).
  </action>
  <verify>
    <automated>bun test src/memory/holder.persistence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/memory/holder.persistence.ts contém `export function persistHolder`
    - src/memory/holder.persistence.ts contém `export function hydrateHolder`
    - src/memory/holder.persistence.ts contém `config.goalStaleMs` (descarte de goal estálido)
    - src/memory/holder.persistence.ts contém `decayPersonality`
    - src/memory/holder.persistence.ts trata `db null` sem lançar (early return)
    - `bun test src/memory/holder.persistence.test.ts` sai 0
  </acceptance_criteria>
  <done>O estado vivo persiste e hidrata por round-trip; decay-on-boot re-decai needs, descarta goals velhos e reverte a personalidade; db null é no-op.</done>
</task>

<task type="auto">
  <name>Task 3: Boot wiring em bot/index.ts — openDb + hydrate 1x + flush no shutdown</name>
  <files>src/bot/index.ts</files>
  <read_first>
    - src/bot/index.ts (boot atual: createCognitiveStateHolder + onBotReady + createBot)
    - src/memory/persistence.ts (openDb)
    - src/memory/holder.persistence.ts (hydrateHolder/persistHolder — Task 2)
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md (Pattern 7 boot; Pitfall 5 WAL/Windows)
  </read_first>
  <action>
    Em `src/bot/index.ts`, ANTES de `createBot(onBotReady)`:
    1. Importar:
       ```typescript
       import { openDb } from '../memory/persistence'
       import { hydrateHolder, persistHolder } from '../memory/holder.persistence'
       ```
    2. Após `const holder = createCognitiveStateHolder()`, abrir o DB e hidratar UMA vez:
       ```typescript
       // Fase 4 (D-04): abrir o DB durável e hidratar a mente do disco 1x no boot.
       const db = openDb()
       holder.db = db
       hydrateHolder(db, holder, Date.now())
       console.log('[MineMind] Mente hidratada do disco (Fase 4).')
       ```
    3. Registrar flush no shutdown gracioso (D-02 / Pitfall 5 — db.close() faz WAL checkpoint):
       ```typescript
       function shutdown(signal: string): void {
         console.log(`[MineMind] ${signal} — persistindo a mente e fechando o DB...`)
         try { persistHolder(db, holder, Date.now()) } catch (e) { console.error('[shutdown] persistHolder falhou:', e) }
         try { db.close() } catch (e) { console.error('[shutdown] db.close falhou:', e) }
         process.exit(0)
       }
       process.on('SIGINT', () => shutdown('SIGINT'))
       process.on('SIGTERM', () => shutdown('SIGTERM'))
       ```
    NÃO mudar a assinatura de onBotReady nem de startCognitiveLoop neste plano (o gatilho de reflexão e o flush por ciclo são wiring do Plan 07). Apenas garantir que `holder.db` está setado antes do loop.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/bot/index.ts contém `openDb`
    - src/bot/index.ts contém `hydrateHolder(db, holder`
    - src/bot/index.ts contém `holder.db = db`
    - src/bot/index.ts contém `process.on('SIGINT'`
    - src/bot/index.ts contém `persistHolder(db, holder`
    - src/bot/index.ts contém `db.close()`
    - `bun run typecheck` sai 0
  </acceptance_criteria>
  <done>O boot abre o DB, hidrata o holder uma vez e seta holder.db; SIGINT/SIGTERM persistem e fecham o DB (WAL checkpoint).</done>
</task>

</tasks>

<verification>
- `bun test src/memory/holder.persistence.test.ts src/cognition/state.test.ts` passa.
- `bun run typecheck` limpo.
- Inspeção: após `bun start` e Ctrl+C, o arquivo `minemind.sqlite` existe e kv['holder'] tem um snapshot (verificável em runtime no Plan 07).
</verification>

<success_criteria>
MEM-02 completo no lado do estado vivo: a mente inteira (needs/goals/currentGoal/disposition/personality + memória LP no DB) sobrevive a um RESTART completo do processo, com mitigação de estado estálido (decay-on-boot). O holder agora carrega o handle do DB para os consumidores do Plan 07.
</success_criteria>

<output>
After completion, create `.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-06-SUMMARY.md`.
DOCUMENTAR: a forma exata do snapshot persistido em kv['holder'] e as assinaturas de persistHolder/hydrateHolder (o Plan 07 chama persistHolder ao fim de cada reflexão).
</output>
