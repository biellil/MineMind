---
phase: 04-persist-ncia-reflex-o-e-identidade-viva
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/memory/vec.smoke.test.ts
autonomous: true
requirements: [MEM-03]
user_setup:
  - service: lm-studio
    why: "Servidor de embeddings (/v1/embeddings) e a validação de dimensão do índice vetorial dependem de um modelo de embedding carregado. NÃO bloqueia este plano (o smoke usa vetores sintéticos), mas o plano 02+ precisa."
    env_vars:
      - name: EMBEDDING_MODEL
        source: "LM Studio -> Developer -> modelo de embedding carregado (default sugerido: text-embedding-nomic-embed-text-v1.5)"

must_haves:
  truths:
    - "sqlite-vec carrega no bun:sqlite no Windows sem setCustomSQLite"
    - "Um embedding Float32Array faz round-trip (INSERT + KNN MATCH) e retorna o mesmo rowid"
  artifacts:
    - path: "src/memory/vec.smoke.test.ts"
      provides: "Prova de que vec0 + Float32Array bind funcionam em bun:sqlite/Windows ANTES de construir o pipeline"
      contains: "vec_version"
    - path: "package.json"
      provides: "Dependência sqlite-vec instalada"
      contains: "sqlite-vec"
  key_links:
    - from: "src/memory/vec.smoke.test.ts"
      to: "sqlite-vec"
      via: "sqliteVec.load(db)"
      pattern: "sqliteVec\\.load"
---

<objective>
De-riscar a decisão D-01 ANTES de construir qualquer pipeline de persistência: provar que `sqlite-vec@0.1.9` carrega no `bun:sqlite` no Windows e que um embedding `Float32Array` faz round-trip por uma tabela virtual `vec0` (INSERT + KNN `MATCH`). A pesquisa marcou isto como o item de maior incerteza (Open Questions 1 e 2): o bind exato de `Float32Array` em `bun:sqlite` (vs `Buffer.from(f32.buffer)`) e a disponibilidade do binário nativo Windows.

Purpose: Se este smoke falhar, todo o resto da fase precisa cair para o fallback `vectra` (D-01) — descobrir isso AGORA, não depois de escrever 6 módulos.
Output: `sqlite-vec` instalado + um teste de smoke que prova load + round-trip vetorial.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-CONTEXT.md
@.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md

<interfaces>
<!-- Padrão de teste existente (bun:test) — replicar a forma. -->
From src/memory/shortTerm.test.ts:
```typescript
import { test, expect } from 'bun:test'
```

<!-- Padrão de abertura do DB (RESEARCH Pattern 1). vec0 espera Float32Array (Pitfall 3). -->
sqlite-vec carrega via: `import * as sqliteVec from 'sqlite-vec'; sqliteVec.load(db)` — Windows NÃO usa setCustomSQLite (macOS-only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar sqlite-vec e provar load + round-trip vetorial (Wave 0 de-risk)</name>
  <files>package.json, src/memory/vec.smoke.test.ts</files>
  <read_first>
    - package.json (ver dependências atuais; confirmar Bun + zod + mineflayer já presentes)
    - src/memory/shortTerm.test.ts (replicar o padrão `import { test, expect } from 'bun:test'`)
    - .planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-RESEARCH.md (Pattern 1, Pitfall 2, Pitfall 3, Open Questions 1 e 2)
  </read_first>
  <action>
    1. Instalar a dependência:
       ```
       bun add sqlite-vec
       ```
       Verificar que `package.json` ganhou `"sqlite-vec"` em dependencies e que a optionalDependency `sqlite-vec-windows-x64@0.1.9` resolveu (binário Windows).

    2. Criar `src/memory/vec.smoke.test.ts` com EXATAMENTE este padrão de smoke (de-risk de D-01 / Open Questions 1+2). Usa um DB em arquivo temporário (vec0 NÃO funciona em `:memory:` de forma confiável em todos os builds — usar arquivo e remover no fim):

       ```typescript
       // src/memory/vec.smoke.test.ts
       // Wave 0 de-risk (D-01 / RESEARCH Open Questions 1+2): prova que sqlite-vec carrega
       // no bun:sqlite (Windows) e que um Float32Array faz round-trip por vec0.
       import { test, expect, afterAll } from 'bun:test'
       import { Database } from 'bun:sqlite'
       import * as sqliteVec from 'sqlite-vec'
       import { unlinkSync, existsSync } from 'node:fs'

       const DB_PATH = './minemind.vecsmoke.test.sqlite'
       afterAll(() => {
         for (const f of [DB_PATH, `${DB_PATH}-wal`, `${DB_PATH}-shm`]) {
           if (existsSync(f)) unlinkSync(f)
         }
       })

       test('sqlite-vec carrega no bun:sqlite (Windows) e expõe vec_version', () => {
         const db = new Database(DB_PATH)
         sqliteVec.load(db)
         const row = db.prepare('select vec_version() as v').get() as { v: string }
         expect(typeof row.v).toBe('string')
         expect(row.v.length).toBeGreaterThan(0)
         db.close()
       })

       test('round-trip: INSERT Float32Array + KNN MATCH retorna o mesmo rowid', () => {
         const db = new Database(DB_PATH)
         sqliteVec.load(db)
         db.run('PRAGMA journal_mode = WAL')
         db.run(`CREATE VIRTUAL TABLE IF NOT EXISTS vec_smoke USING vec0(embedding float[4])`)

         const v1 = new Float32Array([1, 0, 0, 0])
         const v2 = new Float32Array([0, 1, 0, 0])
         db.prepare('INSERT INTO vec_smoke (rowid, embedding) VALUES (?, ?)').run(1, v1)
         db.prepare('INSERT INTO vec_smoke (rowid, embedding) VALUES (?, ?)').run(2, v2)

         const query = new Float32Array([0.9, 0.1, 0, 0]) // mais próximo de v1
         const hits = db
           .prepare('SELECT rowid, distance FROM vec_smoke WHERE embedding MATCH ? AND k = 2 ORDER BY distance')
           .all(query) as { rowid: number; distance: number }[]

         expect(hits.length).toBe(2)
         expect(hits[0].rowid).toBe(1) // o vetor mais próximo do query vem primeiro
         db.close()
       })
       ```

    3. FALLBACK (Pitfall 3): se o bind `Float32Array` direto lançar erro de tipo no `.run()`, trocar AMBOS os binds (INSERT e MATCH) para `Buffer.from(v.buffer)` e adicionar um comentário no topo do arquivo: `// NOTA: Float32Array direto falhou em bun:sqlite — usando Buffer.from(f32.buffer). Propagar esse padrão para longTerm.ts (Plan 03).` Documentar qual variante funcionou no SUMMARY (informa o Plan 03).

    4. FALLBACK MAIOR (D-01): se `sqliteVec.load(db)` lançar (binário Windows ausente/incompatível), NÃO tentar consertar — registrar no SUMMARY o erro exato e a recomendação de cair para `vectra@0.15.0` (D-01 fallback), e marcar este plano como BLOCKED para escalada ao usuário. Não prosseguir cego.
  </action>
  <verify>
    <automated>bun test src/memory/vec.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - package.json contém a string `sqlite-vec` em dependencies
    - src/memory/vec.smoke.test.ts contém `sqliteVec.load`
    - src/memory/vec.smoke.test.ts contém `vec_version`
    - src/memory/vec.smoke.test.ts contém `WHERE embedding MATCH`
    - `bun test src/memory/vec.smoke.test.ts` sai 0 (ambos os testes passam) OU o SUMMARY documenta o fallback acionado (Buffer.from ou vectra)
  </acceptance_criteria>
  <done>sqlite-vec instalado; o smoke prova load + round-trip vetorial; a técnica de bind correta (Float32Array vs Buffer.from) está documentada no SUMMARY para o Plan 03 herdar.</done>
</task>

</tasks>

<verification>
- `bun test src/memory/vec.smoke.test.ts` passa (load + round-trip).
- `bun run typecheck` (tsc --noEmit) continua limpo.
</verification>

<success_criteria>
A decisão D-01 está de-riscada: sabemos com certeza que `sqlite-vec` carrega no Bun/Windows e qual técnica de bind de `Float32Array` usar, ANTES de qualquer outro plano da fase escrever no DB.
</success_criteria>

<output>
After completion, create `.planning/phases/04-persist-ncia-reflex-o-e-identidade-viva/04-01-SUMMARY.md`.
DOCUMENTAR explicitamente: (a) versão de sqlite-vec instalada, (b) técnica de bind que funcionou (Float32Array direto OU Buffer.from(f32.buffer)), (c) se algum fallback foi acionado.
</output>
