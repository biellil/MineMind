---
phase: "07.1"
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cognition/arbiter.ts
  - src/cognition/deliberation.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "nextMechanicalStep() existe em arbiter.ts e retorna MechanicalStep | null"
    - "null é o sinal explícito 'escalar ao LLM' — distinto do fallback atual 'exploring'"
    - "Outcome 'no_effect' ou 'partial' com shouldAbandon ativo retorna null (força LLM)"
    - "O roteador por outcome integra-se ao deliberator existente via enfileiramento void, nunca aguarda o LLM"
    - "arbitrate() original é preservada intacta para compatibilidade com analyze"
  artifacts:
    - path: "src/cognition/arbiter.ts"
      provides: "nextMechanicalStep() classificador com caminho null"
      exports: ["nextMechanicalStep", "MechanicalStep"]
      contains: "MechanicalStep | null"
    - path: "src/cognition/deliberation.ts"
      provides: "integração do roteador por outcome ao pickTrigger/deliberator"
      contains: "lastObservedDelta"
  key_links:
    - from: "src/cognition/arbiter.ts nextMechanicalStep"
      to: "src/cognition/safety.ts shouldAbandon"
      via: "importação e chamada direta"
      pattern: "shouldAbandon"
    - from: "src/cognition/loop.ts pickTrigger"
      to: "holder.lastObservedDelta.outcome"
      via: "leitura do campo outcome do SkillResult grounded (Fase 7)"
      pattern: "lastObservedDelta"
---

<objective>
Este plano promove o arbiter a classificador com caminho null e integra o roteador por outcome (D-06/D-07/D-08). Pode rodar em paralelo com o Plan 01 porque não toca os mesmos arquivos.

Purpose: O arbiter atual nunca retorna null — sempre produz um CognitiveState (fallback 'exploring'). Para D-06, precisamos de um caminho explícito "não há passo mecânico óbvio → delegar ao LLM". Para D-07, o outcome do SkillResult (grounding da Fase 7) deve guiar quando re-deliberar, substituindo o disparo 'periodic' cego como motor de escalação.

Output: src/cognition/arbiter.ts com nextMechanicalStep exportada; src/cognition/deliberation.ts (ou loop.ts pickTrigger) com outcome como gatilho de re-deliberação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07.1-loop-agentico-percepcao-dirigida-por-consequencia/07.1-CONTEXT.md
@.planning/phases/07.1-loop-agentico-percepcao-dirigida-por-consequencia/07.1-RESEARCH.md
</context>

<interfaces>
<!-- Interfaces existentes que o executor precisa conhecer antes de implementar -->

De src/cognition/arbiter.ts (estado atual):
```typescript
export function highestPriorityGatherTarget(
  s: WorldSnapshot,
  excludeTargets: ReadonlySet<string> = new Set(),
): string | null

export function hasNearbyPlayer(s: WorldSnapshot): boolean

export function arbitrate(
  s: WorldSnapshot,
  mode: ControlMode,
  excludeTargets: ReadonlySet<string> = new Set(),
): CognitiveState  // NUNCA retorna null — fallback 'exploring'
```

De src/cognition/safety.ts:
```typescript
export function shouldAbandon(state: SafetyState): boolean
export function cooledDownTargets(state: SafetyState, now: number): ReadonlySet<string>
```

De src/grounding/types.ts:
```typescript
export type SkillOutcome = 'success' | 'partial' | 'no_effect' | 'error'

export interface SkillResult {
  readonly outcome: SkillOutcome
  readonly observed: number
  readonly expected: number
  readonly delta: InventoryDelta
  readonly reason?: string
}
```

De src/cognition/state.ts (CognitiveStateHolder):
```typescript
holder.lastObservedDelta: {
  skill: string
  target: string
  outcome: SkillOutcome
  observed: number
  expected: number
  delta: Record<string, number>
  at: number
} | undefined
```

De src/cognition/loop.ts (função pickTrigger existente):
```typescript
function pickTrigger(holder: CognitiveStateHolder): DeliberationTrigger {
  const mcfg = motivationConfigFor(holder.disposition)
  const t = Date.now()
  const crossed = holder.needs.some((n) => urgency(n, t, mcfg) >= mcfg.goalThreshold)
  return crossed ? 'need_threshold' : 'periodic'
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar nextMechanicalStep ao arbiter (classificador com caminho null)</name>
  <read_first>
    - src/cognition/arbiter.ts (estado completo atual)
    - src/cognition/safety.ts (shouldAbandon, cooledDownTargets — assinaturas)
    - src/grounding/types.ts (SkillOutcome — tipo do outcome)
    - src/cognition/state.ts (CognitiveStateHolder — campo lastObservedDelta)
  </read_first>
  <action>
Adicionar em src/cognition/arbiter.ts, APÓS as funções existentes (não alterar nenhuma função existente):

**Interface MechanicalStep** (exportar):
```typescript
export interface MechanicalStep {
  /** Nome da skill a executar (e.g., 'dig', 'navigate') */
  readonly skill: string
  /** Target serializado (string para dig, JSON para navigate) */
  readonly target: string
}
```

**Função nextMechanicalStep** (exportar):
```typescript
export function nextMechanicalStep(
  snapshot: WorldSnapshot,
  mode: ControlMode,
  safety: import('./safety').SafetyState,
  outcome: import('../grounding/types').SkillOutcome | null,
): MechanicalStep | null
```

Lógica EXATA a implementar (ordem importa):

1. **Gate de outcome (D-07):** Se `outcome === 'no_effect' || outcome === 'partial'`:
   - Chamar `shouldAbandon(safety)`. Se true → retornar `null` (forçar LLM).
   - Se false → continuar (pode ainda ter passo mecânico se há alvo disponível).

2. **Gate de modo:** Se `mode === 'paused'` → retornar `null`.

3. **Gate de socializing:** Se `hasNearbyPlayer(snapshot)` → retornar `null` (socializing é decisão real — o LLM decide como interagir; D-06 não inclui socializing no caminho mecânico).

4. **Gate de standby:** Se `mode === 'standby'` → retornar `null`.

5. **Passo mecânico — gathering:** Chamar `cooledDownTargets(safety, Date.now())` para obter `excluded`. Chamar `highestPriorityGatherTarget(snapshot, excluded)`. Se retornar não-null → retornar `{ skill: 'dig', target: gatherTarget }`.

6. **Exploring mecânico:** Retornar `null` — exploring NÃO é mecânico (D-06). Vagar aleatório sempre vai ao LLM para decidir posição. Sem exceção.

**Importações novas necessárias em arbiter.ts:**
```typescript
import { shouldAbandon, cooledDownTargets } from './safety'
import type { SafetyState } from './safety'
import type { SkillOutcome } from '../grounding/types'
```

**CRÍTICO:** NÃO alterar `arbitrate()`, `hasNearbyPlayer()`, nem `highestPriorityGatherTarget()`. Essas funções são usadas pelo nó analyze (nodes.ts) e devem permanecer intactas.
  </action>
  <verify>
    <automated>cd /root/MineMind && bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/cognition/arbiter.ts exporta `MechanicalStep` (interface) e `nextMechanicalStep` (função)
    - src/cognition/arbiter.ts contém `MechanicalStep | null` como tipo de retorno
    - src/cognition/arbiter.ts contém `shouldAbandon(safety)` dentro de nextMechanicalStep
    - src/cognition/arbiter.ts NÃO modificou a função `arbitrate` existente (grep deve mostrar que ainda existe com assinatura original)
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>nextMechanicalStep exportada em arbiter.ts: retorna MechanicalStep para passos determinísticos e null para escalar ao LLM</done>
</task>

<task type="auto">
  <name>Task 2: Integrar roteador por outcome ao pickTrigger em loop.ts</name>
  <read_first>
    - src/cognition/loop.ts (função pickTrigger linhas 224-230 — modificar)
    - src/cognition/deliberation.ts (DeliberationTrigger type, assinatura maybeDeliberate)
    - src/cognition/state.ts (CognitiveStateHolder — campo lastObservedDelta)
    - src/grounding/types.ts (SkillOutcome)
  </read_first>
  <action>
Modificar a função `pickTrigger` em src/cognition/loop.ts para incorporar o critério de outcome (D-07/D-08):

```typescript
function pickTrigger(holder: CognitiveStateHolder): DeliberationTrigger {
  const mcfg = motivationConfigFor(holder.disposition)
  const t = Date.now()

  // D-07: outcome do SkillResult (Fase 7) como gatilho primário de re-deliberação.
  // no_effect/partial após um passo mecânico indica "tentativa sem progresso real" → LLM decide.
  const lastOutcome = holder.lastObservedDelta?.outcome
  if (lastOutcome === 'no_effect' || lastOutcome === 'partial') {
    return 'need_threshold' // mais urgente que 'periodic' — força re-deliberação mais cedo
  }

  // D-08: necessidade cruzou limiar → disparo por need_threshold.
  const crossed = holder.needs.some((n) => urgency(n, t, mcfg) >= mcfg.goalThreshold)
  return crossed ? 'need_threshold' : 'periodic'
}
```

**Importante (D-08):** `pickTrigger` apenas classifica o gatilho. Quem chama `maybeDeliberate` é o loop e ela usa `inFlight` para single-flight. O roteador NUNCA aguarda o LLM — apenas enfileira via `void deliberator.maybeDeliberate(...)` (pattern já existente no loop).

**NÃO** modificar a assinatura de maybeDeliberate. **NÃO** alterar as chamadas a maybeDeliberate. Apenas a lógica interna de pickTrigger muda.

Esta mudança integra o critério de consequência (D-07) sem duplicar shouldAbandon/recordFailure — esses continuam no nó execute (nodes.ts) como hoje.
  </action>
  <verify>
    <automated>cd /root/MineMind && bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/cognition/loop.ts função pickTrigger contém `holder.lastObservedDelta?.outcome`
    - src/cognition/loop.ts função pickTrigger contém `'no_effect'` e `'partial'` como condições de retorno `'need_threshold'`
    - src/cognition/loop.ts NÃO chama `await deliberator.maybeDeliberate` (deve ser `void deliberator.maybeDeliberate` — D-08)
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>pickTrigger usa outcome do SkillResult grounded como gatilho primário de re-deliberação, substituindo o disparo 'periodic' cego</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| holder.lastObservedDelta → pickTrigger | Outcome do SkillResult (Fase 7) determina se o LLM é chamado — field pode ser undefined (primeiro tick) |
| nextMechanicalStep → execute node | Retorno null/MechanicalStep determina se o LLM é invocado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-05 | Tampering | nextMechanicalStep / outcome gate | mitigate | outcome provém de SkillResult grounded (Fase 7) — deriva de delta real de inventário/posição, não da Promise; adulteração exigiria comprometer o grounding |
| T-07.1-06 | Denial of Service | pickTrigger outcome loop | accept | no_effect/partial sempre escalona ao LLM; o deliberator tem inFlight para evitar chamadas sobrepostas; rate-limit existe via replanMinIntervalMs |
| T-07.1-07 | Elevation of Privilege | nextMechanicalStep null bypass | accept | null sempre resulta em deferimento ao LLM (mais seguro, não menos); o LLM tem prompt system com persona e regras |
</threat_model>

<verification>
Após as duas tasks:
1. `bun run build` exits 0
2. `grep -n "MechanicalStep | null" src/cognition/arbiter.ts` retorna match
3. `grep -n "nextMechanicalStep" src/cognition/arbiter.ts` retorna pelo menos 2 matches (definição + export)
4. `grep -n "arbitrate" src/cognition/arbiter.ts` ainda retorna a função original intacta
5. `grep -n "lastObservedDelta" src/cognition/loop.ts` retorna match em pickTrigger
</verification>

<success_criteria>
- nextMechanicalStep() em arbiter.ts retorna null para escalar ao LLM e MechanicalStep para passos determinísticos
- O caminho mecânico cobre apenas gathering (dig); exploring vai sempre ao LLM
- pickTrigger usa o outcome do SkillResult da Fase 7 como gatilho de re-deliberação
- Todas as funções existentes (arbitrate, highestPriorityGatherTarget, hasNearbyPlayer) preservadas intactas
- bun run build exits 0
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/07.1-loop-agentico-percepcao-dirigida-por-consequencia/07.1-02-SUMMARY.md` com as mudanças realizadas, assinatura exata de nextMechanicalStep, e como o roteador por outcome se integra ao loop existente.
</output>
