# Phase 07.1: Loop Agêntico — Percepção Dirigida por Consequência - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-19
**Phase:** 07.1-loop-agentico-percepcao-dirigida-por-consequencia
**Areas discussed:** Aresta de retorno, Roteador código-vs-LLM, Park/wake, Gatilhos v1 + preempção
**Mode:** advisor (calibration tier: full_maturity — vendor philosophy conservative)

---

## Área 1 — Onde vive a "aresta de retorno"

| Option | Description | Selected |
|--------|-------------|----------|
| A — Driver externo event-driven (aresta FORA do grafo; `await actionFinished` + race) | Preserva decisões travadas, single-flight, MemorySaver; migração concentrada em loop.ts | ✓ (final) |
| B — Ciclo no grafo + `interrupt()` | Topologia ReAct explícita; mas re-executa nó (re-dispara skills), exige checkpointer durável, colide com decisões travadas | ✗ (escolha inicial, revertida) |
| C — Híbrido (rajada interna de micro-passos) | Reduz round-trips; gargalo inexistente; dois pontos de verdade | |

**User's choice:** Inicialmente B; revertido para **A** após sinalização de conflito.
**Notes:** A escolha inicial B conflitava com a Área 3=C (loop no grafo vs park no driver) e com a decisão travada do Roadmap v2.0 + grounding da Fase 7 (interrupt re-executa nó). Conflito sinalizado; usuário escolheu o par coerente A+C.

---

## Área 2 — Roteador código-vs-LLM

| Option | Description | Selected |
|--------|-------------|----------|
| A + C (arbiter null + outcome) | Arbiter classificador devolve passo mecânico ou null (→LLM); outcome do SkillResult gatilha re-deliberação | ✓ |
| Apenas A (arbiter null) | Só arbiter classifica; sem outcome como gatilho | |
| B — Tabela de continuações explícitas | Mais didático; exige dados de percepção (drops/veias) inexistentes hoje | |
| D — Só ajustar gatilhos atuais | Menor mudança; roteador fica implícito | |

**User's choice:** **A + C**
**Notes:** Fecha o loop com o grounding da Fase 7; B diferida até a percepção expor drops/veias.

---

## Área 3 — Mecanismo park/wake

| Option | Description | Selected |
|--------|-------------|----------|
| C — Park no driver + sinal do grafo (`Promise.race([evento, timer])`; grafo emite enteredIdle/nextWakeMs) | Cancela no bot.end sem deadlock; corrige enteredIdle hardcoded; não toca checkpointer | ✓ |
| B — Park puro no driver (sem sinal do grafo) | Mais enxuto; auto-timer vira decisão do driver | |
| A — `interrupt()`+`Command` no grafo | Exige stream + checkpointer durável; risco de checkpoint órfão | |

**User's choice:** **C**
**Notes:** Coerente com Área 1=A (aresta fora do grafo). De quebra corrige o gatilho de reflexão.

---

## Área 4 — Gatilhos v1 + preempção / fronteira Phase 8

| Option | Description | Selected |
|--------|-------------|----------|
| Mínimo + fronteira | actionFinished+nightFell+hostileNearby+stuck+hungry; survival emitido sem handler; 07.1=mecanismo, Phase 8=política | ✓ (final) |
| Mínimo SEM survival | Só actionFinished+stuck+nightFell; toda infra de survival na Phase 8 | |
| Conjunto ampliado (com handlers) | inventoryChanged/playerCollect/healthCritical/breath + handlers agora | ✗ (escolha inicial, revertida) |

**User's choice:** Inicialmente "Conjunto ampliado COM handlers"; revertido para **Mínimo + fronteira**.
**Notes:** Ampliado com handlers implementava política de reflexo reservada à Phase 8. Conflito sinalizado; usuário escolheu manter a fronteira limpa. Preempção: AbortController (contrato) + pathfinder.stop() (skills) + orquestração no execute.

---

## Conflitos sinalizados e resolução

1. **Área 1=B × Área 3=C** (hard): loop dentro do grafo vs park no driver; choque no checkpointer (durável vs MemorySaver podado). → resolvido para **A + C**.
2. **Área 1=B × decisões travadas**: reabria o Roadmap v2.0 (System 1 fora do grafo) e colidia com o grounding da Fase 7 (interrupt re-executa nó). → resolvido mantendo a decisão prévia (Opção A).
3. **Área 4 ampliado-com-handlers × fronteira Phase 8**: invadia a política de reflexo. → resolvido para **Mínimo + fronteira**.

## Claude's Discretion

- Nomes/estrutura do TriggerBus e edge-detectors; valores de limiar/histerese/debounce; forma do contrato grafo↔driver (enteredIdle/nextWakeMs).

## Deferred Ideas

- Opção B (ciclo no grafo + interrupt) — rejeitada; reabrir só com requisito de checkpointing durável.
- Tabela de continuações explícitas — até a percepção expor drops/veias.
- Gatilhos sem consumidor no v1 (inventoryChanged/playerCollect/itemDrop/entityGone/rain).
- Reflexos de sobrevivência + re-teste AO VIVO do [reflect] — Phase 8.
