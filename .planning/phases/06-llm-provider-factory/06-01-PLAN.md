---
phase: 06-llm-provider-factory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/llm/provider.ts
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-04]
must_haves:
  truths:
    - "Definir LLM_PROVIDER=openai faz o provider de chat ser GPT-4.1-mini (cloud); =local (default) usa LM Studio — sem tocar o loop"
    - "Com LLM_PROVIDER=openai, embed() continua chamando o LM Studio local (/v1/embeddings) — embeddings nunca vão para a cloud"
    - "provider.decide sobrevive ao caveat zod v4: se withStructuredOutput lança 'type:None', cai para JSON Schema cru via z.toJSONSchema e ainda retorna objeto válido"
    - "reasoning.effort só é enviado se OPENAI_MODEL for família gpt-5.x/o-series; nunca para gpt-4.1-mini"
  artifacts:
    - path: "src/llm/provider.ts"
      provides: "createOpenAiProvider(), createLocalEmbedder(), createProvider() selector, fallback D-16 em decide()"
      contains: "export function createProvider"
    - path: "src/config.ts"
      provides: "envs do provider/cloud + validação de range no boot"
      contains: "llmProvider"
  key_links:
    - from: "src/llm/provider.ts createOpenAiProvider"
      to: "createLocalEmbedder().embed"
      via: "delegação explícita de embed() (D-11)"
      pattern: "embed.*createLocalEmbedder|embedder\\.embed"
    - from: "src/llm/provider.ts createProvider"
      to: "process.env.LLM_PROVIDER / config.llmProvider"
      via: "seleção local vs openai por env (D-05/D-13)"
      pattern: "llmProvider === 'openai'|LLM_PROVIDER"
---

<objective>
Estender a abstração `LlmProvider` existente com o caminho cloud (GPT-4.1-mini), embeddings sempre locais por composição explícita, e uma factory de seleção por env — tudo SEM tocar `decideAction` nem o loop cognitivo. Adicionar o fallback de structured-output (D-16) que blinda `decide()` contra o caveat zod v4.

Purpose: Esta é a fundação da Phase 6 — os Planos 02 (teto de custo) e 03 (testes de paridade) dependem das funções criadas aqui.
Output: `createOpenAiProvider()`, `createLocalEmbedder()`, `createProvider()` e o fallback D-16 em `src/llm/provider.ts`; envs novos validados em `src/config.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-llm-provider-factory/06-CONTEXT.md

<interfaces>
<!-- Contratos existentes que o executor DEVE respeitar. Extraídos do código. NÃO reexplorar o codebase. -->

De src/llm/provider.ts (interface a NÃO mudar a forma):
```typescript
export interface LlmProvider {
  decide<T>(schema: ZodType<T>, messages: BaseMessage[]): Promise<T>
  chat(messages: BaseMessage[]): Promise<string>
  available(): Promise<boolean>   // probe leve via fetch /models; nunca lança
  embed(text: string): Promise<number[]>  // fetch direto a /v1/embeddings; NÃO usar OpenAIEmbeddings
}
export function createLmStudioProvider(): LlmProvider  // já existe — manter
```

Construção atual de decide() em createLmStudioProvider (ponto de injeção do fallback D-16, ~L62):
```typescript
const structured = model.withStructuredOutput(
  schema as ZodType<Record<string, unknown>>,
  { name: 'decide', method: 'jsonSchema' },
)
return (await structured.invoke(messages)) as T
```

ChatOpenAI já é a classe usada para AMBOS os caminhos (só muda baseURL/apiKey/model):
```typescript
// local: apiKey 'lm-studio', configuration:{ baseURL }, model 'local-model'
// cloud: apiKey OPENAI_API_KEY, SEM baseURL (endpoint padrão OpenAI), model 'gpt-4.1-mini'
```

zod v4 expõe nativamente `z.toJSONSchema(schema)` (preferir ao pacote zod-to-json-schema de terceiros — D-16).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar envs do provider/cloud em config.ts com validação de range</name>
  <files>src/config.ts</files>
  <read_first>
    - src/config.ts (ler a seção "Fase 3: LLM" ~L61-107 e o bloco de validações ~L165-209 para replicar EXATAMENTE o padrão: `process.env.X || default` para strings, `parseInt(...,10)`/`parseFloat` para números, `if (cond) throw new Error(...)` no boot)
    - .planning/phases/06-llm-provider-factory/06-CONTEXT.md (D-05, D-10 — lista de envs; "Claude's Discretion" — nomes/defaults/ranges ficam ao critério, mas use os nomes abaixo)
  </read_first>
  <action>
    Adicionar ao objeto `config` em src/config.ts, numa nova seção comentada `// === Fase 6: Provider LLM configurável (cloud/local) ===`, as seguintes chaves (use `||` para strings por causa do bug de string vazia já documentado no topo do arquivo):

    - `llmProvider: (process.env.LLM_PROVIDER || 'local') as 'local' | 'openai'`  // D-05: default local custo-zero
    - `openaiApiKey: process.env.OPENAI_API_KEY || ''`  // D-10
    - `openaiModel: process.env.OPENAI_MODEL || 'gpt-4.1-mini'`  // D-01
    - `openaiMaxTokens: parseInt(process.env.OPENAI_MAX_TOKENS || '512', 10)`  // D-02: max_tokens baixo
    - `openaiReasoningEffort: (process.env.OPENAI_REASONING_EFFORT || 'low') as 'minimal' | 'low' | 'medium' | 'high'`  // D-04: só aplicado se modelo gpt-5.x/o-series

    Adicionar ao bloco de validação de startup (no final do arquivo, mesmo estilo dos `if (...) throw new Error(...)` existentes), uma nova seção `// Fase 6: validação do provider`:
    - `if (config.llmProvider !== 'local' && config.llmProvider !== 'openai') throw new Error(\`LLM_PROVIDER inválido: ${config.llmProvider}. Deve ser local ou openai.\`)`
    - `if (config.llmProvider === 'openai' && !config.openaiApiKey) throw new Error('LLM_PROVIDER=openai exige OPENAI_API_KEY definido.')`
    - `if (config.openaiMaxTokens < 1) throw new Error(\`OPENAI_MAX_TOKENS inválido: ${config.openaiMaxTokens}. Deve ser >= 1.\`)`

    NÃO remover nem renomear nenhuma chave existente. NÃO tocar `motivationConfigFor`/`needWeightsFor`.
  </action>
  <verify>
    <automated>cd "C:\Users\biel1\OneDrive\Documentos\GitHub\MineMind" && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "llmProvider:" src/config.ts` retorna a linha com `process.env.LLM_PROVIDER || 'local'`
    - `grep -n "openaiModel:" src/config.ts` retorna a linha com default `'gpt-4.1-mini'`
    - `grep -n "openaiReasoningEffort:" src/config.ts` existe
    - `grep -c "LLM_PROVIDER inválido\|OPENAI_API_KEY exige\|exige OPENAI_API_KEY\|OPENAI_MAX_TOKENS inválido" src/config.ts` >= 3
    - `bun run typecheck` sai com código 0
  </acceptance_criteria>
  <done>config.ts expõe llmProvider/openaiApiKey/openaiModel/openaiMaxTokens/openaiReasoningEffort tipados, com validação de range/consistência no boot; typecheck passa.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: createOpenAiProvider + createLocalEmbedder + fallback D-16 em provider.ts</name>
  <files>src/llm/provider.ts</files>
  <read_first>
    - src/llm/provider.ts (TODO o arquivo — replicar o padrão de createLmStudioProvider: ChatOpenAI, decide via withStructuredOutput method:'jsonSchema', chat, available via fetch /models, embed via fetch /v1/embeddings ~L92-106)
    - src/config.ts (usar config.openaiApiKey/openaiModel/openaiMaxTokens/openaiReasoningEffort/llmBaseUrl/embeddingModel adicionados na Task 1)
    - .planning/phases/06-llm-provider-factory/06-CONTEXT.md (D-04, D-11, D-16)
  </read_first>
  <action>
    Em src/llm/provider.ts:

    1) Refatorar o caminho `embed` para uma função exportada reutilizável `createLocalEmbedder(baseURL?: string): Pick<LlmProvider, 'embed'>` (D-11). Mover a lógica atual de `embed()` (fetch a `${baseURL}/embeddings`, body `{ model: config.embeddingModel || 'text-embedding-nomic-embed-text-v1.5', input: text.replace(/\n/g,' ') }`, throw em `!res.ok`, retorna `json.data[0].embedding`) para dentro de `createLocalEmbedder`, usando `resolveBaseUrl()` quando baseURL não vier. `createLmStudioProvider` passa a DELEGAR `embed` a `createLocalEmbedder()` (sem duplicar o fetch).

    2) Extrair o helper `decideWithFallback<T>(model, schema, messages): Promise<T>` que encapsula o D-16:
       ```typescript
       try {
         const structured = model.withStructuredOutput(
           schema as ZodType<Record<string, unknown>>,
           { name: 'decide', method: 'jsonSchema' },
         )
         return (await structured.invoke(messages)) as T
       } catch (e) {
         // D-16: caveat zod v4 (langchainjs #8357, sintoma type:'None'). Fallback: JSON Schema cru.
         const rawSchema = z.toJSONSchema(schema as ZodType<Record<string, unknown>>)
         const structured = model.withStructuredOutput(rawSchema as Record<string, unknown>, { name: 'decide' })
         return (await structured.invoke(messages)) as T
       }
       ```
       Importar `z` de 'zod' no topo (bare import, como em schemas.ts). Tanto `createLmStudioProvider` quanto `createOpenAiProvider` usam `decideWithFallback` no seu `decide()`.

    3) Adicionar `export function createOpenAiProvider(): LlmProvider`:
       - `const model = new ChatOpenAI({ model: config.openaiModel, apiKey: config.openaiApiKey, maxTokens: config.openaiMaxTokens, timeout: Number(process.env.LLM_TIMEOUT_MS ?? 20000), maxRetries: Number(process.env.LLM_MAX_RETRIES ?? 1), ...openaiModelKwargs() })` — SEM `configuration.baseURL` (endpoint padrão OpenAI).
       - `openaiModelKwargs()`: D-04 condicional — retorna `{ reasoning: { effort: config.openaiReasoningEffort } }` SOMENTE se `/^(gpt-5|o\d)/.test(config.openaiModel)`; caso contrário retorna `{}`. Comentar: "gpt-4.1-mini é família NÃO-reasoning; enviar reasoning.effort é erro (D-03/D-04)".
       - `decide`: usa `decideWithFallback(model, schema, messages)`.
       - `chat`: `String((await model.invoke(messages)).content)`.
       - `available`: probe via `fetch('https://api.openai.com/v1/models', { headers: { Authorization: \`Bearer ${config.openaiApiKey}\` }, signal })` com o mesmo timeout/AbortController do padrão existente; `res.ok` ou false; nunca lança.
       - `embed`: DELEGA a `createLocalEmbedder().embed` (D-11/D-03 — embeddings SEMPRE locais mesmo com chat na cloud).

    4) Adicionar `export function createProvider(): LlmProvider` (D-13):
       `return config.llmProvider === 'openai' ? createOpenAiProvider() : createLmStudioProvider()`
       Comentar a composição: "openai compõe createLocalEmbedder → embed local mesmo com chat cloud".

    NÃO alterar a forma da interface `LlmProvider`. NÃO tocar src/llm/structured.ts. Importar `config` de '../config'.
  </action>
  <verify>
    <automated>cd "C:\Users\biel1\OneDrive\Documentos\GitHub\MineMind" && bun run typecheck && bun test src/llm/structured.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function createOpenAiProvider" src/llm/provider.ts` existe
    - `grep -n "export function createLocalEmbedder" src/llm/provider.ts` existe
    - `grep -n "export function createProvider" src/llm/provider.ts` existe
    - `grep -n "z.toJSONSchema" src/llm/provider.ts` existe (fallback D-16)
    - `grep -nE "gpt-5|o\\\\d|reasoning" src/llm/provider.ts` mostra o gate condicional de reasoning.effort
    - `grep -n "createLocalEmbedder()" src/llm/provider.ts` aparece DENTRO de createOpenAiProvider (embed delegado)
    - provider.ts NÃO importa OpenAIEmbeddings: `grep -c "OpenAIEmbeddings" src/llm/provider.ts` == 0
    - `bun run typecheck` sai 0 e `bun test src/llm/structured.test.ts` sai 0 (suíte mock existente intacta)
  </acceptance_criteria>
  <done>provider.ts expõe createOpenAiProvider/createLocalEmbedder/createProvider; decide() de ambos os providers usa o fallback z.toJSONSchema (D-16); reasoning.effort é condicional à família do modelo (D-04); embed cloud delega ao embedder local (D-03/D-11); a suíte mock existente continua verde.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passa (sem erros de tipo introduzidos).
- `bun test src/llm/structured.test.ts` continua 100% verde (interface LlmProvider preservada).
- `grep` confirma createOpenAiProvider, createLocalEmbedder, createProvider e o fallback z.toJSONSchema presentes.
- Nenhum import de OpenAIEmbeddings em provider.ts.
</verification>

<success_criteria>
- PROV-01: existe caminho cloud GPT-4.1-mini selecionável por env (createOpenAiProvider + createProvider lendo LLM_PROVIDER).
- PROV-02: local continua default custo-zero (createProvider → createLmStudioProvider quando llmProvider !== 'openai').
- PROV-03: embed() do provider cloud delega ao createLocalEmbedder local.
- PROV-04 (parcial): fallback D-16 (z.toJSONSchema) blinda decide() contra o caveat zod v4 nos DOIS providers; teste de paridade fica no Plano 03.
</success_criteria>

<output>
Após completar, criar `.planning/phases/06-llm-provider-factory/06-01-SUMMARY.md`.
</output>
