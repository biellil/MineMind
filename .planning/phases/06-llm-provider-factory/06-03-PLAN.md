---
phase: 06-llm-provider-factory
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/llm/parity.test.ts
  - src/llm/structured.test.ts
autonomous: true
requirements: [PROV-04]
must_haves:
  truths:
    - "Um teste schema-only afirma que o JSON Schema derivado de ActionDecisionSchema tem type:'object' — detecta a regressão do caveat zod v4 (type:'None') SEM rede"
    - "Um teste mock prova que o fallback D-16 dispara: quando withStructuredOutput lança 'type:None' na 1ª chamada, decide ainda retorna objeto válido via z.toJSONSchema"
    - "UM teste live gated por RUN_LIVE_PARITY=1 percorre createProvider local E openai pela mesma interface e faz ActionDecisionSchema.parse() em cada saída; CI nunca o aciona"
    - "validate→repair→fallback continua obrigatório nos dois providers (D-17) — os testes existentes de structured.test.ts permanecem verdes"
  artifacts:
    - path: "src/llm/parity.test.ts"
      provides: "teste schema-only (D-14) + teste live gated RUN_LIVE_PARITY (D-15)"
      contains: "RUN_LIVE_PARITY"
    - path: "src/llm/structured.test.ts"
      provides: "novo teste do fallback D-16 (mock que lança type:None na 1ª chamada de decide)"
      contains: "type:None"
  key_links:
    - from: "src/llm/parity.test.ts (schema-only)"
      to: "ActionDecisionSchema via z.toJSONSchema"
      via: "asserção type === 'object' (D-14)"
      pattern: "z.toJSONSchema|type.*object"
    - from: "src/llm/parity.test.ts (live)"
      to: "createProvider() local e openai"
      via: "test.skipIf(!process.env.RUN_LIVE_PARITY) (D-15)"
      pattern: "RUN_LIVE_PARITY|skipIf"
---

<objective>
Garantir paridade de structured output (PROV-04) entre LM Studio local e GPT-4.1-mini cloud por três camadas custo-zero/CI + uma sob demanda: (1) teste schema-only que detecta a regressão zod v4 (type:'None') sem rede, (2) teste mock que prova o fallback D-16, (3) um teste live gated por env que percorre os dois providers reais. Confirmar que validate→repair→fallback (D-17) continua obrigatório nos dois caminhos.

Purpose: O critério de aceite da abstração de provider é "o loop funciona com local E com cloud" (Pitfall 7). Sem o teste schema-only, o caveat zod v4 quebra silenciosamente só num provider.
Output: `src/llm/parity.test.ts` (D-14 schema-only + D-15 live gated) e um novo teste de fallback D-16 em `src/llm/structured.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-llm-provider-factory/06-CONTEXT.md
@.planning/phases/06-llm-provider-factory/06-01-SUMMARY.md

<interfaces>
<!-- Contratos do Plano 01 + suíte existente. NÃO reexplorar. -->

Do Plano 01 (src/llm/provider.ts):
```typescript
export function createProvider(opts?: { db?: Database }): LlmProvider  // seleciona local/openai por env
export interface LlmProvider { decide<T>(schema, messages): Promise<T>; chat; available; embed }
// decide() de ambos os providers usa o fallback D-16: catch → z.toJSONSchema(schema) → withStructuredOutput cru
```

src/llm/schemas.ts:
```typescript
export const ActionDecisionSchema = z.object({
  action: z.enum(['gather','explore','navigate','idle','chat']),
  target: z.string().max(64).optional(),
  reason: z.string().max(200),
})
export type ActionDecision = z.infer<typeof ActionDecisionSchema>
```

src/llm/structured.test.ts — padrão de mock existente a REUSAR (não duplicar mockProvider):
```typescript
function mockProvider(opts: { available: boolean; decide?: () => Promise<unknown> }): LlmProvider
const VALID: ActionDecision = { action: 'gather', target: 'oak_log', reason: '...' }
const FALLBACK: ActionDecision = { action: 'idle', reason: 'fallback determinístico' }
// testes com bun:sqlite/bun:test: import { test, expect } from 'bun:test'
```

bun test gating: `test.skipIf(condition)('nome', fn)` — pular o live test quando RUN_LIVE_PARITY não está setado.
zod v4: `z.toJSONSchema(ActionDecisionSchema)` retorna um JSON Schema; o caveat #8357 produz `type:'None'`/ausente quando quebrado — o teste afirma `=== 'object'`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: parity.test.ts — schema-only (D-14) + live gated RUN_LIVE_PARITY (D-15)</name>
  <files>src/llm/parity.test.ts</files>
  <read_first>
    - src/llm/schemas.ts (ActionDecisionSchema — alvo do schema-only e do parse live)
    - src/llm/structured.test.ts (estilo bun:test: `import { test, expect } from 'bun:test'`; mocks)
    - src/llm/provider.ts (createProvider — alvo do teste live)
    - .planning/phases/06-llm-provider-factory/06-CONTEXT.md (D-14 schema-only type:'object'; D-15 live gated; "Claude's Discretion": formato dos fixtures)
  </read_first>
  <action>
    Criar src/llm/parity.test.ts. `import { test, expect } from 'bun:test'`, `import { z } from 'zod'`, `import { ActionDecisionSchema } from './schemas'`, `import { createProvider } from './provider'`, `import { HumanMessage } from '@langchain/core/messages'`.

    1) Teste schema-only (D-14, SEM rede, roda no CI):
       - `test('JSON Schema de ActionDecisionSchema tem type:object (detecta caveat zod v4 #8357)', () => { ... })`
       - `const json = z.toJSONSchema(ActionDecisionSchema) as Record<string, unknown>`
       - `expect(json.type).toBe('object')`  // a regressão #8357 produz type:'None'/ausente
       - `expect(json.properties).toBeDefined()` e afirmar que `(json.properties as any).action` existe (o enum de ação está no schema derivado).
       - Comentar: "este teste sozinho pega a regressão zod v4 ↔ withStructuredOutput sem custo de rede".

    2) Teste live gated (D-15, NUNCA roda no CI):
       - `const LIVE = !!process.env.RUN_LIVE_PARITY`
       - `test.skipIf(!LIVE)('paridade live: local e openai produzem ActionDecision parseável', async () => { ... })`
       - Dentro: para cada provider configurado por env — rodar com o provider efetivo do ambiente atual via `createProvider()` (sem db → sem cap; o dev seta LLM_PROVIDER antes de rodar) — montar `messages = [new HumanMessage('Decida sua próxima ação. Responda só JSON.')]`, chamar `const out = await provider.decide(ActionDecisionSchema, messages)` e `expect(() => ActionDecisionSchema.parse(out)).not.toThrow()`.
       - Comentar claramente no topo do teste: "Rodar antes de release: `LLM_PROVIDER=local RUN_LIVE_PARITY=1 bun test src/llm/parity.test.ts` e depois `LLM_PROVIDER=openai OPENAI_API_KEY=... RUN_LIVE_PARITY=1 bun test src/llm/parity.test.ts`. CI nunca seta RUN_LIVE_PARITY."

    Não acessar rede no caminho não-gated. O schema-only NÃO deve depender de createProvider.
  </action>
  <verify>
    <automated>cd "C:\Users\biel1\OneDrive\Documentos\GitHub\MineMind" && bun test src/llm/parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "z.toJSONSchema(ActionDecisionSchema)" src/llm/parity.test.ts` existe
    - `grep -n "toBe('object')" src/llm/parity.test.ts` existe (asserção D-14)
    - `grep -n "RUN_LIVE_PARITY" src/llm/parity.test.ts` existe
    - `grep -n "skipIf" src/llm/parity.test.ts` existe (live nunca roda sem o env)
    - `bun test src/llm/parity.test.ts` sai 0 SEM RUN_LIVE_PARITY (schema-only passa, live é skipped) e NÃO faz chamada de rede
  </acceptance_criteria>
  <done>parity.test.ts contém o teste schema-only (type:'object' via z.toJSONSchema) que roda no CI e o teste live gated por RUN_LIVE_PARITY que percorre createProvider; rodar a suíte sem o env passa sem rede.</done>
</task>

<task type="auto">
  <name>Task 2: teste do fallback D-16 em structured.test.ts (mock que lança type:None)</name>
  <files>src/llm/structured.test.ts</files>
  <read_first>
    - src/llm/structured.test.ts (TODO — reusar mockProvider/VALID/FALLBACK; não duplicar helpers)
    - src/llm/provider.ts (decideWithFallback do Plano 01 — o caminho que o teste exercita indiretamente)
    - .planning/phases/06-llm-provider-factory/06-CONTEXT.md (D-16: fallback testável SEM rede; D-17: validate→repair→fallback obrigatório nos dois providers)
  </read_first>
  <action>
    Em src/llm/structured.test.ts, adicionar UM teste novo que prova que o caminho decideAction sobrevive a um provider cujo decide lança na 1ª tentativa simulando o sintoma do caveat zod v4 e tem sucesso na repair (cobrindo D-16/D-17 via a interface pública decideAction):

    - `test('decide lança "type:None" (caveat zod v4 #8357) na 1ª tentativa e a repair recupera -> retorna válido', async () => { ... })`
    - `let calls = 0`
    - `const provider = mockProvider({ available: true, decide: async () => { calls += 1; if (calls === 1) throw new Error("Invalid schema: type:'None' (zod v4 #8357)"); return VALID } })`
    - `const result = await decideAction(provider, messages, fallback)`
    - `expect(result.action).toBe('gather')`
    - `expect(calls).toBe(2)`  // 1ª falhou com o sintoma do caveat, repair recuperou (D-17)
    - Comentar: "Prova que o caminho de decisão sobrevive ao sintoma do caveat zod v4 sem rede; o fallback z.toJSONSchema vive em provider.decide (D-16), e decideAction garante validate→repair→fallback nos dois providers (D-17)."

    NÃO remover nenhum teste existente. NÃO mudar a forma de mockProvider.
  </action>
  <verify>
    <automated>cd "C:\Users\biel1\OneDrive\Documentos\GitHub\MineMind" && bun test src/llm/structured.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "type:None\|type:'None'\|#8357" src/llm/structured.test.ts` existe no teste novo
    - `grep -c "^test(\|^  test(\|test('" src/llm/structured.test.ts` aumentou em 1 vs. o original (>= 7 testes)
    - `bun test src/llm/structured.test.ts` sai 0 com TODOS os testes (existentes + novo) verdes
  </acceptance_criteria>
  <done>structured.test.ts prova que o caminho de decisão recupera do sintoma 'type:None' do caveat zod v4 via repair (D-16/D-17) sem rede; suíte inteira verde.</done>
</task>

</tasks>

<verification>
- `bun test src/llm/parity.test.ts src/llm/structured.test.ts` 100% verde sem RUN_LIVE_PARITY (live skipped, schema-only e fallback passam).
- Nenhum acesso de rede na suíte de CI.
- `grep` confirma: z.toJSONSchema + type:'object' (D-14), RUN_LIVE_PARITY + skipIf (D-15), teste de type:None (D-16/D-17).
</verification>

<success_criteria>
- PROV-04: paridade de structured output verificada por (1) schema-only que pega o caveat zod v4 no CI, (2) teste do fallback D-16, e (3) teste live gated que percorre os dois providers reais sob demanda; D-17 (validate→repair→fallback nos dois) preservado.
</success_criteria>

<output>
Após completar, criar `.planning/phases/06-llm-provider-factory/06-03-SUMMARY.md`.
</output>
