---
phase: 10-tech-tree-dag-needs
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01-PLAN.md
files_modified:
  - src/motivation/tech-tree.ts
  - src/cognition/nodes.ts
  - src/skills/tool-selector.ts
  - src/skills/dig.ts
  - src/config.ts
  - src/cognition/nodes.test.ts
autonomous: true
requirements:
  - TECH-02
  - TECH-04
  - TECH-05
must_haves:
  truths:
    - "Quando resources need está insatisfeita, o agente escolhe o primeiro item da gatheringLadder que falta e aciona resolveDag sem consultar o LLM"
    - "O execute node roteia sub-goals do DAG (gather:/craft:/smelt:/ensure:) para as skills corretas deterministicamente"
    - "dig.ts usa a melhor ferramenta disponível por tier (selectToolFor ranqueado) antes de minerar"
    - "selectToolFor retorna a ferramenta de maior tier da categoria disponível no inventário"
    - "A progressão madeira→pedra→ferro funciona de ponta a ponta: goals encadeados por dependsOn completados sequencialmente"
  artifacts:
    - path: src/skills/tool-selector.ts
      provides: "selectToolFor com ranking por tier (TOOL_TIER table)"
      contains: "TOOL_TIER"
    - path: src/config.ts
      provides: "resourceMinQuantity — quantidade mínima por item da ladder para considerar satisfeito"
      contains: "resourceMinQuantity"
    - path: src/cognition/nodes.ts
      provides: "observe com ponte need→DAG; execute com roteador determinístico goal-tipo→skill"
      contains: "resolveDag\|goalToSkillParams\|pickTechTarget"
  key_links:
    - from: src/cognition/nodes.ts (observe)
      to: src/motivation/tech-tree.ts
      via: "resolveDag chamado quando resources urgente"
      pattern: "resolveDag"
    - from: src/cognition/nodes.ts (execute)
      to: src/skills/index.ts
      via: "roteador goalToSkillParams mapeia id do sub-goal para skill"
      pattern: "goalToSkillParams\|gather:|craft:|smelt:"
    - from: src/skills/dig.ts
      to: src/skills/tool-selector.ts
      via: "pré-flight usa selectToolFor ranqueado (TECH-05); import atualizado de equip.ts → tool-selector.ts"
      pattern: "selectToolFor"
---

<objective>
Fiar o DAG do Plan 01 ao loop cognitivo: (1) ponte determinística `resources` need → `resolveDag` no observe node (D-09/D-10); (2) roteador determinístico no execute node que mapeia sub-goals do DAG para skills sem LLM (Pitfall 5 da pesquisa); (3) evoluir `selectToolFor` com tabela de tier estática (D-12/D-14) e verificar que dig.ts usa a versão ranqueada (TECH-05).

Purpose: Sem o fio, o DAG existe mas ninguém o aciona nem roteia os sub-goals para execução. Este plano é o que transforma o resolver em progressão real ao vivo.

Output: Observe node com ponte need→DAG; execute node com roteador goal→skill; equip.ts com selectToolFor ranqueado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-tech-tree-dag-needs/10-CONTEXT.md
@.planning/phases/10-tech-tree-dag-needs/10-RESEARCH.md
@.planning/phases/10-tech-tree-dag-needs/10-01-SUMMARY.md
@src/cognition/nodes.ts
@src/skills/equip.ts
@src/skills/dig.ts
@src/config.ts

<interfaces>
<!-- Contratos do Plan 01 que este plano consome. Ler 10-01-SUMMARY.md para os valores reais. -->

De src/motivation/tech-tree.ts (Plan 01 criou):
```typescript
export function resolveDag(
  targetItem: string,
  bot: Bot,
  memo?: Map<string, DagResult>,
  depth?: number,
  basePriority?: number,
  now?: number,
): DagResult
// DagResult = Goal[] | { unresolvable: true }
// IDs de goal: 'gather:item', 'craft:item', 'smelt:source', 'ensure:crafting_table'
```

De src/motivation/goals.ts (Plan 01 evoluiu):
```typescript
export function selectGoal(
  current: Goal | null,
  candidates: Goal[],
  ctx: SelectGoalContext,
  cfg: MotivationConfig,
  completedIds?: Set<string>,  // D-06 — default new Set()
): Goal | null
```

De src/cognition/state.ts (Plan 01 adicionou):
```typescript
// holder.completedGoalIds: Set<string>  — adicionado pelo Plan 01
```

De src/config.ts (existente — base para bridge D-09):
```typescript
config.gatheringLadder: ReadonlyArray<string>
// ['oak_log', 'birch_log', ..., 'cobblestone', 'stone', 'coal_ore', 'iron_ore', ...]
config.gatherSearchRadius: number  // 16
config.goalThreshold: number       // 0.5
```

De src/skills/equip.ts (existente — a evoluir):
```typescript
// Função a evoluir (D-14/D-17 Fase 9):
export function selectToolFor(bot: Bot, category: string): Item | null
// Retorna atualmente o PRIMEIRO match (sem ranking). Fase 10 muda para melhor tier.
const CATEGORY_PATTERNS: Record<string, RegExp>  // existente — NÃO alterar
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Evoluir selectToolFor com tier ranking + ponte need→DAG no observe</name>
  <read_first>
    - src/skills/equip.ts (CATEGORY_PATTERNS existente e selectToolFor binário a refatorar — ler para entender padrões antes de criar tool-selector.ts)
    - src/cognition/nodes.ts (observe node completo — onde inserir a ponte D-09; execute node — onde inserir roteador)
    - src/cognition/state.ts (CognitiveStateHolder — campos disponíveis no holder)
    - src/motivation/needs.ts (urgency, NeedKind — para detectar resources insatisfeita)
    - src/config.ts (gatheringLadder, goalThreshold, resourceMinQuantity a adicionar)
    - .planning/phases/10-tech-tree-dag-needs/10-RESEARCH.md (Pattern 3: Tier Table, Pattern 4: Ponte Need→Goal)
  </read_first>
  <files>src/skills/tool-selector.ts, src/skills/dig.ts, src/config.ts, src/cognition/nodes.ts</files>
  <action>
**1. Criar src/skills/tool-selector.ts — selectToolFor com ranking por tier (D-12/D-14 — novo arquivo por decisão do usuário):**

Criar `src/skills/tool-selector.ts` como novo arquivo (D-14 do usuário: "A tabela tier + o mapeamento categoria→materiais ficam em src/skills/tool-selector.ts"). O arquivo exporta `selectToolFor` com ranking por tier e a tabela `TOOL_TIER`.

O `equip.ts` existente exporta atualmente `selectToolFor` — o novo `tool-selector.ts` substituirá e re-exportará essa função. O `dig.ts` importará de `tool-selector.ts` em vez de `equip.ts` (atualizar o import em dig.ts).

`src/skills/tool-selector.ts` (CRIAR):  
Importar `matchesCategory` de `equip.ts` OU duplicar a lógica se não estiver exportada — verificar no equip.ts durante leitura.
```typescript
// src/skills/tool-selector.ts — CRIAR (D-14 Fase 10)
// Exporta selectToolFor com ranking por tier, substituindo a versão binária de equip.ts.

import type { Bot } from 'mineflayer'
import type { Item } from 'prismarine-item'

// Importar matchesCategory de equip.ts se exportada, ou copiar a lógica
// (verificar ao ler equip.ts: se não for exportada, reproduzir inline)
import { matchesCategory } from './equip'

/**
 * Tier de ferramenta por material (D-12 Fase 10).
 * Ranking estático: wooden=1 < stone=2 < iron=3 < diamond=4 < netherite=5.
 * Verificado via minecraft-data 1.21.4 (RESEARCH.md Pattern 3).
 */
const TOOL_TIER: Record<string, number> = {
  wooden_pickaxe: 1, wooden_axe: 1, wooden_shovel: 1, wooden_sword: 1,
  stone_pickaxe:  2, stone_axe:  2, stone_shovel:  2, stone_sword:  2,
  iron_pickaxe:   3, iron_axe:   3, iron_shovel:   3, iron_sword:   3,
  diamond_pickaxe: 4, diamond_axe: 4, diamond_shovel: 4, diamond_sword: 4,
  netherite_pickaxe: 5, netherite_axe: 5, netherite_shovel: 5, netherite_sword: 5,
}
```

Alterar `selectToolFor` para retornar a ferramenta de MAIOR tier da categoria:
```typescript
export function selectToolFor(bot: Bot, category: string): Item | null {
  const items = bot.inventory.items().filter((it) => matchesCategory(it.name, category))
  if (items.length === 0) return null
  // D-12: retorna a ferramenta de maior tier disponível (fase 9: binário; fase 10: ranqueado)
  return items.reduce((best, it) => {
    const tierBest = TOOL_TIER[best.name] ?? 0
    const tierIt   = TOOL_TIER[it.name]   ?? 0
    return tierIt > tierBest ? it : best
  })
}
```

Atualizar o import em `dig.ts`: substituir `from './equip'` por `from './tool-selector'` para `selectToolFor` (D-14: o ponto de chamada não muda, apenas o caminho do import). Confirmar que `equip.ts` ainda exporta `CATEGORY_PATTERNS` e outras funções para quem as consome — `tool-selector.ts` apenas re-exporta `selectToolFor`.

**2. Adicionar resourceMinQuantity a src/config.ts (D-09 — quantidade mínima para satisfação):**

Adicionar campo ao objeto `config` na seção Fase 10, logo após os campos Fase 9:
```typescript
// === Fase 10: Tech Tree DAG + Needs ===
// D-09: quantidade mínima de cada item da gatheringLadder para considerar "satisfeito" (bridge need→DAG).
// O agente considera que tem "suficiente" de um item se tiver pelo menos esta quantidade no inventário.
// Valor baixo (1) garante que a progressão sempre avance para o próximo tier.
resourceMinQuantity: parseInt(process.env.RESOURCE_MIN_QUANTITY || '1', 10),
```

Adicionar validação após os checks de Fase 9:
```typescript
// Fase 10: validação da quantidade mínima de recursos
if (config.resourceMinQuantity < 1) {
  throw new Error(`RESOURCE_MIN_QUANTITY inválido: ${config.resourceMinQuantity}. Deve ser >= 1.`)
}
```

**3. Inserir ponte need→DAG no observe node (src/cognition/nodes.ts):**

No observe node, após `holder.currentGoal = selected` e antes do return, inserir a ponte determinística (D-09/D-10):

```typescript
// === Fase 10 D-09/D-10: Ponte resources need → resolveDag (determinística, sem LLM) ===
// Quando resources está insatisfeita e o goal atual não é um sub-goal de tech-tree:
// percorrer gatheringLadder → primeiro item abaixo de resourceMinQuantity → resolveDag.
// O LLM pode sobrescrever via preempção ASSISTANT (D-10), mas a ponte nunca usa o LLM.
const resourcesNeed = holder.needs.find(n => n.kind === 'resources')
const resourcesUrgent = resourcesNeed !== undefined && urgency(resourcesNeed, t, mcfg) > mcfg.goalThreshold
const currentIsTechGoal = holder.currentGoal?.id.includes(':') && !holder.currentGoal.id.startsWith('need:')

if (resourcesUrgent && !currentIsTechGoal && snapshot) {
  // pickTechTarget: primeiro item da ladder que falta em quantidade suficiente
  const invCounts = new Map<string, number>()
  for (const slot of snapshot.inventory) {
    invCounts.set(slot.name, (invCounts.get(slot.name) ?? 0) + slot.count)
  }
  let techTarget: string | null = null
  for (const item of config.gatheringLadder) {
    const have = invCounts.get(item) ?? 0
    if (have < config.resourceMinQuantity) {
      techTarget = item
      break
    }
  }

  if (techTarget) {
    // Reconstruir DAG somente se o target mudou ou não há sub-goals no holder (D-03)
    const dagKey = `dag:${techTarget}`
    const alreadyHasDag = holder.goals.some(g => g.id.startsWith('gather:') || g.id.startsWith('craft:') || g.id.startsWith('smelt:'))
    if (!alreadyHasDag || holder.currentGoal?.id === dagKey || holder.goals.length === 0) {
      const { resolveDag } = await import('../motivation/tech-tree')
      const dagMemo = new Map()
      const dagResult = resolveDag(techTarget, bot, dagMemo, 0, resourcesNeed ? urgency(resourcesNeed, t, mcfg) : 0.8, t)
      if (!('unresolvable' in dagResult)) {
        // Inserir sub-goals do DAG nos candidatos e no holder
        // Filtro: não re-inserir goals já completados
        const newGoals = dagResult.filter(g => !(holder.completedGoalIds ?? new Set()).has(g.id))
        holder.goals = [...holder.goals.filter(g => !g.id.startsWith('gather:') && !g.id.startsWith('craft:') && !g.id.startsWith('smelt:') && !g.id.startsWith('ensure:')), ...newGoals]
        // Selecionar a folha executável: o goal sem dependsOn não satisfeito
        const completedIds = holder.completedGoalIds ?? new Set<string>()
        const executableLeaf = newGoals.find(g => g.dependsOn.every(dep => completedIds.has(dep)))
        if (executableLeaf) {
          holder.currentGoal = executableLeaf
        }
      } else {
        log(`[tech-tree] ${techTarget} é unresolvable — ignorando`)
      }
    }
  }
}
// === Fim da ponte Fase 10 ===
```

**Adicionar import de resolveDag e urgency no topo de nodes.ts** (urgency já pode estar importado — verificar):
```typescript
import { urgency } from '../motivation/needs'  // já importado provavelmente — verificar antes de duplicar
// resolveDag: dynamic import dentro do bloco (evita circular dep se houver)
```

**Notas de implementação:**
- Usar import dinâmico `await import('../motivation/tech-tree')` dentro do bloco para evitar circular dependency potencial entre nodes.ts e tech-tree.ts (que importa Bot de mineflayer)
- O observe node já tem `const t = now()` e `mcfg` disponíveis — reusar sem redeclarar
- `snapshot.inventory` já tem `{ name: string, count: number }[]` — verificar o tipo exato lendo snapshot.ts
- Se `urgency` já está importado de `'../motivation/needs'` em nodes.ts, não re-importar
  </action>
  <verify>
    <automated>cd /root/MineMind && bun test src/skills/equip.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/skills/tool-selector.ts existe e exporta selectToolFor ranqueado por tier e TOOL_TIER
    - selectToolFor retorna ferramenta de maior tier quando há múltiplas opções (iron antes de wood)
    - TOOL_TIER está em tool-selector.ts com todas as ferramentas de madeira/pedra/ferro/diamante/netherite
    - dig.ts importa selectToolFor de tool-selector.ts (import atualizado)
    - config.resourceMinQuantity existe com default 1
    - observe node tem bloco da ponte resources→DAG inserido
    - `grep -n "TOOL_TIER\|selectToolFor" /root/MineMind/src/skills/tool-selector.ts` retorna resultado
    - `grep -n "resolveDag\|pickTechTarget\|techTarget" /root/MineMind/src/cognition/nodes.ts` retorna resultado
    - Testes de equip.ts continuam passando (nada quebrado pela extração)
  </done>
  <acceptance_criteria>
    - `grep -n "TOOL_TIER" /root/MineMind/src/skills/tool-selector.ts` retorna a tabela com wooden/stone/iron/diamond
    - `grep -n "items.reduce\|tierIt > tierBest" /root/MineMind/src/skills/tool-selector.ts` mostra o reduce de ranking
    - `grep -n "tool-selector" /root/MineMind/src/skills/dig.ts` confirma o import atualizado
    - `grep -n "resourceMinQuantity" /root/MineMind/src/config.ts` retorna resultado com default '1'
    - `grep -n "resolveDag\|techTarget\|gatheringLadder" /root/MineMind/src/cognition/nodes.ts` retorna resultado no observe node
    - `bun test src/skills/equip.test.ts 2>&1 | grep -E "pass|fail"` — todos passando
    - `bun test 2>&1 | grep -E "^Tests\|failed"` — zero novos testes falhando
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Tarefa 2: Roteador determinístico goal→skill no execute node</name>
  <read_first>
    - src/cognition/nodes.ts (execute node completo — bloco de mapeamento estado→skill a estender)
    - src/skills/station.ts (ensureStation — assinatura e como chamá-la no roteador)
    - src/skills/craft.ts (craft — assinatura de CraftSchema)
    - src/skills/smelt.ts (smelt — assinatura de SmeltSchema)
    - src/skills/dig.ts (dig — assinatura de DigSchema)
    - .planning/phases/10-tech-tree-dag-needs/10-RESEARCH.md (Pitfall 5, Code Example 4)
    - .planning/phases/10-tech-tree-dag-needs/10-CONTEXT.md (D-09/D-10)
  </read_first>
  <files>src/cognition/nodes.ts, src/cognition/nodes.test.ts</files>
  <action>
**Adicionar roteador determinístico goalToSkillParams e bloco de execução de sub-goals DAG no execute node:**

O roteador é uma função pura (D-10: sem LLM) que mapeia o ID de um sub-goal do DAG para os params da skill correspondente:

```typescript
// Dentro de createNodes (antes do execute node, ou como helper interno):
/**
 * D-09/D-10 Fase 10: Mapeia ID de sub-goal do DAG para (skill, params) sem LLM.
 * 'gather:X' → dig(X,1); 'craft:X' → craft(X,1); 'smelt:X' → smelt(X,1); 'ensure:X' → ensureStation(X).
 * Pitfall 5: iron_ingot como intermediário — 'smelt:iron_ore' roteia para smelt com oreName='iron_ore'.
 */
export function goalToSkillParams(goalId: string): { skill: string; paramsJson: string } | null {
  const colonIdx = goalId.indexOf(':')
  if (colonIdx === -1) return null
  const type = goalId.slice(0, colonIdx)
  const item = goalId.slice(colonIdx + 1)
  if (!item) return null
  switch (type) {
    case 'gather':
      return { skill: 'dig', paramsJson: JSON.stringify({ target: item, count: 1 }) }
    case 'craft':
      return { skill: 'craft', paramsJson: JSON.stringify({ itemName: item, count: 1 }) }
    case 'smelt':
      return { skill: 'smelt', paramsJson: JSON.stringify({ oreName: item, count: 1 }) }
    case 'ensure':
      // ensure:crafting_table / ensure:furnace → ensureStation é chamado internamente por craft/smelt
      // Aqui apenas sinaliza 'noop' — o craft/smelt dentro do sub-goal seguinte vai chamar ensureStation
      return null
    default:
      return null
  }
}
```

**Inserir bloco de roteamento de sub-goals no execute node (logo ANTES do bloco `if (snap && state === 'gathering')`):**

O bloco verifica se o currentGoal é um sub-goal do DAG e roteia diretamente:
```typescript
// === Fase 10 D-09/D-10: Roteador determinístico de sub-goals do DAG ===
// Se o currentGoal é um sub-goal do DAG (prefixo gather:/craft:/smelt:/ensure:),
// rotear para a skill correspondente SEM depender do estado cognitivo ou da decisão LLM.
// Isso é a ponte determinística: o LLM não precisa conhecer a cadeia de tech-tree.
const currentGoal = holder.currentGoal
if (snap && currentGoal && currentGoal.id.includes(':') && !currentGoal.id.startsWith('need:')) {
  const routing = goalToSkillParams(currentGoal.id)
  if (routing) {
    skill = routing.skill
    target = routing.paramsJson
    log(`[tech-tree] roteando ${currentGoal.id} → ${skill}`)
  } else {
    // 'ensure:*' ou prefixo desconhecido: nenhuma ação, próximo tick vai para o sub-goal seguinte
    log(`[tech-tree] sub-goal ${currentGoal.id} é no-op (ensure/desconhecido) — avançando`)
    if (currentGoal) {
      // Marcar o ensure como completo automaticamente (ensureStation é chamado por craft/smelt)
      const updated = advanceProgress(currentGoal, 1)
      holder.goals = holder.goals.map(g => g.id === currentGoal.id ? updated : g)
      holder.currentGoal = updated
      if (!holder.completedGoalIds) holder.completedGoalIds = new Set()
      holder.completedGoalIds.add(currentGoal.id)
    }
  }
}
// === Fim do roteador Fase 10 ===

// ... continua para os blocos existentes (gathering, exploring, socializing, building)
// NOTA: Se skill já foi setada pelo roteador acima, os blocos seguintes NÃO sobrescrevem
// (a variável `skill` é checada antes de ser reescrita — adicionar guard: `if (!skill)`)
```

**IMPORTANTE: adicionar guard `if (!skill)` antes de cada bloco de mapeamento estado→skill existente:**
```typescript
// Exemplo:
if (!skill && snap && state === 'gathering') { ... }
if (!skill && snap && state === 'exploring') { ... }
if (!skill && snap && state === 'socializing') { ... }
if (!skill && snap && state === 'building' && fresh) { ... }
```

**Adicionar import de advanceProgress no topo de nodes.ts:**
```typescript
import { generateGoals, selectGoal, advanceProgress } from '../motivation/goals'
// advanceProgress já deve estar importado — verificar antes de duplicar
```

**Adicionar testes em src/cognition/nodes.test.ts** (ou criar o arquivo se não existir):
- Teste 1: goalToSkillParams('gather:oak_log') retorna `{ skill: 'dig', paramsJson: '{"target":"oak_log","count":1}' }`
- Teste 2: goalToSkillParams('craft:wooden_pickaxe') retorna skill='craft'
- Teste 3: goalToSkillParams('smelt:iron_ore') retorna skill='smelt' com oreName='iron_ore'
- Teste 4: goalToSkillParams('ensure:crafting_table') retorna null
- Teste 5: goalToSkillParams('need:resources') retorna null (não é sub-goal do DAG)

Exportar `goalToSkillParams` do módulo de nodes para os testes (ou extrair para helper testável em `src/motivation/tech-tree.ts`).

**D-03: Reconstrução do DAG após outcome=no_effect (decisão do usuário):**

Após o bloco de roteamento determinístico e a execução da skill, adicionar no execute node o tratamento de `no_effect` para sub-goals do DAG:

```typescript
// === Fase 10 D-03: Reconstrução do DAG em falha de sub-goal ===
// Quando resultado é no_effect em sub-goal do DAG, limpar os sub-goals do holder
// para forçar reconstrução no próximo tick (alreadyHasDag fica false).
const DAG_PREFIXES = ['gather:', 'craft:', 'smelt:', 'ensure:']
if (
  result.outcome === 'no_effect' &&
  currentGoal &&
  DAG_PREFIXES.some(p => currentGoal.id.startsWith(p))
) {
  log(`[tech-tree] ${currentGoal.id} retornou no_effect — limpando sub-goals DAG para reconstrução`)
  // Remove todos os sub-goals do DAG do holder (qualquer goal com prefixo DAG)
  holder.goals = holder.goals.filter(
    g => !DAG_PREFIXES.some(p => g.id.startsWith(p))
  )
  // Limpar currentGoal para que o observe não tente executar o mesmo sub-goal novamente
  if (DAG_PREFIXES.some(p => (holder.currentGoal?.id ?? '').startsWith(p))) {
    holder.currentGoal = null
  }
  // O observe no próximo tick vai verificar alreadyHasDag=false e reconstruir o DAG
}
// === Fim D-03 ===
```

**Nota sobre `DAG_PREFIXES`:** declarar como constante de módulo no topo de nodes.ts (fora das funções) para reutilizar no bloco do observe e no bloco D-03 do execute:
```typescript
// Constante de módulo — topo de nodes.ts
const DAG_PREFIXES = ['gather:', 'craft:', 'smelt:', 'ensure:'] as const
```
Atualizar o bloco do observe (Tarefa 1) para usar `DAG_PREFIXES` em vez de verificações inline para consistência.

**Verificar compatibilidade com Fase 09 G-01:**
O bloco `state === 'building'` existente (G-01) deve continuar funcionando quando o agente está em modo `building` e o currentGoal NÃO é um sub-goal do DAG. O guard `if (!skill)` garante isso — se o roteador do DAG não setou a skill, o bloco G-01 continua com a lógica LLM.

**Verificar bounds do pathfinder (TECH-05 critério 5):**
As skills que navegam (`dig`, `craft` via `ensureStation`, `smelt` via `ensureStation`) já usam `config.gatherSearchRadius` e `executeWithSafety`. Confirmar que as chamadas geradas pelo roteador herdam esses bounds automaticamente (sem mudança — os bounds estão dentro das próprias skills).
  </action>
  <verify>
    <automated>cd /root/MineMind && bun test src/cognition/nodes.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - goalToSkillParams mapeia todos os tipos de prefixo corretamente (gather/craft/smelt/ensure)
    - goalToSkillParams é exportada (export function) para os testes poderem importá-la diretamente
    - Bloco de roteamento aparece no execute node antes dos blocos existentes de estado
    - Guards `if (!skill)` protegem os blocos de estado existentes de sobrescrita indevida
    - advanceProgress é chamado para sub-goals 'ensure:*' automaticamente
    - D-03: bloco no_effect limpa sub-goals DAG do holder (holder.goals.filter) quando outcome=no_effect em goal com prefixo DAG
    - DAG_PREFIXES declarado como constante de módulo reutilizada em observe e execute
    - Testes de nodes.test.ts passam (incluindo os 5 novos testes do roteador)
    - `grep -n "goalToSkillParams" /root/MineMind/src/cognition/nodes.ts` retorna resultado com `export function`
    - `grep -n "if (!skill)" /root/MineMind/src/cognition/nodes.ts` retorna múltiplas ocorrências (guards)
    - `grep -n "DAG_PREFIXES\|no_effect.*DAG\|limpando sub-goals" /root/MineMind/src/cognition/nodes.ts` retorna resultado
  </done>
  <acceptance_criteria>
    - `grep -n "export function goalToSkillParams" /root/MineMind/src/cognition/nodes.ts` retorna resultado (export obrigatório para testes)
    - `grep -n "goalToSkillParams" /root/MineMind/src/cognition/nodes.ts` retorna pelo menos 2 ocorrências (definição + chamada)
    - `grep -n "if (!skill)" /root/MineMind/src/cognition/nodes.ts` retorna pelo menos 4 ocorrências (guards dos blocos de estado)
    - `grep -n "tech-tree.*roteando" /root/MineMind/src/cognition/nodes.ts` mostra log do roteador
    - `grep -n "DAG_PREFIXES" /root/MineMind/src/cognition/nodes.ts` retorna pelo menos 3 ocorrências (declaração + observe + execute D-03)
    - `grep -n "no_effect" /root/MineMind/src/cognition/nodes.ts` mostra o bloco D-03 que limpa sub-goals do holder
    - `grep -n "ensure.*crafting_table\|ensure:.*completo" /root/MineMind/src/cognition/nodes.ts` mostra o auto-advance do ensure
    - `bun test src/cognition/nodes.test.ts 2>&1 | grep -E "pass|fail"` — testes do roteador passando
    - `bun test 2>&1 | grep -c "fail"` — zero falhas no suite completo (regressão zero)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| observe → resolveDag → holder.goals | DAG resolve popula holder.goals; corrupção pode injetar goals inválidos |
| goalToSkillParams → skillRegistry | string parseada do goal.id vira parâmetro de skill; input não é externo mas pode ter IDs malformados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Denial of Service | observe — resolveDag no hot path | mitigate | DAG reconstruído somente quando `!alreadyHasDag` (D-03); memo compartilhado por chamada evita re-resolução a cada tick; sem I/O no caminho |
| T-10-07 | Denial of Service | Pathfinder — novas navegações do DAG (gather/ensure) | accept | Todas as skills (`dig`, `station.ts`) herdam bounds 999.1 via `executeWithSafety` + `gatherSearchRadius`; sem nova navegação direta no nodes.ts |
| T-10-08 | Tampering | goalToSkillParams — goal.id malformado | mitigate | `colonIdx === -1 → return null`; `!item → return null`; skill inválida não encontrada no skillRegistry retorna undefined → sem execução; guard `if (!skill)` seguro |
| T-10-09 | Denial of Service | holder.goals — flood de sub-goals do DAG | mitigate | Filtro `alreadyHasDag` previne re-inserção; DAG tem cap de 8 níveis → máximo ~10 sub-goals; holder.goals não cresce unbounded |
| T-10-10 | Elevation of Privilege | ensure:crafting_table auto-advance | accept | Auto-advance de 'ensure:*' não acessa recursos externos; ensureStation é chamado pelas skills de craft/smelt que verificam o estado real; falso-advance degradaria para no_effect no craft seguinte (grounded) |
</threat_model>

<verification>
Verificação geral do plano 02:

```bash
cd /root/MineMind

# Suite completo — zero regressões
bun test 2>&1 | tail -20

# Verificar que o roteador está no execute node
grep -n "goalToSkillParams\|roteador\|tech-tree" src/cognition/nodes.ts

# Verificar tier table (em tool-selector.ts — D-14)
grep -n "TOOL_TIER\|iron_pickaxe.*3\|stone_pickaxe.*2" src/skills/tool-selector.ts

# Verificar que dig.ts usa tool-selector (import atualizado — D-14)
grep -n "selectToolFor\|tool-selector" src/skills/dig.ts

# Verificar D-03: limpeza de sub-goals DAG em no_effect
grep -n "DAG_PREFIXES\|no_effect.*DAG\|limpando sub-goals" src/cognition/nodes.ts

# Verificar que a ponte need→DAG está no observe
grep -n "resolveDag\|techTarget\|gatheringLadder" src/cognition/nodes.ts | head -15

# Verificar guards nos blocos de estado
grep -n "if (!skill)" src/cognition/nodes.ts

# Config com resourceMinQuantity
grep -n "resourceMinQuantity" src/config.ts
```
</verification>

<success_criteria>
- `bun test` — zero falhas no suite completo (sem regressões das Fases 6–9)
- src/skills/tool-selector.ts existe e exporta selectToolFor com TOOL_TIER (D-14)
- selectToolFor retorna `iron_pickaxe` quando inventário tem `wooden_pickaxe` e `iron_pickaxe` (tier 3 > tier 1)
- dig.ts importa selectToolFor de tool-selector.ts (import atualizado)
- D-03: no_effect em sub-goal DAG limpa holder.goals de sub-goals e força reconstrução no próximo tick
- observe node tem bloco de ponte resources→DAG com resolveDag
- execute node tem roteador goalToSkillParams mapeando gather:/craft:/smelt: para skills
- dig.ts usa selectToolFor ranqueado sem mudança de call site (D-14)
- config.resourceMinQuantity existe com default 1
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/10-tech-tree-dag-needs/10-02-SUMMARY.md` com:
- Arquivos modificados com descrição das mudanças
- Roteador goalToSkillParams (função e lógica)
- Ponte need→DAG no observe (lógica de pickTechTarget)
- TOOL_TIER adicionada ao equip.ts
- Confirmação de bounds do pathfinder herdados
- Testes executados e resultados
</output>
