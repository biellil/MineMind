---
phase: 999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra
verified: 2026-06-19T18:05:00Z
status: passed
score: 7/7 must-haves verified
---

# Phase 999.1: Otimizar pathfinding da coleta (collectBlock) Verification Report

**Phase Goal:** Permitir PERCEPTION_RADIUS maior (ex: 32) sem estourar memória/OOM no estado Gathering. Bound o pathfinding (thinkTimeout/searchRadius), validar alcançabilidade antes de coletar, e separar "raio de percepção" do "raio de busca de coleta". Critérios D-07: (1) sem OOM, (2) timeout/noPath dispara dentro de digTimeoutMs, (3) event loop responde (lag < 200ms).
**Verified:** 2026-06-19T18:05:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

The phase has no canonical REQ-IDs; verification is against the D-01..D-07 decisions from 999.1-CONTEXT.md and the plan/SUMMARY must_haves. Each decision maps to a concrete observable truth verified in the actual codebase, not SUMMARY claims.

### Observable Truths

| #   | Truth (Decision)                                                                                                                   | Status     | Evidence |
| --- | ---------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------- |
| 1   | **D-01:** raio de coleta desacoplado da percepção — `gatherSearchRadius` (default 16) existe e é usado em `dig.ts`; percepção mantém 32 | ✓ VERIFIED | `config.ts:19` define `gatherSearchRadius`; `dig.ts:38` usa `maxDistance: config.gatherSearchRadius`; `perceptionRadius=32` intocado (`config.ts:15`). `config.perceptionRadius` ausente de `dig.ts`. |
| 2   | **D-02:** bounds do A* (`pathfinderSearchRadius` 48 / `pathfinderThinkTimeoutMs` 2000) expostos via env com validação de range | ✓ VERIFIED | `config.ts:22-23` + validação `config.ts:144-149` (searchRadius 1-256, thinkTimeout >=1). |
| 3   | **D-03:** bounds aplicados nos DOIS lugares (globais `bot.pathfinder.*` + `bot.collectBlock.movements`) após `loadPlugin(collectBlock)` | ✓ VERIFIED | `connection.ts:48-49` (globais), `52-54` (movements principal), `55-61` (`bot.collectBlock.movements` com guard `?.`). Tudo após `loadPlugin(collectBlock)` (linha 33). |
| 4   | **D-04/D-05:** pré-check síncrono `getPathTo` antes de `collect()` filtra inalcançáveis por instância; throw quando 0 alcançáveis alimenta cooldown por-tipo | ✓ VERIFIED | `dig.ts:52-57` filtra via `getPathTo`+`GoalGetToBlock`; `dig.ts:59-61` throw "Nenhuma instância alcançável"; `dig.ts:64` `collect(reachable)`. `nodes.ts:214-216` catch → `recordFailure`. |
| 5   | **D-06:** double-wrap removido — nó execute chama skill diretamente; cada skill registrada se auto-embrulha | ✓ VERIFIED | `nodes.ts:210` `await skillRegistry[skill!]!(bot, params)` (sem wrap externo); import de `executeWithSafety` removido de nodes.ts. dig/navigate auto-embrulham; follow/attack são stubs documentados. |
| 6   | **D-07 (1) sem OOM:** gathering de alvo inalcançável completa sem estourar heap (teto 200MB vs ~78GB do bug) | ✓ VERIFIED | `dig.oom.smoke.test.ts` Teste A asserta `heapAfter - heapBefore < 200MB`. Teste passa. |
| 7   | **D-07 (2+3):** skill rejeita dentro de digTimeoutMs E event loop responde (lag < 200ms) sob PERCEPTION_RADIUS=32 | ✓ VERIFIED | `dig.oom.smoke.test.ts` Testes A+B: `rejects.toThrow`, `maxLag < 200`, `PERCEPTION_RADIUS='32'`. Ambos passam. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact                              | Expected                                                          | Status     | Details |
| ------------------------------------- | ---------------------------------------------------------------- | ---------- | ------- |
| `src/config.ts`                       | 3 campos novos + validação de range                              | ✓ VERIFIED | gatherSearchRadius/pathfinderSearchRadius/pathfinderThinkTimeoutMs presentes; validação L141-149. |
| `.env.example` / `.env`               | Documentação/definição dos novos vars; PERCEPTION_RADIUS=32      | ✓ VERIFIED | Ambos têm os 3 vars + PERCEPTION_RADIUS=32 (sem workaround =8). |
| `src/bot/connection.ts`               | Bounds nos dois lugares após loadPlugin                          | ✓ VERIFIED | L48-61, ordem correta. |
| `src/skills/dig.ts`                   | findBlocks(gatherSearchRadius) + pré-check getPathTo             | ✓ VERIFIED | L38, L52-61, L64. |
| `src/skills/dig.test.ts`              | 3 casos (inalcançáveis/alcançável/sem candidato)                 | ✓ VERIFIED | Existe; 3 testes passam. |
| `src/cognition/nodes.ts`              | Nó execute sem wrap externo                                      | ✓ VERIFIED | L210 chamada direta; import removido. |
| `src/skills/follow.ts`, `attack.ts`   | Stubs documentados como não-embrulhados                          | ✓ VERIFIED | Comentário D-06 explícito em ambos. |
| `src/skills/dig.oom.smoke.test.ts`    | Smoke do tripé D-07 (heapUsed, rejects, setInterval lag<200)     | ✓ VERIFIED | Existe; 2 testes passam; contém todos os padrões exigidos. |

### Key Link Verification

| From                              | To                                                  | Via                                  | Status | Details |
| --------------------------------- | --------------------------------------------------- | ------------------------------------ | ------ | ------- |
| `process.env.GATHER_SEARCH_RADIUS`| `config.gatherSearchRadius`                         | parseInt default 16                  | WIRED  | `config.ts:19` |
| `config.pathfinderSearchRadius`   | `bot.pathfinder.searchRadius` + `collectBlock.movements` | atribuição pós loadPlugin       | WIRED  | `connection.ts:48,57` |
| `bot.pathfinder.getPathTo`        | filtro `reachable` antes de collect                 | status !== noPath/timeout            | WIRED  | `dig.ts:54-57` |
| nó execute                        | skill auto-embrulhada                               | chamada direta `skillRegistry[skill]`| WIRED  | `nodes.ts:210` |
| heartbeat setInterval ~50ms       | asserção lag < 200ms                                | drift do timer                       | WIRED  | `dig.oom.smoke.test.ts:36-41,112,138` |

### Data-Flow Trace (Level 4)

N/A — esta fase produz lógica de pathfinding/config e testes, não artefatos que renderizam dados dinâmicos (sem UI/componentes). O fluxo de dados crítico (config → connection/dig → comportamento do A*) está coberto pelos key links e pelos behavioral spot-checks abaixo.

### Behavioral Spot-Checks

| Behavior                                          | Command                                             | Result          | Status |
| ------------------------------------------------- | --------------------------------------------------- | --------------- | ------ |
| Typecheck limpo (toda a fase)                     | `bun run tsc --noEmit`                              | exit 0          | ✓ PASS |
| D-07 tripé (smoke headless) + dig pré-check       | `bun test dig.oom.smoke.test.ts dig.test.ts`       | 5 pass / 0 fail | ✓ PASS |
| Regressão zero (suíte inteira)                    | `bun test`                                          | 165 pass / 0 fail (21 files) | ✓ PASS |
| Commits da fase presentes                         | `git log` (8 commits de código + docs)             | todos presentes | ✓ PASS |

A contagem 165/165 confirma exatamente a alegação do SUMMARY do Plano 05.

### Requirements Coverage

Fase de backlog sem REQ-IDs canônicos em REQUIREMENTS.md. Cobertura aferida contra as decisões D-01..D-07 do CONTEXT.md.

| Requirement | Source Plan         | Description                                              | Status      | Evidence |
| ----------- | ------------------- | -------------------------------------------------------- | ----------- | -------- |
| D-01        | 999.1-01, 999.1-03  | GATHER_SEARCH_RADIUS independente de percepção           | ✓ SATISFIED | config.ts + dig.ts |
| D-02        | 999.1-01            | Bounds do A* via env + validação                         | ✓ SATISFIED | config.ts:22-23,144-149 |
| D-03        | 999.1-02            | Bounds nos dois lugares pós loadPlugin                   | ✓ SATISFIED | connection.ts:48-61 |
| D-04        | 999.1-03            | Pré-check getPathTo alimenta safety.ts existente         | ✓ SATISFIED | dig.ts:52-61 + nodes.ts catch |
| D-05        | 999.1-03            | Granularidade de instância; cooldown por-tipo só se 0 alcançáveis | ✓ SATISFIED | dig.ts:52-61 (filtro por instância, throw único) |
| D-06        | 999.1-04            | Double-wrap removido; auto-embrulho por skill            | ✓ SATISFIED | nodes.ts:210; navigate/dig auto-wrap; stubs documentados |
| D-07        | 999.1-05            | Critério de aceitação tripé (sem OOM, rejeita, lag<200)  | ✓ SATISFIED | dig.oom.smoke.test.ts (2 testes verdes) |

### Anti-Patterns Found

| File                    | Line  | Pattern                              | Severity | Impact |
| ----------------------- | ----- | ------------------------------------ | -------- | ------ |
| `src/skills/follow.ts`  | 21-24 | Stub que lança "não implementada"    | ℹ️ Info  | Intencional (D-12 de fase anterior), fora do escopo de 999.1, documentado como não-embrulhado por design (D-06). Não bloqueia o goal. |
| `src/skills/attack.ts`  | 18-21 | Stub que lança "não implementada"    | ℹ️ Info  | Idem follow.ts. |
| `dig.ts:54`             | 54    | `(bot.pathfinder as any).getPathTo`  | ℹ️ Info  | Cast pontual intencional — tipos do mineflayer-pathfinder não expõem getPathTo; documentado no plano (Claude's discretion). |
| `connection.ts`         | 47-58 | `@ts-expect-error` em searchRadius/thinkTimeout | ℹ️ Info | Props existem em runtime, não tipadas; cast pontual preserva o texto literal exigido pelo key_link. Não mascara outros erros. |

Nenhum blocker ou warning. Os stubs follow/attack são pré-existentes e fora do escopo; os casts são pontuais e documentados.

### Human Verification Required

Nenhum item bloqueante para verificação humana. A prova do tripé D-07 é automatizada (smoke headless) consistente com o estilo da verificação das Fases 2/3.

**Opcional (validação operacional ao vivo, não bloqueante):** rodar o agente contra um servidor MC real com `PERCEPTION_RADIUS=32` em terreno com alvos genuinamente inalcançáveis, e observar via `process.memoryUsage()` que a heap permanece estável durante Gathering prolongado. O smoke já prova o comportamento sob mock fiel (#222 + noPath), mas a confirmação em mundo real fecha o todo de origem `gathering-collectblock-oom`.

### Gaps Summary

Nenhuma lacuna. Todas as 7 verdades observáveis foram verificadas contra o código real (não apenas SUMMARYs):

- A raiz do OOM está corrigida em dois níveis independentes: (a) `searchRadius` finito (48) reativa o gate `maxCost` do A* nos dois lugares que a coleta usa (D-02/D-03), e (b) o pré-check `getPathTo` evita disparar `collect()`/A* contra alvos inalcançáveis (D-04/D-05), eliminando também o hang #222.
- A separação percepção (32) vs. busca de coleta (16) está implementada e desacoplada (D-01).
- O double-wrap foi removido, garantindo que cada skill tem o watchdog correto (D-06).
- O tripé de aceitação D-07 está provado por teste automatizado verde (sem OOM via teto de heap, rejeição dentro de digTimeoutMs, lag<200ms via heartbeat), com a suíte inteira em 165/165 e tsc limpo — regressão zero.
- O workaround `PERCEPTION_RADIUS=8` foi restaurado para 32 em `.env` e `.env.example` (Folded Todo satisfeito).

---

_Verified: 2026-06-19T18:05:00Z_
_Verifier: Claude (gsd-verifier)_
