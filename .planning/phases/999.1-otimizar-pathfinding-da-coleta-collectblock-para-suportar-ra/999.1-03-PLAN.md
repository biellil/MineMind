---
phase: 999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra
plan: 03
type: execute
wave: 2
depends_on: ["999.1-01"]
files_modified:
  - src/skills/dig.ts
autonomous: true
requirements: [D-01, D-04, D-05]
must_haves:
  truths:
    - "dig.ts usa config.gatherSearchRadius (não config.perceptionRadius) no findBlocks({ maxDistance })"
    - "antes de collect(), um pré-check síncrono getPathTo descarta candidatos inalcançáveis sem mover o bot"
    - "se NENHUMA instância do tipo é alcançável, dig lança Error (alimenta recordFailure/cooldown por-tipo existente)"
    - "se ao menos UMA instância é alcançável, collect() é chamado apenas com as instâncias alcançáveis"
  artifacts:
    - path: "src/skills/dig.ts"
      provides: "findBlocks com gatherSearchRadius + pré-check de alcançabilidade via getPathTo"
      contains: "config.gatherSearchRadius"
  key_links:
    - from: "bot.pathfinder.getPathTo(movements, GoalGetToBlock, timeout)"
      to: "filtro de blocos alcançáveis antes de collect()"
      via: "checagem de status !== 'noPath'/'timeout'"
      pattern: "getPathTo"
---

<objective>
Trocar o raio de busca de coleta em `dig.ts` de `perceptionRadius` para `gatherSearchRadius` (D-01),
e adicionar um pré-check de alcançabilidade barato e síncrono (`getPathTo`) ANTES de `collect()` (D-04),
filtrando candidatos inalcançáveis na granularidade de instância (D-05). O resultado alimenta a maquinaria
de segurança EXISTENTE (`recordFailure`/cooldown por-tipo em safety.ts via o catch do nó execute) — sem
inventar um caminho de "pular silencioso".

Purpose: Evitar que `collect()` dispare o A* contra alvos inalcançáveis (causa do OOM e do hang #222 do
collectBlock que trava sem lançar erro). `getPathTo` é síncrono, não move o bot, e retorna status
'success'|'partial'|'noPath'|'timeout' — base do filtro barato.
Output: `dig.ts` coleta apenas instâncias alcançáveis; quando nenhuma é alcançável, lança Error que o nó
execute converte em recordFailure → cooldown por-tipo (15s).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-CONTEXT.md

<interfaces>
<!-- Fatos de API (CONTEXT.md <root_cause>) — NÃO contradizer: -->
<!-- - bot.pathfinder.getPathTo(movements, goal, timeout) é SÍNCRONO, não move o bot, retorna { status: 'success'|'partial'|'noPath'|'timeout', ... }. -->
<!-- - GoalGetToBlock(x,y,z) é o goal de "chegar adjacente a um bloco" do mineflayer-pathfinder. -->
<!-- - collectBlock pode TRAVAR sem lançar em obstrução inquebrável (issue #222) — por isso o pré-check. -->

Estado atual de dig.ts §32-54 (caminho `typeof target === 'string'`):
```typescript
const blocks = bot.findBlocks({
  matching: (b) => b.name === target,
  maxDistance: config.perceptionRadius,   // <- trocar para config.gatherSearchRadius (D-01)
  count,
}).map((pos) => bot.blockAt(pos)).filter((b): b is NonNullable<typeof b> => b !== null && b.type !== 0)

if (blocks.length === 0) {
  throw new Error(`Bloco do tipo '${target}' não encontrado no raio de ${config.perceptionRadius} blocos`)
}

await executeWithSafety(() => bot.collectBlock.collect(blocks), { /* timeout/watchdog */ })
```

Maquinaria de segurança a alimentar (NÃO modificar safety.ts neste plano):
- nodes.ts §205-216: o catch do nó execute já chama `recordFailure(safety, target, now())` quando a skill
  lança. target = nome do bloco → cooldown por-tipo de 15s (chave = nome). É EXATAMENTE o comportamento
  desejado por D-05 quando nenhuma instância é alcançável. Basta dig lançar Error.

config disponível (Plano 01): config.gatherSearchRadius (16), config.pathfinderThinkTimeoutMs (2000).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trocar raio para gatherSearchRadius + pré-check de alcançabilidade getPathTo (D-01, D-04, D-05)</name>
  <read_first>
    - src/skills/dig.ts (arquivo inteiro — caminho string §32-54 e caminho XYZ §55-73; só o caminho string muda)
    - src/config.ts (confirmar config.gatherSearchRadius existe — Plano 01)
    - src/cognition/nodes.ts §204-216 (ver como o catch do execute chama recordFailure — o Error de dig alimenta isso)
    - src/cognition/safety.ts §35-39 (recordFailure: cooldown por-tipo, chave = target/nome do bloco — NÃO mudar)
    - .planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-CONTEXT.md (D-01, D-04, D-05)
  </read_first>
  <files>src/skills/dig.ts</files>
  <behavior>
    - Comportamento 1 (D-01): findBlocks usa maxDistance = config.gatherSearchRadius (não perceptionRadius).
    - Comportamento 2 (D-05): dentre N candidatos do tipo, o pré-check separa os alcançáveis (getPathTo status
      'success' ou 'partial') dos inalcançáveis ('noPath'/'timeout'). collect() recebe SÓ os alcançáveis.
    - Comportamento 3 (D-04/D-05): se findBlocks retorna 0 candidatos OU 0 alcançáveis, dig lança Error
      (mensagem distinta para cada caso). Nenhum collect() é chamado nesse caso.
    - Comportamento 4: o pré-check é síncrono e NÃO move o bot (usa getPathTo, não goto/collect).
  </behavior>
  <action>
    Modificar SOMENTE o ramo `typeof target === 'string'` de dig.ts (§32-54). O ramo XYZ (§55-73) fica intocado.

    1. Importar o goal e o tipo de Movements no topo do arquivo:
       ```typescript
       import { goals } from 'mineflayer-pathfinder'
       ```
       (usaremos `goals.GoalGetToBlock`). Se o tipo de `bot.pathfinder.getPathTo` não estiver disponível,
       usar cast pontual `(bot.pathfinder as any)` apenas na chamada de getPathTo.

    2. Trocar `maxDistance: config.perceptionRadius` por `maxDistance: config.gatherSearchRadius` (D-01)
       e atualizar a mensagem de erro "não encontrado no raio de ${config.gatherSearchRadius} blocos".

    3. Após montar `blocks` e o early-throw de `blocks.length === 0`, adicionar o pré-check (D-04/D-05):
       ```typescript
       // 999.1 D-04/D-05: pré-check de alcançabilidade SÍNCRONO antes de collect().
       // getPathTo NÃO move o bot e evita o hang #222 do collectBlock em alvos inalcançáveis.
       // Granularidade de INSTÂNCIA: filtra os blocos alcançáveis; o cooldown por-TIPO (safety.ts)
       // só dispara quando NENHUMA instância é alcançável (este throw alimenta o catch do nó execute).
       const movements = bot.collectBlock?.movements ?? bot.pathfinder.movements
       const precheckTimeoutMs = 200  // Claude's discretion: barato (poucas iterações), só para descartar inalcançáveis
       const reachable = blocks.filter((b) => {
         const goal = new goals.GoalGetToBlock(b.position.x, b.position.y, b.position.z)
         const result = (bot.pathfinder as any).getPathTo(movements, goal, precheckTimeoutMs)
         // 'success'/'partial' = alcançável o suficiente para tentar; 'noPath'/'timeout' = descartar
         return result?.status === 'success' || result?.status === 'partial'
       })

       if (reachable.length === 0) {
         throw new Error(`Nenhuma instância alcançável de '${target}' (todas inalcançáveis no raio de ${config.gatherSearchRadius})`)
       }
       ```

    4. Trocar a chamada de coleta para usar `reachable` em vez de `blocks`:
       ```typescript
       await executeWithSafety(
         () => bot.collectBlock.collect(reachable),
         {
           timeoutMs: config.digTimeoutMs * count,
           progressChecker: () => bot.inventory.items().reduce((sum, item) => sum + item.count, 0),
           progressIntervalMs: 2_000,
           noProgressToleranceMs: config.digTimeoutMs,
         }
       )
       ```

    Manter o `count` e o `progressChecker` de inventário existentes. NÃO tocar no caminho XYZ. NÃO modificar
    safety.ts nem nodes.ts (o throw já é capturado lá e vira recordFailure → cooldown por-tipo).
  </action>
  <verify>
    <automated>cd c:/Users/biel1/OneDrive/Documentos/GitHub/MineMind; bun run tsc --noEmit; bun test src/skills/dig.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "maxDistance: config.gatherSearchRadius" src/skills/dig.ts` retorna match
    - `grep "config.perceptionRadius" src/skills/dig.ts` NÃO retorna match (raio de coleta totalmente migrado)
    - `grep "getPathTo" src/skills/dig.ts` retorna match
    - `grep "GoalGetToBlock" src/skills/dig.ts` retorna match
    - `grep "Nenhuma instância alcançável" src/skills/dig.ts` retorna match
    - `grep "bot.collectBlock.collect(reachable)" src/skills/dig.ts` retorna match
    - `bun run tsc --noEmit` sai 0
    - `bun test src/skills/dig.test.ts` passa (teste criado em Task 2)
  </acceptance_criteria>
  <done>dig.ts usa gatherSearchRadius, faz pré-check getPathTo por instância, coleta só alcançáveis e lança Error quando nenhuma é alcançável; perceptionRadius não aparece mais; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Teste unitário do caminho de coleta com bot mockado (pré-check + filtro)</name>
  <read_first>
    - src/skills/dig.ts (assinatura `dig(bot, rawParams)` e DigSchema)
    - src/skills/executor.test.ts (estilo de teste com bun:test + mock de bot)
    - src/cognition/loop.smoke.test.ts §21-40 (padrão de makeMockBot — replicar campos só do que dig usa)
  </read_first>
  <files>src/skills/dig.test.ts</files>
  <action>
    Criar src/skills/dig.test.ts usando `bun:test`. Mockar um `bot` mínimo com os campos que o caminho string
    de dig usa: `findBlocks`, `blockAt`, `inventory.items`, `collectBlock.collect`, `collectBlock.movements`,
    `pathfinder.getPathTo`, `pathfinder.movements`. Casos:

    1. **Todos inalcançáveis → throw**: `findBlocks` retorna 2 posições, `blockAt` devolve blocos válidos
       (name = target, type != 0), mas `getPathTo` retorna `{ status: 'noPath' }` para todos. Esperar que
       `dig(bot, { target: 'oak_log', count: 1 })` rejeite com Error cuja mensagem contém "Nenhuma instância alcançável".
       Asserir que `collectBlock.collect` NÃO foi chamado.

    2. **Ao menos um alcançável → collect chamado**: `getPathTo` retorna `{ status: 'success' }` para o primeiro
       bloco e `{ status: 'noPath' }` para o segundo. `collectBlock.collect` resolve. Esperar que `dig` resolva e
       que `collect` tenha sido chamado com um array de tamanho 1 (só o alcançável).

    3. **Nenhum candidato → throw "não encontrado"**: `findBlocks` retorna `[]`. Esperar rejeição com mensagem
       contendo "não encontrado".

    Notas:
    - `executeWithSafety` aplica delays gaussianos (~300ms+200ms) — usar timeout de teste ≥ 5000ms por caso.
    - `blockAt(pos)` deve devolver objeto com `{ name, type, position: pos }` para os blocos válidos.
    - Mockar `getPathTo` como função síncrona que inspeciona a posição do goal para decidir o status, OU
      retornar status em sequência via contador — desde que o caso 2 distinga primeiro/segundo bloco.
  </action>
  <verify>
    <automated>cd c:/Users/biel1/OneDrive/Documentos/GitHub/MineMind; bun test src/skills/dig.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/skills/dig.test.ts` existe
    - `grep "Nenhuma instância alcançável" src/skills/dig.test.ts` retorna match (caso 1 asserido)
    - `grep "collect" src/skills/dig.test.ts` retorna match (asserção de chamada/não-chamada de collect)
    - `bun test src/skills/dig.test.ts` sai 0 com 3 testes passando
  </acceptance_criteria>
  <done>3 testes cobrem: todos inalcançáveis (throw, sem collect), ao menos um alcançável (collect só com alcançáveis), nenhum candidato (throw); todos passam.</done>
</task>

</tasks>

<verification>
- `bun run tsc --noEmit` limpo
- `bun test src/skills/dig.test.ts` passa (3 testes)
- dig usa gatherSearchRadius; pré-check filtra por instância; throw alimenta o cooldown por-tipo existente
</verification>

<success_criteria>
A coleta nunca dispara collect()/A* contra alvos inalcançáveis; quando nenhuma instância do tipo é
alcançável, o erro propaga para o cooldown por-tipo (15s) já existente em safety.ts. Verificação integrada
sem-OOM/lag ocorre no Plano 05 (D-07).
</success_criteria>

<output>
Após completar, criar `.planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-03-SUMMARY.md`
</output>
