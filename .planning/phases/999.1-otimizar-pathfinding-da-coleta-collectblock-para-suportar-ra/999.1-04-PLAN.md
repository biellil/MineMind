---
phase: 999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cognition/nodes.ts
  - src/skills/navigate.ts
  - src/skills/follow.ts
  - src/skills/attack.ts
autonomous: true
requirements: [D-06]
must_haves:
  truths:
    - "O nó execute em nodes.ts chama a skill diretamente, SEM o executeWithSafety externo (double-wrap removido)"
    - "Toda skill registrada se auto-embrulha em executeWithSafety com seu próprio progressChecker"
    - "Nenhuma skill fica sem timeout/watchdog após a remoção do wrap externo"
  artifacts:
    - path: "src/cognition/nodes.ts"
      provides: "Nó execute sem o wrap externo de executeWithSafety"
      contains: "skillRegistry"
  key_links:
    - from: "nó execute (nodes.ts)"
      to: "skill auto-embrulhada em executeWithSafety"
      via: "chamada direta skillRegistry[skill](bot, params)"
      pattern: "skillRegistry\\[skill"
---

<objective>
Resolver o double-wrap de `executeWithSafety` (D-06): hoje o nó `execute` (nodes.ts §207) embrulha a skill
em `executeWithSafety`, mas `dig.ts` JÁ se auto-embrulha com seu `progressChecker` específico (inventário).
O wrap externo usa defaults genéricos e mascara/duplica o watchdog interno. Regra final: TODA skill
registrada se auto-embrulha; o nó execute chama a skill diretamente.

Purpose: Garantir que cada skill tenha seu watchdog correto (o checker certo para o seu progresso) e
remover a camada externa redundante. Pré-condição para o gate "loop nunca trava" de D-07.
Output: nodes.ts chama skills sem wrap externo; navigate/follow/attack auto-embrulhadas (se ainda não estiverem).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-CONTEXT.md

<interfaces>
<!-- Estado atual do nó execute (nodes.ts §204-217): -->
```typescript
// D-02: single-flight — UMA skill, aguardada. executeWithSafety já faz timeout/watchdog.
try {
  const params = skill === 'dig' ? { target } : { target: JSON.parse(target) }
  await executeWithSafety(() => skillRegistry[skill!]!(bot, params))  // <- WRAP EXTERNO a remover (D-06)
  recordSuccess(safety)
  holder.memory = push(holder.memory, { type: 'action', skill, target, result: 'success', timestamp: now() })
  ...
```
<!-- dig.ts JÁ se auto-embrulha (§45 e §63) com executeWithSafety + progressChecker próprio. -->
<!-- executeWithSafety (executor.ts): timeoutMs default 30000, noProgressToleranceMs default 10000. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar/auto-embrulhar navigate, follow, attack em executeWithSafety</name>
  <read_first>
    - src/skills/dig.ts (referência do padrão de auto-embrulho: executeWithSafety com progressChecker próprio)
    - src/skills/navigate.ts (verificar se já chama executeWithSafety internamente)
    - src/skills/follow.ts (idem)
    - src/skills/attack.ts (idem — pode ser stub)
    - src/skills/executor.ts (assinatura de executeWithSafety e ExecuteOptions)
  </read_first>
  <files>src/skills/navigate.ts, src/skills/follow.ts, src/skills/attack.ts</files>
  <action>
    Para CADA skill registrada (navigate, follow, attack), garantir que a própria função se auto-embrulhe em
    `executeWithSafety` (D-06 regra: "toda skill registrada se auto-embrulha"). Para cada arquivo:

    1. Ler a skill. Se ela JÁ chama `executeWithSafety(...)` internamente, deixar como está (apenas confirmar
       que usa um timeout/checker sensato — navigate deve usar `config.navigateTimeoutMs`).
    2. Se NÃO se auto-embrulha (chama o pathfinder/bot diretamente sem executeWithSafety), envolver a operação
       async principal em:
       ```typescript
       await executeWithSafety(
         () => /* operação original (ex: bot.pathfinder.goto(goal)) */,
         {
           timeoutMs: config.navigateTimeoutMs,  // navigate; follow/attack: escolher timeout sensato existente
           // progressChecker apropriado se houver sinal de progresso (ex: distância ao alvo diminuindo);
           // se não houver sinal claro, omitir progressChecker (só timeout) — NUNCA deixar sem timeout.
         }
       )
       ```
    3. Para `attack.ts`, se for um stub que apenas lança "não implementado" ou resolve imediato, NÃO adicionar
       executeWithSafety (não há operação longa a proteger) — apenas documentar com comentário que o stub é
       intencionalmente não-embrulhado.

    Importar `executeWithSafety` de `./executor` e `config` de `../config` se ainda não importados.
    NÃO alterar a assinatura pública das skills nem o índice de registro.
  </action>
  <verify>
    <automated>cd c:/Users/biel1/OneDrive/Documentos/GitHub/MineMind; bun run tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "executeWithSafety" src/skills/navigate.ts` retorna o arquivo (navigate auto-embrulhada)
    - navigate.ts usa `config.navigateTimeoutMs` no executeWithSafety (`grep "navigateTimeoutMs" src/skills/navigate.ts` retorna match)
    - follow.ts e attack.ts: ou contêm `executeWithSafety`, ou um comentário explícito de stub não-embrulhado
    - `bun run tsc --noEmit` sai 0
  </acceptance_criteria>
  <done>navigate/follow auto-embrulham em executeWithSafety com timeout próprio; attack documentado como stub se aplicável; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Remover o wrap externo de executeWithSafety no nó execute (D-06)</name>
  <read_first>
    - src/cognition/nodes.ts §204-217 (bloco try/catch do nó execute com o wrap externo)
    - src/skills/dig.ts §45, §63 (confirmar auto-embrulho — justifica remover o externo)
    - .planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-CONTEXT.md (D-06)
  </read_first>
  <files>src/cognition/nodes.ts</files>
  <action>
    Em src/cognition/nodes.ts, no nó `execute`, trocar a chamada com wrap externo:
    ```typescript
    await executeWithSafety(() => skillRegistry[skill!]!(bot, params))
    ```
    por chamada DIRETA da skill (a skill já se auto-embrulha — D-06):
    ```typescript
    // 999.1 D-06: sem wrap externo — cada skill se auto-embrulha em executeWithSafety com seu
    // próprio progressChecker (dig usa inventário; navigate usa navigateTimeoutMs). O wrap externo
    // usava defaults genéricos e duplicava o watchdog interno.
    await skillRegistry[skill!]!(bot, params)
    ```
    Atualizar o comentário §204 ("executeWithSafety já faz timeout/watchdog") para refletir que isso agora
    acontece DENTRO de cada skill. Se `executeWithSafety` deixar de ser usado em nodes.ts, remover o import
    correspondente para evitar warning de import não-usado (verificar com tsc/lint). Manter o try/catch e os
    `recordSuccess`/`recordFailure` exatamente como estão.
  </action>
  <verify>
    <automated>cd c:/Users/biel1/OneDrive/Documentos/GitHub/MineMind; bun run tsc --noEmit; bun test src/cognition/loop.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "executeWithSafety(() => skillRegistry" src/cognition/nodes.ts` NÃO retorna match (wrap externo removido)
    - `grep "await skillRegistry\[skill" src/cognition/nodes.ts` retorna match (chamada direta)
    - `recordSuccess` e `recordFailure` ainda presentes no bloco try/catch do execute
    - `bun run tsc --noEmit` sai 0 (sem import não-usado)
    - `bun test src/cognition/loop.smoke.test.ts` passa (loop ainda roda e grava ações)
  </acceptance_criteria>
  <done>O nó execute chama a skill diretamente sem wrap externo; try/catch e safety preservados; smoke do loop passa.</done>
</task>

</tasks>

<verification>
- `bun run tsc --noEmit` limpo
- `bun test src/cognition/loop.smoke.test.ts` passa
- Toda skill registrada se auto-embrulha; nó execute sem wrap externo
</verification>

<success_criteria>
Não há mais double-wrap: cada skill tem seu watchdog correto e o nó execute apenas a chama. O loop continua
gravando sucesso/falha via safety. Pré-condição satisfeita para o gate "loop nunca trava" de D-07 (Plano 05).
</success_criteria>

<output>
Após completar, criar `.planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-04-SUMMARY.md`
</output>
