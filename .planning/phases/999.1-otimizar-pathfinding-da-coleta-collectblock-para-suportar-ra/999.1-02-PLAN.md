---
phase: 999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra
plan: 02
type: execute
wave: 2
depends_on: ["999.1-01"]
files_modified:
  - src/bot/connection.ts
autonomous: true
requirements: [D-03]
must_haves:
  truths:
    - "Após loadPlugin(collectBlock), os globais bot.pathfinder.searchRadius e bot.pathfinder.thinkTimeout recebem os valores da config"
    - "A instância bot.collectBlock.movements (recriada internamente pelo plugin) recebe os mesmos bounds"
    - "O gate maxCost do A* fica ativo (searchRadius != -1) para QUALQUER pathfinding disparado pelo collectBlock"
  artifacts:
    - path: "src/bot/connection.ts"
      provides: "Aplicação dos bounds do A* nos dois lugares (globais + collectBlock.movements)"
      contains: "bot.pathfinder.searchRadius"
  key_links:
    - from: "config.pathfinderSearchRadius"
      to: "bot.pathfinder.searchRadius e bot.collectBlock.movements"
      via: "atribuição após loadPlugin(collectBlock)"
      pattern: "bot.pathfinder.searchRadius\\s*=\\s*config.pathfinderSearchRadius"
---

<objective>
Aplicar os bounds do A* (D-03) nos DOIS lugares que o pathfinding da coleta usa: os globais
`bot.pathfinder.*` E a instância `bot.collectBlock.movements`. Esta é a correção da RAIZ do OOM:
`searchRadius = -1` (default ilimitado) desativa o gate `maxCost` do A*, e a heap cresce sem teto em
alvos distantes/inalcançáveis. Setar `searchRadius` (default 48) reativa o gate.

Purpose: Limitar a memória do A* na fonte, para QUALQUER caminho de pathfinding — incluindo o `Movements`
que o `collectBlock` recria internamente e que ignora o `Movements` passado a `setMovements`.
Output: `connection.ts` aplica searchRadius + thinkTimeout nos dois alvos, após `loadPlugin(collectBlock)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-CONTEXT.md

<interfaces>
<!-- Fatos de API confirmados no código (CONTEXT.md <root_cause>) — NÃO contradizer: -->
<!-- - Movements.maxIterations NÃO existe. Levers reais: bot.pathfinder.searchRadius, bot.pathfinder.thinkTimeout (5000ms default), tickTimeout (40ms). -->
<!-- - collectBlock.collect() cria a PRÓPRIA instância de Movements (new Movements(bot)) e usa os globais bot.pathfinder.*. -->
<!-- - bot.collectBlock.movements existe APÓS loadPlugin(collectBlock) — é o Movements que o plugin usa. -->

Estado atual de connection.ts §28-37 (dentro de bot.once('spawn')):
```typescript
bot.loadPlugin(pathfinder)
bot.loadPlugin(collectBlock)
const movements = new Movements(bot)
movements.canDig = true
movements.allowSprinting = true
bot.pathfinder.setMovements(movements)
```
Config disponível (do Plano 01): config.pathfinderSearchRadius (48), config.pathfinderThinkTimeoutMs (2000).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aplicar bounds do A* nos globais E em bot.collectBlock.movements (D-03)</name>
  <read_first>
    - src/bot/connection.ts (ver bloco bot.once('spawn') §28-55 — onde loadPlugin + Movements rodam)
    - src/config.ts (confirmar que config.pathfinderSearchRadius e config.pathfinderThinkTimeoutMs existem — criados no Plano 01)
    - .planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-CONTEXT.md (D-02, D-03 e <root_cause> sobre searchRadius=-1)
  </read_first>
  <files>src/bot/connection.ts</files>
  <action>
    Em src/bot/connection.ts, dentro de `bot.once('spawn', ...)`, APÓS `bot.pathfinder.setMovements(movements)`
    (§37) e APÓS ambos os `loadPlugin`, adicionar a aplicação dos bounds nos DOIS lugares:

    ```typescript
    // 999.1 D-02/D-03: bounds do A* — raiz do fix de OOM.
    // searchRadius = -1 (default) desativa o gate maxCost do A* → heap cresce sem teto em alvos
    // distantes. Setar searchRadius reativa o gate. thinkTimeout é a rede temporal secundária.
    // Aplicar em DOIS lugares: (1) os globais bot.pathfinder.* usados por todo pathfinding;
    // (2) bot.collectBlock.movements — instância que o collectBlock RECRIA internamente e que
    //     IGNORA o Movements passado a setMovements acima.
    bot.pathfinder.searchRadius = config.pathfinderSearchRadius
    bot.pathfinder.thinkTimeout = config.pathfinderThinkTimeoutMs
    // Aplicar os mesmos bounds + flags de navegação ao Movements do collectBlock.
    movements.searchRadius = config.pathfinderSearchRadius
    movements.thinkTimeout = config.pathfinderThinkTimeoutMs
    if (bot.collectBlock?.movements) {
      bot.collectBlock.movements.searchRadius = config.pathfinderSearchRadius
      bot.collectBlock.movements.thinkTimeout = config.pathfinderThinkTimeoutMs
      bot.collectBlock.movements.canDig = true
      bot.collectBlock.movements.allowSprinting = true
    }
    ```

    Notas:
    - `searchRadius`/`thinkTimeout` são propriedades de `bot.pathfinder` (globais) e do objeto `Movements`.
      Setar nos dois cobre tanto o pathfinder global quanto o Movements interno do collectBlock.
    - Manter `canDig`/`allowSprinting` no Movements do collectBlock para coerência com o Movements principal.
    - NÃO remover nem alterar o `new Movements(bot)`/`setMovements` existente.
    - Se o TypeScript reclamar que `searchRadius`/`thinkTimeout` não existem em `Movements` (tipos do
      mineflayer-pathfinder), usar cast pontual `(movements as any)` apenas nessas atribuições — os globais
      `bot.pathfinder.searchRadius`/`thinkTimeout` são tipados.
  </action>
  <verify>
    <automated>cd c:/Users/biel1/OneDrive/Documentos/GitHub/MineMind; bun run tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "bot.pathfinder.searchRadius = config.pathfinderSearchRadius" src/bot/connection.ts` retorna match
    - `grep "bot.pathfinder.thinkTimeout = config.pathfinderThinkTimeoutMs" src/bot/connection.ts` retorna match
    - `grep "bot.collectBlock" src/bot/connection.ts` retorna match com atribuição de searchRadius
    - As atribuições aparecem APÓS `bot.loadPlugin(collectBlock)` no arquivo (verificar ordem por número de linha)
    - `bun run tsc --noEmit` sai 0
  </acceptance_criteria>
  <done>Os bounds (searchRadius=48, thinkTimeout=2000) são aplicados nos globais bot.pathfinder.* E em bot.collectBlock.movements após loadPlugin; tsc limpo.</done>
</task>

</tasks>

<verification>
- `bun run tsc --noEmit` limpo
- Bounds aplicados em ambos os lugares após loadPlugin(collectBlock)
- gate maxCost do A* ativo (searchRadius finito, não -1)
</verification>

<success_criteria>
QUALQUER pathfinding disparado pela coleta (incluindo o Movements interno do collectBlock) respeita
searchRadius=48 e thinkTimeout=2000ms, eliminando o crescimento ilimitado da heap. Verificação final do
comportamento sem-OOM ocorre no Plano 05 (D-07).
</success_criteria>

<output>
Após completar, criar `.planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-02-SUMMARY.md`
</output>
