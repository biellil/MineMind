---
phase: 999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra
plan: 05
type: execute
wave: 3
depends_on: ["999.1-01", "999.1-02", "999.1-03", "999.1-04"]
files_modified:
  - src/skills/dig.oom.smoke.test.ts
autonomous: true
requirements: [D-07]
must_haves:
  truths:
    - "Com PERCEPTION_RADIUS=32, coletar um alvo inalcançável completa SEM matar o processo (heap abaixo de um teto)"
    - "A skill rejeita (SkillTimeoutError/SkillStuckError ou noPath/timeout do pathfinder) dentro de digTimeoutMs"
    - "Um heartbeat (setInterval ~50ms) mede lag e acusa lag < 200ms durante toda a execução da skill candidata a bloqueio"
  artifacts:
    - path: "src/skills/dig.oom.smoke.test.ts"
      provides: "Smoke headless multi-tick provando o tripé de D-07 (sem OOM, rejeita, lag<200ms)"
      contains: "heapUsed"
  key_links:
    - from: "heartbeat setInterval ~50ms"
      to: "asserção de lag < 200ms durante a skill"
      via: "medição de drift do timer"
      pattern: "setInterval"
---

<objective>
Provar, num smoke test headless multi-tick (estilo da verificação da Fase 2), que o fix funciona — os TRÊS
critérios de D-07 simultaneamente, com `PERCEPTION_RADIUS=32`:
  1. **Sem OOM** — gathering de alvo inalcançável completa sem matar o processo (heapUsed abaixo de um teto).
  2. **Timeout/noPath dispara** — a skill rejeita (SkillTimeoutError/SkillStuckError ou noPath/timeout) dentro de digTimeoutMs.
  3. **Event loop responde** — heartbeat (setInterval ~50ms) mede lag < 200ms durante toda a skill candidata a bloqueio.
O ponto 3 é o GATE PRINCIPAL da propriedade "o loop cognitivo nunca trava".

Purpose: Verificação de aceitação da fase — sem ela, não há prova de que o OOM foi resolvido nem de que o
loop permanece responsivo.
Output: `src/skills/dig.oom.smoke.test.ts` (bun:test) com asserções para os 3 pontos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-CONTEXT.md

<interfaces>
<!-- Estilo de smoke headless a ecoar (loop.smoke.test.ts): bun:test, makeMockBot mínimo, sem servidor MC. -->
<!-- Fatos (CONTEXT.md): getPathTo é síncrono; collectBlock pode travar sem lançar (#222). -->
<!-- dig.ts (Plano 03) agora: findBlocks(gatherSearchRadius) → getPathTo por instância → collect só alcançáveis, -->
<!--   ou throw "Nenhuma instância alcançável" se todas noPath. -->
<!-- executor.ts: SkillTimeoutError / SkillStuckError; executeWithSafety usa Promise.race. -->

API de memória: `process.memoryUsage().heapUsed` (bytes).
Padrão de heartbeat (Claude's discretion — D-07 ponto 3):
```typescript
let maxLag = 0
let expected = Date.now() + 50
const hb = setInterval(() => {
  const now = Date.now()
  const lag = now - expected
  if (lag > maxLag) maxLag = lag
  expected = now + 50
}, 50)
// ... rodar a skill candidata a bloqueio ...
clearInterval(hb)
expect(maxLag).toBeLessThan(200)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke headless do tripé de D-07 (sem OOM + rejeita + lag<200ms)</name>
  <read_first>
    - src/cognition/loop.smoke.test.ts (estilo do smoke headless: bun:test, makeMockBot, timeouts generosos)
    - src/skills/dig.ts (caminho string pós-Plano 03: findBlocks → getPathTo → collect/throw)
    - src/skills/executor.ts (SkillTimeoutError, SkillStuckError, executeWithSafety)
    - src/config.ts (config.digTimeoutMs, config.gatherSearchRadius)
    - .planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-CONTEXT.md (D-07 — os 3 pontos exatos)
  </read_first>
  <files>src/skills/dig.oom.smoke.test.ts</files>
  <action>
    Criar src/skills/dig.oom.smoke.test.ts com `bun:test`. Definir `process.env.PERCEPTION_RADIUS = '32'` no
    topo (antes de qualquer import de config, se necessário usar import dinâmico) para honrar D-07. Mockar um
    `bot` mínimo que simule um alvo INALCANÇÁVEL e a possibilidade de hang do collectBlock (#222):

    - `findBlocks`: retorna N posições (ex: 3) longe do bot.
    - `blockAt(pos)`: retorna `{ name: 'oak_log', type: 17, position: pos }`.
    - `pathfinder.getPathTo(movements, goal, timeout)`: SÍNCRONO, retorna `{ status: 'noPath' }` para todas as
      posições (alvo inalcançável). Como a função é síncrona e barata, NÃO deve gerar lag perceptível.
    - `pathfinder.movements` e `collectBlock.movements`: objetos placeholder `{}`.
    - `collectBlock.collect`: NÃO deve ser chamado neste cenário (todas inalcançáveis → dig lança antes).
      Ainda assim, mockar como uma função que, se chamada, simula o hang #222 (Promise que nunca resolve) —
      para que o teste FALHE explicitamente se o pré-check não filtrar. O timeout do executeWithSafety/digTimeoutMs
      seria a rede de segurança nesse caso.
    - `inventory.items()`: retorna `[]`.

    Escrever DOIS testes:

    **Teste A — alvo inalcançável: rejeita + sem OOM + lag<200ms (tripé D-07):**
    1. Iniciar o heartbeat (setInterval ~50ms medindo maxLag) e capturar `heapBefore = process.memoryUsage().heapUsed`.
    2. Chamar `await expect(dig(bot, { target: 'oak_log', count: 1 })).rejects.toThrow()` — a skill DEVE rejeitar
       (mensagem "Nenhuma instância alcançável" do pré-check, dentro de digTimeoutMs). [Ponto 2 de D-07]
    3. Parar o heartbeat. Capturar `heapAfter`. Asserir:
       - `expect(maxLag).toBeLessThan(200)` — event loop respondeu. [Ponto 3 — GATE PRINCIPAL]
       - `expect(heapAfter - heapBefore).toBeLessThan(<teto>)` — sem crescimento explosivo de heap. Usar um teto
         generoso porém finito (ex: 200 * 1024 * 1024 = 200MB) que jamais seria respeitado se o A* explodisse a
         heap (~78GB). [Ponto 1 de D-07]
       - Asserir que `collectBlock.collect` NÃO foi chamado (pré-check filtrou tudo).
    4. Timeout do teste: `config.digTimeoutMs + 5000` para folga.

    **Teste B — rede de segurança temporal quando collect "trava" (#222):** simular um candidato que o pré-check
    considera alcançável (`getPathTo` → `{ status: 'success' }`) mas cujo `collectBlock.collect` retorna uma
    Promise que NUNCA resolve (hang #222). Esperar que `dig` rejeite com SkillTimeoutError dentro de
    `~config.digTimeoutMs` (executeWithSafety mata o hang), com o heartbeat acusando `maxLag < 200ms` durante
    a espera. Asserir `rejects.toThrow()` e `maxLag < 200`.

    Notas:
    - O heartbeat só é confiável se o teste NÃO bloquear sincronamente o event loop — é exatamente o que estamos
      provando. Usar `await new Promise(r => setTimeout(r, 0))` se precisar ceder o loop entre passos.
    - NÃO depender de servidor MC nem de pathfinder real — tudo mockado, consistente com loop.smoke.test.ts.
  </action>
  <verify>
    <automated>cd c:/Users/biel1/OneDrive/Documentos/GitHub/MineMind; bun test src/skills/dig.oom.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/skills/dig.oom.smoke.test.ts` existe
    - `grep "PERCEPTION_RADIUS" src/skills/dig.oom.smoke.test.ts` retorna match com '32' (D-07 honrado)
    - `grep "heapUsed" src/skills/dig.oom.smoke.test.ts` retorna match (ponto 1)
    - `grep "rejects.toThrow" src/skills/dig.oom.smoke.test.ts` retorna match (ponto 2)
    - `grep "setInterval" src/skills/dig.oom.smoke.test.ts` retorna match e `grep "toBeLessThan(200" src/skills/dig.oom.smoke.test.ts` retorna match (ponto 3, lag<200ms)
    - `bun test src/skills/dig.oom.smoke.test.ts` sai 0 com 2 testes passando
  </acceptance_criteria>
  <done>O smoke prova os 3 pontos de D-07 (sem OOM via teto de heap, rejeição dentro de digTimeoutMs, lag<200ms via heartbeat) em dois cenários: todas inalcançáveis e collect travado (#222); todos os testes passam.</done>
</task>

<task type="auto">
  <name>Task 2: Rodar a suíte completa e confirmar regressão zero</name>
  <read_first>
    - package.json (script de teste — confirmar comando `bun test`)
    - src/cognition/loop.smoke.test.ts (smoke do loop que não pode regredir)
  </read_first>
  <files>src/skills/dig.oom.smoke.test.ts</files>
  <action>
    Rodar a suíte de testes inteira (`bun test`) e `bun run tsc --noEmit` para garantir que as mudanças dos
    Planos 01-04 + este smoke não introduziram regressões. Se algum teste pré-existente falhar por causa das
    mudanças desta fase (ex: dig.test.ts, loop.smoke.test.ts, config.test.ts), investigar e corrigir o teste
    OU o código conforme apropriado — mas NÃO afrouxar as asserções do tripé de D-07. Registrar no SUMMARY o
    total de testes passando (formato da verificação da Fase 2/3, ex: "X/X testes headless").
  </action>
  <verify>
    <automated>cd c:/Users/biel1/OneDrive/Documentos/GitHub/MineMind; bun run tsc --noEmit; bun test</automated>
  </verify>
  <acceptance_criteria>
    - `bun run tsc --noEmit` sai 0
    - `bun test` sai 0 (suíte inteira verde, incluindo dig.test.ts, dig.oom.smoke.test.ts, loop.smoke.test.ts, config.test.ts)
    - Nenhuma asserção do tripé de D-07 foi removida/afrouxada para fazer a suíte passar
  </acceptance_criteria>
  <done>Suíte completa verde + tsc limpo; contagem de testes registrada no SUMMARY; D-07 verificado sem afrouxar asserções.</done>
</task>

</tasks>

<verification>
- `bun run tsc --noEmit` limpo
- `bun test src/skills/dig.oom.smoke.test.ts` passa (2 testes — tripé D-07)
- `bun test` (suíte inteira) verde — regressão zero
</verification>

<success_criteria>
Os três critérios de D-07 estão provados por teste automatizado com PERCEPTION_RADIUS=32: (1) sem OOM
(heap sob teto), (2) skill rejeita dentro de digTimeoutMs, (3) event loop com lag<200ms durante a skill
candidata a bloqueio. A propriedade central "o loop cognitivo nunca trava" está ativamente verificada.
</success_criteria>

<output>
Após completar, criar `.planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-05-SUMMARY.md`
</output>
