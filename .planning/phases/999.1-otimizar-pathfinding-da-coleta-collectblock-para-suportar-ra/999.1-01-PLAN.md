---
phase: 999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - .env.example
  - .env
autonomous: true
requirements: [D-01, D-02]
must_haves:
  truths:
    - "config expõe gatherSearchRadius (default 16), pathfinderSearchRadius (default 48), pathfinderThinkTimeoutMs (default 2000)"
    - "PERCEPTION_RADIUS continua 32 (raio de percepção desacoplado do raio de coleta)"
    - "config rejeita no startup valores fora de range para os novos parâmetros"
  artifacts:
    - path: "src/config.ts"
      provides: "Novos campos de config gatherSearchRadius / pathfinderSearchRadius / pathfinderThinkTimeoutMs + validação"
      contains: "gatherSearchRadius"
    - path: ".env.example"
      provides: "Documentação dos novos env vars"
      contains: "GATHER_SEARCH_RADIUS"
  key_links:
    - from: "process.env.GATHER_SEARCH_RADIUS"
      to: "config.gatherSearchRadius"
      via: "parseInt com default 16"
      pattern: "gatherSearchRadius.*GATHER_SEARCH_RADIUS"
---

<objective>
Desacoplar o "raio de busca de coleta" e os bounds do A* do "raio de percepção", introduzindo três
novos parâmetros de configuração (D-01, D-02). Esta é a fundação consumida por TODOS os outros planos
da fase: `connection.ts` (bounds do pathfinder) e `dig.ts` (raio de findBlocks + pré-check) leem estes
campos.

Purpose: Atacar a fonte do OOM na raiz — limitar candidatos distantes (GATHER_SEARCH_RADIUS) e ativar o
gate `maxCost` do A* (PATHFINDER_SEARCH_RADIUS) — sem degradar a percepção rica do mundo (PERCEPTION_RADIUS=32).
Output: `src/config.ts` com 3 campos novos + validação de range; `.env.example` e `.env` documentados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-CONTEXT.md

<interfaces>
<!-- Padrão estabelecido em config.ts — replicar exatamente. -->
<!-- Env vars usam `|| default` (NÃO `??`): string vazia '' é falsy e deve cair no default. -->
Padrão de campo (config.ts §15-19):
```typescript
perceptionRadius: parseInt(process.env.PERCEPTION_RADIUS || '32', 10),
digTimeoutMs: parseInt(process.env.DIG_TIMEOUT_MS || '10000', 10),
```
Padrão de validação de startup (config.ts §129-131):
```typescript
if (config.perceptionRadius < 1 || config.perceptionRadius > 128) {
  throw new Error(`PERCEPTION_RADIUS inválido: ${config.perceptionRadius}. Deve ser entre 1 e 128.`)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar 3 campos de config + validação de range (D-01, D-02)</name>
  <read_first>
    - src/config.ts (ver padrão de campos com `|| default` §7-70 e bloco de validação §128-155)
    - .planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-CONTEXT.md (D-01, D-02, e "Claude's Discretion" para nomes/ranges)
  </read_first>
  <files>src/config.ts</files>
  <action>
    No objeto `config` em src/config.ts, adicionar TRÊS campos novos, seguindo o padrão `parseInt(process.env.X || 'default', 10)`:

    1. Logo após o campo `perceptionRadius` (§15), adicionar (D-01):
       ```typescript
       // 999.1 D-01: raio de busca de coleta — INDEPENDENTE de perceptionRadius.
       // dig.ts usa este valor no findBlocks({ maxDistance }); percepção (snapshot.ts) mantém perceptionRadius.
       gatherSearchRadius: parseInt(process.env.GATHER_SEARCH_RADIUS || '16', 10),
       ```

    2. Logo após `gatherSearchRadius`, adicionar (D-02) — bounds do A* expostos via env:
       ```typescript
       // 999.1 D-02: bounds do A* do pathfinder — ativam o gate maxCost (raiz do fix de OOM).
       // searchRadius ≈ 1.5-2× perceptionRadius (default 48); thinkTimeout secundário (default 2000ms).
       pathfinderSearchRadius: parseInt(process.env.PATHFINDER_SEARCH_RADIUS || '48', 10),
       pathfinderThinkTimeoutMs: parseInt(process.env.PATHFINDER_THINK_TIMEOUT_MS || '2000', 10),
       ```

    3. No bloco de validação de startup (após o `if` de `perceptionRadius` §129-131), adicionar:
       ```typescript
       if (config.gatherSearchRadius < 1 || config.gatherSearchRadius > 128) {
         throw new Error(`GATHER_SEARCH_RADIUS inválido: ${config.gatherSearchRadius}. Deve ser entre 1 e 128.`)
       }
       if (config.pathfinderSearchRadius < 1 || config.pathfinderSearchRadius > 256) {
         throw new Error(`PATHFINDER_SEARCH_RADIUS inválido: ${config.pathfinderSearchRadius}. Deve ser entre 1 e 256.`)
       }
       if (config.pathfinderThinkTimeoutMs < 1) {
         throw new Error(`PATHFINDER_THINK_TIMEOUT_MS inválido: ${config.pathfinderThinkTimeoutMs}. Deve ser >= 1.`)
       }
       ```

    NÃO alterar `perceptionRadius` nem qualquer outro campo existente. NÃO usar `??` (usar `||` por convenção do arquivo).
  </action>
  <verify>
    <automated>cd c:/Users/biel1/OneDrive/Documentos/GitHub/MineMind; bun run tsc --noEmit; bun test src/config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "gatherSearchRadius: parseInt(process.env.GATHER_SEARCH_RADIUS || '16'" src/config.ts` retorna match
    - `grep "pathfinderSearchRadius: parseInt(process.env.PATHFINDER_SEARCH_RADIUS || '48'" src/config.ts` retorna match
    - `grep "pathfinderThinkTimeoutMs: parseInt(process.env.PATHFINDER_THINK_TIMEOUT_MS || '2000'" src/config.ts` retorna match
    - `grep "GATHER_SEARCH_RADIUS inválido" src/config.ts` retorna match (validação presente)
    - `grep "perceptionRadius: parseInt(process.env.PERCEPTION_RADIUS || '32'" src/config.ts` ainda retorna match (perceptionRadius intocado)
    - `bun run tsc --noEmit` sai 0
  </acceptance_criteria>
  <done>config expõe gatherSearchRadius=16, pathfinderSearchRadius=48, pathfinderThinkTimeoutMs=2000 com validação de range; perceptionRadius=32 inalterado; tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2: Documentar novos env vars em .env.example e garantir PERCEPTION_RADIUS=32 em .env (D-01, D-02)</name>
  <read_first>
    - .env.example (ver bloco "Percepção" §10-11 e estilo de comentários por seção)
    - .env (confirmar valor atual de PERCEPTION_RADIUS)
    - .planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-CONTEXT.md (Folded Todos: "Após o fix, restaurar o default de PERCEPTION_RADIUS=32")
  </read_first>
  <files>.env.example, .env</files>
  <action>
    Em `.env.example`, logo após a linha `PERCEPTION_RADIUS=32` (§11), adicionar um bloco novo:
    ```
    # === 999.1: raio de coleta e bounds do pathfinder (fix de OOM em Gathering) ===
    # Raio de busca de coleta — INDEPENDENTE de PERCEPTION_RADIUS (D-01). Limita candidatos distantes
    # que alimentavam o A* até estourar a memória. Default conservador 16.
    GATHER_SEARCH_RADIUS=16
    # Bounds do A* do pathfinder (D-02) — ativam o gate maxCost que evita o crescimento ilimitado da heap.
    # searchRadius ≈ 1.5-2× PERCEPTION_RADIUS; thinkTimeout é a rede de segurança temporal secundária.
    PATHFINDER_SEARCH_RADIUS=48
    PATHFINDER_THINK_TIMEOUT_MS=2000
    ```

    Em `.env`: confirmar que `PERCEPTION_RADIUS=32` (restaurar de qualquer workaround =8 se presente — D-07/Folded Todos
    pede percepção restaurada a 32). Adicionar as mesmas 3 linhas (`GATHER_SEARCH_RADIUS=16`,
    `PATHFINDER_SEARCH_RADIUS=48`, `PATHFINDER_THINK_TIMEOUT_MS=2000`) ao `.env` se ainda não existirem.
  </action>
  <verify>
    <automated>cd c:/Users/biel1/OneDrive/Documentos/GitHub/MineMind; grep -c "GATHER_SEARCH_RADIUS=16" .env.example; grep -c "PERCEPTION_RADIUS=32" .env</automated>
  </verify>
  <acceptance_criteria>
    - `grep "GATHER_SEARCH_RADIUS=16" .env.example` retorna match
    - `grep "PATHFINDER_SEARCH_RADIUS=48" .env.example` retorna match
    - `grep "PATHFINDER_THINK_TIMEOUT_MS=2000" .env.example` retorna match
    - `grep "PERCEPTION_RADIUS=32" .env` retorna match (workaround =8 NÃO presente)
    - `grep "GATHER_SEARCH_RADIUS=16" .env` retorna match
  </acceptance_criteria>
  <done>.env.example documenta os 3 novos env vars com comentários; .env tem PERCEPTION_RADIUS=32 e os 3 novos vars.</done>
</task>

</tasks>

<verification>
- `bun run tsc --noEmit` limpo
- `bun test src/config.test.ts` passa (validação de range não quebra com defaults)
- config.gatherSearchRadius, config.pathfinderSearchRadius, config.pathfinderThinkTimeoutMs existem e são números
</verification>

<success_criteria>
config.ts expõe os 3 novos parâmetros com defaults (16/48/2000) e validação de range; perceptionRadius permanece 32;
.env.example e .env documentam/definem os novos vars. Planos 02 e 03 podem consumir estes campos.
</success_criteria>

<output>
Após completar, criar `.planning/phases/999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra/999.1-01-SUMMARY.md`
</output>
