---
phase: 999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra
plan: 05
subsystem: testing
tags: [bun-test, smoke-test, oom, pathfinder, collectblock, heartbeat, event-loop]

# Dependency graph
requires:
  - phase: 999.1-01
    provides: GATHER_SEARCH_RADIUS + bounds do A* (config.ts) que limitam a memória do pathfinder
  - phase: 999.1-02
    provides: validação de range das env vars de pathfinding
  - phase: 999.1-03
    provides: dig.ts com pré-check getPathTo (filtra inalcançáveis) + auto-wrap em executeWithSafety
  - phase: 999.1-04
    provides: bounds aplicados em bot.pathfinder.* e bot.collectBlock.movements (connection.ts) + remoção do double-wrap
provides:
  - Smoke headless multi-tick que prova o tripé de D-07 (sem OOM, rejeita dentro de digTimeoutMs, lag<200ms)
  - Verificação de aceitação automatizada da propriedade "o loop cognitivo nunca trava" sob PERCEPTION_RADIUS=32
affects: [verificação de fase, futuras mudanças em dig.ts/executor.ts/pathfinder]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Heartbeat via setInterval ~50ms medindo drift do timer como prova de responsividade do event loop"
    - "Smoke headless com env (PERCEPTION_RADIUS/DIG_TIMEOUT_MS) definida ANTES do import de config para honrar config carregada em module-load"

key-files:
  created:
    - src/skills/dig.oom.smoke.test.ts
  modified: []

key-decisions:
  - "DIG_TIMEOUT_MS reduzido para 1500ms no smoke (via env, antes do import) para manter o teste rápido sem afrouxar nenhuma asserção do tripé"
  - "Teto de heap generoso de 200MB: jamais respeitado se o A* explodisse (~78GB), mas folgado para overhead normal do runtime"

patterns-established:
  - "Heartbeat de event-loop em teste: setInterval(50ms) mede maxLag; expect(maxLag).toBeLessThan(200)"
  - "Mock de hang #222: collectBlock.collect retorna new Promise(() => {}) (nunca resolve) para provar que a rede temporal mata o travamento"

requirements-completed: [D-07]

# Metrics
duration: 3min
completed: 2026-06-19
---

# Phase 999.1 Plan 05: Smoke de aceitação do fix de OOM (tripé D-07) Summary

**Smoke headless `bun:test` que prova simultaneamente os 3 critérios de D-07 sob PERCEPTION_RADIUS=32 — sem OOM (heap sob teto), rejeição dentro de digTimeoutMs, e lag de event loop < 200ms via heartbeat — em dois cenários (todos inalcançáveis e collect travado #222).**

## Performance

- **Duration:** 3 min
- **Started:** 2026-06-19T17:10:52Z
- **Completed:** 2026-06-19T17:13:21Z
- **Tasks:** 2
- **Files modified:** 1 (criado)

## Accomplishments
- Smoke `src/skills/dig.oom.smoke.test.ts` com 2 testes provando o tripé de D-07, gate principal "o loop cognitivo nunca trava" ativamente verificado.
- Teste A (alvo inalcançável): `dig` rejeita "Nenhuma instância alcançável" via pré-check síncrono, heap cresce abaixo de 200MB, `maxLag < 200ms`, `collect` nunca chamado.
- Teste B (hang #222): `collect` que nunca resolve é morto por `digTimeoutMs` (SkillTimeoutError), com `maxLag < 200ms` durante a espera — provando a rede de segurança temporal.
- Suíte completa verde: **165/165 testes** passando em 21 arquivos + `tsc --noEmit` limpo. Regressão zero nos Planos 01-04.

## Task Commits

Each task was committed atomically:

1. **Task 1: Smoke headless do tripé de D-07** - `cf621f1` (test)
2. **Task 2: Suíte completa + tsc (regressão zero)** - sem mudanças de código; verificação executada (165/165 verde, tsc 0). Resultados registrados aqui.

**Plan metadata:** (commit docs final)

## Files Created/Modified
- `src/skills/dig.oom.smoke.test.ts` - Smoke headless multi-tick: heartbeat de event loop, mock de alvo inalcançável e de hang #222, asserções do tripé de D-07 (heapUsed sob teto, rejects.toThrow dentro de digTimeoutMs, maxLag<200ms).

## Decisions Made
- **DIG_TIMEOUT_MS=1500 no smoke:** reduzido via `process.env` antes do import de `config` (carregada em module-load). Mantém o teste rápido (~10s para o cenário de hang) sem tocar nas asserções do tripé.
- **Teto de heap = 200MB:** generoso o suficiente para overhead normal do runtime/Bun, mas finito — jamais seria respeitado se o A* sem bound explodisse (~78GB do bug original).
- **Mock de hang #222 como `new Promise(() => {})`:** simula fielmente o travamento sem lançar, forçando o teste a provar que `executeWithSafety` (rede temporal) é quem mata, não o pré-check.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None. Ambos os testes passaram na primeira execução; tsc e suíte completa verdes sem ajustes.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- D-07 está provado por teste automatizado: o fix de OOM (Planos 01-04) está verificado de ponta a ponta sob PERCEPTION_RADIUS=32.
- Última peça da Fase 999.1. Pendência operacional (fora deste plano): restaurar o default de `PERCEPTION_RADIUS=32` no ambiente de execução (o default em `config.ts` já é 32; conferir `.env` local se ainda fixado em 8 pelo workaround).
- Pronto para transição de fase (`/gsd:transition`).

## Self-Check: PASSED
- FOUND: src/skills/dig.oom.smoke.test.ts
- FOUND commit: cf621f1
- Suite: 165/165 pass, tsc exit 0

---
*Phase: 999.1-otimizar-pathfinding-da-coleta-collectblock-para-suportar-ra*
*Completed: 2026-06-19*
