---
phase: 09-placement-crafting-smelting-grounded
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/skills/shelter.ts
autonomous: true
requirements: [BUILD-01]
must_haves:
  truths:
    - "shelter.ts consome placeBlockSafe (Plano 01) em vez das chamadas cruas bot.placeBlock — placeBlock UMA VEZ compartilhado (D-05)"
    - "Os dois branches (cavar-e-tampar / pilar 1×1) e a lógica de cobertura grounded são preservados"
    - "Os testes do shelter da Fase 7/8 continuam verdes após o refator"
  artifacts:
    - path: "src/skills/shelter.ts"
      provides: "shelter refatorado consumindo placeBlockSafe + getRefAndFace"
      contains: "placeBlockSafe"
  key_links:
    - from: "src/skills/shelter.ts"
      to: "src/skills/placeBlock.ts"
      via: "import placeBlockSafe + getRefAndFace"
      pattern: "import.*placeBlockSafe"
---

<objective>
Refatorar `src/skills/shelter.ts` para consumir o wrapper robusto `placeBlockSafe`/`getRefAndFace` do Plano 01 (D-05) — cumprindo "placeBlock implementado UMA VEZ, compartilhado por abrigo/building/estações". COMMIT ISOLADO. Mantém os dois branches (cavar-e-tampar / pilar 1×1) e a lógica de cobertura grounded; revalida os testes do shelter da Fase 7/8 ANTES de fechar.

Purpose: o abrigo de emergência (Fase 8) passa a herdar o swallow seletivo do timeout de blockUpdate e a verificação por blockAt — sem manter um placeBlock mínimo paralelo que diverge do robusto.
Output: src/skills/shelter.ts refatorado; src/skills/shelter.test.ts verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-placement-crafting-smelting-grounded/09-CONTEXT.md

<interfaces>
placeBlockSafe (Plano 01 — src/skills/placeBlock.ts):
```typescript
export async function placeBlockSafe(bot, ref: Block, faceVector: Vec3, blockItem: Item, targetPos: {x,y,z}): Promise<SkillResult>
// equipa blockItem na mão, chama bot.placeBlock, engole timeout (falso-negativo), deriva outcome de blockAt(targetPos)
export function getRefAndFace(bot, target: {x,y,z}): { ref: Block; face: Vec3 } | null
```

shelter.ts ATUAL (src/skills/shelter.ts):
- Branch CAVAR-E-TAMPAR (L51-65): `bot.dig(below1)` + `bot.placeBlock(headRef, makeVec(0,-1,0))` (tampa o topo, face para baixo).
- Branch PILAR 1×1 (L66-87): `bot.equip(block,'hand')` + `bot.lookAt(pos.offset(0,-1,0))` + `setControlState('jump',true)` + espera 250ms + `bot.placeBlock(belowRef, makeVec(0,1,0))` (face para cima) + `finally { setControlState('jump',false) }`.
- Grounding por COBERTURA REAL (L89-96): `const above = bot.blockAt(pos.offset(0,2,0))`; `covered = above != null && above.name !== 'air' && above.name !== 'cave_air'`; outcome `covered ? 'success' : 'partial'`. PRESERVAR isto exatamente.
- `block` = item colocável selecionado de `bot.inventory.items()` (PLACEABLE regex). `makeVec(x,y,z)` retorna `{x,y,z} as Vec3`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trocar as 2 chamadas cruas bot.placeBlock por placeBlockSafe (D-05)</name>
  <files>src/skills/shelter.ts</files>
  <read_first>
    - src/skills/shelter.ts (arquivo inteiro — preservar branches e grounding por cobertura)
    - src/skills/placeBlock.ts (placeBlockSafe + getRefAndFace — assinaturas exatas, criadas no Plano 01)
    - src/skills/shelter.test.ts (asserções que NÃO podem quebrar)
  </read_first>
  <action>
    Refatorar `src/skills/shelter.ts` (comentários pt-BR, commit isolado). Importar no topo: `import { placeBlockSafe, getRefAndFace } from './placeBlock'`.

    **Branch CAVAR-E-TAMPAR** — hoje (L59-62):
    ```typescript
    const headRef = bot.blockAt(pos.offset(0, 1, 0))
    if (headRef) {
      await bot.placeBlock(headRef, makeVec(0, -1, 0))
    }
    ```
    Trocar por: o alvo de cobertura é a posição do topo (pos.offset(0,2,0) — onde o `above` é checado). Usar:
    ```typescript
    // D-05: usa o wrapper robusto (swallow do timeout de blockUpdate + verificação por blockAt).
    const topTarget = pos.offset(0, 2, 0)
    const rf = getRefAndFace(bot, { x: topTarget.x, y: topTarget.y, z: topTarget.z })
    if (rf) {
      await placeBlockSafe(bot, rf.ref, rf.face, block, { x: topTarget.x, y: topTarget.y, z: topTarget.z })
    }
    ```
    (Nota: o branch cavar-e-tampar desce 1 ao cavar o below1, então o alvo de cobertura real é o que o grounding checa em pos.offset(0,2,0). Se getRefAndFace retornar null, segue para o grounding por cobertura que decidirá partial.)

    **Branch PILAR 1×1** — hoje (L67-77) faz equip+lookAt+jump+espera+`bot.placeBlock(belowRef, makeVec(0,1,0))`. PRESERVAR a mecânica de pulo (equip+lookAt+jump+espera 250ms+finally jump=false — o timing do ápice é específico do pilar e o placeBlockSafe re-equipa o bloco internamente, o que é idempotente). Trocar SÓ a chamada `bot.placeBlock(belowRef, makeVec(0,1,0))` por:
    ```typescript
    const footTarget = pos.offset(0, -1, 0)
    const belowRef = bot.blockAt(footTarget)
    if (belowRef) {
      // alvo = sob os pés; ref = belowRef (o bloco abaixo), face para cima (D-05 reusa o wrapper)
      await placeBlockSafe(bot, belowRef, makeVec(0, 1, 0), block, { x: footTarget.x, y: footTarget.y, z: footTarget.z })
    }
    ```
    Manter o `bot.equip(block,'hand')` do pilar (não atrapalha; placeBlockSafe re-equipa idempotente) OU removê-lo já que placeBlockSafe equipa — Claude's discretion, mas se remover, garantir que o lookAt/jump continuam. Manter `makeVec` (ainda usado para os faceVectors).

    **PRESERVAR INTACTO:** seleção do `block` (PLACEABLE), guarda anti-lava `UNSAFE_BELOW`/`canDig`, o `bot.dig(below1)` do cavar-e-tampar, o bloco de grounding por cobertura (L89-96), o schema, o tool descriptor. NÃO mudar a assinatura de `shelter`.

    Se o teste shelter.test.ts quebrar porque o mock não tem o que getRefAndFace/placeBlockSafe lê (ex: blockAt de vizinhos do topo), ajustar o MOCK do teste (adicionar blocos vizinhos sólidos no mapa de blocks) para refletir um cenário onde getRefAndFace encontra um ref — SEM mudar as asserções de outcome (canDig→cava, !canDig→pilar, sem blocos→no_effect, cobertura→success).
  </action>
  <verify>
    <automated>bun test src/skills/shelter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/skills/shelter.ts contains "import { placeBlockSafe, getRefAndFace } from './placeBlock'"
    - src/skills/shelter.ts contains "placeBlockSafe("
    - src/skills/shelter.ts does NOT contain "bot.placeBlock(" (as 2 chamadas cruas foram substituídas)
    - src/skills/shelter.ts still contains "UNSAFE_BELOW" and "above.name !== 'air'" (guarda anti-lava + grounding por cobertura preservados)
    - src/skills/shelter.ts still contains "setControlState('jump', true)" (mecânica do pilar preservada)
    - `bun test src/skills/shelter.test.ts` exits 0 (testes da Fase 7/8 revalidados)
  </acceptance_criteria>
  <done>shelter consome placeBlockSafe nos dois branches; guarda anti-lava, mecânica do pilar e grounding por cobertura preservados; testes verdes.</done>
</task>

</tasks>

<verification>
- `bun test src/skills/shelter.test.ts` passa (revalidação Fase 7/8).
- `bun test` global verde (nenhuma regressão).
- Nenhuma chamada `bot.placeBlock(` crua resta em shelter.ts (grep retorna 0).
</verification>

<success_criteria>
- BUILD-01: placeBlock implementado UMA VEZ — shelter agora consome o wrapper robusto (D-05).
- Os dois branches e o grounding por cobertura preservados; testes da Fase 7/8 revalidados.
- Commit isolado (só shelter.ts).
</success_criteria>

<output>
Após completar, criar `.planning/phases/09-placement-crafting-smelting-grounded/09-02-SUMMARY.md`.
</output>
