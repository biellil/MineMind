---
phase: 09-placement-crafting-smelting-grounded
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/skills/equip.ts
  - src/skills/equip.test.ts
  - src/skills/dig.ts
  - src/skills/attack.ts
autonomous: true
requirements: [CRAFT-04]
must_haves:
  truths:
    - "equip(itemName, destination?) é skill standalone grounded por estado LOCAL (heldItem/slot de armadura), não por delta de inventário"
    - "selectToolFor(bot, category) escolhe binariamente por categoria (pickaxe/sword/axe), SEM ranking por tier"
    - "dig e attack fazem pré-flight de selectToolFor antes de agir (rede de segurança B2)"
  artifacts:
    - path: "src/skills/equip.ts"
      provides: "equip + selectToolFor + matchesCategory + EquipSchema + equipTool"
      contains: "export function selectToolFor"
    - path: "src/skills/dig.ts"
      provides: "pré-flight selectToolFor('pickaxe') antes do collect/dig"
      contains: "selectToolFor"
    - path: "src/skills/attack.ts"
      provides: "pré-flight selectToolFor('weapon') antes do golpe"
      contains: "selectToolFor"
  key_links:
    - from: "src/skills/equip.ts"
      to: "src/grounding/evaluate.ts"
      via: "import evaluateEquip"
      pattern: "evaluateEquip"
    - from: "src/skills/dig.ts"
      to: "src/skills/equip.ts"
      via: "import selectToolFor"
      pattern: "import.*selectToolFor"
---

<objective>
Implementar `equip` como verbo de 1ª classe (B1) + o pré-flight `selectToolFor` em dig/attack (B2) — D-16/D-17. Grounding LOCAL (D-19): equipar não muda contagem de inventário, então a verdade é `bot.heldItem`/`inventory.slots[5..8]`, não delta. Heurística binária por categoria (D-17): tem pickaxe? equipa; sword/axe? equipa — SEM ranking por tier (isso é Fase 10).

Purpose: CRAFT-04 ("equipa a ferramenta/armadura apropriada"); a rede de segurança B2 protege o Core Value (o LLM local frequentemente omite equipar antes de minerar/atacar).
Output: src/skills/equip.ts (+test), pré-flight em dig.ts e attack.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-placement-crafting-smelting-grounded/09-CONTEXT.md
@.planning/phases/09-placement-crafting-smelting-grounded/09-RESEARCH.md

<interfaces>
evaluateEquip (criado no Plano 01 — src/grounding/evaluate.ts):
```typescript
export function evaluateEquip(equipped: boolean): SkillResult
// equipped=true -> success/observed 1; false -> no_effect/observed 0; delta {}
```

eat.ts (molde de equip→ação + grounding LOCAL):
- extrai signal antes do Zod; usa `bot.inventory.items().find(...)`; `bot.equip(item, 'hand')`; lê `bot.heldItem` como verdade LOCAL (Pitfall 2: NÃO usar captureGroundState/delta para equip).

dig.ts (ponto do pré-flight, ANTES do bloco try de collect/dig — depois do parse, antes do executeWithSafety): caminho `target` string (collect) e caminho posição (dig). attack.ts: pré-flight ANTES de `bot.attack(target)`.

Destinos de bot.equip (confirmado): 'hand' (default), 'head', 'torso', 'legs', 'feet', 'off-hand'.
Slots de armadura: bot.inventory.slots[5]=head, [6]=torso, [7]=legs, [8]=feet.
equip NÃO altera inventory.items() counts (D-19/Pitfall 2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar src/skills/equip.ts (equip standalone + selectToolFor + matchesCategory)</name>
  <files>src/skills/equip.ts, src/skills/equip.test.ts</files>
  <read_first>
    - src/skills/eat.ts (molde: extrair signal, find no inventário, bot.equip, grounding LOCAL via estado vital — aqui via heldItem)
    - src/skills/dig.ts (padrão de extrair signal antes do Zod + tool descriptor)
    - src/grounding/types.ts (SkillResult)
    - src/grounding/evaluate.ts (evaluateEquip — criado no Plano 01)
    - .planning/phases/09-placement-crafting-smelting-grounded/09-RESEARCH.md (Pattern 5, D-16/D-17/D-19, Pitfall 2)
  </read_first>
  <behavior>
    equip (bot mockado):
    - Test: item presente, destination 'hand', após bot.equip bot.heldItem.name === itemName → success, observed 1, delta {}.
    - Test: item ausente no inventário → no_effect, observed 0, reason 'item ausente'.
    - Test: item presente mas bot.heldItem continua diferente após equip → no_effect.
    - Test: destination 'head', inventory.slots[5].name === itemName após equip → success.
    selectToolFor (puro, bot mockado com inventory.items()):
    - Test: inventário com 'stone_pickaxe' e category 'pickaxe' → retorna o item pickaxe.
    - Test: inventário com 'iron_sword' e category 'weapon' → retorna o sword.
    - Test: inventário sem ferramenta da categoria → null.
    - Test: NÃO faz ranking — com 'wooden_pickaxe' e 'diamond_pickaxe', retorna o PRIMEIRO match (qualquer um), não o melhor tier (D-17).
  </behavior>
  <action>
    Criar `src/skills/equip.ts` (comentários pt-BR). Estrutura:

    1. Imports: `z` (zod), `Bot` (mineflayer), `Item` de 'prismarine-item' (type-only), `SkillResult` de '../grounding/types', `evaluateEquip` de '../grounding/evaluate'.

    2. `export const EquipSchema = z.object({ itemName: z.string().max(64).describe('Nome do item a equipar (ferramenta/armadura/bloco)'), destination: z.enum(['hand','head','torso','legs','feet','off-hand']).optional().describe('Slot de destino (padrão hand)') })`.

    3. Categorias (D-17, binário — SEM tier). `type ToolCategory = 'pickaxe' | 'weapon' | 'axe' | 'shovel'` (Claude's discretion sobre o conjunto; mínimo: 'pickaxe' e 'weapon').
    ```typescript
    const CATEGORY_PATTERNS: Record<string, RegExp> = {
      pickaxe: /_pickaxe$/,
      weapon: /_(sword|axe)$/,   // arma de corpo-a-corpo p/ attack
      axe: /_axe$/,
      shovel: /_shovel$/,
    }
    function matchesCategory(name: string, category: string): boolean {
      const re = CATEGORY_PATTERNS[category]
      return re ? re.test(name) : false
    }
    ```

    4. `export function selectToolFor(bot: Bot, category: string): Item | null` (D-17, binário):
    ```typescript
    return bot.inventory.items().find((it) => matchesCategory(it.name, category)) ?? null
    ```
    Comentário explícito: "binário por categoria; ranking por tier (madeira<pedra<ferro<diamante) é Fase 10 — troca o find por seletor ranqueado mantendo este ponto de chamada."

    5. `export async function equip(bot, rawParams): Promise<SkillResult>`:
       - Extrair signal antes do parse: `const signal = (rawParams as Record<string, unknown>)?.signal as AbortSignal | undefined; void signal` (não há ação longa; manter padrão).
       - `const { itemName, destination } = EquipSchema.parse(rawParams)`.
       - `const item = bot.inventory.items().find((i) => i.name === itemName)`; se ausente → `{ outcome:'no_effect', observed:0, expected:1, delta:{}, reason:'item ausente' }`.
       - `const dest = destination ?? 'hand'`.
       - `let threw: unknown = null; try { await bot.equip(item, dest) } catch (e) { threw = e }`.
       - Verdade LOCAL (D-19/Pitfall 2):
       ```typescript
       const ARMOR_SLOT: Record<string, number> = { head: 5, torso: 6, legs: 7, feet: 8 }
       const equipped = dest === 'hand'
         ? bot.heldItem?.name === itemName
         : dest === 'off-hand'
           ? bot.inventory.slots[45]?.name === itemName
           : bot.inventory.slots[ARMOR_SLOT[dest]]?.name === itemName
       const result = evaluateEquip(!!equipped)
       return threw ? { ...result, reason: threw instanceof Error ? threw.message : String(threw) } : result
       ```

    6. `export const equipTool = { name: 'equip', description: 'Equipa uma ferramenta/armadura/bloco do inventário no slot indicado (mão por padrão)', schema: EquipSchema, execute: equip } as const`.

    NÃO registrar no index.ts aqui (Plano 03 registra os 4 juntos). Criar `src/skills/equip.test.ts` cobrindo o <behavior> (molde eat.test.ts: bot mockado com inventory.items(), heldItem mutável, inventory.slots, bot.equip async que muda heldItem/slot).
  </action>
  <verify>
    <automated>bun test src/skills/equip.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File src/skills/equip.ts exists
    - src/skills/equip.ts contains "export function selectToolFor"
    - src/skills/equip.ts contains "evaluateEquip"
    - src/skills/equip.ts contains "bot.heldItem?.name === itemName"
    - src/skills/equip.ts contains "_pickaxe$"
    - src/skills/equip.ts contains "export const equipTool"
    - src/skills/equip.ts does NOT contain "captureGroundState" (D-19/Pitfall 2: grounding LOCAL, não delta)
    - `bun test src/skills/equip.test.ts` exits 0 including the "no ranking by tier" test
  </acceptance_criteria>
  <done>equip standalone grounded por estado LOCAL; selectToolFor binário sem tier; ambos testados.</done>
</task>

<task type="auto">
  <name>Task 2: Pré-flight selectToolFor em dig.ts e attack.ts (B2/D-16)</name>
  <files>src/skills/dig.ts, src/skills/attack.ts</files>
  <read_first>
    - src/skills/dig.ts (ponto de inserção: após o parse/before, antes do bloco try de collect/dig)
    - src/skills/attack.ts (ponto de inserção: antes de bot.attack(target))
    - src/skills/equip.ts (selectToolFor — criado na Task 1)
  </read_first>
  <action>
    **A) src/skills/dig.ts** — pré-flight de pickaxe (B2/D-16). Importar `selectToolFor` de './equip'. Inserir DEPOIS do `const before = captureGroundState(...)` (linha ~41) e ANTES do bloco `try` de mineração, um pré-flight best-effort que NÃO altera o fluxo grounded:
    ```typescript
    // B2/D-16: pré-flight de ferramenta — rede de segurança (o LLM local frequentemente omite equipar).
    // Binário por categoria (D-17, sem tier). Best-effort: falha de equip NÃO aborta o dig (grounded decide).
    try {
      const tool = selectToolFor(bot, 'pickaxe')
      if (tool && bot.heldItem?.name !== tool.name) await bot.equip(tool, 'hand')
    } catch { /* pré-flight best-effort; o grounding por delta decide o sucesso */ }
    ```
    NÃO mudar a assinatura, o schema, nem o caminho de grounding do dig.

    **B) src/skills/attack.ts** — pré-flight de arma. Importar `selectToolFor` de './equip'. Inserir DEPOIS de encontrar `target` (e do guard `if (!target)`) e ANTES de `bot.attack(target)`:
    ```typescript
    // B2/D-16: pré-flight de arma (sword/axe) — binário por categoria (D-17, sem tier). Best-effort.
    try {
      const weapon = selectToolFor(bot, 'weapon')
      if (weapon && bot.heldItem?.name !== weapon.name) await bot.equip(weapon, 'hand')
    } catch { /* best-effort; não bloqueia o golpe */ }
    ```
    Como attack.ts hoje não é async no corpo após o parse, manter `attack` já é `async` (é) — o `await bot.equip` cabe. NÃO mudar o retorno grounded (success/no_effect) do attack.

    Revalidar os testes existentes de dig e attack (os mocks podem precisar de `bot.heldItem` e `bot.equip` — se os testes quebrarem por falta desses no mock, adicionar os stubs mínimos no mock de teste correspondente, sem mudar as asserções de outcome).
  </action>
  <verify>
    <automated>bun test src/skills/dig.test.ts src/skills/attack.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/skills/dig.ts contains "import { selectToolFor }" (ou import combinado contendo selectToolFor de './equip')
    - src/skills/dig.ts contains "selectToolFor(bot, 'pickaxe')"
    - src/skills/attack.ts contains "selectToolFor(bot, 'weapon')"
    - both pré-flights are wrapped in try/catch (best-effort, não abortam a skill)
    - `bun test src/skills/dig.test.ts src/skills/attack.test.ts` exits 0 (testes existentes revalidados)
  </acceptance_criteria>
  <done>dig equipa pickaxe e attack equipa arma antes de agir, best-effort, sem alterar o grounding; testes existentes verdes.</done>
</task>

</tasks>

<verification>
- `bun test src/skills/equip.test.ts src/skills/dig.test.ts src/skills/attack.test.ts` passa.
- equip reporta success quando heldItem muda (Pitfall 2 evitado — não usa delta de inventário).
- selectToolFor não ranqueia por tier (teste explícito).
</verification>

<success_criteria>
- CRAFT-04: equip de 1ª classe + pré-flight em dig/attack, grounded LOCAL, binário por categoria.
- Nenhuma regressão nos testes de dig/attack.
- equip ainda NÃO registrado no registry (Plano 03 registra os 4 juntos).
</success_criteria>

<output>
Após completar, criar `.planning/phases/09-placement-crafting-smelting-grounded/09-04-SUMMARY.md`.
</output>
