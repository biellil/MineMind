---
phase: 09-placement-crafting-smelting-grounded
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [BUILD-01, CRAFT-01, CRAFT-02, CRAFT-03, CRAFT-04]
files_modified:
  - src/llm/schemas.ts
  - src/cognition/nodes.ts
  - src/cognition/nodes.test.ts
must_haves:
  truths:
    - "Dada uma decisão LLM action='craft' fresca, o execute node despacha skillRegistry.craft com {itemName, count} derivados do target e grava um MemEvent action grounded (outcome do SkillResult)"
    - "Dada uma decisão LLM action='smelt', o execute node despacha skillRegistry.smelt com {oreName, count}"
    - "Dada uma decisão LLM action='equip', o execute node despacha skillRegistry.equip com {itemName, destination?}"
    - "Dada uma decisão LLM action='place', o execute node despacha skillRegistry.placeBlock com {target:{x,y,z}, itemName}"
    - "ActionDecisionSchema.parse aceita 'craft','smelt','equip','place' e rejeita valores fora do enum (continua FECHADO — LLM-02/D-10)"
    - "Sem target válido (item desconhecido / posição não parseável), o verbo degrada para 'sem ação' SEM lançar — o tick continua (Core Value)"
  artifacts:
    - path: "src/llm/schemas.ts"
      provides: "Enum de ação estendido com craft/smelt/equip/place + describe do target por verbo"
      contains: "'craft'"
    - path: "src/cognition/nodes.ts"
      provides: "actionToCognitiveState mapeia os 4 verbos novos + branch de dispatch que monta params físicos do target"
      contains: "skillRegistry"
    - path: "src/cognition/nodes.test.ts"
      provides: "Cobertura agent-level: cada verbo despacha a skill certa com params corretos e grava MemEvent grounded"
      contains: "skillRegistry.craft"
  key_links:
    - from: "src/llm/schemas.ts"
      to: "src/cognition/nodes.ts"
      via: "actionToCognitiveState(fresh.decision.action) + branch de dispatch no execute"
      pattern: "case 'craft'"
    - from: "src/cognition/nodes.ts execute"
      to: "skillRegistry.craft/smelt/equip/placeBlock"
      via: "skillRegistry[skill]!(bot, params) com params montados do llmTarget"
      pattern: "skillRegistry\\[skill"
---

<objective>
Fechar a lacuna G-01 (09-VERIFICATION.md): as 4 skills da Fase 9 (placeBlock/craft/smelt/equip) estão
construídas, grounded e registradas, mas NÃO são alcançáveis a partir da decisão do agente — o enum de
ação do LLM é fechado em ['gather','explore','navigate','idle','chat'] e o execute node só despacha
dig/navigate. Este plano abre a superfície de decisão (enum + target) e fia os verbos ao dispatch,
reusando o caminho de grounding/memória já existente no execute node.

Purpose: tornar CRAFT-01/02/03 e BUILD-01 verdadeiros no nível COMPORTAMENTAL (o agente CRAFTA/FUNDE/
COLOCA quando o LLM escolhe), não só no nível de capacidade (skill-quando-invocada).

Output: ActionDecisionSchema estendido (FECHADO), branch de dispatch no execute que monta params físicos
do target de alto nível, e teste agent-level provando o despacho + memória grounded de cada verbo.

ESCOPO (G-01 "Required to close" — MÍNIMO): abrir o enum, mapear ação→skill, montar params do target,
reusar o grounding/memória do execute, e cobrir com teste. NADA de tech-tree DAG, priorização por needs,
ou progressão recursiva madeira→pedra→ferro — isso é a Phase 10 (ROADMAP §"Phase 10: Tech Tree DAG +
Needs"). O LLM continua "diretor criativo" (escolhe verbo+alvo de alto nível); a mecânica física continua
nas skills (D-10). Se o trabalho começar a "decidir O QUE craftar com base em needs", PARE — é Phase 10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-placement-crafting-smelting-grounded/09-VERIFICATION.md
@.planning/phases/09-placement-crafting-smelting-grounded/09-CONTEXT.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código — NÃO explorar a base. -->

ActionDecisionSchema (src/llm/schemas.ts:20-39) — enum FECHADO a estender:
```typescript
action: z.enum(['gather', 'explore', 'navigate', 'idle', 'chat'])
target: z.string().max(64).optional()  // alvo de alto nível textual
```

CognitiveState (src/cognition/types.ts:6-13): 'idle'|'exploring'|'gathering'|'socializing'|'reflecting'|'fighting'|'building'

actionToCognitiveState (src/cognition/nodes.ts:61-74) — switch action→CognitiveState (atualmente só os 5 verbos).

execute node dispatch (src/cognition/nodes.ts:184-225) — mapeia state→(skill,target). Hoje:
  gathering → skill='dig', target=bloco
  exploring → skill='navigate', target=JSON {x,y,z}
  socializing → skill='navigate' se longe do player
A montagem de params está em :286-288:
```typescript
const params = skill === 'dig'
  ? { target, signal: skillAbort.signal }
  : { target: JSON.parse(target), signal: skillAbort.signal }
const result = await skillRegistry[skill!]!(bot, params)
```
llmTarget disponível em :179-180:
```typescript
const llmTarget = fresh && now() - fresh.at < config.replanMinIntervalMs * 2
  ? fresh.decision.target : undefined
```

Param-schemas das 4 skills (cada skill faz seu próprio .parse() — o execute só precisa passar o objeto certo + signal):
  craft  (src/skills/craft.ts:28-31):  { itemName: string, count: number(1-64, default 1), signal? }
  smelt  (src/skills/smelt.ts:27-30):  { oreName: string, count: number(1-64, default 1), signal? }
  equip  (src/skills/equip.ts:18-24):  { itemName: string, destination?: 'hand'|'head'|'torso'|'legs'|'feet'|'off-hand', signal? }
  placeBlock (src/skills/placeBlock.ts:130-135): { target: {x:number,y:number,z:number}, itemName: string, signal? }

skillRegistry (src/skills/index.ts:52-64): tem 'navigate','dig','follow','attack','eat','flee','shelter','placeBlock','craft','smelt','equip'.

MemEvent action (src/cognition/types.ts:21-31): { type:'action', skill:string, target:string, outcome:SkillOutcome, observed, expected, result:'success'|'failure', reason?, timestamp }. O execute (:305-315) JÁ grava isto do result.outcome — o branch novo só precisa setar (skill, target) corretos; o caminho grounded/memória é reusado SEM alteração.

SkillResult (src/grounding/types.ts:34-44): { outcome:'success'|'partial'|'no_effect'|'error', observed, expected, delta, reason? }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Abrir o enum de ação (craft/smelt/equip/place) mantendo-o FECHADO</name>
  <files>src/llm/schemas.ts</files>
  <read_first>
    - src/llm/schemas.ts (a forma exata do enum, o .describe do action e do target, e o comentário LLM-02/D-10 no topo — preservar a invariante "LLM escolhe ação+alvo de alto nível, nunca params físicos")
    - src/cognition/nodes.ts:61-74 (actionToCognitiveState — para nomear os verbos de forma consistente com o mapeamento que a Task 2 fará)
  </read_first>
  <behavior>
    - ActionDecisionSchema.parse({action:'craft', target:'wooden_pickaxe', reason:'x'}) → válido
    - ActionDecisionSchema.parse({action:'smelt', target:'iron_ore', reason:'x'}) → válido
    - ActionDecisionSchema.parse({action:'equip', target:'stone_pickaxe', reason:'x'}) → válido
    - ActionDecisionSchema.parse({action:'place', target:'crafting_table @ 10,64,-3', reason:'x'}) → válido
    - ActionDecisionSchema.parse({action:'mine_diamonds', ...}) → THROW (enum continua FECHADO — LLM-02)
    - target continua z.string().max(64).optional() (NÃO virar objeto — params físicos são montados na cognição, D-10)
  </behavior>
  <action>
    Estender o enum de `action` em ActionDecisionSchema (linha ~23) adicionando os 4 verbos:
    `z.enum(['gather', 'explore', 'navigate', 'idle', 'chat', 'craft', 'smelt', 'equip', 'place'])`.
    Manter o enum FECHADO (NÃO trocar por z.string()) — a invariante LLM-02/D-10 do comentário do topo
    do arquivo continua: o LLM escolhe verbo+alvo de alto nível, os params físicos são validados pelos
    schemas das skills DEPOIS.

    Atualizar o `.describe()` do `action` (linha ~24-27) acrescentando os novos verbos, ex:
    `'... craft=craftar item (target=nome do item, opcional :N para quantidade); smelt=fundir minério ' +
     '(target=nome do minério); equip=equipar ferramenta/armadura (target=nome do item); ' +
     'place=colocar bloco (target="nome @ x,y,z")'`.

    Atualizar o `.describe()` do `target` (linha ~33-36) documentando o formato por verbo (será o contrato
    de parse da Task 2):
      - craft: nome do item, opcionalmente "nome:N" (ex: "stick", "wooden_pickaxe:1")
      - smelt: nome do minério (ex: "iron_ore", "raw_iron")
      - equip: nome do item (ex: "stone_pickaxe"); opcionalmente "nome@slot" (head/torso/legs/feet/off-hand)
      - place: "nome_do_bloco @ x,y,z" (ex: "cobblestone @ 10,64,-3")
    Manter os formatos existentes (gather/navigate/explore/chat) intactos.

    Atualizar o comentário-cabeçalho do mapeamento ação→estado (linhas ~5-12) acrescentando as 4 novas
    linhas (craft/smelt/equip/place → 'building' — ver Task 2). NÃO adicionar lógica nova ao arquivo;
    schemas.ts continua só declaração de schema.
  </action>
  <verify>
    <automated>bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/llm/schemas.ts` contém `'craft'`, `'smelt'`, `'equip'`, `'place'` DENTRO do `z.enum([...])` de `action` (grep: `z.enum\(\[.*'craft'.*'place'`)
    - O `target` permanece `z.string().max(64).optional()` (NÃO virou objeto)
    - `bunx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>O enum aceita os 4 verbos novos e continua fechado; describe documenta o formato de target por verbo; tsc limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fiar os 4 verbos ao dispatch no execute node (montar params físicos do target)</name>
  <files>src/cognition/nodes.ts</files>
  <read_first>
    - src/cognition/nodes.ts (LEITURA OBRIGATÓRIA INTEIRA: actionToCognitiveState :61-74, o branch de dispatch state→(skill,target) :184-225, a montagem de params :286-291, e o caminho de grounding/memória :293-319 que será REUSADO sem alteração)
    - src/skills/craft.ts:28-45 (CraftSchema + extração de signal antes do Zod — confirmar a forma {itemName,count,signal})
    - src/skills/smelt.ts:27-32,107-110 (SmeltSchema {oreName,count})
    - src/skills/equip.ts:18-24,76-82 (EquipSchema {itemName,destination?})
    - src/skills/placeBlock.ts:130-150 (PlaceBlockSchema {target:{x,y,z},itemName})
    - src/skills/index.ts:52-64 (skillRegistry tem craft/smelt/equip/placeBlock — confirmar os nomes-chave EXATOS: a skill de colocar bloco está registrada como 'placeBlock', não 'place')
  </read_first>
  <behavior>
    - state derivado de action='craft' → execute despacha skillRegistry['craft'] com params {itemName, count, signal}; grava MemEvent {type:'action', skill:'craft', outcome do result}
    - action='smelt' → skillRegistry['smelt'] com {oreName, count, signal}
    - action='equip' → skillRegistry['equip'] com {itemName, destination?, signal}
    - action='place' → skillRegistry['placeBlock'] com {target:{x,y,z}, itemName, signal}
    - target ausente/não-parseável (ex: place sem "@ x,y,z", craft com string vazia) → nenhuma skill é disparada (log "sem acao"), tick continua via actionFinished (NÃO lança)
    - o caminho de grounding/memória existente (:293-319) é reusado: o branch novo só seleciona skill + monta params; o recordEvent/holder.lastObservedDelta continua derivando de result.outcome
  </behavior>
  <action>
    PARTE A — actionToCognitiveState (:61-74): adicionar os 4 cases mapeando para 'building'
    (estado já existente como stub em CognitiveState — reusar, NÃO criar estado novo no grafo):
      `case 'craft': case 'smelt': case 'equip': case 'place': return 'building'`
    (escolha de 'building' porque os 4 são ações de construção/produção; mantém a Fase 10 livre para
    refinar a granularidade de estado sem mexer aqui).

    PARTE B — dispatch no execute (:184-225): adicionar UM branch novo APÓS o branch socializing e ANTES
    do comentário `// idle / fighting(stub) / building(stub)`. O branch dispara quando `state === 'building'`
    E há `llmTarget` (a decisão LLM fresca carrega o alvo). Reusar a var `llmTarget` já calculada em :179-180.

    Resolver o VERBO a partir de `fresh.decision.action` (não do state, que é 'building' agregado):
    ```typescript
    } else if (snap && state === 'building' && fresh) {
      const verb = fresh.decision.action
      const raw = (llmTarget ?? '').trim()
      if (verb === 'craft' && raw) {
        // "item" ou "item:N"
        const [name, n] = raw.split(':')
        const count = Math.min(64, Math.max(1, parseInt(n ?? '1', 10) || 1))
        skill = 'craft'; target = JSON.stringify({ itemName: name.trim(), count })
      } else if (verb === 'smelt' && raw) {
        const [name, n] = raw.split(':')
        const count = Math.min(64, Math.max(1, parseInt(n ?? '1', 10) || 1))
        skill = 'smelt'; target = JSON.stringify({ oreName: name.trim(), count })
      } else if (verb === 'equip' && raw) {
        // "item" ou "item@slot"
        const [name, slot] = raw.split('@')
        const dest = slot?.trim()
        skill = 'equip'
        target = JSON.stringify(dest ? { itemName: name.trim(), destination: dest } : { itemName: name.trim() })
      } else if (verb === 'place' && raw) {
        // "nome @ x,y,z" — exige posição; sem ela, NÃO dispara (degrada para sem-ação)
        const [name, posStr] = raw.split('@')
        const coords = posStr?.split(',').map((c) => parseInt(c.trim(), 10))
        if (coords && coords.length === 3 && coords.every((c) => Number.isFinite(c))) {
          skill = 'placeBlock'
          target = JSON.stringify({ target: { x: coords[0], y: coords[1], z: coords[2] }, itemName: name.trim() })
        }
      }
    }
    ```
    Notas de implementação OBRIGATÓRIAS:
    - A chave do registry para colocar bloco é `'placeBlock'` (não `'place'`) — confirmar em index.ts.
    - `target` é uma STRING JSON (mesmo padrão do branch exploring/socializing que já faz JSON.stringify);
      a montagem final de params na :286-291 precisa fazer JSON.parse para esses verbos.

    PARTE C — montagem de params (:286-291): hoje é `skill === 'dig' ? {target, signal} : {target: JSON.parse(target), signal}`.
    Os 4 verbos novos passam um OBJETO de params completo serializado como JSON (não um {target}). Trocar
    para spread do objeto parseado + signal:
    ```typescript
    const SKILL_TARGET_IS_RAW = skill === 'dig' // dig recebe target string cru
    const params = SKILL_TARGET_IS_RAW
      ? { target, signal: skillAbort.signal }
      : skill === 'navigate'
        ? { target: JSON.parse(target), signal: skillAbort.signal }
        : { ...JSON.parse(target), signal: skillAbort.signal } // craft/smelt/equip/placeBlock: params completos
    ```
    (navigate continua recebendo {target: {x,y,z}}; os 4 novos recebem os campos no topo: itemName/count,
     oreName/count, itemName/destination, ou target/itemName.)

    NÃO TOCAR: o caminho de grounding/memória (:293-319), o anti-repetição (:236-256), os listeners de
    preempção (:270-280), o emit actionFinished (:348). Eles já derivam tudo de result.outcome e funcionam
    para qualquer skill do registry. NÃO adicionar lógica de "o que craftar/fundir" — o verbo+alvo vêm
    prontos do LLM (isso é Phase 10).
  </action>
  <verify>
    <automated>bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/cognition/nodes.ts` tem em actionToCognitiveState os cases `'craft'`/`'smelt'`/`'equip'`/`'place'` retornando `'building'` (grep: `case 'craft':`)
    - `src/cognition/nodes.ts` tem um branch `state === 'building'` que seta `skill = 'craft'`, `skill = 'smelt'`, `skill = 'equip'` e `skill = 'placeBlock'` (grep: `skill = 'placeBlock'`)
    - A montagem de params faz spread `...JSON.parse(target)` para os verbos novos (grep: `\.\.\.JSON\.parse\(target\)`)
    - `bunx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>action='craft'/'smelt'/'equip'/'place' fresca → execute despacha a skill correta do skillRegistry com params montados do target; target inválido degrada para sem-ação sem lançar; grounding/memória reusados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Teste agent-level — cada verbo despacha a skill certa e grava memória grounded</name>
  <files>src/cognition/nodes.test.ts</files>
  <read_first>
    - src/cognition/nodes.ts (o execute node final das Tasks 1-2: como NodeDeps é montado — bot/holder/provider/triggerBus — e como holder.llmDecision/holder.lastObservedDelta/holder.memory são lidos/escritos)
    - src/skills/index.ts (skillRegistry — o teste precisa interceptar/injetar skillRegistry.craft etc.; ver se há seam de injeção ou se o teste monkeypatcha o registry importado)
    - src/skills/craft.test.ts OU src/skills/equip.test.ts (padrão de mock de bot + convenção de injeção SEM mock.module — "vaza global no bun"; ver __craftDeps/__smeltDeps como precedente de seam)
    - src/cognition/state.ts (CognitiveStateHolder — montar um holder mínimo com llmDecision/memory/safety/control para o teste)
    - src/grounding/types.ts (SkillResult — formato do retorno mockado de cada skill)
  </read_first>
  <behavior>
    - Teste "craft dispatch": holder.llmDecision = {decision:{action:'craft', target:'wooden_pickaxe:1', reason:'x'}, at: now}; após execute, skillRegistry.craft foi chamado com bot e params contendo {itemName:'wooden_pickaxe', count:1}; um MemEvent {type:'action', skill:'craft', outcome:'success'} foi gravado (holder.memory / recordEvent observável)
    - Teste "smelt dispatch": target 'iron_ore' → skillRegistry.smelt chamado com {oreName:'iron_ore', count:1}
    - Teste "equip dispatch": target 'stone_pickaxe' → skillRegistry.equip chamado com {itemName:'stone_pickaxe'}
    - Teste "place dispatch": target 'cobblestone @ 10,64,-3' → skillRegistry.placeBlock chamado com {target:{x:10,y:64,z:-3}, itemName:'cobblestone'}
    - Teste "grounded memory": a skill mockada retorna outcome:'no_effect', observed:0 → o MemEvent gravado tem result:'failure' e outcome:'no_effect' (prova que a memória deriva do SkillResult, não do não-throw — D-09 B reusado)
    - Teste "place sem posição não despacha": target 'cobblestone' (sem @x,y,z) → nenhuma skill de craft/place chamada; actionFinished emitido com skill:null
  </behavior>
  <action>
    Criar `src/cognition/nodes.test.ts` cobrindo o caminho agent-level de dispatch dos 4 verbos novos.

    Setup (seguir a convenção de injeção SEM mock.module — ver __craftDeps em craft.ts / o padrão dos
    testes de skill): montar `createNodes(deps)` com:
      - `bot` mock mínimo (o suficiente para o execute: snapshot via buildWorldSnapshot é a parte difícil —
        preferir injetar o snapshot diretamente no LoopState passado a execute, já que execute lê s.snapshot,
        não chama buildWorldSnapshot; só observe chama). Passar um snapshot mínimo válido (status.position).
      - `holder` mínimo: { control: getMode()=> 'autonomous', safety: stub que não abandona, memory, db:null,
        currentGoal:null, llmDecision: <a decisão sob teste>, lastObservedDelta:null }.
      - `provider` stub (não usado por execute).
      - `triggerBus` mock com on/once/off/emit (registrar os listeners de preempção e capturar actionFinished).
    Interceptar as skills: substituir `skillRegistry.craft/smelt/equip/placeBlock` por mocks que registram
    os args e retornam um SkillResult fixo, restaurando no afterEach (ou usar o seam de injeção se existir).
    IMPORTANTE: o execute chama `skillRegistry[skill]!(bot, params)` pelo nome — então o teste deve
    sobrescrever as entradas do objeto skillRegistry importado e restaurá-las (padrão de monkeypatch
    pontual; NÃO mock.module).

    Asserções por teste (ver <behavior>): o mock da skill recebeu params com os campos físicos corretos
    (itemName/count, oreName/count, itemName/destination, target/itemName — descartar `signal` na asserção);
    o MemEvent gravado tem skill/outcome/result esperados; no caso 'place sem posição', a skill NÃO foi
    chamada e actionFinished saiu com skill:null.

    Garantir freshness: setar `holder.llmDecision.at = Date.now()` para passar o gate
    `now() - fresh.at < config.replanMinIntervalMs * 2` (senão llmTarget é undefined e o branch não dispara).
  </action>
  <verify>
    <automated>bun test src/cognition/nodes.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/cognition/nodes.test.ts` existe e contém `skillRegistry.craft` (ou referência de monkeypatch ao skillRegistry) e asserções dos params {itemName, count} / {oreName} / {target:{x,y,z}, itemName}
    - O teste assert que um MemEvent action com outcome derivado do SkillResult mock é gravado (grep: `outcome`)
    - `bun test src/cognition/nodes.test.ts` → todos verdes, 0 fail
    - `bunx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Suite agent-level prova: cada verbo novo despacha a skill correta do skillRegistry com params montados do target, a memória deriva do SkillResult (no_effect→failure), e place sem posição não dispara. Verde.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit` → exit 0 (sem regressão de tipo após estender o enum e o dispatch).
- `bun test src/cognition/nodes.test.ts` → novo arquivo verde (dispatch dos 4 verbos + memória grounded).
- `bun test` → suite global continua verde (420+ pass; o branch novo é aditivo, não toca dig/navigate/socializing).
- grep `z.enum(\[.*'place'` em src/llm/schemas.ts → enum estendido e ainda fechado.
- grep `skill = 'placeBlock'` em src/cognition/nodes.ts → dispatch do place fiado.
- Confirmar (grep) que NÃO há lógica de "decidir o que craftar/fundir com base em needs/tech-tree" no diff — o verbo+alvo vêm prontos do LLM (boundary da Phase 10 respeitada).
</verification>

<success_criteria>
- G-01 fechado no nível comportamental: dada uma decisão LLM com action ∈ {craft,smelt,equip,place}, o
  execute node despacha skillRegistry.{craft,smelt,equip,placeBlock} com params físicos montados do target
  e grava MemEvent grounded (outcome do SkillResult).
- ActionDecisionSchema continua FECHADO (rejeita verbos fora do enum) — LLM-02/D-10 preservados.
- Target inválido degrada para sem-ação sem lançar (Core Value: o tick nunca cai).
- Nenhuma lógica de tech-tree/priorização-por-needs introduzida (Phase 10 intacta).
- Os planos 09-01..09-04 não foram tocados.
</success_criteria>

<output>
After completion, create `.planning/phases/09-placement-crafting-smelting-grounded/09-05-SUMMARY.md`
</output>
