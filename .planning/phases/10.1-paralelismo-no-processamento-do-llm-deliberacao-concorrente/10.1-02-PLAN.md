---
phase: 10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/cognition/deliberation.ts
  - src/cognition/deliberation.test.ts
  - src/cognition/loop.ts
  - src/cognition/loop.reflect-priority.test.ts
  - src/cognition/concurrency-wiring.test.ts
autonomous: true
requirements:
  - CONC-WIRE      # trocar inFlight por gate-por-tipo + semáforo no loop; rotear handleConversation (D-01/D-08)
  - CONC-MERGE     # commit síncrono + merge-by-id protege holder.goals do clobber (D-04/D-05/D-06)
  - CONC-PREEMPT   # player preempta AÇÃO via AbortController; reflexão protegida (D-11/D-12/D-13)

must_haves:
  truths:
    - "Com um goal de player_request empurrado em holder.goals DURANTE o await da reflexão, esse goal SOBREVIVE ao commit da reflexão (não é clobberado) — Pitfall 2 fechado (D-05/D-06)"
    - "maybeDeliberate não tem mais await entre o read e o write de holder.goals na reflexão (commit 100% síncrono) (D-04)"
    - "O gate por tipo permite AÇÃO e REFLEXÃO coexistirem quando o semáforo tem permits>=2 (deixa de ser XOR como o pickDispatch antigo) (D-01/Pitfall 6)"
    - "handleConversation passa pelo mesmo gate/semáforo que a deliberação (não dispara mais LLM sem coordenação) (D-08)"
    - "Um sinal de preempção (AbortController da ação) aborta a deliberação de AÇÃO em voo e libera o slot via finally; a ação re-despacha no próximo tick (D-12)"
    - "A REFLEXÃO nunca recebe signal de preempção (roda até o flush B2) e nunca starva (piso shouldReflect preservado) (D-13)"
    - "release() é chamado em try/finally em TODOS os caminhos de aquisição (sem permit leak após throw/abort) (Pitfall 3)"
  artifacts:
    - path: "src/cognition/deliberation.ts"
      provides: "DeliberationState sem inFlight booleano único; maybeDeliberate sob gate+semáforo; runReflection com commit síncrono merge-by-id"
      contains: "Semaphore"
    - path: "src/cognition/loop.ts"
      provides: "Semaphore(provider.maxConcurrency)+gate instanciados na sessão; handleConversation roteado pelo gate; AbortController de ação para preempção do player"
      contains: "createTaskGate"
  key_links:
    - from: "src/cognition/loop.ts"
      to: "src/cognition/concurrency.ts (Semaphore/createTaskGate)"
      via: "import + instanciação por sessão dimensionada por provider.maxConcurrency"
      pattern: "new Semaphore\\(provider.maxConcurrency\\)|createTaskGate"
    - from: "src/chat/conversation.ts (handleConversation)"
      to: "o gate/semáforo do loop"
      via: "loop.ts envolve a chamada void handleConversation com acquire(player)/finally release()"
      pattern: "handleConversation"
    - from: "src/cognition/deliberation.ts (runReflection)"
      to: "holder.goals (re-leitura no commit síncrono)"
      via: "applyGoalUpdates(holder.goals, updates, now) SEM await entre read e write"
      pattern: "holder.goals = applyGoalUpdates"
---

<objective>
Fiar as primitivas do Plan 01 no LOOP COGNITIVO: substituir o lock single-flight `inFlight: boolean` por gate-por-tipo + semáforo global (`= provider.maxConcurrency`), rotear TODOS os caminhos LLM (inclusive `handleConversation`, hoje `void` fora do lock) pelo mesmo gate, blindar `holder.goals` do clobber concorrente via commit síncrono + merge-by-id, e habilitar a preempção jogador→ação via AbortController com a reflexão protegida (nunca abortada, nunca starvada).

Purpose: É a entrega comportamental da fase — a contenção que faz a reflexão starvar e a brecha do chat-sem-coordenação somem; a infra de concorrência que a Phase 11 (autônomo+assistente) e a Phase 14 (refletir enquanto age) exigem fica pronta. Com `maxConcurrency=1` o comportamento degrada ao single-flight priorizado de hoje + preempção (D-03); com >1 (LM Studio batching / cloud) as tarefas distintas sobrepõem de verdade.

Output: `deliberation.ts` (gate+semáforo, commit síncrono), `loop.ts` (instanciação por sessão, roteamento do chat, AbortController de ação), e testes determinísticos do clobber e da preempção.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente/10.1-CONTEXT.md
@.planning/phases/10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente/10.1-RESEARCH.md
@.planning/phases/10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente/10.1-01-SUMMARY.md

<interfaces>
<!-- Contratos do Plan 01 (já implementados). Use-os diretamente. -->

De src/cognition/concurrency.ts (Plan 01):
```typescript
export type TaskType = 'action' | 'reflection' | 'player'
export class Semaphore {
  constructor(permits: number)
  acquire(priority: number): Promise<void>   // menor número = mais urgente (player=0, action=1, reflection=2)
  release(): void                            // SEMPRE no finally
}
export function createTaskGate(): { tryEnter(t: TaskType): boolean; leave(t: TaskType): void; isBusy(t: TaskType): boolean }
```

De src/llm/provider.ts (Plan 01):
```typescript
export interface LlmProvider {
  readonly maxConcurrency: number
  decide<T>(schema: ZodType<T>, messages: BaseMessage[], opts?: { signal?: AbortSignal }): Promise<T>
  chat(messages: BaseMessage[], opts?: { signal?: AbortSignal }): Promise<string>
  available(): Promise<boolean>
  embed(text: string): Promise<number[]>
}
```

Estado ATUAL a refatorar (src/cognition/deliberation.ts):
- DeliberationState (L71-75): `{ inFlight: boolean; lastRunAt: number }` — o `inFlight` único vira gate-por-tipo.
- maybeDeliberate (L139-204): L148 `if (state.inFlight) return false`; L155 `state.inFlight = true`; L157 ramo reflect; L160-198 ramo ação; L199-201 finally reseta inFlight.
- runReflection (L217-282): L242 `await provider.decide(...)` é o await longo; L278 `holder.goals = applyGoalUpdates(holder.goals, updates, now)` é o commit — HOJE captura holder.goals pós-await mas com risco se houvesse await entre read e write; garantir que NÃO há await entre a obtenção de `updates` e o write.
- createDeliberator (L288-302): instancia o state.

Estado ATUAL a refatorar (src/cognition/loop.ts):
- pickDispatch (L478-489): `inFlight ? 'none' : reflectDue ? 'reflect' : 'action'` — XOR por tick a substituir.
- dispatch no while (L376-431): bloco que chama pickDispatch e dispara reflect XOR ação.
- handleConversation via void FORA do lock (L218-220): `void handleConversation(provider, holder, bot, username, message, Date.now())`.
- startCognitiveLoop (L152-467): onde instanciar o Semaphore+gate por sessão (perto de `const deliberator = createDeliberator()` L157).

applyGoalUpdates (src/cognition/reflection.ts L101-124) JÁ É merge-by-id imutável: goals sem id em `updates` caem no ramo "inalterado" (L112-113) e sobrevivem. NÃO precisa mudar — só garantir que recebe `holder.goals` ATUAL no instante síncrono do write.

makePlayerRequestGoal (src/chat/conversation.ts L68-78): cria goal `player_request:${kind}:${now}` empurrado em holder.goals.push (L160) — o escritor concorrente que o merge-by-id protege.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trocar inFlight por gate-por-tipo + semáforo em deliberation.ts; commit síncrono merge-by-id (D-01/D-04/D-05/D-13)</name>
  <files>src/cognition/deliberation.ts, src/cognition/deliberation.test.ts</files>
  <read_first>
    - src/cognition/deliberation.ts (INTEIRO — DeliberationState L71-75, maybeDeliberate L139-204, runReflection L217-282, createDeliberator L288-302)
    - src/cognition/concurrency.ts (Plan 01 — Semaphore/createTaskGate/TaskType: assinaturas a consumir)
    - src/cognition/reflection.ts (applyGoalUpdates L101-124 — confirmar que é merge-by-id; shouldReflect L37-48 — piso anti-starvation a preservar)
    - src/cognition/deliberation.test.ts (INTEIRO — testes single-flight existentes que serão reescritos para gate+semáforo; okProvider já tem maxConcurrency:1 do Plan 01)
    - .planning/phases/10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente/10.1-RESEARCH.md (§Architecture Pattern 2 e Pattern 3 — gate por tipo e commit síncrono merge-by-id)
  </read_first>
  <behavior>
    - Test (clobber — o coração da fase): chamar runReflection com um provider cujo `decide` resolve depois de um await; DURANTE esse await, fazer `holder.goals.push(makePlayerRequestGoal('gather', now))`; após runReflection terminar, o goal `player_request:gather:*` AINDA está em holder.goals (não foi clobberado). Mesmo quando `updates` da reflexão dropa/reprioriza OUTROS goals. (D-05/D-06/Pitfall 2)
    - Test (commit síncrono): asserir (por inspeção do código via teste de comportamento) que `updates` é obtido ANTES e `holder.goals = applyGoalUpdates(holder.goals, updates, now)` é a única reatribuição, sem await no meio. Verificável: empurrar um goal entre o await e o commit usando um provider que resolve numa microtask controlada e checar sobrevivência (mesmo teste acima cobre).
    - Test (gate por tipo): maybeDeliberate com trigger 'reflect' enquanto gate.reflection está busy → no-op (return false); com gate.action busy mas gate.reflection livre, 'reflect' AINDA dispara (tipos independentes — D-01). Substitui o "single-flight: inFlight=true não dispara" antigo.
    - Test (semáforo permits=1): com o semáforo esgotado (permit já adquirido por outra tarefa), maybeDeliberate aguarda o release antes de chamar o provider (serialização emerge — D-03). Com permits>=2, ação e reflexão coexistem.
    - Test (finally libera): forçar throw dentro da seção crítica → o gate.leave e o sem.release são chamados (gate volta livre, permits volta ao valor inicial — Pitfall 3).
  </behavior>
  <action>
    Refatorar `src/cognition/deliberation.ts` para substituir o lock booleano único por gate-por-tipo + semáforo (D-01), mantendo o commit do `holder.goals` 100% síncrono (D-04/D-05):

    1. `DeliberationState` (L71-75): REMOVER `inFlight: boolean`. Manter `lastRunAt: number`. O controle de sobreposição passa a ser o `gate` + `semaphore`, injetados em maybeDeliberate. Nova forma:
       ```typescript
       export interface DeliberationState { lastRunAt: number }
       ```

    2. `maybeDeliberate` (L139-204): adicionar DOIS parâmetros — `gate: ReturnType<typeof createTaskGate>` e `semaphore: Semaphore` — e um `signal?: AbortSignal` (propagado só ao caminho de AÇÃO). Mapear trigger → TaskType: `'reflect'` → `'reflection'`; demais → `'action'`. Lógica:
       ```
       const taskType: TaskType = trigger === 'reflect' ? 'reflection' : 'action'
       const priority = trigger === 'reflect' ? 2 : 1   // player(0) é roteado pelo loop, não aqui
       // gate por tipo (D-01/D-12: não sobrepor o mesmo tipo)
       if (!gate.tryEnter(taskType)) return false
       // orçamento de replan SÓ para ação (preserva D-13: reflexão não é limitada pelo budget)
       if (taskType === 'action' && now - state.lastRunAt < config.replanMinIntervalMs) { gate.leave(taskType); return false }
       if (!shouldTrigger(trigger, holder)) { gate.leave(taskType); return false }
       await semaphore.acquire(priority)
       try {
         if (trigger === 'reflect') await runReflection(holder, provider, snapshot, now, chroma)  // SEM signal (D-13)
         else { /* caminho de AÇÃO — passa signal a decideAction/provider.decide (D-12) */ }
       } finally {
         semaphore.release()    // Pitfall 3: SEMPRE
         gate.leave(taskType)   // libera o tipo
         state.lastRunAt = now
       }
       return true
       ```
       O caminho de AÇÃO (hoje L160-198) permanece igual EXCETO que a chamada estruturada propaga o `signal`: `decideAction(provider, messages, fallback)` deve repassar o signal — ver Task 2 (o loop cria o AbortController e passa o signal). Se `decideAction` ainda não aceitar signal, passe o signal a `provider.decide` via o opts já adicionado no Plan 01: adapte `decideAction` para aceitar `opts?: { signal?: AbortSignal }` e repassá-lo a `provider.decide(...)` nas duas chamadas internas (structured.ts L32 e L38). Mantenha `decideAction` nunca-lança.

    3. `runReflection` (L217-282) — garantir COMMIT SÍNCRONO (D-04/D-05): o `await provider.decide(...)` (L242) e o `await provider.embed(...)` (L257) e o `await chroma.addVector(...)` (L268) computam tudo FORA do holder. A reatribuição `holder.goals = applyGoalUpdates(holder.goals, updates, now)` (L278) DEVE ler `holder.goals` no instante do write (já é o caso — está pós-await), e NÃO pode haver await entre essa leitura e o `persistHolder` (L281). Confirmar/garantir que da L278 à L281 não há `await`. Adicionar comentário citando D-04/D-05/Pitfall 2 explicando que o re-read no commit é o que protege o goal de player_request empurrado durante o await. NUNCA passar signal a runReflection (D-13 — reflexão nunca abortada).

    4. `createDeliberator` (L288-302): remover `inFlight: false` do state inicial (`{ lastRunAt: -Infinity }`). A assinatura de maybeDeliberate retornada no objeto ganha os params `gate`/`semaphore`/`signal?`. (O loop, Task 2, instancia gate+semaphore e os passa.)

    5. Atualizar `src/cognition/deliberation.test.ts`: reescrever os testes que dependiam de `inFlight` (L42-50 single-flight, L85-92 reset, L123-146 concorrente) para o novo modelo gate+semáforo. Criar helper `freshDeps()` que retorna `{ gate: createTaskGate(), semaphore: new Semaphore(1) }` e passá-lo a maybeDeliberate. Adicionar o TESTE DE CLOBBER do bloco <behavior> (push concorrente durante o await sobrevive). Os testes de orçamento de replan / shouldTrigger / fallback ao arbiter permanecem (ajustando a assinatura).
  </action>
  <verify>
    <automated>cd /c/Users/biel1/OneDrive/Documentos/GitHub/MineMind && bun test src/cognition/deliberation.test.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "inFlight" src/cognition/deliberation.ts` retorna VAZIO (o booleano único foi removido)
    - `grep -q "gate.tryEnter" src/cognition/deliberation.ts` retorna 0
    - `grep -q "semaphore.acquire" src/cognition/deliberation.ts` retorna 0
    - `grep -q "semaphore.release()" src/cognition/deliberation.ts` retorna 0 (no finally)
    - `grep -q "gate.leave" src/cognition/deliberation.ts` retorna 0 (no finally)
    - `grep -q "holder.goals = applyGoalUpdates(holder.goals, updates, now)" src/cognition/deliberation.ts` retorna 0 (commit re-lê holder.goals)
    - Entre a linha do `holder.goals = applyGoalUpdates(...)` e o `persistHolder(...)` NÃO existe `await` (commit síncrono D-04) — verificar por leitura
    - deliberation.test.ts tem um teste cujo nome contém "clobber" ou "player_request" provando a sobrevivência do goal: `grep -iE "clobber|player_request.*sobreviv|sobreviv.*goal" src/cognition/deliberation.test.ts` retorna 0
    - `bun test src/cognition/deliberation.test.ts` passa (incluindo o teste de clobber)
    - `bunx tsc --noEmit` verde
  </acceptance_criteria>
  <done>DeliberationState sem inFlight; maybeDeliberate roda sob gate-por-tipo (não sobrepõe o mesmo tipo) + semáforo (teto global), com release/leave no finally; runReflection comita holder.goals re-lendo o array no instante síncrono (merge-by-id protege o goal de player); reflexão nunca recebe signal; suíte verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fiar Semaphore+gate no loop por sessão; rotear handleConversation; preempção player→ação via AbortController (D-08/D-11/D-12/D-13)</name>
  <files>src/cognition/loop.ts, src/cognition/loop.reflect-priority.test.ts, src/cognition/concurrency-wiring.test.ts</files>
  <read_first>
    - src/cognition/loop.ts (INTEIRO — startCognitiveLoop L152, deliberator L157, handler de chat L205-221 com void handleConversation L218-220, bloco de dispatch L376-431, pickDispatch L478-489, pickTrigger L491-505)
    - src/cognition/deliberation.ts (a nova assinatura de maybeDeliberate da Task 1 — gate/semaphore/signal)
    - src/cognition/concurrency.ts (Plan 01 — Semaphore/createTaskGate)
    - src/chat/conversation.ts (handleConversation L90-163 — o caminho de chat a rotear pelo gate 'player')
    - src/cognition/loop.reflect-priority.test.ts (INTEIRO — testa pickDispatch; precisará ajuste já que pickDispatch deixa de ser XOR puro)
    - .planning/phases/10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente/10.1-RESEARCH.md (§Code Examples — "Driver: AÇÃO preemptável + REFLEXÃO protegida"; §Pitfall 6 — gate não pode virar single-flight de novo)
  </read_first>
  <behavior>
    - Test (roteamento do chat): handleConversation disparado pelo handler de chat passa pelo acquire(0)/finally release() do semáforo da sessão — verificável por um teste de unidade da função wrapper (extrair a lógica de roteamento numa função testável, ex.: `routePlayerTurn(semaphore, gate, fn)`), asserindo que o gate 'player' é entrado e o semáforo adquirido/liberado mesmo quando fn lança (D-08/Pitfall 3).
    - Test (preempção): quando há um turno de player a despachar e uma AÇÃO está em voo, o AbortController da ação é abortado (actionAbort.abort()) ANTES do player adquirir, e o slot da ação é liberado via finally; a ação re-despacha no próximo tick (D-12). Verificável por uma função pura `shouldPreemptAction(hasPlayerTurn, actionInFlight)` → true.
    - Test (reflexão protegida): o caminho de reflexão NUNCA recebe um signal e NUNCA é abortado pela chegada de um player (D-13). Verificável: a reflexão é despachada com priority=2 e sem AbortController.
    - Test (pickDispatch deixa de ser XOR): com permits>=2, action e reflection podem ambos ser despachados (o gate por tipo, não o pickDispatch, é quem coordena). Ajustar/substituir os testes de pickDispatch para o novo modelo OU remover pickDispatch se a coordenação migrar inteiramente para gate+semáforo (documentar a escolha).
  </behavior>
  <action>
    Fiar as primitivas no driver `src/cognition/loop.ts` (D-08/D-12/D-13), extraindo helpers PUROS para testabilidade:

    1. Em `startCognitiveLoop`, logo após `const deliberator = createDeliberator()` (L157), instanciar POR SESSÃO:
       ```typescript
       // Fase 10.1 (D-01/D-07): semáforo global dimensionado pela capacidade do provider + gate por tipo.
       // permits=1 ⇒ single-flight priorizado + preempção (D-03); >1 ⇒ tarefas distintas sobrepõem.
       const llmSemaphore = new Semaphore(provider.maxConcurrency)
       const taskGate = createTaskGate()
       // D-12: AbortController da AÇÃO em voo — recriado a cada dispatch de ação; o player o aborta.
       let actionAbort: AbortController | null = null
       ```
       Importar `Semaphore, createTaskGate` de `./concurrency`.

    2. ROTEAR `handleConversation` pelo gate/semáforo (D-08) — o handler de chat (L205-221) hoje faz `void handleConversation(...)` FORA do lock. Extrair uma função PURA testável no módulo:
       ```typescript
       // D-08/D-11: todo turno conversacional atravessa o mesmo gate/semáforo (prioridade player=0).
       // Pitfall 3: release/leave SEMPRE no finally. Se o gate 'player' já estiver ocupado, descarta o turno
       // (não enfileira chat duplicado — o jogador re-fala). Antes de adquirir, PREEMPTA a ação em voo (D-12).
       export async function routePlayerTurn(
         semaphore: Semaphore, gate: ReturnType<typeof createTaskGate>,
         preemptAction: () => void, run: () => Promise<void>,
       ): Promise<void> {
         if (!gate.tryEnter('player')) return
         preemptAction()                 // D-12: aborta a AÇÃO em voo para liberar o slot
         await semaphore.acquire(0)       // prioridade máxima
         try { await run() } finally { semaphore.release(); gate.leave('player') }
       }
       ```
       No handler de chat (L218-220), trocar `void handleConversation(...)` por:
       ```typescript
       void routePlayerTurn(llmSemaphore, taskGate, () => actionAbort?.abort(),
         () => handleConversation(provider, holder, bot, username, message, Date.now()))
       ```

    3. PREEMPÇÃO da AÇÃO (D-12) no dispatch de ação (L418-429): antes de despachar a ação, criar `actionAbort = new AbortController()` e passar `actionAbort.signal` a `maybeDeliberate` (que o repassa ao caminho de AÇÃO via decideAction → provider.decide). Ao terminar (no `.then`/finally do dispatch), limpar `actionAbort = null`. A REFLEXÃO (L409-417) NÃO cria nem passa AbortController (D-13).

    4. Passar `taskGate` e `llmSemaphore` a TODAS as chamadas de `maybeDeliberate` (reflect L409-410 e action L421-428), conforme a nova assinatura da Task 1.

    5. `pickDispatch` (L478-489) e o bloco XOR (L400-431): com gate+semáforo a coordenação muda. Escolha concreta (documentar no SUMMARY): MANTER `pickDispatch` apenas como hint de PRIORIDADE (reflect-due tem precedência ao escolher o que despachar PRIMEIRO no tick), mas NÃO como exclusão mútua — o gate por tipo agora permite ambos coexistirem quando o semáforo tem permits. Reescrever o bloco para: se `reflectDue` e gate.reflection livre → despachar reflect; INDEPENDENTEMENTE, se gate.action livre → despachar ação (não mais XOR). Isto resolve o Pitfall 6 (gate não pode virar single-flight de novo). Ajustar `loop.reflect-priority.test.ts`: os testes de "in-flight ⇒ none" deixam de valer literalmente — substituir por testes do novo comportamento (reflect tem prioridade de PERMIT quando o semáforo é escasso, mas action não é bloqueada por reflect quando há permit).

    6. Criar `src/cognition/concurrency-wiring.test.ts` com os testes do bloco <behavior>: `routePlayerTurn` (entra no gate, adquire/libera, chama preemptAction, libera no finally mesmo com throw) e `shouldPreemptAction`. Usar bot/provider mocks no estilo de `conversation.test.ts` L10-30.
  </action>
  <verify>
    <automated>cd /c/Users/biel1/OneDrive/Documentos/GitHub/MineMind && bun test src/cognition/concurrency-wiring.test.ts src/cognition/loop.reflect-priority.test.ts src/cognition/loop.smoke.test.ts src/cognition/loop.phase3.smoke.test.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "new Semaphore(provider.maxConcurrency)" src/cognition/loop.ts` retorna 0
    - `grep -q "createTaskGate()" src/cognition/loop.ts` retorna 0
    - `grep -q "export async function routePlayerTurn" src/cognition/loop.ts` retorna 0
    - `grep -q "routePlayerTurn(llmSemaphore, taskGate" src/cognition/loop.ts` retorna 0 (o handler de chat usa o roteador)
    - `grep -q "void handleConversation(" src/cognition/loop.ts` retorna VAZIO (não há mais chamada direta fora do gate)
    - `grep -q "actionAbort = new AbortController()" src/cognition/loop.ts` retorna 0
    - `grep -q "actionAbort?.abort()" src/cognition/loop.ts` retorna 0 (preempção do player)
    - O dispatch de REFLEXÃO (trigger 'reflect') NÃO cria AbortController nem passa signal: `grep -A3 "'reflect'" src/cognition/loop.ts` não contém `actionAbort` no ramo reflect — verificar por leitura (D-13)
    - `bun test src/cognition/concurrency-wiring.test.ts src/cognition/loop.reflect-priority.test.ts` passa
    - `bun test src/cognition/loop.smoke.test.ts src/cognition/loop.phase3.smoke.test.ts` passa (sem regressão do driver)
    - `bunx tsc --noEmit` verde
  </acceptance_criteria>
  <done>Semaphore(provider.maxConcurrency)+gate instanciados por sessão; handleConversation roteado por routePlayerTurn (gate 'player', priority 0, preempta a ação, release/leave no finally); ação despachada com AbortController que o player aborta (D-12); reflexão sem signal (D-13); pickDispatch deixa de ser XOR (Pitfall 6); smokes do loop sem regressão; tsc verde.</done>
</task>

</tasks>

<verification>
- `bun test src/cognition` — toda a suíte de cognição verde (deliberation, loop smokes, reflect-priority, concurrency-wiring)
- `bun test` — suíte completa sem regressão (especialmente src/llm e src/chat)
- `bunx tsc --noEmit` — sem erros de tipo
- Teste de clobber (Task 1) e teste de preempção/roteamento (Task 2) presentes e verdes — as duas garantias centrais da fase
- Grep confirma: `inFlight` removido de deliberation.ts; `void handleConversation` removido de loop.ts (roteado pelo gate)
</verification>

<success_criteria>
- O lock booleano único `inFlight` foi substituído por gate-por-tipo + semáforo `= provider.maxConcurrency` (D-01); com permits>=2 ação e reflexão coexistem (deixa de ser XOR — Pitfall 6)
- `handleConversation` atravessa o mesmo gate/semáforo (prioridade player=0) — a brecha do chat-sem-coordenação fechada (D-08)
- `holder.goals` é protegido do clobber: um goal de player empurrado durante o await da reflexão sobrevive ao commit síncrono merge-by-id (D-05/D-06/Pitfall 2) — provado por teste determinístico
- Player preempta a AÇÃO em voo via AbortController (libera o slot via finally; ação re-despacha no próximo tick — D-12); a REFLEXÃO nunca é abortada nem starva (D-13)
- release/leave sempre no finally (sem permit leak — Pitfall 3); zero regressão na suíte completa; tsc verde
</success_criteria>

<output>
Após completar, criar `.planning/phases/10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente/10.1-02-SUMMARY.md` documentando: a forma final do roteamento do chat, a decisão sobre pickDispatch (mantido como hint de prioridade vs. removido), como a preempção foi fiada, e qualquer caveat de validação AO VIVO (LM Studio Max Concurrent Predictions, AbortSignal.any no Bun alvo) a checar num teste manual.
</output>
