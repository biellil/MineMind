---
phase: 10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cognition/concurrency.ts
  - src/cognition/concurrency.test.ts
  - src/llm/provider.ts
  - src/llm/spendStore.ts
  - src/llm/spendStore.test.ts
  - src/llm/spendCap.ts
  - src/llm/spendCap.test.ts
  - src/config.ts
autonomous: true
requirements:
  - CONC-SEM       # gate-por-tipo + semáforo global = provider.maxConcurrency (D-01/D-02/D-03)
  - CONC-PROVIDER  # maxConcurrency na interface + propagação de signal (D-07/D-14)
  - CONC-SPENDCAP  # corrigir TOCTOU do withSpendCap via reserveCall/releaseCall (D-10)

must_haves:
  truths:
    - "Uma instância de Semaphore com permits=1 serializa duas aquisições (a 2ª só resolve após o 1º release) — comportamento single-flight emerge de maxConcurrency=1 (D-03)"
    - "Com permits=2, duas aquisições resolvem sem release intermediário (sobreposição real) (D-03)"
    - "Quando o slot é escasso, acquire de menor número de prioridade (player=0) resolve ANTES de um waiter de maior número (reflection=2) já enfileirado (D-11)"
    - "release() sempre devolve o permit mesmo após throw na seção crítica (try/finally) — permits volta ao valor inicial (Pitfall 3)"
    - "provider.decide/chat aceitam opts?.signal e o repassam ao model.invoke como RunnableConfig.signal (D-14)"
    - "provider.maxConcurrency existe e é lido da config (local ~4, cloud ~3) (D-07/D-09)"
    - "reserveCall incrementa-then-checa atomicamente: 2 reservas concorrentes simuladas (sequenciais no single-thread) na mesma janela com maxCalls=1 — a 1ª retorna true, a 2ª false (D-10)"
    - "releaseCall decrementa o contador com piso 0 (estorno do fallback-to-local) (D-10)"
  artifacts:
    - path: "src/cognition/concurrency.ts"
      provides: "Semaphore (acquire por prioridade/release) + Gate por tipo de tarefa (action/reflection/player)"
      exports: ["Semaphore", "createTaskGate", "TaskType"]
      min_lines: 40
    - path: "src/llm/provider.ts"
      provides: "LlmProvider.maxConcurrency + decide/chat com opts?.signal propagado a RunnableConfig.signal"
      contains: "maxConcurrency"
    - path: "src/llm/spendStore.ts"
      provides: "reserveCall (INSERT...ON CONFLICT RETURNING atômico) + releaseCall"
      exports: ["reserveCall", "releaseCall"]
    - path: "src/llm/spendCap.ts"
      provides: "withSpendCap usando reserveCall/releaseCall (sem TOCTOU)"
      contains: "reserveCall"
  key_links:
    - from: "src/cognition/concurrency.ts"
      to: "src/llm/provider.ts (maxConcurrency)"
      via: "o semáforo do Plan 02 será dimensionado por provider.maxConcurrency"
      pattern: "maxConcurrency"
    - from: "src/llm/spendCap.ts"
      to: "src/llm/spendStore.ts (reserveCall/releaseCall)"
      via: "import + chamada no decide/chat"
      pattern: "reserveCall|releaseCall"
---

<objective>
Construir as PRIMITIVAS e CONTRATOS de concorrência que o Plan 02 (wiring no loop) consome. Esta é a fase de fundação/contratos da 10.1: nada toca o loop cognitivo ainda — apenas se cria o `Semaphore`/`Gate`, expõe-se `maxConcurrency` na interface `LlmProvider`, propaga-se `AbortSignal` por `decide`/`chat`, e fecha-se o TOCTOU do `withSpendCap` (D-10).

Purpose: O Plan 02 precisa de blueprints prontos (o `Semaphore`, o campo `provider.maxConcurrency`, a assinatura `decide(schema, msgs, { signal })`, e `reserveCall`/`releaseCall`) — sem eles o executor do Plan 02 teria que inventar contratos. Interface-first: este plano define os contratos; o Plan 02 implementa contra eles.

Output: `src/cognition/concurrency.ts` (novo, com testes), `provider.ts` estendido (maxConcurrency + signal), `spendStore.ts`/`spendCap.ts` com reserva atômica (D-10), `config.ts` com `llmMaxConcurrencyLocal`/`llmMaxConcurrencyCloud`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente/10.1-CONTEXT.md
@.planning/phases/10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente/10.1-RESEARCH.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do código real (linhas verificadas). -->
<!-- Use-os diretamente — sem exploração adicional do codebase. -->

LlmProvider ATUAL (src/llm/provider.ts L20-29) — a ESTENDER (adicionar maxConcurrency + opts?):
```typescript
export interface LlmProvider {
  decide<T>(schema: ZodType<T>, messages: BaseMessage[]): Promise<T>
  chat(messages: BaseMessage[]): Promise<string>
  available(): Promise<boolean>
  embed(text: string): Promise<number[]>
}
```

decideWithFallback ATUAL (src/llm/provider.ts L72-89) — recebe os structured.invoke(messages) SEM config; deve passar a aceitar { signal } e repassar a `structured.invoke(messages, { signal })`:
```typescript
async function decideWithFallback<T>(model: ChatOpenAI, schema: ZodType<T>, messages: BaseMessage[]): Promise<T> {
  const structured = model.withStructuredOutput(schema, { name: 'decide', method: 'jsonSchema' })
  return (await structured.invoke(messages)) as T   // ← passar { signal } no 2º arg
  // (no catch: model.withStructuredOutput(rawSchema, ...).invoke(messages) — também recebe { signal })
}
```

SpendStore (interface injetável, src/llm/spendCap.ts L19-24) — a ESTENDER com reserve/release:
```typescript
export interface SpendStore {
  getCallCount(now: number): number
  incrementCall(now: number, tokens?: number): void
}
```

spendStore.ts statements existentes (src/llm/spendStore.ts):
- windowKey(ts): 'YYYY-MM-DD' (L29-31) — REUSAR
- ensureSpendTable(db): CREATE TABLE IF NOT EXISTS llm_spend (window_key TEXT PRIMARY KEY, calls INTEGER, tokens INTEGER) (L15-23) — REUSAR
- incrementCall já usa `INSERT...ON CONFLICT(window_key) DO UPDATE SET calls = calls + 1` (L40-43) — o RETURNING é a evolução

ChatOpenAI já tem `timeout` interno (provider.ts L107/171) que cria seu próprio AbortController; o `signal` externo do RunnableConfig é COMPOSTO automaticamente pelo LangChain — NÃO precisa AbortSignal.any manual.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Semaphore + Gate por tipo (concurrency.ts) — primitiva nativa zero-dep (D-01/D-02/D-03/D-11)</name>
  <files>src/cognition/concurrency.ts, src/cognition/concurrency.test.ts</files>
  <read_first>
    - .planning/phases/10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente/10.1-RESEARCH.md (§Architecture Pattern 1 e Pattern 2 — código de referência do Semaphore com fila ordenada por prioridade e do Gate por tipo)
    - src/cognition/deliberation.ts (L71-75: DeliberationState.inFlight — o boolean que o Gate substitui; L100-124 padrão de funções puras)
    - src/cognition/loop.reflect-priority.test.ts (padrão de teste de unidade puro com bun:test usado neste subsistema)
  </read_first>
  <behavior>
    - Test: `new Semaphore(1)` — primeiro `acquire(1)` resolve imediatamente; segundo `acquire(1)` fica pendente; após `release()` o segundo resolve.
    - Test: `new Semaphore(2)` — dois `acquire()` resolvem sem release intermediário (permits suficientes).
    - Test: prioridade — com permits=0 (esgotado após 1 acquire), enfileira `acquire(2)` (reflection) e depois `acquire(0)` (player); o próximo `release()` resolve o de prioridade 0 ANTES do de prioridade 2 (inserção ordenada por prioridade).
    - Test: estabilidade — dois waiters de MESMA prioridade resolvem em ordem FIFO (o que entrou primeiro sai primeiro).
    - Test: permits guard — `new Semaphore(0)` é normalizado para permits>=1 (Math.max(1, permits)).
    - Test: `release()` sem waiter incrementa permits; com waiter, passa o permit direto (não incrementa).
    - Test: Gate — `createTaskGate()` retorna gate onde `tryEnter('action')` true na 1ª vez e false na 2ª (sem `leave`); `leave('action')` libera; tipos são independentes (`tryEnter('action')` não afeta `tryEnter('reflection')`).
  </behavior>
  <action>
    Criar `src/cognition/concurrency.ts` com DUAS primitivas nativas, zero dependências (D-02 — NÃO usar p-queue/p-limit/async-mutex):

    1. `export type TaskType = 'action' | 'reflection' | 'player'`

    2. `export class Semaphore` (Pattern 1 da RESEARCH, ~30 linhas):
       - `private permits: number` inicializado no construtor com `Math.max(1, permits)`.
       - `private readonly waiters: Array<{ priority: number; resolve: () => void }> = []`.
       - `acquire(priority: number): Promise<void>` — se `permits > 0`, decrementa e `return Promise.resolve()`; senão cria um Promise e INSERE o waiter na fila em posição ORDENADA por prioridade (menor número = mais urgente): encontra o primeiro waiter com `priority > novaPrioridade` via `findIndex` e usa `splice(idx, 0, w)`; se nenhum, `push(w)`. Isso garante: (a) player(0) fura na frente de reflection(2); (b) FIFO entre mesma prioridade (só insere ANTES de prioridade ESTRITAMENTE maior).
       - `release(): void` — `const next = this.waiters.shift(); if (next) next.resolve(); else this.permits++`. O permit passa DIRETO ao próximo waiter (não incrementa quando há fila).
       - Comentário no topo da classe citando D-01/D-02/D-03 e o invariante "release() obrigatório no finally (Pitfall 3 — evitar permit leak)".

    3. `export function createTaskGate(): { tryEnter(t: TaskType): boolean; leave(t: TaskType): void; isBusy(t: TaskType): boolean }`:
       - Closure sobre `const gate: Record<TaskType, boolean> = { action: false, reflection: false, player: false }`.
       - `tryEnter(t)`: se `gate[t]` já true → `return false` (não sobrepor o mesmo tipo, D-01/D-12); senão `gate[t] = true; return true`.
       - `leave(t)`: `gate[t] = false`.
       - `isBusy(t)`: `return gate[t]`.
       - Comentário citando D-01/D-12: "gate por tipo preserva 'uma inferência por tipo' (modelo local fraco); o Semaphore impõe o teto global de recurso".

    Criar `src/cognition/concurrency.test.ts` com os testes do bloco <behavior> usando `import { test, expect } from 'bun:test'`. Para os testes de pendência, drenar microtasks com `await Promise.resolve()` e usar flags de resolução (ex.: `let resolved = false; p.then(() => { resolved = true })`) para asserir ordem — espelhar o padrão de drenagem de microtask de `deliberation.test.ts` L137-142.
  </action>
  <verify>
    <automated>cd /c/Users/biel1/OneDrive/Documentos/GitHub/MineMind && bun test src/cognition/concurrency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export class Semaphore" src/cognition/concurrency.ts` retorna 0
    - `grep -q "Math.max(1" src/cognition/concurrency.ts` retorna 0 (guarda de permits)
    - `grep -q "findIndex" src/cognition/concurrency.ts` retorna 0 (inserção ordenada por prioridade)
    - `grep -q "export function createTaskGate" src/cognition/concurrency.ts` retorna 0
    - `grep -q "export type TaskType" src/cognition/concurrency.ts` retorna 0
    - `bun test src/cognition/concurrency.test.ts` passa com TODOS os testes do bloco <behavior> verdes (mínimo 7 testes)
    - NENHUM import de 'p-queue', 'p-limit', 'async-mutex' ou 'async-sema' em concurrency.ts (D-02): `grep -E "p-queue|p-limit|async-mutex|async-sema" src/cognition/concurrency.ts` retorna VAZIO
  </acceptance_criteria>
  <done>Semaphore (acquire por prioridade/release passando o permit direto) + createTaskGate (3 flags independentes) existem, exportados e testados; zero deps externas; ordem por prioridade e FIFO provadas por teste.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LlmProvider ganha maxConcurrency + propagação de AbortSignal por decide/chat (D-07/D-09/D-14)</name>
  <files>src/llm/provider.ts, src/llm/spendCap.ts, src/config.ts, src/llm/spendCap.test.ts, src/cognition/deliberation.test.ts</files>
  <read_first>
    - src/llm/provider.ts (INTEIRO — interface L20-29, decideWithFallback L72-89, createLmStudioProvider L97-146, createOpenAiProvider L165-208, withSpendCap em createProvider L220-235)
    - src/llm/spendCap.ts (decide/chat L49-61 — o decorator precisa repassar opts? aos providers roteados)
    - src/config.ts (L128-144 bloco "Fase 6" — onde adicionar as novas chaves; L303-318 bloco de validação do provider)
    - src/llm/structured.ts (decideAction L23-45 — chama provider.decide; confirmar que NÃO quebra ao adicionar opts? opcional)
    - .planning/phases/10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente/10.1-RESEARCH.md (§Code Examples — "Propagar AbortSignal por decide/chat → model.invoke")
  </read_first>
  <behavior>
    - Test: a interface compila com `decide(schema, msgs, { signal })` e `chat(msgs, { signal })` — opts é OPCIONAL (chamadas existentes sem opts continuam válidas).
    - Test (config): `config.llmMaxConcurrencyLocal` default 4 e `config.llmMaxConcurrencyCloud` default 3, ambos overridáveis por env (`LLM_MAX_CONCURRENCY_LOCAL`/`LLM_MAX_CONCURRENCY_CLOUD`) e validados >= 1.
    - Test (provider): `createLmStudioProvider().maxConcurrency === config.llmMaxConcurrencyLocal`; `createOpenAiProvider().maxConcurrency === config.llmMaxConcurrencyCloud`.
    - Test (signal propagado): um AbortController abortado ANTES de decide faz a chamada rejeitar/abortar (verificável com um mock de model que respeita o signal, OU teste de tipo que asserta a assinatura). Manter o teste leve/sem rede — asserir que decide aceita o 3º arg e que o withSpendCap o repassa.
  </behavior>
  <action>
    Estender a interface e as 3 implementações de `LlmProvider` em `src/llm/provider.ts` (D-07/D-14):

    1. INTERFACE `LlmProvider` (L20-29):
       - Adicionar `readonly maxConcurrency: number` (D-07).
       - Mudar `decide<T>(schema, messages): Promise<T>` → `decide<T>(schema: ZodType<T>, messages: BaseMessage[], opts?: { signal?: AbortSignal }): Promise<T>`.
       - Mudar `chat(messages): Promise<string>` → `chat(messages: BaseMessage[], opts?: { signal?: AbortSignal }): Promise<string>`.

    2. `decideWithFallback` (L72-89): adicionar param `signal?: AbortSignal` e repassar `{ signal }` nos DOIS `structured.invoke(messages)` (1ª tentativa L82 E o fallback D-16 L87): `structured.invoke(messages, { signal })`. Manter o try/catch do fallback zod v4 intacto.

    3. `createLmStudioProvider` (L97-146):
       - Adicionar `maxConcurrency: config.llmMaxConcurrencyLocal` no objeto retornado.
       - `decide` (L115-119): aceitar `opts?` e passar `decideWithFallback(model, schema, messages, opts?.signal)`.
       - `chat` (L121-124): aceitar `opts?` e passar `model.invoke(messages, { signal: opts?.signal })`.

    4. `createOpenAiProvider` (L165-208):
       - Adicionar `maxConcurrency: config.llmMaxConcurrencyCloud` no objeto retornado.
       - `decide` (L180-182): aceitar `opts?` e passar `decideWithFallback(model, schema, messages, opts?.signal)`.
       - `chat` (L184-186): aceitar `opts?` e passar `model.invoke(messages, { signal: opts?.signal })`.

    5. `withSpendCap` em `src/llm/spendCap.ts` (decide L49-55, chat L57-61): aceitar `opts?: { signal?: AbortSignal }` em ambos e repassar `p.decide(schema, messages, opts)` / `p.chat(messages, opts)`. Adicionar `maxConcurrency: cloud.maxConcurrency` ao objeto retornado pelo decorator (o cap envolve o cloud; expõe a capacidade do provider efetivo). NOTA: este passo PREPARA o spendCap; o fix do TOCTOU (reserveCall) é a Task 3 — manter os passos isolados em commits separados.

    6. `src/config.ts` — adicionar no bloco "Fase 6" (após L144, antes do bloco "Fase 07.1"):
       ```
       // === Fase 10.1: Paralelismo (semáforo de concorrência LLM) ===
       // D-07/D-09: teto de inferências LLM concorrentes lido pelo semáforo do driver.
       // Local ~4 espelha o Max Concurrent Predictions default do LM Studio 0.4.0 (continuous batching).
       llmMaxConcurrencyLocal: parseInt(process.env.LLM_MAX_CONCURRENCY_LOCAL || '4', 10),
       // Cloud modesto: o withSpendCap é o teto ECONÔMICO real; o semáforo só suaviza burst (D-10).
       llmMaxConcurrencyCloud: parseInt(process.env.LLM_MAX_CONCURRENCY_CLOUD || '3', 10),
       ```
       E adicionar validação no bloco "Fase 6" de validações (após L318):
       ```
       if (config.llmMaxConcurrencyLocal < 1) throw new Error(`LLM_MAX_CONCURRENCY_LOCAL inválido: ${config.llmMaxConcurrencyLocal}. Deve ser >= 1.`)
       if (config.llmMaxConcurrencyCloud < 1) throw new Error(`LLM_MAX_CONCURRENCY_CLOUD inválido: ${config.llmMaxConcurrencyCloud}. Deve ser >= 1.`)
       ```

    7. Atualizar os mocks de `LlmProvider` em testes que quebrarão por falta de `maxConcurrency`: adicionar `maxConcurrency: 1` aos mocks em `src/llm/spendCap.test.ts` (função `mockProvider`, L16-35). Os mocks de `deliberation.test.ts` (okProvider L27-34, providers inline L96-101 e L126-131) também precisam de `maxConcurrency: 1` — adicioná-lo. Rodar `bun test src/llm src/cognition/deliberation.test.ts` para confirmar zero regressão.
  </action>
  <verify>
    <automated>cd /c/Users/biel1/OneDrive/Documentos/GitHub/MineMind && bun test src/llm src/cognition/deliberation.test.ts src/config.test.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "readonly maxConcurrency: number" src/llm/provider.ts` retorna 0
    - `grep -q "opts?: { signal?: AbortSignal }" src/llm/provider.ts` retorna 0
    - `grep -c "structured.invoke(messages, { signal })" src/llm/provider.ts` retorna >= 2 (1ª tentativa + fallback D-16)
    - `grep -q "model.invoke(messages, { signal: opts?.signal })" src/llm/provider.ts` retorna 0
    - `grep -q "llmMaxConcurrencyLocal" src/config.ts` retorna 0 E `grep -q "llmMaxConcurrencyCloud" src/config.ts` retorna 0
    - `grep -q "LLM_MAX_CONCURRENCY_LOCAL inválido" src/config.ts` retorna 0 (validação presente)
    - `grep -q "maxConcurrency: cloud.maxConcurrency" src/llm/spendCap.ts` retorna 0
    - `bunx tsc --noEmit` passa sem erros (todas as impls de LlmProvider satisfazem maxConcurrency; todos os mocks atualizados)
    - `bun test src/llm src/cognition/deliberation.test.ts src/config.test.ts` passa sem regressão
  </acceptance_criteria>
  <done>LlmProvider expõe maxConcurrency e aceita opts?.signal em decide/chat, propagado a RunnableConfig.signal nos 3 providers e no withSpendCap; config tem os defaults local=4/cloud=3 validados; tsc verde e suíte LLM+deliberation sem regressão.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fechar o TOCTOU do withSpendCap — reserveCall/releaseCall atômicos (D-10)</name>
  <files>src/llm/spendStore.ts, src/llm/spendStore.test.ts, src/llm/spendCap.ts, src/llm/spendCap.test.ts</files>
  <read_first>
    - src/llm/spendStore.ts (INTEIRO — windowKey L29-31, ensureSpendTable L15-23, incrementCall L38-44 com o INSERT...ON CONFLICT já presente, getCallCount L50-56)
    - src/llm/spendStore.test.ts (INTEIRO — padrão de teste com Database(':memory:') e ts() determinístico)
    - src/llm/spendCap.ts (INTEIRO — route() L46 check-then-increment é o TOCTOU; decide L49-55, chat L57-61)
    - src/llm/spendCap.test.ts (INTEIRO — mockProvider rotulado, fakeStore — o fake precisará dos novos métodos reserveCall/releaseCall)
    - .planning/phases/10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente/10.1-RESEARCH.md (§Architecture Pattern 4 — código de referência de reserveCall/releaseCall com INSERT...ON CONFLICT RETURNING)
  </read_first>
  <behavior>
    - Test (spendStore): `reserveCall(db, ts, maxCalls=2)` na 1ª chamada retorna true e deixa calls=1; 2ª retorna true (calls=2); 3ª retorna false (calls=3 > 2). Increment-then-check: o contador SOBE mesmo quando retorna false (a reserva é especulativa; o caller estorna).
    - Test (spendStore): `releaseCall(db, ts)` após uma reserva decrementa calls de volta; `releaseCall` num contador 0 mantém 0 (MAX(0, calls-1) — piso).
    - Test (spendStore): reserveCall em janela inexistente cria a linha com calls=1 (INSERT) e retorna `1 <= maxCalls`.
    - Test (spendCap): abaixo do teto → decide chama cloud.decide; reserveCall foi chamado 1x; releaseCall NÃO foi chamado (sucesso). No teto → decide cai para local.decide; reserveCall retornou false; releaseCall chamado 1x (estorna a reserva especulativa).
    - Test (spendCap): erro real do cloud.decide → releaseCall é chamado no catch (estorna), e o erro é re-lançado (decisão de discrição: estornar no erro — manter consistente).
    - Test (spendCap): embed NUNCA chama reserveCall (delega direto ao cloud.embed).
  </behavior>
  <action>
    Fechar o TOCTOU de D-10 (check-then-increment em 2 passos abre janela para 2 chamadas cloud furarem o hard-cap).

    1. `src/llm/spendStore.ts` — adicionar DUAS funções (reusar windowKey/ensureSpendTable):
       ```typescript
       // D-10: reserva 1 slot ATOMICAMENTE (increment-then-check). Retorna true se ficou <= maxCalls,
       // false se estourou (caller faz releaseCall + fallback-to-local). INSERT...ON CONFLICT RETURNING
       // é indivisível no single-thread do bun:sqlite — fecha a janela TOCTOU do withSpendCap.
       export function reserveCall(db: Database, ts: number, maxCalls: number): boolean {
         ensureSpendTable(db)
         const row = db.prepare(
           `INSERT INTO llm_spend (window_key, calls, tokens) VALUES (?, 1, 0)
            ON CONFLICT(window_key) DO UPDATE SET calls = calls + 1
            RETURNING calls`,
         ).get(windowKey(ts)) as { calls: number }
         return row.calls <= maxCalls
       }
       // Estorna o slot quando caímos no fallback-to-local (não consumiu cloud) ou em erro real.
       export function releaseCall(db: Database, ts: number): void {
         ensureSpendTable(db)
         db.prepare(`UPDATE llm_spend SET calls = MAX(0, calls - 1) WHERE window_key = ?`).run(windowKey(ts))
       }
       ```
       MANTER `incrementCall`/`getCallCount` existentes (ainda usados/testados); NÃO removê-los.

    2. `src/llm/spendCap.ts` — estender a interface `SpendStore` (L19-24) com:
       ```typescript
       /** D-10: reserva atômica de 1 slot; true se sob o teto, false se estourou. */
       reserveCall(now: number, maxCalls: number): boolean
       /** D-10: estorna 1 slot (fallback-to-local ou erro real não consumiu cloud). */
       releaseCall(now: number): void
       ```
       e o adaptador `sqliteSpendStore` (L27-32) com:
       ```typescript
       reserveCall: (now, maxCalls) => reserveCall(db, now, maxCalls),
       releaseCall: (now) => releaseCall(db, now),
       ```
       importando `reserveCall, releaseCall` de `./spendStore` (L12).

    3. `withSpendCap.decide` (L49-55) — SUBSTITUIR o route()+incrementCall pelo padrão increment-then-check (Pattern 4 da RESEARCH):
       ```typescript
       async decide<T>(schema, messages, opts) {
         const now = Date.now()
         if (store.reserveCall(now, cfg.maxCalls)) {
           try { return await cloud.decide(schema, messages, opts) }
           catch (e) { store.releaseCall(now); throw e }   // estorna no erro real (decisão de discrição D-10)
         }
         store.releaseCall(now)                            // estourou: estorna a reserva especulativa
         return local.decide(schema, messages, opts)       // fallback-to-local (custo-zero)
       }
       ```

    4. `withSpendCap.chat` (L57-61) — mesmo padrão de reserveCall/releaseCall + repasse de `opts`.

    5. `withSpendCap.available` (L63-66) — o `route()` antigo usava getCallCount. Manter `available()` consultando `store.getCallCount(Date.now()) >= cfg.maxCalls ? local.available() : cloud.available()` (available NÃO consome slot — é só probe; manter getCallCount para isso). Manter `embed: cloud.embed` (L70) intacto (embed nunca conta).

    6. Atualizar `src/llm/spendCap.test.ts`: o `fakeStore` (L38-50) ganha `reserveCall(now, maxCalls)` (incrementa this.count e retorna `this.count <= maxCalls`) e `releaseCall(now)` (decrementa com piso 0, rastreando `releases`). Ajustar/adicionar os testes do bloco <behavior> (estorno no fallback e no erro). Os testes existentes que asseriam `store.increments` devem migrar para asserir `reserveCall`/`releaseCall` (o decorator não chama mais incrementCall no caminho decide/chat).
  </action>
  <verify>
    <automated>cd /c/Users/biel1/OneDrive/Documentos/GitHub/MineMind && bun test src/llm/spendStore.test.ts src/llm/spendCap.test.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function reserveCall" src/llm/spendStore.ts` retorna 0
    - `grep -q "export function releaseCall" src/llm/spendStore.ts` retorna 0
    - `grep -q "RETURNING calls" src/llm/spendStore.ts` retorna 0 (reserva atômica)
    - `grep -q "MAX(0, calls - 1)" src/llm/spendStore.ts` retorna 0 (estorno com piso)
    - `grep -q "reserveCall(now, maxCalls)" src/llm/spendCap.ts` retorna 0 (na interface SpendStore)
    - `grep -q "store.reserveCall(now, cfg.maxCalls)" src/llm/spendCap.ts` retorna 0
    - `grep -q "store.releaseCall(now)" src/llm/spendCap.ts` retorna 0
    - `grep -q "incrementCall" src/llm/spendCap.ts` retorna VAZIO no caminho decide/chat (o decorator não usa mais incrementCall para contar a chamada) — confirmar que decide/chat usam reserveCall
    - `bun test src/llm/spendStore.test.ts src/llm/spendCap.test.ts` passa com os testes do bloco <behavior> (reserva atômica + estorno no fallback + estorno no erro)
    - `bunx tsc --noEmit` verde
  </acceptance_criteria>
  <done>reserveCall (INSERT...ON CONFLICT RETURNING, increment-then-check) + releaseCall (MAX(0,..) com piso) existem em spendStore; withSpendCap usa-os no decide/chat (reserva antes do disparo, estorna no fallback-to-local e no erro real), repassando opts?.signal; TOCTOU fechado e provado por teste; tsc verde.</done>
</task>

</tasks>

<verification>
- `bun test src/cognition/concurrency.test.ts src/llm src/cognition/deliberation.test.ts src/config.test.ts` — todos verdes
- `bunx tsc --noEmit` — sem erros de tipo (todas as impls de LlmProvider têm maxConcurrency; decide/chat aceitam opts?)
- `grep -E "p-queue|p-limit|async-mutex|async-sema" src/cognition/concurrency.ts` — VAZIO (D-02: zero-dep)
- Nenhum arquivo do loop cognitivo (loop.ts, reflection.ts) foi tocado neste plano (a fiação é o Plan 02)
</verification>

<success_criteria>
- `Semaphore` (acquire por prioridade com fila ordenada + FIFO por desempate; release passa permit direto) e `createTaskGate` (3 flags independentes por tipo) existem, exportados, testados (>=7 testes)
- `LlmProvider.maxConcurrency` exposto; `decide`/`chat` propagam `opts?.signal` → `RunnableConfig.signal` nos 3 providers e no withSpendCap
- `config.llmMaxConcurrencyLocal` (4) e `llmMaxConcurrencyCloud` (3) com validação >= 1
- `reserveCall`/`releaseCall` atômicos fecham o TOCTOU do withSpendCap (D-10), provados por teste
- Zero regressão na suíte LLM existente; zero dependência nova (D-02)
</success_criteria>

<output>
Após completar, criar `.planning/phases/10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente/10.1-01-SUMMARY.md` documentando: assinaturas finais do Semaphore/Gate, a forma escolhida da propagação de signal, e como o reserveCall/releaseCall foi fatorado — handoff para o Plan 02 (que fia tudo no loop).
</output>
