---
phase: 10.1-paralelismo-no-processamento-do-llm-deliberacao-concorrente
verified: 2026-06-22T00:00:00Z
status: human_needed
score: 15/15 must-haves verified
re_verification: false
human_verification:
  - test: "Coexistência real ação×reflexão com LM Studio Max Concurrent Predictions >= 2"
    expected: "Com maxConcurrency local=4, ação e reflexão sobrepõem de verdade (não serializam). Com Max Concurrent Predictions=1 no LM Studio, degrada ao single-flight priorizado + preempção (D-03) — ainda correto, sem ganho de sobreposição."
    why_human: "Requer LM Studio rodando com Max Concurrent Predictions configurado; comportamento de batching do servidor não é observável por teste determinístico (caveat documentado em 10.1-02-SUMMARY.md §Caveats de Validação AO VIVO item 1)."
  - test: "RunnableConfig.signal aborta o fetch local do LM Studio ao preemptar a AÇÃO"
    expected: "actionAbort.abort() de fato cancela o fetch em voo do LM Studio e libera o slot do semáforo. No cloud o servidor segue gerando (D-15, aceito)."
    why_human: "A propagação opts.signal → RunnableConfig.signal é provada por teste de tipo/assinatura, mas o cancelamento efetivo do fetch HTTP só se confirma ao vivo (caveat 10.1-02-SUMMARY.md item 2)."
  - test: "[reflect] ao vivo + preempção jogador→ação sob carga"
    expected: "(a) a reflexão dispara e não starva sob carga de ação; (b) um pedido de jogador preempta uma AÇÃO em voo e a ação re-despacha no próximo tick."
    why_human: "Comportamento temporal de starvation/preempção sob carga real de inferência (caveat 10.1-02-SUMMARY.md item 3)."
---

# Phase 10.1: Paralelismo no Processamento do LLM (Deliberação Concorrente) Verification Report

**Phase Goal:** Substituir a deliberação single-flight (serial) por execução concorrente de tarefas cognitivas distintas (ação, reflexão, resposta a jogador), de modo que elas não disputem mais o mesmo lock `inFlight` — destravando a concorrência autônomo+assistente que a Phase 11 exige e eliminando a contenção que hoje faz a reflexão starvar.
**Verified:** 2026-06-22
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1   | Semaphore(1) serializa duas aquisições (2ª só resolve após release) — single-flight emerge de maxConcurrency=1 | ✓ VERIFIED | `concurrency.ts` L40-60 acquire/release; teste verde |
| 2   | Semaphore(2) permite sobreposição real (duas aquisições sem release intermediário) | ✓ VERIFIED | `Math.max(1,permits)` guard + `permits--`; teste verde |
| 3   | acquire de prioridade menor (player=0) fura waiter de prioridade maior (reflection=2) já enfileirado | ✓ VERIFIED | `findIndex(x.priority > priority)` + splice (L50-52); teste verde |
| 4   | release() devolve permit mesmo após throw (try/finally) — sem permit leak | ✓ VERIFIED | maybeDeliberate L236-241 finally; routePlayerTurn L550-553 finally |
| 5   | provider.decide/chat aceitam opts?.signal repassado a RunnableConfig.signal | ✓ VERIFIED | provider.ts L93/L98/L136/L202; decideWithFallback propaga em ambos invoke |
| 6   | provider.maxConcurrency existe e é lido da config (local=4, cloud=3) | ✓ VERIFIED | provider.ts L127/L195; config.ts L149/L151 |
| 7   | reserveCall incrementa-then-checa atomicamente (INSERT...ON CONFLICT RETURNING) | ✓ VERIFIED | spendStore.ts L65-75 `RETURNING calls`; teste verde |
| 8   | releaseCall decrementa com piso 0 | ✓ VERIFIED | spendStore.ts L81-84 `MAX(0, calls - 1)`; teste verde |
| 9   | goal player_request empurrado DURANTE o await da reflexão sobrevive ao commit (Pitfall 2) | ✓ VERIFIED | deliberation.test.ts L219-273 teste "clobber"; passa |
| 10  | maybeDeliberate não tem await entre read e write de holder.goals (commit síncrono) | ✓ VERIFIED | deliberation.ts L324→L327 sem await entre applyGoalUpdates e persistHolder |
| 11  | gate por tipo permite AÇÃO e REFLEXÃO coexistirem (deixa de ser XOR) | ✓ VERIFIED | pickDispatch L517-526 retorna `{reflect, action}` independentes |
| 12  | handleConversation passa pelo mesmo gate/semáforo (não dispara LLM sem coordenação) | ✓ VERIFIED | loop.ts L231-233 routePlayerTurn; `void handleConversation(` removido |
| 13  | preempção: AbortController da ação aborta a AÇÃO em voo e libera slot via finally | ✓ VERIFIED | loop.ts L443-452; routePlayerTurn L546 preemptAction() antes do acquire |
| 14  | REFLEXÃO nunca recebe signal de preempção e nunca starva | ✓ VERIFIED | loop.ts L426-427 ramo reflect sem signal; deliberation.ts L195 |
| 15  | release()/leave() em try/finally em TODOS os caminhos de aquisição | ✓ VERIFIED | deliberation.ts L236-241; loop.ts L550-553 |

**Score:** 15/15 truths verified (automated). 3 itens adicionais de validação AO VIVO roteados a human_verification.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/cognition/concurrency.ts` | Semaphore + createTaskGate + TaskType, zero-dep | ✓ VERIFIED | 92 linhas; exports presentes; zero imports externos |
| `src/llm/provider.ts` | maxConcurrency + opts?.signal nos 3 providers | ✓ VERIFIED | interface L26/L31/L33; 3 impls propagam signal |
| `src/llm/spendStore.ts` | reserveCall (RETURNING) + releaseCall (MAX floor) | ✓ VERIFIED | L65-84; incrementCall/getCallCount preservados |
| `src/llm/spendCap.ts` | withSpendCap usando reserveCall/releaseCall (sem TOCTOU) | ✓ VERIFIED | L58-84 reserva+estorno; available via getCallCount |
| `src/config.ts` | llmMaxConcurrencyLocal=4 / Cloud=3 com validação >=1 | ✓ VERIFIED | L149/L151 defaults; L327/L328 validação |
| `src/cognition/deliberation.ts` | sem inFlight; gate+semáforo; commit síncrono merge-by-id | ✓ VERIFIED | `inFlight` removido (0 ocorrências); L324-327 commit síncrono |
| `src/cognition/loop.ts` | Semaphore(provider.maxConcurrency)+gate; routePlayerTurn; AbortController | ✓ VERIFIED | L163-167; L231-233; L443-452 |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| concurrency.ts | provider.ts (maxConcurrency) | semáforo dimensionado por provider.maxConcurrency | ✓ WIRED | loop.ts L163 `new Semaphore(provider.maxConcurrency)` |
| spendCap.ts | spendStore.ts (reserveCall/releaseCall) | import + chamada no decide/chat | ✓ WIRED | spendCap.ts L12 import; L60/L68/L74/L82 chamadas |
| loop.ts | concurrency.ts (Semaphore/createTaskGate) | import + instanciação por sessão | ✓ WIRED | L15 import; L163-164 instanciação |
| conversation.ts (handleConversation) | gate/semáforo do loop | routePlayerTurn com acquire(player)/finally release | ✓ WIRED | loop.ts L231-233 envolve com routePlayerTurn |
| deliberation.ts (runReflection) | holder.goals (re-leitura no commit) | applyGoalUpdates SEM await entre read/write | ✓ WIRED | deliberation.ts L324 `holder.goals = applyGoalUpdates(holder.goals, updates, now)` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| deliberation.ts (runReflection) | holder.goals | applyGoalUpdates (merge-by-id de reflection.ts) re-lido no commit | Yes — merge real, goals desconhecidos preservados | ✓ FLOWING |
| spendCap.ts (decide/chat) | reserveCall result | bun:sqlite INSERT...ON CONFLICT RETURNING calls | Yes — query real ao DB de gasto | ✓ FLOWING |
| loop.ts (routePlayerTurn) | semáforo/gate state | createTaskGate()/new Semaphore() por sessão; handler real | Yes — fiado ao handler de chat de produção | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Primitivas de concorrência (Semaphore/Gate/reserveCall/clobber) | `bun test concurrency.test.ts deliberation.test.ts concurrency-wiring.test.ts spendStore.test.ts spendCap.test.ts` | 38 pass / 0 fail | ✓ PASS |
| Regressão do loop (smokes + reflect-priority) | `bun test loop.smoke loop.phase3.smoke loop.reflect-priority reflection.integration.smoke` | 18 pass / 0 fail | ✓ PASS |
| Type safety | `bunx tsc --noEmit` | exit 0 | ✓ PASS |
| Suíte completa (baseline) | `bun test` | 477 pass / 1 skip / 1 fail (pré-existente `.env`) | ✓ PASS |
| Zero deps externas (D-02) | grep `p-queue\|p-limit\|async-mutex\|async-sema` em concurrency.ts | 0 ocorrências | ✓ PASS |

### Requirements Coverage

CONC-* são must-haves LOCAIS dos plans (derivados das decisões D-01..D-15 da CONTEXT.md), NÃO constam na REQUIREMENTS.md global — verificados via `requirements-completed` dos SUMMARYs.

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| CONC-SEM | 10.1-01 | gate-por-tipo + semáforo global = provider.maxConcurrency (D-01/D-02/D-03) | ✓ SATISFIED | concurrency.ts Semaphore+createTaskGate; loop.ts L163-164 |
| CONC-PROVIDER | 10.1-01 | maxConcurrency na interface + propagação de signal (D-07/D-14) | ✓ SATISFIED | provider.ts L26/L31/L33; structured.ts L30-42 |
| CONC-SPENDCAP | 10.1-01 | corrigir TOCTOU do withSpendCap via reserveCall/releaseCall (D-10) | ✓ SATISFIED | spendStore.ts L65-84; spendCap.ts L58-84 |
| CONC-WIRE | 10.1-02 | trocar inFlight por gate+semáforo no loop; rotear handleConversation (D-01/D-08) | ✓ SATISFIED | deliberation.ts sem inFlight; loop.ts L231-233 routePlayerTurn |
| CONC-MERGE | 10.1-02 | commit síncrono + merge-by-id protege holder.goals do clobber (D-04/D-05/D-06) | ✓ SATISFIED | deliberation.ts L324-327; teste clobber verde |
| CONC-PREEMPT | 10.1-02 | player preempta AÇÃO via AbortController; reflexão protegida (D-11/D-12/D-13) | ✓ SATISFIED | loop.ts L443-452/L546; reflexão sem signal L426-427 |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (nenhum) | — | — | — | Nenhum stub, TODO, placeholder ou empty-handler encontrado nos arquivos da fase |

Os comentários "stub → idle" em deliberation.ts L121-122 (fighting/building) são pré-existentes da arquitetura de estados (não desta fase) e intencionais — mapeiam estados não-implementados para idle, não são stubs introduzidos pela 10.1.

### Human Verification Required

Ver frontmatter `human_verification`. Resumo dos 3 itens de validação AO VIVO (não-bloqueantes, documentados em 10.1-02-SUMMARY.md):

1. **Coexistência ação×reflexão com LM Studio Max Concurrent Predictions >= 2** — sem isso degrada ao single-flight priorizado + preempção (ainda correto).
2. **RunnableConfig.signal aborta o fetch local** — cancelamento efetivo do fetch HTTP só confirmável ao vivo.
3. **[reflect] ao vivo + preempção jogador→ação sob carga** — comportamento temporal de starvation/preempção.

### Gaps Summary

Nenhum gap automatizado. Todas as 15 verdades observáveis estão VERIFIED no código e cobertas por testes determinísticos verdes; todos os 7 artefatos passam os 4 níveis (exists/substantive/wired/data-flows); todos os 5 key links estão WIRED; todos os 6 requisitos CONC-* SATISFIED. tsc verde, suíte completa em baseline (a única falha é o teste de config `.env` pré-existente e documentado, não-regressão).

O status é **human_needed** apenas porque a coexistência REAL ação×reflexão e o cancelamento efetivo do fetch dependem de LM Studio ao vivo (Max Concurrent Predictions, cancelamento de fetch HTTP) e de carga real — caveats explicitamente documentados como HUMAN-UAT no 10.1-02-SUMMARY.md. A lógica de concorrência, a coordenação gate+semáforo, a proteção contra clobber e a preempção estão todas provadas por teste no nível de código.

---

_Verified: 2026-06-22_
_Verifier: Claude (gsd-verifier)_
