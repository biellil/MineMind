---
phase: 03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/motivation/types.ts
  - src/motivation/needs.ts
  - src/motivation/goals.ts
  - src/motivation/needs.test.ts
  - src/motivation/goals.test.ts
autonomous: true
requirements: [NEED-01, NEED-02, GOAL-01, GOAL-02]

must_haves:
  truths:
    - "Sobrevivência e recursos derivam do estado real do WorldSnapshot (vida/fome/inventário); curiosidade decai por timer."
    - "Uma necessidade ignorada por mais tempo ganha urgência crescente (anti-starvation), garantindo que nenhuma fique permanentemente preterida."
    - "Abrigo e socialização existem no enum/estrutura como stub (sem decaimento real)."
    - "O agente segura o objetivo atual (histerese) e só troca por gatilho de reconsideração: sobrevivência crítica ou (em ASSISTANT) pedido de jogador."
    - "Objetivos têm prioridade, progresso e dependências."
  artifacts:
    - path: "src/motivation/types.ts"
      provides: "Need, NeedKind, Goal, GoalSource, MotivationConfig"
      exports: ["Need", "NeedKind", "Goal", "GoalSource", "MotivationConfig", "SelectGoalContext"]
    - path: "src/motivation/needs.ts"
      provides: "evaluateNeeds (híbrido) + urgency (anti-starvation) — módulo PURO"
      exports: ["evaluateNeeds", "urgency", "createNeeds", "ACTIVE_NEEDS", "STUB_NEEDS"]
    - path: "src/motivation/goals.ts"
      provides: "selectGoal (histerese/preempção) + generateGoals (de necessidades) — módulo PURO"
      exports: ["selectGoal", "generateGoals", "advanceProgress"]
  key_links:
    - from: "src/motivation/needs.ts"
      to: "WorldSnapshot"
      via: "evaluateNeeds lê snapshot.status.health/food e snapshot.inventory"
      pattern: "snapshot\\.(status|inventory)"
    - from: "src/motivation/goals.ts"
      to: "urgency/needs"
      via: "generateGoals deriva objetivos de necessidades que cruzam limiar"
      pattern: "generateGoals"
    - from: "src/motivation/goals.ts"
      to: "preempção"
      via: "selectGoal respeita survivalCritical e playerRequestPending"
      pattern: "survivalCritical|playerRequestPending"
---

<objective>
Construir o sistema de motivação intrínseca como MÓDULOS PUROS (estilo `arbiter.ts`/`safety.ts` da Fase 2 — sem bot, sem LLM, sem efeito colateral, tempo e config entram por parâmetro): necessidades híbridas que decaem com anti-starvation (NEED-01/02) e objetivos dinâmicos com prioridade/progresso/dependências e comprometimento por histerese com preempção bem definida (GOAL-01/02).

Estes módulos NÃO tocam o grafo/loop/config global — recebem `MotivationConfig` por parâmetro. A integração (ler `.env`, plugar no `analyze`/`decide`) é o Plan 03. Isto mantém os módulos 100% testáveis por TDD e independentes do Plan 01.

Tipo de plano: TDD — escrever os testes PRIMEIRO (RED), confirmar a falha, implementar até GREEN, refatorar se preciso. Commits atômicos por ciclo.

Purpose: Provar o mecanismo decai→objetivo→comprometimento de forma testável antes de acoplar ao loop/LLM.
Output: `src/motivation/{types,needs,goals}.ts` + testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md

# Padrão de módulo puro a espelhar + contrato de entrada
@src/cognition/arbiter.ts
@src/cognition/safety.ts
@src/perception/types.ts

<interfaces>
<!-- Contratos extraídos do codebase. -->

De src/perception/types.ts (entrada de evaluateNeeds):
```typescript
export interface BotStatus { health: number /*0-20*/; food: number /*0-20*/; position; timeOfDay; isDay }
export interface InventorySlot { slot: number; name: string; count: number; ... }
export interface WorldSnapshot { status: BotStatus; inventory: ReadonlyArray<InventorySlot>; players; nearbyBlockTypes; ... }
```

Padrão PURO a espelhar (de src/cognition/safety.ts): funções recebem `now` por parâmetro, sem `Date.now()` interno; estado mutável criado por factory `createX()`. De src/cognition/arbiter.ts: funções recebem `config`/snapshot read-only, sem efeito colateral.

Formas prescritas pela RESEARCH.md (Code Examples — usar como base):
```typescript
export interface Need { kind: 'survival'|'resources'|'curiosity'|'shelter'|'social'; value: number /*0..1*/; lastSatisfiedAt: number }
// híbrido (D-09): survival/resources do snapshot; curiosity por timer
export function evaluateNeeds(prev: Need[], snap: WorldSnapshot, now: number, cfg): Need[]
// anti-starvation (D-11): urgência = weight * ((1-value) + boost crescente com tempo ignorado)
export function urgency(n: Need, now: number, cfg): number

export interface Goal { id: string; kind: string; priority: number; progress: number /*0..1*/; dependsOn: string[]; source: 'need'|'player_request'; committedAt: number }
// guarded execution (D-15): mantém o atual, salvo gatilho de reconsideração
export function selectGoal(current: Goal|null, candidates: Goal[], ctx, cfg): Goal|null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tipos de motivação + necessidades híbridas com anti-starvation (NEED-01/02)</name>
  <files>src/motivation/types.ts, src/motivation/needs.ts, src/motivation/needs.test.ts</files>
  <read_first>
    - src/cognition/safety.ts (padrão de módulo PURO: now por parâmetro, factory createX, sem Date.now interno)
    - src/cognition/arbiter.ts (padrão de função pura lendo snapshot read-only + config)
    - src/perception/types.ts (BotStatus.health/food 0-20, InventorySlot.name/count, WorldSnapshot)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md (Code Example "Necessidade híbrida"; Pitfall 5 anti-starvation)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md (D-08 3 ativas + shelter/social stub; D-09 híbrido; D-10 pesos equilibrados; D-11 anti-starvation configurável)
  </read_first>
  <behavior>
    - evaluateNeeds atualiza 'survival' = (health/20 + food/20)/2 a partir do snapshot (D-09).
    - evaluateNeeds atualiza 'resources' por satisfação derivada do inventário (fração de cfg.resourceTargets presentes → 0..1).
    - evaluateNeeds decai 'curiosity' por timer: value = max(0, prev - cfg.curiosityDecayPerMs * (now - lastSatisfiedAt)).
    - 'shelter' e 'social' permanecem inalterados (stub D-08 — sem decaimento real).
    - urgency cresce monotonicamente com o tempo desde lastSatisfiedAt (anti-starvation NEED-02): para a mesma need com value fixo, urgency(now+Δ) > urgency(now).
    - urgency pondera por cfg.weights[kind] (pesos equilibrados por default — D-10).
  </behavior>
  <action>
    Criar `src/motivation/types.ts`:
    - `export type NeedKind = 'survival'|'resources'|'curiosity'|'shelter'|'social'`
    - `export const ACTIVE_NEEDS: NeedKind[] = ['survival','resources','curiosity']` e `export const STUB_NEEDS: NeedKind[] = ['shelter','social']` (D-08).
    - `export interface Need { kind: NeedKind; value: number; lastSatisfiedAt: number }`
    - `export type GoalSource = 'need'|'player_request'`
    - `export interface Goal { id: string; kind: string; priority: number; progress: number; dependsOn: string[]; source: GoalSource; committedAt: number }`
    - `export type Disposition = 'AUTONOMOUS'|'ASSISTANT'` (declarar LOCALMENTE aqui para manter Plan 02 independente do Plan 01; o Plan 03 reconcilia com src/llm/prompts.ts — ambos têm a mesma forma de string literal union, compatíveis estruturalmente).
    - `export interface SelectGoalContext { survivalCritical: boolean; playerRequestPending: boolean; disposition: Disposition }`
    - `export interface MotivationConfig { weights: Record<NeedKind, number>; curiosityDecayPerMs: number; starvationBoostPerMs: number; goalThreshold: number; hysteresisMargin: number; survivalCriticalThreshold: number; resourceTargets: string[] }`

    Criar `src/motivation/needs.ts` (PURO, espelhando safety.ts/arbiter.ts — NÃO importar src/config.ts, NÃO usar Date.now()):
    - `export function createNeeds(now: number): Need[]` — inicializa as 5 needs (value 1 = satisfeita; lastSatisfiedAt = now).
    - `export function evaluateNeeds(prev: Need[], snap: WorldSnapshot, now: number, cfg: MotivationConfig): Need[]` conforme <behavior> (híbrido D-09; stub para shelter/social — retorna o Need inalterado).
    - `resourceSatisfaction(inventory, cfg): number` helper interno: fração de cfg.resourceTargets cujo nome aparece no inventário → clamp 0..1.
    - `export function urgency(n: Need, now: number, cfg: MotivationConfig): number` = `cfg.weights[n.kind] * ((1 - n.value) + cfg.starvationBoostPerMs * (now - n.lastSatisfiedAt))`.

    TDD: escrever needs.test.ts PRIMEIRO (RED) cobrindo cada item do <behavior> com cfg de teste fixo e snapshots mock mínimos (só status/inventory). Commit `test(03-02): failing needs tests`. Implementar até GREEN. Commit `feat(03-02): hybrid needs + anti-starvation`.
  </action>
  <verify>
    <automated>bun test src/motivation/needs.test.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export const ACTIVE_NEEDS" src/motivation/types.ts` e `grep -q "export const STUB_NEEDS" src/motivation/types.ts` (D-08 stub)
    - `grep -q "snapshot.status\|snap.status" src/motivation/needs.ts` (survival/resources do snapshot — D-09)
    - `grep -q "starvationBoostPerMs" src/motivation/needs.ts` (anti-starvation — NEED-02/D-11)
    - `grep -rn "Date.now()" src/motivation/needs.ts` retorna VAZIO (pureza)
    - `grep -rn "from '../config'" src/motivation/needs.ts` retorna VAZIO (config por parâmetro)
    - O teste prova: survival reflete health/food; curiosity decai no tempo; shelter/social inalterados; urgency monotônica no tempo ignorado
    - `bun test src/motivation/needs.test.ts` passa; `bunx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>Tipos de motivação definidos; evaluateNeeds híbrido com shelter/social stub; urgency com anti-starvation monotônica; módulo 100% puro; testes verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Objetivos dinâmicos com histerese e preempção (GOAL-01/02)</name>
  <files>src/motivation/goals.ts, src/motivation/goals.test.ts</files>
  <read_first>
    - src/motivation/types.ts (Need/Goal/SelectGoalContext/MotivationConfig — criados na Task 1)
    - src/motivation/needs.ts (urgency — usada para priorizar candidatos)
    - src/cognition/safety.ts (padrão de módulo puro a espelhar)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md (Code Example "Comprometimento com histerese"; Pitfall 4 oscilação de objetivo)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md (D-15 histerese + preempção; D-16 prioridade/progresso/dependências + fontes need/player_request)
  </read_first>
  <behavior>
    - generateGoals cria um Goal por necessidade ativa cuja urgency cruza cfg.goalThreshold (source:'need'), com priority = urgency, progress 0, dependsOn [] (GOAL-01).
    - selectGoal com current != null e SEM gatilho de reconsideração retorna o current (histerese — não troca a cada tick, GOAL-02).
    - selectGoal só troca se o melhor candidato superar o atual por margem: best.priority >= current.priority + cfg.hysteresisMargin.
    - selectGoal PREEMPTA (ignora histerese e escolhe o melhor candidato) quando ctx.survivalCritical === true (D-15a).
    - selectGoal PREEMPTA quando ctx.disposition === 'ASSISTANT' && ctx.playerRequestPending === true (D-15b/D-13); em AUTONOMOUS playerRequestPending NÃO preempta.
    - advanceProgress(goal, delta) retorna novo goal com progress clamp 0..1 (imutável).
  </behavior>
  <action>
    Criar `src/motivation/goals.ts` (PURO; importar urgency de './needs', tipos de './types'; NÃO importar src/config.ts; NÃO usar Date.now()):
    - `export function generateGoals(needs: Need[], now: number, cfg: MotivationConfig): Goal[]` → Goal[] das needs ATIVAS (ACTIVE_NEEDS) com urgency(need,now,cfg) >= cfg.goalThreshold; id estável por kind (ex.: `need:${kind}`); priority = urgency; progress 0; dependsOn []; source 'need'; committedAt now.
    - `export function selectGoal(current: Goal|null, candidates: Goal[], ctx: SelectGoalContext, cfg: MotivationConfig): Goal|null` com guarded execution + preempção (D-15):
      ```
      const preempt = ctx.survivalCritical || (ctx.disposition === 'ASSISTANT' && ctx.playerRequestPending)
      if (current && !preempt) {
        const best = bestCandidate(candidates)
        if (!best || best.priority < current.priority + cfg.hysteresisMargin) return current
      }
      return bestCandidate(candidates) ?? current
      ```
    - `bestCandidate(candidates)` helper interno = candidato de maior priority (null se vazio).
    - `export function advanceProgress(goal: Goal, delta: number): Goal` imutável com clamp 0..1.

    TDD: escrever goals.test.ts PRIMEIRO (RED) cobrindo cada item do <behavior> com cfg de teste fixo. Commit `test(03-02): failing goals tests`. Implementar até GREEN. Commit `feat(03-02): dynamic goals with hysteresis + preemption`.
  </action>
  <verify>
    <automated>bun test src/motivation/goals.test.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function generateGoals" src/motivation/goals.ts` e `grep -q "export function selectGoal" src/motivation/goals.ts`
    - `grep -q "survivalCritical" src/motivation/goals.ts` e `grep -q "playerRequestPending" src/motivation/goals.ts` (preempção — D-15)
    - `grep -q "hysteresisMargin" src/motivation/goals.ts` (histerese — GOAL-02)
    - `grep -q "source: 'need'\|source:'need'" src/motivation/goals.ts` (fonte de objetivo — D-16)
    - `grep -rn "Date.now()\|from '../config'" src/motivation/goals.ts` retorna VAZIO (pureza)
    - O teste prova: histerese segura o objetivo; preempção por survivalCritical e por player em ASSISTANT (não em AUTONOMOUS); advanceProgress clamp 0..1
    - `bun test src/motivation/goals.test.ts` passa; `bunx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>generateGoals deriva objetivos de necessidades acima do limiar; selectGoal segura objetivo por histerese e só preempta por sobrevivência crítica ou pedido em ASSISTANT; advanceProgress imutável; módulo puro; testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorldSnapshot → needs | Dados de jogo (potencialmente do servidor) alimentam o cálculo de necessidades. |
| pedido de jogador → goal (preempção) | Sinal `playerRequestPending` derivado de chat de jogador influencia seleção de objetivo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | src/motivation/goals.ts (preempção por jogador) | mitigate | playerRequestPending só preempta em disposição ASSISTANT (D-13); em AUTONOMOUS é ignorado. A geração de objetivo a partir de pedido (texto livre→tipo) é restrita a um conjunto fechado no Plan 04; aqui o sinal é booleano, não texto. |
| T-03-06 | Denial of Service (oscilação) | src/motivation/goals.ts (selectGoal) | mitigate | Histerese (hysteresisMargin) + guarded execution impede troca de objetivo a cada tick (GOAL-02, Pitfall 4). Preempção é só por sobrevivência crítica ou pedido em ASSISTANT — gatilhos explícitos e limitados. |
| T-03-07 | Tampering | src/motivation/needs.ts (valores do snapshot) | accept | Snapshot vem do servidor local offline confiável; valores clampados em 0..1. Sem superfície externa nesta fase. |
</threat_model>

<verification>
- `bun test src/motivation/` — todos verdes (RED→GREEN comprovado pelos commits TDD).
- `bunx tsc --noEmit` — sem erros em src/motivation/*.
- `grep -rn "Date.now()" src/motivation/` retorna VAZIO (pureza: tempo por parâmetro).
- `grep -rn "from '../config'" src/motivation/` retorna VAZIO (config por parâmetro, não global).
</verification>

<success_criteria>
- NEED-01: evaluateNeeds híbrido (survival/resources do snapshot; curiosity por timer); shelter/social stub.
- NEED-02: urgency monotônica no tempo ignorado (anti-starvation) com pesos por config.
- GOAL-01: Goal com priority/progress/dependsOn; generateGoals a partir de necessidades.
- GOAL-02: selectGoal com histerese + preempção (sobrevivência crítica / pedido em ASSISTANT).
- Módulos 100% puros (sem Date.now, sem config global, sem bot/LLM).
</success_criteria>

<output>
Após completar, criar `.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-02-SUMMARY.md`.
</output>
