---
phase: 03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - src/cognition/loop.phase3.smoke.test.ts
  - src/cognition/reconnect.test.ts
autonomous: false
requirements: [COG-03, CONN-03, NEED-01, NEED-02, GOAL-01, GOAL-02, LLM-02, CHAT-01, CHAT-02]

must_haves:
  truths:
    - "Sem LM Studio (provider indisponível), o loop continua rodando degradado ao arbiter (D-17) — nunca trava nem age inseguro."
    - "Após uma reconexão simulada (bot.once('end')→nova sessão com o MESMO holder), needs/goals/memory NÃO reiniciam do zero (CONN-03)."
    - "O tick reativo não bloqueia na deliberação LLM (a deliberação é single-flight fora do grafo)."
    - "Um humano confirma ao vivo: o agente raciocina/responde via LLM em ASSISTANT, sobrevive autônomo em AUTONOMOUS, troca de disposição por chat, e degrada gracioso quando o LLM cai."
  artifacts:
    - path: "src/cognition/loop.phase3.smoke.test.ts"
      provides: "Smoke headless do loop Fase 3 (deliberação fallback, needs/goals no grafo) sem servidor MC"
      contains: "fallback"
    - path: "src/cognition/reconnect.test.ts"
      provides: "Prova de CONN-03: holder reusado entre sessões preserva needs/goals/memory"
      contains: "CONN-03"
  key_links:
    - from: "src/cognition/reconnect.test.ts"
      to: "src/cognition/state.ts"
      via: "cria holder 1x, simula duas sessões, asserta estado preservado"
      pattern: "createCognitiveStateHolder"
    - from: "src/cognition/loop.phase3.smoke.test.ts"
      to: "deliberação/grafo"
      via: "provider indisponível → arbiter fallback; ticks múltiplos sem travar"
      pattern: "available|fallback"
---

<objective>
Verificar a Fase 3 em duas frentes: (1) testes headless automatizados que provam as propriedades estruturais críticas sem servidor MC nem LM Studio — degradação ao arbiter quando o LLM está off (D-17), CONN-03 (mente sobrevive à reconexão), e tick não-bloqueante; (2) um checkpoint humano ao vivo que confirma raciocínio/conversa LLM, os dois modos de disposição, troca por chat e degradação gracioso — o que só faz sentido com um servidor real + LM Studio.

Este plano não adiciona features — fecha a fase provando que os critérios de sucesso da ROADMAP estão atendidos. Espelha o padrão do Plano 02-04 (smoke headless + checkpoint ao vivo).

Purpose: Garantir que o loop nunca trava (com ou sem LLM) e que a mente persiste à reconexão, com validação humana do comportamento emergente.
Output: dois arquivos de teste headless + um checkpoint de verificação humana.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md

# Saídas dos planos anteriores
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-01-SUMMARY.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-02-SUMMARY.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-03-SUMMARY.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-04-SUMMARY.md

# Padrão de smoke test a espelhar + contratos
@src/cognition/loop.smoke.test.ts
@src/cognition/graph.ts
@src/cognition/state.ts
@src/cognition/deliberation.ts

<interfaces>
<!-- Padrão de mock de bot já existente em loop.smoke.test.ts (makeMockBot) — reusar/estender. -->

makeMockBot() de src/cognition/loop.smoke.test.ts fornece: username, health, food, entity.position, time, entities, players, inventory.items(), findBlocks, blockAt, findBlock, pathfinder.goto, on, once. Estender se a Fase 3 ler campos novos.

Holder/deliberação (Plan 03):
```typescript
export function createCognitiveStateHolder(now?): CognitiveStateHolder  // needs(5), goals[], memory, control, safety, disposition, llmDecision:null
export function buildGraph({ bot, holder, provider })
export function createDeliberator(): { maybeDeliberate(...) }  // OU maybeDeliberate(state, holder, provider, snapshot, trigger, now)
```

LlmProvider MOCK p/ "LM Studio off": `{ available: async () => false, decide: async () => { throw new Error('off') }, chat: async () => '' }` → força o caminho fallback (D-17).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke headless Fase 3 — degradação ao arbiter (D-17) + tick não-bloqueante (COG-03)</name>
  <files>src/cognition/loop.phase3.smoke.test.ts</files>
  <read_first>
    - src/cognition/loop.smoke.test.ts (makeMockBot e estrutura de teste a espelhar)
    - src/cognition/graph.ts (nova LoopAnnotation/buildGraph com holder+provider)
    - src/cognition/nodes.ts (analyze usa llmDecision OU arbiter; observe pluga needs/goals)
    - src/cognition/deliberation.ts (maybeDeliberate single-flight)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md (Pitfall 3 tick não bloqueia; D-17; Environment Availability: LM Studio ausente coberto por D-17)
  </read_first>
  <action>
    Criar `src/cognition/loop.phase3.smoke.test.ts` com `bun:test`, reusando/estendendo o makeMockBot do smoke da Fase 2:
    - Teste A (D-17 LLM off → arbiter): construir o grafo com holder (createCognitiveStateHolder) e um LlmProvider MOCK com `available: async () => false`. Rodar ~10 ticks via graph.invoke. Asserir que o loop NÃO lança, que cogState é resolvido (sem decisão LLM → arbiter), e que a memória acumula eventos (o agente continua agindo degradado). Provar que llmDecision permanece null ou não impede a ação.
    - Teste B (needs/goals plugados no grafo — NEED-01/GOAL-01): com snapshot mock de vida/fome baixas, rodar ticks e asserir que holder.needs foi atualizado (survival reflete health/food do snapshot — D-09) e que ao menos um goal pode ser gerado quando a urgência cruza o limiar (usar uma config de teste com goalThreshold baixo se necessário, ou asserir generateGoals diretamente sobre holder.needs).
    - Teste C (tick não-bloqueante — COG-03/Pitfall 3): usar um LlmProvider MOCK cujo `available` resolve true e `decide` demora (ex.: 200ms via setTimeout) — disparar maybeDeliberate em paralelo e asserir que múltiplos ticks do grafo completam ENQUANTO a "deliberação lenta" ainda está pendente (a deliberação é single-flight fora do grafo; o tick não a aguarda). Verificar que uma segunda chamada de maybeDeliberate durante a primeira NÃO dispara (single-flight).
    - Manter timeouts generosos (60_000) como no smoke da Fase 2.
  </action>
  <verify>
    <automated>bun test src/cognition/loop.phase3.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "available: async () => false" src/cognition/loop.phase3.smoke.test.ts` (cenário LM Studio off — D-17)
    - `grep -q "fallback\|arbiter\|available" src/cognition/loop.phase3.smoke.test.ts`
    - O teste prova: loop não lança com provider off; memória acumula; needs atualizadas do snapshot; single-flight bloqueia segunda deliberação concorrente
    - `bun test src/cognition/loop.phase3.smoke.test.ts` passa (todos os 3 testes A/B/C verdes)
  </acceptance_criteria>
  <done>Smoke headless prova degradação ao arbiter sem LLM, motivação plugada no grafo, e tick reativo não bloqueia na deliberação single-flight.</done>
</task>

<task type="auto">
  <name>Task 2: Teste de reconexão — CONN-03 (mente sobrevive à troca de sessão)</name>
  <files>src/cognition/reconnect.test.ts</files>
  <read_first>
    - src/cognition/state.ts (createCognitiveStateHolder — fonte única em-processo)
    - src/cognition/loop.ts (startCognitiveLoop(bot, holder) — bot.once('end') encerra a sessão)
    - src/bot/index.ts (holder criado 1x antes de createBot)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md (Pattern 4; Pitfall 2 estado reinicia na reconexão)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md (D-20 em-processo apenas)
  </read_first>
  <action>
    Criar `src/cognition/reconnect.test.ts` com `bun:test` provando CONN-03 a nível de unidade/integração (sem servidor real):
    - Criar UM holder via createCognitiveStateHolder. Mutar o estado para simular trabalho de uma sessão: empurrar eventos na memória (push), avançar progresso de um goal / setar holder.goals, alterar holder.disposition para 'ASSISTANT', degradar uma need (ex.: survival value baixo).
    - Simular o fim da sessão 1 e o início da sessão 2 reusando o MESMO holder (o padrão real: bot/index cria 1x e passa às duas sessões). Construir um grafo/loop novo (nova "sessão") apontando para o MESMO holder.
    - Asserir que após a "reconexão": holder.memory.events ainda contém os eventos da sessão 1; holder.goals/currentGoal preservados; holder.disposition ainda 'ASSISTANT'; holder.needs com os valores degradados (NÃO resetados para 1). Isto prova que a mente não reinicia (Pitfall 2 evitado).
    - Comentar o arquivo referenciando CONN-03 e D-20 (em-processo; o restart completo do processo é Fase 4 — este teste NÃO testa persistência em disco).
    - (Opcional, se viável com o mock) usar o makeMockBot + bot.once('end') para exercitar o ciclo real de encerramento de loop, asserindo que o holder injetado é o mesmo objeto entre as duas chamadas de startCognitiveLoop.
  </action>
  <verify>
    <automated>bun test src/cognition/reconnect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "createCognitiveStateHolder" src/cognition/reconnect.test.ts` (holder único reusado)
    - `grep -q "CONN-03" src/cognition/reconnect.test.ts` (intenção documentada)
    - O teste prova: memory/goals/disposition/needs preservados após simular reconexão com o mesmo holder
    - `grep -rn "bun:sqlite\|writeFile\|readFile" src/cognition/reconnect.test.ts` retorna VAZIO (em-processo apenas — D-20)
    - `bun test src/cognition/reconnect.test.ts` passa
  </acceptance_criteria>
  <done>Teste prova que o estado cognitivo (memória/objetivos/disposição/necessidades) sobrevive a uma reconexão simulada via holder reusado, sem qualquer persistência em disco.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humano ao vivo — raciocínio LLM, dois modos, troca por chat, degradação</name>
  <files>(nenhum arquivo — verificação manual ao vivo)</files>
  <read_first>
    - .planning/ROADMAP.md (Fase 3 — 5 critérios de sucesso a confirmar ao vivo)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md (D-01/D-02/D-07/D-13/D-15/D-17)
  </read_first>
  <action>
    Pausar a execução e apresentar ao USUÁRIO o roteiro de verificação ao vivo do bloco <how-to-verify> abaixo. NÃO automatizar — este passo requer um servidor Minecraft real + LM Studio carregado, ambos na máquina do usuário. Aguardar o sinal de retomada (<resume-signal>). Se o usuário relatar falha, registrar passo+sintoma para gerar gaps via `/gsd:plan-phase --gaps`.
  </action>
  <what-built>
    O loop completo da Fase 3: LLM local (LM Studio) guiando decisão/conversa atrás do provider abstraído, com saída restrita + fallback ao arbiter; necessidades→objetivos com histerese; eixo de disposição AUTONOMOUS/ASSISTANT trocável por chat literal; estado que sobrevive à reconexão. Todos os testes headless (Tasks 1-2 + Planos 01-04) já passam automaticamente.
  </what-built>
  <how-to-verify>
    Pré-requisitos do USUÁRIO (humano): servidor Minecraft Java local 1.21.x rodando (dir `minecraft-server/`); LM Studio com um modelo >= 7B carregado e o servidor habilitado em http://localhost:1234/v1; `.env` com LLM_MODEL correto.

    1. Iniciar: `bun run start`. Entrar no servidor com um cliente Minecraft.
    2. **AUTONOMOUS (default):** Observar no console que o agente percebe→decide→age sozinho (logs de estado/needs/goals). No chat, mande uma mensagem qualquer ("oi") — o agente deve praticamente IGNORAR (D-07: conversa mínima). Confirmar que ele segue sobrevivendo/coletando/explorando.
    3. **Trocar disposição por chat:** Digite `!ajudante` no chat. O agente passa a ASSISTANT.
    4. **ASSISTANT — conversa coerente (CHAT-02):** Mande "oi, tudo bem?" — o agente responde de forma coerente, curta e na sua língua (pt-BR, D-02), com tom de sobrevivente pragmático (D-01).
    5. **ASSISTANT — pedido vira objetivo (D-13):** Mande "me ajuda a coletar madeira" — o agente deve aceitar e gerar um objetivo (ver logs de goal source player_request) e agir nele, com comprometimento (não troca a cada tick — GOAL-02).
    6. **Preempção por sobrevivência (D-15):** Deixe a vida/fome cair (ou force perigo) e confirme que sobrevivência crítica interrompe o objetivo atual.
    7. **Controle ainda funciona:** `!pausar` para o agente; `!auto` (ou `!livre`) volta ao autônomo — confirmar que controle e disposição são independentes.
    8. **Degradação gracioso (D-17):** Pare o servidor do LM Studio. Confirme no console que o loop CONTINUA rodando (degradado ao arbiter da Fase 2), sem travar nem lançar — o agente segue agindo por regra fixa. Religue o LM Studio e confirme que o raciocínio LLM volta.
    9. **Reconexão (CONN-03):** Force uma desconexão (kick/restart do servidor MC). Após o reconnect automático, confirme que o agente NÃO recomeçou do zero (objetivos/memória/disposição preservados — pode confirmar pelos logs do holder).
  </how-to-verify>
  <acceptance_criteria>
    - Usuário responde "aprovado" após executar os 9 passos do <how-to-verify>, OU descreve falhas específicas (passo + sintoma).
    - Confirmados ao vivo: raciocínio/conversa LLM em ASSISTANT; conversa mínima em AUTONOMOUS; troca de disposição por chat; pedido→objetivo restrito; preempção por sobrevivência; controle independente da disposição; degradação ao arbiter sem LLM; mente preservada na reconexão.
  </acceptance_criteria>
  <done>Usuário confirma "aprovado" cobrindo os critérios de sucesso 1-5 da ROADMAP Fase 3, ou gaps registrados.</done>
  <resume-signal>Digite "aprovado" se todos os passos funcionarem, ou descreva o que falhou (passo + sintoma) para gerar gaps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LM Studio ausente/instável → loop | Indisponibilidade do LLM durante operação ao vivo. |
| reconexão real → holder | Ciclo real de end→nova sessão com holder reusado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Denial of Service | loop ao vivo sem LM Studio | mitigate | Verificação explícita (passo 8) de que o loop continua degradado ao arbiter quando o LLM cai (D-17) — provado também headless na Task 1. |
| T-03-17 | Tampering | conversa ao vivo (prompt injection do jogador) | mitigate | Validado ao vivo (passos 4-5) que injection no chat não produz ação fora do enum fechado; comandos !pausar etc. são literais (passo 7). Cobertura headless nos Planos 01/03/04. |
</threat_model>

<verification>
- `bun test` — TODA a suíte da fase verde (Planos 01-05 + testes da Fase 1/2 intactos).
- `bunx tsc --noEmit` — sem erros.
- Checkpoint humano: "aprovado" cobrindo raciocínio LLM, dois modos, troca por chat, preempção, degradação e reconexão.
</verification>

<success_criteria>
- COG-03/D-17: loop degrada ao arbiter sem LLM (headless + ao vivo); tick não bloqueia.
- CONN-03: mente sobrevive à reconexão (headless + ao vivo).
- NEED/GOAL: necessidades→objetivos com histerese observáveis (headless + ao vivo).
- CHAT-01/02: conversa coerente em ASSISTANT, mínima em AUTONOMOUS (ao vivo).
- Critérios de sucesso 1-5 da ROADMAP Fase 3 confirmados.
</success_criteria>

<output>
Após completar, criar `.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-05-SUMMARY.md`.
</output>
