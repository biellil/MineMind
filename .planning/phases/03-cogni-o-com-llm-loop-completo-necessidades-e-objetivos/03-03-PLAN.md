---
phase: 03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/config.ts
  - src/cognition/state.ts
  - src/cognition/deliberation.ts
  - src/cognition/state.test.ts
  - src/cognition/deliberation.test.ts
  - src/cognition/graph.ts
  - src/cognition/nodes.ts
  - src/cognition/loop.ts
  - src/bot/index.ts
  - .env.example
autonomous: true
requirements: [COG-03, CONN-03, NEED-02, GOAL-02, LLM-01, LLM-02]

must_haves:
  truths:
    - "O estado cognitivo (needs, goals, control, safety, memory, disposition, decisão LLM, playerRequestPending) vive num holder criado UMA vez em bot/index.ts e injetado em cada sessão — sobrevive ao ciclo bot.once('end')→nova sessão (CONN-03)."
    - "O peso das necessidades varia por disposição: motivationConfigFor(disposition) entrega um MotivationConfig com weights distintos para AUTONOMOUS vs ASSISTANT (D-06/D-10), e observe/deliberation usam o cfg da disposição atual do holder."
    - "A deliberação LLM roda FORA do grafo, single-flight, event-driven com intervalo mínimo de replanejamento, escrevendo a decisão no holder."
    - "O nó decide lê a decisão LLM fresca/válida do holder; se ausente/velha, usa o arbiter (D-17 fallback)."
    - "O tick rápido (camada reativa) nunca bloqueia esperando a inferência LLM."
    - "Após uma reconexão simulada, needs/goals/memory do holder NÃO reiniciam do zero."
  artifacts:
    - path: "src/cognition/state.ts"
      provides: "CognitiveStateHolder + createCognitiveStateHolder (CONN-03/D-20)"
      exports: ["CognitiveStateHolder", "createCognitiveStateHolder"]
    - path: "src/cognition/deliberation.ts"
      provides: "maybeDeliberate single-flight event-driven (COG-03/D-19)"
      exports: ["maybeDeliberate", "shouldTrigger"]
    - path: "src/config.ts"
      provides: "Knobs .env de disposição/necessidades/objetivos/LLM/replan + motivationConfigFor(disposition) (pesos por disposição — D-06/D-10)"
      contains: "motivationConfigFor"
  key_links:
    - from: "src/bot/index.ts"
      to: "src/cognition/state.ts"
      via: "createCognitiveStateHolder() chamado 1x antes de createBot"
      pattern: "createCognitiveStateHolder\\(\\)"
    - from: "src/cognition/loop.ts"
      to: "holder"
      via: "startCognitiveLoop(bot, holder) recebe o holder por parâmetro"
      pattern: "startCognitiveLoop\\(bot, holder\\)|holder"
    - from: "src/cognition/deliberation.ts"
      to: "src/llm/structured.ts"
      via: "decideAction(provider, messages, arbiterFallback)"
      pattern: "decideAction\\("
    - from: "src/cognition/nodes.ts"
      to: "holder.llmDecision"
      via: "decide lê a decisão LLM do holder com checagem de frescor"
      pattern: "llmDecision"
    - from: "src/cognition/nodes.ts"
      to: "src/config.ts (motivationConfigFor)"
      via: "observe usa motivationConfigFor(holder.disposition) para evaluateNeeds/generateGoals/selectGoal (pesos por disposição — D-06)"
      pattern: "motivationConfigFor\\("
---

<objective>
Integrar a fundação LLM (Plan 01) e a motivação (Plan 02) no loop da Fase 2, entregando as três peças de integração mais estruturais da fase:

1. **Estado durável fora-do-bot (CONN-03/D-20):** mover a criação de control/safety/memory + os novos needs/goals/disposition/llmDecision para um `CognitiveStateHolder` criado UMA vez em `bot/index.ts` e injetado em cada `startCognitiveLoop(bot, holder)`. A reconexão deixa de reiniciar a mente.
2. **Deliberação em duas taxas (COG-03/D-19):** um serviço event-driven single-flight FORA do grafo que chama `decideAction` (Plan 01) com o arbiter como fallback (D-17) e escreve a decisão no holder. A camada reativa (grafo da Fase 2) lê a decisão pronta — nunca espera o LLM.
3. **Wiring needs/goals no grafo + config .env:** o `analyze`/`decide` consultam needs/goals do holder; novos knobs em `config.ts`.

Purpose: Acoplar LLM + motivação ao loop provado da Fase 2 sem violar single-flight nem a separação de taxas, mantendo o arbiter como piso.
Output: state.ts, deliberation.ts, graph/nodes/loop refatorados, config + .env.example, bot/index wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md

# Saídas dos planos dependentes (contratos a consumir)
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-01-SUMMARY.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-02-SUMMARY.md

# Código existente a refatorar (LER O ESTADO ATUAL ANTES DE EDITAR)
@src/cognition/loop.ts
@src/cognition/graph.ts
@src/cognition/nodes.ts
@src/cognition/arbiter.ts
@src/cognition/safety.ts
@src/control/commands.ts
@src/bot/index.ts
@src/config.ts

<interfaces>
<!-- Contratos do Plan 01 e Plan 02 (ler os SUMMARYs para assinaturas finais). -->

Plan 01 — src/llm/provider.ts, structured.ts, schemas.ts, prompts.ts:
```typescript
export interface LlmProvider { decide<T>(schema, messages): Promise<T>; chat(messages): Promise<string>; available(): Promise<boolean> }
export function createLmStudioProvider(): LlmProvider
export async function decideAction(provider, messages: BaseMessage[], fallback: () => ActionDecision): Promise<ActionDecision>
export const ActionDecisionSchema; export type ActionDecision = { action: 'gather'|'explore'|'navigate'|'idle'|'chat'; target?: string; reason: string }
export function buildPersonaPrompt(disposition): string; export function serializeContext(snap, needs, goals, events): string
```

Plan 02 — src/motivation/{types,needs,goals}.ts:
```typescript
export type NeedKind; export interface Need; export interface Goal; export interface MotivationConfig; export interface SelectGoalContext; export type Disposition
export function createNeeds(now): Need[]; export function evaluateNeeds(prev, snap, now, cfg): Need[]; export function urgency(n, now, cfg): number
export function generateGoals(needs, now, cfg): Goal[]; export function selectGoal(current, candidates, ctx, cfg): Goal|null; export function advanceProgress(goal, delta): Goal
```

Existente — assinaturas atuais a MUDAR:
```typescript
// loop.ts (HOJE): export function startCognitiveLoop(bot: Bot): void  — cria control/safety/graph internamente
// graph.ts (HOJE): export function buildGraph(deps: NodeDeps)  — LoopAnnotation { snapshot, cogState, memory }
// nodes.ts (HOJE): NodeDeps { bot, control, safety }; nós observe/analyze/updateMemory/decide/execute
// bot/index.ts (HOJE): onBotReady(bot) -> startCognitiveLoop(bot); createBot(onBotReady)
// arbiter.ts: arbitrate(snapshot, mode, excluded): CognitiveState  — vira a base do fallback (D-17)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CognitiveStateHolder (CONN-03/D-20) + knobs de config .env</name>
  <read_first>
    - src/cognition/loop.ts (onde control/safety/memory são criados por sessão hoje — linhas 16-18)
    - src/control/commands.ts (createControlState/ControlState — vai para o holder)
    - src/cognition/safety.ts (createSafetyState/SafetyState — vai para o holder)
    - src/memory/shortTerm.ts (createMemory/ShortTermMemory — vai para o holder)
    - src/config.ts (estrutura atual de knobs e validações de startup)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-02-SUMMARY.md (assinaturas finais de createNeeds/MotivationConfig/Disposition)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md (Pattern 4 Estado Durável Fora-do-Bot; A4 holder = fonte única)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md (D-20 escopo EM-PROCESSO apenas; D-10 pesos por disposição; D-19 replan)
  </read_first>
  <action>
    Criar `src/cognition/state.ts`:
    - `export interface CognitiveStateHolder { control: ControlState; safety: SafetyState; memory: ShortTermMemory; needs: Need[]; goals: Goal[]; currentGoal: Goal | null; disposition: Disposition; playerRequestPending: boolean; llmDecision: { decision: ActionDecision; at: number } | null }`. Importar tipos de '../control/commands', './safety', '../memory/shortTerm', '../motivation/types', '../llm/schemas'. NOTA: `playerRequestPending` é declarado AQUI (default `false`) porque é consumido por `selectGoal` (Plan 02 via SelectGoalContext) e derivado no `observe` (Task 3); o Plan 04 apenas SETA o valor (caminho conversacional), nunca estende a estrutura — evita furo temporal de contrato/tsc.
    - `export function createCognitiveStateHolder(now: number = Date.now()): CognitiveStateHolder` que cria control (`createControlState('autonomous')`), safety (`createSafetyState()`), memory (`createMemory(config.memoryTokenBudget)`), needs (`createNeeds(now)`), goals `[]`, currentGoal `null`, disposition lida de `config.dispositionDefault`, playerRequestPending `false`, llmDecision `null`.
    - `memory` é mutável aqui (o holder é a fonte única — Pattern 4/A4): expor um setter `setMemory(m)` OU deixar `memory` como propriedade reatribuível. Escolher: propriedade reatribuível `holder.memory = ...` (mais simples; o holder é objeto mutável vivo no processo). Documentar que NÃO há persistência em disco (D-20 em-processo; NÃO usar bun:sqlite/JSON).

    Em `src/config.ts`, ADICIONAR (sem remover nada existente) uma seção "=== Fase 3: LLM, disposição, necessidades, objetivos ===" com estes knobs (todos com default e lidos via `process.env.X || default`):
    - `llmBaseUrl: process.env.LLM_BASE_URL || 'http://localhost:1234/v1'`
    - `llmModel: process.env.LLM_MODEL || 'local-model'`
    - `llmTemperature: parseFloat(process.env.LLM_TEMPERATURE || '0.4')`
    - `dispositionDefault: (process.env.DISPOSITION_DEFAULT || 'AUTONOMOUS') as 'AUTONOMOUS'|'ASSISTANT'` (D-04)
    - `proactivity: (process.env.PROACTIVITY || 'reactive') as 'reactive'|'proactive'` (D-12)
    - `replanMinIntervalMs: parseInt(process.env.REPLAN_MIN_INTERVAL_MS || '8000', 10)` (D-19 orçamento de replanejamento; default conservador/lento)
    - `needWeightsFor(disposition)`: pesos POR DISPOSIÇÃO (D-06/D-10 — o eixo de disposição modula o peso das necessidades). Valores default CONCRETOS:
      - **AUTONOMOUS** (sobrevivência autônoma, equilibrado): `{ survival: 1.0, resources: 1.0, curiosity: 1.0, shelter: 0, social: 0 }` — as 3 ativas equilibradas; o agente explora/coleta/sobrevive sem viés para jogadores.
      - **ASSISTANT** (companheiro): `{ survival: 1.0, resources: 0.9, curiosity: 0.4, shelter: 0, social: 0 }` — curiosity reduzida (menos vagueia sozinho, fica mais disponível ao jogador) e survival mantido como PISO anti-starvation (nunca abaixo dos demais, casa com D-11/D-15a).
      - Cada peso default acima é sobrescrevível por envs opcionais por disposição: `NEED_WEIGHT_<DISP>_SURVIVAL/RESOURCES/CURIOSITY` (ex.: `NEED_WEIGHT_ASSISTANT_CURIOSITY=0.4`). Implementar como função pura de leitura de config, não estado global.
    - `curiosityDecayPerMs: parseFloat(process.env.CURIOSITY_DECAY_PER_MS || '0.00001')`
    - `starvationBoostPerMs: parseFloat(process.env.STARVATION_BOOST_PER_MS || '0.000005')` (D-11)
    - `goalThreshold: parseFloat(process.env.GOAL_THRESHOLD || '0.5')`
    - `hysteresisMargin: parseFloat(process.env.HYSTERESIS_MARGIN || '0.25')` (D-15 histerese alta/conservadora)
    - `survivalCriticalThreshold: parseFloat(process.env.SURVIVAL_CRITICAL_THRESHOLD || '0.3')` (D-15 preempção por perigo)
    - `resourceTargets: (process.env.RESOURCE_TARGETS || 'oak_log,cobblestone,bread').split(',')`
    - Adicionar `export function motivationConfigFor(disposition: Disposition): MotivationConfig` que monta um `MotivationConfig` com `weights: needWeightsFor(disposition)` (D-06) e os demais knobs compartilhados (curiosityDecayPerMs, starvationBoostPerMs, goalThreshold, hysteresisMargin, survivalCriticalThreshold, resourceTargets). É esta função (não um cfg global único) que `observe`/`deliberation` chamam com `holder.disposition` para passar às funções puras do Plan 02. Manter também um `motivationConfig` default (= `motivationConfigFor(config.dispositionDefault)`) só por conveniência/retrocompat, mas o caminho preferido é `motivationConfigFor(holder.disposition)`.
    - Adicionar validações de startup: replanMinIntervalMs >= 0; goalThreshold/hysteresisMargin/survivalCriticalThreshold em [0,1]; dispositionDefault ∈ {AUTONOMOUS,ASSISTANT}.

    Atualizar `.env.example` adicionando todos os novos knobs com comentários (D-XX) e valores default, incluindo os envs opcionais de pesos por disposição `NEED_WEIGHT_AUTONOMOUS_*` e `NEED_WEIGHT_ASSISTANT_*` (D-06/D-10) com os defaults concretos comentados.

    Criar `src/cognition/state.test.ts`: cobre que createCognitiveStateHolder inicializa needs com as 5 kinds, disposition = default da config, playerRequestPending false, llmDecision null; que mutar holder.needs/holder.goals reflete no mesmo objeto (prova de fonte única em-processo); e que `motivationConfigFor('AUTONOMOUS')` e `motivationConfigFor('ASSISTANT')` retornam weights distintos (curiosity menor em ASSISTANT; survival como piso — D-06/D-10).
  </action>
  <verify>
    <automated>bun test src/cognition/state.test.ts && bunx tsc --noEmit && grep -q "replanMinIntervalMs" src/config.ts && grep -q "dispositionDefault" src/config.ts && grep -q "motivationConfigFor" src/config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function createCognitiveStateHolder" src/cognition/state.ts`
    - `grep -q "createNeeds(" src/cognition/state.ts` (needs do Plan 02 inicializadas no holder)
    - `grep -q "replanMinIntervalMs" src/config.ts` e `grep -q "hysteresisMargin" src/config.ts` e `grep -q "starvationBoostPerMs" src/config.ts`
    - `grep -q "motivationConfigFor" src/config.ts` (pesos por disposição — D-06/D-10) e o teste prova que `motivationConfigFor('AUTONOMOUS').weights.curiosity !== motivationConfigFor('ASSISTANT').weights.curiosity`
    - `grep -q "playerRequestPending" src/cognition/state.ts` (campo no holder, default false — consumido por selectGoal/observe; Plan 04 só seta)
    - `grep -q "dispositionDefault" src/config.ts`
    - `grep -rn "bun:sqlite\|writeFileSync\|fs.writeFile" src/cognition/state.ts` retorna VAZIO (D-20 em-processo, sem persistência em disco)
    - `grep -q "REPLAN_MIN_INTERVAL_MS" .env.example` e `grep -q "DISPOSITION_DEFAULT" .env.example`
    - `bun test src/cognition/state.test.ts` passa
    - `bunx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>Holder criável com needs/goals/control/safety/memory/disposition/playerRequestPending/llmDecision; config + .env.example com todos os knobs da Fase 3 + motivationConfigFor(disposition) com pesos por disposição (D-06); sem persistência em disco; teste do holder verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deliberação single-flight FORA do grafo (COG-03/D-19) com fallback ao arbiter (D-17)</name>
  <read_first>
    - src/cognition/state.ts (CognitiveStateHolder — criado na Task 1; escreve llmDecision aqui)
    - src/cognition/arbiter.ts (arbitrate — vira o fallback determinístico)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-01-SUMMARY.md (assinaturas de decideAction/createLmStudioProvider/buildPersonaPrompt/serializeContext)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md (Pattern 3 Duas Taxas single-flight fora do grafo; Pitfall 3 tick não bloqueia)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md (D-19 event-driven + intervalo mínimo; D-02 Fase 2 single-flight)
  </read_first>
  <behavior>
    - Com inFlight=true, maybeDeliberate retorna imediatamente sem chamar o provider (single-flight, D-02/D-19).
    - Com (now - lastRunAt) < replanMinIntervalMs, maybeDeliberate retorna sem chamar (orçamento de replanejamento, D-19).
    - Com shouldTrigger=false, maybeDeliberate não chama (event-driven, D-19).
    - Quando dispara: chama decideAction com um fallback que produz uma ActionDecision a partir do arbiter; grava holder.llmDecision = { decision, at: now }; reseta inFlight no finally mesmo se decideAction lançar (mas decideAction não lança — Plan 01).
    - shouldTrigger é true para: evento de chat (ASSISTANT), objetivo concluído/falho, ou necessidade que cruza limiar; false caso contrário (com teto de frequência aplicado por replanMinIntervalMs no maybeDeliberate).
  </behavior>
  <action>
    Criar `src/cognition/deliberation.ts`:
    - Estado de single-flight module-scoped OU encapsulado num objeto criado por factory (preferir factory `createDeliberator()` retornando `{ maybeDeliberate }` com `inFlight`/`lastRunAt` no closure — testável e evita estado global entre testes).
    - `export type DeliberationTrigger = 'chat' | 'goal_changed' | 'need_threshold' | 'periodic'`
    - `export function shouldTrigger(trigger: DeliberationTrigger, holder: CognitiveStateHolder): boolean` — true p/ chat (só se disposition ASSISTANT — em AUTONOMOUS conversa mínima, D-07), goal_changed, need_threshold; periodic só como rede (true mas o teto de frequência limita).
    - `export async function maybeDeliberate(state, holder, provider, snapshot, trigger, now): Promise<void>` (`state` = o closure de inFlight/lastRunAt). Lógica EXATA do Pattern 3:
      ```
      if (state.inFlight) return
      if (now - state.lastRunAt < config.replanMinIntervalMs) return
      if (!shouldTrigger(trigger, holder)) return
      state.inFlight = true
      try {
        const messages = [systemMessage(buildPersonaPrompt(holder.disposition)),
                          humanMessage(serializeContext(snapshot, holder.needs, holder.currentGoal, getEvents(holder.memory)))]
        const fallback = () => arbiterToDecision(snapshot, holder.control.getMode())   // D-17
        const decision = await decideAction(provider, messages, fallback)
        holder.llmDecision = { decision, at: now }
      } finally { state.inFlight = false; state.lastRunAt = now }
      ```
    - Implementar helper `arbiterToDecision(snapshot, mode): ActionDecision` que chama `arbitrate(snapshot, mode, new Set())` e mapeia o CognitiveState resultante para uma ActionDecision do enum fechado (gathering→'gather', exploring→'explore', socializing→'chat'/'navigate', idle→'idle'), com reason 'fallback:arbiter'. Isto materializa "o arbiter é o piso" (D-17).
    - Importar systemMessage/humanMessage helpers de '@langchain/core/messages' (SystemMessage/HumanMessage).
    - NÃO colocar nada disto como nó do grafo (Pattern 3/Pitfall 3 — a inferência lenta nunca entra no tick rápido).

    Criar `src/cognition/deliberation.test.ts` cobrindo cada item do <behavior> com um LlmProvider MOCK e um snapshot mock mínimo (sem rede). Testar especialmente: single-flight (segunda chamada concorrente não dispara), replan budget (chamada dentro do intervalo não dispara), e que holder.llmDecision é gravado quando dispara.
  </action>
  <verify>
    <automated>bun test src/cognition/deliberation.test.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "if (state.inFlight) return" src/cognition/deliberation.ts` (single-flight)
    - `grep -q "config.replanMinIntervalMs" src/cognition/deliberation.ts` (orçamento de replanejamento)
    - `grep -q "decideAction(" src/cognition/deliberation.ts` (usa o caminho do Plan 01 com fallback)
    - `grep -q "arbitrate(" src/cognition/deliberation.ts` (arbiter como piso/fallback — D-17)
    - `grep -q "holder.llmDecision" src/cognition/deliberation.ts` (escreve no holder, não no grafo)
    - `grep -rn "addNode\|StateGraph" src/cognition/deliberation.ts` retorna VAZIO (deliberação NÃO é nó do grafo — Pattern 3)
    - `bun test src/cognition/deliberation.test.ts` passa todos os casos do <behavior>
    - `bunx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>maybeDeliberate respeita single-flight + replan budget + event-driven, escreve a decisão LLM no holder, e usa o arbiter como fallback; nunca é um nó do grafo.</done>
</task>

<task type="auto">
  <name>Task 3: Wiring — grafo/nós consomem holder+needs+goals+llmDecision; loop dispara deliberação; bot/index injeta holder (CONN-03)</name>
  <read_first>
    - src/cognition/graph.ts (LoopAnnotation e buildGraph atuais — adicionar campos needs/goals/disposition; alimentar do holder)
    - src/cognition/nodes.ts (nós atuais; analyze usa arbitrate; decide é no-op; execute dispara skill)
    - src/cognition/loop.ts (driver atual; muda assinatura para receber holder; dispara maybeDeliberate)
    - src/bot/index.ts (onBotReady atual chama startCognitiveLoop(bot))
    - src/cognition/state.ts e src/cognition/deliberation.ts (criados nas Tasks 1-2)
    - src/config.ts (motivationConfigFor + knobs Fase 3 — criados na Task 1; observe usa motivationConfigFor(holder.disposition))
    - src/llm/provider.ts (createLmStudioProvider — instanciado 1x por sessão no loop)
    - src/control/commands.ts (registerChatCommands — passa a receber holder.control)
    - src/cognition/loop.smoke.test.ts (smoke test existente — NÃO quebrar; a assinatura buildGraph/startCognitiveLoop muda, o smoke do Plan 05 cobre a nova forma)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md (Pattern 4; Pitfall 2 estado reinicia na reconexão; Pitfall 3)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md (D-20; D-17; D-19)
  </read_first>
  <action>
    NOTA DE ESCOPO: esta task toca os 4 arquivos estruturais (graph.ts, nodes.ts, loop.ts, bot/index.ts) numa cascata de assinaturas — LER OS 4 (+ state.ts/deliberation.ts/config.ts/provider.ts/commands.ts do read_first) ANTES de qualquer edição, e editar na ordem graph→nodes→loop→bot/index para reconciliar tipos numa passada só.

    **graph.ts:** Estender `LoopAnnotation` com campos `needs: Need[]`, `goals: Goal[]`, `currentGoal: Goal|null`, `disposition: Disposition` (reducers `(_p,u)=>u`, defaults vindos do holder). `buildGraph(deps)` passa a receber `deps` estendido `NodeDeps & { holder: CognitiveStateHolder; provider: LlmProvider }`. Manter MemorySaver, mas o holder é a fonte única (A4): o `observe` semeia `needs/goals/memory` do holder no estado anotado a cada tick e o `execute`/`analyze` escrevem de volta no holder.

    **nodes.ts:**
    - `NodeDeps` estende para `{ bot, holder, provider }` (control/safety agora vêm de `holder.control`/`holder.safety`).
    - `observe`: além do snapshot, resolver o cfg da disposição atual UMA vez por tick `const mcfg = motivationConfigFor(holder.disposition)` (pesos por disposição — D-06/D-10); atualizar needs do holder via `holder.needs = evaluateNeeds(holder.needs, snapshot, now(), mcfg)`; gerar candidatos `generateGoals(holder.needs, now(), mcfg)` e selecionar `holder.currentGoal = selectGoal(holder.currentGoal, candidates, ctx, mcfg)` onde ctx = `{ survivalCritical: urgency(survivalNeed, now(), mcfg) cruza mcfg.survivalCriticalThreshold, playerRequestPending: holder.playerRequestPending, disposition: holder.disposition }`. Após consumir um pedido de jogador (objetivo player_request selecionado), resetar `holder.playerRequestPending = false`. Retornar snapshot + needs + goals + currentGoal para o estado anotado.
    - `analyze`: derivar cogState. Prioridade: se há `holder.llmDecision` FRESCA (now - at < config.replanMinIntervalMs*2, e válida) → mapear decision.action → CognitiveState; SENÃO usar `arbitrate(snapshot, holder.control.getMode(), excluded)` (D-17 fallback). Manter shouldFallbackToIdle/cooledDownTargets do safety.
    - `decide`: deixar de ser no-op — resolve target a partir de `holder.currentGoal`/llmDecision.target quando aplicável (ex.: gathering usa highestPriorityGatherTarget como hoje; goal de player_request fornece target). Documentar que params físicos continuam montados pelo executor/skillRegistry (D-10), nunca pelo LLM cru.
    - `execute`: inalterado no mecanismo single-flight de skill (D-02), mas grava memória no `holder.memory` (`holder.memory = push(holder.memory, ...)`) além de retornar para o estado anotado — holder é fonte única.
    - Importar evaluateNeeds/urgency/generateGoals/selectGoal de '../motivation/needs' e '../motivation/goals'; `motivationConfigFor` de '../config' (NÃO usar um cfg global único — os pesos variam por disposição, D-06).

    **loop.ts:** `export function startCognitiveLoop(bot: Bot, holder: CognitiveStateHolder): void`. NÃO criar control/safety internamente — usar `holder.control`/`holder.safety`. Criar `provider = createLmStudioProvider()` (1x por sessão; barato) e `deliberator = createDeliberator()`. `buildGraph({ bot, holder, provider })`. `registerChatCommands(bot, holder.control, ...)` (handler 1x por sessão; o caminho conversacional/disposição é adicionado no Plan 04). No driver assíncrono, A CADA tick: depois de `graph.invoke`, disparar (sem await bloqueante do tick) `void deliberator.maybeDeliberate(state, holder, provider, lastSnapshot, trigger, Date.now())` — a deliberação roda em paralelo ao próximo tick (Pattern 3/Pitfall 3: o tick NUNCA espera o LLM). Escolher `trigger` simples por ora: 'need_threshold' quando alguma urgency cruza goalThreshold, senão 'periodic'. NÃO bloquear o while na inferência.

    **bot/index.ts:** Criar o holder UMA vez ANTES de `createBot`: `const holder = createCognitiveStateHolder()`. `onBotReady(bot)` passa a chamar `startCognitiveLoop(bot, holder)`. Assim a reconexão (connection.ts chama onReady de novo) reusa o MESMO holder → a mente não reinicia (CONN-03/D-20/Pitfall 2). Atualizar o log da Fase 2 para Fase 3.
  </action>
  <verify>
    <automated>bunx tsc --noEmit && bun test src/cognition/state.test.ts src/cognition/deliberation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "startCognitiveLoop(bot, holder)" src/bot/index.ts` (holder injetado — CONN-03)
    - `grep -q "createCognitiveStateHolder()" src/bot/index.ts` e que a chamada está FORA/ANTES de createBot (holder criado 1x): `grep -B2 "createBot" src/bot/index.ts` mostra o holder antes
    - `grep -q "holder: CognitiveStateHolder" src/cognition/loop.ts` (assinatura recebe holder)
    - `grep -q "holder.control" src/cognition/loop.ts` (não recria control — usa o do holder)
    - `grep -q "evaluateNeeds(" src/cognition/nodes.ts` e `grep -q "selectGoal(" src/cognition/nodes.ts` (motivação plugada no grafo)
    - `grep -q "motivationConfigFor(holder.disposition)" src/cognition/nodes.ts` (pesos por disposição aplicados no observe — D-06/D-10; NÃO um cfg global único)
    - `grep -q "holder.playerRequestPending" src/cognition/nodes.ts` (sinal de pedido consumido por selectGoal e resetado após uso)
    - `grep -q "holder.llmDecision" src/cognition/nodes.ts` (decide/analyze lê a decisão LLM)
    - `grep -q "arbitrate(" src/cognition/nodes.ts` (arbiter mantido como fallback — D-17)
    - `grep -q "maybeDeliberate" src/cognition/loop.ts` e o tick NÃO usa `await` bloqueante nessa chamada: `grep -q "void deliberator.maybeDeliberate\|void deliberator\." src/cognition/loop.ts` (não bloqueia o tick — Pitfall 3)
    - `bunx tsc --noEmit` sem erros (assinaturas reconciliadas em todo o codebase)
    - `bun test src/cognition/state.test.ts src/cognition/deliberation.test.ts` verdes
  </acceptance_criteria>
  <done>Grafo consome needs/goals/llmDecision do holder; analyze degrada ao arbiter sem decisão LLM fresca; loop dispara deliberação sem bloquear o tick; holder criado 1x e injetado a cada sessão (reconexão não reinicia a mente).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decisão LLM (holder) → ação executada | A decisão escrita pela deliberação dirige o nó decide/execute. |
| reconexão (nova sessão bot) → holder compartilhado | O holder em-processo é reusado entre sessões; risco de leak/estado inconsistente. |
| `.env` → config (knobs LLM/replan) | Parâmetros operacionais lidos do ambiente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | src/cognition/nodes.ts (analyze lê llmDecision) | mitigate | analyze só usa a decisão LLM se FRESCA e mapeável ao enum fechado; senão `arbitrate` determinístico (D-17). decision.target nunca é montado como chamada física crua — executor/skillRegistry valida (D-10). |
| T-03-09 | DoS (auto-infligido) | src/cognition/loop.ts (deliberação por tick) | mitigate | maybeDeliberate é single-flight (inFlight) + cooldown replanMinIntervalMs (D-19) + event-driven; o tick nunca aguarda a inferência (void, não await). Anti-repetição/backoff da Fase 2 (safety) seguem ativos. |
| T-03-10 | Tampering | src/cognition/state.ts (holder reusado na reconexão) | mitigate | Holder criado 1x em bot/index.ts; handlers de chat/loop registrados 1x por sessão e morrem com a sessão (Pitfall 6 da Fase 2). Estado em-processo, sem disco (D-20). |
| T-03-11 | Information Disclosure | src/config.ts (.env knobs) | accept | Sem segredos reais (LM Studio local, apiKey dummy); knobs são parâmetros de tuning. Servidor local offline. |
</threat_model>

<verification>
- `bunx tsc --noEmit` — todo o codebase reconciliado com as novas assinaturas.
- `bun test src/cognition/` — state + deliberation verdes (o smoke da forma nova é Plan 05).
- grep prova: holder injetado (CONN-03), deliberação fora do grafo e não-bloqueante (COG-03), arbiter como fallback (D-17).
</verification>

<success_criteria>
- CONN-03: holder criado 1x em bot/index.ts, injetado por parâmetro; reconexão reusa a mesma mente.
- COG-03: deliberação LLM single-flight event-driven FORA do grafo; tick rápido não bloqueia.
- NEED-02/GOAL-02: needs/goals do Plan 02 plugados no observe (evaluateNeeds/selectGoal) com config por .env.
- LLM-01/02: decideAction integrado com o arbiter como fallback (D-17) materializado em arbiterToDecision.
</success_criteria>

<output>
Após completar, criar `.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-03-SUMMARY.md`.
</output>
