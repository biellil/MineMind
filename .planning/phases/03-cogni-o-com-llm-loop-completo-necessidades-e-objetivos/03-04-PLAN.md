---
phase: 03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos
plan: 04
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - src/control/commands.ts
  - src/control/disposition.ts
  - src/control/commands.test.ts
  - src/control/disposition.test.ts
  - src/chat/conversation.ts
  - src/chat/conversation.test.ts
  - src/cognition/loop.ts
autonomous: true
requirements: [CHAT-01, CHAT-02, GOAL-01]

must_haves:
  truths:
    - "O agente lê o chat do servidor e, em ASSISTANT (ou AUTONOMOUS com proatividade permitida), responde mensagens de jogadores de forma coerente via bot.chat — usando o prompt de persona."
    - "Em AUTONOMOUS o agente praticamente ignora jogadores: conversa essencialmente desligada (D-07)."
    - "Comandos de controle literais (incl. !auto) e comandos de disposição literais (!ajudante/!sozinho) são parseados ANTES do caminho conversacional, sem LLM — imunes a prompt injection."
    - "Em ASSISTANT, um pedido de jogador vira sinal para gerar objetivo dinâmico (playerRequestPending), restrito a tipos suportados; pedidos fora do conjunto recebem resposta conversacional educada, nunca objetivo inválido."
  artifacts:
    - path: "src/control/disposition.ts"
      provides: "parseDisposition literal (!ajudante/!sozinho) + estado de disposição (D-05)"
      exports: ["parseDisposition", "DISPOSITION_COMMANDS"]
    - path: "src/chat/conversation.ts"
      provides: "Caminho conversacional isolado: shouldRespond + handleConversation (CHAT-02)"
      exports: ["shouldRespond", "handleConversation"]
    - path: "src/control/commands.ts"
      provides: "!auto adicionado ao mapa COMMANDS (D-14)"
      contains: "!auto"
  key_links:
    - from: "src/cognition/loop.ts"
      to: "src/chat/conversation.ts"
      via: "handler bot.on('chat') chama parseCommand → parseDisposition → conversation"
      pattern: "parseDisposition|handleConversation|shouldRespond"
    - from: "src/chat/conversation.ts"
      to: "src/llm/provider.ts"
      via: "provider.chat(messages) com persona prompt"
      pattern: "provider\\.chat\\("
---

<objective>
Adicionar o caminho conversacional (CHAT-01/02) e o eixo de disposição (D-04/D-05), mantendo-os ISOLADOS do parser literal de controle (Pattern 5 / Pitfall 6). O agente lê todo chat de jogador próximo (D-12) e responde de forma coerente com a persona estática, mas só quando a disposição/proatividade permite — em AUTONOMOUS a conversa é mínima (D-07). Em ASSISTANT, pedidos viram sinal para objetivo dinâmico restrito a tipos suportados (D-13/GOAL-01). Adiciona `!auto` ao mapa de controle (D-14) e os comandos literais de disposição `!ajudante`/`!sozinho` (D-05).

A ordem no handler de chat é estrita: (1) comando de controle literal → muda modo; (2) comando de disposição literal → muda disposição; (3) caso contrário, caminho conversacional (só se permitido). Tudo literal/sem-LLM exceto o passo 3, que é a única conversa com LLM.

Purpose: Dar voz coerente ao agente sem deixar a conversa engolir comandos nem violar a separação Fase 2/Fase 3.
Output: disposition.ts, conversation.ts, commands.ts (+!auto), wiring no handler de chat do loop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md

# Saídas dos planos dependentes
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-01-SUMMARY.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-03-SUMMARY.md

# Código a estender (LER ANTES DE EDITAR)
@src/control/commands.ts
@src/cognition/loop.ts
@src/control/commands.test.ts

<interfaces>
<!-- Contratos a consumir. -->

De src/control/commands.ts (HOJE):
```typescript
const COMMANDS: Readonly<Record<string, ControlMode>> = { '!pausar':'paused','!vem':'standby','!aqui':'standby','!livre':'autonomous' }
export function parseCommand(message: string): ControlMode | null   // match literal exato (trim+lowercase)
export interface ControlState { getMode(): ControlMode; setMode(mode: ControlMode): void }
export function registerChatCommands(bot, control, onCommand?): void  // 1 handler bot.on('chat')
```

Plan 01 — src/llm/provider.ts, prompts.ts:
```typescript
export interface LlmProvider { chat(messages: BaseMessage[]): Promise<string>; ... }
export function buildPersonaPrompt(disposition): string
export type Disposition = 'AUTONOMOUS' | 'ASSISTANT'
```

Plan 03 — src/cognition/state.ts:
```typescript
export interface CognitiveStateHolder { control: ControlState; disposition: Disposition; goals: Goal[]; ... }
// loop.ts já registra registerChatCommands(bot, holder.control, ...) 1x por sessão (estendido aqui)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: !auto no controle (D-14) + comandos literais de disposição (D-05)</name>
  <read_first>
    - src/control/commands.ts (mapa COMMANDS e parseCommand — adicionar !auto SEM remover !livre)
    - src/control/commands.test.ts (padrão de teste literal a estender)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md (D-14 !auto alias de controle; D-05 disposição !ajudante/!sozinho; NÃO confundir controle com disposição)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-01-SUMMARY.md (tipo Disposition exato de src/llm/prompts.ts)
  </read_first>
  <behavior>
    - parseCommand('!auto') retorna 'autonomous' (D-14 alias); parseCommand('!livre') continua retornando 'autonomous' (não quebra a Fase 2).
    - parseCommand('!ajudante') retorna null (disposição NÃO é controle — não vaza para o mapa de controle).
    - parseDisposition('!ajudante') retorna 'ASSISTANT'; parseDisposition('!sozinho') retorna 'AUTONOMOUS'; parseDisposition('!pausar') retorna null (controle NÃO é disposição).
    - parseDisposition/parseCommand fazem match literal exato (trim+lowercase), ignorando entrada desconhecida (sem eval/interpolação — ASVS V5).
  </behavior>
  <action>
    Em `src/control/commands.ts`: adicionar `'!auto': 'autonomous'` ao mapa `COMMANDS` (mantendo `'!livre': 'autonomous'` — D-14). Nenhuma outra mudança no parseCommand.

    Criar `src/control/disposition.ts` (espelhando o padrão literal/imutável de commands.ts):
    - importar `Disposition` de '../llm/prompts'.
    - `export const DISPOSITION_COMMANDS: Readonly<Record<string, Disposition>> = Object.freeze({ '!ajudante': 'ASSISTANT', '!sozinho': 'AUTONOMOUS' })` (D-05; keywords escolhidas).
    - `export function parseDisposition(message: string): Disposition | null` — match literal exato (trim+lowercase), igual ao parseCommand. Comentar a segurança ASVS V5 (lookup literal, sem eval).

    Estender `src/control/commands.test.ts` (ou criar `src/control/disposition.test.ts`) cobrindo cada item do <behavior>, garantindo o isolamento controle↔disposição (nenhum termo de um vaza para o outro).
  </action>
  <verify>
    <automated>bun test src/control/ && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'!auto': 'autonomous'" src/control/commands.ts` e `grep -q "'!livre': 'autonomous'" src/control/commands.ts` (ambos presentes — D-14 não quebra Fase 2)
    - `grep -q "'!ajudante': 'ASSISTANT'" src/control/disposition.ts` e `grep -q "'!sozinho': 'AUTONOMOUS'" src/control/disposition.ts`
    - `grep -q "export function parseDisposition" src/control/disposition.ts`
    - Teste prova: parseCommand('!ajudante')===null e parseDisposition('!pausar')===null (isolamento)
    - `grep -rn "eval\|Function(" src/control/disposition.ts` retorna VAZIO (segurança literal)
    - `bun test src/control/` passa; `bunx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>!auto adicionado ao controle sem remover !livre; parseDisposition literal para !ajudante/!sozinho; controle e disposição totalmente isolados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Caminho conversacional isolado (CHAT-01/02) com pedido→objetivo restrito (D-13)</name>
  <read_first>
    - src/chat (criar dir); src/control/commands.ts (registerChatCommands — modelo de handler)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-01-SUMMARY.md (provider.chat, buildPersonaPrompt)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-03-SUMMARY.md (CognitiveStateHolder: disposition/goals/control)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md (Pattern 5 conversa separada; Pitfall 6 handler duplicado/vazamento; Open Question 3 pedido→objetivo restrito a gather/follow/navigate)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md (D-01 respostas curtas; D-02 idioma-espelho; D-07 AUTONOMOUS ignora; D-12 todo chat próximo + proatividade; D-13 pedido vira objetivo em ASSISTANT)
  </read_first>
  <behavior>
    - shouldRespond(disposition='AUTONOMOUS', proactivity='reactive', ...) retorna false (D-07: conversa mínima).
    - shouldRespond(disposition='ASSISTANT', ...) retorna true para mensagem de jogador próximo (D-12).
    - shouldRespond ignora a própria mensagem do bot (username === bot.username).
    - handleConversation chama provider.chat com [system(buildPersonaPrompt(disposition)), human(message)] e retorna a string de resposta (curta, persona reservada). Não lança se provider.chat lançar (degrada para silêncio/log).
    - Em ASSISTANT, se a mensagem casa um pedido de tipo SUPORTADO (gather/follow/navigate via detecção simples de palavra-chave/intenção), handleConversation sinaliza geração de objetivo (retorna/seta playerRequestPending + um goal candidato source:'player_request'); pedido de tipo NÃO suportado → resposta conversacional educada, NUNCA objetivo inválido (Open Question 3).
  </behavior>
  <action>
    Criar `src/chat/conversation.ts`:
    - `export function shouldRespond(disposition: Disposition, proactivity: 'reactive'|'proactive', username: string, botUsername: string): boolean` — false se username===botUsername; em AUTONOMOUS retorna false salvo proactivity==='proactive' com gatilho mínimo (cumprimentar quem chega — manter simples: AUTONOMOUS reactive => false, D-07); em ASSISTANT => true (D-12).
    - `export const SUPPORTED_REQUEST_KINDS = ['gather','follow','navigate'] as const` (Open Question 3 — conjunto FECHADO de objetivos extraíveis).
    - `export async function handleConversation(provider, holder, bot, username, message, now): Promise<void>`:
      - monta messages com buildPersonaPrompt(holder.disposition) + a mensagem do jogador (incluir o username no contexto humano para coerência).
      - chama `const reply = await provider.chat(messages)` dentro de try/catch; se ok e não vazio → `bot.chat(reply.slice(0, 256))` (resposta curta — D-01); se falhar → log e retorna (degrada gracioso).
      - se holder.disposition === 'ASSISTANT': inspecionar a mensagem por um pedido de tipo suportado (heurística literal simples por palavras-chave pt/en: "coletar/coleta/gather"→gather, "vem/segue/follow"→follow, "vai/leva/navigate"→navigate). Se casar: setar `holder.playerRequestPending = true` (o campo JÁ EXISTE no CognitiveStateHolder desde o Plan 03 Task 1, default false — aqui apenas SETAMOS o valor, não estendemos a estrutura) e empurrar um Goal candidato source:'player_request' em holder.goals (priority alta, progress 0). Se NÃO casar nenhum tipo suportado: NÃO criar objetivo (a resposta conversacional já cobre — "não consigo isso ainda" fica a cargo do LLM via persona). Em AUTONOMOUS: pedidos NUNCA viram objetivo (D-13).
    - NOTA: `playerRequestPending` JÁ É um campo do CognitiveStateHolder (declarado no Plan 03 Task 1, default false) — esta task NÃO estende o holder, apenas seta o booleano. O reset do sinal é feito pelo `observe` (Plan 03 Task 3) após o selectGoal consumir o pedido. selectGoal do Plan 02 já consome esse sinal via SelectGoalContext.
    - Importar SystemMessage/HumanMessage de '@langchain/core/messages'.

    Criar `src/chat/conversation.test.ts` com provider MOCK e bot MOCK (bot.chat spy): cobrir cada item do <behavior>, especialmente AUTONOMOUS=>silêncio, ASSISTANT=>responde, pedido suportado=>objetivo criado, pedido não suportado=>sem objetivo, provider.chat lança=>sem throw.
  </action>
  <verify>
    <automated>bun test src/chat/ && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "provider.chat(" src/chat/conversation.ts` (caminho conversacional usa provider.chat — CHAT-02)
    - `grep -q "buildPersonaPrompt(" src/chat/conversation.ts` (persona aplicada — CHAT-03)
    - `grep -q "SUPPORTED_REQUEST_KINDS" src/chat/conversation.ts` (conjunto fechado de pedidos — D-13/OQ3)
    - `grep -q "bot.chat(" src/chat/conversation.ts` (responde no jogo — CHAT-02)
    - Teste prova: AUTONOMOUS reactive → bot.chat NÃO chamado; ASSISTANT → bot.chat chamado; pedido não suportado em ASSISTANT → holder.goals NÃO ganha goal player_request
    - `bun test src/chat/` passa; `bunx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>Caminho conversacional responde via provider.chat+persona só quando permitido (AUTONOMOUS mínimo); pedido de jogador vira objetivo restrito a tipos suportados em ASSISTANT; nunca lança.</done>
</task>

<task type="auto">
  <name>Task 3: Wiring do handler de chat no loop — ordem controle → disposição → conversa (Pattern 5)</name>
  <read_first>
    - src/cognition/loop.ts (onde registerChatCommands é chamado por sessão — estender com disposição+conversa)
    - src/control/commands.ts e src/control/disposition.ts (parseCommand/parseDisposition — Task 1)
    - src/chat/conversation.ts (shouldRespond/handleConversation — Task 2)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md (Pattern 5 ordem estrita; Pitfall 6 handler 1x por sessão, não duplicar)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md (D-09 controle literal primeiro; D-05 disposição; D-12 conversa)
  </read_first>
  <action>
    Em `src/cognition/loop.ts`, dentro de `startCognitiveLoop(bot, holder)`: substituir a chamada atual `registerChatCommands(bot, holder.control)` por um ÚNICO handler de chat por sessão que implementa a ordem estrita do Pattern 5. Preferir manter `registerChatCommands` para o passo de controle e adicionar os passos 2-3 no mesmo handler OU registrar um handler unificado. Implementar a ordem EXATA:
    ```typescript
    bot.on('chat', (username: string, message: string) => {
      if (username === bot.username) return
      const mode = parseCommand(message)                        // 1) controle literal (D-09/D-14)
      if (mode) { holder.control.setMode(mode); /* push mem chat_command */ return }
      const disp = parseDisposition(message)                    // 2) disposição literal (D-05)
      if (disp) { holder.disposition = disp; return }
      // 3) conversa (CHAT) — só se permitido (AUTONOMOUS mínimo, D-07)
      if (shouldRespond(holder.disposition, config.proactivity, username, bot.username)) {
        void handleConversation(provider, holder, bot, username, message, Date.now())  // não bloqueia (single-flight do provider em conversa é separado da decisão)
      }
    })
    ```
    - Garantir registro 1x por sessão (handler morre com a sessão — Pitfall 6; NÃO registrar handler dentro do while). Se `registerChatCommands` já registra um handler, NÃO duplicar — usar UM só handler unificado (remover a chamada antiga registerChatCommands e inlinar, ou estender a assinatura de registerChatCommands para aceitar os callbacks de disposição/conversa). Escolher a opção que resulta em EXATAMENTE um `bot.on('chat')` registrado por sessão.
    - A conversa usa `void` (não-bloqueante) para não travar nada; a decisão de ação (deliberation do Plan 03) continua separada da conversa (D-13: pedido em ASSISTANT vira objetivo via handleConversation→holder, consumido pelo selectGoal no observe).
    - Importar parseDisposition de '../control/disposition' e shouldRespond/handleConversation de '../chat/conversation'.
  </action>
  <verify>
    <automated>bunx tsc --noEmit && bun test src/control/ src/chat/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bot.on('chat'" src/cognition/loop.ts` retorna 1 (EXATAMENTE um handler por sessão — Pitfall 6); se registerChatCommands ainda for usado, então `grep -c "bot.on('chat'" src/control/commands.ts` + uso no loop NÃO somam 2 handlers ativos por sessão (verificar manualmente que só um caminho registra)
    - A ordem no handler é controle → disposição → conversa: `grep -n "parseCommand\|parseDisposition\|shouldRespond" src/cognition/loop.ts` mostra parseCommand antes de parseDisposition antes de shouldRespond
    - `grep -q "void handleConversation" src/cognition/loop.ts` (conversa não bloqueia)
    - `grep -q "holder.disposition = disp" src/cognition/loop.ts` (disposição muda em runtime — D-05)
    - `bunx tsc --noEmit` sem erros; `bun test src/control/ src/chat/` verdes
  </acceptance_criteria>
  <done>Um único handler de chat por sessão aplica controle→disposição→conversa na ordem correta; conversa não bloqueia; disposição troca em runtime via chat literal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat de jogador (texto livre) → handler | Entrada não confiável que pode tentar prompt injection ou disparar comandos. |
| chat de jogador → provider.chat (conversa) | Texto do jogador vira prompt do LLM de conversa. |
| pedido de jogador → objetivo dinâmico | Texto do jogador influencia geração de objetivo (D-13). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering / Elevation | src/control/commands.ts + disposition.ts (parsers literais) | mitigate | parseCommand/parseDisposition são lookup literal exato em mapa imutável (Object.freeze), sem eval/interpolação (ASVS V5). Parseados ANTES da conversa — injection no texto não muda modo/disposição a menos que seja a keyword exata. |
| T-03-13 | Tampering / Elevation | src/chat/conversation.ts (pedido→objetivo) | mitigate | Objetivo extraível restrito a SUPPORTED_REQUEST_KINDS (conjunto FECHADO gather/follow/navigate); pedido fora do conjunto → resposta conversacional, nunca objetivo inválido (OQ3/D-13). LLM de conversa não executa código nem toca o bot. |
| T-03-14 | Elevation | src/chat/conversation.ts (prompt injection "ignore suas regras") | mitigate | A conversa é texto→texto (bot.chat); não dirige ação diretamente. A AÇÃO só vem da deliberação com enum fechado + Zod + fallback (Plan 01/03). Em AUTONOMOUS a conversa é off (D-07), reduzindo a superfície. |
| T-03-15 | DoS | src/cognition/loop.ts (handler de chat) | mitigate | UM handler por sessão (Pitfall 6, morre com a sessão); conversa é void/não-bloqueante; provider.chat tem o provider single behind o LlmProvider. Spam de chat não trava o loop reativo. |
</threat_model>

<verification>
- `bun test src/control/ src/chat/` — verdes.
- `bunx tsc --noEmit` — sem erros.
- grep prova: ordem controle→disposição→conversa; um handler por sessão; pedido restrito a conjunto fechado; !auto e !livre coexistem.
</verification>

<success_criteria>
- CHAT-01: handler lê todo chat de jogador próximo (D-12).
- CHAT-02: handleConversation responde via provider.chat+persona, curto e em ASSISTANT; AUTONOMOUS mínimo (D-07).
- GOAL-01 (fonte player_request): pedido suportado em ASSISTANT vira objetivo dinâmico restrito (D-13).
- D-14/D-05: !auto no controle (sem quebrar !livre); !ajudante/!sozinho mudam disposição em runtime; controle e disposição isolados.
</success_criteria>

<output>
Após completar, criar `.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-04-SUMMARY.md`.
</output>
