---
phase: 03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/llm/provider.ts
  - src/llm/schemas.ts
  - src/llm/structured.ts
  - src/llm/prompts.ts
  - src/llm/structured.test.ts
  - src/llm/schemas.test.ts
  - src/memory/shortTerm.ts
  - .env.example
autonomous: true
requirements: [LLM-01, LLM-02, LLM-03, CHAT-03]
user_setup:
  - service: lm-studio
    why: "LLM local OpenAI-compatível para raciocínio/conversa (LLM-01). Sem ele o agente degrada para o arbiter da Fase 2 (D-17) — o loop continua rodando."
    env_vars:
      - name: LLM_BASE_URL
        source: "LM Studio — Developer tab → Server → URL (padrão http://localhost:1234/v1)"
      - name: LLM_MODEL
        source: "LM Studio — nome do modelo carregado (ex.: qwen2.5-7b-instruct). Recomendado >= 7B para structured output."
    dashboard_config:
      - task: "Habilitar o servidor local e carregar um modelo de chat >= 7B com suporte a structured output (json_schema)"
        location: "LM Studio → Developer tab → Start Server + Load Model"

must_haves:
  truths:
    - "Uma chamada de decisão ao LLM retorna um objeto validado contra um enum de ações FECHADO + Zod, ou cai para o fallback determinístico quando o LLM falha/está indisponível."
    - "Uma chamada de conversa ao LLM retorna texto livre usando o prompt de persona estática (sobrevivente pragmático)."
    - "O cliente LLM é acessível somente por trás de uma interface de provedor (LlmProvider), sem ChatOpenAI vazando para a cognição."
    - "O tokenizer real (js-tiktoken) substitui a heurística 4-chars/token na memória de curto prazo, mantendo a assinatura."
  artifacts:
    - path: "src/llm/provider.ts"
      provides: "Interface LlmProvider + createLmStudioProvider (ChatOpenAI baseURL, apiKey dummy)"
      exports: ["LlmProvider", "createLmStudioProvider"]
    - path: "src/llm/schemas.ts"
      provides: "Schema Zod da decisão com action enum FECHADO + params"
      exports: ["ActionDecision", "ActionDecisionSchema"]
    - path: "src/llm/structured.ts"
      provides: "decideAction com repair/retry + fallback ao arbiter (D-17)"
      exports: ["decideAction"]
    - path: "src/llm/prompts.ts"
      provides: "Persona estática + serialização de contexto + prompts por disposição"
      exports: ["buildPersonaPrompt", "serializeContext"]
    - path: "src/memory/shortTerm.ts"
      provides: "estimateTokens via js-tiktoken o200k_base"
      contains: "js-tiktoken"
  key_links:
    - from: "src/llm/structured.ts"
      to: "src/llm/provider.ts"
      via: "provider.decide(schema, messages) + provider.available()"
      pattern: "provider\\.(decide|available)"
    - from: "src/llm/structured.ts"
      to: "fallback (arbiter)"
      via: "callback de fallback quando LLM inválido/indisponível"
      pattern: "fallback\\(\\)"
    - from: "src/memory/shortTerm.ts"
      to: "js-tiktoken"
      via: "getEncoding('o200k_base').encode"
      pattern: "getEncoding\\('o200k_base'\\)"
---

<objective>
Construir a fundação de LLM da Fase 3: um cliente abstraído por provedor (LLM-03), saída estruturada restrita com repair/fallback (LLM-02), o caminho de raciocínio/decisão e o caminho de conversa (LLM-01), e o prompt de persona estática "sobrevivente pragmático" (CHAT-03). Trocar a heurística de tokens da memória de curto prazo pelo tokenizer real js-tiktoken.

Este plano NÃO toca o grafo nem o loop — entrega módulos puros/isolados consumidos pelos planos de integração (03 e 04). É a camada mais arriscada da fase: domar um modelo local fraco com enum fechado + Zod + repair + fallback determinístico (D-17/D-18).

Purpose: Isolar toda a superfície LLM atrás de uma interface, com a rede de fallback embutida, antes de plugar na cognição.
Output: `src/llm/*` (provider, schemas, structured, prompts) + tokenizer real em `src/memory/shortTerm.ts` + deps instaladas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md
@.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md

# Código existente que define contratos a respeitar
@src/skills/index.ts
@src/memory/shortTerm.ts
@src/cognition/types.ts
@src/perception/types.ts

<interfaces>
<!-- Contratos extraídos do codebase. O executor deve usar estes diretamente. -->

Já instalado (verificado): @langchain/openai NÃO está instalado; @langchain/core@1.2.0 e @langchain/langgraph@1.4.4 SIM; zod@4.4.3 (Zod v4, schemas usam .toJSONSchema()).

De src/memory/shortTerm.ts (assinatura a PRESERVAR — troca só interna):
```typescript
export function estimateTokens(e: MemEvent): number   // hoje: Math.ceil(JSON.stringify(e).length / 4)
export function createMemory(budget?: number): ShortTermMemory
export function push(mem: ShortTermMemory, e: MemEvent): ShortTermMemory
export function totalTokens(mem: ShortTermMemory): number
```

De src/cognition/types.ts:
```typescript
export type CognitiveState = 'idle'|'exploring'|'gathering'|'socializing'|'fighting'|'building'
export type MemEvent = /* discriminated union por type: state_transition|action|world|chat_command */
```

De src/perception/types.ts (entrada do serializeContext):
```typescript
export interface WorldSnapshot {
  capturedAt: number
  status: { health: number; food: number; position: {x,y,z}; timeOfDay: number; isDay: boolean }
  entities: ReadonlyArray<EntityInfo>
  players: ReadonlyArray<PlayerInfo>     // { username, distance: number|null, position: {x,y,z}|null }
  nearbyBlockTypes: Record<string, { count: number; examples: Position3D[] }>
  inventory: ReadonlyArray<InventorySlot>  // { slot, name, count, ... }
}
```

Padrão canônico LM Studio (RESEARCH.md Code Examples):
```typescript
import { ChatOpenAI } from '@langchain/openai'
const llm = new ChatOpenAI({
  model: process.env.LLM_MODEL || 'local-model',
  apiKey: 'lm-studio',                                   // dummy; LM Studio ignora
  configuration: { baseURL: process.env.LLM_BASE_URL || 'http://localhost:1234/v1' },
  temperature: Number(process.env.LLM_TEMPERATURE ?? 0.4),
})
const structured = llm.withStructuredOutput(schema, { name: 'decide', method: 'jsonSchema' })
```
LM Studio response_format esperado (CITED docs): `{ type:'json_schema', json_schema:{ name, strict, schema } }` — withStructuredOutput(method:'jsonSchema') gera isso. Modelos < 7B não confiáveis → fallback é obrigatório.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar deps e trocar o tokenizer da memória por js-tiktoken (MEM-01 real)</name>
  <read_first>
    - src/memory/shortTerm.ts (assinatura atual de estimateTokens/push/totalTokens a preservar)
    - src/cognition/types.ts (tipo MemEvent que estimateTokens recebe)
    - package.json (deps atuais; confirmar que @langchain/openai e js-tiktoken NÃO estão)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md (seção "Tokenizer real" + Standard Stack)
  </read_first>
  <action>
    Rodar exatamente: `bun add @langchain/openai@1.5.1 js-tiktoken@1.0.21`. Confirmar que entram em `dependencies` do package.json.

    Em `src/memory/shortTerm.ts`, substituir a heurística interna de `estimateTokens` por js-tiktoken, MANTENDO a assinatura `estimateTokens(e: MemEvent): number` (sem breaking change no contrato da memória). Carregar o encoding UMA vez no escopo do módulo (não por chamada):
    ```typescript
    import { getEncoding } from 'js-tiktoken'
    const enc = getEncoding('o200k_base')   // aproxima Llama/local; erro ±3–12%, suficiente p/ orçamento FIFO
    export function estimateTokens(e: MemEvent): number {
      return enc.encode(JSON.stringify(e)).length
    }
    ```
    Atualizar o comentário do topo do arquivo que dizia "Esqueleto pronto para a Fase 3 trocar estimateTokens" para refletir que o tokenizer real (js-tiktoken o200k_base) já está em uso. NÃO mudar `createMemory`, `push`, `totalTokens`, `getEvents` — só a implementação interna de `estimateTokens`.
  </action>
  <verify>
    <automated>bun add @langchain/openai@1.5.1 js-tiktoken@1.0.21 && bun test src/memory/shortTerm.test.ts && grep -q "getEncoding('o200k_base')" src/memory/shortTerm.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"@langchain/openai"' package.json` retorna sucesso (dep instalada)
    - `grep -q '"js-tiktoken"' package.json` retorna sucesso
    - `grep -q "getEncoding('o200k_base')" src/memory/shortTerm.ts` retorna sucesso
    - `grep -q "JSON.stringify(e).length / 4" src/memory/shortTerm.ts` retorna FALHA (heurística removida)
    - `bun test src/memory/shortTerm.test.ts` passa (assinatura preservada; testes existentes continuam verdes)
    - `bunx tsc --noEmit` sem erros novos em src/memory/shortTerm.ts
  </acceptance_criteria>
  <done>Deps instaladas; estimateTokens usa js-tiktoken o200k_base com encoding carregado 1x; testes de memória existentes passam sem alteração.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provider abstraído + schema de decisão (enum FECHADO) + persona prompt</name>
  <read_first>
    - src/skills/index.ts (toolRegistry/SkillTool — schemas Zod reaproveitados; nomes de skill: navigate, dig, follow, attack)
    - src/perception/types.ts (WorldSnapshot que serializeContext recebe)
    - src/cognition/types.ts (CognitiveState; MemEvent que vira contexto)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md (Pattern 1 Provider Interface, Pattern 2 Structured Output, Pattern 5 conversa separada, Code Examples)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md (D-01/D-02/D-03 persona; D-18 provider; D-04/D-06/D-07 disposição modula prompt)
  </read_first>
  <behavior>
    - ActionDecisionSchema rejeita action fora do enum fechado (ex.: 'fly' → ZodError); aceita 'gather'/'explore'/'navigate'/'idle'/'chat'.
    - ActionDecisionSchema aceita target opcional (string max 64) e exige reason (string max 200).
    - buildPersonaPrompt('AUTONOMOUS') e buildPersonaPrompt('ASSISTANT') retornam strings diferentes (proatividade/aceitação de tarefas variam — D-06); ambas contêm a instrução de espelhar o idioma do interlocutor (D-02) e o arquétipo "sobrevivente pragmático" (D-01).
    - serializeContext(snapshot, needs, goals, memEvents) retorna string compacta contendo health/food, blocos próximos relevantes e os eventos recentes — sem lançar com arrays vazios.
  </behavior>
  <action>
    Criar `src/llm/schemas.ts`:
    - Definir `ActionDecisionSchema = z.object({ action: z.enum(['gather','explore','navigate','idle','chat']), target: z.string().max(64).optional(), reason: z.string().max(200) })` — enum FECHADO (LLM-02; nunca string livre). Exportar `export type ActionDecision = z.infer<typeof ActionDecisionSchema>`.
    - Documentar no comentário que o enum mapeia para os estados cognitivos da Fase 2 e que params físicos de skill são validados depois pelos schemas Zod do `toolRegistry` (D-10 Fase 1: o LLM só escolhe ação+alvo, nunca monta a chamada física).

    Criar `src/llm/prompts.ts`:
    - `export type Disposition = 'AUTONOMOUS' | 'ASSISTANT'` (eixo de disposição D-04).
    - `export function buildPersonaPrompt(disposition: Disposition): string` — system prompt ESTÁTICO (D-01) com: arquétipo "sobrevivente pragmático" (focado em tarefas, reservado, fala pouco e direto); instrução de DETECTAR e ESPELHAR o idioma do interlocutor (D-02, na prática pt-BR); auto-percepção honesta como agente sem ênfase (D-03 default). Em ASSISTANT: proativo, aceita pedidos de jogadores como objetivos. Em AUTONOMOUS: praticamente ignora jogadores, foco em sobreviver sozinho (D-07). NÃO usar evolução de personalidade (isso é Fase 4).
    - `export function serializeContext(snapshot: WorldSnapshot | null, needs: unknown, goals: unknown, recentEvents: ReadonlyArray<MemEvent>): string` — monta uma string COMPACTA (orçamento de prompt limitado) com: status (health/food/timeOfDay), até ~8 tipos de bloco próximos com count, jogadores próximos (username+distance), o objetivo atual (se houver) e os últimos ~10 eventos de memória. Tolerante a snapshot null e arrays vazios. Tipar `needs`/`goals` como `unknown` por ora (Plan 02/03 fornecem os tipos reais via integração) — serializar defensivamente com JSON.stringify truncado.

    Criar `src/llm/provider.ts`:
    - `export interface LlmProvider { decide<T>(schema: ZodType<T>, messages: BaseMessage[]): Promise<T>; chat(messages: BaseMessage[]): Promise<string>; available(): Promise<boolean> }` (D-18; `decide` = caminho de ação estruturada LLM-01/02; `chat` = caminho conversacional CHAT-02; `available` = probe p/ degradar D-17).
    - `export function createLmStudioProvider(): LlmProvider` usando `new ChatOpenAI({ model: process.env.LLM_MODEL || 'local-model', apiKey: 'lm-studio', configuration: { baseURL: process.env.LLM_BASE_URL || 'http://localhost:1234/v1' }, temperature: Number(process.env.LLM_TEMPERATURE ?? 0.4) })`.
      - `decide`: `model.withStructuredOutput(schema, { name: 'decide', method: 'jsonSchema' }).invoke(messages)`.
      - `chat`: `model.invoke(messages)` e retornar `String(res.content)`.
      - `available`: tentar um probe leve (ex.: GET no baseURL `/models` via fetch com timeout curto ~1500ms) e retornar boolean; qualquer erro/timeout → false (sem lançar). NÃO usar o ChatOpenAI para o probe (evita custo de inferência).
    - Importar `ChatOpenAI` de `@langchain/openai`, `BaseMessage` de `@langchain/core/messages`, `ZodType` de `zod`. NÃO exportar ou expor `ChatOpenAI` fora deste módulo (encapsulamento LLM-03).
  </action>
  <verify>
    <automated>bun test src/llm/schemas.test.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/llm/schemas.test.ts` existe e testa: action inválida ('fly') rejeitada por ActionDecisionSchema.parse (throws); 'gather' aceito; reason obrigatório; ambos os prompts de disposição diferem entre si e contêm a instrução de idioma.
    - `grep -q "z.enum(\['gather','explore','navigate','idle','chat'\])" src/llm/schemas.ts` OU equivalente com aspas duplas (enum fechado presente)
    - `grep -q "method: 'jsonSchema'" src/llm/provider.ts` retorna sucesso
    - `grep -q "baseURL" src/llm/provider.ts` e `grep -q "apiKey: 'lm-studio'" src/llm/provider.ts` retornam sucesso
    - `grep -rn "ChatOpenAI" src/ | grep -v 'src/llm/provider.ts'` retorna VAZIO (ChatOpenAI só vive no provider — LLM-03)
    - `grep -q "available" src/llm/provider.ts` (probe de disponibilidade presente — D-17)
    - `bunx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>Provider isola ChatOpenAI atrás de LlmProvider; decisão usa enum fechado + withStructuredOutput(jsonSchema); persona estática por disposição com espelho de idioma; serializeContext compacto e tolerante.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: decideAction com repair/retry + fallback determinístico (LLM-02 / D-17)</name>
  <read_first>
    - src/llm/provider.ts (interface LlmProvider — criada na Task 2)
    - src/llm/schemas.ts (ActionDecisionSchema — criada na Task 2)
    - src/cognition/arbiter.ts (forma do fallback determinístico que será injetado — assinatura arbitrate(snapshot, mode, excluded))
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-RESEARCH.md (Pattern 2 Structured Output com repair + fallback; Pitfall 1 LM Studio cai pra content)
    - .planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-CONTEXT.md (D-17 fallback gracioso)
  </read_first>
  <behavior>
    - Quando provider.available() é false → retorna o resultado do callback fallback() SEM chamar provider.decide.
    - Quando provider.decide retorna objeto válido → retorna o objeto parseado por ActionDecisionSchema.
    - Quando provider.decide lança na 1ª tentativa mas o retry (com repairHint) retorna válido → retorna o reparado.
    - Quando ambas as tentativas falham/inválidas → retorna fallback().
    - decideAction NUNCA lança (sempre retorna uma decisão ou o fallback) — garante que o loop nunca trava (D-17).
  </behavior>
  <action>
    Criar `src/llm/structured.ts` com:
    ```typescript
    import { ActionDecisionSchema, type ActionDecision } from './schemas'
    import type { LlmProvider } from './provider'
    import type { BaseMessage } from '@langchain/core/messages'

    export async function decideAction(
      provider: LlmProvider,
      messages: BaseMessage[],
      fallback: () => ActionDecision,
    ): Promise<ActionDecision> {
      if (!(await provider.available())) return fallback()           // D-17: LLM off -> fallback
      try {
        const raw = await provider.decide(ActionDecisionSchema, messages)
        return ActionDecisionSchema.parse(raw)
      } catch (e1) {
        try {
          const hint = repairHint(e1)                               // re-prompt curto com o erro
          const repaired = await provider.decide(ActionDecisionSchema, [...messages, hint])
          return ActionDecisionSchema.parse(repaired)
        } catch {
          return fallback()                                         // D-17: irreparável -> fallback
        }
      }
    }
    ```
    Implementar `repairHint(err: unknown): BaseMessage` retornando um HumanMessage curto instruindo o modelo a responder SOMENTE com JSON que satisfaça o schema (action no enum fechado), citando a mensagem de erro. Importar HumanMessage de '@langchain/core/messages'. Garantir que NENHUM caminho lança para fora de decideAction (try/catch cobre tudo). NÃO chamar o bot nem o arbiter diretamente aqui — o fallback é injetado por parâmetro (mantém structured.ts puro/testável; o Plan 03 passa o arbiter como fallback).
  </action>
  <verify>
    <automated>bun test src/llm/structured.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/llm/structured.test.ts` existe e cobre os 5 comportamentos do bloco <behavior> usando um LlmProvider MOCK (sem rede): available=false→fallback; decide válido→parseado; decide throws então retry válido→reparado; ambos falham→fallback; decideAction nunca rejeita.
    - `grep -q "if (!(await provider.available()))" src/llm/structured.ts` (probe antes de chamar — D-17)
    - `grep -c "fallback()" src/llm/structured.ts` >= 2 (fallback em LLM-off E em irreparável)
    - `grep -q "ActionDecisionSchema.parse" src/llm/structured.ts` (validação Zod presente)
    - `bun test src/llm/structured.test.ts` passa com todos os casos
    - `bunx tsc --noEmit` sem erros
  </acceptance_criteria>
  <done>decideAction valida com Zod, repara via re-prompt, degrada ao fallback injetado quando LLM indisponível ou irreparável, e nunca lança.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat do jogador → contexto do LLM | Texto não confiável do jogador entra no prompt (serializeContext / conversa). Risco de prompt injection. |
| saída do LLM → decisão de ação | Saída de um modelo local fraco/não confiável dirige a escolha de ação. |
| `.env` → provider | Endpoint/segredo do LM Studio (apiKey dummy) lido do ambiente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | src/llm/structured.ts (decisão de ação) | mitigate | Enum de ação FECHADO em ActionDecisionSchema (`z.enum`) + `ActionDecisionSchema.parse` rejeita qualquer ação inventada; saída inválida → `fallback()` determinístico (D-17). O LLM nunca escolhe ação fora do conjunto. |
| T-03-02 | Tampering / Elevation | src/llm/prompts.ts (serializeContext de chat de jogador) | mitigate | O chat do jogador entra só como CONTEXTO de texto; o LLM só pode emitir action ∈ enum fechado validado por Zod. Nenhum code-gen/eval (Out of Scope no REQUIREMENTS.md). Prompt de persona reforça o escopo de ações. |
| T-03-03 | Denial of Service (auto-infligido) | src/llm/provider.ts (available probe) | mitigate | `available()` usa fetch com timeout curto (~1500ms) e nunca lança; LLM indisponível → fallback imediato, o loop não trava. (Single-flight/replan-budget reforçados no Plan 03.) |
| T-03-04 | Information Disclosure | src/llm/provider.ts (apiKey dummy / baseURL) | accept | apiKey é literal dummy 'lm-studio' (LM Studio ignora); baseURL é localhost por padrão. Servidor local offline, superfície externa baixa — sem segredo real a vazar nesta fase. |
</threat_model>

<verification>
- `bun test src/llm/ src/memory/shortTerm.test.ts` — todos verdes.
- `bunx tsc --noEmit` — sem erros de tipo em src/llm/* e src/memory/shortTerm.ts.
- ChatOpenAI confinado a src/llm/provider.ts (grep prova LLM-03).
- Enum de ação fechado presente em src/llm/schemas.ts (grep prova LLM-02).
</verification>

<success_criteria>
- LLM-01: provider.decide e provider.chat existem e apontam para LM Studio via baseURL.
- LLM-02: ActionDecisionSchema com enum fechado + decideAction com repair + fallback que nunca lança.
- LLM-03: LlmProvider esconde ChatOpenAI (confinado ao provider).
- CHAT-03: buildPersonaPrompt entrega persona estática "sobrevivente pragmático" com espelho de idioma, variando por disposição.
- MEM-01 (real): estimateTokens usa js-tiktoken o200k_base sem mudar o contrato da memória.
</success_criteria>

<output>
Após completar, criar `.planning/phases/03-cogni-o-com-llm-loop-completo-necessidades-e-objetivos/03-01-SUMMARY.md`.
</output>
