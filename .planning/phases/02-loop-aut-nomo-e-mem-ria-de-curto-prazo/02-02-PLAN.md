---
phase: 02-loop-aut-nomo-e-mem-ria-de-curto-prazo
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/config.ts
  - src/cognition/states.ts
  - src/cognition/arbiter.ts
  - src/cognition/arbiter.test.ts
  - src/cognition/safety.ts
  - src/cognition/safety.test.ts
autonomous: true
requirements: [COG-02, COG-04]
must_haves:
  truths:
    - "O estado cognitivo é escolhido por prioridade fixa: Socializing > Gathering > Exploring > Idle"
    - "Modo Pausado força Idle; modo Standby força Socializing (vem para perto, aguarda)"
    - "Gathering escolhe o bloco-alvo de MAIOR prioridade presente em nearbyBlockTypes (escada de sobrevivência configurável)"
    - "A mesma acao/alvo repetida N vezes sem progresso e abandonada (rede anti-repeticao)"
    - "Apos M falhas consecutivas de skill o agente cai para Idle (backoff)"
  artifacts:
    - path: "src/cognition/arbiter.ts"
      provides: "Arbitragem por prioridade fixa (D-05) + escada de Gathering (D-07)"
      contains: "function arbitrate"
    - path: "src/cognition/safety.ts"
      provides: "Anti-repeticao (D-10) + backoff de falha de skill (D-11)"
      contains: "recordFailure"
    - path: "src/config.ts"
      provides: "Config do loop: gatheringLadder, minTickMs, N, M, tokenBudget"
      contains: "gatheringLadder"
  key_links:
    - from: "src/cognition/arbiter.ts"
      to: "src/config.ts gatheringLadder"
      via: "highestPriorityGatherTarget percorre a escada e cruza com nearbyBlockTypes"
      pattern: "gatheringLadder"
    - from: "src/cognition/safety.ts"
      to: "SkillTimeoutError / SkillStuckError"
      via: "backoff incrementa contador de falha e marca alvo em cooldown"
      pattern: "consecutiveFailures"
---

<objective>
Implementar a logica de decisao sem-LLM do loop: a arbitragem de estado por prioridade fixa (D-05), a escada de Gathering orientada a sobrevivencia e configuravel (D-07), e a rede de seguranca anti-repeticao + backoff de falha de skill (D-10/D-11). Tambem centraliza os parametros do loop em src/config.ts (escada, intervalo de tick, N, M, orcamento de tokens).

Estas sao funcoes puras de regra fixa sobre o WorldSnapshot e contadores — NAO tocam o bot, NAO chamam skill, NAO usam LLM. O Plano 03 as fia dentro do StateGraph e do driver.

Purpose: Entregar COG-02 (estados por regra fixa) e COG-04 (deteccao de repeticao/sem-progresso) como nucleo testavel antes de montar o grafo.
Output: src/cognition/states.ts, src/cognition/arbiter.ts, src/cognition/safety.ts e config estendida.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-CONTEXT.md
@.planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-RESEARCH.md
@.planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-01-SUMMARY.md

<interfaces>
<!-- Contratos consumidos. Plano 01 ja criou cognition/types.ts. -->

De src/cognition/types.ts (Plano 01):
```typescript
export type CognitiveState = 'idle' | 'exploring' | 'gathering' | 'socializing' | 'fighting' | 'building'
export type ControlMode = 'autonomous' | 'paused' | 'standby'
```

De src/perception/types.ts (Fase 1 — entrada da arbitragem):
```typescript
export interface WorldSnapshot {
  readonly status: { readonly health: number; readonly food: number; readonly position: {x:number;y:number;z:number}; readonly timeOfDay: number; readonly isDay: boolean }
  readonly players: ReadonlyArray<{ username: string; position: {x:number;y:number;z:number} | null; distance: number | null }>
  readonly nearbyBlockTypes: Readonly<Record<string, { count: number; examples: ReadonlyArray<{x:number;y:number;z:number}> }>>
  readonly inventory: ReadonlyArray<{ slot: number; name: string; type: number; count: number }>
}
```

De src/skills/executor.ts (Fase 1 — erros que o backoff consome):
```typescript
export class SkillTimeoutError extends Error {}   // name === 'SkillTimeoutError'
export class SkillStuckError extends Error {}     // name === 'SkillStuckError'
```

De src/config.ts (Fase 1 — estender, NAO substituir; manter host/port/perceptionRadius/navigateTimeoutMs/digTimeoutMs/reconnectDelayMs e as validacoes existentes):
```typescript
export const config = { host, port, username, mcVersion, perceptionRadius, navigateTimeoutMs, digTimeoutMs, reconnectDelayMs } as const
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender config.ts com parametros do loop + states.ts</name>
  <read_first>
    - src/config.ts (config existente da Fase 1 — estender sem quebrar campos/validacoes)
    - src/cognition/types.ts (CognitiveState)
    - .planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-CONTEXT.md (D-07 escada de sobrevivencia; D-02 intervalo de tick; Claude's discretion para N/M/budget)
  </read_first>
  <files>src/config.ts, src/cognition/states.ts</files>
  <action>
Estender o objeto `config` em src/config.ts ADICIONANDO os campos abaixo ao objeto existente (NAO remover nada, manter as validacoes). Valores escolhidos (Claude's discretion):

```typescript
  // === Fase 2: Loop cognitivo ===
  // D-02: intervalo minimo entre ticks do driver externo
  minTickMs: parseInt(process.env.MIN_TICK_MS || '500', 10),
  // D-07: escada de prioridade de sobrevivencia (mais prioritario primeiro).
  // O agente coleta o bloco de MAIOR prioridade presente em nearbyBlockTypes.
  gatheringLadder: [
    'oak_log', 'birch_log', 'spruce_log', 'jungle_log', 'acacia_log', 'dark_oak_log',  // madeira (ferramentas)
    'cobblestone', 'stone',                                                              // pedra
    'coal_ore', 'iron_ore', 'copper_ore',                                               // minerios basicos
    'diamond_ore', 'gold_ore',                                                          // minerios valiosos
  ] as ReadonlyArray<string>,
  // D-10: repeticoes da mesma acao/alvo sem progresso antes de abandonar
  antiRepeatN: parseInt(process.env.ANTI_REPEAT_N || '3', 10),
  // D-11: falhas consecutivas de skill antes de cair para Idle
  backoffM: parseInt(process.env.BACKOFF_M || '3', 10),
  // D-11: cooldown curto (ms) de um alvo marcado como falho
  targetCooldownMs: parseInt(process.env.TARGET_COOLDOWN_MS || '15000', 10),
  // D-05: raio (blocos) para considerar um jogador "proximo" (gatilho de Socializing)
  socialRadius: parseInt(process.env.SOCIAL_RADIUS || '8', 10),
  // D-13: orcamento de tokens da memoria de curto prazo (override do default do modulo)
  memoryTokenBudget: parseInt(process.env.MEMORY_TOKEN_BUDGET || '2000', 10),
```

Adicionar validacao de sanidade: `if (config.minTickMs < 0) throw ...` e `if (config.antiRepeatN < 1 || config.backoffM < 1) throw ...`.

Criar src/cognition/states.ts re-exportando o tipo e helpers de ordem de prioridade:

```typescript
// src/cognition/states.ts
// COG-02 / D-05 / D-06: estados e ordem de prioridade fixa.
import type { CognitiveState } from './types'
export type { CognitiveState } from './types'

/** Ordem de prioridade fixa de arbitragem (D-05), do mais ao menos prioritario. */
export const PRIORITY_ORDER: ReadonlyArray<CognitiveState> = ['socializing', 'gathering', 'exploring', 'idle']

/** Estados stub (D-06): entram no enum mas nao tem logica real. */
export const STUB_STATES: ReadonlyArray<CognitiveState> = ['fighting', 'building']

export function isStub(s: CognitiveState): boolean {
  return STUB_STATES.includes(s)
}
```
  </action>
  <verify>
    <automated>grep -q 'gatheringLadder' src/config.ts && grep -q 'minTickMs' src/config.ts && grep -q 'antiRepeatN' src/config.ts && grep -q 'backoffM' src/config.ts && grep -q "PRIORITY_ORDER" src/cognition/states.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/config.ts contem `gatheringLadder`, `minTickMs`, `antiRepeatN`, `backoffM`, `socialRadius`, `memoryTokenBudget`
    - Campos da Fase 1 (host, port, perceptionRadius, navigateTimeoutMs, digTimeoutMs, reconnectDelayMs) AINDA presentes
    - src/cognition/states.ts contem `PRIORITY_ORDER` e `STUB_STATES`
    - bun run typecheck sai 0
  </acceptance_criteria>
  <done>Config do loop centralizada; ordem de prioridade e stubs declarados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Arbitragem por prioridade fixa + escada de Gathering (D-05 / D-07)</name>
  <read_first>
    - src/config.ts (gatheringLadder, socialRadius — criados na Task 1)
    - src/cognition/types.ts (CognitiveState, ControlMode)
    - src/perception/types.ts (WorldSnapshot: players[].distance, nearbyBlockTypes)
    - .planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-RESEARCH.md (Pattern 2: funcao arbitrate de referencia)
  </read_first>
  <files>src/cognition/arbiter.ts, src/cognition/arbiter.test.ts</files>
  <behavior>
    - mode 'paused' -> retorna 'idle' (modo de controle vence)
    - mode 'standby' -> retorna 'socializing' (vem para perto, aguarda)
    - jogador com distance <= socialRadius -> 'socializing'
    - sem jogador proximo + nearbyBlockTypes contem item da escada -> 'gathering'
    - highestPriorityGatherTarget retorna o bloco de MAIOR prioridade presente (oak_log vence stone se ambos presentes)
    - highestPriorityGatherTarget retorna null se nenhum bloco da escada esta presente
    - sem jogador e sem alvo da escada -> 'exploring'
    - alvo em cooldown (passado em excludeTargets) e ignorado pela escada
  </behavior>
  <action>
Criar src/cognition/arbiter.ts com funcoes PURAS (sem efeito colateral, sem bot):

```typescript
// src/cognition/arbiter.ts
// D-05: arbitragem por prioridade fixa. D-07: escada de Gathering de sobrevivencia.
import type { WorldSnapshot } from '../perception/types'
import type { CognitiveState, ControlMode } from './types'
import { config } from '../config'

/**
 * Retorna o tipo de bloco de MAIOR prioridade da escada (config.gatheringLadder)
 * que esteja presente em nearbyBlockTypes e NAO esteja em excludeTargets (cooldown).
 * null se nenhum alvo elegivel.
 */
export function highestPriorityGatherTarget(
  s: WorldSnapshot,
  excludeTargets: ReadonlySet<string> = new Set(),
): string | null {
  for (const blockType of config.gatheringLadder) {       // escada ordenada por prioridade
    if (excludeTargets.has(blockType)) continue
    const entry = s.nearbyBlockTypes[blockType]
    if (entry && entry.count > 0) return blockType
  }
  return null
}

/** True se ha um jogador dentro de config.socialRadius. */
export function hasNearbyPlayer(s: WorldSnapshot): boolean {
  return s.players.some((p) => (p.distance ?? Infinity) <= config.socialRadius)
}

/**
 * Arbitragem por prioridade fixa (D-05): Socializing > Gathering > Exploring > Idle.
 * O modo de controle (D-08) sobrepoe a prioridade autonoma.
 */
export function arbitrate(
  s: WorldSnapshot,
  mode: ControlMode,
  excludeTargets: ReadonlySet<string> = new Set(),
): CognitiveState {
  if (mode === 'paused') return 'idle'            // freio: nao age
  if (mode === 'standby') return 'socializing'    // vem para perto e aguarda
  if (hasNearbyPlayer(s)) return 'socializing'
  if (highestPriorityGatherTarget(s, excludeTargets) !== null) return 'gathering'
  return 'exploring'                              // fallback antes de idle
}
```

Criar src/cognition/arbiter.test.ts cobrindo o bloco <behavior>. Construa WorldSnapshots minimos (objetos literais que satisfazem a interface — pode usar `as WorldSnapshot` com os campos necessarios). Use `import { test, expect } from 'bun:test'`.
  </action>
  <verify>
    <automated>cd /root/MineMind && bun test src/cognition/arbiter.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/cognition/arbiter.ts contem `function arbitrate`, `highestPriorityGatherTarget`, `hasNearbyPlayer`
    - Teste comprova: paused->idle, standby->socializing, jogador proximo->socializing, escada presente->gathering, vazio->exploring
    - Teste comprova prioridade da escada: oak_log vence stone quando ambos presentes
    - bun test src/cognition/arbiter.test.ts passa
    - bun run typecheck sai 0
  </acceptance_criteria>
  <done>Arbitragem e escada de Gathering implementadas como funcoes puras testadas; COG-02 coberto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rede anti-repeticao + backoff de falha de skill (D-10 / D-11 / COG-04)</name>
  <read_first>
    - src/skills/executor.ts (SkillTimeoutError, SkillStuckError — name das classes que o backoff inspeciona)
    - src/config.ts (antiRepeatN, backoffM, targetCooldownMs — criados na Task 1)
    - src/cognition/types.ts (CognitiveState)
    - .planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-RESEARCH.md (Pitfall 4: contador de repeticao; Open Question 2: deteccao de progresso)
  </read_first>
  <files>src/cognition/safety.ts, src/cognition/safety.test.ts</files>
  <behavior>
    - createSafetyState() inicia com repeatCount 0, consecutiveFailures 0, cooldown vazio
    - recordAttempt com a mesma (skill,target) do anterior incrementa repeatCount; muda -> reseta para 1
    - shouldAbandon retorna true quando repeatCount >= config.antiRepeatN
    - recordFailure incrementa consecutiveFailures e poe o target em cooldown (until = now + targetCooldownMs)
    - recordSuccess zera consecutiveFailures e repeatCount
    - shouldFallbackToIdle retorna true quando consecutiveFailures >= config.backoffM
    - isInCooldown(target, now) true enquanto now < until; false depois
    - cooledDownTargets(now) retorna o Set de targets ainda em cooldown (consumido por arbitrate excludeTargets)
  </behavior>
  <action>
Criar src/cognition/safety.ts com uma maquina de estado PURA (sem timers, sem bot) — o tempo entra como parametro `now`:

```typescript
// src/cognition/safety.ts
// D-10: anti-repeticao (mesma acao N vezes sem progresso -> abandona).
// D-11: backoff de falha (cooldown de alvo; M falhas consecutivas -> Idle).
import { config } from '../config'

export interface SafetyState {
  lastKey: string | null          // `${skill}:${target}` do ultimo attempt
  repeatCount: number             // quantas vezes a mesma key repetiu sem progresso
  consecutiveFailures: number     // falhas de skill seguidas (D-11)
  cooldownUntil: Map<string, number>  // target -> timestamp (ms) ate quando esta em cooldown
}

export function createSafetyState(): SafetyState {
  return { lastKey: null, repeatCount: 0, consecutiveFailures: 0, cooldownUntil: new Map() }
}

const keyOf = (skill: string, target: string) => `${skill}:${target}`

/** Registra uma tentativa. Se for a mesma do anterior, incrementa repeatCount; senao reseta para 1. */
export function recordAttempt(s: SafetyState, skill: string, target: string): void {
  const key = keyOf(skill, target)
  if (key === s.lastKey) s.repeatCount += 1
  else { s.lastKey = key; s.repeatCount = 1 }
}

/** D-10: abandonar quando a mesma acao/alvo repetiu N vezes sem progresso. */
export function shouldAbandon(s: SafetyState): boolean {
  return s.repeatCount >= config.antiRepeatN
}

/** D-11: registra falha de skill (timeout/stuck), poe alvo em cooldown e conta falha consecutiva. */
export function recordFailure(s: SafetyState, target: string, now: number = Date.now()): void {
  s.consecutiveFailures += 1
  s.cooldownUntil.set(target, now + config.targetCooldownMs)
}

/** Sucesso zera os contadores de seguranca. */
export function recordSuccess(s: SafetyState): void {
  s.consecutiveFailures = 0
  s.repeatCount = 0
  s.lastKey = null
}

/** D-11: cair para Idle quando M falhas consecutivas. */
export function shouldFallbackToIdle(s: SafetyState): boolean {
  return s.consecutiveFailures >= config.backoffM
}

export function isInCooldown(s: SafetyState, target: string, now: number = Date.now()): boolean {
  const until = s.cooldownUntil.get(target)
  return until !== undefined && now < until
}

/** Set de targets ainda em cooldown — passar como excludeTargets para arbitrate (D-05/D-07). */
export function cooledDownTargets(s: SafetyState, now: number = Date.now()): Set<string> {
  const out = new Set<string>()
  for (const [target, until] of s.cooldownUntil) if (now < until) out.add(target)
  return out
}

/** Helper: classifica um erro de skill como falha conhecida do executor (D-11). */
export function isSkillFailure(err: unknown): boolean {
  return err instanceof Error && (err.name === 'SkillTimeoutError' || err.name === 'SkillStuckError' || true)
}
```

NOTA sobre isSkillFailure: qualquer erro lancado por uma skill conta como falha para o backoff (timeout, stuck, ou "bloco nao encontrado"). Mantenha a checagem explicita dos names para documentar a origem, mas trate todo Error como falha (o `|| true` acima); ajuste o teste para refletir isso.

Criar src/cognition/safety.test.ts cobrindo o bloco <behavior>, passando `now` explicito para testar cooldown deterministicamente (ex.: recordFailure(s,'oak_log',1000); isInCooldown(s,'oak_log',1000+x)). Use `import { test, expect } from 'bun:test'`.
  </action>
  <verify>
    <automated>cd /root/MineMind && bun test src/cognition/safety.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/cognition/safety.ts contem `recordFailure`, `recordSuccess`, `shouldAbandon`, `shouldFallbackToIdle`, `cooledDownTargets`, `consecutiveFailures`
    - Teste comprova: N repeticoes -> shouldAbandon true; M falhas -> shouldFallbackToIdle true; recordSuccess zera contadores; cooldown expira com `now`
    - bun test src/cognition/safety.test.ts passa
    - bun run typecheck sai 0
  </acceptance_criteria>
  <done>Rede anti-repeticao e backoff implementados como maquina pura testavel; COG-04 coberto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WorldSnapshot -> arbitragem | Snapshot ja imutavel (deep-frozen na Fase 1); arbitragem so le. Sem nova entrada externa nesta fase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | arbitrate/highestPriorityGatherTarget | accept | So leem o WorldSnapshot read-only e config estatica; nenhuma escrita, nenhum bot, nenhum eval. Sem superficie de adulteracao. |
| T-02-06 | Denial of Service | gatheringLadder (config) | mitigate | Lista finita e estatica de tipos de bloco; loop O(escada) por tick. Nenhuma entrada do usuario controla o tamanho. Sem amplificacao. |
| T-02-07 | Denial of Service | safety cooldownUntil Map | mitigate | Chaves sao tipos de bloco da escada (conjunto finito), nao entrada arbitraria de jogador. Map limitado pelo tamanho da escada. |
</threat_model>

<verification>
- bun run typecheck sai 0 com config estendida + tres modulos cognitivos.
- bun test src/cognition/arbiter.test.ts src/cognition/safety.test.ts todos verdes.
- arbitrate, highestPriorityGatherTarget, recordFailure, shouldFallbackToIdle presentes e cobertos.
</verification>

<success_criteria>
- COG-02: estado escolhido por prioridade fixa Socializing>Gathering>Exploring>Idle; modos paused/standby sobrepoem; stubs declarados.
- COG-04: repeticao N detectada (abandona) e M falhas consecutivas (cai para Idle); alvos falhos em cooldown.
- D-07: escada de sobrevivencia configuravel escolhe o alvo de maior prioridade presente.
- Funcoes puras testadas, prontas para o Plano 03 fiar no grafo e no driver.
</success_criteria>

<output>
Apos completar, criar `.planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-02-SUMMARY.md`
</output>
