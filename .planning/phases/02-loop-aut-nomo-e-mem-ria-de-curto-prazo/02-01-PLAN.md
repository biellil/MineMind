---
phase: 02-loop-aut-nomo-e-mem-ria-de-curto-prazo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/cognition/types.ts
  - src/memory/shortTerm.ts
  - src/memory/shortTerm.test.ts
  - src/control/commands.ts
  - src/control/commands.test.ts
autonomous: true
requirements: [MEM-01]
must_haves:
  truths:
    - "A memória de curto prazo é um ring buffer limitado por orçamento de tokens estimado"
    - "Ao estourar o orçamento, eventos mais antigos são removidos por FIFO"
    - "Um comando literal de chat (!pausar/!vem/!livre) altera o modo de controle sem nenhum LLM"
    - "Comandos não reconhecidos são ignorados (no-op) sem alterar o modo"
  artifacts:
    - path: "src/cognition/types.ts"
      provides: "Contratos compartilhados: CognitiveState, ControlMode, MemEvent"
      contains: "export type CognitiveState"
    - path: "src/memory/shortTerm.ts"
      provides: "Ring buffer rico com evicção por orçamento de tokens (D-12/D-13)"
      contains: "createMemory"
    - path: "src/control/commands.ts"
      provides: "Máquina de modo de controle + parser literal de chat (D-08/D-09)"
      contains: "registerChatCommands"
  key_links:
    - from: "src/control/commands.ts"
      to: "bot.on('chat')"
      via: "match literal de palavra-chave -> setMode"
      pattern: "bot\\.on\\(['\"]chat['\"]"
    - from: "src/memory/shortTerm.ts"
      to: "estimateTokens"
      via: "evicção FIFO enquanto total > budget"
      pattern: "length / 4"
---

<objective>
Estabelecer a fundação sem-LLM da Fase 2: os contratos de tipos compartilhados, a memória de curto prazo (ring buffer com orçamento de tokens) e a máquina de modo de controle dirigida por comando literal de chat. Também instala o `@langchain/langgraph` 1.x (que o Plano 02 consome).

Este plano NÃO toca no `bot` além de registrar um único handler `bot.on('chat', ...)` isolado no módulo de controle (D-09). Nenhum nó cognitivo aqui — apenas as peças que o grafo do Plano 02 vai fiar.

Purpose: Entregar MEM-01 (ring buffer + esqueleto de orçamento de tokens) e o freio de segurança do modo de controle (D-08/D-09) como contratos prontos antes do loop existir.
Output: `src/cognition/types.ts`, `src/memory/shortTerm.ts`, `src/control/commands.ts` (+ testes) e dependências instaladas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-CONTEXT.md
@.planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-RESEARCH.md

<interfaces>
<!-- Contratos da Fase 1 que este plano consome. Use diretamente, sem explorar o codebase. -->

De src/perception/types.ts (entrada da camada cognitiva — NÃO modificar):
```typescript
export interface WorldSnapshot {
  readonly capturedAt: number
  readonly status: { readonly health: number; readonly food: number; readonly position: { x: number; y: number; z: number }; readonly timeOfDay: number; readonly isDay: boolean }
  readonly players: ReadonlyArray<{ username: string; position: {x:number;y:number;z:number} | null; distance: number | null }>
  readonly nearbyBlockTypes: Readonly<Record<string, { count: number; examples: ReadonlyArray<{x:number;y:number;z:number}> }>>
  readonly inventory: ReadonlyArray<{ slot: number; name: string; type: number; count: number }>
}
```

De src/bot/connection.ts (assinatura do callback de sessão):
```typescript
export type BotReadyCallback = (bot: Bot) => void
// onBotReady(bot) roda UMA vez por sessão de bot (cada reconexão cria sessão nova -> novo onBotReady).
// Registrar bot.on('chat') aqui é seguro: o handler morre com a sessão (confirmado em connection.ts:64).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar @langchain/langgraph 1.x + @langchain/core e criar contratos de tipos compartilhados</name>
  <read_first>
    - package.json (deps atuais: mineflayer 4.37.1, zod 4.4.3 — confirmar antes de adicionar)
    - .planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-RESEARCH.md (seção "Standard Stack" + "Code Examples")
    - src/perception/types.ts (WorldSnapshot — entrada do loop, não modificar)
  </read_first>
  <files>package.json, src/cognition/types.ts</files>
  <action>
Instalar as dependências do loop (versões verificadas por runtime-test sob Bun 1.3.2 — IGNORAR o "0.4.x" desatualizado da CLAUDE.md/STACK.md, ver Pitfall 2 do RESEARCH):

```bash
bun add @langchain/langgraph@^1.4.4 @langchain/core@^1.2.0
```

Depois criar `src/cognition/types.ts` com os contratos compartilhados que os Planos 02 e 03 consomem. Conteúdo EXATO:

```typescript
// src/cognition/types.ts
// Contratos compartilhados da camada cognitiva (Fase 2). Sem LLM.

/** Estados cognitivos (COG-02 / D-06). Fighting e Building são stub. */
export type CognitiveState =
  | 'idle'
  | 'exploring'
  | 'gathering'
  | 'socializing'
  | 'fighting'   // stub (D-06)
  | 'building'   // stub (D-06)

/** Modos de controle por comando de chat (D-08). Autônomo é o padrão. */
export type ControlMode = 'autonomous' | 'paused' | 'standby'

/** Eventos ricos gravados na memória de curto prazo (D-12). Discriminados por `type`. */
export type MemEvent =
  | { type: 'state_transition'; from: CognitiveState; to: CognitiveState; timestamp: number }
  | { type: 'action'; skill: string; target: string; result: 'success' | 'failure'; reason?: string; timestamp: number }
  | { type: 'world'; event: 'damage' | 'hunger' | 'player_joined' | 'player_left'; detail: string; timestamp: number }
  | { type: 'chat_command'; command: string; from: string; mode: ControlMode; timestamp: number }
```

NÃO adicionar lógica de loop aqui — apenas tipos. NÃO importar `mineflayer` neste arquivo (tipos puros).
  </action>
  <verify>
    <automated>cd /root/MineMind && grep -q '"@langchain/langgraph"' package.json && grep -q '"@langchain/core"' package.json && grep -q 'export type CognitiveState' src/cognition/types.ts && grep -q 'export type ControlMode' src/cognition/types.ts && grep -q 'export type MemEvent' src/cognition/types.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contém `"@langchain/langgraph"` e `"@langchain/core"`
    - `src/cognition/types.ts` contém `export type CognitiveState`, `export type ControlMode`, `export type MemEvent`
    - `bun run typecheck` sai com código 0
    - `src/cognition/types.ts` NÃO contém `import` de `'mineflayer'`
  </acceptance_criteria>
  <done>Dependências do langgraph 1.x instaladas e tipos compartilhados exportados; typecheck passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ring buffer de memória de curto prazo com orçamento de tokens (MEM-01 / D-12 / D-13)</name>
  <read_first>
    - src/cognition/types.ts (MemEvent — criado na Task 1, é o tipo de entrada do buffer)
    - .planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-RESEARCH.md (Pattern 4: Ring buffer com orçamento de tokens — código de referência)
  </read_first>
  <files>src/memory/shortTerm.ts, src/memory/shortTerm.test.ts</files>
  <behavior>
    - push de um evento dentro do orçamento mantém o evento no buffer (length cresce)
    - push que estoura o orçamento remove eventos mais ANTIGOS primeiro (FIFO): o primeiro a sair é o de menor timestamp
    - estimateTokens de um evento = Math.ceil(JSON.stringify(evento).length / 4)
    - getEvents() retorna os eventos em ordem cronológica (mais antigo primeiro)
    - totalTokens() nunca excede o orçamento após um push
    - createMemory(budget) com budget pequeno (ex.: 20 tokens) descarta eventos antigos até caber
  </behavior>
  <action>
Criar `src/memory/shortTerm.ts` implementando o ring buffer rico (D-12) com evicção FIFO por orçamento de tokens estimado (D-13). Valor padrão do orçamento (Claude's discretion): `DEFAULT_TOKEN_BUDGET = 2000` tokens.

Estrutura EXATA:

```typescript
// src/memory/shortTerm.ts
// MEM-01 / D-12 / D-13: ring buffer rico de eventos + evicção por orçamento de tokens estimado.
// Esqueleto pronto para a Fase 3 trocar estimateTokens por um tokenizer real (js-tiktoken).
import type { MemEvent } from '../cognition/types'

/** Orçamento padrão de tokens da memória de curto prazo (D-13, Claude's discretion). */
export const DEFAULT_TOKEN_BUDGET = 2000

export interface ShortTermMemory {
  readonly events: MemEvent[]   // ordem cronológica: [0] = mais antigo
  readonly budget: number       // orçamento de tokens
}

/** Estima tokens de um evento via heurística ~4 chars/token (D-13). Fase 3 troca por tokenizer real. */
export function estimateTokens(e: MemEvent): number {
  return Math.ceil(JSON.stringify(e).length / 4)
}

/** Cria uma memória vazia com o orçamento informado. */
export function createMemory(budget: number = DEFAULT_TOKEN_BUDGET): ShortTermMemory {
  return { events: [], budget }
}

/** Soma estimada de tokens de todos os eventos no buffer. */
export function totalTokens(mem: ShortTermMemory): number {
  return mem.events.reduce((sum, e) => sum + estimateTokens(e), 0)
}

/**
 * Acrescenta um evento e faz evicção FIFO enquanto o total estourar o orçamento.
 * Retorna uma NOVA ShortTermMemory (imutável — não muta a entrada).
 */
export function push(mem: ShortTermMemory, e: MemEvent): ShortTermMemory {
  const events = [...mem.events, e]
  let total = events.reduce((sum, x) => sum + estimateTokens(x), 0)
  while (total > mem.budget && events.length > 0) {
    total -= estimateTokens(events.shift()!)   // remove o mais antigo (FIFO)
  }
  return { events, budget: mem.budget }
}

/** Retorna os eventos em ordem cronológica (mais antigo primeiro). */
export function getEvents(mem: ShortTermMemory): ReadonlyArray<MemEvent> {
  return mem.events
}
```

Criar `src/memory/shortTerm.test.ts` com testes `bun test` cobrindo os casos do bloco <behavior>: (1) push dentro do orçamento, (2) evicção FIFO ao estourar (o evento de menor timestamp sai primeiro), (3) totalTokens nunca > budget após push, (4) estimateTokens = ceil(len/4). Use `import { test, expect } from 'bun:test'`.
  </action>
  <verify>
    <automated>cd /root/MineMind && bun test src/memory/shortTerm.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/memory/shortTerm.ts` contém `createMemory`, `push`, `estimateTokens`, `totalTokens`, `getEvents`
    - `src/memory/shortTerm.ts` contém `length / 4` (heurística de token D-13)
    - `bun test src/memory/shortTerm.test.ts` passa (todos os testes verdes)
    - Teste comprova evicção FIFO: após estourar orçamento, `getEvents()[0]` NÃO é o evento original mais antigo
    - `bun run typecheck` sai com código 0
  </acceptance_criteria>
  <done>Ring buffer com evicção FIFO por orçamento de tokens implementado e testado; MEM-01 satisfeito.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Máquina de modo de controle + parser literal de chat (D-08 / D-09)</name>
  <read_first>
    - src/cognition/types.ts (ControlMode — modos Autônomo/Pausado/Standby)
    - src/bot/connection.ts (confirmar que onBotReady roda 1x por sessão; o handler de chat morre com a sessão)
    - .planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-RESEARCH.md (Pattern 3 + Pitfall 5: ignorar bot.username, registrar 1x por sessão)
  </read_first>
  <files>src/control/commands.ts, src/control/commands.test.ts</files>
  <behavior>
    - parseCommand('!pausar') === 'paused'
    - parseCommand('!vem') === 'standby' e parseCommand('!aqui') === 'standby'
    - parseCommand('!livre') === 'standby' NÃO — deve ser 'autonomous'
    - parseCommand com case/espaços ('  !PAUSAR ') normaliza para 'paused'
    - parseCommand de texto não reconhecido ('oi tudo bem?') retorna null (no-op)
    - createControlState() inicia em 'autonomous'; setMode/getMode funcionam
    - parseCommand NUNCA usa eval/Function/interpolação — só lookup literal em um objeto
  </behavior>
  <action>
Criar `src/control/commands.ts` com a máquina de modo de controle (estado fora do `bot`) e o parser literal de chat. Palavras-chave EXATAS escolhidas (Claude's discretion, D-09): `!pausar` -> paused, `!vem` -> standby, `!aqui` -> standby, `!livre` -> autonomous.

Estrutura EXATA:

```typescript
// src/control/commands.ts
// D-08/D-09: máquina de modo de controle + parser de comando literal de chat (SEM LLM).
// Segurança: lookup literal exato em um mapa imutável — nenhum eval, Function ou interpolação.
import type { Bot } from 'mineflayer'
import type { ControlMode, MemEvent } from '../cognition/types'

/** Mapa literal palavra-chave -> modo (D-09). Match EXATO após trim+lowercase. */
const COMMANDS: Readonly<Record<string, ControlMode>> = Object.freeze({
  '!pausar': 'paused',
  '!vem': 'standby',
  '!aqui': 'standby',
  '!livre': 'autonomous',
})

/**
 * Mapeia uma mensagem de chat para um modo, ou null se não for comando reconhecido.
 * Match literal exato — segurança ASVS: nenhuma interpolação/eval, ignora entrada desconhecida.
 */
export function parseCommand(message: string): ControlMode | null {
  const key = message.trim().toLowerCase()
  return Object.prototype.hasOwnProperty.call(COMMANDS, key) ? COMMANDS[key]! : null
}

/** Estado de controle mutável que vive FORA do bot (lido pelo nó decide do grafo). */
export interface ControlState {
  getMode(): ControlMode
  setMode(mode: ControlMode): void
}

export function createControlState(initial: ControlMode = 'autonomous'): ControlState {
  let mode: ControlMode = initial
  return {
    getMode: () => mode,
    setMode: (m: ControlMode) => { mode = m },
  }
}

/**
 * Registra UM handler bot.on('chat') que aplica comandos literais ao ControlState.
 * Chamar dentro de onBotReady (1x por sessão — handler morre com a sessão, Pitfall 5).
 * onCommand (opcional): callback p/ gravar o comando na memória de curto prazo (D-12).
 */
export function registerChatCommands(
  bot: Bot,
  control: ControlState,
  onCommand?: (e: Extract<MemEvent, { type: 'chat_command' }>) => void,
): void {
  bot.on('chat', (username: string, message: string) => {
    if (username === bot.username) return            // Pitfall 5: ignora a si mesmo
    const mode = parseCommand(message)
    if (mode === null) return                        // no-op em entrada não reconhecida
    control.setMode(mode)
    onCommand?.({ type: 'chat_command', command: message.trim().toLowerCase(), from: username, mode, timestamp: Date.now() })
  })
}
```

Criar `src/control/commands.test.ts` cobrindo o bloco <behavior> (parseCommand para cada keyword, normalização case/espaços, null em entrada desconhecida, createControlState inicial autonomous + setMode/getMode). Use `import { test, expect } from 'bun:test'`. NÃO precisa de um bot real para testar parseCommand/createControlState.
  </action>
  <verify>
    <automated>cd /root/MineMind && bun test src/control/commands.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/control/commands.ts` contém `registerChatCommands`, `parseCommand`, `createControlState`
    - `src/control/commands.ts` contém `bot.on('chat'` e `username === bot.username`
    - `src/control/commands.ts` NÃO contém `eval(`, `new Function(`, nem template-string interpolada montando comando
    - `bun test src/control/commands.test.ts` passa (todos verdes)
    - Teste comprova: `parseCommand('!livre') === 'autonomous'` e `parseCommand('texto qualquer') === null`
    - `bun run typecheck` sai com código 0
  </acceptance_criteria>
  <done>Máquina de modo de controle e parser literal de chat prontos e testados; freio de segurança D-08/D-09 disponível para o loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jogador (chat) -> parser de comando | Entrada não confiável do chat do servidor cruza para o `ControlState`. Única nova superfície de entrada da fase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | parseCommand (src/control/commands.ts) | mitigate | Match literal exato via `Object.prototype.hasOwnProperty` em mapa `Object.freeze`. Nenhum `eval`/`new Function`/interpolação. Entrada desconhecida vira no-op (retorna null). |
| T-02-02 | Elevation of Privilege | registerChatCommands | mitigate | Qualquer jogador pode mudar o modo (pausar/standby/livre) por design — é um freio de segurança cooperativo, sem auth no MVP local. Risco LOW: servidor Java local offline-mode, sem PII, sem persistência. Documentar; auth de comando é fora de escopo da v1. |
| T-02-03 | Denial of Service | registerChatCommands | accept | Spam de comandos só alterna um enum em memória (O(1)), não dispara skill nem I/O. Sem amplificação. Aceito. |
| T-02-04 | Tampering | bot.on('chat') auto-trigger | mitigate | Ignora `username === bot.username` (Pitfall 5) — o bot não reage às próprias mensagens. |
</threat_model>

<verification>
- `bun run typecheck` sai 0 com os três novos módulos.
- `bun test src/memory/shortTerm.test.ts src/control/commands.test.ts` todos verdes.
- `grep -r "eval(\|new Function(" src/control/` retorna vazio (segurança do parser).
- `@langchain/langgraph` e `@langchain/core` presentes em package.json.
</verification>

<success_criteria>
- MEM-01 satisfeito: ring buffer limitado por orçamento de tokens estimado com evicção FIFO, testado.
- D-08/D-09: comando literal de chat altera modo sem LLM; entrada desconhecida é no-op; bot ignora a si mesmo.
- Contratos compartilhados (CognitiveState, ControlMode, MemEvent) exportados para os Planos 02/03.
- LangGraph 1.x instalado e Bun-safe.
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-01-SUMMARY.md`
</output>
