---
phase: 02-loop-aut-nomo-e-mem-ria-de-curto-prazo
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/cognition/nodes.ts
  - src/cognition/graph.ts
  - src/cognition/loop.ts
  - src/bot/index.ts
autonomous: true
requirements: [COG-01, COG-02, COG-04, MEM-01]
must_haves:
  truths:
    - "O loop roda continuamente via um driver externo (while + graph.invoke por tick) sem estourar recursionLimit"
    - "Cada tick observa o mundo, decide um estado por regra fixa, executa NO MAXIMO uma skill e aguarda concluir (single-flight, D-02)"
    - "O nó execute usa executeWithSafety (Fase 1) — nunca recria timeout/watchdog"
    - "O driver para quando a sessao do bot termina (nao deixa loop orfao apos reconexao)"
    - "Transicoes de estado e acoes sao gravadas na memoria de curto prazo e logadas no console (comportamento visivel)"
  artifacts:
    - path: "src/cognition/graph.ts"
      provides: "StateGraph com Annotation.Root + MemorySaver (Pattern 1)"
      contains: "Annotation.Root"
    - path: "src/cognition/loop.ts"
      provides: "startCognitiveLoop: driver externo single-flight com stop-on-disconnect"
      contains: "startCognitiveLoop"
    - path: "src/cognition/nodes.ts"
      provides: "Nós observe/analyze/updateMemory/decide/execute"
      contains: "executeWithSafety"
    - path: "src/bot/index.ts"
      provides: "onBotReady inicia o loop em vez da demo da Fase 1"
      contains: "startCognitiveLoop"
  key_links:
    - from: "src/cognition/loop.ts"
      to: "graph.invoke"
      via: "while externo re-invoca o grafo por tick com thread_id"
      pattern: "graph\\.invoke"
    - from: "src/cognition/nodes.ts"
      to: "executeWithSafety + skillRegistry"
      via: "execute chama 1 skill por nome via executor centralizado"
      pattern: "executeWithSafety"
    - from: "src/bot/index.ts"
      to: "startCognitiveLoop + registerChatCommands"
      via: "onBotReady fia o loop e o parser de comando por sessao"
      pattern: "startCognitiveLoop"
---

<objective>
Montar e fiar o loop cognitivo completo: o StateGraph finito-por-tick (Observe -> Analyze -> Update Memory -> Decide -> Execute -> END) com MemorySaver, e o driver externo single-flight (while + graph.invoke por tick) que satisfaz a "aresta de retorno" da D-01 sem estourar o recursionLimit do LangGraph (Pitfall 1 do RESEARCH). Por fim, substituir a demo da Fase 1 em onBotReady por startCognitiveLoop + registro do parser de comando de chat — com parada do driver quando a sessao do bot termina (Open Question 1).

O nó execute dispara NO MAXIMO uma skill por tick via executeWithSafety (D-02), consumindo arbiter/safety (Plano 02) e o ring buffer + control (Plano 01). O `bot` chega aos nós por closure — NUNCA entra no estado anotado (Pitfall 3).

Purpose: Entregar COG-01 (loop ciclico rodando sozinho) integrando todas as pecas; provar a arquitetura central sem LLM.
Output: src/cognition/nodes.ts, src/cognition/graph.ts, src/cognition/loop.ts e src/bot/index.ts religado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-CONTEXT.md
@.planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-RESEARCH.md
@.planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-01-SUMMARY.md
@.planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-02-SUMMARY.md

<interfaces>
<!-- API LangGraph verificada por runtime-test sob Bun 1.3.2 (RESEARCH). Use EXATAMENTE assim. -->

```typescript
import { StateGraph, Annotation, START, END, MemorySaver } from '@langchain/langgraph'
// Annotation.Root({ field: Annotation<T>({ reducer: (prev, upd) => next, default: () => init }) })
// new StateGraph(Root).addNode(name, fn).addEdge(a,b).compile({ checkpointer })
// graph.invoke(delta, { configurable: { thread_id } })  -> estado persiste por thread_id entre invokes
// START === '__start__', END === '__end__'
```

De Plano 01 (src/cognition/types.ts, src/memory/shortTerm.ts, src/control/commands.ts):
```typescript
export type CognitiveState; export type ControlMode; export type MemEvent
export interface ShortTermMemory; export function createMemory(budget?): ShortTermMemory
export function push(mem, e: MemEvent): ShortTermMemory; export function getEvents(mem): ReadonlyArray<MemEvent>
export interface ControlState { getMode(): ControlMode; setMode(m: ControlMode): void }
export function createControlState(initial?): ControlState
export function registerChatCommands(bot, control, onCommand?): void
```

De Plano 02 (src/cognition/arbiter.ts, src/cognition/safety.ts):
```typescript
export function arbitrate(s, mode, excludeTargets?): CognitiveState
export function highestPriorityGatherTarget(s, excludeTargets?): string | null
export interface SafetyState; export function createSafetyState(): SafetyState
export function recordAttempt(s, skill, target): void; export function shouldAbandon(s): boolean
export function recordFailure(s, target, now?): void; export function recordSuccess(s): void
export function shouldFallbackToIdle(s): boolean; export function cooledDownTargets(s, now?): Set<string>
```

De Fase 1 (src/skills/index.ts, src/skills/executor.ts, src/perception/snapshot.ts):
```typescript
export const skillRegistry: Record<string, (bot, params) => Promise<void>>  // navigate, dig, follow(stub), attack(stub)
export function executeWithSafety<T>(action: () => Promise<T>, opts?): Promise<T>
export function buildWorldSnapshot(bot): WorldSnapshot
```

De Fase 1 (src/bot/connection.ts): `onReady(bot)` roda 1x por sessao; reconexao cria sessao nova. NAO ha evento custom de "stop" — use `bot.on('end', ...)` para sinalizar parada do driver desta sessao.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nós do grafo (observe / analyze / updateMemory / decide / execute)</name>
  <read_first>
    - src/perception/snapshot.ts (buildWorldSnapshot — chamado pelo nó observe)
    - src/skills/index.ts e src/skills/executor.ts (skillRegistry, executeWithSafety — chamados pelo execute)
    - src/cognition/arbiter.ts e src/cognition/safety.ts (Plano 02)
    - src/memory/shortTerm.ts (push/createMemory — Plano 01)
    - .planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-RESEARCH.md (Pattern 1 + Anti-Patterns: bot por closure, 1 skill por tick, nao recriar watchdog)
  </read_first>
  <files>src/cognition/nodes.ts</files>
  <action>
Criar src/cognition/nodes.ts. Os nós sao funcoes que recebem o estado anotado e retornam um DELTA parcial. O `bot`, o `ControlState` e o `SafetyState` chegam por CLOSURE atraves de uma factory `createNodes(deps)` — NUNCA pelo estado anotado (Pitfall 3). Single-flight: o execute dispara no maximo UMA skill por tick e a aguarda (D-02).

Estrutura:

```typescript
// src/cognition/nodes.ts
// Nós do StateGraph (Fase 2). bot/control/safety por closure (Pitfall 3). Sem LLM.
import type { Bot } from 'mineflayer'
import type { WorldSnapshot } from '../perception/types'
import { buildWorldSnapshot } from '../perception/snapshot'
import { skillRegistry, executeWithSafety } from '../skills/index'
import { config } from '../config'
import type { CognitiveState, MemEvent } from './types'
import type { ShortTermMemory } from '../memory/shortTerm'
import { push } from '../memory/shortTerm'
import type { ControlState } from '../control/commands'
import { arbitrate, highestPriorityGatherTarget } from './arbiter'
import {
  type SafetyState, recordAttempt, shouldAbandon, recordFailure, recordSuccess,
  shouldFallbackToIdle, cooledDownTargets,
} from './safety'

/** Estado anotado do loop. Carrega apenas dados puros — NUNCA o bot. */
export interface LoopState {
  snapshot: WorldSnapshot | null
  cogState: CognitiveState
  memory: ShortTermMemory
}

export interface NodeDeps {
  bot: Bot
  control: ControlState
  safety: SafetyState
}

const now = () => Date.now()
const log = (msg: string) => console.log(`[loop] ${msg}`)

export function createNodes(deps: NodeDeps) {
  const { bot, control, safety } = deps

  // OBSERVE: captura snapshot imutavel (D-04). bot via closure.
  const observe = async (_s: LoopState): Promise<Partial<LoopState>> => {
    return { snapshot: buildWorldSnapshot(bot) }
  }

  // ANALYZE: deriva estado por arbitragem de prioridade fixa (D-05), excluindo alvos em cooldown (D-11).
  const analyze = async (s: LoopState): Promise<Partial<LoopState>> => {
    if (!s.snapshot) return { cogState: 'idle' }
    const excluded = cooledDownTargets(safety, now())
    let next = arbitrate(s.snapshot, control.getMode(), excluded)
    if (shouldFallbackToIdle(safety)) next = 'idle'   // D-11: backoff -> Idle
    return { cogState: next }
  }

  // UPDATE MEMORY: grava transicao de estado se mudou (D-12). Log torna o comportamento visivel.
  const updateMemory = async (s: LoopState): Promise<Partial<LoopState>> => {
    return {}  // transicao gravada no decide/execute abaixo para ter from/to; placeholder no-op aqui
  }

  // DECIDE: registra a tentativa e grava a transicao na memoria (from prev -> to atual).
  const decide = async (s: LoopState): Promise<Partial<LoopState>> => {
    return {}  // logica de transicao consolidada no execute (precisa do cogState ja resolvido)
  }

  // EXECUTE: dispara NO MAXIMO uma skill via executeWithSafety (D-02 single-flight). Atualiza memoria + safety.
  const execute = async (s: LoopState): Promise<Partial<LoopState>> => {
    const snap = s.snapshot
    const state = s.cogState
    let memory = s.memory

    // grava transicao de estado (D-12)
    log(`estado=${state} modo=${control.getMode()}`)

    // mapeia estado -> (skill, target). Apenas estados ativos disparam skill.
    let skill: string | null = null
    let target = ''
    if (snap && state === 'gathering') {
      const t = highestPriorityGatherTarget(snap, cooledDownTargets(safety, now()))
      if (t) { skill = 'dig'; target = t }
    } else if (snap && state === 'exploring') {
      // exploring: navega para um ponto deslocado (vaguear visivel). Sem alvo de bloco.
      skill = 'navigate'
      const p = snap.status.position
      target = JSON.stringify({ x: Math.round(p.x + (Math.random() * 16 - 8)), y: Math.round(p.y), z: Math.round(p.z + (Math.random() * 16 - 8)) })
    } else if (snap && state === 'socializing') {
      // standby/jogador proximo: aproxima-se do jogador mais proximo e aguarda (usa navigate, nao o stub follow)
      const player = [...snap.players].filter(p => p.position).sort((a, b) => (a.distance ?? 1e9) - (b.distance ?? 1e9))[0]
      if (player?.position) { skill = 'navigate'; target = JSON.stringify(player.position) }
    }
    // idle / fighting(stub) / building(stub): nenhuma skill (D-06)

    if (!skill) {
      log(`sem acao (estado=${state})`)
      return { memory }
    }

    // D-10: anti-repeticao
    recordAttempt(safety, skill, target)
    if (shouldAbandon(safety)) {
      log(`abandonando ${skill}:${target} (repetido ${config.antiRepeatN}x sem progresso)`)
      recordFailure(safety, target, now())   // marca cooldown e conta como falha
      memory = push(memory, { type: 'action', skill, target, result: 'failure', reason: 'anti-repeat', timestamp: now() })
      return { memory }
    }

    // D-02: single-flight — UMA skill, aguardada. executeWithSafety ja faz timeout/watchdog (Fase 1).
    try {
      const params = skill === 'dig' ? { target } : { target: JSON.parse(target) }
      await executeWithSafety(() => skillRegistry[skill!]!(bot, params))
      recordSuccess(safety)
      memory = push(memory, { type: 'action', skill, target, result: 'success', timestamp: now() })
      log(`OK ${skill} ${target}`)
    } catch (err) {
      const reason = err instanceof Error ? err.name : String(err)
      recordFailure(safety, target, now())   // D-11: backoff
      memory = push(memory, { type: 'action', skill, target, result: 'failure', reason, timestamp: now() })
      log(`FALHA ${skill} ${target}: ${reason}`)
    }
    return { memory }
  }

  return { observe, analyze, updateMemory, decide, execute }
}
```

NOTA: consolidei a logica de transicao/execucao no nó `execute` (analyze ja resolveu cogState). Os nós updateMemory/decide ficam como no-op simples para manter o ciclo nominal Observe->Analyze->UpdateMemory->Decide->Execute do roadmap (D-01) visivel no grafo, sem duplicar logica. Mantenha-os no grafo (Task 2) para fidelidade ao ciclo nomeado.
  </action>
  <verify>
    <automated>grep -q 'executeWithSafety' src/cognition/nodes.ts && grep -q 'buildWorldSnapshot' src/cognition/nodes.ts && grep -q 'createNodes' src/cognition/nodes.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/cognition/nodes.ts contem `createNodes`, `observe`, `analyze`, `execute`, `executeWithSafety`, `buildWorldSnapshot`
    - O nó execute NAO recria Promise.race/setInterval (usa executeWithSafety) — `grep -c "setInterval" src/cognition/nodes.ts` retorna 0
    - O estado anotado (LoopState) NAO contem campo do tipo Bot (bot so via closure em NodeDeps)
    - bun run typecheck sai 0
  </acceptance_criteria>
  <done>Nós do grafo prontos: observe captura snapshot, analyze arbitra, execute dispara 1 skill via executor e atualiza memoria/safety.</done>
</task>

<task type="auto">
  <name>Task 2: StateGraph com Annotation.Root + MemorySaver (D-01 / D-03)</name>
  <read_first>
    - src/cognition/nodes.ts (createNodes, LoopState — Task 1)
    - src/memory/shortTerm.ts (createMemory — default da memoria)
    - .planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-RESEARCH.md (Pattern 1: grafo finito por tick terminando em END; Anti-Pattern: NAO addEdge execute->observe)
  </read_first>
  <files>src/cognition/graph.ts</files>
  <action>
Criar src/cognition/graph.ts. O grafo e FINITO por tick (termina em END) — a "aresta de retorno" e o driver externo (Plano Task 3), NAO um self-loop (Pitfall 1: self-loop estoura recursionLimit aos 25). MemorySaver (D-03) como checkpointer, JS puro Bun-safe.

```typescript
// src/cognition/graph.ts
// D-01: StateGraph finito por tick (Observe->Analyze->UpdateMemory->Decide->Execute->END).
// D-03: MemorySaver (em memoria, Bun-safe, sem better-sqlite3). A "aresta de retorno" e o driver externo (loop.ts).
import { StateGraph, Annotation, START, END, MemorySaver } from '@langchain/langgraph'
import type { WorldSnapshot } from '../perception/types'
import type { CognitiveState } from './types'
import { type ShortTermMemory, createMemory } from '../memory/shortTerm'
import { config } from '../config'
import { createNodes, type NodeDeps } from './nodes'

const LoopAnnotation = Annotation.Root({
  snapshot: Annotation<WorldSnapshot | null>({ reducer: (_p, u) => u, default: () => null }),
  cogState: Annotation<CognitiveState>({ reducer: (_p, u) => u, default: () => 'idle' }),
  memory:   Annotation<ShortTermMemory>({ reducer: (_p, u) => u, default: () => createMemory(config.memoryTokenBudget) }),
})

/** Compila um grafo finito-por-tick com bot/control/safety injetados por closure. */
export function buildGraph(deps: NodeDeps) {
  const n = createNodes(deps)
  return new StateGraph(LoopAnnotation)
    .addNode('observe', n.observe)
    .addNode('analyze', n.analyze)
    .addNode('updateMemory', n.updateMemory)
    .addNode('decide', n.decide)
    .addNode('execute', n.execute)
    .addEdge(START, 'observe')
    .addEdge('observe', 'analyze')
    .addEdge('analyze', 'updateMemory')
    .addEdge('updateMemory', 'decide')
    .addEdge('decide', 'execute')
    .addEdge('execute', END)          // FINITO: driver externo (loop.ts) fecha o ciclo (D-01)
    .compile({ checkpointer: new MemorySaver() })
}
```
  </action>
  <verify>
    <automated>cd /root/MineMind && grep -q 'Annotation.Root' src/cognition/graph.ts && grep -q 'MemorySaver' src/cognition/graph.ts && grep -q "addEdge('execute', END)" src/cognition/graph.ts && ! grep -q "addEdge('execute', 'observe')" src/cognition/graph.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/cognition/graph.ts contem `Annotation.Root`, `MemorySaver`, `buildGraph`, `compile({ checkpointer`
    - Contem `addEdge('execute', END)` e NAO contem `addEdge('execute', 'observe')` (sem self-loop — Pitfall 1)
    - Importa de `@langchain/langgraph` (nao redefine StateGraph)
    - bun run typecheck sai 0
  </acceptance_criteria>
  <done>StateGraph finito-por-tick compilado com MemorySaver; pronto para o driver re-invocar.</done>
</task>

<task type="auto">
  <name>Task 3: Driver externo single-flight + wiring em onBotReady (COG-01 / D-02 / stop-on-disconnect)</name>
  <read_first>
    - src/cognition/graph.ts (buildGraph — Task 2)
    - src/control/commands.ts (createControlState, registerChatCommands — Plano 01)
    - src/cognition/safety.ts (createSafetyState — Plano 02)
    - src/bot/index.ts (onBotReady atual — substituir a demo da Fase 1)
    - src/bot/connection.ts (bot.on('end') sinaliza fim de sessao — usar para parar o driver; Open Question 1)
    - .planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-RESEARCH.md (Pattern 1 driver + Open Question 1 stop-on-disconnect)
  </read_first>
  <files>src/cognition/loop.ts, src/bot/index.ts</files>
  <action>
Criar src/cognition/loop.ts com o driver externo single-flight (Pattern 1). O `while` checa uma flag de vivacidade setada no `bot.on('end')` para PARAR quando a sessao morre (Open Question 1) — assim o novo onBotReady da reconexao inicia um loop fresco sem deixar o antigo orfao.

```typescript
// src/cognition/loop.ts
// COG-01 / D-01 / D-02: driver externo single-flight. A "aresta de retorno" = re-invocar o grafo por tick.
import type { Bot } from 'mineflayer'
import { config } from '../config'
import { buildGraph } from './graph'
import { createControlState } from '../control/commands'
import { registerChatCommands } from '../control/commands'
import { createSafetyState } from './safety'

const sleep = (ms: number) => new Promise<void>((r) => setTimeout(r, ms))

/**
 * Inicia o loop cognitivo para UMA sessao de bot. Para automaticamente quando a sessao termina.
 * Chamar em onBotReady (1x por sessao). Cada reconexao chama de novo com bot novo (D-03 estado do zero).
 */
export function startCognitiveLoop(bot: Bot): void {
  const control = createControlState('autonomous')
  const safety = createSafetyState()
  const graph = buildGraph({ bot, control, safety })

  // parser de comando literal de chat (D-09) — registrado 1x por sessao (handler morre com a sessao)
  registerChatCommands(bot, control)

  // stop-on-disconnect (Open Question 1): a sessao morre -> o while termina
  let alive = true
  bot.once('end', () => { alive = false })

  const cfg = { configurable: { thread_id: 'minemind-agent' } }

  // driver assincrono — nao bloqueia onBotReady
  void (async () => {
    console.log('[loop] iniciado (modo autonomo)')
    while (alive) {
      const started = Date.now()
      try {
        await graph.invoke({}, cfg)        // input vazio: MemorySaver+thread_id carregam o estado do tick anterior
      } catch (err) {
        console.error('[loop] erro no tick:', err instanceof Error ? err.message : err)
      }
      const elapsed = Date.now() - started
      if (elapsed < config.minTickMs) await sleep(config.minTickMs - elapsed)   // D-02 intervalo minimo
    }
    console.log('[loop] encerrado (sessao terminou)')
  })()
}
```

Depois religar src/bot/index.ts: substituir o corpo de demo de `onBotReady` por uma chamada a `startCognitiveLoop(bot)`. Manter o log inicial de "Online". Remover a demo de snapshot/skills da Fase 1 (ou reduzir a um unico log). onBotReady fica:

```typescript
import { startCognitiveLoop } from '../cognition/loop'
// ...
function onBotReady(bot: Bot): void {
  console.log('[MineMind] Bot pronto — iniciando loop cognitivo (Fase 2).')
  startCognitiveLoop(bot)
}
```

NAO alterar src/bot/connection.ts (o fluxo de reconexao da Fase 1 ja chama onReady por sessao). NAO passar `bot` para dentro do estado anotado (Pitfall 3) — ele entra so via buildGraph deps (closure).
  </action>
  <verify>
    <automated>cd /root/MineMind && grep -q 'startCognitiveLoop' src/cognition/loop.ts && grep -q 'graph.invoke' src/cognition/loop.ts && grep -q "bot.once('end'" src/cognition/loop.ts && grep -q 'startCognitiveLoop' src/bot/index.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/cognition/loop.ts contem `startCognitiveLoop`, `graph.invoke`, `while (alive)`, `config.minTickMs`, `registerChatCommands`
    - src/cognition/loop.ts para o driver via `bot.once('end'` (stop-on-disconnect)
    - src/bot/index.ts importa e chama `startCognitiveLoop(bot)` dentro de onBotReady; NAO contem mais a demo de skills da Fase 1 (`grep -c "Skills registradas" src/bot/index.ts` retorna 0)
    - src/bot/connection.ts NAO foi modificado
    - bun run typecheck sai 0
  </acceptance_criteria>
  <done>Driver externo single-flight rodando o grafo por tick, parando na desconexao, e fiado em onBotReady — COG-01 integrado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jogador (chat) -> control mode | Reusa o parser literal do Plano 01 (registerChatCommands). Unica entrada externa; ja mitigada la. |
| WorldSnapshot -> nós do grafo | Snapshot imutavel (Fase 1); nós so leem. bot por closure, nunca serializado (Pitfall 3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Denial of Service | loop driver (while) | mitigate | Single-flight + minTickMs garantem no maximo 1 skill por tick com intervalo; executeWithSafety impoe timeout/watchdog (Fase 1) — um tick nunca trava o loop. |
| T-02-09 | Tampering | graph state / MemorySaver | mitigate | bot NUNCA entra no estado anotado (so closure); estado carrega apenas WorldSnapshot deep-frozen e dados puros — MemorySaver nao serializa referencias vivas (Pitfall 3). |
| T-02-10 | Denial of Service | driver orfao apos reconexao | mitigate | bot.once('end') seta alive=false; o while da sessao morta termina, evitando dois drivers concorrentes (Open Question 1). |
| T-02-11 | Elevation of Privilege | comando de chat pausa o agente | accept | Identico a T-02-02 (Plano 01): freio cooperativo por design, servidor local offline-mode, sem auth na v1. |
</threat_model>

<verification>
- bun run typecheck sai 0 com nodes/graph/loop e index religado.
- `! grep -q "addEdge('execute', 'observe')" src/cognition/graph.ts` (sem self-loop — Pitfall 1).
- `grep -q "bot.once('end'" src/cognition/loop.ts` (stop-on-disconnect).
- src/bot/index.ts chama startCognitiveLoop; src/bot/connection.ts inalterado.
- (Live, no Plano 04) o loop roda contra um servidor MC e alterna estados visivelmente.
</verification>

<success_criteria>
- COG-01: loop ciclico roda sozinho via driver externo (sem GraphRecursionError), um tick = no maximo uma skill aguardada (D-02), MemorySaver persiste estado entre ticks.
- Comportamento visivel: logs `[loop] estado=...` a cada tick (Criterio #2).
- Stop-on-disconnect: driver da sessao morta termina; reconexao inicia loop fresco (D-03).
- Todas as pecas dos Planos 01/02 fiadas; o `bot` so e tocado em observe (snapshot) e no parser de chat.
</success_criteria>

<output>
Apos completar, criar `.planning/phases/02-loop-aut-nomo-e-mem-ria-de-curto-prazo/02-03-SUMMARY.md`
</output>
