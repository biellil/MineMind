# Phase 08.1: Refatorar memória — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-21
**Phase:** 08.1-refatorar-memoria
**Mode:** discuss (advisor mode ON — USER-PROFILE.md present; calibration tier = full_maturity, vendor_philosophy=conservative)
**Areas discussed:** Topologia ChromaDB · Gravação de eventos & embedding · Recuperação & uso pelo LLM · POIs/Morte/Lições

---

## Área 1 — Topologia & resiliência do ChromaDB

| Opção | Descrição | Selected |
|--------|-----------|----------|
| A. Híbrido puro | Relacional no SQLite, só vetores no Chroma; confia no try/catch+fallback existente | |
| D. Híbrido A + circuit breaker | A + breaker/timeout no fetch p/ Chroma offline não travar o tick | ✓ |
| C. Não migrar agora | Só consertar gravação morta, manter sqlite-vec | |
| B. Tudo no Chroma | Relacional + vetorial no Chroma (ponto único de falha) | |

**User's choice:** Híbrido A + circuit breaker (D)
**Notes:** Fato decisivo — cliente `chromadb` JS V3 é HTTP-puro (sem embedded em JS), exige `chroma run`, suporta BYO embeddings, roda no Bun. SQLite permanece source of truth; Chroma = índice descartável reconstruível. Collection única `events` com metadata; dimensão trava em 768 no boot.

**Follow-up (pós-tabela, pedido do dev):**
- D-22 — quando for usar o Chroma e ele estiver offline, avisar (log acionável "inicie `chroma run`") pra poder ligar; aviso debounced, loop segue degradado.
- D-23 — se a collection não existir no Chroma, criar automaticamente (get-or-create no boot/1ª escrita).

---

## Área 2 — Gravação de eventos & custo de embedding

| Opção | Descrição | Selected |
|--------|-----------|----------|
| C. recordEvent + embed só na reflexão | Helper push+persistEvent(null); embed só no batch consolidate já existente | ✓ |
| B. recordEvent + worker deferido | Persiste relacional na hora + fila/worker embedda depois | |
| D. Híbrido por floor | Embed deferido só p/ alta importância (≥7), resto no batch | |
| A. Embed síncrono no push | Sempre fresco, mas viola "tick nunca espera o LLM" | (rejeitado) |

**User's choice:** recordEvent + embed só na reflexão (C)
**Notes:** Bug-raiz = stream cru nunca persistido (só `push`, nunca `persistEvent`). `consolidate` já é o batch de embedding. Risco mínimo, código já testado, sem dep nova. B/D ficam como evolução p/ recall semântico por evento atômico.

---

## Área 3 — Recuperação & uso real pelo LLM

| Opção | Descrição | Selected |
|--------|-----------|----------|
| A→B | Landa A (fallback recentes + injeção + log) e migra já p/ B (query=goal, cacheado por hash) | ✓ |
| B direto | Query semântica pelo goal de cara, sem o passo A | |
| C | Query = snapshot serializado | |
| D | Query híbrida goal+needs+gancho do mundo | |

**User's choice:** A primeiro, depois B (goal)
**Notes:** Caminho de AÇÃO hoje não injeta NADA recuperado. Fixos: top-k=3 (pool=retrievalK=12), seção `Memórias relevantes:` no HumanMessage antes do `FATO VERIFICADO`, log `[recall]` p/ verificação ao vivo. Cache do embedding por hash do goal → ~1 embed por troca de goal.

---

## Área 4 — POIs espaciais, morte & lições evolutivas

| Opção | Descrição | Selected |
|--------|-----------|----------|
| Aceitar os 3 recomendados | POIs (type+bucket+bbox) · Morte (heurística local→death imp.10 + danger POI) · Lições (tabela própria, storage-only) | ✓ |
| Aceitar, discutir POIs | — | |
| Aceitar, discutir Lições | — | |
| Aceitar, discutir Morte | — | |

**User's choice:** Aceitar os 3 recomendados
**Notes:**
- POIs: coluna `type` TEXT (enum TS), nascimento determinístico + dedup por bucket espacial (`ON CONFLICT DO UPDATE visits+1`), busca por bounding-box SQL (não embedding).
- Morte: inferência local determinística da causa (último entityHurt + nearestHostile + posição), `MemEvent type:'death'` importância 10, upsert `danger` POI no local.
- Lições: tabela própria `lessons` (confidence + reinforce/contradict counts), reforço/decay aritmético + consulta top-por-confiança. **Fronteira:** só storage/reforço/decay/consulta — geração e Beta-Bernoulli ficam na Phase 14.
- Convergência: morte liga POIs (danger) + futura lição (Phase 14).

## Claude's Discretion

- Granularidade do bucket espacial (8 vs 16 blocos).
- Constantes de reforço/decay e threshold de remoção das lições.
- Janela de inferência da causa de morte.
- Timeout/retry do circuit breaker do Chroma.
- Formato textual das seções `Memórias relevantes:` / `POIs próximos:`.

## Deferred Ideas

- Worker de embed deferido / híbrido por floor (Área 2 B/D).
- Query de recuperação por snapshot/híbrida (Área 3 C/D).
- Plugin `mineflayer-death-event` (causa exata).
- Geração de lições + Beta-Bernoulli + belief revision → Phase 14.
- Fusão euclidiana de POIs / scoring híbrido.
- Migração total p/ Chroma (opção B) — descartada (contraria Core Value).
