---
phase: 08.1-refatorar-memoria
plan: 02
subsystem: database
tags: [memory, sqlite, persistence, longterm, wiring, cognitive-loop]

# Dependency graph
requires:
  - phase: 08.1-01
    provides: schema v2 (events/places/lessons), persistEvent (longTerm.ts), openDb (persistence.ts)
provides:
  - "Helper central recordEvent(holder, e, now, player?) = push (CP) + persistEvent (LP, embedding null)"
  - "4 pontos de origem do loop (nodes.ts x3, loop.ts x1) agora persistem eventos em LP ao vivo"
  - "Bug de fiação morto: persistEvent é chamado em produção (não só em testes)"
affects: [08.1-04-chromadb, 08.1-05-pois, 08.1-06-mortes-licoes, 14-aprendizado]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "recordEvent como ponto único de gravação CP+LP (evita push solto que esquece LP)"
    - "embedding null no caminho quente — semântica fica só no batch de reflexão (D-07)"

key-files:
  created:
    - src/memory/recordEvent.ts
    - src/memory/recordEvent.test.ts
  modified:
    - src/cognition/nodes.ts
    - src/cognition/loop.ts

key-decisions:
  - "recordEvent envolve persistEvent em try/catch — falha de LP nunca derruba o tick (Core Value)"
  - "embedding sempre null no recordEvent — não toca LLM/tick; retrieve já degrada para recência"

patterns-established:
  - "Gravação de evento = recordEvent(holder, e, now); NUNCA holder.memory = push(...) solto nos pontos de origem"

requirements-completed: []

# Metrics
duration: 4min
completed: 2026-06-21
---

# Phase 08.1 Plan 02: Conserto da Fiação de Gravação de Eventos Summary

**Helper `recordEvent` (push CP + persistEvent LP com embedding null) plugado nos 4 pontos de origem do loop — mata o bug `events=0` ao vivo sem tocar o tick nem o LLM.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-21T14:33:37Z
- **Completed:** 2026-06-21T14:37:14Z
- **Tasks:** 2
- **Files modified:** 4 (2 criados, 2 modificados)

## Accomplishments
- Criado `recordEvent(holder, e, now, player?)`: faz `push` (curto prazo) e `persistEvent(db, e, null, now, player)` (longo prazo), envolto em try/catch — nunca lança.
- Plugado nos 4 pontos de origem reais do loop: 3 no nó `execute` (anti-repeat, sucesso grounded, catch de erro) em `nodes.ts` e 1 no reflexo idle (`runReflex`) em `loop.ts`.
- Removido o `import { push }` agora órfão em ambos os arquivos (tsc limpo, sem unused-import).
- `persistEvent` passa a rodar em produção: a persistência de eventos deixa de estar morta — `SELECT count(*) FROM events` passa a crescer ao vivo (verificação automatizada via teste; live é manual fora do escopo).

## Task Commits

Cada task foi commitada atomicamente:

1. **Task 1: Criar o helper recordEvent (TDD)** - `640771f` (feat) — teste + implementação no mesmo commit (helper trivial, RED→GREEN atômico).
2. **Task 2: Plugar recordEvent nos 4 pontos de origem** - `f98bb4a` (fix)

_Nota: a Task 1 era TDD; teste e implementação foram commitados juntos por serem mínimos e indissociáveis._

## Files Created/Modified
- `src/memory/recordEvent.ts` - Helper central de gravação CP+LP, embedding null, nunca lança.
- `src/memory/recordEvent.test.ts` - 4 testes: CP+LP com db válido, db null gracioso, reatribuição de memory, não-lançamento em falha de LP.
- `src/cognition/nodes.ts` - 3 `push(holder.memory, ...)` do nó execute → `recordEvent(holder, ..., now())`; import migrado.
- `src/cognition/loop.ts` - `push` do reflexo idle (`runReflex`) → `recordEvent(holder, ..., nowTs)`; import migrado.

## Decisions Made
- **embedding null no caminho quente (D-07):** `recordEvent` nunca embedda — não chama o LLM nem bloqueia o tick. O embedding semântico continua vindo só do batch de reflexão (Plan 04). `retrieve` já degrada gracioso para recência quando o evento não tem embedding.
- **try/catch interno (Core Value):** `persistEvent` é envolto em try/catch dentro de `recordEvent`; falha de LP (ou `holder.db === null`) não derruba o loop — só o `push` em CP é garantido.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None. O log `[recordEvent] persistEvent falhou (degradando): no such table: events` que aparece na suíte é esperado — é o Test 4 verificando a degradação graciosa (DB sem schema de propósito).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Eventos passam a persistir em LP via `recordEvent` — base pronta para o Plan 04 (migração do vector store para ChromaDB) que vai embeddar via batch de reflexão.
- Verificação AO VIVO recomendada (fora do escopo automatizado): após uma sessão real, conferir `SELECT count(*) FROM events` em `minemind.sqlite` > 0.

## Self-Check: PASSED

- FOUND: src/memory/recordEvent.ts
- FOUND: src/memory/recordEvent.test.ts
- FOUND commit: 640771f (Task 1)
- FOUND commit: f98bb4a (Task 2)
- Verificação: `grep -c "recordEvent(holder"` = 3 (nodes.ts), 1 (loop.ts); 0 push de evento residual; tsc exit 0; `bun test src/memory src/cognition` = 144 pass, 0 fail.

---
*Phase: 08.1-refatorar-memoria*
*Completed: 2026-06-21*
