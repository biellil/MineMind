---
phase: 08.1-refatorar-memoria
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memory/poi-detect.ts
  - src/memory/poi-detect.test.ts
  - src/cognition/nodes.ts
autonomous: true
gap_closure: true
requirements: [GAP-01]

must_haves:
  truths:
    - "Ao coletar/minerar um recurso com SUCESSO, o agente cria um POI type:'resource' no local"
    - "Ao perceber um aldeão (villager) no snapshot, o agente cria um POI type:'village' no local do aldeão"
    - "Coleta SEM sucesso (no_effect/partial/error) NÃO cria POI resource"
    - "POIs resource/village deduplicam por bucket espacial (mesma veia/aldeia colapsa via ON CONFLICT) e nunca lançam"
    - "Os novos POIs entram no prompt via nearbyPlacesString (já fiado em deliberation.ts) — 'POIs próximos:' passa a conter resource/village além de danger"
  artifacts:
    - path: "src/memory/poi-detect.ts"
      provides: "recordResourcePoi + recordVillagePoi (puros, db+snapshot, reusam upsertPlace)"
      exports: ["recordResourcePoi", "recordVillagePoi"]
      min_lines: 40
    - path: "src/memory/poi-detect.test.ts"
      provides: "Prova criação de POI resource em success, village em villager, e não-criação em falha"
      contains: "recordResourcePoi"
    - path: "src/cognition/nodes.ts"
      provides: "Chamada de recordResourcePoi (success da skill) + recordVillagePoi (snapshot) no nó execute"
      contains: "recordResourcePoi"
  key_links:
    - from: "src/cognition/nodes.ts (execute, success da skill)"
      to: "recordResourcePoi → upsertPlace(type:'resource')"
      via: "if (success && holder.db) recordResourcePoi(...)"
      pattern: "recordResourcePoi"
    - from: "src/cognition/nodes.ts (execute, por tick com snapshot)"
      to: "recordVillagePoi → upsertPlace(type:'village')"
      via: "if (snap && holder.db) recordVillagePoi(...)"
      pattern: "recordVillagePoi"
    - from: "src/memory/poi-detect.ts"
      to: "src/memory/places.ts (upsertPlace)"
      via: "import { upsertPlace }"
      pattern: "upsertPlace"
---

<objective>
Fechar o GAP-01 da 08.1-VERIFICATION.md: a memória espacial só cria POIs `danger` (na morte, loop.ts:267).
O goal da fase exige POIs por proximidade para que "o agente saiba o que tem onde". Os helpers
(`upsertPlace` com dedup por bucket, `nearbyPlaces`, `nearbyPlacesString`) JÁ EXISTEM e o
`nearbyPlacesString` JÁ é injetado no prompt (deliberation.ts:170/186). Falta apenas CHAMAR
`upsertPlace` nos pontos de percepção/ação certos.

Este plano fia DOIS tipos de POI que o teste ao vivo confirmou faltarem:
- `resource` — quando o agente coleta/minera um recurso com SUCESSO (outcome do SkillResult no nó execute).
- `village` — quando o snapshot contém um aldeão (villager) percebido.

Purpose: concretizar a memória espacial "o que tem onde" além de perigos — base p/ tech-tree (Phase 10).
Output: módulo `poi-detect.ts` (helpers puros e testáveis) + 2 call-sites no nó execute + testes.

FORA DO ESCOPO (NÃO endereçar):
- `landmark`/`base`: nenhum gatilho claro e barato existe hoje — deixados de fora deliberadamente
  (NÃO inventar gatilhos artificiais). Documentado no topo de `poi-detect.ts`.
- Lições (`lessons`): deferido para Phase 14 (criação/leitura no prompt). NÃO tocar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-refatorar-mem-ria-migrar-vetores-para-chromadb-j-rodando-local-consertar-grava-o-de-eventos-lugares-garantir-uso-real-pelo-llm-mem-ria-espacial-pois-e-registro-de-morte/08.1-VERIFICATION.md

# Helpers existentes a REUSAR (NÃO criar API nova de POI):
@src/memory/places.ts
# Padrão a replicar (único call-site de upsertPlace hoje — death→danger):
@src/cognition/loop.ts
# Onde plugar (nó execute: success da skill + snapshot):
@src/cognition/nodes.ts
# Confirma que POIs entram no prompt via nearbyPlacesString (JÁ fiado):
@src/cognition/deliberation.ts
# Teste-modelo de POIs (convenção bun test + openDb(':memory:')):
@src/memory/places.test.ts

<interfaces>
<!-- Contratos que o executor precisa — extraídos do código. Usar diretamente, sem explorar. -->

De src/memory/persistence.ts:
```typescript
export type PlaceType = 'base' | 'resource' | 'danger' | 'village' | 'landmark'
export function openDb(path: string): Database  // ':memory:' nos testes
```

De src/memory/places.ts (REUSAR — assinatura exata):
```typescript
export function upsertPlace(
  db: Database,
  poi: { x: number; y: number; z: number; type: PlaceType; label?: string; notes?: string },
  now: number,
): void  // dedup por bucket (GRID=12) via ON CONFLICT; try/catch interno NUNCA lança
```

De src/perception/types.ts (snapshot que o nó execute já tem como `s.snapshot`):
```typescript
interface Position3D { readonly x: number; readonly y: number; readonly z: number }
interface BlockSummary { readonly count: number; readonly examples: ReadonlyArray<Position3D> }
interface EntityInfo {
  readonly type: string        // 'player' | 'mob' | 'object' | ...
  readonly kind: string        // 'Hostile mobs' | 'Passive mobs' | ...
  readonly name: string        // username (player) ou name/type do mob (ex.: 'villager')
  readonly position: Position3D
  readonly distance: number
}
interface BotStatus { readonly position: Position3D /* ... */ }
interface WorldSnapshot {
  readonly status: BotStatus
  readonly entities: ReadonlyArray<EntityInfo>
  readonly nearbyBlockTypes: Readonly<Record<string, BlockSummary>>  // chave = nome do bloco
  /* ... */
}
```

De src/cognition/state.ts (holder no nó execute):
```typescript
holder.db: Database | null   // SEMPRE guardar com `if (holder.db)` antes de chamar (padrão do death handler)
```

Padrão a replicar — death handler em src/cognition/loop.ts:267:
```typescript
if (holder.db) upsertPlace(holder.db, { x: pos.x, y: pos.y, z: pos.z, type: 'danger', notes: cause }, ts)
```

No nó execute (src/cognition/nodes.ts) o success já é derivado:
```typescript
const result = await skillRegistry[skill!]!(bot, params)
const success = result.outcome === 'success'   // ~L289
// ... holder.lastObservedDelta + recordEvent já gravados aqui (~L293-312)
```
O snapshot do tick é `const snap = s.snapshot` (~L173), e `skill`/`target` estão em escopo.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar src/memory/poi-detect.ts (recordResourcePoi + recordVillagePoi) com testes</name>
  <read_first>
    - src/memory/places.ts (assinatura de upsertPlace; dedup por bucket; NUNCA lança)
    - src/memory/places.test.ts (convenção: openDb(':memory:'), countPlaces, bun:test test/expect)
    - src/perception/types.ts (WorldSnapshot.entities/nearbyBlockTypes/status.position)
    - src/cognition/loop.ts (~L256-275 death handler — padrão `if (holder.db) upsertPlace(...)`)
  </read_first>
  <files>src/memory/poi-detect.ts, src/memory/poi-detect.test.ts</files>
  <behavior>
    recordResourcePoi(db, snap, target, outcome, ts):
    - Test 1: outcome 'success' + snap.nearbyBlockTypes[target].examples[0] presente
      → cria 1 POI type:'resource', notes:target, na posição do exemplo mais próximo. count(places)=1.
    - Test 2: outcome 'success' mas target SEM examples no snapshot
      → cria 1 POI type:'resource' na posição do bot (snap.status.position) como fallback. count=1.
    - Test 3: outcome 'no_effect' (ou 'partial'/'error') → NÃO cria POI. count(places)=0.
    - Test 4: outcome 'success' mas target vazio/desconhecido sem examples nem posição válida
      → degrada sem lançar (não cria lixo). (aceitável criar na posição do bot; o teste só exige não-throw.)
    - Test 5 (dedup): duas coletas success do mesmo recurso no mesmo bucket → 1 linha, visits=2.

    recordVillagePoi(db, snap, ts):
    - Test 6: snap.entities contém uma EntityInfo com name 'villager'
      → cria 1 POI type:'village' na posição do aldeão, notes:'villager'. count=1.
    - Test 7: snap.entities SEM aldeão (só 'zombie','player') → NÃO cria POI. count=0.
    - Test 8: 'zombie_villager' NÃO conta como village (é hostil) → NÃO cria POI.
    - Test 9 (idempotência por tick): chamar 2x com o mesmo aldeão no mesmo bucket → 1 linha, visits=2.
  </behavior>
  <action>
    Criar `src/memory/poi-detect.ts` com um cabeçalho documentando o GAP-01 e que landmark/base
    ficam FORA (sem gatilho barato) e lições ficam para Phase 14.

    Importar: `import type { Database } from 'bun:sqlite'`, `import type { WorldSnapshot } from '../perception/types'`,
    `import type { SkillOutcome } from '../grounding/types'` (o tipo do result.outcome — confirmar o caminho
    real grepando `export type SkillOutcome` em src/grounding/; se diferente, ajustar o import),
    `import { upsertPlace } from './places'`.

    export function recordResourcePoi(
      db: Database,
      snap: WorldSnapshot,
      target: string,
      outcome: SkillOutcome,
      ts: number,
    ): void {
      // SÓ em sucesso (D-09 B: só fato verificado vira memória espacial).
      if (outcome !== 'success') return
      // posição: exemplo mais próximo do bloco coletado (snapshot) OU posição do bot como fallback.
      const ex = target ? snap.nearbyBlockTypes[target]?.examples?.[0] : undefined
      const pos = ex ?? snap.status?.position
      if (!pos) return // sem posição válida — degrada (não lança, não cria lixo)
      upsertPlace(db, { x: pos.x, y: pos.y, z: pos.z, type: 'resource', notes: target || undefined }, ts)
    }

    export function recordVillagePoi(db: Database, snap: WorldSnapshot, ts: number): void {
      // Aldeão percebido → POI village no local dele. Dedup por bucket no upsertPlace evita flood.
      // Detecção: name === 'villager' (mineflayer entity name). EXCLUIR 'zombie_villager' (hostil).
      const villager = snap.entities?.find(
        (e) => e.name === 'villager' || e.type === 'villager',
      )
      if (!villager) return
      const p = villager.position
      if (!p) return
      upsertPlace(db, { x: p.x, y: p.y, z: p.z, type: 'village', notes: 'villager' }, ts)
    }

    Ambas as funções NUNCA lançam (upsertPlace já tem try/catch interno; os guards acima cobrem
    snapshot parcial). NÃO criar API nova de POI — só compor upsertPlace existente.

    Criar `src/memory/poi-detect.test.ts` seguindo a convenção de places.test.ts:
    - `import { openDb } from './persistence'`, `import { upsertPlace ... }` NÃO necessário — testar via
      recordResourcePoi/recordVillagePoi.
    - helper `memDb()` = openDb(':memory:'); `countPlaces(db)` como em places.test.ts.
    - helper `fakeSnap(overrides)` montando um WorldSnapshot mínimo: status.position {x,y,z},
      entities:[], nearbyBlockTypes:{}. Usar `as unknown as WorldSnapshot` se for mais simples que
      preencher todos os campos readonly.
    - Cobrir os 9 cases do bloco <behavior>.
  </action>
  <verify>
    <automated>bun test src/memory/poi-detect.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `export function recordResourcePoi` em src/memory/poi-detect.ts → 1 ocorrência
    - grep `export function recordVillagePoi` em src/memory/poi-detect.ts → 1 ocorrência
    - grep `upsertPlace` em src/memory/poi-detect.ts → reusa o helper (não reimplementa SQL)
    - grep `type: 'resource'` e `type: 'village'` em src/memory/poi-detect.ts → presentes
    - grep `INSERT INTO places` em src/memory/poi-detect.ts → 0 (não reimplementa o upsert)
    - `bun test src/memory/poi-detect.test.ts` → todos os testes passam (9 cases)
  </acceptance_criteria>
  <done>
    Coleta success → POI resource criado no local; falha → nada criado; aldeão no snapshot → POI village;
    dedup por bucket confirmado; nenhum case lança. Tudo via upsertPlace existente.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fiar recordResourcePoi (success da skill) + recordVillagePoi (snapshot) no nó execute</name>
  <read_first>
    - src/cognition/nodes.ts (~L172-344: nó execute; `snap = s.snapshot` L173; `success` L289;
      bloco try com holder.lastObservedDelta/recordEvent L293-312; imports no topo L1-29)
    - src/cognition/loop.ts (~L267: padrão `if (holder.db) upsertPlace(...)`)
    - src/cognition/deliberation.ts (~L168-186: nearbyPlacesString já injeta POIs no prompt — confirmar
      que NENHUMA mudança é necessária aqui; os novos POIs aparecem automaticamente)
  </read_first>
  <files>src/cognition/nodes.ts</files>
  <action>
    1. Import no topo de nodes.ts (junto aos outros imports de memory, ~L13):
       `import { recordResourcePoi, recordVillagePoi } from '../memory/poi-detect'`

    2. POI village — discreto, NÃO em loop quente: no nó execute, logo após `const snap = s.snapshot`
       (~L173), adicionar UMA chamada por tick guardada:
       ```typescript
       // GAP-01: aldeão percebido → POI village no local (dedup por bucket no upsertPlace evita flood).
       if (snap && holder.db) recordVillagePoi(holder.db, snap, now())
       ```
       Justificativa de performance: roda 1x por tick do execute (evento discreto), não por
       percepção contínua; o dedup por bucket colapsa repetições. NUNCA lança (helper degrada).

    3. POI resource — no ponto de SUCESSO da skill, dentro do bloco try, logo após
       `const success = result.outcome === 'success'` e os recordSuccess/recordFailure (~L289-291),
       OU logo após o recordEvent de success (~L312). Adicionar:
       ```typescript
       // GAP-01: coleta/mineração com SUCESSO → POI resource no local do recurso (replica o
       // padrão death→danger de loop.ts:267). Só em success (recordResourcePoi guarda o outcome).
       if (snap && holder.db) recordResourcePoi(holder.db, snap, target, result.outcome, now())
       ```
       OBS: `recordResourcePoi` já filtra `outcome !== 'success'` internamente — passar `result.outcome`
       é seguro e mantém a guarda em um lugar só. `target` aqui é o nome do bloco (ex.: 'oak_log')
       para dig/gather; para navigate o target é JSON e o snapshot não terá `nearbyBlockTypes[target]`,
       então cai no fallback de posição do bot — aceitável (navigate success raramente marca recurso,
       e o dedup por bucket impede poluição). Se quiser restringir a dig: envolver com
       `if (skill === 'dig') ...` — DISCRICIONÁRIO; o padrão mínimo acima é suficiente.

    4. NÃO tocar deliberation.ts — `nearbyPlacesString` (L170/186) já injeta os novos POIs no prompt.
       NÃO tocar loop.ts — o death→danger continua como está.
       NÃO adicionar landmark/base nem lições.

    Restrições: nenhuma das chamadas pode bloquear o tick nem lançar (helpers degradam graciosamente
    + guarda `if (holder.db)`); mínima mudança cirúrgica (3 linhas + 1 import).
  </action>
  <verify>
    <automated>bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep `import { recordResourcePoi, recordVillagePoi }` em src/cognition/nodes.ts → 1 ocorrência
    - grep `recordResourcePoi(holder.db, snap, target, result.outcome` em src/cognition/nodes.ts → presente
      no bloco try do nó execute (dentro do success path)
    - grep `recordVillagePoi(holder.db, snap` em src/cognition/nodes.ts → presente no início do execute
    - ambas as chamadas guardadas por `if (snap && holder.db)` (grep `if (snap && holder.db)` → ≥2)
    - `bunx tsc --noEmit` → exit 0, 0 erros
    - `bun test` → suíte inteira passa (sem regressão; 364+ testes)
    - deliberation.ts e loop.ts NÃO modificados (git diff só toca nodes.ts + poi-detect.*)
  </acceptance_criteria>
  <done>
    O nó execute cria POI resource em coleta bem-sucedida e POI village quando há aldeão no snapshot,
    reusando os helpers; type-check limpo, suíte sem regressão; POIs novos aparecem no prompt via
    nearbyPlacesString já fiado.
  </done>
</task>

</tasks>

<verification>
- `bun test src/memory/poi-detect.test.ts` — prova criação resource/village + não-criação em falha + dedup
- `bunx tsc --noEmit` — type-check limpo após a fiação
- `bun test` — suíte inteira sem regressão
- grep `recordResourcePoi` e `recordVillagePoi` em src/cognition/nodes.ts — call-sites fiados
- grep `INSERT INTO places` em src/memory/poi-detect.ts — 0 (reusa upsertPlace, sem API nova)
- (manual/ao vivo, opcional — Known Gap / gate Phase 14) coletar madeira e ver POI resource no
  `places` e a linha "POIs próximos:" com resource; aproximar-se de uma aldeia e ver POI village.
</verification>

<success_criteria>
- POI `resource` criado ao coletar/minerar com SUCESSO (outcome do SkillResult), no local do recurso.
- POI `village` criado ao perceber um aldeão no snapshot, no local do aldeão.
- Coleta sem sucesso NÃO cria POI resource.
- Dedup por bucket reusado (sem nova lógica); criação nunca bloqueia o tick nem lança.
- Novos POIs entram no prompt via nearbyPlacesString já fiado (sem mudar deliberation.ts).
- landmark/base e lições explicitamente FORA do escopo.
</success_criteria>

<output>
Após completar, criar `.planning/phases/08.1-refatorar-mem-ria-migrar-vetores-para-chromadb-j-rodando-local-consertar-grava-o-de-eventos-lugares-garantir-uso-real-pelo-llm-mem-ria-espacial-pois-e-registro-de-morte/08.1-07-SUMMARY.md`
</output>
