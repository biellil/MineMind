---
phase: 08.1-refatorar-memoria
plan: 05
subsystem: memory
tags: [memory, retrieval, embedding, prompt, langgraph, llm, cache]

# Dependency graph
requires:
  - phase: 08.1-04
    provides: "retrieve async (KNN no ChromaDB + scoring Generative Agents, chroma opcional com fallback de recência)"
  - phase: 08.1-01
    provides: "schema events + tabela base p/ persistEvent/retrieve"
provides:
  - "Caminho de AÇÃO recupera memórias (top-k=3) por query = embedding(currentGoal) cacheado e as injeta no prompt (seção 'Memórias relevantes:')"
  - "Cache do query embedding no holder (queryEmbedding/queryEmbeddingHash) — ~1 embed por troca de goal"
  - "Log [recall] por memória recuperada — torna o uso da memória verificável ao vivo"
affects: [08.1-06, phase-14-aprendizado, llm-prompt, deliberation]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Cache de embedding chaveado por hash do goal (djb2) — gasta embed por TROCA de goal, não por deliberação"
    - "Recall async no caminho de AÇÃO ANTES de decideAction (recupera → injeta no prompt → decide)"

key-files:
  created:
    - src/cognition/deliberation.recall.test.ts
  modified:
    - src/cognition/state.ts
    - src/llm/prompts.ts
    - src/cognition/deliberation.ts
    - src/cognition/deliberation.test.ts
    - src/cognition/reflection.integration.smoke.test.ts

key-decisions:
  - "D-11: query = embedding(currentGoal) cacheado por hash (djb2) do JSON do goal — degrada gracioso p/ null em falha de embed"
  - "D-12: seção 'Memórias relevantes:' renderizada após eventos recentes e ANTES do FATO VERIFICADO (que continua a última linha)"
  - "D-13: console.log [recall] por memória (id, score, summary truncado) p/ verificação ao vivo"
  - "truncate local (slice) em deliberation.ts em vez de exportar o helper não-exportado de prompts (menos acoplamento)"

patterns-established:
  - "Cache de query embedding por hash do goal — invariante de custo de embedding (1 embed por troca de goal)"
  - "Recall determinístico injetado no HumanMessage do caminho de AÇÃO antes de decideAction"

requirements-completed: []

# Metrics
duration: 7min
completed: 2026-06-21
---

# Phase 08.1 Plan 05: Uso Real da Memória pelo LLM Summary

**O caminho de AÇÃO agora recupera memórias (top-k=3) por embedding(currentGoal) cacheado por hash e as injeta no prompt como seção 'Memórias relevantes:', com log [recall] por memória — a correção central da fase (o LLM finalmente consome a memória recuperada).**

## Performance

- **Duration:** 7 min
- **Started:** 2026-06-21T15:01:03Z
- **Completed:** 2026-06-21T15:08:37Z
- **Tasks:** 2
- **Files modified:** 5 (1 criado, 4 modificados)

## Accomplishments
- `serializeContext` ganha o param opcional `recalled` e renderiza a seção `Memórias relevantes:` na posição correta (após eventos recentes, antes do FATO VERIFICADO) — D-12.
- Cache do query embedding no holder (`queryEmbedding`/`queryEmbeddingHash`) via `computeGoalQueryEmbedding` — re-embeda só quando o hash do goal muda (~1 embed por troca de goal) — D-11.
- Caminho de AÇÃO de `maybeDeliberate` faz `retrieve` top-k=3 com `query = embedding(currentGoal)`, loga `[recall]` por memória e passa `recalled` a `serializeContext` — D-13.
- Suíte verde: `bun test src/llm src/cognition` → 148 pass / 0 fail; `bunx tsc --noEmit` limpo.

## Task Commits

Cada task foi committada atomicamente:

1. **Task 1: Cache do query embedding + seção 'Memórias relevantes:' no prompt** - `b1419b3` (feat)
2. **Task 2 (TDD RED): teste falho de recuperação no caminho de AÇÃO** - `6c7928e` (test)
3. **Task 2 (TDD GREEN): recupera no caminho de AÇÃO + log [recall]** - `74bd636` (feat)

_Task 2 é TDD: commit de teste falho (RED) → commit de implementação (GREEN). Nenhum refactor necessário._

## Files Created/Modified
- `src/cognition/state.ts` - CognitiveStateHolder ganha `queryEmbedding: number[] | null` e `queryEmbeddingHash: string | null` (null no create).
- `src/llm/prompts.ts` - `serializeContext` aceita param opcional `recalled` e renderiza `Memórias relevantes:` antes do FATO VERIFICADO; JSDoc atualizado.
- `src/cognition/deliberation.ts` - `hashString` (djb2), `computeGoalQueryEmbedding` (cache D-11), `truncate` local; caminho de AÇÃO faz `retrieve` top-k=3 + log `[recall]` + passa `recalled` ao prompt.
- `src/cognition/deliberation.recall.test.ts` - **(novo)** 4 comportamentos: cache hit, troca de goal, goal null, log `[recall]`.
- `src/cognition/deliberation.test.ts` - ajuste de timing (drena microtasks) por conta do recall async.
- `src/cognition/reflection.integration.smoke.test.ts` - ajuste de timing (drena microtasks) por conta do recall async.

## Decisions Made
- **D-11 (cache por hash do goal):** `computeGoalQueryEmbedding` chaveia o cache pelo hash djb2 do `JSON.stringify(currentGoal)`; goal null → query embedding null (retrieve cai no fallback de recência); falha de embed → null sem lançar (Core Value preservado).
- **D-12 (posição da seção):** `Memórias relevantes:` entra depois dos eventos recentes e antes do FATO VERIFICADO, que permanece a última linha do prompt.
- **D-13 (log verificável):** `console.log('[recall] #id score=.. summary')` por memória recuperada.
- **truncate local em vez de exportar o de prompts:** evita acoplar `deliberation.ts` a um helper interno de `prompts.ts`; um slice local é trivial e desacoplado.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Ajuste de timing em 2 testes de single-flight pré-existentes**
- **Found during:** Task 2 (verificação `bun test src/llm src/cognition`)
- **Issue:** `deliberation.test.ts` ("single-flight concorrente...") e `reflection.integration.smoke.test.ts` (F — D-12) assertavam sobre `decideCalls`/`resolveDecide` num ponto de microtask que assumia que o caminho de AÇÃO chegava a `decideAction` de imediato. A recuperação async adicionada (`computeGoalQueryEmbedding` + `retrieve`) agora roda ANTES de `decideAction`, adicionando hops de microtask e desincronizando as asserções (resolveDecide undefined; actionDecideCalls=0).
- **Fix:** Adicionado drain de microtasks (`await Promise.resolve()`) antes das asserções dependentes de `decideAction`. A intenção dos testes (single-flight, sem sobreposição ação×reflexão) permanece intacta — só o ponto de observação foi ajustado.
- **Files modified:** src/cognition/deliberation.test.ts, src/cognition/reflection.integration.smoke.test.ts
- **Verification:** `bun test src/llm src/cognition` → 148 pass / 0 fail; confirmado via `git stash` que ambos passavam antes da mudança (causa = a nova ordem async correta).
- **Committed in:** `74bd636` (parte do commit GREEN da Task 2)

---

**Total deviations:** 1 auto-fixed (1 bug de timing de teste)
**Impact on plan:** Ajuste necessário e mínimo — preserva a intenção dos testes; nenhum scope creep. A reordenação (recall antes de decideAction) é o comportamento correto exigido pela fase.

## Issues Encountered
- Erro de tipo no mock `chat_command` do teste novo (`mode: 'assistant'` não é `ControlMode`) — corrigido para `'autonomous'` antes do commit GREEN.

## User Setup Required
None - nenhuma configuração de serviço externo necessária. (O recall usa o ChromaDB do Plan 04 quando online; offline cai no fallback de recência.)

## Next Phase Readiness
- Critério central da fase ("o LLM usa a memória") agora verificável ao vivo via log `[recall]` + seção `Memórias relevantes:` no HumanMessage.
- Verificação ao vivo (bot rodando com goal definido + LM Studio + Chroma) permanece como item HUMAN-UAT — alinhado ao Known Gap da Fase 4 (gate da Phase 14).
- Plan 06 (memória espacial/POIs + morte + lições) pode prosseguir.

## Self-Check: PASSED

- Arquivos verificados: state.ts, prompts.ts, deliberation.ts, deliberation.recall.test.ts, SUMMARY.md — todos presentes.
- Commits verificados: b1419b3, 6c7928e, 74bd636 — todos presentes.

---
*Phase: 08.1-refatorar-memoria*
*Completed: 2026-06-21*
