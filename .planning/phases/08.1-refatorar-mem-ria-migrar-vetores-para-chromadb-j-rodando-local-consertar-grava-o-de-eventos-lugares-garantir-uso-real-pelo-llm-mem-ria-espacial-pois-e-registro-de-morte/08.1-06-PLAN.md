---
phase: 08.1-refatorar-memoria
plan: 06
type: execute
wave: 4
depends_on: ["08.1-01", "08.1-02", "08.1-05"]
files_modified:
  - src/memory/places.ts
  - src/memory/lessons.ts
  - src/cognition/types.ts
  - src/memory/longTerm.ts
  - src/cognition/loop.ts
  - src/llm/prompts.ts
  - src/cognition/deliberation.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Quando o bot morre, um MemEvent type:'death' (importância 10) é gravado e um POI 'danger' é criado/atualizado no local"
    - "POIs são deduplicados por bucket espacial (mesma veia/abrigo colapsa num POI via ON CONFLICT)"
    - "O LLM recebe uma string 'POIs próximos:' com os POIs mais próximos por distância euclidiana"
    - "Existem helpers de lições (insert/reinforce/contradict/decay/topLessons) com reforço/decay aritmético"
  artifacts:
    - path: "src/memory/places.ts"
      provides: "upsertPlace (ON CONFLICT por bucket espacial) + nearbyPlaces (bounding-box) + nearbyPlacesString"
      exports: ["upsertPlace", "nearbyPlaces", "nearbyPlacesString"]
    - path: "src/memory/lessons.ts"
      provides: "insertLesson/reinforceLesson/contradictLesson/decayLessons/topLessons (D-19/D-20)"
      exports: ["insertLesson", "reinforceLesson", "contradictLesson", "decayLessons", "topLessons"]
    - path: "src/cognition/types.ts"
      provides: "MemEvent variante type:'death'"
      contains: "'death'"
  key_links:
    - from: "bot.on('death') (loop.ts)"
      to: "recordEvent (death) + upsertPlace (danger)"
      via: "inferência local da causa via buildReflexSensors + posição"
      pattern: "upsertPlace|type: 'danger'"
    - from: "caminho de AÇÃO (deliberation.ts)"
      to: "nearbyPlacesString → serializeContext"
      via: "POIs próximos injetados no prompt"
      pattern: "nearbyPlaces"
---

<objective>
Entregar a memória espacial (POIs), o registro de morte e a semente de lições (D-14..D-21):
- **POIs:** `places.type` (já migrado no Plan 01) com upsert dedup por bucket espacial (D-15) e busca por proximidade bounding-box + euclidiana (D-16), injetada no prompt como "POIs próximos:".
- **Morte:** evento `type:'death'` (importância forçada 10) com inferência local da causa (D-17) + upsert de POI `danger` no local (D-18). Hook em `bot.on('death')` que hoje só loga.
- **Lições:** tabela `lessons` (já migrada no Plan 01) com helpers de reforço/decay aritmético e consulta top-por-confiança (D-19/D-20). FRONTEIRA: só storage + reforço/decay + consulta — a GERAÇÃO de lições e o Beta-Bernoulli são da Phase 14.

Purpose: A morte é o ponto de convergência (D-21) — gera o danger POI e é a fonte grounded mais forte para uma lição futura. Os helpers seguem o padrão determinístico-sem-LLM já estabelecido (`importanceOf`/`summarizeEvent`). Depende do Plan 01 (schema), Plan 02 (recordEvent para o evento de morte) e Plan 05 (prompts.ts/deliberation.ts estáveis para injetar "POIs próximos:" sem conflito).
Output: `places.ts`, `lessons.ts` novos; `types.ts` (death); `longTerm.ts` (importanceOf/summarizeEvent death); `loop.ts` (death handler); `prompts.ts`/`deliberation.ts` (injeção de "POIs próximos:").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-refatorar-mem-ria-migrar-vetores-para-chromadb-j-rodando-local-consertar-grava-o-de-eventos-lugares-garantir-uso-real-pelo-llm-mem-ria-espacial-pois-e-registro-de-morte/08.1-CONTEXT.md
@.planning/phases/08.1-refatorar-mem-ria-migrar-vetores-para-chromadb-j-rodando-local-consertar-grava-o-de-eventos-lugares-garantir-uso-real-pelo-llm-mem-ria-espacial-pois-e-registro-de-morte/08.1-RESEARCH.md

<interfaces>
Do Plan 01 (persistence.ts):
```ts
export type PlaceType = 'base' | 'resource' | 'danger' | 'village' | 'landmark'
export interface LessonRow { id; text; confidence; reinforce_count; contradict_count; last_seen; created_at }
// places: key TEXT PK, label, x INT, y INT, z INT, type TEXT (nullable), first_seen, last_seen, visits, notes
// lessons: id INT PK, text, confidence REAL=0.5, reinforce_count=0, contradict_count=0, last_seen, created_at
// índice idx_places_xz em places(x,z)
```
Do Plan 02 (recordEvent.ts):
```ts
export function recordEvent(holder: CognitiveStateHolder, e: MemEvent, now: number, player?: string|null): void
```
MemEvent ATUAL (types.ts) — union discriminado por `type`: 'state_transition' | 'action' | 'world' | 'chat_command'. Adicionar 'death'.

`importanceOf(e)` / `summarizeEvent(e)` (longTerm.ts) — switch EXAUSTIVO por `e.type` (default=1 em importanceOf; summarizeEvent não tem default → precisa cobrir o novo caso).

`buildReflexSensors(bot)` (loop.ts) — já expõe `nearestHostile { name, distance }` e `isNight`. `bot.entity?.position` dá {x,y,z}.

`bot.on('death')` / `bot.on('respawn')` (loop.ts ~233-234) — hoje só `console.log`.

Caminho de AÇÃO (deliberation.ts, após Plan 05) já chama `serializeContext(snapshot, needs, goal, events, lastObservedDelta, recalled)`. `serializeContext` (prompts.ts) tem o helper `truncate` e a ordem Status→...→Memórias relevantes→FATO VERIFICADO.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MemEvent death + importanceOf/summarizeEvent + helpers de POIs (places.ts)</name>
  <files>src/cognition/types.ts, src/memory/longTerm.ts, src/memory/places.ts, src/memory/places.test.ts</files>
  <read_first>
    - src/cognition/types.ts (union MemEvent)
    - src/memory/longTerm.ts (importanceOf switch, summarizeEvent switch)
    - src/memory/persistence.ts (após Plan 01: PlaceType, schema places, idx_places_xz)
    - .planning/phases/08.1-.../08.1-RESEARCH.md (§ "Pattern 6: Upsert por bucket espacial", § "Pattern 7: Busca por proximidade", D-14/D-15/D-16/D-18)
  </read_first>
  <action>
    1. **types.ts** — adicionar a variante `death` ao union `MemEvent`:
       ```ts
       | { type: 'death'; cause: string; x: number; y: number; z: number; timestamp: number }
       ```
       (`cause` = string inferida; x/y/z = local da morte.)

    2. **longTerm.ts** — estender os switches (manter pt-BR):
       - `importanceOf`: adicionar `case 'death': return 10` (acima de damage=9 — D-18).
       - `summarizeEvent`: adicionar `case 'death': return \`Morri: ${e.cause} em (${Math.round(e.x)},${Math.round(e.y)},${Math.round(e.z)}).\`` (cobrir o novo caso para o switch continuar exaustivo).

    3. **Criar src/memory/places.ts** com (D-15/D-16):
       ```ts
       // src/memory/places.ts
       // D-14/D-15/D-16: memória espacial (POIs). Determinístico-sem-LLM, degrada gracioso (nunca lança).
       import type { Database } from 'bun:sqlite'
       import type { PlaceType } from './persistence'

       const GRID = 12  // bucket espacial em blocos (D-15 discricionário 8-16). Colapsa "a mesma veia/abrigo".
       const SEARCH_RADIUS = 64  // raio da busca por proximidade em blocos (D-16 discricionário)

       export interface PlaceRow {
         key: string; label: string | null; type: PlaceType | null
         x: number; y: number; z: number; visits: number; notes: string | null
       }

       /** Deriva a chave PK por bucket espacial + tipo (D-15): mesma região+tipo → mesmo POI. */
       function bucketKey(x: number, y: number, z: number, type: PlaceType): string {
         const bx = Math.round(x / GRID), by = Math.round(y / GRID), bz = Math.round(z / GRID)
         return `${bx}:${by}:${bz}:${type}`
       }

       /** Upsert de POI com dedup por bucket (D-15). ON CONFLICT incrementa visits + atualiza last_seen. */
       export function upsertPlace(
         db: Database,
         poi: { x: number; y: number; z: number; type: PlaceType; label?: string; notes?: string },
         now: number,
       ): void {
         try {
           const key = bucketKey(poi.x, poi.y, poi.z, poi.type)
           db.prepare(`
             INSERT INTO places (key, label, x, y, z, type, first_seen, last_seen, visits, notes)
             VALUES (?, ?, ?, ?, ?, ?, ?, ?, 1, ?)
             ON CONFLICT(key) DO UPDATE SET visits = visits + 1, last_seen = excluded.last_seen
           `).run(key, poi.label ?? null, Math.round(poi.x), Math.round(poi.y), Math.round(poi.z), poi.type, now, now, poi.notes ?? null)
         } catch (err) {
           console.error('[places] upsertPlace falhou (degradando):', err instanceof Error ? err.message : err)
         }
       }

       /** Busca por proximidade: bounding-box (índice x,z) + ordenação euclidiana top-N (D-16). */
       export function nearbyPlaces(
         db: Database, px: number, py: number, pz: number, n: number,
       ): Array<PlaceRow & { dist: number }> {
         try {
           const rows = db.prepare(`
             SELECT key, label, type, x, y, z, visits, notes FROM places
             WHERE x BETWEEN ? AND ? AND z BETWEEN ? AND ?
           `).all(px - SEARCH_RADIUS, px + SEARCH_RADIUS, pz - SEARCH_RADIUS, pz + SEARCH_RADIUS) as PlaceRow[]
           return rows
             .map((r) => ({ ...r, dist: Math.hypot(r.x - px, r.y - py, r.z - pz) }))
             .sort((a, b) => a.dist - b.dist)
             .slice(0, n)
         } catch (err) {
           console.error('[places] nearbyPlaces falhou (degradando):', err instanceof Error ? err.message : err)
           return []
         }
       }

       /** Render para o prompt (D-16): "POIs próximos: base (12m), veia de ferro (40m)". '' se vazio. */
       export function nearbyPlacesString(db: Database, px: number, py: number, pz: number, n = 3): string {
         const near = nearbyPlaces(db, px, py, pz, n)
         if (near.length === 0) return ''
         const items = near.map((p) => `${p.label ?? p.type ?? 'lugar'} (${Math.round(p.dist)}m)`)
         return `POIs próximos: ${items.join(', ')}`
       }
       ```
       Ajustar o `label` default e o formato textual livremente (D-16 é discricionário no formato).

    4. Criar `src/memory/places.test.ts` (DB :memory:, schema via openDb): testar (a) upsert insere e o segundo upsert no MESMO bucket incrementa visits (não cria 2 linhas); (b) buckets diferentes → 2 linhas; (c) nearbyPlaces ordena por distância e respeita o bounding-box (um POI fora do raio não aparece); (d) nearbyPlacesString retorna '' sem POIs.
  </action>
  <verify>
    <automated>bun test src/memory/places.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `types.ts` MemEvent contém a variante `{ type: 'death'; cause: string; x; y; z; timestamp }`
    - `longTerm.ts` importanceOf contém `case 'death': return 10` e summarizeEvent cobre `case 'death'`
    - `places.ts` exporta `upsertPlace`, `nearbyPlaces`, `nearbyPlacesString` e usa `ON CONFLICT(key) DO UPDATE SET visits = visits + 1`
    - `places.ts` usa bounding-box `WHERE x BETWEEN ? AND ? AND z BETWEEN ?` + `Math.hypot` para a ordenação
    - `places.test.ts` cobre dedup por bucket, bounding-box e render; `bun test src/memory/places.test.ts` sai com código 0
    - `bunx tsc --noEmit` sem erro (switch exaustivo de summarizeEvent cobre 'death')
  </acceptance_criteria>
  <done>
    A morte é modelável (importância 10), os POIs deduplicam por bucket e a busca por proximidade funciona; testes verdes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Helpers de lições (lessons.ts) — reforço/decay aritmético + consulta</name>
  <files>src/memory/lessons.ts, src/memory/lessons.test.ts</files>
  <read_first>
    - src/memory/persistence.ts (após Plan 01: tabela lessons, LessonRow)
    - .planning/phases/08.1-.../08.1-RESEARCH.md (§ "Lessons: reforço/decay aritmético (D-20)", D-19/D-20 — FRONTEIRA: só storage)
  </read_first>
  <action>
    Criar `src/memory/lessons.ts` com os helpers expostos como GANCHO para a Phase 14 (NÃO gerar lições aqui — só storage/reforço/decay/consulta). Constantes discricionárias (D-20). Tudo degrada gracioso (try/catch, nunca lança).

    ```ts
    // src/memory/lessons.ts
    // D-19/D-20: conhecimento durável evolutivo (lições). FRONTEIRA DESTA FASE: só storage + reforço/decay
    // + consulta. A GERAÇÃO de lições (LLM) e o modelo Beta-Bernoulli (α/β) são da Phase 14 — sobre esta
    // mesma tabela. Reforço/decay ARITMÉTICO simples. Determinístico-sem-LLM, nunca lança.
    import type { Database } from 'bun:sqlite'
    import type { LessonRow } from './persistence'

    const REINFORCE_STEP = 0.1     // confiança sobe ao confirmar (D-20 discricionário)
    const CONTRADICT_STEP = 0.2    // confiança desce ao contradizer (mais forte que o reforço)
    const DECAY_STEP = 0.02        // decaimento temporal por aplicação de decay (boot/heartbeat)
    const REMOVAL_THRESHOLD = 0.1  // abaixo disto a lição é descartada da consulta/poda

    /** Insere uma lição nova (texto + confiança inicial). Retorna o id, ou null em falha. */
    export function insertLesson(db: Database, text: string, now: number, confidence = 0.5): number | null {
      try {
        const res = db.prepare(
          `INSERT INTO lessons (text, confidence, reinforce_count, contradict_count, last_seen, created_at)
           VALUES (?, ?, 0, 0, ?, ?)`
        ).run(text, confidence, now, now)
        return Number(res.lastInsertRowid)
      } catch (err) {
        console.error('[lessons] insertLesson falhou:', err instanceof Error ? err.message : err)
        return null
      }
    }

    /** Reforça: confidence += REINFORCE_STEP (cap 1), reinforce_count++, last_seen=now. */
    export function reinforceLesson(db: Database, id: number, now: number): void {
      try {
        db.prepare(
          `UPDATE lessons SET confidence = MIN(1.0, confidence + ?), reinforce_count = reinforce_count + 1, last_seen = ? WHERE id = ?`
        ).run(REINFORCE_STEP, now, id)
      } catch (err) { console.error('[lessons] reinforceLesson falhou:', err instanceof Error ? err.message : err) }
    }

    /** Contradiz: confidence -= CONTRADICT_STEP (piso 0), contradict_count++, last_seen=now. */
    export function contradictLesson(db: Database, id: number, now: number): void {
      try {
        db.prepare(
          `UPDATE lessons SET confidence = MAX(0.0, confidence - ?), contradict_count = contradict_count + 1, last_seen = ? WHERE id = ?`
        ).run(CONTRADICT_STEP, now, id)
      } catch (err) { console.error('[lessons] contradictLesson falhou:', err instanceof Error ? err.message : err) }
    }

    /** Decaimento temporal: confidence -= DECAY_STEP (piso 0). Chamar no boot/heartbeat, NÃO no tick. */
    export function decayLessons(db: Database): void {
      try {
        db.prepare(`UPDATE lessons SET confidence = MAX(0.0, confidence - ?)`).run(DECAY_STEP)
      } catch (err) { console.error('[lessons] decayLessons falhou:', err instanceof Error ? err.message : err) }
    }

    /** Top-N lições por confiança (>= REMOVAL_THRESHOLD). Gancho de consulta para o LLM/Phase 14. */
    export function topLessons(db: Database, n: number): LessonRow[] {
      try {
        return db.prepare(
          `SELECT * FROM lessons WHERE confidence >= ? ORDER BY confidence DESC LIMIT ?`
        ).all(REMOVAL_THRESHOLD, n) as LessonRow[]
      } catch (err) {
        console.error('[lessons] topLessons falhou:', err instanceof Error ? err.message : err)
        return []
      }
    }
    ```

    Criar `src/memory/lessons.test.ts` (DB :memory: via openDb): testar (a) insert→topLessons retorna a lição; (b) reinforce sobe confidence e incrementa reinforce_count (cap em 1.0 após muitos reforços); (c) contradict desce confidence (piso 0.0) e incrementa contradict_count; (d) decay baixa confidence; (e) topLessons exclui lições abaixo de REMOVAL_THRESHOLD.
  </action>
  <verify>
    <automated>bun test src/memory/lessons.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lessons.ts` exporta `insertLesson`, `reinforceLesson`, `contradictLesson`, `decayLessons`, `topLessons`
    - reforço usa `MIN(1.0, confidence + ?)` e decay/contradict usam `MAX(0.0, confidence - ?)` (clamps)
    - `topLessons` filtra por `confidence >= ?` e ordena `ORDER BY confidence DESC`
    - Nenhum helper lança (todos sob try/catch)
    - `lessons.test.ts` cobre insert/reinforce/contradict/decay/top + threshold; `bun test src/memory/lessons.test.ts` sai com código 0
    - NÃO há geração de lições nem Beta-Bernoulli (apenas storage/reforço/decay/consulta — FRONTEIRA D-20)
  </acceptance_criteria>
  <done>
    Os helpers de lições existem como gancho durável; reforço/decay aritmético com clamps; consulta top-por-confiança; testes verdes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Death handler (loop.ts) + injeção de "POIs próximos:" no prompt</name>
  <files>src/cognition/loop.ts, src/llm/prompts.ts, src/cognition/deliberation.ts, src/memory/places.ts</files>
  <read_first>
    - src/cognition/loop.ts (bot.on('death') ~233; buildReflexSensors ~82; createChromaClient/holder no startCognitiveLoop)
    - src/memory/recordEvent.ts (recordEvent — Plan 02)
    - src/memory/places.ts (upsertPlace, nearbyPlacesString — Task 1)
    - src/cognition/deliberation.ts (caminho de AÇÃO após Plan 05 — onde recalled é montado e serializeContext chamado)
    - src/llm/prompts.ts (serializeContext após Plan 05 — param recalled, ordem das linhas)
    - .planning/phases/08.1-.../08.1-RESEARCH.md (§ "MemEvent death + danger POI", D-17/D-18/D-21)
  </read_first>
  <action>
    1. **Death handler (loop.ts) — D-17/D-18.** Substituir o `bot.on('death', () => console.log('[loop] bot morreu — aguardando respawn'))` por um handler que infere a causa localmente e grava evento+POI (MANTER o log existente como parte do handler):
       ```ts
       bot.on('death', () => {
         console.log('[loop] bot morreu — aguardando respawn')
         try {
           const s = buildReflexSensors(bot)  // já expõe nearestHostile { name, distance } + isNight
           const pos = bot.entity?.position
           const cause = s.nearestHostile
             ? `morto perto de ${s.nearestHostile.name} a ${Math.round(s.nearestHostile.distance)}m${s.isNight ? ', à noite' : ''}`
             : 'morto por causa ambiental/desconhecida'
           const ts = Date.now()
           if (pos) {
             recordEvent(holder, { type: 'death', cause, x: pos.x, y: pos.y, z: pos.z, timestamp: ts }, ts)  // importância 10
             if (holder.db) upsertPlace(holder.db, { x: pos.x, y: pos.y, z: pos.z, type: 'danger', notes: cause }, ts)  // D-21: morte→danger POI
           } else {
             recordEvent(holder, { type: 'death', cause, x: 0, y: 0, z: 0, timestamp: ts }, ts)
           }
           console.log(`[death] ${cause}`)
         } catch (err) {
           console.error('[death] handler falhou (degradando):', err instanceof Error ? err.message : err)
         }
       })
       ```
       Imports em loop.ts: `import { upsertPlace } from '../memory/places'` (recordEvent já importado no Plan 02). Confirmar a forma exata de `buildReflexSensors` (campos `nearestHostile`/`isNight`) antes de usar — ajustar os nomes dos campos ao que a função realmente retorna.

       NOTA: o handler `death` precisa do `holder` em escopo — ele é parâmetro de `startCognitiveLoop(bot, holder)`, então está disponível. Manter o `bot.on('respawn', ...)` como está.

    2. **decayLessons no boot (D-20):** no `startCognitiveLoop`, chamar `if (holder.db) decayLessons(holder.db)` uma vez no boot (ou no `flushTimer`/heartbeat — NÃO no tick). Import `import { decayLessons } from '../memory/lessons'`. Isso exercita o decay sem custo no caminho quente.

    3. **Injeção de "POIs próximos:" no prompt (D-16).** No caminho de AÇÃO de `maybeDeliberate` (deliberation.ts, onde `recalled` já é montado pelo Plan 05), computar a string de POIs e passá-la ao `serializeContext`:
       - Em `prompts.ts` `serializeContext`: adicionar um parâmetro opcional `poisLine?: string` ao final (após `recalled`). Renderizar `if (poisLine) lines.push(poisLine)` LOGO APÓS o bloco "Memórias relevantes:" e ANTES do `FATO VERIFICADO` (mundo espacial agrupado com as memórias; FATO VERIFICADO continua último).
       - Em `deliberation.ts` (caminho de AÇÃO): após montar `recalled`, computar:
         ```ts
         const pos = snapshot?.status?.position
         const poisLine = (holder.db && pos) ? nearbyPlacesString(holder.db, pos.x, pos.y, pos.z, 3) : ''
         ```
         e passar `poisLine || undefined` como novo último argumento de `serializeContext(... , recalled..., poisLine || undefined)`. Import `import { nearbyPlacesString } from '../memory/places'`.

    4. Atualizar/garantir testes: os testes de `serializeContext` continuam válidos (novo param opcional). Adicionar um caso em `prompts.test.ts` (ou places.test.ts) que assere que, passando `poisLine`, a string `POIs próximos:` aparece no output e ANTES do FATO VERIFICADO.
  </action>
  <verify>
    <automated>bun test src/cognition src/llm src/memory</automated>
  </verify>
  <acceptance_criteria>
    - `loop.ts` bot.on('death') chama `recordEvent(holder, { type: 'death'` e `upsertPlace(holder.db, { ... type: 'danger'`
    - `loop.ts` importa `upsertPlace` e chama `decayLessons` no boot/heartbeat (não no tick)
    - `prompts.ts` serializeContext aceita `poisLine?` e o renderiza antes do FATO VERIFICADO
    - `deliberation.ts` caminho de AÇÃO computa `nearbyPlacesString(...)` e passa ao serializeContext
    - Um teste assere que `POIs próximos:` aparece no prompt e precede `FATO VERIFICADO`
    - `bun test src/cognition src/llm src/memory` sai com código 0
    - `bunx tsc --noEmit` sem erro
  </acceptance_criteria>
  <done>
    A morte grava evento+danger POI com causa inferida; o LLM recebe "POIs próximos:"; decay de lições roda fora do tick; testes verdes.
  </done>
</task>

</tasks>

<verification>
- `bun test src/memory src/cognition src/llm` passa.
- `bunx tsc --noEmit` sem erro.
- `grep "type: 'death'" src/cognition/types.ts src/cognition/loop.ts` e `grep "type: 'danger'" src/cognition/loop.ts` retornam match.
- `grep "ON CONFLICT(key)" src/memory/places.ts`, `grep "ORDER BY confidence DESC" src/memory/lessons.ts`, `grep "POIs próximos:" src/memory/places.ts` retornam match.
- (Ao vivo, manual): matar o bot → log `[death]` + uma linha em `places` com `type='danger'` + um evento `type='death'` em `events`.
</verification>

<success_criteria>
- Morte registrada (importância 10) + danger POI no local com causa inferida localmente (D-17/D-18/D-21).
- POIs deduplicados por bucket espacial e buscáveis por proximidade; "POIs próximos:" no prompt (D-14/D-15/D-16).
- Helpers de lições duráveis (reforço/decay/consulta) como gancho para a Phase 14 (D-19/D-20, dentro da FRONTEIRA).
</success_criteria>

<output>
Após completar, criar `.planning/phases/08.1-refatorar-mem-ria-migrar-vetores-para-chromadb-j-rodando-local-consertar-grava-o-de-eventos-lugares-garantir-uso-real-pelo-llm-mem-ria-espacial-pois-e-registro-de-morte/08.1-06-SUMMARY.md`
</output>
