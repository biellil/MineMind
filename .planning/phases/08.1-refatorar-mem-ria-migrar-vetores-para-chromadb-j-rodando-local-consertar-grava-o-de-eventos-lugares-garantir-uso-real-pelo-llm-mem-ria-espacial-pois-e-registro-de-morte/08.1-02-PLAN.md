---
phase: 08.1-refatorar-memoria
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memory/recordEvent.ts
  - src/cognition/loop.ts
  - src/cognition/nodes.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Após uma sessão de loop, SELECT count(*) FROM events é > 0 (a persistência deixa de estar morta)"
    - "Eventos de ação (execute), anti-repeat, erro de skill e reflexo idle são gravados em LP"
    - "Gravar um evento nunca bloqueia o tick nem chama o LLM (embedding=null no push)"
  artifacts:
    - path: "src/memory/recordEvent.ts"
      provides: "Helper central recordEvent(holder, e, now) = push CP + persistEvent LP (embedding null)"
      exports: ["recordEvent"]
    - path: "src/cognition/nodes.ts"
      provides: "3 pontos de origem do execute usam recordEvent (linhas ~238/302/322)"
      contains: "recordEvent("
    - path: "src/cognition/loop.ts"
      provides: "reflexo idle usa recordEvent (linha ~125)"
      contains: "recordEvent("
  key_links:
    - from: "recordEvent"
      to: "persistEvent (longTerm.ts)"
      via: "chamada com embedding=null quando holder.db existe"
      pattern: "persistEvent\\(.*null"
    - from: "nodes.ts/loop.ts pontos de origem"
      to: "recordEvent"
      via: "substituição dos push(holder.memory, ...)"
      pattern: "recordEvent\\(holder"
---

<objective>
Consertar o bug de fiação central da fase (D-06, D-08): `persistEvent` (longTerm.ts) NUNCA é chamado em produção — só em testes. Os 4 pontos de origem reais do loop só fazem `push(holder.memory, …)` (CP), nunca persistem em LP. Resultado ao vivo: `events=0`. A correção é um helper `recordEvent(holder, e, now)` = `push` (CP) + `persistEvent(db, e, null, now)` (LP, embedding **null**), plugado em cada ponto de origem.

Purpose: É a maior alavanca da fase e independe do ChromaDB. Mata `events=0` imediatamente. `recordEvent` grava embedding **null** (não chama o LLM, não toca o tick rápido — D-07); o embedding semântico continua vindo só do batch de reflexão (Plan 04). `retrieve` já degrada gracioso para recência quando o evento não tem embedding.
Output: `src/memory/recordEvent.ts` novo + substituição dos 4 `push(holder.memory, …)` por `recordEvent(holder, …)` em `loop.ts` (1) e `nodes.ts` (3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-refatorar-mem-ria-migrar-vetores-para-chromadb-j-rodando-local-consertar-grava-o-de-eventos-lugares-garantir-uso-real-pelo-llm-mem-ria-espacial-pois-e-registro-de-morte/08.1-CONTEXT.md
@.planning/phases/08.1-refatorar-mem-ria-migrar-vetores-para-chromadb-j-rodando-local-consertar-grava-o-de-eventos-lugares-garantir-uso-real-pelo-llm-mem-ria-espacial-pois-e-registro-de-morte/08.1-RESEARCH.md

<interfaces>
Assinaturas existentes (NÃO alterar):
```ts
// src/memory/longTerm.ts
export function persistEvent(
  db: Database, e: MemEvent, embedding: number[] | null, now: number, player?: string | null,
): number | null   // retorna null se importance < config.ltImportanceFloor

// src/memory/shortTerm.ts
export function push(memory: ShortTermMemory, e: MemEvent): ShortTermMemory  // ring buffer imutável

// src/cognition/state.ts
interface CognitiveStateHolder { memory: ShortTermMemory; db: Database | null; ... }
```

Pontos de origem CONFIRMADOS por grep (substituir cada `push(holder.memory, {...})` por `recordEvent(holder, {...}, now)`):
- `src/cognition/loop.ts:125` — dentro de `runReflex` (reflexo idle). Tem `nowTs` em escopo (usar como `now`).
- `src/cognition/nodes.ts:238` — execute, ramo anti-repeat/abandon (`shouldAbandon`). Tem `now()` helper em escopo.
- `src/cognition/nodes.ts:302` — execute, ramo de sucesso/grounded da skill. Tem `now()`.
- `src/cognition/nodes.ts:322` — execute, ramo catch (erro inesperado). Tem `now()`.

Os três pontos de nodes.ts estão no nó `execute`, que tem `holder` (logo `holder.db`) no closure (MakeNodesArgs.holder). loop.ts:125 está em `runReflex(bot, holder, reflex, lastReflexAt)` — `holder` no parâmetro.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar o helper recordEvent (push CP + persistEvent LP, embedding null)</name>
  <files>src/memory/recordEvent.ts, src/memory/recordEvent.test.ts</files>
  <read_first>
    - src/memory/longTerm.ts (assinatura de persistEvent, importanceOf, floor)
    - src/memory/shortTerm.ts (assinatura de push, ShortTermMemory)
    - src/cognition/state.ts (CognitiveStateHolder: memory reatribuível, db nullable)
    - .planning/phases/08.1-.../08.1-RESEARCH.md (§ "D-06" e § "Pitfall 3")
  </read_first>
  <behavior>
    - Test 1: recordEvent com holder.db válido → empurra o evento na memória (CP) E chama persistEvent com embedding null; após a chamada, SELECT count(*) FROM events reflete o evento (se importance >= floor).
    - Test 2: recordEvent com holder.db === null → ainda empurra na memória (CP) e NÃO lança (degradação graciosa).
    - Test 3: recordEvent reatribui holder.memory (ring buffer imutável) — holder.memory após a chamada contém o novo evento (getEvents inclui o último).
    - Test 4: recordEvent NUNCA lança mesmo se persistEvent falhar (wrap try/catch interno) — o tick nunca aborta (Core Value).
  </behavior>
  <action>
    Criar `src/memory/recordEvent.ts`:

    ```ts
    // src/memory/recordEvent.ts
    // D-06/D-08: helper central de gravação de eventos. Substitui os push(holder.memory, …) espalhados
    // nos pontos de origem do loop (loop.ts:125, nodes.ts:238/302/322). Faz DUAS coisas:
    //  - push na memória de curto prazo (CP) — comportamento que já existia;
    //  - persistEvent na memória de longo prazo (LP) com embedding NULL — o que estava FALTANDO (bug
    //    de fiação: persistEvent só era chamado em testes → events=0 ao vivo).
    // O embedding é SEMPRE null aqui: NÃO embeddar no caminho quente (D-07 — o único embed é o batch
    // da reflexão). retrieve já degrada gracioso para recência quando o evento não tem embedding.
    // NUNCA lança (Core Value: o loop nunca aborta) — persistEvent é envolto em try/catch.
    import type { MemEvent } from '../cognition/types'
    import type { CognitiveStateHolder } from '../cognition/state'
    import { push } from './shortTerm'
    import { persistEvent } from './longTerm'

    export function recordEvent(
      holder: CognitiveStateHolder,
      e: MemEvent,
      now: number,
      player?: string | null,
    ): void {
      // CP: ring buffer imutável — reatribui (o holder é a fonte única).
      holder.memory = push(holder.memory, e)
      // LP: embedding null (D-06). Degradação graciosa — nunca propaga.
      if (holder.db) {
        try {
          persistEvent(holder.db, e, null, now, player)
        } catch (err) {
          console.error('[recordEvent] persistEvent falhou (degradando):', err instanceof Error ? err.message : err)
        }
      }
    }
    ```

    Criar `src/memory/recordEvent.test.ts` cobrindo os 4 comportamentos do bloco <behavior>. Usar um holder mínimo de teste (criar via `createCognitiveStateHolder()` e setar `holder.db = openDb(':memory:')` para o Test 1; `holder.db = null` para o Test 2). Para o Test 1 usar um evento com importance >= floor (ex.: `{ type:'world', event:'damage', detail:'x', timestamp: now }` → importanceOf=9). Assertar via `db.prepare('SELECT count(*) AS n FROM events').get()`.
  </action>
  <verify>
    <automated>bun test src/memory/recordEvent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/memory/recordEvent.ts` existe e exporta `recordEvent`
    - A assinatura é `recordEvent(holder: CognitiveStateHolder, e: MemEvent, now: number, player?: string | null): void`
    - `recordEvent.ts` chama `persistEvent(holder.db, e, null, now, player)` (embedding literal `null`) dentro de um `try/catch`
    - `recordEvent.ts` reatribui `holder.memory = push(holder.memory, e)`
    - `src/memory/recordEvent.test.ts` cobre os 4 comportamentos e `bun test src/memory/recordEvent.test.ts` sai com código 0
  </acceptance_criteria>
  <done>
    O helper grava em CP+LP, nunca lança e os 4 testes passam.
  </done>
</task>

<task type="auto">
  <name>Task 2: Plugar recordEvent nos 4 pontos de origem (loop.ts + nodes.ts)</name>
  <files>src/cognition/loop.ts, src/cognition/nodes.ts, src/memory/recordEvent.ts</files>
  <read_first>
    - src/cognition/nodes.ts (linhas ~228-340: os 3 push do nó execute)
    - src/cognition/loop.ts (linhas ~110-141: runReflex e o push:125)
    - src/memory/recordEvent.ts (helper criado na Task 1)
  </read_first>
  <action>
    Substituir cada um dos 4 `holder.memory = push(holder.memory, { ... })` por `recordEvent(holder, { ... }, <now>)`, mantendo o objeto MemEvent EXATAMENTE igual (mesmos campos). O `recordEvent` já faz o `push` internamente, então a linha `holder.memory = push(...)` some.

    1. **src/cognition/nodes.ts** — adicionar o import no topo: `import { recordEvent } from '../memory/recordEvent'`.
       - Linha ~238 (ramo anti-repeat): trocar
         ```ts
         holder.memory = push(holder.memory, { type:'action', skill, target, outcome:'no_effect', observed:0, expected:0, result:'failure', reason:'anti-repeat', timestamp: now() })
         ```
         por
         ```ts
         recordEvent(holder, { type:'action', skill, target, outcome:'no_effect', observed:0, expected:0, result:'failure', reason:'anti-repeat', timestamp: now() }, now())
         ```
       - Linha ~302 (ramo de sucesso grounded): trocar o `holder.memory = push(holder.memory, { type:'action', ...result... })` por `recordEvent(holder, { ...mesmo objeto... }, now())`.
       - Linha ~322 (ramo catch/erro): trocar o `holder.memory = push(holder.memory, { type:'action', ..., outcome:'error', ... })` por `recordEvent(holder, { ...mesmo objeto... }, now())`.
       - NÃO remover/alterar o `holder.lastObservedDelta = {...}` que precede os pushes de 302/322 — só o push vira recordEvent.
       - Se após a troca o import `push` ficar sem uso em nodes.ts, removê-lo do import para não quebrar o lint (`grep "push(" src/cognition/nodes.ts` para confirmar antes de remover).

    2. **src/cognition/loop.ts** — adicionar `import { recordEvent } from '../memory/recordEvent'` no topo.
       - Linha ~125 (dentro de `runReflex`): trocar
         ```ts
         holder.memory = push(holder.memory, { type:'action', skill: reflex, target:'reflex', outcome: result.outcome, observed: result.observed, expected: result.expected, result: result.outcome === 'success' ? 'success' : 'failure', reason: result.reason, timestamp: nowTs })
         ```
         por
         ```ts
         recordEvent(holder, { type:'action', skill: reflex, target:'reflex', outcome: result.outcome, observed: result.observed, expected: result.expected, result: result.outcome === 'success' ? 'success' : 'failure', reason: result.reason, timestamp: nowTs }, nowTs)
         ```
       - Manter o `console.log([reflex] ...)` e a guarda de debounce `lastReflexAt` exatamente como estão.
       - Se `push` ficar sem uso em loop.ts após a troca, removê-lo do import (confirmar com grep antes).

    3. Rodar a suíte completa para garantir que nenhum consumidor de `holder.memory` quebrou (os testes de smoke já fazem push manualmente e seguem válidos).
  </action>
  <verify>
    <automated>bun test src/cognition</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recordEvent(holder" src/cognition/nodes.ts` retorna 3
    - `grep -c "recordEvent(holder" src/cognition/loop.ts` retorna >= 1
    - `grep "holder.memory = push(holder.memory" src/cognition/nodes.ts` retorna 0 matches (todos os 3 pushes de evento foram migrados)
    - nodes.ts e loop.ts contêm `import { recordEvent } from '../memory/recordEvent'`
    - `bunx tsc --noEmit` não reporta erro novo (incluindo "push declarado e não usado" — remover o import se necessário)
    - `bun test src/cognition` sai com código 0
  </acceptance_criteria>
  <done>
    Os 4 pontos de origem persistem eventos em LP via recordEvent; nenhum `push(holder.memory)` de evento de origem permanece; a suíte cognition passa.
  </done>
</task>

</tasks>

<verification>
- `bun test src/memory src/cognition` passa.
- `grep "recordEvent(holder"` aparece nos 4 pontos (3 em nodes.ts, ≥1 em loop.ts).
- `bunx tsc --noEmit` sem novos erros.
- (Verificação ao vivo, manual fora do escopo automatizado): após uma sessão, `SELECT count(*) FROM events` em `minemind.sqlite` > 0.
</verification>

<success_criteria>
- O bug de fiação está morto: persistEvent é chamado em produção via recordEvent nos 4 pontos de origem.
- Gravar evento nunca chama o LLM nem bloqueia o tick (embedding null).
- Degradação graciosa preservada (holder.db null ou falha de persistEvent não derruba o loop).
</success_criteria>

<output>
Após completar, criar `.planning/phases/08.1-refatorar-mem-ria-migrar-vetores-para-chromadb-j-rodando-local-consertar-grava-o-de-eventos-lugares-garantir-uso-real-pelo-llm-mem-ria-espacial-pois-e-registro-de-morte/08.1-02-SUMMARY.md`
</output>
