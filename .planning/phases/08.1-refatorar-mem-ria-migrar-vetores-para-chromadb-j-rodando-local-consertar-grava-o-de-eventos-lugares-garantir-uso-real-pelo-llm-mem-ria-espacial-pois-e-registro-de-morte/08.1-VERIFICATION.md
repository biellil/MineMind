---
phase: 08.1-refatorar-memoria
verified: 2026-06-21T00:00:00Z
status: gaps_found
score: 24/24 must-have truths verified (automated); live testing PASSED core loop; 1 wiring gap found (POIs)
re_verification:
  previous_status: human_needed
  note: live testing 2026-06-21 — core memory loop CONFIRMED (events/reflection/Chroma/recall/death all live); discovered POI creation gap (only 'danger' wired)
human_verification:
  - test: "Rodar o bot com LM Studio + servidor Chroma online por uma sessão e checar SELECT count(*) FROM events > 0"
    expected: "events > 0 (a persistência deixou de estar morta); logs [recall] aparecem no caminho de AÇÃO"
    why_human: "Exige loop ao vivo com LLM e Chroma; out-of-scope automatizado (gate da Phase 14 / Known Gap Phase 4)"
  - test: "Com Chroma ONLINE, observar o log '[reflect] consolidado #<id> (vetor enviado ao chroma...)' e depois '[recall] #<id> score=...' usando memória recuperada"
    expected: "Vetor consolidado gravado no Chroma e KNN recupera memórias reais injetadas no prompt"
    why_human: "Integração com servidor Chroma + embeddings LM Studio reais não roda no CI"
  - test: "Derrubar o Chroma a meio de uma sessão e confirmar que o loop não aborta, emite '[chroma] OFFLINE' debounced e cai no fallback de recência"
    expected: "Loop segue; aviso debounced; retrieve usa recência; relacional intacto; religa sozinho quando o Chroma volta"
    why_human: "Comportamento de circuit breaker em tempo real contra serviço externo"
  - test: "Matar o bot no jogo (mob/queda/lava) e checar o MemEvent type:'death' (importância 10) + POI 'danger' no local"
    expected: "Linha [death] com causa inferida; events tem death; places tem POI danger no bucket do local"
    why_human: "Exige evento bot.on('death') real do Mineflayer num servidor Minecraft ao vivo"
---

# Phase 08.1: Refatorar Memória — ChromaDB + Fiação + POIs + Morte + Lições — Verification Report

**Phase Goal:** A memória do bot funciona de verdade — eventos, lugares e perfis são gravados e o LLM realmente os usa para decidir e lembrar entre reinícios. Escopo: (1) consertar o bug de fiação (`persistEvent` nunca chamado → events=0); (2) migrar a camada vetorial de sqlite-vec para ChromaDB; (3) garantir que o LLM consome memórias recuperadas (KNN no caminho de AÇÃO); (4) memória espacial via `places` com POIs + busca por proximidade; (5) registrar evento de morte; (6) categoria de lições aprendidas evolutiva.

**Verified:** 2026-06-21
**Status:** human_needed (all automated must-haves verified; live-runtime items require manual testing)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth (Plan) | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Schema migra v1→v2 automaticamente sem perder dados (P01) | ✓ VERIFIED | `persistence.ts:92-125` applySchema por degraus, idempotente; `PRAGMA user_version = ${SCHEMA_VERSION}` (=2) |
| 2 | places ganha coluna type nullable (P01) | ✓ VERIFIED | `persistence.ts:110` addColumnIfMissing(db,'places','type','TEXT') |
| 3 | Tabela lessons com confidence/reinforce/contradict (P01) | ✓ VERIFIED | `persistence.ts:112-119` CREATE TABLE lessons + colunas |
| 4 | Índice places(x,z) para bounding-box (P01) | ✓ VERIFIED | `persistence.ts:121` CREATE INDEX idx_places_xz ON places(x,z) |
| 5 | Após sessão, count(*) FROM events > 0 (P02) | ⚠️ HUMAN | Fiação corrigida (6 call sites recordEvent); contagem real exige loop ao vivo |
| 6 | execute/anti-repeat/erro-skill/idle gravam em LP (P02) | ✓ VERIFIED | recordEvent em nodes.ts:238/302/322 e loop.ts:129 |
| 7 | Gravar evento nunca bloqueia o tick nem chama LLM (P02) | ✓ VERIFIED | `recordEvent.ts:26` persistEvent(...,null,...) try/catch, embedding null |
| 8 | Cliente conecta localhost:8000, get-or-create collection cosine dim768 (P03) | ✓ VERIFIED | `chromaClient.ts:159-164` getOrCreateCollection space:'cosine', embeddingFunction:null; config 8000/'events' |
| 9 | Toda chamada passa por breaker + timeout e degrada sem lançar (P03) | ✓ VERIFIED | `chromaClient.ts:75-106` withBreaker + withTimeout(Promise.race); fallback retornado |
| 10 | Chroma offline → aviso DEBOUNCED, loop nunca aborta (P03) | ✓ VERIFIED | `chromaClient.ts:42-47` warnOfflineDebounced; breaker devolve fallback |
| 11 | Caminho vetorial religa sozinho (half-open após cooldown) (P03) | ✓ VERIFIED | `chromaClient.ts:82-89` open→half-open após chromaCooldownMs |
| 12 | consolidate grava vetor consolidado no Chroma (id=String) (P04) | ✓ VERIFIED | addVector movido p/ caller: `deliberation.ts:268` addVector({id:String(idConsolidado)}) |
| 13 | retrieve recupera candidatos do Chroma (KNN) c/ scoring Gen Agents (P04) | ✓ VERIFIED | `longTerm.ts:196` chroma.queryVectors; scoring min-max W=1 linhas 236-251 |
| 14 | Chroma offline → fallback de recência, relacional intacto (P04) | ✓ VERIFIED | `longTerm.ts:228-230` recencyCandidates; persistEvent não toca vetor |
| 15 | Health-check no boot + re-sondagem periódica (P04) | ✓ VERIFIED | `loop.ts:164` chroma.health() no boot; `loop.ts:326` chromaProbeTimer |
| 16 | vec0 aposentado (sem write ativo em vec_events) (P04) | ✓ VERIFIED | Zero `INSERT INTO vec_events`; persistEvent/consolidate não escrevem vetor; tabela só DDL órfão |
| 17 | LLM recebe 'Memórias relevantes:' com até 3 memórias (P05) | ✓ VERIFIED | `prompts.ts:186-189` seção; `deliberation.ts:164` retrieve limit:3 |
| 18 | Query = embedding(currentGoal) cacheado por hash (P05) | ✓ VERIFIED | `deliberation.ts:54-69` computeGoalQueryEmbedding c/ queryEmbeddingHash |
| 19 | Seção depois do mundo e antes do FATO VERIFICADO (P05) | ✓ VERIFIED | `prompts.ts:185-203` recalled→poisLine→FATO VERIFICADO (último) |
| 20 | Cada recuperação loga [recall] (id, score, summary) (P05) | ⚠️ HUMAN | `deliberation.ts:166` console.log [recall]; emissão real exige loop ao vivo |
| 21 | Morte → MemEvent death (imp 10) + POI danger no local (P06) | ✓ VERIFIED | `loop.ts:256-275` bot.on('death') recordEvent death + upsertPlace danger |
| 22 | POIs deduplicados por bucket espacial via ON CONFLICT (P06) | ✓ VERIFIED | `places.ts:27-32` bucketKey GRID; `places.ts:45` ON CONFLICT(key) DO UPDATE |
| 23 | LLM recebe 'POIs próximos:' por distância euclidiana (P06) | ✓ VERIFIED | `places.ts:88-93` nearbyPlacesString; `deliberation.ts:170,186` injetado |
| 24 | Helpers de lições insert/reinforce/contradict/decay/topLessons (P06) | ✓ VERIFIED | `lessons.ts:14-70` os 5 helpers + reforço/decay aritmético |

**Score:** 24/24 truths verified by code; truths #5 and #20 additionally require live confirmation (counted as verified at the code level, routed to human for runtime proof).

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/memory/persistence.ts` | Migração v1→2 (places.type, lessons, idx) | ✓ VERIFIED | applySchema por degraus; SCHEMA_VERSION=2 |
| `src/memory/recordEvent.ts` | Helper central push CP + persistEvent LP null | ✓ VERIFIED | recordEvent exportado, 6 call sites |
| `src/memory/chromaClient.ts` | Wrapper Chroma + breaker + timeout | ✓ VERIFIED | createChromaClient/ChromaMemoryClient exportados |
| `src/config.ts` | Config Chroma host/port/timeouts/breaker | ✓ VERIFIED | chromaHost/Port/Collection/FetchTimeout/FailThreshold/Cooldown/WarnDebounce |
| `src/memory/longTerm.ts` | retrieve via Chroma KNN; persistEvent sem vec0 | ✓ VERIFIED | queryVectors usado; _embedding no-op |
| `src/cognition/reflection.ts` | consolidate (vetor via caller) | ✓ VERIFIED | consolidate retorna id; addVector no caller (design) |
| `src/cognition/deliberation.ts` | recall top-3 + [recall] + addVector + POIs | ✓ VERIFIED | linhas 164/166/170/186/268 |
| `src/cognition/state.ts` | Cache queryEmbedding/queryEmbeddingHash | ✓ VERIFIED | usado em deliberation.ts:60-66 |
| `src/llm/prompts.ts` | serializeContext + 'Memórias relevantes:' | ✓ VERIFIED | params recalled/poisLine; ordering correto |
| `src/cognition/types.ts` | MemEvent variante type:'death' | ✓ VERIFIED | linha 35 |
| `src/memory/places.ts` | upsertPlace/nearbyPlaces/nearbyPlacesString | ✓ VERIFIED | os 3 exportados |
| `src/memory/lessons.ts` | 5 helpers de lições | ✓ VERIFIED | todos exportados |
| `src/cognition/loop.ts` | health-check + probe + death handler | ✓ VERIFIED | linhas 164/326/256 |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| openDb | applySchema | boot do DB | ✓ WIRED | openDb→applySchema (persistence.ts:145) |
| recordEvent | persistEvent (embedding null) | holder.db existe | ✓ WIRED | recordEvent.ts:26 persistEvent(...,null,...) |
| pontos de origem | recordEvent | substituição push | ✓ WIRED | nodes.ts:238/302/322, loop.ts:129/266/269 |
| chromaClient | getOrCreateCollection cosine | embeddingFunction null | ✓ WIRED | chromaClient.ts:159-164 |
| withBreaker | timeout (Promise.race) | corte que conta como falha | ✓ WIRED | withTimeout via Promise.race:55-62 |
| consolidate | chromaClient.addVector | após INSERT, String(id) | ✓ WIRED | addVector no caller deliberation.ts:268 (design: consolidate síncrono) |
| retrieve | chromaClient.queryVectors | KNN → JOIN events | ✓ WIRED | longTerm.ts:196 |
| startCognitiveLoop | chromaClient.health | boot + re-sondagem | ✓ WIRED | loop.ts:164 + 326 |
| deliberation (AÇÃO) | retrieve + serializeContext | recalled top-3 último-1 param | ✓ WIRED | deliberation.ts:164,185 |
| currentGoal | holder.queryEmbedding (hash) | embed só quando hash muda | ✓ WIRED | deliberation.ts:60-66 |
| bot.on('death') | recordEvent(death) + upsertPlace(danger) | causa via buildReflexSensors | ✓ WIRED | loop.ts:256-275 |
| caminho AÇÃO | nearbyPlacesString → serializeContext | POIs no prompt | ✓ WIRED | deliberation.ts:170,186 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| serializeContext (recalled) | recalled | retrieve(db, chroma, queryEmb) | Sim quando há events + (Chroma online ou fallback recência) | ✓ FLOWING (relacional) / ⚠️ live p/ KNN Chroma |
| serializeContext (poisLine) | poisLine | nearbyPlacesString(db, pos) | Sim — lê places relacional | ✓ FLOWING |
| retrieve | candidates | queryVectors OU recencyCandidates | Sim — fallback garante dados quando há events | ✓ FLOWING |
| events relacional | count | recordEvent→persistEvent (6 origens reais) | Fiação real; contagem >0 exige sessão ao vivo | ⚠️ runtime (truth #5) |

Nota: a fiação de produção que faltava (`persistEvent` nunca chamado → events=0) está corrigida — `recordEvent` está nos 6 pontos de origem reais do loop. A prova final de `count(*) > 0` é runtime.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Type-check limpo | `tsc --noEmit` | exit 0, 0 erros | ✓ PASS |
| Suite de testes | `bun test` | 364 pass / 1 skip / 0 fail (50 files) | ✓ PASS |
| Sem write ativo em vec0 | grep `INSERT INTO vec_events` (prod) | 0 ocorrências | ✓ PASS |
| Sem TODO/stub em memory | grep TODO/FIXME/PLACEHOLDER (src/memory) | 0 ocorrências | ✓ PASS |
| KNN/recall real (Chroma+LLM) | loop ao vivo | n/d | ? SKIP (sem servidor) |

### Requirements Coverage

Fase INSERIDA sem IDs formais em REQUIREMENTS.md. Verificada contra o goal/escopo e os must_haves de cada plano (acima). Todos os 6 itens de escopo (fiação, ChromaDB, consumo pelo LLM, POIs espaciais, morte, lições) têm evidência de implementação substantiva e fiada.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| persistence.ts | 56 | `CREATE VIRTUAL TABLE IF NOT EXISTS vec_events` | ℹ️ Info | Tabela vec0 permanece como DDL órfão (sem write) — documentado/intencional (D-01); aposentar a tabela em si fica para fase futura |
| longTerm.ts / reflection.ts | 104 / 71 | param `_embedding` no-op na assinatura | ℹ️ Info | Mantido por compat com call sites — documentado; não é stub |

Nenhum blocker. Nenhum stub funcional. As duas "falhas" do gsd-tools são falsos-negativos: (1) Plan 01 `PRAGMA user_version = 2` usa interpolação `${SCHEMA_VERSION}` (valor=2, correto); (2) Plan 04 `addVector` foi deliberadamente movido de `reflection.ts` para o caller `deliberation.ts:268` (decisão de design documentada — mantém `consolidate` síncrono/testável sem Chroma).

### Human Verification Required

1. **events > 0 ao vivo** — rodar uma sessão (LM Studio + Chroma) e `SELECT count(*) FROM events`. Esperado: > 0 (fiação morta consertada). Por que humano: exige loop autônomo real.
2. **Vetor consolidado + recall** — com Chroma online, ver `[reflect] consolidado #id` e `[recall] #id score=...`. Por que humano: integração Chroma + embeddings LM Studio reais.
3. **Degradação do breaker** — derrubar o Chroma a meio da sessão: loop segue, `[chroma] OFFLINE` debounced, fallback de recência, religa ao voltar. Por que humano: comportamento de rede em tempo real.
4. **Morte → death + danger POI** — morrer no jogo e checar MemEvent death (imp 10) + POI danger. Por que humano: evento bot.on('death') real do Mineflayer.

(Itens 1-4 estão atados ao Known Gap da Phase 4 / gate da Phase 14 — live verification fora do escopo automatizado, conforme briefing.)

### Gaps Summary

Nenhum gap bloqueante. Os 6 planos entregaram o escopo da fase com código substantivo e corretamente fiado:

- **Fiação (P02):** o bug central (`persistEvent` nunca chamado em produção) está consertado — `recordEvent` nos 6 pontos de origem reais do loop (nodes.ts × 3, loop.ts idle + 2 de morte).
- **ChromaDB (P03/P04):** wrapper completo com circuit breaker + timeout + get-or-create cosine; retrieve via KNN com fallback de recência; vec0 aposentado (sem write ativo); health-check no boot + probe periódico.
- **Consumo pelo LLM (P05):** caminho de AÇÃO recupera top-3 (query=embedding do goal cacheado por hash), injeta "Memórias relevantes:" antes do FATO VERIFICADO, loga [recall].
- **Espacial (P06):** upsertPlace com dedup por bucket (ON CONFLICT), nearbyPlaces bounding-box + euclidiano, "POIs próximos:" no prompt.
- **Morte (P06):** bot.on('death') grava death (imp 10) + danger POI com causa inferida localmente.
- **Lições (P06):** 5 helpers (insert/reinforce/contradict/decay/topLessons) com reforço/decay aritmético.

Qualidade: `tsc --noEmit` limpo, 364 testes passando, 0 falhas. A única lacuna é a prova RUNTIME (bot+LLM+Chroma ao vivo), explicitamente fora do escopo automatizado e roteada para verificação humana.

---

## Gaps (Live Testing 2026-06-21)

Teste manual ao vivo (bot + LM Studio + Chroma) executado após os fixes de starvation (quick 260621-ir4) e parse lenient (quick 260621-jhi). **Loop central de memória CONFIRMADO ao vivo:** eventos persistem, reflexão dispara (`[reflect] consolidado`), vetor gravado no Chroma (`minemind_memory`, 7 vetores), recall semântico por KNN injetado no prompt (`[recall] score=1.89`), morte → death event + danger POI. Itens humanos 1-4 acima: **PASSARAM**.

**GAP-01 (POIs incompletos vs. goal) — escopo deste gap-closure:**

O goal da fase lista 5 tipos de POI (`base/build`, `resource`, `danger`, `village`/`villager`, `landmark`) com busca por proximidade ("o agente passa a saber o que tem onde"). A verificação automatizada confirmou que os HELPERS existem (`upsertPlace`/`nearbyPlaces`/`nearbyPlacesString`) e são lidos no prompt, mas a investigação ao vivo revelou que **só `danger` é criado** (via `bot.on('death')` em loop.ts:267). Nenhum call-site fia a criação de:

| Tipo de POI | Quando deveria ser criado | Status |
| --- | --- | --- |
| `resource` | ao coletar/minerar um recurso com sucesso (veia/árvore) | ✅ **fiado** (08.1-07: `recordResourcePoi` no nó execute) |
| `village` / `villager` | ao perceber um aldeão/vila no snapshot | ✅ **fiado** (08.1-07: `recordVillagePoi` no nó execute) |
| `base` / `build` | local de base/abrigo do agente | ⏭️ fora do escopo (sem gatilho barato — documentado) |
| `landmark` | ponto de referência notável percebido | ⏭️ fora do escopo (sem gatilho barato — documentado) |
| `danger` | na morte | ✅ fiado (loop.ts:267) |

Efeito: `places` ao vivo só acumulava POIs `danger`. A memória espacial "o que tem onde" do goal não se concretizava além de perigos. **Resolvido no 08.1-07:** `recordResourcePoi`/`recordVillagePoi` (módulo `poi-detect.ts`) compõem `upsertPlace` (dedup por bucket) e estão fiados no nó execute; os novos POIs entram no prompt via `nearbyPlacesString` já existente. `base`/`landmark` ficaram deliberadamente fora (nenhum gatilho barato existe hoje — não inventar gatilhos artificiais).

**GAP-01 status: ✅ resolvido** (resource/village fiados no 08.1-07; base/landmark explicitamente fora do escopo).

**Fora do escopo deste gap-closure (deferido para Phase 14):** lições (`lessons`) não são criadas nem lidas no prompt (`topLessons` nunca chamado). O roadmap define lições como "semente da Phase 14" — tabela + helpers + decay-on-boot são a entrega desta fase; criação/consumo ativo fica para o loop de aprendizado da Phase 14. NÃO endereçar aqui.

**Não-gap:** `players` está fiado (`upsertPlayer` em conversation.ts:101); vazio ao vivo apenas porque o bot rodou sozinho (sem chat de jogador).

---

_Verified: 2026-06-21_
_Verifier: Claude (gsd-verifier)_
_Updated 2026-06-21: live testing results + GAP-01 (POIs) for gap-closure_
_Updated 2026-06-21: GAP-01 RESOLVED via 08.1-07 (recordResourcePoi + recordVillagePoi fiados; base/landmark fora do escopo)_
