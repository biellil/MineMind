---
phase: 08.1-refatorar-memoria
verified: 2026-06-21T00:00:00Z
status: passed
score: 25/25 must-have truths verified (24 fase + GAP-01); core loop PASSED em teste ao vivo; GAP-01 code-verified
re_verification:
  previous_status: gaps_found
  previous_score: 24/24 (automated) com 1 wiring gap (POIs)
  gaps_closed:
    - "GAP-01: POIs resource/village agora fiados (recordResourcePoi + recordVillagePoi no nó execute via 08.1-07)"
  gaps_remaining: []
  regressions: []
  note: >-
    Gap-closure 08.1-07 fechou GAP-01 (POIs resource/village). Verificação automatizada: poi-detect.test.ts
    9/9 pass, tsc --noEmit exit 0, wiring confirmado no nó execute (L176 village, L318 resource, ambos
    guardados por `if (snap && holder.db)`), reutiliza upsertPlace (0 INSERT INTO places), surface no prompt
    via nearbyPlacesString (deliberation.ts:170) sem mudança. Itens live (Chroma + LM Studio loop) já PASSARAM
    no teste ao vivo de 2026-06-21. base/landmark/lições explicitamente fora de escopo (deferidos p/ Phase 14).
human_verification_passed:
  - test: "Loop central de memória ao vivo (events>0, reflexão, vetor no Chroma, recall KNN, morte→death+danger)"
    result: "PASSED em teste manual 2026-06-21 (bot + LM Studio + Chroma; minemind_memory 7 vetores; [recall] score=1.89)"
---

# Phase 08.1: Refatorar Memória — ChromaDB + Fiação + POIs + Morte + Lições — Verification Report

**Phase Goal:** A memória do bot funciona de verdade — eventos, lugares e perfis são gravados e o LLM realmente os usa para decidir e lembrar entre reinícios. Escopo: (1) consertar o bug de fiação (`persistEvent` nunca chamado → events=0); (2) migrar a camada vetorial de sqlite-vec para ChromaDB; (3) garantir que o LLM consome memórias recuperadas (KNN no caminho de AÇÃO); (4) memória espacial via `places` com POIs + busca por proximidade; (5) registrar evento de morte; (6) categoria de lições aprendidas evolutiva.

**Verified:** 2026-06-21
**Status:** passed (todos os must-haves automatizados verificados; GAP-01 fechado e code-verified; itens live já PASSARAM no teste ao vivo de 2026-06-21)
**Re-verification:** Yes — após gap-closure 08.1-07 (GAP-01: POIs resource/village)

## Re-Verification Summary (08.1-07 / GAP-01)

| Item | Verificação | Resultado |
| --- | --- | --- |
| `recordResourcePoi` + `recordVillagePoi` exportados | `src/memory/poi-detect.ts:27,46` | ✓ VERIFIED |
| Reusa `upsertPlace` (sem SQL novo) | `import { upsertPlace }` L20; 0 `INSERT INTO places` | ✓ VERIFIED |
| `recordVillagePoi` fiado no execute (guardado) | `nodes.ts:176` `if (snap && holder.db) recordVillagePoi(...)` | ✓ WIRED |
| `recordResourcePoi` fiado no success path (guardado) | `nodes.ts:318` `if (snap && holder.db) recordResourcePoi(..., result.outcome, ...)` após recordEvent | ✓ WIRED |
| POIs novos entram no prompt | `nearbyPlacesString` em `deliberation.ts:170` (inalterado) surface resource/village | ✓ FLOWING |
| `bun test src/memory/poi-detect.test.ts` | 9 pass / 0 fail / 24 expect() | ✓ PASS |
| `bunx tsc --noEmit` | exit 0, 0 erros | ✓ PASS |
| Escopo: só `nodes.ts` + `poi-detect.*` tocados | deliberation.ts / loop.ts intactos | ✓ VERIFIED |

**GAP-01 status: ✅ RESOLVIDO e code-verified.** `resource`/`village` fiados; `base`/`landmark` deliberadamente fora (sem gatilho barato); lições deferidas p/ Phase 14. Nenhuma regressão.

## Goal Achievement

### Observable Truths

| # | Truth (Plan) | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Schema migra v1→v2 automaticamente sem perder dados (P01) | ✓ VERIFIED | `persistence.ts:92-125` applySchema por degraus, idempotente; `PRAGMA user_version = ${SCHEMA_VERSION}` (=2) |
| 2 | places ganha coluna type nullable (P01) | ✓ VERIFIED | `persistence.ts:110` addColumnIfMissing(db,'places','type','TEXT') |
| 3 | Tabela lessons com confidence/reinforce/contradict (P01) | ✓ VERIFIED | `persistence.ts:112-119` CREATE TABLE lessons + colunas |
| 4 | Índice places(x,z) para bounding-box (P01) | ✓ VERIFIED | `persistence.ts:121` CREATE INDEX idx_places_xz ON places(x,z) |
| 5 | Após sessão, count(*) FROM events > 0 (P02) | ✓ PASSED (live) | Fiação corrigida (6 call sites recordEvent); confirmado >0 em teste ao vivo 2026-06-21 |
| 6 | execute/anti-repeat/erro-skill/idle gravam em LP (P02) | ✓ VERIFIED | recordEvent em nodes.ts:238/302/322 e loop.ts:129 |
| 7 | Gravar evento nunca bloqueia o tick nem chama LLM (P02) | ✓ VERIFIED | `recordEvent.ts:26` persistEvent(...,null,...) try/catch, embedding null |
| 8 | Cliente conecta localhost:8000, get-or-create collection cosine dim768 (P03) | ✓ VERIFIED | `chromaClient.ts:159-164` getOrCreateCollection space:'cosine', embeddingFunction:null; config 8000/'events' |
| 9 | Toda chamada passa por breaker + timeout e degrada sem lançar (P03) | ✓ VERIFIED | `chromaClient.ts:75-106` withBreaker + withTimeout(Promise.race); fallback retornado |
| 10 | Chroma offline → aviso DEBOUNCED, loop nunca aborta (P03) | ✓ VERIFIED | `chromaClient.ts:42-47` warnOfflineDebounced; breaker devolve fallback |
| 11 | Caminho vetorial religa sozinho (half-open após cooldown) (P03) | ✓ VERIFIED | `chromaClient.ts:82-89` open→half-open após chromaCooldownMs |
| 12 | consolidate grava vetor consolidado no Chroma (id=String) (P04) | ✓ VERIFIED | addVector movido p/ caller: `deliberation.ts:268` addVector({id:String(idConsolidado)}) |
| 13 | retrieve recupera candidatos do Chroma (KNN) c/ scoring Gen Agents (P04) | ✓ VERIFIED | `longTerm.ts:196` chroma.queryVectors; scoring min-max W=1 linhas 236-251 |
| 14 | Chroma offline → fallback de recência, relacional intacto (P04) | ✓ VERIFIED | `longTerm.ts:228-230` recencyCandidates; persistEvent não toca vetor |
| 15 | Health-check no boot + re-sondagem periódica (P04) | ✓ VERIFIED | `loop.ts:164` chroma.health() no boot; `loop.ts:326` chromaProbeTimer |
| 16 | vec0 aposentado (sem write ativo em vec_events) (P04) | ✓ VERIFIED | Zero `INSERT INTO vec_events`; persistEvent/consolidate não escrevem vetor; tabela só DDL órfão |
| 17 | LLM recebe 'Memórias relevantes:' com até 3 memórias (P05) | ✓ VERIFIED | `prompts.ts:186-189` seção; `deliberation.ts:164` retrieve limit:3 |
| 18 | Query = embedding(currentGoal) cacheado por hash (P05) | ✓ VERIFIED | `deliberation.ts:54-69` computeGoalQueryEmbedding c/ queryEmbeddingHash |
| 19 | Seção depois do mundo e antes do FATO VERIFICADO (P05) | ✓ VERIFIED | `prompts.ts:185-203` recalled→poisLine→FATO VERIFICADO (último) |
| 20 | Cada recuperação loga [recall] (id, score, summary) (P05) | ✓ PASSED (live) | `deliberation.ts:166` console.log [recall]; confirmado ao vivo ([recall] score=1.89) |
| 21 | Morte → MemEvent death (imp 10) + POI danger no local (P06) | ✓ VERIFIED / PASSED (live) | `loop.ts:256-275` bot.on('death') recordEvent death + upsertPlace danger; confirmado ao vivo |
| 22 | POIs deduplicados por bucket espacial via ON CONFLICT (P06) | ✓ VERIFIED | `places.ts:27-32` bucketKey GRID; `places.ts:45` ON CONFLICT(key) DO UPDATE |
| 23 | LLM recebe 'POIs próximos:' por distância euclidiana (P06) | ✓ VERIFIED | `places.ts:88-93` nearbyPlacesString; `deliberation.ts:170,186` injetado |
| 24 | Helpers de lições insert/reinforce/contradict/decay/topLessons (P06) | ✓ VERIFIED | `lessons.ts:14-70` os 5 helpers + reforço/decay aritmético |
| 25 | POIs resource (coleta success) + village (aldeão) criados e no prompt (GAP-01 / P07) | ✓ VERIFIED | `poi-detect.ts:27,46` recordResourcePoi/recordVillagePoi (reusam upsertPlace); fiados em `nodes.ts:176,318` guardados; surface via nearbyPlacesString; 9/9 testes |

**Score:** 25/25 truths verified por código. Truths #5, #20, #21 adicionalmente confirmados em teste ao vivo 2026-06-21 (PASSED).

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/memory/persistence.ts` | Migração v1→2 (places.type, lessons, idx) | ✓ VERIFIED | applySchema por degraus; SCHEMA_VERSION=2 |
| `src/memory/recordEvent.ts` | Helper central push CP + persistEvent LP null | ✓ VERIFIED | recordEvent exportado, 6 call sites |
| `src/memory/chromaClient.ts` | Wrapper Chroma + breaker + timeout | ✓ VERIFIED | createChromaClient/ChromaMemoryClient exportados |
| `src/config.ts` | Config Chroma host/port/timeouts/breaker | ✓ VERIFIED | chromaHost/Port/Collection/FetchTimeout/FailThreshold/Cooldown/WarnDebounce |
| `src/memory/longTerm.ts` | retrieve via Chroma KNN; persistEvent sem vec0 | ✓ VERIFIED | queryVectors usado; _embedding no-op |
| `src/cognition/reflection.ts` | consolidate (vetor via caller) | ✓ VERIFIED | consolidate retorna id; addVector no caller (design) |
| `src/cognition/deliberation.ts` | recall top-3 + [recall] + addVector + POIs | ✓ VERIFIED | linhas 164/166/170/186/268 |
| `src/cognition/state.ts` | Cache queryEmbedding/queryEmbeddingHash | ✓ VERIFIED | usado em deliberation.ts:60-66 |
| `src/llm/prompts.ts` | serializeContext + 'Memórias relevantes:' | ✓ VERIFIED | params recalled/poisLine; ordering correto |
| `src/cognition/types.ts` | MemEvent variante type:'death' | ✓ VERIFIED | linha 35 |
| `src/memory/places.ts` | upsertPlace/nearbyPlaces/nearbyPlacesString | ✓ VERIFIED | os 3 exportados |
| `src/memory/lessons.ts` | 5 helpers de lições | ✓ VERIFIED | todos exportados |
| `src/cognition/loop.ts` | health-check + probe + death handler | ✓ VERIFIED | linhas 164/326/256 |
| `src/memory/poi-detect.ts` | recordResourcePoi + recordVillagePoi (GAP-01) | ✓ VERIFIED | 2 helpers puros que compõem upsertPlace; 0 SQL novo; 53 linhas |
| `src/memory/poi-detect.test.ts` | Prova resource/village + não-criação em falha + dedup | ✓ VERIFIED | 9 testes, todos passam |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| openDb | applySchema | boot do DB | ✓ WIRED | openDb→applySchema (persistence.ts:145) |
| recordEvent | persistEvent (embedding null) | holder.db existe | ✓ WIRED | recordEvent.ts:26 persistEvent(...,null,...) |
| pontos de origem | recordEvent | substituição push | ✓ WIRED | nodes.ts:238/302/322, loop.ts:129/266/269 |
| chromaClient | getOrCreateCollection cosine | embeddingFunction null | ✓ WIRED | chromaClient.ts:159-164 |
| withBreaker | timeout (Promise.race) | corte que conta como falha | ✓ WIRED | withTimeout via Promise.race:55-62 |
| consolidate | chromaClient.addVector | após INSERT, String(id) | ✓ WIRED | addVector no caller deliberation.ts:268 |
| retrieve | chromaClient.queryVectors | KNN → JOIN events | ✓ WIRED | longTerm.ts:196 |
| startCognitiveLoop | chromaClient.health | boot + re-sondagem | ✓ WIRED | loop.ts:164 + 326 |
| deliberation (AÇÃO) | retrieve + serializeContext | recalled top-3 último-1 param | ✓ WIRED | deliberation.ts:164,185 |
| currentGoal | holder.queryEmbedding (hash) | embed só quando hash muda | ✓ WIRED | deliberation.ts:60-66 |
| bot.on('death') | recordEvent(death) + upsertPlace(danger) | causa via buildReflexSensors | ✓ WIRED | loop.ts:256-275 |
| caminho AÇÃO | nearbyPlacesString → serializeContext | POIs no prompt | ✓ WIRED | deliberation.ts:170,186 |
| nodes.ts (execute, tick) | recordVillagePoi → upsertPlace(village) | `if (snap && holder.db)` | ✓ WIRED | nodes.ts:176 (GAP-01) |
| nodes.ts (execute, success) | recordResourcePoi → upsertPlace(resource) | `if (snap && holder.db)` após recordEvent | ✓ WIRED | nodes.ts:318 (GAP-01) |
| poi-detect.ts | places.ts (upsertPlace) | `import { upsertPlace }` | ✓ WIRED | poi-detect.ts:20 (0 INSERT INTO places) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| serializeContext (recalled) | recalled | retrieve(db, chroma, queryEmb) | Sim — confirmado ao vivo (KNN Chroma + fallback recência) | ✓ FLOWING |
| serializeContext (poisLine) | poisLine | nearbyPlacesString(db, pos) | Sim — lê places relacional (agora inclui resource/village além de danger) | ✓ FLOWING |
| retrieve | candidates | queryVectors OU recencyCandidates | Sim — fallback garante dados quando há events | ✓ FLOWING |
| events relacional | count | recordEvent→persistEvent (6 origens reais) | Sim — events>0 confirmado ao vivo | ✓ FLOWING |
| places (resource/village) | upsertPlace | recordResourcePoi/recordVillagePoi (nodes.ts:176,318) | Sim — fiado em success da skill + snapshot de aldeão; entra no prompt via nearbyPlacesString | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Type-check limpo | `bunx tsc --noEmit` | exit 0, 0 erros | ✓ PASS |
| Testes GAP-01 | `bun test src/memory/poi-detect.test.ts` | 9 pass / 0 fail / 24 expect() | ✓ PASS |
| Suite de testes (regressão) | `bun test` (08.1-07) | 380 pass / 1 skip / 0 fail | ✓ PASS |
| poi-detect reusa upsertPlace | grep `INSERT INTO places` (poi-detect.ts) | 0 ocorrências | ✓ PASS |
| Loop central de memória ao vivo | bot + LM Studio + Chroma (2026-06-21) | events>0, [reflect], 7 vetores no Chroma, [recall] score=1.89, death+danger | ✓ PASS |

### Requirements Coverage

Fase INSERIDA sem IDs formais em REQUIREMENTS.md. GAP-01 é o único ID formal (`requirements: [GAP-01]` no 08.1-07-PLAN). Verificada contra o goal/escopo e os must_haves de cada plano. Todos os 6 itens de escopo (fiação, ChromaDB, consumo pelo LLM, POIs espaciais, morte, lições) têm evidência substantiva e fiada; GAP-01 (POIs resource/village) ✅ SATISFEITO.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| persistence.ts | 56 | `CREATE VIRTUAL TABLE IF NOT EXISTS vec_events` | ℹ️ Info | DDL órfão vec0 (sem write) — documentado/intencional (D-01); aposentar a tabela fica p/ fase futura |
| longTerm.ts / reflection.ts | 104 / 71 | param `_embedding` no-op na assinatura | ℹ️ Info | Mantido por compat com call sites — documentado; não é stub |

Nenhum blocker. Nenhum stub funcional. `poi-detect.ts` sem TODO/FIXME/stub; helpers degradam graciosamente (guards de posição) e nunca lançam.

### Human Verification Required

Nenhum item bloqueante pendente. Os itens runtime já foram exercitados e **PASSARAM** no teste manual ao vivo de 2026-06-21 (bot + LM Studio + Chroma): events>0, reflexão consolidada, vetor no Chroma (`minemind_memory`, 7 vetores), recall KNN injetado no prompt (`[recall] score=1.89`), morte→death+danger POI.

Opcional (não-bloqueante, gate Phase 14): coletar madeira e ver POI `resource` na linha "POIs próximos:"; aproximar-se de uma aldeia e ver POI `village`. Code-path verificado; observação ao vivo é confirmação extra, não gate.

### Gaps Summary

Nenhum gap bloqueante restante. GAP-01 (POIs incompletos — só `danger` criado) foi fechado pelo 08.1-07: `recordResourcePoi` + `recordVillagePoi` (módulo `poi-detect.ts`) compõem `upsertPlace` (dedup por bucket, sem SQL novo) e estão fiados no nó execute (village 1x/tick L176; resource no success path L318, ambos guardados por `if (snap && holder.db)`). Os novos POIs entram no prompt via `nearbyPlacesString` já existente, sem tocar deliberation.ts/loop.ts. Verificação: 9/9 testes, tsc limpo, 0 `INSERT INTO places`, 380 testes na suíte sem regressão.

`base`/`landmark` ficaram deliberadamente fora (nenhum gatilho barato existe hoje — não inventar gatilhos artificiais). Lições (`lessons`) — tabela + helpers + decay-on-boot entregues nesta fase; criação/consumo ativo deferido para o loop de aprendizado da Phase 14 por design do roadmap. Nenhum dos dois deve ser re-flagado como gap.

---

## Histórico (Live Testing 2026-06-21 + GAP-01)

Teste manual ao vivo (bot + LM Studio + Chroma) após os fixes de starvation (quick 260621-ir4) e parse lenient (quick 260621-jhi). **Loop central de memória CONFIRMADO ao vivo:** eventos persistem, reflexão dispara (`[reflect] consolidado`), vetor gravado no Chroma (`minemind_memory`, 7 vetores), recall semântico por KNN injetado no prompt (`[recall] score=1.89`), morte → death event + danger POI. Itens humanos: **PASSARAM**.

GAP-01 (POIs incompletos vs. goal): o teste ao vivo revelou que só `danger` era criado. **Resolvido no 08.1-07** — `resource` (coleta success) e `village` (aldeão no snapshot) fiados no nó execute; `base`/`landmark` fora do escopo (sem gatilho barato); lições deferidas p/ Phase 14.

---

_Verified: 2026-06-21_
_Verifier: Claude (gsd-verifier)_
_Re-verified 2026-06-21: GAP-01 RESOLVED via 08.1-07 (recordResourcePoi + recordVillagePoi fiados, code-verified) → status passed_
