---
phase: 08.1-refatorar-memoria
plan: 05
type: execute
wave: 3
depends_on: ["08.1-04"]
files_modified:
  - src/cognition/state.ts
  - src/llm/prompts.ts
  - src/cognition/deliberation.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "No caminho de AÇÃO, o LLM recebe uma seção 'Memórias relevantes:' com até 3 memórias recuperadas"
    - "A query de recuperação é o embedding do currentGoal, cacheado por hash do goal (~1 embed por troca de goal)"
    - "A seção fica depois do contexto de mundo e antes do FATO VERIFICADO (que permanece o último)"
    - "Cada recuperação loga [recall] (id, score, summary truncado) — verificável ao vivo"
  artifacts:
    - path: "src/llm/prompts.ts"
      provides: "serializeContext ganha param recalled + seção 'Memórias relevantes:'"
      contains: "Memórias relevantes:"
    - path: "src/cognition/deliberation.ts"
      provides: "Caminho de AÇÃO recupera (top-k=3) com query=embedding(currentGoal) cacheado + log [recall]"
      contains: "[recall]"
    - path: "src/cognition/state.ts"
      provides: "Cache do query embedding no holder (queryEmbedding + queryEmbeddingHash)"
      contains: "queryEmbeddingHash"
  key_links:
    - from: "deliberation.ts (caminho de AÇÃO)"
      to: "retrieve + serializeContext"
      via: "recalled top-k=3 passado como novo último param de serializeContext"
      pattern: "serializeContext\\([^)]*recalled"
    - from: "currentGoal"
      to: "holder.queryEmbedding (cache por hash)"
      via: "provider.embed só quando o hash do goal muda"
      pattern: "queryEmbeddingHash"
---

<objective>
Garantir o USO REAL da memória pelo LLM (a correção central do critério da fase): hoje o caminho de AÇÃO (`serializeContext` em deliberation.ts) NÃO injeta nenhuma memória recuperada. Adicionar a recuperação top-k=3 com `query = embedding(currentGoal)` cacheado por hash do goal (D-11), renderizar uma seção `Memórias relevantes:` no HumanMessage depois do mundo e antes do `FATO VERIFICADO` (D-12), e logar `[recall]` por recuperação (D-13).

Purpose: É o que torna o critério "o LLM usa a memória" verificável ao vivo. A estratégia faseada D-10 aterrissa direto em B (query = embedding do goal, o alvo da fase). O cache por hash do goal gasta ~1 embed por TROCA de goal (não por deliberação), respeitando o invariante de custo de embedding. Depende do Plan 04 (retrieve já consulta o Chroma e é async).
Output: `state.ts` (campos de cache), `prompts.ts` (param `recalled` + seção), `deliberation.ts` (recuperação no caminho de AÇÃO + cache + log).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-refatorar-mem-ria-migrar-vetores-para-chromadb-j-rodando-local-consertar-grava-o-de-eventos-lugares-garantir-uso-real-pelo-llm-mem-ria-espacial-pois-e-registro-de-morte/08.1-CONTEXT.md
@.planning/phases/08.1-refatorar-mem-ria-migrar-vetores-para-chromadb-j-rodando-local-consertar-grava-o-de-eventos-lugares-garantir-uso-real-pelo-llm-mem-ria-espacial-pois-e-registro-de-morte/08.1-RESEARCH.md

<interfaces>
`serializeContext` ATUAL (prompts.ts) — assinatura (último param é o FATO VERIFICADO):
```ts
export function serializeContext(
  snapshot: WorldSnapshot | null, needs: unknown, goals: unknown,
  recentEvents: ReadonlyArray<MemEvent>,
  lastObservedDelta?: { skill; target; outcome; observed; expected } | null,
): string
```
Ordem ATUAL das linhas: Status/mundo → Necessidades → Objetivos → Eventos recentes → **FATO VERIFICADO (último)**.

`retrieve` (após Plan 04) é async: `retrieve(db, chroma, queryEmbedding, now, { limit }): Promise<RetrievedEvent[]>`. `RetrievedEvent = { id:number; summary:string; payload:string; importance:number; score:number }`.

Caminho de AÇÃO em `maybeDeliberate` (deliberation.ts, ramo else): monta o HumanMessage com `serializeContext(snapshot, holder.needs, holder.currentGoal, getEvents(holder.memory), holder.lastObservedDelta)`. Tem `provider` (com `provider.embed`), `holder`, `now`, e (após Plan 04) o `chroma`.

`holder.currentGoal: Goal | null`. `provider.embed(text): Promise<number[]>` (LM Studio, local).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cache do query embedding no holder + seção 'Memórias relevantes:' no prompt</name>
  <files>src/cognition/state.ts, src/llm/prompts.ts</files>
  <read_first>
    - src/cognition/state.ts (CognitiveStateHolder — adicionar campos de cache; createCognitiveStateHolder)
    - src/llm/prompts.ts (serializeContext — ordem das linhas, bloco FATO VERIFICADO, helper truncate)
    - src/llm/prompts.test.ts e src/llm/schemas.test.ts (chamadas existentes de serializeContext a manter compatíveis)
    - .planning/phases/08.1-.../08.1-RESEARCH.md (§ "Seção Memórias relevantes:" — render; D-11/D-12)
  </read_first>
  <action>
    1. Em `src/cognition/state.ts` → `CognitiveStateHolder`, adicionar dois campos (D-11) e inicializá-los em `createCognitiveStateHolder`:
       ```ts
       /** Cache do embedding da query de recuperação (D-11), chaveado por hash do texto do goal. */
       queryEmbedding: number[] | null
       queryEmbeddingHash: string | null
       ```
       No `createCognitiveStateHolder`: `queryEmbedding: null, queryEmbeddingHash: null`.

    2. Em `src/llm/prompts.ts` → `serializeContext`, adicionar um NOVO parâmetro `recalled` ao FINAL da lista (depois de `lastObservedDelta`), opcional, para não quebrar os chamadores existentes de teste:
       ```ts
       export function serializeContext(
         snapshot, needs, goals, recentEvents,
         lastObservedDelta?: {...} | null,
         recalled?: ReadonlyArray<{ id: number; summary: string; score: number }>,
       ): string
       ```
       Renderizar a seção `Memórias relevantes:` DEPOIS do bloco de "Eventos recentes" e ANTES do bloco `FATO VERIFICADO` (a ordem importa — D-12; o FATO VERIFICADO continua sendo a ÚLTIMA linha):
       ```ts
       if (recalled && recalled.length > 0) {
         lines.push(
           'Memórias relevantes:\n  ' +
             recalled.map((r) => truncate(r.summary, 120)).join('\n  '),
         )
       }
       ```
       Reusar o helper `truncate` já existente no arquivo. NÃO mover o bloco `FATO VERIFICADO` — ele permanece após este novo bloco.

    3. Atualizar o JSDoc de `serializeContext` para mencionar o novo parâmetro `recalled` e a posição da seção (antes do FATO VERIFICADO).
  </action>
  <verify>
    <automated>bun test src/llm/prompts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `state.ts` CognitiveStateHolder tem `queryEmbedding: number[] | null` e `queryEmbeddingHash: string | null`, inicializados como null em createCognitiveStateHolder
    - `prompts.ts` serializeContext aceita o parâmetro `recalled` opcional no FINAL
    - `prompts.ts` contém a string literal `Memórias relevantes:`
    - A seção `Memórias relevantes:` é renderizada ANTES do bloco `FATO VERIFICADO` (verificável: no código, o push de "Memórias relevantes" precede o `if (lastObservedDelta)`)
    - Os testes existentes de serializeContext (prompts.test.ts, schemas.test.ts) continuam passando (param opcional não quebra chamadas com 4-5 args)
    - `bunx tsc --noEmit` sem erro
  </acceptance_criteria>
  <done>
    O holder cacheia o query embedding e o prompt sabe renderizar a seção de memórias na posição correta; testes existentes verdes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recuperar no caminho de AÇÃO (query=embedding(goal) cacheado) + log [recall]</name>
  <files>src/cognition/deliberation.ts, src/cognition/deliberation.recall.test.ts, src/memory/longTerm.ts</files>
  <read_first>
    - src/cognition/deliberation.ts (ramo else do maybeDeliberate — montagem do HumanMessage; presença de provider/chroma/holder/now após Plan 04)
    - src/memory/longTerm.ts (retrieve async, após Plan 04)
    - src/cognition/state.ts (campos de cache adicionados na Task 1)
    - .planning/phases/08.1-.../08.1-RESEARCH.md (§ "Recuperação com query = embedding(currentGoal) cacheado")
  </read_first>
  <behavior>
    - Test 1: dado um currentGoal, computeQueryEmbedding chama provider.embed UMA vez e cacheia (segunda chamada com o MESMO goal NÃO re-chama embed).
    - Test 2: trocar o currentGoal (hash diferente) re-chama provider.embed e atualiza o cache.
    - Test 3: currentGoal null → query embedding null (e retrieve cai no fallback de recência).
    - Test 4: o log [recall] é emitido por memória recuperada (id, score, summary truncado).
  </behavior>
  <action>
    No ramo de AÇÃO de `maybeDeliberate` (deliberation.ts, o `else` que monta o HumanMessage), ANTES de montar as `messages`:

    1. Extrair um helper testável `computeGoalQueryEmbedding(holder, provider)` (export para teste), que implementa o cache D-11:
       ```ts
       export async function computeGoalQueryEmbedding(
         holder: CognitiveStateHolder, provider: LlmProvider,
       ): Promise<number[] | null> {
         const goalText = holder.currentGoal ? JSON.stringify(holder.currentGoal) : ''
         const goalHash = hashString(goalText)            // hash estável barato (ver passo 2)
         if (holder.queryEmbeddingHash !== goalHash) {
           try {
             holder.queryEmbedding = goalText ? await provider.embed(goalText) : null
           } catch {
             holder.queryEmbedding = null                 // degrada: sem embedding → fallback recência
           }
           holder.queryEmbeddingHash = goalHash
         }
         return holder.queryEmbedding
       }
       ```

    2. Implementar (ou importar) um `hashString(s: string): string` estável e barato (ex.: djb2/FNV — uma função pura local; NÃO usar crypto pesado). Pode viver em `deliberation.ts` ou num util pequeno.

    3. Recuperar e logar (D-12/D-13) — top-k=3:
       ```ts
       const queryEmb = await computeGoalQueryEmbedding(holder, provider)
       const recalled = holder.db ? await retrieve(holder.db, chroma, queryEmb, now, { limit: 3 }) : []
       for (const r of recalled) {
         console.log(`[recall] #${r.id} score=${r.score.toFixed(2)} ${truncate(r.summary, 60)}`)
       }
       ```
       (Importar/replicar `truncate` — ou inline uma versão simples; preferir reusar a de prompts se exportada, senão um slice local.)

    4. Passar `recalled` como novo último argumento de `serializeContext`:
       ```ts
       serializeContext(
         snapshot, holder.needs, holder.currentGoal, getEvents(holder.memory),
         holder.lastObservedDelta,
         recalled.map((r) => ({ id: r.id, summary: r.summary, score: r.score })),
       )
       ```

    5. Garantir que `chroma` está em escopo no ramo de AÇÃO (o Plan 04 já adicionou `chroma` à assinatura de `maybeDeliberate`). Se o ramo de AÇÃO ainda não recebia o `chroma`, ele agora está disponível via parâmetro de `maybeDeliberate`.

    Criar `src/cognition/deliberation.recall.test.ts` cobrindo os 4 comportamentos do bloco <behavior>, usando um provider fake (com `embed` espionável/contador) e um holder de teste (db null para isolar o cache; ou db em :memory: para o caminho completo). Para o Test 4, espiar `console.log` e assertar o prefixo `[recall]`.
  </action>
  <verify>
    <automated>bun test src/cognition/deliberation.recall.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `deliberation.ts` exporta `computeGoalQueryEmbedding` e usa `holder.queryEmbeddingHash` para evitar re-embed (cache D-11)
    - `deliberation.ts` chama `await retrieve(holder.db, chroma, queryEmb, now, { limit: 3 })` no caminho de AÇÃO
    - `deliberation.ts` emite `console.log` com prefixo `[recall]` por memória recuperada (id, score, summary)
    - A chamada de `serializeContext` no caminho de AÇÃO passa `recalled` como último argumento
    - `src/cognition/deliberation.recall.test.ts` cobre os 4 comportamentos e sai com código 0
    - `bunx tsc --noEmit` sem erro
  </acceptance_criteria>
  <done>
    O caminho de AÇÃO recupera memórias por embedding do goal (cacheado), injeta no prompt e loga [recall]; o cache evita re-embed por deliberação.
  </done>
</task>

</tasks>

<verification>
- `bun test src/llm src/cognition` passa.
- `bunx tsc --noEmit` sem erro.
- `grep "Memórias relevantes:" src/llm/prompts.ts` e `grep "\[recall\]" src/cognition/deliberation.ts` retornam match.
- `grep "queryEmbeddingHash" src/cognition/state.ts src/cognition/deliberation.ts` retorna match.
- (Ao vivo, manual): rodar o bot com goal definido → o log `[recall]` aparece e a seção `Memórias relevantes:` entra no HumanMessage.
</verification>

<success_criteria>
- O LLM consome memórias recuperadas no caminho de AÇÃO (correção central — D-12).
- query = embedding(currentGoal) cacheado por hash → ~1 embed por troca de goal (D-11).
- Seção posicionada antes do FATO VERIFICADO (que continua último) — D-12.
- Log [recall] torna o critério verificável ao vivo (D-13).
</success_criteria>

<output>
Após completar, criar `.planning/phases/08.1-refatorar-mem-ria-migrar-vetores-para-chromadb-j-rodando-local-consertar-grava-o-de-eventos-lugares-garantir-uso-real-pelo-llm-mem-ria-espacial-pois-e-registro-de-morte/08.1-05-SUMMARY.md`
</output>
