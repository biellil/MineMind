---
phase: 08.1-refatorar-memoria
plan: 04
type: execute
wave: 2
depends_on: ["08.1-03"]
files_modified:
  - src/memory/longTerm.ts
  - src/cognition/reflection.ts
  - src/cognition/deliberation.ts
  - src/cognition/loop.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "O batch de reflexão (consolidate) grava o vetor do evento consolidado no ChromaDB (id = String do evento)"
    - "retrieve recupera candidatos do ChromaDB (KNN) quando há queryEmbedding, mantendo o scoring Generative Agents"
    - "Com o Chroma offline, retrieve cai no fallback de recência e a reflexão grava só o relacional (índice stale, não corrompido)"
    - "O health-check do Chroma roda no boot e o caminho vetorial religa periodicamente"
    - "A escrita do vec0 é aposentada (consolidate E persistEvent não inserem mais em vec_events) — sem tabela órfã com write ativo"
  artifacts:
    - path: "src/cognition/reflection.ts"
      provides: "consolidate grava vetor no Chroma via chromaClient.addVector com String(id) (D-07)"
      contains: "addVector"
    - path: "src/memory/longTerm.ts"
      provides: "retrieve consulta o Chroma (KNN) em vez do vec0 quando há queryEmbedding; persistEvent não escreve mais em vec_events"
      contains: "queryVectors"
  key_links:
    - from: "consolidate (reflection.ts)"
      to: "chromaClient.addVector"
      via: "escrita do vetor consolidado após o INSERT relacional, com String(idConsolidado)"
      pattern: "addVector\\("
    - from: "retrieve (longTerm.ts)"
      to: "chromaClient.queryVectors"
      via: "KNN no Chroma → ids/distances → JOIN com events relacional"
      pattern: "queryVectors\\("
    - from: "startCognitiveLoop (loop.ts)"
      to: "chromaClient.health"
      via: "health-check no boot + re-sondagem periódica"
      pattern: "\\.health\\(\\)"
---

<objective>
Fiar o `chromaClient` (Plan 03) no caminho de memória: (1) `consolidate` (o ÚNICO ponto de embed — D-07) passa a gravar o vetor do evento consolidado no ChromaDB; (2) `retrieve` passa a consultar o Chroma (KNN) em vez do `vec0` quando há `queryEmbedding`, preservando o scoring Generative Agents; (3) health-check no boot + re-sondagem periódica (D-22); (4) confirmar que a reflexão dispara ao vivo (D-09 — Known Gap da Fase 4); (5) aposentar a escrita do `vec0` (consolidate E persistEvent) para não deixar tabela órfã com write ativo.

Purpose: Sem isto, os vetores nunca chegam ao Chroma e o `retrieve` ainda lê o `vec0` legado. `consolidate` já é o batch de embedding (1 evento/ciclo, fora do tick, sob `inFlight`) — só falta direcionar a escrita do vetor para o Chroma usando `String(id)` (Open Question 2 da pesquisa). `retrieve` já tem o scoring e o fallback de recência prontos — só troca a FONTE do KNN (vec0 → Chroma). O índice é derivado/descartável (D-01): se o Chroma falhar, o relacional segue intacto.
Output: `reflection.ts` (consolidate grava no Chroma, sem vec0), `longTerm.ts` (retrieve usa Chroma; persistEvent não escreve mais vec0), `loop.ts` (health-check boot + re-sonda), `deliberation.ts` (passa o chromaClient OPCIONAL ao runReflection).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-refatorar-mem-ria-migrar-vetores-para-chromadb-j-rodando-local-consertar-grava-o-de-eventos-lugares-garantir-uso-real-pelo-llm-mem-ria-espacial-pois-e-registro-de-morte/08.1-CONTEXT.md
@.planning/phases/08.1-refatorar-mem-ria-migrar-vetores-para-chromadb-j-rodando-local-consertar-grava-o-de-eventos-lugares-garantir-uso-real-pelo-llm-mem-ria-espacial-pois-e-registro-de-morte/08.1-RESEARCH.md
@src/memory/chromaClient.ts

<interfaces>
Do Plan 03 (chromaClient.ts):
```ts
export interface ChromaMemoryClient {
  health(): Promise<boolean>
  addVector(args: { id: number|string; embedding: number[]; metadata: Record<string,string|number|boolean>; document: string }): Promise<void>
  queryVectors(args: { queryEmbedding: number[]; nResults: number; player?: string|null }): Promise<Array<{ id: string; distance: number }>>
  isAvailable(): boolean
}
export function createChromaClient(): ChromaMemoryClient
```

`consolidate` ATUAL (reflection.ts) — INSERT em events (importance FORÇADA=8) e, hoje, em vec_events:
```ts
export function consolidate(db, recent, now, embedding: number[]|null, summary?): number|null
// retorna o id do evento consolidado (lastInsertRowid). HOJE grava o vetor em vec_events na mesma tx.
```
`retrieve` ATUAL (longTerm.ts) — quando há queryEmbedding faz `vec_events MATCH ?` + JOIN events; sem embedding cai no fallback "mais recentes". Retorna `RetrievedEvent[]` com scoring Generative Agents (min-max recência×importância×relevância).

`maybeDeliberate` (deliberation.ts L100) é o ÚNICO dispatcher: o ramo `trigger==='reflect'` chama `runReflection(holder, provider, snapshot, now)` na L119; o ramo de AÇÃO chama `decideAction`. `maybeDeliberate` é chamado em DOIS sites do loop.ts: L322 (dispatch de AÇÃO) e L360 (dispatch de 'reflect'). `runReflection` (L165) chama `consolidate(holder.db, recent, now, emb, summary)` na L209 (hoje DESCARTA o retorno). `createDeliberator` (L222) cria provider/deliberator 1x por sessão. `startCognitiveLoop(bot, holder)` tem timers `flushTimer`/`pruneTimer` (heartbeats) onde a re-sondagem do Chroma pode morar, e um cleanup `bot.once('end')`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: consolidate grava o vetor no Chroma (D-07) — id = String do evento; aposenta a escrita vec0 (consolidate + persistEvent)</name>
  <files>src/cognition/reflection.ts, src/cognition/deliberation.ts, src/memory/longTerm.ts</files>
  <read_first>
    - src/cognition/reflection.ts (consolidate inteiro — INSERT events + vec_events; CONSOLIDATION_IMPORTANCE; import EMBEDDING_DIM)
    - src/cognition/deliberation.ts (runReflection L165-216 — emb computado L201-208, consolidate chamado L209; maybeDeliberate L100-152 chama runReflection na L119; createDeliberator L222-235)
    - src/memory/longTerm.ts (persistEvent L98-130 — o ramo `if (embedding ...) INSERT INTO vec_events` L120-125; o helper `toVecBlob` L86-89)
    - src/memory/chromaClient.ts (addVector — assinatura `{ id, embedding, metadata, document }`)
    - .planning/phases/08.1-.../08.1-RESEARCH.md (§ "Open Question 2" — reconciliação de ids; § "Pitfall 2" — dim)
  </read_first>
  <action>
    Objetivo: o vetor do evento consolidado vai para o Chroma com `String(idConsolidado)`, fora da transação SQLite e SEM bloquear o relacional (D-01: relacional é source of truth; se o Chroma falhar, o evento já existe). E APOSENTA a escrita do `vec0` em AMBOS os pontos (consolidate E persistEvent) — Warning 2, opção (b): remover o INSERT em vec_events, sem deixar tabela órfã com write ativo.

    ABORDAGEM (param OPCIONAL — alinhada a D-02, menor ripple — DECISÃO EXPLÍCITA): o `chroma` é injetado como parâmetro **OPCIONAL com default null** em `maybeDeliberate`, `runReflection` (e em `retrieve`, Task 2). Quando `chroma` é null/ausente, o caminho degrada para o fallback de recência (D-02) — exatamente o comportamento "Chroma offline". Isso mantém os ~12 call sites de teste existentes (deliberation.test.ts, loop.phase3.smoke.test.ts, reflection.integration.smoke.test.ts) e os dois call sites do loop.ts compilando SEM mudança, pois nenhum precisa passar `chroma`. NÃO usar param posicional obrigatório.

    1. Em `src/cognition/reflection.ts` → `consolidate`:
       - Manter o INSERT relacional em `events` (importance forçada CONSOLIDATION_IMPORTANCE) dentro da transação e o retorno do `id`.
       - REMOVER o INSERT em `vec_events` de dentro da transação (o `vec0` sai do caminho ativo de escrita — D-01). Manter o `import EMBEDDING_DIM` SE ele ainda validar a dimensão em consolidate; se a validação de dim migrar inteiramente para o caller (runReflection valida `emb.length === EMBEDDING_DIM` antes de `addVector`), remover o import morto de reflection.ts.
       - `consolidate` continua SÍNCRONO e retorna o `id`. A ESCRITA NO CHROMA é feita pelo CALLER (`runReflection`) após o consolidate, usando o `id` retornado, o `emb` e o `summary`. Mantém o scoring/teste do consolidate intactos.
       - Atualizar o JSDoc/comentário de "DUPLICAÇÃO INTENCIONAL ... vec_events" para refletir que o vetor agora vai ao Chroma (não mais vec0).

    2. Em `src/memory/longTerm.ts` → `persistEvent` (Warning 2 — aposentar o vec0 órfão):
       - REMOVER o bloco `if (embedding && embedding.length === EMBEDDING_DIM) { INSERT INTO vec_events ... }` (L120-125). O `persistEvent` deixa de escrever no `vec0`; o vetor passa a ser responsabilidade EXCLUSIVA da reflexão (D-07: único embed). O parâmetro `embedding` da assinatura de `persistEvent` PERMANECE (compatibilidade dos call sites; vira no-op aceito) — NÃO mudar a assinatura.
       - Após a Task 2 (que também remove o `vec_events MATCH` de `retrieve`), rodar `grep "toVecBlob\|EMBEDDING_DIM" src/memory/longTerm.ts`: se 0 usos restantes, REMOVER o helper `toVecBlob` (L86-89) e o `import { EMBEDDING_DIM }` para não deixar import/função morta. Se `EMBEDDING_DIM` ainda for usado em outro lugar do arquivo, manter só o que for usado.

    3. Em `src/cognition/deliberation.ts` → `runReflection` (L165) e seu caller `maybeDeliberate` (L100):
       - Importar `ChromaMemoryClient` de `../memory/chromaClient`.
       - `runReflection`: adicionar à assinatura um parâmetro OPCIONAL `chroma: ChromaMemoryClient | null = null`.
       - Capturar o retorno do consolidate (Warning 1): trocar a L209 `if (holder.db) consolidate(holder.db, recent, now, emb, summary)` por `const idConsolidado = holder.db ? consolidate(holder.db, recent, now, emb, summary) : null`.
       - Após capturar `idConsolidado`, se `chroma && emb && summary && idConsolidado != null`:
         ```ts
         await chroma.addVector({
           id: String(idConsolidado),                 // Open Question 2: String do lastInsertRowid consolidado
           embedding: emb,                              // 768-dim, sempre local
           metadata: { type: 'reflection', ts: now, importance: 8 },
           document: summary,
         })
         ```
         `addVector` já degrada gracioso (breaker) — não precisa try/catch extra, mas manter o `await` dentro do try/catch existente do runReflection por segurança.
       - O `emb` continua vindo de `provider.embed(summary)` (LM Studio, local) exatamente como hoje (D-07: único embed).
       - `maybeDeliberate` (L100): adicionar o parâmetro OPCIONAL `chroma: ChromaMemoryClient | null = null` à assinatura e repassá-lo na chamada de `runReflection` da L119: `await runReflection(holder, provider, snapshot, now, chroma)`.
       - Atualizar o tipo do campo `maybeDeliberate` no retorno de `createDeliberator` (L222-232) para incluir o parâmetro opcional `chroma?: ChromaMemoryClient | null`. Como o param é opcional, a Task 3 só ADICIONA o argumento `chroma` no site de 'reflect' (loop.ts L360); o site de AÇÃO (loop.ts L322) compila inalterado (param opcional → null → fallback de recência, comportamento D-02).
  </action>
  <verify>
    <automated>bun test src/cognition/reflection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `reflection.ts` consolidate NÃO contém mais `INSERT INTO vec_events`
    - `consolidate` ainda retorna o `id` (lastInsertRowid) e o INSERT relacional em `events` permanece
    - `longTerm.ts` persistEvent NÃO contém mais `INSERT INTO vec_events` (Warning 2 — escrita vec0 aposentada); a assinatura de `persistEvent` mantém o parâmetro `embedding`
    - `grep "toVecBlob\|EMBEDDING_DIM" src/memory/longTerm.ts` retorna 0 após Tasks 1+2 (helper/import mortos removidos)
    - `deliberation.ts` runReflection chama `chroma.addVector({ id: String(` com `embedding: emb`, sob guarda `if (chroma && emb && summary && idConsolidado != null)`
    - `deliberation.ts` captura `const idConsolidado = holder.db ? consolidate(` na L209 (Warning 1) e a chamada de `runReflection` na L119 passa `chroma`
    - `runReflection` e `maybeDeliberate` recebem `chroma: ChromaMemoryClient | null = null` como parâmetro OPCIONAL (default null) — os call sites de teste existentes (deliberation.test.ts, loop.phase3.smoke.test.ts, reflection.integration.smoke.test.ts) e o site de AÇÃO do loop.ts L322 compilam SEM mudança
    - `bun test src/cognition/reflection.test.ts` sai com código 0 (consolidate testado sem Chroma — o vetor é responsabilidade do caller)
    - `bunx tsc --noEmit` sem erro
  </acceptance_criteria>
  <done>
    O batch de reflexão grava o vetor consolidado no Chroma com String(id); a escrita do vec0 foi aposentada em consolidate E persistEvent (sem tabela órfã com write ativo); o relacional permanece a fonte de verdade; consolidate continua síncrono e testável; o `chroma` é opcional (default null → fallback D-02), preservando todos os call sites existentes.
  </done>
</task>

<task type="auto">
  <name>Task 2: retrieve consulta o Chroma (KNN) preservando o scoring Generative Agents</name>
  <files>src/memory/longTerm.ts, src/cognition/deliberation.ts</files>
  <read_first>
    - src/memory/longTerm.ts (retrieve L156-... inteiro — KNN vec0 + fallback recência + scoring min-max; assinatura síncrona de 3 args)
    - src/memory/longTerm.test.ts (L161 e L178 — as DUAS chamadas `retrieve(db, synthEmbedding(...), now, {...})` na assinatura síncrona antiga de 3 args; L194 — `retrieve(db, null, now, {...})` fallback)
    - src/memory/chromaClient.ts (queryVectors retorna [{id: string, distance}])
    - .planning/phases/08.1-.../08.1-RESEARCH.md (§ "Pattern 2", § "Don't Hand-Roll" — reusar o scoring)
  </read_first>
  <action>
    Trocar a FONTE do KNN no `retrieve` (longTerm.ts) de `vec_events` para o Chroma, MANTENDO todo o scoring e o fallback. `retrieve` precisa de acesso ao `ChromaMemoryClient` e o `queryVectors` é assíncrono — portanto `retrieve` passa a ser `async`. O `chroma` é o 2º parâmetro (entre `db` e `queryEmbedding`).

    1. Mudar a assinatura para receber o cliente e virar async:
       ```ts
       export async function retrieve(
         db: Database,
         chroma: ChromaMemoryClient | null,
         queryEmbedding: number[] | null,
         now: number,
         opts?: { player?: string; limit?: number },
       ): Promise<RetrievedEvent[]>
       ```
       (Importar o tipo `ChromaMemoryClient` de `./chromaClient`.)

    2. No Passo 1 (com queryEmbedding válido E `chroma` disponível — `chroma && chroma.isAvailable()`): em vez do `SELECT ... FROM vec_events v JOIN events e`, fazer:
       ```ts
       const hits = await chroma.queryVectors({ queryEmbedding, nResults: config.retrievalK, player: opts?.player })
       // hits: Array<{ id: string; distance: number }> — id é String(events.id)
       if (hits.length > 0) {
         const ids = hits.map(h => Number(h.id))
         const placeholders = ids.map(() => '?').join(', ')
         const rows = db.prepare(
           `SELECT id, importance, last_access, summary, payload FROM events WHERE id IN (${placeholders})`
         ).all(...ids) as Array<{ id:number; importance:number; last_access:number; summary:string; payload:string }>
         const distById = new Map(hits.map(h => [Number(h.id), h.distance]))
         candidates = rows.map(r => ({
           id: r.id, summary: r.summary, payload: r.payload, importance: r.importance, last_access: r.last_access,
           relevance: Math.max(0, Math.min(1, 1 - (distById.get(r.id) ?? 1))),  // cosine distance → relevance clamp [0,1]
         }))
       } else {
         // Chroma disponível mas sem hits → cai no fallback de recência (igual ao caminho sem embedding).
         candidates = <usar o MESMO bloco do fallback "mais recentes" já existente>
       }
       ```
       O filtro por `player` migra do JOIN para o `where` do Chroma (D-04) — o `queryVectors` já aplica. O fallback relacional por player (Passo 3) continua usando `WHERE player = ?`.

    3. Quando `queryEmbedding` for null OU `chroma` for null/indisponível (`!chroma || !chroma.isAvailable()`): usar o fallback de recência JÁ EXISTENTE (os `retrievalK` eventos mais recentes, relevance=0). Extrair esse bloco para uma função interna reusável se ajudar a evitar duplicação entre o "sem hits" e o "sem embedding".

    4. Os Passos restantes (min-max → soma ponderada → ordena → corta → renova last_access) ficam IDÊNTICOS. NÃO mexer no scoring (Don't Hand-Roll). Manter o `try/catch` externo que degrada para `[]` e loga (NUNCA lança).

    5. Atualizar TODOS os chamadores de `retrieve` para `await` e passar o `chroma` (2º arg):
       - `deliberation.ts` runReflection (L178): `const recalled = holder.db ? await retrieve(holder.db, chroma, null, now, { limit: 5 }) : []` (o `chroma` já está disponível como parâmetro da função após Task 1; quando null → fallback de recência, que é exatamente o que essa chamada já fazia com `null` no lugar do queryEmbedding).
       - Testes em `src/memory/longTerm.test.ts` (assinatura síncrona antiga de 3 args → nova assinatura async de 4-args `(db, chroma, queryEmbedding, now, opts)`):
         - L161 `const results = retrieve(db, synthEmbedding(5), now, { limit: 3 })` → migrar para o caminho KNN via Chroma fake: `const results = await retrieve(db, fakeChroma, synthEmbedding(5), now, { limit: 3 })`, onde `fakeChroma` é um stub com `isAvailable: () => true` e `queryVectors` devolvendo `[{ id: String(target), distance: 0 }, ...]` com os ids conhecidos inseridos no teste (preferir esta opção (b) para manter cobertura do caminho KNN).
         - L178 `const results = retrieve(db, synthEmbedding(7), now, { limit: 5 })` → mesma migração: `const results = await retrieve(db, fakeChroma, synthEmbedding(7), now, { limit: 5 })` com `fakeChroma.queryVectors` devolvendo o id renovado (`{ id: String(id), distance: 0 }`); o assert de renovação de `last_access` permanece.
         - L194 `results = retrieve(db, null, now, { limit: 5 })` (fallback sem embedding) → `results = await retrieve(db, null, null, now, { limit: 5 })` (chroma=null + queryEmbedding=null → exercita o fallback de recência inalterado).
         - Definir o `fakeChroma` uma vez no topo do arquivo de teste (objeto literal satisfazendo `ChromaMemoryClient`: `health`/`addVector` no-op, `isAvailable: () => true`, `queryVectors` parametrizável por teste).
  </action>
  <verify>
    <automated>bun test src/memory/longTerm.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `longTerm.ts` retrieve é `async` e recebe `chroma: ChromaMemoryClient | null` como 2º parâmetro
    - retrieve chama `chroma.queryVectors(` quando há queryEmbedding e chroma disponível
    - retrieve NÃO contém mais `vec_events MATCH` (o vec0 saiu do caminho de leitura)
    - O scoring (min-max recência/importância/relevância, soma ponderada, renovação de last_access) permanece inalterado
    - retrieve NUNCA lança (try/catch externo degradando para [])
    - Todos os chamadores usam `await retrieve(...)` e passam o `chroma` (ou null) como 2º arg
    - `longTerm.test.ts` L161 e L178 migrados para `await retrieve(db, fakeChroma, synthEmbedding(...), now, {...})` (caminho KNN via Chroma fake); L194 migrado para `await retrieve(db, null, null, now, {...})` (fallback)
    - `bun test src/memory/longTerm.test.ts` sai com código 0 (testes ajustados ao novo contrato)
    - `bunx tsc --noEmit` sem erro
  </acceptance_criteria>
  <done>
    retrieve usa o Chroma como fonte do KNN, preserva o scoring e o fallback de recência, e degrada gracioso quando o Chroma está offline. As três chamadas de teste (L161/L178/L194) estão migradas para a assinatura async de 4 args.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire do chromaClient no loop — health-check no boot + re-sondagem periódica (D-22)</name>
  <files>src/cognition/loop.ts, src/cognition/deliberation.ts, src/memory/chromaClient.ts</files>
  <read_first>
    - src/cognition/loop.ts (startCognitiveLoop: criação de provider/deliberator; flushTimer/pruneTimer; bot.once('end') cleanup; o dispatch de AÇÃO L322 e o dispatch de 'reflect' L359-368)
    - src/cognition/deliberation.ts (maybeDeliberate/createDeliberator após Task 1 — agora recebem chroma OPCIONAL)
    - src/memory/chromaClient.ts (createChromaClient, health, isAvailable)
    - .planning/phases/08.1-.../08.1-RESEARCH.md (§ "Pattern 4", § "Pitfall 4" — reflexão precisa disparar ao vivo)
  </read_first>
  <action>
    1. Em `startCognitiveLoop` (loop.ts), criar o cliente 1x por sessão, junto de `provider`/`deliberator`:
       ```ts
       const chroma = createChromaClient()
       ```
       Import: `import { createChromaClient } from '../memory/chromaClient'`.

    2. **Health-check no boot (D-22 / Pattern 4):** após criar o `chroma`, disparar (sem bloquear o boot — usar `void` + then) um health-check que loga o status:
       ```ts
       void chroma.health().then((ok) => {
         if (ok) console.log('[chroma] online — memória vetorial habilitada')
         // se !ok, o próprio chromaClient já emitiu o aviso [chroma] OFFLINE debounced (D-22)
       })
       ```

    3. **Re-sondagem periódica (D-22 — religa sozinho):** hospedar uma re-sondagem no heartbeat já existente. Adicionar um `setInterval` (mesma cadência do `pruneTimer`, ou criar `chromaProbeTimer` com `config.chromaCooldownMs`) que chama `void chroma.health()` periodicamente — isso, combinado com o half-open do breaker, faz o caminho vetorial religar quando o Chroma volta. Declarar o timer ANTES de `bot.once('end')` e fazer `clearInterval(chromaProbeTimer)` no cleanup do `end` (junto dos outros clearInterval) — evitar listener/timer leak.

    4. **Passar o `chroma` ao deliberator APENAS no dispatch de 'reflect' (L360):** na chamada `deliberator.maybeDeliberate(deliberator.state, holder, provider, lastSnapshot, 'reflect', reflectNow)` da L360, adicionar o `chroma` como 7º argumento, conforme o param OPCIONAL da Task 1: `deliberator.maybeDeliberate(deliberator.state, holder, provider, lastSnapshot, 'reflect', reflectNow, chroma)`. O dispatch de AÇÃO (L322) NÃO muda — o param opcional default null faz a deliberação de ação seguir o caminho atual sem Chroma (a injeção de memória no caminho de AÇÃO é da Área 3/D-10, fora deste plano). O tipo do retorno de `createDeliberator` já aceita o param opcional (Task 1).

    5. **D-09 — confirmar disparo da reflexão ao vivo:** NÃO há mudança de lógica de gatilho aqui (shouldReflect já existe e o STATE registra o re-teste pós-System 1 como blocker manual ao vivo). Garantir apenas que o log `[reflect] reflexão executada` (loop.ts L365) permanece e que o caminho `'reflect'` agora carrega o `chroma` até o `addVector`. Adicionar um log dentro de runReflection (após o consolidate, na ramificação que envia ao Chroma) para tornar o disparo+gravação verificável ao vivo (complementa D-13/Pitfall 4): `console.log(\`[reflect] consolidado #\${idConsolidado} (vetor enviado ao chroma se online)\`)`.
  </action>
  <verify>
    <automated>bun test src/cognition</automated>
  </verify>
  <acceptance_criteria>
    - `loop.ts` contém `createChromaClient()` e `chroma.health()`
    - `loop.ts` declara um timer de re-sondagem e faz `clearInterval` dele no cleanup do `bot.once('end')`
    - A chamada `deliberator.maybeDeliberate(...)` no dispatch de 'reflect' (L360) passa o `chroma` como 7º argumento; o dispatch de AÇÃO (L322) permanece inalterado
    - `deliberation.ts` log de reflexão consolidada presente (`grep "\[reflect\]" src/cognition/deliberation.ts`)
    - O log `[reflect] reflexão executada` continua existindo em loop.ts
    - `bun test src/cognition` sai com código 0
    - `bunx tsc --noEmit` sem erro
  </acceptance_criteria>
  <done>
    O chromaClient é criado 1x por sessão, sonda no boot e periodicamente (religa sozinho), é entregue ao caminho de reflexão (e SÓ a ele, via param opcional no site L360), e o timer é limpo no end sem leak. O site de AÇÃO (L322) compila e roda inalterado.
  </done>
</task>

</tasks>

<verification>
- `bun test src/memory src/cognition` passa.
- `bunx tsc --noEmit` sem erro (inclui os ~12 call sites de teste de maybeDeliberate e os dois sites do loop.ts — todos compilam por o `chroma` ser opcional).
- `grep "queryVectors" src/memory/longTerm.ts` e `grep "addVector" src/cognition/deliberation.ts` retornam match.
- `grep "vec_events" src/memory/longTerm.ts src/cognition/reflection.ts` retorna 0 (vec0 saiu do caminho ativo de leitura E escrita — Warning 2 resolvido por remoção).
- (Ao vivo, manual): com `chroma run` ativo, após uma reflexão a collection `events` no Chroma tem count > 0; com o Chroma desligado, o loop segue e loga `[chroma] OFFLINE`.
</verification>

<success_criteria>
- consolidate grava o vetor consolidado no Chroma (id = String do evento) — D-07.
- retrieve consulta o Chroma (KNN) e preserva o scoring Generative Agents — Don't Hand-Roll.
- Chroma offline → fallback de recência, índice stale não corrompido (D-01/D-02).
- Health-check no boot + re-sondagem periódica (D-22); timers limpos no end.
- Escrita do vec0 aposentada (consolidate + persistEvent); nenhuma tabela órfã com write ativo.
</success_criteria>

<output>
Após completar, criar `.planning/phases/08.1-refatorar-mem-ria-migrar-vetores-para-chromadb-j-rodando-local-consertar-grava-o-de-eventos-lugares-garantir-uso-real-pelo-llm-mem-ria-espacial-pois-e-registro-de-morte/08.1-04-SUMMARY.md`
</output>
