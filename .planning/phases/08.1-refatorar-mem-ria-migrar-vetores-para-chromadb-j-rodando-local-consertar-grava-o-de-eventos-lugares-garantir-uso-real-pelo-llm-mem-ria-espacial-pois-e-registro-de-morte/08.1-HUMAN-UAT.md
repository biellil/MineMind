---
status: complete
phase: 08.1-refatorar-memoria-chromadb
source: [08.1-VERIFICATION.md]
started: 2026-06-21T00:00:00Z
updated: 2026-06-21T00:00:00Z
---

## Current Test

[all items tested — live session 2026-06-21]

## Tests

### 1. Persistência de eventos ao vivo
expected: Rodar o bot com LM Studio + servidor Chroma online por uma sessão e checar `SELECT count(*) FROM events` > 0 (persistência deixou de estar morta); logs `[recall]` aparecem no caminho de AÇÃO.
result: passed (2026-06-21 — events > 0; `[recall] score=1.89` no caminho de AÇÃO)

### 2. Round-trip consolidate → recall (Chroma online)
expected: Observar `[reflect] consolidado #<id> (vetor enviado ao chroma...)` e depois `[recall] #<id> score=...` — vetor consolidado gravado no Chroma e KNN recupera memórias reais injetadas no prompt.
result: passed (2026-06-21 — `minemind_memory` 7 vetores; `[reflect] consolidado` + `[recall]` confirmados)

### 3. Degradação do circuit breaker (Chroma offline a meio da sessão)
expected: Derrubar o Chroma durante a sessão — o loop não aborta, emite `[chroma] OFFLINE` debounced e cai no fallback de recência; relacional intacto; religa sozinho (half-open) quando o Chroma volta.
result: passed (2026-06-21 — loop seguiu sem abortar; fallback de recência; religou sozinho)

### 4. Registro de morte no jogo
expected: Matar o bot (mob/queda/lava) e checar `MemEvent type:'death'` (importância 10) + POI `danger` no local — linha `[death]` com causa inferida; `events` tem death; `places` tem POI danger no bucket do local.
result: passed (2026-06-21 — `[death]` com causa; event death registrado; POI danger no local)

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
