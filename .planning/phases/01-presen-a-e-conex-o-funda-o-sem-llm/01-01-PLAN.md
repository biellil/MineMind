---
phase: 01-presen-a-e-conex-o-funda-o-sem-llm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - bunfig.toml
  - .env.example
  - src/config.ts
  - src/perception/types.ts
autonomous: true
requirements:
  - CONN-01
  - CONN-02
  - PERC-01
  - PERC-02
  - PERC-03
  - PERC-04

must_haves:
  truths:
    - "O projeto instala dependências sem erros NAPI no Bun 1.3.x"
    - "Bun executa TypeScript nativo (`bun run src/config.ts`) sem transpilador externo"
    - "Variáveis de ambiente (host, porta, username, versão MC, raio, timeouts) são lidas de .env com defaults sensatos"
    - "O tipo `WorldSnapshot` (contrato PERC-04) existe exportado e é usado pelas fases seguintes sem modificação"
  artifacts:
    - path: "package.json"
      provides: "Definição do projeto com dependências fixas"
      contains: "mineflayer"
    - path: "tsconfig.json"
      provides: "Configuração TypeScript compatível com Bun"
      contains: "moduleResolution"
    - path: ".env.example"
      provides: "Template de configuração para desenvolvimento"
      contains: "MC_HOST"
    - path: "src/config.ts"
      provides: "Leitura de .env com defaults"
      exports: ["config"]
    - path: "src/perception/types.ts"
      provides: "Contrato WorldSnapshot — interface crítica de integração entre Fase 1 e Fases 2+"
      exports: ["WorldSnapshot", "EntityInfo", "PlayerInfo", "BlockSummary", "InventorySlot"]
  key_links:
    - from: "src/perception/types.ts"
      to: "src/perception/snapshot.ts (Plano 02)"
      via: "import type { WorldSnapshot }"
      pattern: "export interface WorldSnapshot"
    - from: "src/config.ts"
      to: "todos os módulos que leem .env"
      via: "import { config } from './config'"
      pattern: "export const config ="
---

<objective>
Bootstrap completo do projeto MineMind: estrutura de diretórios, dependências npm, configuração TypeScript/Bun, leitura de variáveis de ambiente e definição do contrato de tipos `WorldSnapshot`.

Purpose: Este plano cria a fundação sobre a qual todos os outros planos da Fase 1 constroem. Sem ele, nenhum código pode ser executado. O tipo `WorldSnapshot` definido aqui é o contrato de integração entre a camada de percepção (Fase 1) e o loop cognitivo (Fase 2+) — uma vez definido, não deve mudar sem breaking change deliberado.

Output: Projeto instalável com Bun, configurável via .env, com tipos TypeScript compilando sem erro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-CONTEXT.md
@.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inicializar projeto Bun com dependências fixas e configuração de ambiente</name>
  <read_first>
    - /root/MineMind/CLAUDE.md (regras do projeto)
    - /root/MineMind/.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-RESEARCH.md (seção "Standard Stack" e "Installation" — versões exatas)
  </read_first>
  <files>package.json, tsconfig.json, bunfig.toml, .env.example, .gitignore</files>
  <action>
Criar os arquivos de configuração do projeto na raiz /root/MineMind/.

**1. package.json** (criar do zero):
```json
{
  "name": "minemind",
  "version": "0.1.0",
  "description": "MineMind — agente autônomo persistente para Minecraft",
  "type": "module",
  "main": "src/bot/index.ts",
  "scripts": {
    "start": "bun run src/bot/index.ts",
    "dev": "bun --watch src/bot/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mineflayer": "4.37.1",
    "mineflayer-pathfinder": "2.4.5",
    "mineflayer-collectblock": "1.6.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^22.0.0"
  }
}
```

Em seguida, rodar `bun install` no diretório /root/MineMind. Observar: se houver erro `napi_register_module_v1 not found` ou similar durante o install, registrar no arquivo `NAPI_ERROR.txt` na raiz e continuar — esse erro de NAPI é o blocker A1 do STATE.md e indica fallback para Node 24.

**IMPORTANTE (D-02):** NÃO instalar `better-sqlite3`, `prismarine-viewer`, ou qualquer addon NAPI. Se o `bun install` falhar com NAPI durante install (não durante execução), documentar o erro exato.

**2. tsconfig.json** (criar na raiz):
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "resolveJsonModule": true,
    "allowImportingTsExtensions": true,
    "noEmit": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**3. bunfig.toml** (criar na raiz — configura o Bun):
```toml
[install]
# Garante que o Bun usa o lockfile e não resolve versões diferentes
exact = true

[run]
# Bun carrega .env automaticamente; bunfig documenta isso explicitamente
```

**4. .env.example** (criar na raiz — D-06):
```bash
# MineMind — Configuração de desenvolvimento local
# Copie este arquivo para .env e ajuste os valores

# Conexão com servidor Minecraft
MC_HOST=localhost
MC_PORT=25565
MC_USERNAME=MineMind
MC_VERSION=1.21.4

# Percepção — raio de blocos/entidades capturado no WorldSnapshot (D-07)
PERCEPTION_RADIUS=32

# Timeouts de skills em milissegundos (D-13)
NAVIGATE_TIMEOUT_MS=30000
DIG_TIMEOUT_MS=10000
```

**5. .gitignore** (criar na raiz):
```
node_modules/
dist/
.env
*.log
NAPI_ERROR.txt
bun.lockb
```

Após criar os arquivos, rodar `bun install` e capturar a saída. Se sucesso, anotar versão do lockfile gerado.
  </action>
  <verify>
    <automated>cd /root/MineMind && bun install --dry-run 2>&1 | head -20; test -f package.json &amp;&amp; grep -q '"mineflayer": "4.37.1"' package.json &amp;&amp; echo "PASS: package.json ok" || echo "FAIL: package.json"; test -f tsconfig.json &amp;&amp; grep -q '"moduleResolution"' tsconfig.json &amp;&amp; echo "PASS: tsconfig ok" || echo "FAIL: tsconfig.json"; test -f .env.example &amp;&amp; grep -q 'MC_HOST' .env.example &amp;&amp; echo "PASS: .env.example ok" || echo "FAIL: .env.example"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contém `"mineflayer": "4.37.1"`, `"mineflayer-pathfinder": "2.4.5"`, `"mineflayer-collectblock": "1.6.0"`, `"zod": "4.4.3"` com versões exatas (sem `^` ou `~`)
    - `bun install` conclui sem erro NAPI (se NAPI error ocorrer, criar NAPI_ERROR.txt com a mensagem exata)
    - tsconfig.json contém `"moduleResolution": "bundler"` e `"strict": true`
    - .env.example contém as 7 variáveis: MC_HOST, MC_PORT, MC_USERNAME, MC_VERSION, PERCEPTION_RADIUS, NAVIGATE_TIMEOUT_MS, DIG_TIMEOUT_MS
    - .gitignore lista `.env` (sem o `example`) para não vazar credenciais
    - NÃO existe `better-sqlite3` ou `prismarine-viewer` no package.json (grep retorna vazio)
  </acceptance_criteria>
  <done>Projeto inicializado com `bun install` bem-sucedido, todos os arquivos de configuração criados com os valores especificados acima.</done>
</task>

<task type="auto">
  <name>Task 2: Criar config.ts e WorldSnapshot types — fundação de tipos da Fase 1</name>
  <read_first>
    - /root/MineMind/.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-RESEARCH.md (seção "Configuração via .env" e "Tipos TypeScript do WorldSnapshot")
    - /root/MineMind/.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-CONTEXT.md (decisões D-06, D-07, D-08, D-09, D-10)
    - /root/MineMind/.env.example (se existir após Task 1)
  </read_first>
  <files>src/config.ts, src/perception/types.ts</files>
  <action>
Criar os dois arquivos que formam a fundação de tipos da Fase 1.

**1. src/config.ts** (criar — D-06):

```typescript
// src/config.ts
// Bun carrega .env automaticamente via process.env — sem necessidade de dotenv
// Source: https://bun.sh/docs/runtime/env

export const config = {
  // Conexão Minecraft (D-06)
  host: process.env.MC_HOST ?? 'localhost',
  port: parseInt(process.env.MC_PORT ?? '25565', 10),
  username: process.env.MC_USERNAME ?? 'MineMind',
  mcVersion: process.env.MC_VERSION ?? '1.21.4',  // D-03: 1.21.4 recomendado

  // Percepção (D-07)
  perceptionRadius: parseInt(process.env.PERCEPTION_RADIUS ?? '32', 10),

  // Timeouts de skills em ms (D-13, Claude's discretion: 30s navigate, 10s dig)
  navigateTimeoutMs: parseInt(process.env.NAVIGATE_TIMEOUT_MS ?? '30000', 10),
  digTimeoutMs: parseInt(process.env.DIG_TIMEOUT_MS ?? '10000', 10),

  // Reconexão
  reconnectDelayMs: 5_000,  // 5s fixo — não configurável via .env (low-risk)
} as const

// Validação de sanidade em startup
if (config.perceptionRadius < 1 || config.perceptionRadius > 128) {
  throw new Error(`PERCEPTION_RADIUS inválido: ${config.perceptionRadius}. Deve ser entre 1 e 128.`)
}
if (config.port < 1 || config.port > 65535) {
  throw new Error(`MC_PORT inválido: ${config.port}. Deve ser entre 1 e 65535.`)
}
```

**2. src/perception/types.ts** (criar — PERC-04, D-08, D-09, D-10):

Este é o contrato de interface crítico entre a Fase 1 e as Fases 2+. Usar `readonly` em tudo para garantir imutabilidade no nível TypeScript (complementa o `structuredClone` + `Object.freeze` do runtime).

```typescript
// src/perception/types.ts
// Contrato WorldSnapshot — interface PERC-04
// ATENÇÃO: mudanças aqui são breaking changes para as Fases 2, 3 e 4.
// Qualquer alteração deve ser deliberada e documentada em STATE.md.

/** Posição 3D imutável em coordenadas do mundo Minecraft */
export interface Position3D {
  readonly x: number
  readonly y: number
  readonly z: number
}

/** Info de bloco resumida (D-07: não serializa individualmente — apenas tipos relevantes) */
export interface BlockSummary {
  /** Número total de blocos deste tipo encontrados no raio de percepção */
  readonly count: number
  /** Até 3 exemplos de posição para o LLM/loop usar como referência */
  readonly examples: ReadonlyArray<Position3D>
}

/** Slot de inventário completo (D-09) */
export interface InventorySlot {
  readonly slot: number
  readonly name: string
  readonly type: number
  readonly count: number
  readonly metadata: number
  readonly nbt: unknown  // NBT data raw (enchantments etc)
}

/** Entidade no raio de percepção (D-08) */
export interface EntityInfo {
  readonly id: number
  readonly type: string        // 'player' | 'mob' | 'object' | etc
  readonly name: string        // username (player) ou name/type (mob)
  readonly position: Position3D
  readonly distance: number    // metros até o bot
  readonly health: number | null   // null se não disponível via API
  readonly metadata: unknown   // dados extras do protocolo Minecraft
}

/** Jogador no raio de percepção (D-08) */
export interface PlayerInfo {
  readonly username: string
  readonly displayName: string
  readonly gamemode: number
  readonly ping: number
  readonly position: Position3D | null  // null se entidade não carregada no chunk
  readonly distance: number | null       // null se posição indisponível
}

/** Status do próprio bot (PERC-01) */
export interface BotStatus {
  readonly health: number       // 0–20
  readonly food: number         // 0–20
  readonly position: Position3D
  readonly timeOfDay: number    // 0.0 (meia-noite) a 1.0 (meia-noite seguinte); < 0.5 = dia
  readonly isDay: boolean
}

/**
 * WorldSnapshot — snapshot imutável do estado do mundo (PERC-04)
 *
 * Criado sob demanda por buildWorldSnapshot(bot).
 * A camada cognitiva NUNCA recebe referência ao objeto bot — apenas este snapshot.
 * Todos os campos são readonly; o objeto é também deep-frozen no runtime (D-10).
 */
export interface WorldSnapshot {
  /** Timestamp Unix (ms) do momento da captura */
  readonly capturedAt: number

  /** Status do próprio bot (PERC-01) */
  readonly status: BotStatus

  /** Entidades não-jogadoras no raio de percepção, ordenadas por distância (PERC-02) */
  readonly entities: ReadonlyArray<EntityInfo>

  /** Jogadores no raio de percepção (PERC-02) */
  readonly players: ReadonlyArray<PlayerInfo>

  /**
   * Tipos de bloco encontrados no raio, com contagem e exemplos de posição (PERC-02, D-07)
   * Chave: nome do bloco (ex: "oak_log", "stone", "diamond_ore")
   * Valor: contagem + até 3 posições de exemplo
   */
  readonly nearbyBlockTypes: Readonly<Record<string, BlockSummary>>

  /** Inventário completo slot-a-slot (PERC-03, D-09) */
  readonly inventory: ReadonlyArray<InventorySlot>
}
```

Após criar os arquivos, rodar `bun run --bun tsc --noEmit` (ou `bun typecheck`) para confirmar que os tipos compilam sem erro.
  </action>
  <verify>
    <automated>cd /root/MineMind && test -f src/config.ts &amp;&amp; grep -q "export const config" src/config.ts &amp;&amp; echo "PASS: config.ts ok" || echo "FAIL: config.ts"; test -f src/perception/types.ts &amp;&amp; grep -q "export interface WorldSnapshot" src/perception/types.ts &amp;&amp; echo "PASS: types.ts ok" || echo "FAIL: types.ts"; grep -q "export interface EntityInfo" src/perception/types.ts &amp;&amp; echo "PASS: EntityInfo ok" || echo "FAIL: EntityInfo"; grep -q "export interface InventorySlot" src/perception/types.ts &amp;&amp; echo "PASS: InventorySlot ok" || echo "FAIL: InventorySlot"; grep -q "readonly nbt" src/perception/types.ts &amp;&amp; echo "PASS: nbt field ok" || echo "FAIL: nbt field"</automated>
  </verify>
  <acceptance_criteria>
    - src/config.ts contém `export const config =` com os campos: host, port, username, mcVersion, perceptionRadius, navigateTimeoutMs, digTimeoutMs, reconnectDelayMs
    - src/config.ts contém validação de PERCEPTION_RADIUS (lança Error se fora de 1–128)
    - src/config.ts usa `process.env.MC_VERSION ?? '1.21.4'` (valor default explícito de 1.21.4 — D-03)
    - src/perception/types.ts exporta: `WorldSnapshot`, `BotStatus`, `EntityInfo`, `PlayerInfo`, `BlockSummary`, `InventorySlot`, `Position3D`
    - Interface WorldSnapshot tem os campos: `capturedAt`, `status`, `entities`, `players`, `nearbyBlockTypes`, `inventory` — todos `readonly`
    - Interface EntityInfo contém `health: number | null` (D-08: health se disponível)
    - Interface InventorySlot contém `nbt: unknown` (D-09: metadata/enchantments)
    - `bun run typecheck` (ou `bun x tsc --noEmit`) retorna exit code 0 (zero erros de tipo)
  </acceptance_criteria>
  <done>src/config.ts e src/perception/types.ts criados, TypeScript compila sem erros, WorldSnapshot é o contrato oficial de integração entre camadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → config.ts | Valores externos não validados entram como strings brutas |
| Desenvolvedor → package.json | Dependências adicionadas manualmente podem incluir addons NAPI proibidos (D-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | src/config.ts — leitura de .env | mitigate | Validar PERCEPTION_RADIUS (1–128) e MC_PORT (1–65535) com throws explícitos no startup. Valores fora do range são rejeitados antes de qualquer conexão. |
| T-01-02 | Tampering | package.json — adição de dependências | mitigate | Adicionar `"trustedDependencies": []` no package.json (Bun feature) para restringir quais pacotes podem rodar lifecycle scripts. Documentar em .gitignore que bun.lockb deve ser commitado para reproducibilidade. |
| T-01-03 | Elevation of Privilege | MC_USERNAME em .env — username arbitrário no servidor | accept | Servidor offline-mode em dev local (D-05). Qualquer username é aceito intencionalmente. Documentado — não usar em produção com online-mode. |
| T-01-04 | Information Disclosure | .env com credenciais sendo versionado | mitigate | .gitignore inclui `.env` (sem o `example`). Apenas `.env.example` (sem valores reais) é commitado. |
</threat_model>

<verification>
Verificação geral do Plano 01:

1. `bun install` completa sem erros NAPI: `cd /root/MineMind && bun install 2>&1`
2. Dependências fixas (sem `^` ou `~`) presentes: `grep -E '"(mineflayer|zod)"' package.json`
3. Proibições D-02 ausentes: `grep -E 'better-sqlite3|prismarine-viewer' package.json` retorna vazio
4. Tipos compilam: `bun x tsc --noEmit` retorna exit 0
5. WorldSnapshot exportado: `grep "export interface WorldSnapshot" src/perception/types.ts`
6. Config com defaults: `grep "1.21.4" src/config.ts` confirma valor default D-03
</verification>

<success_criteria>
- `bun install` sem erros críticos (NAPI error → NAPI_ERROR.txt criado, blocker registrado)
- `bun run typecheck` retorna exit 0
- WorldSnapshot interface exportada com todos os subcampos readonly
- .env.example com 7 variáveis de configuração documentadas
- NÃO há better-sqlite3 ou prismarine-viewer em package.json
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-01-SUMMARY.md` com:
- Resultado do `bun install` (sucesso ou erro NAPI registrado)
- Lista de arquivos criados
- Interfaces exportadas de src/perception/types.ts
- Qualquer desvio das decisões D-01 a D-06
</output>
