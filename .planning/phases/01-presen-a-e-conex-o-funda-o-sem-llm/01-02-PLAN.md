---
phase: 01-presen-a-e-conex-o-funda-o-sem-llm
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01-PLAN.md"
files_modified:
  - src/bot/connection.ts
  - src/bot/index.ts
  - src/perception/snapshot.ts
autonomous: true
requirements:
  - CONN-01
  - CONN-02
  - PERC-01
  - PERC-02
  - PERC-03
  - PERC-04

must_haves:
  truths:
    - "O bot se conecta ao servidor Minecraft local e emite o evento `spawn` (CONN-01)"
    - "Ao desconectar (kick ou queda de rede), o bot reconecta automaticamente em 5s criando uma sessão nova (CONN-02)"
    - "Chamando buildWorldSnapshot(bot) após spawn, retorna um objeto com status, entities, players, nearbyBlockTypes e inventory (PERC-01 a PERC-04)"
    - "O snapshot retornado é imutável: tentativas de modificar campos lançam TypeError em modo strict (D-10)"
  artifacts:
    - path: "src/bot/connection.ts"
      provides: "createBot() com reconexão automática e lifecycle events"
      exports: ["createBot"]
    - path: "src/bot/index.ts"
      provides: "Entry point — monta bot, carrega pathfinder, expõe snapshot"
      exports: ["main"]
    - path: "src/perception/snapshot.ts"
      provides: "buildWorldSnapshot(bot) — cria WorldSnapshot imutável"
      exports: ["buildWorldSnapshot"]
  key_links:
    - from: "src/bot/connection.ts"
      to: "mineflayer.createBot()"
      via: "bot.on('end', setTimeout(createBot, reconnectDelayMs))"
      pattern: "bot.on.*end.*createBot"
    - from: "src/perception/snapshot.ts"
      to: "src/perception/types.ts"
      via: "import type { WorldSnapshot }"
      pattern: "import.*WorldSnapshot.*from.*types"
    - from: "src/bot/index.ts"
      to: "mineflayer-pathfinder"
      via: "bot.loadPlugin(pathfinder)"
      pattern: "loadPlugin.*pathfinder"
---

<objective>
Implementar a camada de conexão (CONN-01, CONN-02) e percepção (PERC-01 a PERC-04) do MineMind: o bot conecta ao servidor Minecraft Java local, mantém-se vivo com reconexão automática, e expõe um snapshot imutável do estado do mundo.

Purpose: Com este plano, o agente é capaz de "existir" no mundo Minecraft — conectado, resiliente a quedas, e capaz de observar o ambiente. Estes são os dois pré-requisitos para qualquer skill (Plano 03) e para o loop cognitivo (Fase 2).

Output: src/bot/connection.ts, src/bot/index.ts, src/perception/snapshot.ts funcionando. `bun run start` conecta ao servidor e emite log de spawn.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-CONTEXT.md
@.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-RESEARCH.md
@.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-01-SUMMARY.md

<interfaces>
<!-- Tipos e contratos disponíveis após Plano 01 -->
<!-- Executor não precisa explorar o codebase — usar estas definições diretamente -->

De src/perception/types.ts:
```typescript
export interface Position3D {
  readonly x: number; readonly y: number; readonly z: number
}
export interface BlockSummary {
  readonly count: number
  readonly examples: ReadonlyArray<Position3D>
}
export interface InventorySlot {
  readonly slot: number; readonly name: string; readonly type: number
  readonly count: number; readonly metadata: number; readonly nbt: unknown
}
export interface EntityInfo {
  readonly id: number; readonly type: string; readonly name: string
  readonly position: Position3D; readonly distance: number
  readonly health: number | null; readonly metadata: unknown
}
export interface PlayerInfo {
  readonly username: string; readonly displayName: string
  readonly gamemode: number; readonly ping: number
  readonly position: Position3D | null; readonly distance: number | null
}
export interface BotStatus {
  readonly health: number; readonly food: number
  readonly position: Position3D; readonly timeOfDay: number; readonly isDay: boolean
}
export interface WorldSnapshot {
  readonly capturedAt: number
  readonly status: BotStatus
  readonly entities: ReadonlyArray<EntityInfo>
  readonly players: ReadonlyArray<PlayerInfo>
  readonly nearbyBlockTypes: Readonly<Record<string, BlockSummary>>
  readonly inventory: ReadonlyArray<InventorySlot>
}
```

De src/config.ts:
```typescript
export const config = {
  host: string,
  port: number,
  username: string,
  mcVersion: string,        // '1.21.4' por padrão
  perceptionRadius: number, // 32 por padrão
  navigateTimeoutMs: number,
  digTimeoutMs: number,
  reconnectDelayMs: number, // 5000ms fixo
} as const
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar conexão e reconexão automática com Mineflayer</name>
  <read_first>
    - /root/MineMind/src/config.ts (já criado — valores de conexão)
    - /root/MineMind/.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-RESEARCH.md (Pattern 1: Conexão e Reconexão, Pitfall 3: Memory leak de listeners)
    - /root/MineMind/.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-01-SUMMARY.md (resultado do bun install — confirmar se Bun funcionou)
  </read_first>
  <files>src/bot/connection.ts, src/bot/index.ts</files>
  <action>
**Contexto crítico (PITFALL 3):** NÃO registrar `bot.on('end', createBot)` em escopo global. Cada listener deve viver dentro do escopo da função `createBot()` para ser GC'd junto com a instância de bot morta. O padrão correto é criar o callback de reconexão dentro da função.

**1. src/bot/connection.ts** — fábrica de bots com reconexão automática (CONN-01, CONN-02):

```typescript
// src/bot/connection.ts
// Padrão baseado no exemplo canônico:
// https://github.com/PrismarineJS/mineflayer/blob/master/examples/reconnector.js
import mineflayer, { type Bot } from 'mineflayer'
import { pathfinder, Movements } from 'mineflayer-pathfinder'
import { config } from '../config'

export type BotReadyCallback = (bot: Bot) => void

/**
 * Cria uma instância de bot Mineflayer com reconexão automática.
 * CONN-01: conecta e permanece online.
 * CONN-02: ao cair, cria sessão de bot NOVA (não reutiliza a instância morta).
 *
 * @param onReady - Callback chamado após spawn bem-sucedido.
 *                  Recebe a instância de bot com pathfinder já carregado.
 */
export function createBot(onReady?: BotReadyCallback): void {
  const bot = mineflayer.createBot({
    host: config.host,
    port: config.port,
    username: config.username,
    version: config.mcVersion,  // D-03: '1.21.4' por padrão
    auth: 'offline',            // D-05: offline-mode para desenvolvimento local
  })

  bot.once('spawn', () => {
    // Carregar pathfinder após spawn (ACT-01 — usado pelo navigate skill no Plano 03)
    bot.loadPlugin(pathfinder)
    const movements = new Movements(bot)
    movements.canDig = true           // permite mineração durante navegação
    movements.allowSprinting = true   // sprinting é comportamento humano normal
    bot.pathfinder.setMovements(movements)

    console.log(
      `[MineMind] Online — ${config.host}:${config.port} | ` +
      `HP: ${bot.health} | Pos: ${Math.round(bot.entity.position.x)},` +
      `${Math.round(bot.entity.position.y)},${Math.round(bot.entity.position.z)}`
    )

    onReady?.(bot)
  })

  bot.on('error', (err: Error) => {
    // Não recriar aqui — 'end' é sempre emitido após 'error'
    console.error(`[MineMind] Erro de conexão: ${err.message}`)
  })

  bot.on('kicked', (reason: string) => {
    console.warn(`[MineMind] Kicked pelo servidor: ${reason}`)
  })

  // CONN-02: reconexão automática — listener registrado DENTRO do escopo de createBot()
  // para garantir que seja GC'd junto com a instância morta (evita memory leak, PITFALL 3)
  bot.on('end', (reason: string) => {
    console.log(
      `[MineMind] Desconectado: "${reason}". ` +
      `Reconectando em ${config.reconnectDelayMs / 1000}s...`
    )
    // Cria instância NOVA — não reutiliza 'bot' (referência sai de escopo após 'end')
    setTimeout(() => createBot(onReady), config.reconnectDelayMs)
  })
}
```

**2. src/bot/index.ts** — entry point do agente:

```typescript
// src/bot/index.ts
// Entry point do MineMind — inicia a conexão e o loop de vida do bot
import type { Bot } from 'mineflayer'
import { createBot } from './connection'
import { buildWorldSnapshot } from '../perception/snapshot'

/**
 * Callback chamado sempre que o bot está pronto (após spawn ou reconexão).
 * Na Fase 1: apenas demonstra a percepção capturando um snapshot inicial.
 * Na Fase 2: este callback será substituído pelo loop do StateGraph.
 */
function onBotReady(bot: Bot): void {
  // Demonstra PERC-01 a PERC-04: snapshot inicial após spawn
  // Pitfall 5: bot.inventory só está populado APÓS o spawn — ok aqui pois
  // onBotReady é chamado dentro do handler 'once spawn'
  const snapshot = buildWorldSnapshot(bot)

  console.log('[MineMind] Snapshot inicial capturado:')
  console.log(`  Status: HP ${snapshot.status.health} | Food ${snapshot.status.food} | Dia: ${snapshot.status.isDay}`)
  console.log(`  Entidades próximas: ${snapshot.entities.length}`)
  console.log(`  Jogadores próximos: ${snapshot.players.length}`)
  console.log(`  Tipos de bloco no raio: ${Object.keys(snapshot.nearbyBlockTypes).length}`)
  console.log(`  Inventário slots: ${snapshot.inventory.length}`)

  // Verificação de imutabilidade (D-10) — apenas em desenvolvimento
  if (process.env.NODE_ENV !== 'production') {
    try {
      // @ts-expect-error — teste de imutabilidade intencional
      ;(snapshot as any).capturedAt = 0
      console.warn('[MineMind] AVISO: snapshot NÃO é imutável! Verificar Object.freeze.')
    } catch {
      console.log('[MineMind] Snapshot imutável confirmado (Object.freeze funcionando).')
    }
  }
}

// Iniciar o agente
console.log(`[MineMind] Iniciando... Conectando a ${process.env.MC_HOST ?? 'localhost'}:${process.env.MC_PORT ?? '25565'}`)
createBot(onBotReady)
```
  </action>
  <verify>
    <automated>cd /root/MineMind && test -f src/bot/connection.ts &amp;&amp; grep -q "export function createBot" src/bot/connection.ts &amp;&amp; echo "PASS: createBot exportado" || echo "FAIL: createBot"; grep -q "auth: 'offline'" src/bot/connection.ts &amp;&amp; echo "PASS: offline-mode ok" || echo "FAIL: offline-mode"; grep -q "bot.on.*end" src/bot/connection.ts &amp;&amp; echo "PASS: reconexão registrada" || echo "FAIL: sem reconexão"; grep -q "bot.loadPlugin(pathfinder)" src/bot/connection.ts &amp;&amp; echo "PASS: pathfinder carregado" || echo "FAIL: pathfinder"; test -f src/bot/index.ts &amp;&amp; grep -q "createBot" src/bot/index.ts &amp;&amp; echo "PASS: index.ts ok" || echo "FAIL: index.ts"</automated>
  </verify>
  <acceptance_criteria>
    - src/bot/connection.ts exporta `createBot(onReady?: BotReadyCallback): void`
    - `createBot` usa `auth: 'offline'` (D-05 — nunca 'microsoft' ou 'mojang')
    - `bot.on('end', ...)` registrado DENTRO da função createBot (não em escopo global)
    - `setTimeout(() => createBot(onReady), config.reconnectDelayMs)` presente no handler 'end'
    - `bot.loadPlugin(pathfinder)` chamado dentro do handler `bot.once('spawn', ...)`
    - `new Movements(bot)` com `canDig = true` e `allowSprinting = true` configurados
    - src/bot/index.ts importa `buildWorldSnapshot` e a chama dentro de `onBotReady(bot)`
    - Nenhuma referência ao objeto `bot` vaza para escopo global (nenhuma variável `let bot` em módulo-level)
  </acceptance_criteria>
  <done>Bot se conecta ao servidor local, emite log de spawn com HP e posição, e registra reconexão automática sem memory leak.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar buildWorldSnapshot — percepção imutável do mundo</name>
  <read_first>
    - /root/MineMind/src/perception/types.ts (contrato WorldSnapshot — NÃO modificar esta interface)
    - /root/MineMind/src/config.ts (config.perceptionRadius — raio de blocos)
    - /root/MineMind/.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-RESEARCH.md (Pattern 2: WorldSnapshot Imutável, Pitfall 4: findBlocks com raio grande, Pitfall 5: inventory vazio antes de spawn)
    - /root/MineMind/.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-CONTEXT.md (D-07, D-08, D-09, D-10)
  </read_first>
  <files>src/perception/snapshot.ts</files>
  <action>
**Contexto crítico:**
- PITFALL 4: `bot.findBlocks()` é síncrono e pode bloquear o event loop. Usar `maxDistance: config.perceptionRadius` (máx 32) e `count: 200` para limitar.
- PITFALL 5: Só chamar `buildWorldSnapshot()` APÓS o evento `spawn` ter disparado.
- D-10: Usar `structuredClone()` para garantir cópia profunda, depois `Object.freeze` para imutabilidade. `Object.freeze` sozinho é raso.

**src/perception/snapshot.ts**:

```typescript
// src/perception/snapshot.ts
// PERC-01 a PERC-04: captura snapshot imutável do estado do mundo
// CHAMAR APENAS após bot.once('spawn') ter disparado (PITFALL 5)
import type { Bot } from 'mineflayer'
import type {
  WorldSnapshot,
  EntityInfo,
  PlayerInfo,
  InventorySlot,
  BlockSummary,
  Position3D,
} from './types'
import { config } from '../config'

/**
 * Captura um snapshot imutável do estado atual do mundo.
 * PERC-04: retorna um objeto plain sem nenhuma referência ao objeto bot.
 *
 * @param bot - Instância de bot Mineflayer (deve ter passado pelo evento 'spawn')
 * @returns WorldSnapshot imutável (structuredClone + Object.freeze)
 */
export function buildWorldSnapshot(bot: Bot): WorldSnapshot {
  const pos = bot.entity.position

  // === PERC-01: Status do próprio bot ===
  const status = {
    health: bot.health,
    food: bot.food,
    position: { x: pos.x, y: pos.y, z: pos.z } satisfies Position3D,
    timeOfDay: bot.time.timeOfDay,
    isDay: bot.time.timeOfDay < 0.5,
  }

  // === PERC-02: Entidades próximas (D-08: tipo, posição, distância, vida, metadata) ===
  const entities: EntityInfo[] = Object.values(bot.entities)
    .filter((e) => e !== bot.entity)  // excluir o próprio bot
    .map((e) => {
      const distance = pos.distanceTo(e.position)
      return {
        id: e.id,
        type: e.type,
        name: (e as any).username ?? (e as any).name ?? e.type,
        position: { x: e.position.x, y: e.position.y, z: e.position.z },
        distance,
        health: (e as any).health ?? null,
        metadata: (e as any).metadata ?? null,
      }
    })
    .filter((e) => e.distance <= config.perceptionRadius)
    .sort((a, b) => a.distance - b.distance)

  // === PERC-02: Jogadores próximos ===
  const players: PlayerInfo[] = Object.values(bot.players)
    .filter((p) => p.entity != null && p.username !== bot.username)
    .map((p) => {
      const entityPos = p.entity?.position
      const distance = entityPos ? pos.distanceTo(entityPos) : null
      return {
        username: p.username,
        displayName: p.displayName ?? p.username,
        gamemode: p.gamemode,
        ping: p.ping,
        position: entityPos
          ? { x: entityPos.x, y: entityPos.y, z: entityPos.z }
          : null,
        distance,
      }
    })

  // === PERC-02: Tipos de bloco no raio (D-07: resumo por tipo, não serialização individual) ===
  // PITFALL 4: count: 200 limita a iteração; maxDistance usa o raio configurado
  const blockTypeMap = new Map<string, { count: number; examples: Position3D[] }>()

  const blockPositions = bot.findBlocks({
    maxDistance: config.perceptionRadius,
    count: 200,
    matching: (block) => block.type !== 0,  // excluir ar (type 0)
  })

  for (const bpos of blockPositions) {
    const block = bot.blockAt(bpos)
    if (!block) continue
    const name = block.name
    if (!blockTypeMap.has(name)) {
      blockTypeMap.set(name, { count: 0, examples: [] })
    }
    const entry = blockTypeMap.get(name)!
    entry.count++
    if (entry.examples.length < 3) {
      // Guardar até 3 exemplos de posição por tipo (D-07)
      entry.examples.push({ x: bpos.x, y: bpos.y, z: bpos.z })
    }
  }

  const nearbyBlockTypes: Record<string, BlockSummary> = Object.fromEntries(blockTypeMap)

  // === PERC-03: Inventário slot-a-slot (D-09) ===
  const inventory: InventorySlot[] = bot.inventory.items().map((item) => ({
    slot: item.slot,
    name: item.name,
    type: item.type,
    count: item.count,
    metadata: item.metadata,
    nbt: (item as any).nbt ?? null,
  }))

  // === D-10: Montar snapshot e torná-lo imutável ===
  // structuredClone: cria cópia profunda sem NENHUMA referência ao objeto bot
  // Object.freeze: torna o objeto e seus filhos diretos somente-leitura em runtime
  const raw: WorldSnapshot = {
    capturedAt: Date.now(),
    status,
    entities,
    players,
    nearbyBlockTypes,
    inventory,
  }

  // structuredClone garante cópia profunda (zero compartilhamento com bot)
  // Object.freeze garante erro ao tentar modificar (D-10)
  return Object.freeze(structuredClone(raw)) as WorldSnapshot
}
```
  </action>
  <verify>
    <automated>cd /root/MineMind && test -f src/perception/snapshot.ts &amp;&amp; grep -q "export function buildWorldSnapshot" src/perception/snapshot.ts &amp;&amp; echo "PASS: buildWorldSnapshot exportado" || echo "FAIL: buildWorldSnapshot"; grep -q "structuredClone" src/perception/snapshot.ts &amp;&amp; echo "PASS: structuredClone presente" || echo "FAIL: sem structuredClone"; grep -q "Object.freeze" src/perception/snapshot.ts &amp;&amp; echo "PASS: Object.freeze presente" || echo "FAIL: sem Object.freeze"; grep -q "config.perceptionRadius" src/perception/snapshot.ts &amp;&amp; echo "PASS: raio configurável ok" || echo "FAIL: raio hardcoded"; grep -q "count: 200" src/perception/snapshot.ts &amp;&amp; echo "PASS: limit count ok" || echo "FAIL: sem limit count"</automated>
  </verify>
  <acceptance_criteria>
    - src/perception/snapshot.ts exporta `buildWorldSnapshot(bot: Bot): WorldSnapshot`
    - Função usa `structuredClone(raw)` seguido de `Object.freeze(...)` — nessa ordem — para imutabilidade profunda (D-10)
    - `bot.findBlocks({ maxDistance: config.perceptionRadius, count: 200, ... })` — raio configurável e count limitado a 200 (D-07, PITFALL 4)
    - Cada entidade tem: `id`, `type`, `name`, `position`, `distance`, `health: number | null`, `metadata` (D-08)
    - Cada slot de inventário tem: `slot`, `name`, `type`, `count`, `metadata`, `nbt` (D-09)
    - Nenhum campo do resultado é referência direta ao objeto `bot` (verificar ausência de `bot.entity`, `bot.inventory` no retorno)
    - `bun run typecheck` retorna exit code 0 (sem erros de tipo novos)
  </acceptance_criteria>
  <done>buildWorldSnapshot(bot) retorna WorldSnapshot imutável com status, entities, players, nearbyBlockTypes e inventory, sem nenhuma referência ao objeto bot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Servidor Minecraft → bot | Dados de entidades, blocos, jogadores vêm do servidor — podem conter valores inesperados |
| bot.entities → WorldSnapshot | Conversão de tipos dinâmicos do Mineflayer para tipos TS fortemente tipados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | buildWorldSnapshot — dados de entidade com `(e as any).health` | mitigate | Usar `?? null` como fallback para qualquer campo opcional — nunca assumir que campos existem. O tipo `health: number | null` força o consumidor a checar nulidade antes de usar. |
| T-02-02 | Denial of Service | bot.findBlocks() bloqueando event loop | mitigate | `count: 200` limita a iteração. `maxDistance: config.perceptionRadius` (padrão 32). Documentado em PITFALL 4 do RESEARCH.md. |
| T-02-03 | Information Disclosure | WorldSnapshot expondo dados de jogadores (posição, ping, gamemode) | accept | Servidor local de desenvolvimento (D-04). Em produção, filtrar jogadores por confiança — fora do escopo Fase 1. |
| T-02-04 | Elevation of Privilege | Listeners acumulando em reconexões (memory leak) | mitigate | `bot.on('end', ...)` registrado dentro de `createBot()` — cada instância tem seus próprios listeners que são GC'd após 'end'. Pattern 1 do RESEARCH.md seguido. |
</threat_model>

<verification>
Verificação geral do Plano 02:

1. Arquivo de conexão correto: `grep -c "bot.on.*end" src/bot/connection.ts` → deve retornar 1 (um único handler 'end')
2. Imutabilidade profunda: `grep -A2 "structuredClone" src/perception/snapshot.ts` → deve mostrar `Object.freeze` logo após
3. Raio configurável: `grep "config.perceptionRadius" src/perception/snapshot.ts` → presente
4. Offline-mode: `grep "auth.*offline" src/bot/connection.ts` → presente
5. TypeCheck: `bun x tsc --noEmit` → exit 0
6. Teste de integração (requer servidor MC ativo): `timeout 15 bun run start` → deve imprimir "[MineMind] Online" e "[MineMind] Snapshot inicial capturado"
</verification>

<success_criteria>
- `bun run start` com servidor MC ativo imprime "[MineMind] Online" e snapshot initial com campos de percepção
- Ao desconectar o servidor (Ctrl+C no servidor), bot imprime mensagem de desconexão e agenda reconexão
- `buildWorldSnapshot(bot)` retorna objeto imutável: tentar setar campo lança erro
- TypeScript compila sem erros (`bun run typecheck` → exit 0)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-02-SUMMARY.md` com:
- Resultado do teste de conexão (`bun run start` funcionou? Bun ou Node?)
- Resultado do teste de reconexão
- Resultado do snapshot (campos presentes?)
- Se Bun teve problema NAPI, registrar e indicar fallback para Node 24
</output>
