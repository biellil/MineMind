---
phase: 01-presen-a-e-conex-o-funda-o-sem-llm
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01-PLAN.md"
  - "01-02-PLAN.md"
files_modified:
  - src/skills/executor.ts
  - src/skills/navigate.ts
  - src/skills/dig.ts
  - src/skills/follow.ts
  - src/skills/attack.ts
  - src/skills/index.ts
  - src/bot/connection.ts
autonomous: true
requirements:
  - ACT-01
  - ACT-02
  - ACT-03
  - ACT-04
  - ACT-05

must_haves:
  truths:
    - "O executor centralizado aplica timeout (navigate: 30s, dig: 10s) e o skill falha com erro descritivo em vez de travar (ACT-03)"
    - "O executor detecta ausência de progresso via polling de posição e interrompe a skill se o bot ficar parado além do tolerado (ACT-03)"
    - "Cada ação é precedida e seguida por um delay com distribuição gaussiana — ritmo humanizado para evitar kick por velocidade (ACT-04)"
    - "navigate() e dig() funcionam como funções TypeScript e cada skill tem schema Zod que será consumido pelo LangGraph na Fase 3 sem refatoração (ACT-05)"
    - "follow() e attack() existem como stubs que retornam erro imediato — contratos Zod presentes para Fase 3 (D-12)"
  artifacts:
    - path: "src/skills/executor.ts"
      provides: "SkillExecutor com timeout, watchdog de progresso e humanized delay"
      exports: ["executeWithSafety", "gaussianDelay"]
    - path: "src/skills/navigate.ts"
      provides: "navigate() + NavigateSchema (Zod)"
      exports: ["navigate", "NavigateSchema", "navigateTool"]
    - path: "src/skills/dig.ts"
      provides: "dig() + DigSchema (Zod)"
      exports: ["dig", "DigSchema", "digTool"]
    - path: "src/skills/follow.ts"
      provides: "follow() stub + FollowSchema (Zod)"
      exports: ["follow", "FollowSchema", "followTool"]
    - path: "src/skills/attack.ts"
      provides: "attack() stub + AttackSchema (Zod)"
      exports: ["attack", "AttackSchema", "attackTool"]
    - path: "src/skills/index.ts"
      provides: "Registry de todas as skills e tools"
      exports: ["skillRegistry", "toolRegistry"]
  key_links:
    - from: "src/skills/navigate.ts"
      to: "src/skills/executor.ts"
      via: "executeWithSafety(action, { timeoutMs: config.navigateTimeoutMs, ... })"
      pattern: "executeWithSafety"
    - from: "src/skills/index.ts"
      to: "src/bot/connection.ts"
      via: "expõe skillRegistry para uso no onBotReady (Fase 2+)"
      pattern: "export.*skillRegistry"
    - from: "NavigateSchema / DigSchema"
      to: "Fase 3 LangGraph tools"
      via: "NavigateSchema.toJSONSchema() — Zod v4 built-in"
      pattern: "z.object.*describe"
---

<objective>
Implementar o executor centralizado de skills e as 4 skills da Fase 1: navigate, dig (reais), follow e attack (stubs). Todas as skills têm timeout, watchdog de progresso, ritmo humanizado e schemas Zod prontos para Fase 3.

Purpose: Este plano completa a "espinha dorsal motora" do agente — sem ele, o bot pode perceber o mundo mas não agir sobre ele. O executor centralizado (ACT-03, ACT-04) é um contrato crítico: as Fases 2 e 3 reutilizam o mesmo executor sem modificação.

Output: 6 arquivos de skills. `bun run start` com servidor ativo demonstra navigate até uma posição e dig de um bloco com timeout e ritmo humanizado funcionando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-CONTEXT.md
@.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-RESEARCH.md
@.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-02-SUMMARY.md

<interfaces>
<!-- Contratos disponíveis após Planos 01 e 02 -->

De src/config.ts:
```typescript
export const config = {
  navigateTimeoutMs: number,  // 30000 por padrão
  digTimeoutMs: number,       // 10000 por padrão
  perceptionRadius: number,   // 32 por padrão
  // ...demais campos de conexão
} as const
```

De src/bot/connection.ts:
```typescript
export type BotReadyCallback = (bot: Bot) => void
export function createBot(onReady?: BotReadyCallback): void
// bot tem bot.pathfinder já carregado após spawn
// bot.pathfinder.goto(goal) retorna Promise<void>
// bot.dig(block) retorna Promise<void>
```

De mineflayer-pathfinder (API relevante):
```typescript
import { pathfinder, goals, Movements } from 'mineflayer-pathfinder'
// goals.GoalNear(x, y, z, range) — navegar perto de uma coordenada
// goals.GoalBlock(x, y, z) — navegar até um bloco exato
// goals.GoalXZ(x, z) — navegar ignorando Y
// bot.pathfinder.goto(goal): Promise<void> — PITFALL: pode não resolver se inatingível
// bot.pathfinder.stop() — cancela navegação em progresso
```

De mineflayer-collectblock (API relevante):
```typescript
// bot.collectBlock.collect(block, options): Promise<void>
// block: Block obtido via bot.findBlock({ matching: ..., maxDistance: ... })
// options.count — quantos blocos coletar
```

De zod v4 (API relevante):
```typescript
import { z } from 'zod'
// z.object({ ... }).describe('...')
// z.string().describe('...')
// z.number().min(1).max(10).default(2)
// z.union([...])
// z.enum(['a', 'b'])
// Schema.parse(data) — lança ZodError se inválido
// Schema.toJSONSchema() — built-in no Zod v4, sem biblioteca externa
// type Params = z.infer<typeof Schema>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar executor centralizado com timeout, watchdog e delay gaussiano</name>
  <read_first>
    - /root/MineMind/src/config.ts (timeouts: navigateTimeoutMs, digTimeoutMs)
    - /root/MineMind/.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-RESEARCH.md (Pattern 3: Executor Centralizado, Pitfall 2: pathfinder.goto nunca resolve)
    - /root/MineMind/.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-CONTEXT.md (D-13: executor centralizado com timeout + watchdog + gaussian delay)
  </read_first>
  <files>src/skills/executor.ts</files>
  <action>
**Contexto crítico (PITFALL 2 + D-13):**
- `pathfinder.goto()` pode nunca resolver se destino é inatingível — SEMPRE usar Promise.race com timeout
- Watchdog detecta ausência de movimento (posição que não muda) e interrompe a skill
- Delays gaussianos Box-Muller simulam timing humano — sem dependência externa

**Parâmetros (Claude's discretion):**
- Delay pré-ação: média 300ms, desvio-padrão 100ms
- Delay pós-ação: média 200ms, desvio-padrão 80ms
- Watchdog: polling a cada 2s, tolerância de 10s sem progresso

**src/skills/executor.ts**:

```typescript
// src/skills/executor.ts
// ACT-03: timeout e watchdog de progresso
// ACT-04: ritmo humanizado via distribuição gaussiana (Box-Muller)

/**
 * Gera um delay humanizado usando distribuição gaussiana (Box-Muller transform).
 * Sem dependência externa — 4 linhas de implementação.
 *
 * @param meanMs - Valor médio do delay em ms
 * @param stdDevMs - Desvio padrão do delay em ms
 * @returns Delay em ms (sempre >= 0)
 */
export function gaussianDelay(meanMs: number, stdDevMs: number): number {
  let u = 0, v = 0
  while (u === 0) u = Math.random()
  while (v === 0) v = Math.random()
  const normal = Math.sqrt(-2.0 * Math.log(u)) * Math.cos(2.0 * Math.PI * v)
  return Math.max(0, Math.round(meanMs + normal * stdDevMs))
}

/** Função que retorna um número que muda enquanto há progresso */
export type ProgressChecker = () => number

export interface ExecuteOptions {
  /** Timeout máximo em ms. Após expirar, a skill falha com SkillTimeoutError. */
  timeoutMs?: number
  /** Função que retorna valor numérico — deve mudar para indicar progresso */
  progressChecker?: ProgressChecker
  /** Intervalo de polling do watchdog em ms (padrão: 2000) */
  progressIntervalMs?: number
  /** Tempo máximo sem progresso antes de abortar em ms (padrão: 10000) */
  noProgressToleranceMs?: number
}

/** Erro lançado quando skill excede o timeout (ACT-03) */
export class SkillTimeoutError extends Error {
  constructor(timeoutMs: number) {
    super(`Skill timeout após ${timeoutMs}ms`)
    this.name = 'SkillTimeoutError'
  }
}

/** Erro lançado quando watchdog detecta ausência de progresso (ACT-03) */
export class SkillStuckError extends Error {
  constructor(toleranceMs: number) {
    super(`Skill sem progresso por ${toleranceMs}ms (bot preso)`)
    this.name = 'SkillStuckError'
  }
}

/**
 * Executa uma skill async com:
 * - Delay humanizado pré/pós ação (ACT-04)
 * - Timeout via Promise.race (ACT-03)
 * - Watchdog de progresso via polling (ACT-03)
 *
 * NUNCA tranca o loop — todos os caminhos resolvem ou rejeitam a Promise.
 *
 * @param action - Skill async a executar
 * @param opts - Opções de timeout e watchdog
 * @returns Promise que resolve com resultado da skill ou rejeita com SkillTimeoutError/SkillStuckError
 */
export async function executeWithSafety<T>(
  action: () => Promise<T>,
  opts: ExecuteOptions = {}
): Promise<T> {
  const {
    timeoutMs = 30_000,
    progressChecker,
    progressIntervalMs = 2_000,
    noProgressToleranceMs = 10_000,
  } = opts

  // ACT-04: delay humanizado ANTES da ação (média 300ms, stddev 100ms)
  await new Promise<void>((r) => setTimeout(r, gaussianDelay(300, 100)))

  // ACT-03: timeout — rejeita após timeoutMs
  const timeoutPromise = new Promise<never>((_, reject) => {
    const id = setTimeout(() => reject(new SkillTimeoutError(timeoutMs)), timeoutMs)
    // cleanup via AbortController não é necessário — Promise.race descarta as perdedoras
    return id
  })

  // ACT-03: watchdog de progresso — rejeita se valor não muda por noProgressToleranceMs
  let watchdogTimer: ReturnType<typeof setInterval> | undefined
  const watchdogPromise = new Promise<never>((_, reject) => {
    if (!progressChecker) return  // sem watchdog se não fornecido

    let lastValue = progressChecker()
    let lastProgressAt = Date.now()

    watchdogTimer = setInterval(() => {
      const current = progressChecker()
      if (current !== lastValue) {
        lastValue = current
        lastProgressAt = Date.now()
      } else if (Date.now() - lastProgressAt > noProgressToleranceMs) {
        reject(new SkillStuckError(noProgressToleranceMs))
      }
    }, progressIntervalMs)
  })

  try {
    const racers: Promise<T | never>[] = [action(), timeoutPromise]
    if (progressChecker) racers.push(watchdogPromise)

    const result = await Promise.race(racers)

    // ACT-04: delay humanizado APÓS a ação (média 200ms, stddev 80ms)
    await new Promise<void>((r) => setTimeout(r, gaussianDelay(200, 80)))

    return result
  } finally {
    // Limpar watchdog independentemente do resultado
    if (watchdogTimer !== undefined) clearInterval(watchdogTimer)
  }
}
```
  </action>
  <verify>
    <automated>cd /root/MineMind && test -f src/skills/executor.ts &amp;&amp; grep -q "export function executeWithSafety" src/skills/executor.ts &amp;&amp; echo "PASS: executeWithSafety ok" || echo "FAIL: executeWithSafety"; grep -q "export function gaussianDelay" src/skills/executor.ts &amp;&amp; echo "PASS: gaussianDelay ok" || echo "FAIL: gaussianDelay"; grep -q "SkillTimeoutError" src/skills/executor.ts &amp;&amp; echo "PASS: SkillTimeoutError ok" || echo "FAIL: SkillTimeoutError"; grep -q "SkillStuckError" src/skills/executor.ts &amp;&amp; echo "PASS: SkillStuckError ok" || echo "FAIL: SkillStuckError"; grep -q "clearInterval" src/skills/executor.ts &amp;&amp; echo "PASS: cleanup watchdog ok" || echo "FAIL: sem clearInterval"</automated>
  </verify>
  <acceptance_criteria>
    - src/skills/executor.ts exporta: `executeWithSafety`, `gaussianDelay`, `SkillTimeoutError`, `SkillStuckError`, `ExecuteOptions`, `ProgressChecker`
    - `executeWithSafety` usa `Promise.race([action(), timeoutPromise, watchdogPromise])` — nunca `await action()` diretamente sem race
    - `gaussianDelay(meanMs, stdDevMs)` usa Box-Muller transform e retorna `Math.max(0, ...)` (nunca negativo)
    - `SkillTimeoutError` e `SkillStuckError` são subclasses de `Error` com `this.name` configurado
    - `finally { clearInterval(watchdogTimer) }` presente para evitar timers orphaned
    - Delay pré-ação: `gaussianDelay(300, 100)` — delay pós-ação: `gaussianDelay(200, 80)` (valores da Claude's discretion)
    - `bun run typecheck` retorna exit 0 após criar este arquivo
  </acceptance_criteria>
  <done>executor.ts implementado com timeout, watchdog e delays gaussianos. Toda skill da Fase 1 usará este executor.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar skills navigate, dig (reais) e follow/attack (stubs) com schemas Zod</name>
  <read_first>
    - /root/MineMind/src/skills/executor.ts (executeWithSafety — acabado de criar)
    - /root/MineMind/src/config.ts (navigateTimeoutMs, digTimeoutMs, perceptionRadius)
    - /root/MineMind/.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-RESEARCH.md (Pattern 4: Skill com Schema Zod, Anti-Patterns, Don't Hand-Roll)
    - /root/MineMind/.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-CONTEXT.md (D-11, D-12, D-13)
  </read_first>
  <files>src/skills/navigate.ts, src/skills/dig.ts, src/skills/follow.ts, src/skills/attack.ts, src/skills/index.ts</files>
  <action>
**Contexto crítico (D-11, D-12, ACT-05):**
- Cada skill tem: (a) schema Zod com `.describe()` em todos os campos e (b) função TypeScript que usa `Schema.parse(params)` para validação
- Zod v4: usar `.toJSONSchema()` built-in (não `zodToJsonSchema` de biblioteca separada)
- Skills não tocam o objeto `bot` diretamente nas funções — passam pelo `executeWithSafety`
- navigate e dig são skills reais; follow e attack são stubs que lançam SkillNotImplementedError

**1. src/skills/navigate.ts** (ACT-01):

```typescript
// src/skills/navigate.ts
// ACT-01: navegação autônoma via mineflayer-pathfinder
// D-11: schema Zod implementado agora para Fase 3 consumir sem refatoração
import { z } from 'zod'
import type { Bot } from 'mineflayer'
import { goals } from 'mineflayer-pathfinder'
import { executeWithSafety } from './executor'
import { config } from '../config'

/** Schema Zod do skill navigate (D-11) — consumido pelo LangGraph na Fase 3 */
export const NavigateSchema = z.object({
  target: z.union([
    z.object({
      x: z.number().describe('Coordenada X absoluta'),
      y: z.number().describe('Coordenada Y absoluta'),
      z: z.number().describe('Coordenada Z absoluta'),
    }).describe('Coordenadas absolutas no mundo'),
    z.string().max(64).describe('Nome do tipo de bloco para navegar até o mais próximo (ex: "oak_log", "diamond_ore")'),
  ]).describe('Destino de navegação: coordenadas XYZ ou nome de tipo de bloco'),
  range: z.number().min(1).max(10).default(2)
    .describe('Distância tolerada do alvo em blocos (1–10, padrão 2)'),
})

export type NavigateParams = z.infer<typeof NavigateSchema>

/**
 * Navega até uma posição XYZ ou até o bloco do tipo especificado mais próximo.
 * ACT-01: usa mineflayer-pathfinder com timeout e watchdog via executor centralizado.
 * PITFALL 2: pathfinder.goto sem timeout pode travar — executor resolve isso.
 *
 * @param bot - Instância do bot (deve ter pathfinder carregado após spawn)
 * @param rawParams - Parâmetros não validados (validados via Zod antes de usar)
 */
export async function navigate(bot: Bot, rawParams: unknown): Promise<void> {
  const { target, range } = NavigateSchema.parse(rawParams)

  let goal: goals.Goal

  if (typeof target === 'string') {
    // Navegar até o bloco do tipo especificado mais próximo
    const block = bot.findBlock({
      matching: (b) => b.name === target,
      maxDistance: config.perceptionRadius * 2,  // busca no dobro do raio de percepção
    })
    if (!block) {
      throw new Error(`Bloco do tipo '${target}' não encontrado no raio de ${config.perceptionRadius * 2} blocos`)
    }
    goal = new goals.GoalNear(block.position.x, block.position.y, block.position.z, range)
  } else {
    goal = new goals.GoalNear(target.x, target.y, target.z, range)
  }

  // ACT-03: executor centralizado aplica timeout de 30s e watchdog de posição
  await executeWithSafety(
    () => bot.pathfinder.goto(goal),
    {
      timeoutMs: config.navigateTimeoutMs,  // 30000ms padrão
      // Watchdog: posição que não muda indica bot preso
      progressChecker: () => {
        const pos = bot.entity.position
        return Math.round(pos.x * 10) + Math.round(pos.y * 10) * 1000 + Math.round(pos.z * 10) * 1_000_000
      },
      progressIntervalMs: 2_000,
      noProgressToleranceMs: 10_000,
    }
  )
}

/** Tool descriptor para LangGraph Fase 3 (D-11) */
export const navigateTool = {
  name: 'navigate',
  description: 'Navega até uma posição XYZ absoluta ou até o bloco do tipo especificado mais próximo no mundo',
  schema: NavigateSchema,
  execute: navigate,
} as const
```

**2. src/skills/dig.ts** (ACT-02):

```typescript
// src/skills/dig.ts
// ACT-02: mineração/coleta de bloco-alvo via mineflayer-collectblock
import { z } from 'zod'
import type { Bot } from 'mineflayer'
import { executeWithSafety } from './executor'
import { config } from '../config'

export const DigSchema = z.object({
  target: z.union([
    z.object({
      x: z.number().describe('Coordenada X do bloco'),
      y: z.number().describe('Coordenada Y do bloco'),
      z: z.number().describe('Coordenada Z do bloco'),
    }).describe('Posição absoluta do bloco a minerar'),
    z.string().max(64).describe('Tipo de bloco a coletar (ex: "oak_log", "coal_ore") — coleta o mais próximo'),
  ]).describe('Alvo de mineração: posição específica ou tipo de bloco'),
  count: z.number().int().min(1).max(64).default(1)
    .describe('Quantidade de blocos a coletar (1–64, padrão 1)'),
})

export type DigParams = z.infer<typeof DigSchema>

/**
 * Minera/coleta um bloco-alvo.
 * ACT-02: usa mineflayer-collectblock para encontrar, navegar, equipar ferramenta e minerar.
 * ACT-03: executor centralizado aplica timeout de 10s e watchdog de inventário.
 */
export async function dig(bot: Bot, rawParams: unknown): Promise<void> {
  const { target, count } = DigSchema.parse(rawParams)

  if (typeof target === 'string') {
    // Coletar bloco pelo nome usando mineflayer-collectblock
    const block = bot.findBlock({
      matching: (b) => b.name === target,
      maxDistance: config.perceptionRadius,
    })
    if (!block) {
      throw new Error(`Bloco do tipo '${target}' não encontrado no raio de ${config.perceptionRadius} blocos`)
    }

    const inventoryCountBefore = bot.inventory.items().reduce((sum, item) => sum + item.count, 0)

    await executeWithSafety(
      () => bot.collectBlock.collect(block, { count }),
      {
        timeoutMs: config.digTimeoutMs * count,  // timeout proporcional à quantidade
        // Watchdog: inventário que cresce indica progresso
        progressChecker: () => bot.inventory.items().reduce((sum, item) => sum + item.count, 0),
        progressIntervalMs: 2_000,
        noProgressToleranceMs: config.digTimeoutMs,
      }
    )

    const inventoryCountAfter = bot.inventory.items().reduce((sum, item) => sum + item.count, 0)
    const collected = inventoryCountAfter - inventoryCountBefore
    console.log(`[MineMind/dig] Coletou ${collected} item(s) de '${target}'`)
  } else {
    // Minerar bloco em posição específica
    const blockPos = bot.world.getBlock(bot.entity.position.offset(
      target.x - Math.floor(bot.entity.position.x),
      target.y - Math.floor(bot.entity.position.y),
      target.z - Math.floor(bot.entity.position.z)
    ))
    // Fallback direto via blockAt para coordenadas absolutas
    const blockAtPos = bot.blockAt({ x: target.x, y: target.y, z: target.z } as any)
    if (!blockAtPos || blockAtPos.type === 0) {
      throw new Error(`Nenhum bloco em (${target.x}, ${target.y}, ${target.z}) ou é ar`)
    }

    await executeWithSafety(
      () => bot.dig(blockAtPos),
      {
        timeoutMs: config.digTimeoutMs,
        progressIntervalMs: 1_000,
        noProgressToleranceMs: config.digTimeoutMs,
      }
    )
  }
}

export const digTool = {
  name: 'dig',
  description: 'Minera ou coleta um bloco. Pode receber posição absoluta XYZ ou nome do tipo de bloco para coletar o mais próximo.',
  schema: DigSchema,
  execute: dig,
} as const
```

**3. src/skills/follow.ts** (stub — D-12):

```typescript
// src/skills/follow.ts
// D-12: stub — Fase 1 não implementa follow real (alta superfície de falha)
import { z } from 'zod'
import type { Bot } from 'mineflayer'

export const FollowSchema = z.object({
  entityName: z.string().max(64).describe('Nome do jogador ou entidade a seguir'),
  maxDistance: z.number().min(1).max(32).default(3)
    .describe('Distância máxima a manter do alvo em blocos'),
})

export type FollowParams = z.infer<typeof FollowSchema>

/** Stub — implementação real na Fase 2+ */
export async function follow(_bot: Bot, rawParams: unknown): Promise<void> {
  FollowSchema.parse(rawParams)  // valida params mesmo como stub
  throw new Error('Skill follow não implementada na Fase 1 (stub). Será implementada na Fase 2.')
}

export const followTool = {
  name: 'follow',
  description: '[STUB] Segue um jogador ou entidade mantendo distância configurada. Não implementado na Fase 1.',
  schema: FollowSchema,
  execute: follow,
} as const
```

**4. src/skills/attack.ts** (stub — D-12):

```typescript
// src/skills/attack.ts
// D-12: stub — Fase 1 não implementa combate (alta superfície de falha)
import { z } from 'zod'
import type { Bot } from 'mineflayer'

export const AttackSchema = z.object({
  entityName: z.string().max(64).describe('Nome do jogador ou mob a atacar'),
})

export type AttackParams = z.infer<typeof AttackSchema>

/** Stub — implementação real na Fase 2+ */
export async function attack(_bot: Bot, rawParams: unknown): Promise<void> {
  AttackSchema.parse(rawParams)  // valida params mesmo como stub
  throw new Error('Skill attack não implementada na Fase 1 (stub). Será implementada em fase futura.')
}

export const attackTool = {
  name: 'attack',
  description: '[STUB] Ataca um jogador ou mob. Não implementado na Fase 1.',
  schema: AttackSchema,
  execute: attack,
} as const
```

**5. src/skills/index.ts** — registry de skills (ACT-05):

```typescript
// src/skills/index.ts
// Registry centralizado de skills — exposto para o loop cognitivo (Fase 2+)
// D-11: skills acessíveis tanto como funções quanto como tool descriptors com schemas Zod
import type { Bot } from 'mineflayer'
import { navigate, NavigateSchema, navigateTool } from './navigate'
import { dig, DigSchema, digTool } from './dig'
import { follow, FollowSchema, followTool } from './follow'
import { attack, AttackSchema, attackTool } from './attack'

// Re-exportar individualmente para uso direto
export { navigate, NavigateSchema, navigateTool } from './navigate'
export { dig, DigSchema, digTool } from './dig'
export { follow, FollowSchema, followTool } from './follow'
export { attack, AttackSchema, attackTool } from './attack'
export { executeWithSafety, gaussianDelay, SkillTimeoutError, SkillStuckError } from './executor'

/** Função de skill genérica */
export type SkillFunction = (bot: Bot, params: unknown) => Promise<void>

/** Descriptor de uma skill com schema Zod e função de execução */
export interface SkillTool {
  readonly name: string
  readonly description: string
  readonly schema: { parse: (data: unknown) => unknown; toJSONSchema: () => object }
  readonly execute: SkillFunction
}

/**
 * Registry de skills como funções TypeScript.
 * Fase 2 usa para executar skills por nome no loop cognitivo.
 */
export const skillRegistry: Record<string, SkillFunction> = {
  navigate,
  dig,
  follow,
  attack,
}

/**
 * Registry de tool descriptors para o LangGraph (Fase 3).
 * Contém schemas Zod prontos para `zodToJsonSchema()` ou `.toJSONSchema()` (Zod v4).
 * D-11: Fase 3 consome isso sem nenhuma refatoração.
 */
export const toolRegistry: SkillTool[] = [
  navigateTool,
  digTool,
  followTool,
  attackTool,
]
```

**Atualizar src/bot/index.ts** para demonstrar as skills após spawn (adicionar ao `onBotReady`):

Adicionar ao final de `onBotReady(bot)` em src/bot/index.ts:
```typescript
  // Demonstração de skills (Fase 1 — remover na Fase 2 quando o loop cognitivo assumir)
  // Não chamar skills em prod sem um servidor ativo — apenas para teste local
  const { skillRegistry } = await import('../skills/index')
  console.log('[MineMind] Skills registradas:', Object.keys(skillRegistry).join(', '))
  console.log('[MineMind] Fase 1 completa — conexão, percepção e skills prontas.')
```
  </action>
  <verify>
    <automated>cd /root/MineMind && test -f src/skills/navigate.ts &amp;&amp; grep -q "export const NavigateSchema" src/skills/navigate.ts &amp;&amp; echo "PASS: NavigateSchema ok" || echo "FAIL: NavigateSchema"; test -f src/skills/dig.ts &amp;&amp; grep -q "export const DigSchema" src/skills/dig.ts &amp;&amp; echo "PASS: DigSchema ok" || echo "FAIL: DigSchema"; grep -q "executeWithSafety" src/skills/navigate.ts &amp;&amp; echo "PASS: executor usado em navigate" || echo "FAIL: navigate sem executor"; grep -q "executeWithSafety" src/skills/dig.ts &amp;&amp; echo "PASS: executor usado em dig" || echo "FAIL: dig sem executor"; test -f src/skills/index.ts &amp;&amp; grep -q "export const skillRegistry" src/skills/index.ts &amp;&amp; echo "PASS: skillRegistry ok" || echo "FAIL: skillRegistry"; grep -q "export const toolRegistry" src/skills/index.ts &amp;&amp; echo "PASS: toolRegistry ok" || echo "FAIL: toolRegistry"</automated>
  </verify>
  <acceptance_criteria>
    - src/skills/navigate.ts exporta: `NavigateSchema`, `NavigateParams`, `navigate`, `navigateTool`
    - NavigateSchema aceita target como `{x,y,z}` OU `string` (z.union) com `.describe()` em todos os campos
    - navigate() chama `executeWithSafety(...)` com `timeoutMs: config.navigateTimeoutMs` e progressChecker baseado em posição
    - src/skills/dig.ts exporta: `DigSchema`, `DigParams`, `dig`, `digTool`
    - dig() chama `executeWithSafety(...)` com `timeoutMs: config.digTimeoutMs * count`
    - src/skills/follow.ts e src/skills/attack.ts exportam schemas Zod e lançam Error com mensagem de stub (não retornam void silenciosamente)
    - src/skills/index.ts exporta `skillRegistry` (Record&lt;string, SkillFunction&gt;) com chaves: navigate, dig, follow, attack
    - src/skills/index.ts exporta `toolRegistry` (SkillTool[]) com 4 entries
    - Nenhuma skill acessa `bot.pathfinder`, `bot.dig`, ou `bot.collectBlock` sem passar por `executeWithSafety` (exceto dig() direto que usa bot.dig internamente mas envolto em executeWithSafety)
    - `bun run typecheck` retorna exit 0 com todos os 6 arquivos novos
  </acceptance_criteria>
  <done>4 skills implementadas (navigate e dig reais, follow e attack stubs), todas com schemas Zod e passando pelo executor centralizado. skillRegistry e toolRegistry prontos para Fase 2 e 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Smoke test integral — conectar, perceber e navegar</name>
  <what-built>
    Plano 03 completo: executor com timeout+watchdog+gaussian, 4 skills (navigate real, dig real, follow stub, attack stub), todos com schemas Zod. O agente tem conexão, percepção e motor de ação funcionando.
  </what-built>
  <how-to-verify>
    **Pré-requisito:** Um servidor Minecraft Java 1.21.4 rodando localmente em offline-mode na porta 25565. Se não tiver, consulte o RESEARCH.md seção "Configuração do servidor Minecraft Vanilla offline-mode".

    **Passo 1 — TypeCheck:**
    ```
    cd /root/MineMind && bun run typecheck
    ```
    Esperado: saída sem erros, exit 0.

    **Passo 2 — Iniciar o agente:**
    ```
    cd /root/MineMind && bun run start
    ```
    Esperado (com servidor ativo):
    - `[MineMind] Iniciando...`
    - `[MineMind] Online — localhost:25565 | HP: 20 | Pos: X,Y,Z`
    - `[MineMind] Snapshot inicial capturado:`
    - `  Status: HP 20 | Food 20 | Dia: true/false`
    - `[MineMind] Snapshot imutável confirmado (Object.freeze funcionando).`
    - `[MineMind] Skills registradas: navigate, dig, follow, attack`
    - `[MineMind] Fase 1 completa — conexão, percepção e skills prontas.`

    **Passo 3 — Testar reconexão (CONN-02):**
    Com o bot rodando, parar o servidor Minecraft (Ctrl+C no servidor). Esperado:
    - `[MineMind] Desconectado: "...". Reconectando em 5s...`
    Reiniciar o servidor. Esperado (após ~5s):
    - `[MineMind] Online — ...` (bot reconectou e capturou novo snapshot)

    **Passo 4 — Verificar Bun compatibility (D-01):**
    Confirmar que o agente rodou com Bun (não com Node):
    ```
    which bun && bun --version
    ```
    Se houve erro NAPI durante execução (não durante install), verificar se existe `NAPI_ERROR.txt`.
  </how-to-verify>
  <resume-signal>Digite "aprovado" se o smoke test passou, ou descreva o problema encontrado (ex: "erro NAPI: napi_register_module_v1", "pathfinder não carregou", "reconexão não funcionou")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chamador → executeWithSafety | Parâmetros `rawParams` não validados entram nas skills |
| Bot → Servidor MC | Comandos de navegação/mineração enviados ao servidor podem ser bloqueados ou limitados |
| Schema Zod → execute() | Dados parsed pelo Zod são confiáveis para uso; dados antes do parse são untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | navigate() — target do tipo string (nome de bloco) | mitigate | `z.string().max(64)` no NavigateSchema limita tamanho; bloco só é buscado com `bot.findBlock()` que rejeita nomes inválidos retornando null. Erro descritivo lançado se não encontrado. |
| T-03-02 | Denial of Service | executeWithSafety — ação que nunca resolve (PITFALL 2) | mitigate | `Promise.race` com `SkillTimeoutError` garante que o executor sempre resolve ou rejeita em no máximo `timeoutMs`. Watchdog cobre o caso de pathfinder stuck. |
| T-03-03 | Tampering | DigSchema — count até 64 blocos | mitigate | `z.number().int().min(1).max(64)` limita quantidade via Zod parse antes de qualquer ação. Valores fora do range lançam ZodError imediatamente. |
| T-03-04 | Elevation of Privilege | skillRegistry — execute acessa objeto bot diretamente | accept | Em Fase 1, bot é passado diretamente às skills (aceitável — sem LLM na cadeia). Na Fase 3, o LangGraph mediará o acesso: o LLM nunca recebe o objeto bot, apenas os params do schema Zod. Documentado em D-13. |
| T-03-05 | Denial of Service | dig() com count multiplicando timeout | mitigate | `timeoutMs: config.digTimeoutMs * count` — count já limitado a 64 por Zod; timeout máximo = 10000 × 64 = 640s. Aceitável para coleta em lote em ambiente dev local. |
</threat_model>

<verification>
Verificação geral do Plano 03:

1. Todos os schemas Zod presentes: `grep -l "export const.*Schema" src/skills/*.ts` → deve listar navigate.ts, dig.ts, follow.ts, attack.ts
2. Executor usado em todas as skills reais: `grep -l "executeWithSafety" src/skills/*.ts` → deve incluir navigate.ts e dig.ts
3. Stubs lançam erro: `grep "throw new Error" src/skills/follow.ts src/skills/attack.ts` → presente em ambos
4. Registry completo: `grep "navigate\|dig\|follow\|attack" src/skills/index.ts` → todos 4 presentes
5. TypeCheck final: `bun run typecheck` → exit 0
6. Zod v4 built-in usado: ausência de `zodToJsonSchema` em imports: `grep -r "zodToJsonSchema" src/` → vazio
</verification>

<success_criteria>
- `bun run typecheck` retorna exit 0 (projeto inteiro sem erros de tipo)
- navigate(), dig() passam pelo executeWithSafety com timeout configurado
- NavigateSchema.toJSONSchema() pode ser chamado sem erro (Zod v4 built-in — preparação para Fase 3)
- skillRegistry tem 4 entradas, toolRegistry tem 4 entries
- follow() e attack() lançam Error com mensagem de stub (não retornam silenciosamente)
- Checkpoint humano aprovado: bot conectou, capturou snapshot, demonstrou skills registradas
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-presen-a-e-conex-o-funda-o-sem-llm/01-03-SUMMARY.md` com:
- Resultado do smoke test (Bun funcionou? Node necessário como fallback?)
- Skills implementadas e resultado de navigate/dig se testados
- Resultado do checkpoint humano
- Qualquer desvio das decisões D-11 a D-13
- Nota sobre compatibilidade Bun↔Mineflayer (resolver blocker de STATE.md)
</output>
