---
phase: quick
plan: 260621-ir4
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cognition/loop.ts
  - src/cognition/loop.reflect-priority.test.ts
autonomous: true
requirements: [REFL-01, D-10, D-12]

must_haves:
  truths:
    - "Quando shouldReflect=true e nada está in-flight, a reflexão dispara naquele tick"
    - "Quando a reflexão é escolhida no tick, a deliberação de AÇÃO NÃO dispara (não rouba o lock antes)"
    - "Quando shouldReflect=false, o caminho de AÇÃO normal continua disparando"
    - "Quando algo já está in-flight, nem ação nem reflexão são re-disparadas (single-flight preservado, D-12)"
    - "lastReflectionAt/importanceAccum só rearmam quando a reflexão DE FATO executou (ran === true, B1)"
  artifacts:
    - path: "src/cognition/loop.ts"
      provides: "pickDispatch puro + reordenação do dispatch no driver (reflect tem prioridade)"
      contains: "export function pickDispatch"
    - path: "src/cognition/loop.reflect-priority.test.ts"
      provides: "Teste de regressão que falha com o bug atual (ação rouba o lock da reflexão)"
      contains: "pickDispatch"
  key_links:
    - from: "src/cognition/loop.ts (driver while)"
      to: "pickDispatch"
      via: "decisão de qual maybeDeliberate disparar no tick"
      pattern: "pickDispatch\\("
    - from: "src/cognition/loop.reflect-priority.test.ts"
      to: "pickDispatch"
      via: "import + assert reflect>action"
      pattern: "import.*pickDispatch"
---

<objective>
Consertar a starvation da reflexão no driver do loop cognitivo. Hoje, todo tick, a deliberação de AÇÃO é disparada PRIMEIRO e seta `state.inFlight = true` sincronamente (deliberation.ts:155). Quando o gatilho de REFLEXÃO checa `if (state.inFlight) return false` (deliberation.ts:148) no mesmo tick, sempre faz no-op — a reflexão NUNCA roda em produção (evidência ao vivo: importância acumulada 85 vs limiar 50, 0 eventos `type='reflection'` no banco, `[reflect]` nunca apareceu no log).

Purpose: restaurar a cadência da reflexão (REFL-01/D-10) sem violar o single-flight (D-12 — uma inferência por vez). Como a reflexão é rara (acúmulo de 50 OU 10 min OU idle+goal-done), ceder o tick da ação a ela quando ela é devida é barato.

Output: helper puro `pickDispatch` que decide `reflect | action | none` por tick (reflect tem prioridade); driver reordenado para usá-lo; teste de regressão que falha com o bug atual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Arquivos centrais do fix
@src/cognition/loop.ts
@src/cognition/deliberation.ts
@src/cognition/reflection.ts

# Convenções de teste a espelhar (bun test, provider mock, holder helper)
@src/cognition/deliberation.test.ts

<interfaces>
<!-- Contratos que o executor usa diretamente — sem explorar o codebase. -->

De src/cognition/reflection.ts:
```typescript
export function shouldReflect(args: {
  enteredIdle: boolean
  goalDoneOrFailed: boolean
  importanceAccum: number
  lastReflectionAt: number
  now: number
}): boolean
export interface ReflectionState { lastReflectionAt: number; importanceAccum: number }
```

De src/cognition/deliberation.ts:
```typescript
export interface DeliberationState { inFlight: boolean; lastRunAt: number }
// maybeDeliberate retorna Promise<boolean> — true só quando o trabalho de fato executou (B1).
// O lock single-flight (inFlight) é interno e está CORRETO — NÃO mudar.
```

Driver atual (src/cognition/loop.ts), trecho a reordenar — dentro de `if (lastSnapshot)` (~L371-419):
- L373: `void deliberator.maybeDeliberate(..., trigger, ...)`  ← AÇÃO dispara PRIMEIRO (rouba o lock)
- L382-388: acúmulo de importância (PRESERVAR — alimenta reflState.importanceAccum)
- L395: `const enteredIdle = result.enteredIdle === true` (PRESERVAR — sinal real do grafo)
- L396-419: gatilho de reflexão que SEMPRE faz no-op porque a ação tomou o lock
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extrair pickDispatch puro e reordenar o driver para dar prioridade ao reflect</name>
  <files>src/cognition/loop.ts</files>
  <behavior>
    Comportamento de `pickDispatch({ inFlight, reflectDue })`:
    - inFlight=true  → 'none'   (single-flight preservado, D-12 — nem ação nem reflexão re-disparam)
    - inFlight=false, reflectDue=true  → 'reflect'  (reflexão tem PRIORIDADE no tick)
    - inFlight=false, reflectDue=false → 'action'   (caminho de ação normal)
  </behavior>
  <action>
Em src/cognition/loop.ts:

1) Adicionar um helper PURO exportado (ao lado de pickTrigger, no fim do arquivo):

```typescript
/** Dispatch a disparar neste tick (D-12 single-flight + D-10 prioridade do reflect). */
export type Dispatch = 'reflect' | 'action' | 'none'

/**
 * Decide o ÚNICO dispatch do tick. A reflexão tem PRIORIDADE sobre a ação quando devida e o
 * lock está livre — corrige a starvation (a ação tomava o lock sincronamente todo tick, deixando
 * a reflexão faminta para sempre). Single-flight (D-12) preservado: in-flight ⇒ 'none'.
 */
export function pickDispatch(args: { inFlight: boolean; reflectDue: boolean }): Dispatch {
  if (args.inFlight) return 'none'
  return args.reflectDue ? 'reflect' : 'action'
}
```

2) Reordenar o bloco `if (lastSnapshot)` do driver (atual ~L371-419). A ORDEM nova:
   a) PRIMEIRO acumular a importância dos eventos novos (mover o bloco L382-388 para ANTES de qualquer dispatch — o acúmulo NÃO depende de dispatch e precisa estar atualizado antes de avaliar shouldReflect). PRESERVAR a lógica de seenEvents/slice/importanceOf intacta.
   b) Ler `enteredIdle` do sinal real do grafo (PRESERVAR: `const enteredIdle = result.enteredIdle === true`).
   c) Computar `reflectDue` via shouldReflect (mesmos args de hoje: enteredIdle, goalDoneOrFailed: holder.currentGoal == null, importanceAccum: reflState.importanceAccum, lastReflectionAt: reflState.lastReflectionAt, now: reflectNow).
   d) `const dispatch = pickDispatch({ inFlight: deliberator.state.inFlight, reflectDue })`.
   e) Despachar conforme:
      - `dispatch === 'reflect'`: disparar SÓ a reflexão (NÃO a ação). Manter o padrão `void ... .then((ran) => { if (ran) { reflState.lastReflectionAt = reflectNow; reflState.importanceAccum = 0; console.log('[reflect] reflexão executada') } })` (B1 — só rearma quando ran===true). Manter o argumento `chroma`.
      - `dispatch === 'action'`: disparar SÓ a ação — `const trigger = pickTrigger(holder); void deliberator.maybeDeliberate(deliberator.state, holder, provider, lastSnapshot, trigger, Date.now())`.
      - `dispatch === 'none'`: no-op neste tick (algo in-flight; o próximo tick reavalia).

CRÍTICO — restrições do problema:
- NÃO alterar deliberation.ts nem a semântica do lock single-flight (o lock está correto; o bug é a ORDEM no driver).
- A ação e a reflexão são MUTUAMENTE EXCLUSIVAS por tick agora (uma ou outra, nunca as duas) — é isso que mata a starvation e respeita D-12.
- Reflexão continua disparada com `void`/`.then(...)` (não bloqueia o tick).
- PRESERVAR o comentário B1 explicando por que só rearma com ran===true.
- Atualizar o comentário antigo (L405-409) que dizia "tenta no próximo tick" — agora a reflexão ganha o tick imediatamente quando devida.
  </action>
  <verify>
    <automated>cd c:/Users/biel1/OneDrive/Documentos/GitHub/MineMind && bunx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>pickDispatch exportado e puro; driver reordenado para acumular importância antes do dispatch e disparar reflect OU action (nunca ambas) por tick; chroma e a lógica B1 (ran===true) preservados; tsc sem erros.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Teste de regressão — reflect tem prioridade e a ação não rouba o lock</name>
  <files>src/cognition/loop.reflect-priority.test.ts</files>
  <behavior>
    - Test A (prova o bug): pickDispatch({ inFlight:false, reflectDue:true }) === 'reflect' (NÃO 'action').
    - Test B: pickDispatch({ inFlight:false, reflectDue:false }) === 'action'.
    - Test C: pickDispatch({ inFlight:true, reflectDue:true }) === 'none' (single-flight D-12).
    - Test D (integração leve com shouldReflect): com importanceAccum >= reflectionImportanceThreshold
      (o cenário ao vivo: acúmulo 85 > limiar 50) e inFlight=false, o dispatch resolve para 'reflect'.
      ANTES do fix esse caminho dava 'action' porque a ação tomava o lock primeiro — este teste
      falharia contra o driver antigo.
  </behavior>
  <action>
Criar src/cognition/loop.reflect-priority.test.ts espelhando o estilo de deliberation.test.ts (bun:test, sem rede):

```typescript
// src/cognition/loop.reflect-priority.test.ts
// IR4: regressão da starvation da reflexão. pickDispatch dá PRIORIDADE ao reflect quando devido
// e o lock está livre, preservando o single-flight (D-12). Antes do fix a ação tomava o lock
// sincronamente todo tick e a reflexão nunca rodava (events type='reflection' = 0 ao vivo).
import { test, expect } from 'bun:test'
import { pickDispatch } from './loop'
import { shouldReflect } from './reflection'
import { config } from '../config'

test('pickDispatch: reflect tem prioridade quando devido e nada in-flight (mata a starvation)', () => {
  expect(pickDispatch({ inFlight: false, reflectDue: true })).toBe('reflect')
})

test('pickDispatch: cai na ação quando reflect não é devido', () => {
  expect(pickDispatch({ inFlight: false, reflectDue: false })).toBe('action')
})

test('pickDispatch: in-flight ⇒ none (single-flight D-12 preservado)', () => {
  expect(pickDispatch({ inFlight: true, reflectDue: true })).toBe('none')
  expect(pickDispatch({ inFlight: true, reflectDue: false })).toBe('none')
})

test('cenário ao vivo: acúmulo > limiar com lock livre ⇒ reflect (não action)', () => {
  // Reproduz o estado do minemind.sqlite ao vivo: importância 85 vs limiar 50.
  const reflectDue = shouldReflect({
    enteredIdle: false,
    goalDoneOrFailed: false,
    importanceAccum: config.reflectionImportanceThreshold + 35,
    lastReflectionAt: 0,
    now: 1, // dentro do piso temporal → só o acúmulo decide
  })
  expect(reflectDue).toBe(true)
  // Com o lock livre, o dispatch DEVE ser reflect — antes do fix a ação tomava o lock e isto seria 'action'.
  expect(pickDispatch({ inFlight: false, reflectDue })).toBe('reflect')
})
```
  </action>
  <verify>
    <automated>cd c:/Users/biel1/OneDrive/Documentos/GitHub/MineMind && bun test src/cognition/loop.reflect-priority.test.ts</automated>
  </verify>
  <done>4 testes passam; o teste do cenário ao vivo (acúmulo > limiar ⇒ reflect) prova a prioridade que antes não existia.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit` sem erros (fix não quebra tipos).
- `bun test src/cognition/loop.reflect-priority.test.ts` verde.
- `bun test src/cognition/deliberation.test.ts src/cognition/reflection.test.ts` ainda verdes (semântica do lock e da reflexão intactas).
- Revisão manual: no driver, ação e reflexão são mutuamente exclusivas por tick; importância é acumulada ANTES do dispatch; B1 (ran===true) e chroma preservados.
</verification>

<success_criteria>
- pickDispatch puro e exportado decide um único dispatch por tick com prioridade ao reflect.
- Driver reordenado: quando shouldReflect=true e lock livre, a reflexão dispara e a ação NÃO dispara naquele tick (lock não roubado).
- shouldReflect=false → caminho de ação inalterado.
- Single-flight (D-12) preservado: in-flight ⇒ nenhum re-disparo.
- B1 preservado: lastReflectionAt/importanceAccum só rearmam com ran===true.
- deliberation.ts NÃO modificado.
- Teste de regressão prova a correção (falharia contra a ordem antiga).
</success_criteria>

<output>
After completion, create `.planning/quick/260621-ir4-consertar-starvation-da-reflexao-no-loop/260621-ir4-SUMMARY.md`
</output>
